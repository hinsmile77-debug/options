# NEXT_TODO — 다음 할 일 목록

_완료 항목은 삭제하거나 SESSION_LOG로 이관_

---

## [MW0601] 2026-08-18 — 08-17 착수분 **첫 실가동 판정 완료**. 남은 결정은 하나다

> 상세: `docs/동작점검/2026-08-18_점검_intra.md` · `hypotheses.yaml` `2026-08-17-p1~p4`(판정 확정).

### 판정

| 가설 | 예측 | 실측 | 판정 |
|---|---|---|---|
| p1 후보 비율 | >= 90% | **19.5%** | **반증** |
| p2 당일만기 제외 | 0 | 0 | 확인(오늘이 실제 만기일) |
| p3 주문 0건 | 0 | 0 (기록 293분) | 확인 |
| p4 사이클 비용 | 0 | 0 | 확인 |

단축코드 해결률 **100%**, `exec_mode` 293행 전부 ADVISORY, 선택기 기록 **NULL 0건**.

### 검증 캠페인 제도 도입 — 검토·계획 완료 (2026-08-18)

> `docs/Dev_md/VALIDATION_CAMPAIGN_도입검토_및_구현계획_2026-08-18.md`

미륵이(futures)가 상시 운영 중인 **검증 캠페인**을 마흐디에 도입한다. 아래 «남은 결정 하나»가
지금 **살 곳이 없다**는 것이 도입 근거다 — `hypotheses.yaml`은 단일 검증예정일 구조라 표본이
며칠~몇 주 걸리는 질문을 담지 못하고, `NEXT_TODO` 체크박스는 아무도 표본을 세지 않는다.

가져오는 것은 코드가 아니라 제도 셋이다: **① `INSUFFICIENT`를 일급 상태로**(표본 미달과 반증을
가른다) **② 판정과 결정의 분리**(일부러 안 한 것을 코드가 기억한다) **③ 상시 운영**(기한 없음).

**누적 인프라는 이미 있다** — 일별 지표가 `docs/동작점검/auto/*_지표.json`에 커밋 위치로 매일
쌓이므로 새 폴러·테이블·스케줄러가 필요 없다. 코드 ~400줄 규모.

- [x] **Phase 1 완료(2026-08-18)** — `docs/동작점검/validation_campaign.yaml` +
      `mahdi/ops/campaign.py` + 일일 리포트 **§0-2 절** + `tests/test_ops_campaign.py` 25건.
      채널 2개 등재(C1 `strangle-delta-band-reach` · C2 `symbol-resolution-rate`), 둘 다 개시일
      2026-08-18. **오늘 EOD부터 표본이 쌓인다.**
      - 캠페인은 `auto/*_지표.json`만 읽는다 — DB·폴러·스케줄러 추가 없음.
      - EOD 체인에서 **json 기록 뒤에** 평가한다(앞에 두면 오늘치가 누적에서 빠진다).
      - 판정 우선순위: 선행 대기 → 경로 없음 → **표본 미달** → 관측/합격/불합격.
        `표본 미달`이 `불합격`보다 **먼저** 온다는 것이 이 모듈의 존재 이유다.
      - 규약 F/G의 캠페인판을 기계로 강제한다: 주장 규칙은 `비율`/`관측`만(누적 건수 임계 금지),
        시장 상태 의존 지표는 `관측`으로만.
      - ⚠ C1의 임계 0.30은 **08-18 하루치를 본 뒤에 정한 값**이고 그 사실을 채널 주석에 적었다.
- [ ] **Phase 2** — 결정 레지스트리 + 📌 마커 + 근거지표 무효화 감지.
- [ ] **Phase 3** — 채널 종결·이관 규약 + 표본 기아 경보. **지금 설계하지 않는다.**

---

### ⚠ 남은 결정 하나 — 사람이 골라야 한다 (하루치로 정하지 말 것)

`small_strangle_buy`가 요구하는 |델타| 0.20~0.30이 **구독 창(ATM±N) 밖**이라 163건이 실패했다
(08-20 북 분당 4.5레그, 풋 밴드 보유율 **6.2%**). 셋 중 하나다:

- [ ] (a) 구독 창을 넓힌다 — REST 예산이 이미 빡빡하다(예산 컷 71건). **비용이 실재한다.**
- [ ] (b) 그 전략의 밴드를 관측 가능 범위로 본다 — **그러면 그 숫자가 곧 전략의 정의가 된다.**
- [ ] (c) 현재 데이터로 계측 불가임을 기록하고 둔다.

### 이번에 실린 것

| # | 무엇 | 되돌리면 깨지는 테스트 |
|---|---|---|
| 1 | `unknown_side_count` NULL 가드 — 판단 4분을 날린 그 버그 | `test_data_db.py` 3건 |
| 2 | 마이그레이션 **032** `option_analysis_1m.price` + 수집·선택·실행 배선 | `test_main.py` 4건 외 |
| 3 | 필드 오독 가드(`price`가 기초자산 수준이면 경고) | `test_main.py` 2건 |

**마이그레이션 032는 라이브에 이미 적용했다**(재기동 시 INSERT 실패를 막기 위해 코드보다 먼저).

### 옵션 가격 필드 — 확정됐다

`futs_prpr`가 **그 옵션의 프리미엄**이다. 응답에 `optn_prpr`는 **없다**(공식 문서엔 있으나 다른
TR 스키마). 근거는 `KIS_RAW_FIELD_RANGES.md`와 08-18 점검 §4-2.

---

## [MW0601] 2026-08-17 — Phase 2 착수분 **구현 완료**. 다음은 08-18 기동이 판정한다

> 이날 확정한 것: **우리는 Phase 1에 있지 않았다.** 판단 층(Fusion·Risk)은 이미 라이브였고
> 실행 층만 `main.py`에 한 줄도 없었다. 진단 상세는 `CURRENT_STATE.md` 맨 위 절.
> 테스트 1,500 → **1,552개 전부 통과**.

### 이번에 실린 것 (08-18 07:30 기동부터 돈다)

| # | 무엇 | 되돌리면 깨지는 테스트 |
|---|---|---|
| 1 | **§11.5 종목 선택기** — `fusion/instrument_selection.py` 신설 + 라이브 배선 | `test_fusion_instrument_selection.py` 26건 |
| 2 | 마이그레이션 **031** `signal_decisions.selected_instruments` | `test_data_db.py` 2건 |
| 3 | 체인 스냅샷에 `delta`/`volume`/`spread_state` 합류(**GEX와 같은 스냅샷**) | `test_data_db.py::test_latest_option_chain_maps_rows_to_dicts` |
| 4 | **ExecutionEngine 시각·거래정지 게이트 복구**(P0급) | `test_execution_engine.py` 3건 |
| 5 | 백테스트 레짐을 **실측**으로(`exit_rules_regime`) | `test_backtest_data_adapter.py` 4건 |
| 6 | `hybrid_mode` 설정을 처음으로 **읽는다** + 배선 전 ADVISORY 클램프 | `test_execution_hybrid_mode.py` 5건 |
| 7 | **ExecutionEngine 그림자 배선** — 주문 없이 판정만 기록 | `test_main.py` 3건 |
| 8 | COCKPIT "선택 종목" 카드 | `test_dashboard_decision_panel.py` 5건 |

**마이그레이션 031은 수동 적용이 필요 없다** — `start_mahdi_premarket.bat`(2026-07-21)이 매일
07:30 기동마다 `db/migrations/*.sql` **전체를 파일명 순으로 재적용**한다. 010/011이 파일로만
커밋되고 라이브엔 반영 안 된 사고가 두 번 난 뒤 만든 구조이고, 전 파일이 `IF NOT EXISTS` /
`COMMENT ON`이라 매일 재실행해도 안전하다. (08-17 확인: 라이브 DB에 컬럼·코멘트 반영 완료.)

⚠ **다만 그 자동 적용은 실패해도 조용하다** — `경고: 마이그레이션 적용 실패 (계속 진행)`을
로그에 남기고 넘어간다. 2차 방어선은 COCKPIT "스키마 정합성" 배지이고, 그 배지의 입력인
`db._SIGNAL_DECISION_COLUMNS`에 `selected_instruments`가 **빠져 있었다**(08-18 수정).
이제 `test_signal_decision_columns_match_the_actual_insert`가 실행된 INSERT문에서 컬럼을 뽑아
대조하므로 다음에 컬럼을 늘리고 목록을 잊으면 테스트가 막는다.

### ✅ 08-18 주문 왕복 실측 **완료** — Phase 1의 마지막 미결이 닫혔다

`submit_order`/`inquire_ccnl`/`cancel_order` 셋의 **최초 실호출.** 모의계좌 1계약,
`B09FAWA37`(코스피위클리 C 2608W3 1,090.0) 지정가 15.40. 제출·취소 모두 `rt_cd=0`,
**잔여 주문 0**(원주문 잔량 0 확인). 상세는 `KIS_RAW_FIELD_RANGES.md` 「주문 왕복 실측」 절.

**결함 넷을 찾았고 셋을 고쳤다:**

| # | 결함 | 상태 |
|---|---|---|
| ① | 시장구분 미전달 — 선물을 옵션으로 조회(응답은 200에 전 필드 0) | 수정 |
| ② | 30% 원거리 지정가가 일일 제한폭 밖(±8%) | 수정 — 응답의 상/하한가로 클램프 |
| ③ | **주문번호 패딩 불일치**(제출 `"0000007047"` vs 조회 `"      7047"`) | 수정 — **P0급** |
| ④ | 취소주문 행이 조회에 남고 `parse_fill_status`가 PENDING으로 읽음 | **관측만** |

**③이 P0인 이유**: `get_order_fill_status()`가 같은 비교를 해서, 라이브에서는 **모든 주문이
영원히 PENDING**에 머물고 체결이 나도 상태머신이 그것을 못 본다. 그리고 실패가 조용하다.

**④는 지금 무해하다** — `odno`로 먼저 거르므로 원주문 조회에는 안 섞인다. 위험해지는 지점은
「열린 주문이 몇 건인가」를 `output1` 전체로 세는 코드(주문 정합성 대조기)다. 구분 축은
`orgn_odno != 0`. 필요해질 때 고친다.

- [ ] **모의계좌 증거금** — 5,000만원으로 KOSPI200 **정규선물 1계약은 못 낸다**
      (`"주문가능금액이 부족합니다"`). 선물 기반 실측이 필요하면 미니선물이나 증거금 확충이 선행.

### 08-18 실주문 왕복 실측 — 지정가가 호가단위를 벗어나던 것을 고쳤다

`measure_order_roundtrip._away_price()`가 `round(x, 2)`(0.01 격자)로 지정가를 만들고 있었다.
주석은 "옵션 최소 호가단위"라 적었지만 **첫 실측 대상은 선물 A01609**이고 선물 격자는 0.05다.
실측: 현재가 2,000개 중 **1,600개(80%)가 격자 위반**. 거래소가 거부하면 포지션은 안 생기지만
한 번뿐인 실측이 날아가고 **«주문 API가 안 된다»로 오귀속**되기 쉽다.

- 호가단위 단일 출처를 `execution/entry.py`의 `DEFAULT_TICK_SIZE = 0.05`로 세웠다
  (0.05 배수는 0.01 배수이기도 하므로 선물·옵션 양쪽에서 유효하다).
- 스냅은 **항상 더 멀어지는 쪽으로만** 한다(매수 내림/매도 올림) — 가장 가까운 격자로 반올림하면
  가격이 현재가 쪽으로 움직여 체결 위험이 늘 수 있다.
- 격자로 표현 불가한 저가 구간(프리미엄 0.05 미만)에서는 **멈추고 `--tick`을 묻는다.**
  0을 내보내거나 현재가 위쪽 값을 지어내지 않는다.
- 예행연습 출력에 `호가단위 … 격자 스냅 완료 · 전송 문자열 …` 한 줄이 추가됐다 —
  08-17까지 **거리만 보고 격자는 아무도 안 봤다.**
- 되돌리면 깨지는 테스트: `test_scripts_measure_order_roundtrip.py` 31건.

**실행 시 확인할 것**
- [ ] 예행연습 출력의 전송 문자열 끝자리가 **0 또는 5**인가(위 한 줄이 그대로 인쇄한다).
- [ ] 장 시간 게이트는 실제로 **15:35**까지 허용한다(`CLOSING_AUCTION_START`) — 에러 메시지의
      "15:20" 문구는 낡았다. 선물은 15:45까지 거래되므로 15:35 이후엔 `--force-outside-hours`.
- [ ] 토큰 403(분당 1회 제한)이 뜨면 65초 대기 재시도가 최대 2회 — **정상 동작이다.**
- [ ] ⚠ 이 스크립트는 `signal_decisions`에 **아무것도 쓰지 않는다**(결과는
      `logs/order_roundtrip_*.json`에만). 그래서 `2026-08-17-p3`의 불변식
      `order_submitted_true_count == 0`은 이 왕복 때문에 깨지지 않는다 — 그 0은 여전히
      «라이브 루프가 주문을 안 냈다»를 뜻한다. **두 경로를 헷갈리지 말 것.**
- [ ] 실행 후 응답 원문의 필드명을 `KIS_RAW_FIELD_RANGES.md`에 옮길 것(R8).
      ⚠ A01609는 선물이라 `futs_prpr`만 확정된다. 스크립트 147행이 이미 쓰고 있는
      **`optn_prpr`는 아직 검증되지 않은 추정**이다 — 옵션 가격 필드는 아래 항목이 답한다.

### ⚠ 새로 드러난 하드 전제 — **옵션 가격이 수집되지 않는다**

`option_analysis_1m`에 **가격 컬럼이 아예 없다**(그릭스·IV·OI·거래량뿐). Passive-first 지정가는
기준가에서 틱을 빼서 만드는 것이므로 **가격 없이는 진입 계획이 안 나온다.** 지금은 ADVISORY라
`build_entry_plan()`이 아예 안 불려 무해하지만, **CONFIRM 승격의 하드 전제**다.

- [ ] 08-18 로그에서 `KIS 옵션시세 output1 필드 목록` INFO 한 줄을 찾아 **가격 필드명 확정**.
      추측 금지 — `KIS_RAW_FIELD_RANGES.md` 체크리스트가 정확히 그것을 금한다(theta 오버플로우를
      5일간 못 잡은 이유가 "실제 값을 안 보고 컬럼을 추측으로 정함"이었다).
- [ ] 확정 후 마이그레이션으로 가격 컬럼 신설 → `_parse_option_quote()` 매핑 →
      `EntryContext.reference_price` 실값 배선. 그때 `_ENTRY_PLAN_BLOCKED_NO_PRICE`를 걷어낸다.

### 판정 — 존재부터 본다 (08-18)

- [ ] `signal_decisions.selected_instruments`가 **NULL이 아닌 행이 있는가.** 전부 NULL이면
      마이그레이션이 안 실렸거나 선택기가 안 돈 것이다(둘은 로그로 갈린다).
- [ ] ENTER 분에서 후보 비율 — 예측치는 `2026-08-17-p1`에 등재.
- [ ] **불변식**: 제안 종목의 만기가 당일인 건수 **0**(0DTE 제외가 깨지면 0이 아니다).
- [ ] `execution_engine.order_submitted`가 **전 행 False**인가 — 하나라도 True면 주문 경로가
      의도치 않게 열린 것이다(이 파일에 `submit()` 호출부가 없으므로 나올 수 없어야 한다).
- [ ] `exec_mode`가 전 행 "ADVISORY"인가. 설정과 갈리면 `configured_mode`가 그 사실을 담는다.
- [ ] `symbol_resolved` 비율 — **임계를 두지 않는다.** 정상 분포를 모른다(규약 G).

### 대가 감시

- [ ] `overrun.count`와 최대 밀림 — 선택기가 매 사이클 체인을 훑으므로 사이클이 늘 수 있다.
- [ ] `rest.total_calls` — 선택기는 REST를 **안 부른다**(체인은 이미 읽은 것을 나눠 쓴다).
      이 값이 움직였다면 원인은 선택기가 아니다.

### 재지 않는 것

**"더 좋은 행사가를 골랐는가"** — 체결이 0건인 동안 그것은 정의상 관측 불가다. 추정하려 들면
선택기가 아니라 우리의 상상을 재게 된다.

### 다음 (08-18 이후, 순서대로)

- [ ] **사이징 입력 실측화.** `main.py`의 `target_vol=0.01, realized_vol=0.01,
      liquidity_score=1.0, portfolio_capacity_remaining_pct=1.0`은 전부 중립 상수다 — Kelly
      사이징의 변동성 타게팅·유동성 축이 **라이브에서 사실상 무력**이다. `market_raw_1m` 봉으로
      realized_vol을, `spread_state`로 liquidity_score를 낼 수 있다. **한 번에 하나씩** 켠다.
- [ ] **모의계좌 실주문 1건** — 주문 상태머신이 테스트로만 검증돼 있고 실주문 0건이다.
      Phase 1의 마지막 미결이 여기서 함께 닫힌다.
- [ ] `VOL_COMPRESSION` 청산 파라미터 결정 — **코드가 아니라 사람의 일이다.** 실측 레짐의
      71.7%가 이 구간인데 `exit_rules`에 행이 없어 타임스톱이 안 걸린다. 값을 정하거나
      그 레짐의 진입을 막거나 둘 중 하나다.
- [ ] Meta-Labeling 정식화(Triple Barrier + Purged CV) — `signal_decisions` + 사후 가격으로
      라벨이 성립할 만큼 쌓인 뒤. 지금 미착수인 것이 **올바른 상태**다.

---

## [MW0601] §11.5 Instrument Selection — 착수 계획 (2026-08-17 등재, **같은 날 구현 완료**)

> 아래 체크리스트는 위 절의 1~3·8번으로 실렸다. 설계 의도(왜 이렇게 골랐는가)를 남기기 위해
> 원문을 보존한다 — 무엇을 하기로 했는지와 무엇을 했는지가 갈리면 그것이 곧 회귀 신호다.

> v6 §11.5를 2026-08-17에 신설했다. **스펙에 절이 생긴 것과 코드가 도는 것은 다르다** —
> 워치독이 08-06에 만들어져 08-11까지 한 번도 안 돈 것과 같은 형태를 여기서 반복하지 않는다.
>
> **왜 지금인가**: §11.4의 출력은 전략 이름(`atm_long`, `debit_spread`, …)이고 Execution의
> 입력은 단축상품번호(`EntryContext.symbol`)인데, 그 사이를 잇는 계층이 **코드에도 스펙에도
> 없었다.** 유일한 호출부인 백테스트는 `symbol="BACKTEST"` 자리표시자를 넣고 있었고,
> `FusionDecision`에는 `symbol` 필드 자체가 없다. 라이브가 미배선이라 아무도 이 빈칸을
> 밟지 않았을 뿐이다.
>
> **모의투자 단계에서 하는 이유**: 후보 생성은 §13.1 3모드와 무관하게 항상 자동이어야 한다.
> 모드가 가르는 것은 "실행할 것인가"이지 "누가 고를 것인가"가 아니다. ADVISORY에서 안 돌리면
> CONFIRM 승격일에 **검증된 적 없는 선택 로직이 그날 처음** 돈다.

### 착수 조건

없다 — ADVISORY라 제안을 기록만 하므로 계좌에 무해하다. `ExecutionEngine` 배선을 기다리지
않는다(오히려 배선 **전에** 며칠치 제안이 쌓여 있어야 배선일에 비교 대상이 생긴다).

### 1. 선택기 골격 — 전략명 + 체인 스냅샷 → 후보 종목

- [ ] **입력**: `allowed_strategies`(§11.4 팔레트 출력) + `option_analysis_1m` 최신 스냅샷
      + 선물 체결가(ATM 기준 — `_roll_subscriptions_to_spot`이 쓰는 그 값).
      **출력**: 후보 목록 + **선택 근거**(델타 · OI · `bid_ask_spread` · 만기 잔존일).
- [ ] **만기(북) 선택은 실측을 따른다** — `option_analysis_1m.expiry`에 관측된 만기만 쓴다.
      요일 규칙 금지: 2026-08-18 만기는 **화요일**이다(08-17 대체공휴일 이월). 같은 형태로
      이미 다쳤다(08-11 북 선택 역전, `features/options_intel.py`).
      `db.observed_future_expiries()`가 2026-08-17에 생겼으니 그것을 쓴다.
- [ ] **만기 당일 북은 일반 전략 후보에서 제외**한다(0DTE 플레이북 전용, §11.4).
      `exit_stack.exit_rules_key(is_expiry_day=...)`와 **같은 소스**를 먹어야 한다 —
      두 곳이 만기 여부를 따로 판정하면 어느 쪽이 옳은지 알 수 없게 된다.
- [ ] **유동성 하한 필터는 만들되 임계값은 비워 둔다.** `oi` 하한 / `bid_ask_spread` 상한 /
      당일 체결 유무. **초기 임계를 지금 정하지 말 것** — 모의투자 실측 분포를 본 뒤
      `strategy_params.yaml`에 넣는다(모르는 채 임계를 정하면 그 임계가 곧 결론이 된다 —
      08-05 스팟 괴리율에서 한 실수).
- [ ] 전 후보가 필터에 걸리면 **`no_liquid_instrument` 사유로 기록하고 진입하지 않는다.**
      빈 목록은 실패가 아니라 판정이다(`defensive_regime_no_new_entries` 등과 같은 규약).

### 2. `signal_decisions`에 제안 종목 기록

- [ ] 마이그레이션 신설 — 제안 종목 + 근거를 담을 자리(기존 `risk_gate_state`와 같은 JSONB 방식).
      **근거를 반드시 함께 남긴다** — 계산해 놓고 버리면 나중에 "무엇이 이 행사가를 골랐나"에
      답할 수 없다(`member_scores`를 뒤늦게 살린 것과 정확히 같은 이유).
- [ ] 제안이 **없을 때의 사유**도 남긴다(`no_liquid_instrument` / 팔레트가 관망 지시 / 방어 레짐).
      「후보가 없었다」와 「선택기가 안 돌았다」가 구분되지 않으면 이 기록은 쓸모가 없다(규약 C).
- [ ] COCKPIT "판단 현황"에 한 줄 — 지금 `허용 전략`까지만 보이고 **어느 종목인지가 없다.**

### 주장 지표 / 대가 (숫자를 보기 전에 적는다 — `hypotheses.yaml`에 선등재할 것)

- **주장**: ENTER 계열 분에서 제안 종목이 NULL이 아닌 비율 ≥ 90%.
  그보다 낮으면 필터가 과하거나 체인 스냅샷이 비어 있는 것이다 — **둘을 사유 코드로 가른다.**
- **주장**: 제안 종목의 만기가 **당일 만기인 건수 0**(불변식 — 0이 아니면 0DTE 제외가 깨진 것).
- **대가**: 선택기가 매 사이클 체인 스냅샷을 훑으므로 사이클 시간이 늘 수 있다.
  `overrun.count`와 최대 밀림을 함께 볼 것.
- **재지 않는 것**: "더 좋은 행사가를 골랐는가" — 체결이 0건인 동안 그것은 정의상 관측 불가다.
  추정하려 들면 선택기가 아니라 우리의 상상을 재게 된다.

### 승격 경로 (§11.5)

```text
① 선택기 가동 + 기록      ← 지금 착수하는 것 (ADVISORY, 주문 없음)
② 사람 선택과 갈림 측정    며칠치 축적. 갈림 감소가 ③의 정량 근거다
③ CONFIRM 승격            원클릭 승인 대상에 종목 포함
④ FULL_AUTO               챔피언 전략 한정
```

⚠ ②의 "사람 선택"은 왕복 실측·수동 주문에서 실제로 고른 종목이다. **그 기록이 남아야
비교가 가능하다** — 수동으로 주문한 날에도 무엇을 왜 골랐는지 적어 둘 것.

---

## [MW0601] 2026-08-13(목) — 08-12 Fix 8종 검증 + **레버 E**

> 예측치는 `hypotheses.yaml`의 `2026-08-12-fix1~fix8`에 전부 등재돼 있다(숫자를 보기 전에 적었다).
> 상세는 `docs/동작점검/2026-08-12_마흐디_운영점검보고서.md`. **08-13 07:30 기동이 함께 집어든다.**
>
> ⚠ **오늘은 먼슬리 만기일(08-13)이다.** 위클리(월)가 08-18로 밀린 그 구조가 08-11에 북 선택을
> 뒤집었다 — 만기 당일 북은 잔존만기 0이라 감마플립이 정의되지 않고 핀 리스크만 의미를 갖는다.

### 켤 것 — 오늘 하나

- [ ] **레버 E — 혼잡 시간대 위클리 격분화**
      `main.OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS = {10: 4, 11: 4, 12: 4, 13: 4, 14: 4}`
      **발동 조건은 이미 네 번 성립했고 네 번 미뤘다.** 08-12에도 이틀 연속 14구간이 성립했다
      (`inquire-price` 10·11·12·13·14시 포함). `2026-08-11-eE`가 *"08-13에 새 유예 사유가 없으면
      켠다"* 로 못 박아 뒀다.
      - ⚠ **08-12 Fix 8종이 새 유예 사유가 되는가 — 아니다.** 그 여덟은 인프라·계측 축이고
        E는 폴링 총량 축이다. 다만 **Fix#4(조기 포기 북 우선순위)와 E는 둘 다 위클리 레그 수를
        움직인다** — 귀속을 위해 E의 대가 지표(`db.book_coverage` 위클리 50%→약 25%)와
        Fix#4의 주장 지표(`timeout_abort.priority_before_others_minutes == 0`)를 **따로** 읽을 것.
      - 주장 지표는 `budget_exceeded.count` 감소다. **p95는 예측하지 않는다**(KIS 귀속이라
        우리가 콜을 줄여도 그들의 p95는 안 바뀐다 — 줄어드는 것은 우리의 노출이다).
      - **먼슬리는 어느 시간대에도 안 건드린다 — 판단 입력이다.**

- [ ] ⚠ **확신도 분모 레버(F)는 08-14다.** 둘 다 판단 출력을 움직이므로 같은 날 금지.
      08-12에 **켠다고 해 놓고 안 켰다** — `2026-08-11-eF`를 08-14로 옮기고 `전제레버`를 달아
      뒀으므로, 안 켜면 이제 §0-1이 「미실행」으로 인쇄한다(규약 H).

### 판정 — 존재부터 본다

- [ ] **§0 레버 표가 뜨는가**(신설). `git HEAD`가 08-12 커밋인가 — 안 바뀌었으면 기동이 새 코드를
      안 집은 것이다.
- [ ] **§11-1 워치독**: `silence_over_cadence_ratio <= 2.0`(08-12 **33.1**) ·
      `restart_failures == 0`(08-12 1건, 그 1건이 오보였다).
- [ ] **`regime_state` 분 수 == 선물봉(A01609) 분 수** — Fix#3의 유일한 직접 지표.
      08-12는 376 vs 406(−30)이었다. ⚠ **WS 재연결이 0인 날에는 판정 불가다**(경로가 안 돈다).
- [ ] `timeout_abort.priority_before_others_minutes == 0` — Fix#4의 불변식.
      ⚠ **`priority_cut_minutes`는 이 fix로 안 내려간다**(홀수분 꼬리 컷은 위반이 아니다).
      26 근처를 유지하는 것이 정상이고, **그 값으로 반증하지 말 것.**
- [ ] `process_starts`가 **1회**인가(08-12는 2회) · 「관측 루프 미가동」 결손 0분인가.

### 대가 감시

- [ ] `overrun.count == 0` · `최대 밀림 0.0초` — Fix#4의 1단계가 사이클을 늘리지 않았는지.
- [ ] `rest.total_calls`(08-12 기준선 9,331) — Fix#4로 먼슬리 확정 실패 호출이 늘 수 있다.
      **E를 같은 날 켜므로 이 값은 두 힘이 반대로 당긴다** — 총계만 보고 어느 쪽도 판정하지 말 것.
- [ ] `db.monthly_leg_completeness.below_design_pct`(08-12 기준선 **10.7%**).

### 아직 안 켠 레버 (§0 표가 매일 인쇄한다)

`SIGNAL_FUSION_PHASE_OFFSET_SECONDS`(Fix#10 위상) — **E를 먼저 켜는 것이 08-12의 결론이다.**
E가 짝수분을 20레그→10레그로 줄이므로 stale이 저절로 내려갈 수 있다(= E가 위상 레버의 대체재일
가능성). 08-14에 `chain_input_source.stale_pct`를 보고 정한다(08-12 84.7% / 정규장 96%).

`OPTION_CHAIN_READ_TIMEOUT_SECONDS` · `reentry_cooldown_minutes` · `REGIME_RESTORE_SESSION_WINDOW`
— 조건은 각 상수 위 주석과 `hypotheses.yaml`에 있다.

### 08-13 이후로 넘긴 것

- [ ] **Fix#7 재생 검증**(예정일 08-19). 복원한 세션 창의 예측이 연속 실행과 같은지 오프라인
      재생으로 확인한 뒤에만 `REGIME_RESTORE_SESSION_WINDOW`를 켠다.
- [ ] **ZN/ES 권한**(§4 Fix#9) — 코드가 아니라 결정이다. (a) CME/CBOT SUB거래소 신청,
      (b) 매크로 축에서 공식 제외하고 `cross_asset_stress` 계산식에서 빼기. **지금은 있는 척하면서 없다.**
- [ ] **WS 재연결 폭주의 원인**(§7-1) — 09:13~10:10에만 31회, 재기동 후 0회. 시간대별 카운터를
      며칠 쌓아 재발 패턴부터 본다. **오늘은 결론을 쓰지 않는다.**

---

## [MW0601] 2026-08-12(수) 검증 — 08-11 Fix 10종 + 고도화 6종

> 예측치는 `hypotheses.yaml`의 `2026-08-11-*` 8건에 **전부 등재돼 있다**(숫자를 보기 전에 적었다).
> 상세는 `docs/동작점검/2026-08-11_마흐디_운영점검보고서.md`.
> **08-12 07:30 기동이 이것들을 함께 집어든다.**

### 켤 것 — 오늘 단 하나만

- [ ] **확신도 분모 레버(고도화 F)** — `strategy_params.yaml`의 `use_effective_member_count: true`.
      원계획 기준일("재학습 성공 확인 다음 영업일")이 08-12다. 예측은 `2026-08-11-eF`에 등재.
      08-11 기준선: 가용 3.41 / 실질 2.36 / 죽은 축 1.06 / HIGH_CONVICTION 34건 / ENTER 281건.
- [ ] ⚠ **KIS 지연 레버(E)는 08-13이다.** 둘 다 판단 출력을 움직이므로 같은 날 켜면 귀속이
      안 갈린다. **E를 미룬 이유의 절반이 F를 오늘 단독으로 켜기 위해서다.**

### 내려 둔 레버 셋 — 조건이 성립해도 오늘은 안 켠다

| 레버 | 상수 | 켤 조건 |
|---|---|---|
| 옵션체인 read 타임아웃(Fix#3) | `rest_client.OPTION_CHAIN_READ_TIMEOUT_SECONDS` | **Fix#1 효과가 먼저 실측돼야** 한다(`timeout_abort.count > 0`인 날) |
| fusion 위상(Fix#10) | `main.SIGNAL_FUSION_PHASE_OFFSET_SECONDS` | Fix#1·고도화A와 같은 날 금지(둘 다 체인 나이를 움직인다) |
| 재진입 쿨다운(고도화 D) | `strategy_params.strategy_gates.reentry_cooldown_minutes` | **실주문 배선 전**. 그 전에 며칠치 ENTER 빈도 분포를 본다 |

### 판정 — 존재부터 본다

- [ ] `budget_exceeded.labelled == budget_exceeded.count` — **규약 C.** 08-11 로그에는 라벨이
      없어 `priority_cut_minutes`가 `None`이었다(«못 쟀다»). 08-12부터 숫자가 나와야 한다.
- [ ] `budget_exceeded.priority_cut_minutes == 0` — **불변식.** 0이 아니면 먼슬리 우선 순서가 깨졌다.
- [ ] `db.signal_reach.chain_input_source.available == True` — 마이그레이션 029가 실렸는가.
      `stale_pct`는 **임계 없이 인쇄만** 한다(Fix#10을 켜기 전에는 높은 것이 정상이다).
- [ ] `db.decision_outcomes.horizons.*.abs_move_pct` 키 존재 — 고도화 C.
- [ ] `timeout_abort` / `failure_budget_abort` — **0건이 반증이 아니다.** 그날 KIS가 안 느렸다는 뜻이다.
      판정은 «발생한 분에서 `budget_exceeded`와 갈렸는가»이지 «몇 건인가»가 아니다.

### 대가 감시

- [ ] `db.monthly_leg_completeness.below_design_pct` — 08-11 기준선 **12.4%**.
      **나빠지면 실패 예산(6건)이 너무 빡빡한 것**이다.
- [ ] `db.decisions.decision.ENTER` — 08-11 기준선 **281건**. 쿨다운은 OFF이므로 이 fix 때문에
      바뀌면 안 된다(바뀌면 OFF가 OFF가 아니다).

### ⚠ 규약 G가 새로 생겼다 — 예측을 쓸 때 걸린다

**시장 상태에 의존하는 지표에 무조건부 하한을 걸지 않는다.** 08-11에 `regime_hmm 비영 분 > 0`과
`dead_axis_mean < 1.02`가 멀쩡한 엔진을 반증으로 찍었다 — 그날 시장이 추세가 아니었을 뿐이다.
`hypotheses.violates_market_state_rule()`이 기계적으로 막고, **도입 당일 `2026-08-06-e2`의 살아
있는 예측을 잡았다**(규약 F가 도입 당일 셋을 잡은 것과 같다).

---

## [MW0601] 날짜가 걸린 두 건 — **결정 절차를 08-10에 미리 확정한다**

> 이 저장소의 규약은 *"숫자를 보기 전에 규칙을 확정한다"* 다(`2026-08-04-p5`가 그렇게 쓰였다).
> 판정일에 규칙을 만들면 그 규칙이 그날 숫자를 따라간다. 그래서 **도구와 절차는 08-10에
> 만들어 두고, 판정만 그날 한다.**

### A. 부호 규약 감사 — **판정일 08-11** · 하네스는 08-10에 완성

- [x] **감사 하네스 구현 완료**(`tests/test_fusion_signal_layer.py` 하단 6건, 전부 통과).
      임계는 걸지 않았다 — 정상 변동폭을 모르는 상태에서 임계를 정하면 그것이 곧 결론이 된다.
      대신 **두 멤버가 각각 무엇을 말하기로 돼 있는지**를 고정 케이스로 박제했다.
- [x] **08-10 실측으로 이미 답이 나왔다 — 규약은 뒤집히지 않았다.**

      GEX부호   options_flow  flow_position   분
      GEX<0        −              +          308   ← 대부분
      GEX<0        +              +           64
      GEX<0        −              −            6
      GEX<0        0              +            4

      **그날은 하루 종일 GEX<0**(GEX>=0인 분이 0)이었고 외국인은 종일 순매수, 스팟은 기준선
      아래였다. 즉 *수급은 사는데 딜러가 숏감마라 하락이 증폭되는 국면*이다 —
      **두 멤버가 반대 부호를 내는 것이 설계상 정상**이다.
- [ ] **08-11 판정은 이 순서로 한다**(숫자를 보기 전에 확정):
  1. 일치율이 20% 밑이면 → 먼저 **그날 GEX 부호 분포**를 본다. GEX<0이 지배적이고
     스팟이 기준선 한쪽에 몰려 있으면 **시장 사실이지 결함이 아니다.** 여기서 끝낸다.
  2. GEX 부호가 섞였는데도 일치율이 20% 밑이면 → 감사 하네스 6건을 돌린다.
     **깨지면 규약이 바뀐 것이다.** 안 깨지면 다음으로.
  3. 그때도 설명이 안 되면 `gamma_wall` 기준선을 의심한다 — 아래 참고.
- [ ] ⚠ **참고로 남길 것: 이 멤버는 사실상 gamma_flip을 써 본 적이 없다.**
      `signal_decisions.gamma_flip` 실측(08-03~08-10, 2,944 판단): 08-05의 22건 외 전부 NULL.
      실동작 기준선은 **감마 월**이고 docstring이 말하는 "Gamma Flip"이 아니다.
      2026-08-04 Fix#3이 명시적으로 고른 폴백이라 결함은 아니지만, **부호를 해석할 때
      무엇과 비교한 값인지 착각하면 안 된다.** 하네스에 박제해 뒀다.

### B. KIS 지연 레버 — **판정일 08-12** · 발동 조건은 이미 성립, 예측치를 여기서 확정

- [ ] **발동 조건은 이미 3회 성립했고 3번 미뤘다.** 그 유예가 표류하지 않게 아래를 못 박는다.

      성립일      미룬 이유                                      그 이유의 현재 상태
      08-06      "먼슬리 재시도가 회복시킬 수 있다"              **해소됨**(27.1%→1.0%, 08-10 3.4%)
      08-07      "변수를 하나만 바꾼다 — 08-11 재판단"           08-11 도래
      08-10      "R1~R7이 08-11에 처음 실린다 — 08-12 재판단"    08-12 도래

      **08-12에 새 유예 사유가 없으면 켠다.** 유예하려면 그날 새로 실린 변경을 명시할 것.
- [ ] ⚠ **예측치 정정 — `p95가 내려간다`고 쓰면 안 된다.**
      레버 주석(`main.py`)은 *"주장: 그 시간대 p95 하락"* 이라고 적어 뒀는데,
      §17 교차점검은 이 지연을 **KIS 귀속**으로 판정했다(*"페이서가 한가한데 타임아웃이 나는
      것은 예약 큐 경합이 아니다 — 스케줄링 최적화로는 줄지 않는다"*).
      **KIS 쪽 혼잡이면 우리가 콜을 줄여도 그들의 p95는 안 바뀐다.**
      줄어드는 것은 **우리의 노출**이다. 그래서 주장 지표는 이것이어야 한다:

      주장  `budget_exceeded.count` 감소 (08-10 기준선 **27건**, 그중 15시 8건)
      주장  `db.signal_reach.gex_input_missing_minutes == 0` 유지
      참고  `rest_latency` p95 — **예측하지 않는다.** 안 내려가도 반증이 아니다
      대가  `db.book_coverage` 위클리 커버리지 50% → **약 25%**
      대가  `rest.by_group.옵션체인` 북당 값 — 08-10 3,666 기준(총계로 읽지 말 것)
- [ ] **켤 값은 실측으로 이미 정해져 있다** — 10~14시가 5영업일 연속 초과다:
      `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS = {10: 4, 11: 4, 12: 4, 13: 4, 14: 4}`
      절감 규모(08-10 실측): 혼잡 5시간의 위클리 행 1,435 / 먼슬리 2,979.
      위클리를 2분→4분으로 늘리면 **약 700콜/일 절감**(옵션체인의 9.5%, 총 REST의 7%).
      **먼슬리는 어느 시간대에도 안 건드린다 — 판단 입력이다.**
- [ ] ⚠ **15시는 넣지 않는다.** 종일 최악 구간이지만(느린 호출 203건, 예산 초과 8건)
      15:20 이후는 현물 마감이라 성격이 다르고, 위클리(월) 만기일에는 그 시간대가
      핀 리스크의 유일한 관측 창이다. 10~14시 효과를 먼저 보고 08-13 이후에 따로 정한다.

---

## [MW0601] 2026-08-11(화) 검증 — 08-10 레짐 엔진 배포

> 예측치는 `hypotheses.yaml` `2026-08-10-r1r3` / `r6` / `r4r7`.
> 상세는 `docs/동작점검/2026-08-10_마흐디_운영점검보고서.md`, 근거는 `DECISION_LOG.md` 2026-08-10.
> **08-11 07:30 기동이 코드와 모델을 함께 집어든다** — 코드+모델은 한 fix의 두 반쪽이라
> 분리 탑재가 불가능하고, 귀속도 갈리지 않는다.

### 최우선 — 모델이 실제로 실렸는가 (존재부터)

- [ ] **`regime_state.is_warmup=false`인 분이 존재하는가.** 25영업일 연속 WARMUP이었다.
      0이면 모델이 안 실린 것이고, 그때 `data/models/regime_engine.pkl` 존재부터 확인한다.
- [ ] **§14-3 `regime_hmm` 비영 산출 분 > 0** — 25일간 매분 0점(중립)이던 축이 처음 의견을 낸다.
- [ ] **하루 방문 레짐 2종 이상** — 재생 실측 5/5일 2종. **조용한 날 1종은 정상**이므로
      하루로 반증하지 않는다(며칠 중 2일 이상이면 확인).
- [ ] **`dead_axis_mean` < 1.02**(08-10 기준선 1.02).

### 대가 — 채터링 회귀 감시

- [ ] **레짐 전이 로그 하루 2~20회.** 25영업일간 1회였다. 재생 실측 0.5~5.0%/분.
      ⚠ **100회를 넘으면 08-10에 격리한 모델과 같은 병**이다 — 그때는 `iv_chg`의 홀짝 격차부터 본다.
- [ ] `stability_flag=False` 분은 재생에서 0분이었다. **임계(0.40)를 지금 바꾸지 않는다** —
      "확신하며 채터링"을 못 잡는다는 것은 알지만, 정상 확신도 분포를 모른 채 임계를 정하면
      그 임계가 곧 결론이 된다(08-05 스팟 괴리율의 교훈).

### R6 — iv_chg 구형파가 사라졌는가

- [ ] **`feature_store.iv_chg`의 분 홀짝 평균 격차 < 0.01.** 08-10 실측 격차 0.568
      (짝수분 −0.26420 / 홀수분 +0.30419).
- [ ] **차분 lag1 자기상관 > −0.6.** 08-10 −0.900. 재구성 실측 −0.459이고 **그 값은 잡음의
      이론적 하한(−0.5) 근처라 정상**이다 — 0을 기대하면 안 된다.
- [ ] ⚠ **`iv_chg`는 08-11을 경계로 모집단이 다르다** — 경계를 넘어 시계열을 직접 비교하지 말 것
      (08-07 Fix#1의 GEX와 같은 상황).

### 게이트

- [ ] 게이트가 **거부한 적이 있는가**(주장). 도입 당일 두 번 일했다 — startprob이 붕괴 모델을,
      수렴이 발산 승자를 거부. "한 번도 안 걸렸다"는 좋다는 뜻이 아니라 **안 재고 있다**는 뜻일 수 있다.
- [ ] 게이트 때문에 **정상 모델이 막힌 적**(대가). 0이 목표.

### 미룬 것

- [ ] **확신도 분모 레버(`use_effective_member_count`)는 08-12 이후다.** 원계획
      "재학습 성공 확인 **다음** 영업일"의 기준일이 08-10이 아니라 08-11이 됐다.
      같은 날 켜면 확신도 변화의 귀속이 불가능해진다. 켜기 **전에** 예측을 먼저 적는다.
- [ ] **KIS 지연 레버(고도화 E)** — 조건은 이틀 연속 성립했으나 08-11에 R1~R7이 처음 실린다.
      **변수를 하나만 바꾼다** — 08-12에 재판단.

---

## [MW0601] 다음 세션 Fix — 08-10 장중 점검에서 나온 것

- [ ] **로그축 결손 지표가 사이클을 한 분 앞으로 귀속한다**(보고서 §3-1). `log_metrics.py:803`의
      `seen = {_hhmm(c["start"])}`를 **`분=` 라벨 축**으로 이전하고, 라벨 없는 과거 로그에서만
      파생 start로 폴백. §4의 "두 축이 어긋난다" 문구도 **파서 축을 먼저 의심**하도록 수정.
      - 실측: 11:09 결손은 **허위**다(`분=11:09` 라벨 사이클이 로그에 있다). 파생 start가
        11:08:59.996으로 떨어져 11:08에 귀속됐다.
      - 주장 지표: `cycles.missing.count == 0` **그리고** `duplicate_poll_minutes.labelled ==
        cycles.count`(규약 C — 라벨이 0이면 count 0은 「못 쟀다」).
- [ ] **절대 건수 대가 지표에 북 수 정규화**(보고서 §3-5). 옵션체인 REST 9,241→6,860은 fix가
      아니라 **북이 3→2로 준 것**이다. `_COUNT_METRIC_SUFFIXES`에 `by_group.*` 등록.
- [ ] **`flow_position ↔ options_flow` 부호 규약 감사**(보고서 §3-3). 이틀 연속 20% 밑.
      임계를 걸지 말고 **고정 케이스 테스트**로 "같은 시장 상황에서 어떤 부호를 내야 하는가"를
      박제한다. 3영업일째 20% 밑이면 착수.
- [ ] **REJECT 대조군 순열검정을 리포트에 내장**(보고서 §3-2). 지평별 p값 + 누적 부호 이력.
      **임계는 아직 안 건다** — 3지평 × 매일이라 5영업일이면 우연히 나올 확률이 72%.

---

## [MW0601] 2026-08-07(금) 장마감 후 — **오늘 안에 끝낼 것**

- [ ] **장마감 자동 집계 확인** — `stop_mahdi_marketclose.bat`이 종일 값으로 다시 낸다.
      오늘 조사는 **반나절(07:30~12:20)** 기준이라 아래 두 건이 종일 값으로 재판정돼야 한다:
  - [ ] `2026-08-06-p2` 대가 지표(`db.tables.expiry_liquidity_1m.rows`) — **100행 아래로
        끝나면** 08:30 경계가 실제보다 늦은 것이므로 경계를 앞당긴다. 반나절 45행 /
        시간당 0.196행·분(08-06은 0.269)이라 그냥 넘기지 말 것.
  - [ ] §14-3 멤버 부호 일치율 — 고도화#5로 직전 3영업일 열이 붙는다. 08-05/08-06 사이드카가
        있으므로 오늘부터 3일치가 보인다. **아직 임계를 정하지 않는다**(그것이 결정이다).
- [ ] **장중/장후 재시동을 하지 않는다** — Fix/고도화는 전부 **08-10(월) 07:30 기동**부터
      실려야 예측과 실측의 경계가 깔끔하다(08-04 p4: 커밋 07:45 / 기동 07:30, 15분 차이로
      하루를 잃고 멀쩡한 fix를 '반증'으로 적을 뻔했다).
      **비대칭이 근거다**: 오늘 재시동해서 얻는 105분은 월요일에 495분으로 다시 얻지만,
      오늘 잃는 pre-fix 기준선은 다시 못 얻는다. 특히 in-memory 카운터
      (`atm_roll_count_today` 95회)와 로그축 지표(`rest.by_group`, `cycles.*`, `process_starts`)는
      **재계산으로 되살릴 수 없다.**
      재시동으로만 잡을 수 있던 위험은 08-07 리플레이로 대부분 제거했다(아래).

## [MW0601] 2026-08-10(월) **장전 07:30~09:00** — 재시동을 미룬 대가를 여기서 치른다

> 08-07에 재시동하지 않기로 한 근거는 "잔여 위험을 오프라인 리플레이로 없앴다"였다.
> 리플레이로 **못 잡는 것 하나**가 남아 있고, 그것을 개장 전 90분에 확인한다.
> 장전은 ADVISORY이고 실주문도 없으므로 문제가 보이면 09:00 전에 되돌릴 시간이 있다.

> **2026-08-10 종결 — 3항목 전부 판정. 상세는 그날 운영점검보고서 §1.**

- [x] **리플레이로 검증된 것(재확인만)**: 슬롯 고갈 없음(3북 활성 최대 39/41),
      Fix#1 창 자르기(실DB 327분), Fix#3(순수 함수), HMM 저장 가드(드라이런).
      → **HMM 저장 가드는 그날 결함으로 드러났다**(`2026-08-07-e4` inconclusive). 나머지는 확인.
- [x] ⚠ **리플레이로 못 잡는 것 — KIS WS가 「유지 구독」에 계속 데이터를 보내는가.**
      → **보낸다. 서버측 아이들 타임아웃 없음.** `BAFBRW990` 08:46~15:14 367행 등, 창 밖에
      있던 시간에도 봉이 안 끊겼다. `market_raw_1m` 4,405행/44심볼(08-07 1,073행/20심볼).
      **`2026-08-07-e1`의 `market_raw_1m.rows` 예측을 내릴 필요가 없어졌다.**
      (원문)
      우리가 해제하지 않으면 당연히 와야 하지만, **서버측 아이들 타임아웃이 있다면**
      창 밖 종목의 봉이 결국 끊기고 이 고도화의 이득이 사라진다.
      판정: 08:45 첫 롤 이후 창을 벗어난 종목이 `market_raw_1m`에 계속 행을 남기는지
      (COCKPIT Flow Radar에서 육안으로도 보인다). 안 남으면 **이득은 WS 트래픽 감소(-76%)뿐**이고
      「봉 없음」은 안 줄어든다 — 그때는 `2026-08-07-e1`의 `market_raw_1m.rows` 예측을 내린다.
- [x] **기동 로그에 `retention_pool` 경로가 실제로 탔는지** 확인 — 매니저 3개 생성 블록은
      단위테스트가 안 타는 유일한 경로다(`main()` 안). ~~07:31 첫 롤에서 해제 로그가
      **안 나오는 것**이 정상 신호다.~~
      → **탔다.** 롤·북당 끊긴 구독 0.46건(풀 없을 때 2.42).
      ⚠ 다만 **위 판정 규칙은 못 썼다** — 07:31에 롤 자체가 없었고 첫 롤은 08:45다.
      "특정 시각에 무엇이 안 나온다"를 신호로 삼을 때는 **그 시각에 그 이벤트가 일어나는지부터**
      확인할 것.
- [x] 마이그레이션 028은 08-07 장중에 이미 적용해 뒀다 — 장전 재적용은 멱등이라 무해하다.
      → 07:30:26 정상 재적용.

---

## [MW0601] 2026-08-10(월) 검증 항목 — 08-07 **저녁** 2차 (전일 완결 분석 기반)

> 예측치는 `hypotheses.yaml` `2026-08-07-fix1/fix2/fix3/fix5/eC`.
> 낮 커밋(`643cff8`/`bc67cac`)의 예측과 **같은 기동에 함께 실린다** — 서로 다른 것을 재므로
> 귀속은 갈리지 않지만 그 사실을 알고 읽을 것.

### Fix#1·#2 — 현물 연속거래 종료(15:20)

- [ ] **`index_frozen_max_run` <= 2** (기준선 **9분**, 08-05·08-06·08-07 모두 정확히 9분).
      나흘 연속 원인 규명 대기 목록에 있던 항목이 여기서 닫힌다.
- [ ] ⚠ **진짜 정지를 가리지 않는지가 핵심이다** — 08-07의 09:59·14:57 두 건은 경계 적용 후에도
      남았다(`index_frozen_minutes` 2). 그 둘까지 사라지면 경계를 너무 넓게 잡은 것이다.
- [ ] **`options_flow.structural_minutes`를 두 구간의 합으로 분해해 읽는다** —
      장전 89 + 현물마감 25 ≈ 114. **89 근처면 새 경로가 안 탄 것**, 140 초과면 09:00~15:20
      안에서도 스팟이 빠진 것이다.
- [ ] **가용률 하락(81.8% → 약 77%)은 대가이지 회귀가 아니다** — 그 25분은 원래 죽은 스팟을
      쓰고 있었다.

### Fix#3 — 덮어쓴 분

- [ ] **`cycles.duplicate_poll_minutes.count == 0`** — 그리고 **`labelled`를 반드시 함께 볼 것**
      (규약 C). 라벨이 0이면 count 0은 「중복이 없었다」가 아니라 「못 쟀다」이다.
- [ ] 낮 커밋의 `zero_row_count == 0`과 **둘 다** 0이어야 그 사건이 정말 사라진 것이다
      (원인 쪽=경계 스냅, 관측 쪽=중복 감시).

### Fix#5 — 규약 F

- [ ] 08-10 §0에 「반증」으로 찍힌 항목 중 **fix는 맞았는데 예측이 틀린 것**이 몇 건인가. 0이 목표.
      1건 이상이면 그 지표가 어떤 구조 변수에 비례하는지 적고 `_COUNT_METRIC_SUFFIXES`에 넣는다.
      (도입 당일 살아 있는 예측 **셋**을 잡았다 — e1의 `market_raw_1m.rows`, p5의
      `structural_minutes 75~95`, 그리고 내가 방금 쓴 fix2의 `110~120`.)

### 고도화 C — REJECT 대조군

- [ ] **차이의 부호가 고착되는지**를 본다. 08-07 실측(시간대 매칭 후)은 전 지평에서 음수였다:
      5m −1.4pt / 15m −2.5pt / **30m −11.3pt** — 즉 우리가 거른 판단이 더 잘 맞혔다.
- [ ] **하루치로 결론 내지 않는다 — 순열 검정으로 확정했다.** 08-07 30분 지평(202 vs 97)에서
      실력이 같다고 가정해도 그만한 격차가 **8.2%(약 12번에 한 번)** 나온다. 통상 기준 5%를
      못 넘는다. 게다가 지평 3개 x 매일이라 **영업일 5일이면 우연히 나올 확률이 72%** 다.
- [ ] 부호가 음수로 고착되면 그때 (a) 표본 시간대 (b) 무변동 제외 규칙 (c) 컷오프로 인한
      구조적 비대칭을 전부 확인한 뒤에 팔레트 기준을 의심한다.
      **(c)는 시(hour) 매칭으로 안 없어진다** — 같은 14시라도 ENTER는 14:50 이전에만 있다.
      매칭 단위를 분/컷오프 이전으로 좁히면 되지만 표본이 더 준다(며칠 쌓인 뒤에).
- [ ] `shared_hours`가 3개 미만이면 **그날은 판정 보류**다.

### 고도화 D — HMM 재학습 (**08-10이 도달일**)

- [ ] `feature_store` 08-07 종일 누적 **7,832 / 8,000** — 168행 남음(하루 410행) → 오전 도달.
- [ ] `--baseline`은 08-06에 이미 찍혀 있다(덮어쓰기 거부). 바로 재학습으로 간다.
- [ ] **수렴 실패면 저장이 거부된다**(08-07 가드). 그때 `n_iter`를 늘리거나 피처 분산을 먼저 본다.
- [ ] **확신도 분모 레버는 재학습 다음 영업일에 올린다** — 같은 날 켜면 귀속이 불가능해진다.

### 고도화 E — KIS 지연 레버: **08-10에도 내려둔다**

- [ ] 조건은 더 강해졌다(08-07 종일 `inquire-price` p95가 10~14시 **5구간** 이틀 연속 초과,
      최대 14시 3.67초). 그런데 08-10에는 Fix#1·고도화#1(낮 커밋)과 Fix#1·#2(저녁 커밋)가
      처음 실린다 — **변수를 하나만 바꾸는 원칙**상 레버는 계속 내려둔다. **08-11에 재판단.**

### Fix#6 — EGW00201 1건: **고치지 않는다**(결정)

- [ ] 08-07 15:02:35 USDCNH 단발이고, 그 1건이 백오프를 1.00 → 1.50배로 올렸다(15:03:53 회복).
      자동 판정은 이걸 **반증 2건**으로 셌지만 실체는 하나다.
- [ ] 폴러 위상을 다시 만지는 것은 **07-08에 203분을 잃은 그 구조**다. 1건/일은 그 위험을
      감수할 근거가 못 된다. **관측만 유지**하고 여러 날 재발하면 그때 본다.

---

## [MW0601] 2026-08-10(월) 검증 항목 — 08-07 **낮** 1차 (Fix 6종 + 고도화 5종)

> 예측치는 `hypotheses.yaml` `2026-08-07-fix1/fix3/e1/e2/e4`. 상세 근거는
> `docs/dev_memory/DECISION_LOG.md` 2026-08-07.
> **오늘(08-07) 실측이 전부 「적용 전 기준선」이다** — 아래 괄호 안 숫자가 그것이다.

### Fix#1 (체인 창 자르기) — 이 커밋에서 가장 파급이 큰 변경

- [ ] **`db.signal_reach.chain_leg_over_design_minutes` == 0** (기준선 **210분**) — 불변식이다.
      0이 아니면 의심할 것 둘: (a) 한 사이클의 행이 두 분에 걸쳐 적재됐다(Fix#3이 막으려는 것)
      (b) 가장자리 레그가 실패해 범위가 좁아진 뒤 다음 분에 되돌아왔다(1~2분이면 자기 복구, 정상).
- [ ] **`chain_leg_median` == 10** (기준선 12) / **`chain_age_seconds_max` <= 150** (기준선 250.1)
- [ ] **`monthly_leg_completeness.below_design_pct` <= 5** (기준선 1.4) — 창 길이(시간축)는
      안 건드렸으므로 **결손 이월 능력이 그대로여야** 한다. 크게 오르면 범위 자르기가 이월까지
      먹은 것이다.
- [ ] ⚠ **|GEX|가 줄어드는 것은 성공이지 회귀가 아니다.** 08-07까지의 GEX 시계열과 08-10 이후는
      **모집단이 다르므로 직접 비교하지 말 것**(전자는 여러 창의 합이었다). 그 여파로
      `short_gamma_requires_not_met` 건수(08-07 49건)와 §15 핀 행사가/집중도가 바뀔 수 있고,
      **그 변화는 이 fix가 만든 것**이다.

### Fix#3 (분 경계 스냅)

- [ ] **`db.chain_minute_coverage.zero_row_count` == 0** (기준선 1 — 07:31).
      하루 1분짜리라 통계로는 안 보인다. 판정은 §4 「0행 분」 목록에 **기동 첫 분**이 남아
      있는지로 한다. 남아 있으면 스냅 폭(2초)이 부족한 것이다.

### 고도화#1 (구독 유지 풀) — **롤 횟수가 안 줄어드는 것이 정상이다**

- [ ] **`db.ws_status.atm_roll_dropped_subs_today` <= 4** (기준선 없음 — 오늘은 구 코드라 NULL).
      0이면 창은 따라갔는데 1분봉은 하나도 안 끊긴 것이다.
- [ ] **`db.tables.market_raw_1m.rows` >= 900** (반나절 726 → 종일 환산 약 1,050).
      **안 늘면 옵션 자체에 체결이 없는 것**이므로(08-07 심볼별 최대가 `BAFBRW980` 66분)
      이 fix의 실패가 아니라 시장 사실이다 — §14-1 `orderflow_ofi_vpin` 가용률과 함께 읽는다.
- [ ] **`rest.by_group.옵션체인` <= 9,500**(대가) — 유지 구독이 REST를 **늘리지 않는다**는 것이
      이 설계의 전제다. 늘었다면 폴링 대상이 `active_subscriptions`로 새어 나간 것이므로 즉시 되돌린다.
- [ ] **COCKPIT Flow Radar에 「봉 없음 N분」이 줄었는지 육안 확인** — 08-07은 8분이었다.

### 고도화#2 (롤 대가 지표)

- [ ] **배지가 "N회 · 그 때문에 실제로 끊긴 WS 구독 0건"으로 초록이면 성공**이다.
      롤 횟수(기준선 95회, 12:20 시점 76회)가 임계 20을 넘는데 초록인 것이 목표 상태다.
- [ ] `_ATM_ROLL_DROPPED_WARNING = 4`는 **잠정치**다 — 08-05에 20을 잠정치로 두고 이틀 만에
      근거가 틀린 것으로 드러난 전례가 바로 이 항목이므로, 며칠 전에는 이 값으로 결론 내지 말 것.

### 고도화#4 (HMM 재학습) — **08-10이 임계 도달일이다**

- [ ] **`db.feature_store.total` >= 8,000** (08-07 12:20 기준 7,634 / 장중 7,709, 하루 약 380행).
- [ ] 도달하면 **`--baseline`은 이미 08-06에 찍혀 있다**(덮어쓰기 거부됨). 바로 재학습으로 간다.
- [ ] **수렴 실패가 재현되면 저장이 거부된다** — 그때 `n_iter`를 늘리거나 피처 분산을 먼저 본다.
      `--allow-nonconverged`는 근거를 적기 전에는 쓰지 않는다.
- [ ] 저장에 성공하면 다음날 `regime_state`에 RANGE_BALANCED 외의 값이 나오는지 확인
      (08-07까지 24영업일 연속 상태 2 하나뿐이었다).

### 고도화#3 (확신도 분모) — **08-10에는 켜지 않는다**

- [ ] 재학습 성공을 확인한 **다음 영업일**에 `strategy_params`의
      `use_effective_member_count: true`. 재학습과 같은 날 켜면 확신도 변화를 둘 중 무엇이
      만들었는지 영영 못 가른다.
- [ ] 켜기 **전에** `hypotheses.yaml`에 예측을 적는다(08-07 기준선: 가용 3.16 / 실질 2.15).

### 이월 (08-07에 못 고친 것)

- [ ] **ReadTimeout 92건 / KIS `inquire-price` p95 3구간 이틀 연속 초과** — 사전 대응 규칙
      (`2026-08-04-p5`)의 발동 조건은 성립했으나 **레버는 계속 내려 둔다**. 근거: 08-06
      고도화#4가 "먼슬리 두께가 회복되면 감축 불필요"라고 적었고, 08-07에 설계 미만 분이
      27.1% → 1.0%로 실제 회복됐다. 08-10에 다시 본다.
- [ ] **`flow_position ↔ options_flow` 부호 일치율 22.3%** — 무작위면 50% 근처여야 한다.
      고도화#5로 3영업일이 보이게 됐으니 **고착이면 `signal_layer`의 부호 규약을 의심**하고,
      흔들리면 앙상블이 살아 있다는 증거다. 임계는 아직 정하지 않는다.
- [ ] **장운영정보(H0UNMKO0) 누적 수신 0건** — 배지는 이제 "미검증"이라 쓴다(Fix#6). 다음 단계는
      KIS 문서로 "정상일에 이 채널이 무엇을 보내야 하는가"를 확인하는 것이다.

---

## [MW0601] 2026-08-06(목) 검증 항목 — 08-05 **저녁** 운영점검 Fix 6종

> 예측치는 `hypotheses.yaml` `2026-08-05-p8~p13`. 상세는
> `docs/동작점검/2026-08-05_마흐디_운영점검보고서.md` §5「구현 결과」.
> **08-06은 위클리(목) 만기일**이라 §15 핀 리스크·이벤트 캘린더 페널티와 겹친다 — 섞어 읽지 말 것.

- [ ] **감마플립 범위 밖 0건**(p8) — §14 「그중 수집 행사가 범위 밖」이 **0**이어야 한다(불변식).
      **`gamma_flip_pct`가 0%로 돌아가는 것은 실패가 아니다** — 08-04 §2-3대로 이 북은 0%가 정상이고,
      08-05의 4.5%는 개선이 아니라 21/22가 허수였다.
- [ ] **0행 분 지표가 찍히는가**(p9) — §4에 「DB 기준」 줄이 있는지. **0건을 성공으로 읽지 말 것**:
      이 fix는 손실을 줄이는 게 아니라 보이게 하는 것이다. 키 자체가 없으면 지표가 안 붙은 것이다.
- [ ] **먼슬리 두께가 찍히는가**(p10) — §12 「먼슬리 레그 완전성」. **38.2%가 그대로여도 반증이 아니다**
      (재기만 하는 fix다). 개선하려면 타임아웃/예산/폴링 폭을 건드려야 하고 그건 며칠 쌓고 결정한다.
- [ ] **트레이스백 ≤ 1,500줄 / `read_timeout` ≥ 1**(p11) — §11 항등식 줄을 볼 것.
      **`read_timeout`이 0이면 그것이 실패다**(트레이스백을 껐는데 유형 카운터까지 죽은 것).
      `e02fae8`(엔드포인트별 타임아웃)과 효과가 섞이므로 **분리 판정은 트레이스백/타임아웃 비율**
      (08-05 실측 78.9줄/건)로 한다.
- [ ] **§14-1 「그중 구조적」 5~12분**(p12) — 15:36~15:44에 `종가 단일가(연속체결 없음)`가 뜨는지.
      12분을 크게 넘으면 단일가 **밖에서도** 체결이 끊긴 것이다(진짜 이상).
- [ ] **`index_frozen_max_run` ≤ 11분**(p13) — 08-05는 15분(장전 블록). 장전 적재 중단(`9ffcb9c`)이
      걸리면 15:21~15:29의 **9분**만 남아야 한다. 15분 그대로면 장전 차단이 안 걸린 것이다.
  - [ ] **15:21~15:29 지수 정지는 아직 안 고쳤다** — 08-04·08-05 **양일 공통**으로 관측된
        신규 발견이다(선물은 계속 움직이는데 KIS 지수만 9분 정지). 그 구간 판단은 stale 스팟으로
        GEX를 계산한다. **08-06에도 재현되면 3일 연속**이므로 그때 원인 규명에 착수한다
        (숫자를 보기 전에 규칙을 얹지 않는다).

---

## [MW0601] 2026-08-06(목) 검증 항목 — 08-05 구현한 P0 Fix 2종

> 예측치는 `docs/동작점검/hypotheses.yaml`의 `2026-08-05-p1`/`p2`에 등재돼 있고 08-06 자동
> 리포트 §0이 대조한다. 상세 근거는 `docs/dev_memory/DECISION_LOG.md` 2026-08-05 1·2차.
> **08-05 장중(09:47)에 구현했으므로 라이브 반영은 08-06 07:30 기동부터다** — 08-05 장마감
> 리포트에는 아무 효과도 안 나타나는 것이 정상이다.

### Fix#1 (VRP 배선)

- [ ] **`strategy_palette:wait_only`가 절반 이하로 줄고 `short_gamma_requires_not_met`이
      나타나는지** — 이것이 **주장 지표**다. 08-05 리플레이 기준 overpriced 111분 / fair 31분.
- [ ] **`signal_decisions.vrp`가 NULL이 아닌 비율 70% 이상** — 전부 NULL이면 계산 경로가 죽은 것.
      장전(08:45 이전)은 체인/스팟이 없어 NULL이 정상이므로 분모에서 감안할 것.
- [ ] **`decision='ENTER'` 5건 이하.** 07-30에 팔레트 오독으로 하루 419건이 찍힌 전례가 있다.
      **건수보다 그 분의 `vrp`가 아티팩트인지가 판정 기준이다**(hypotheses.yaml 주의 참고).
- [ ] 08:45~09:00 구간의 ENTER는 **스팟 신선도 문제**일 수 있다 — 아래 이월 항목과 함께 볼 것.

### Fix#2 (엔드포인트별 read 타임아웃)

- [ ] **`계좌 잔고 폴링 사이클 실패` 0건** / `만기 유동성 폴링 실패` 1건 이하 — 주장 지표.
- [ ] **`qualitative.read_timeout`이 16건 근처**(08-05는 21건). **0건이면 오히려 이상**이다 —
      옵션체인은 여전히 4초를 쓰므로 완전히 사라질 수 없다. 0이면 `timeout_for_url()`의 매칭이
      너무 넓게 잡힌 것이니 확인할 것.
- [ ] `overrun.count` / `cycles.missing.unrecovered_count`가 08-05 수준(0건/0분)을 유지하는지 —
      악화하면 범위 축소가 옵션체인 쪽으로 샜다는 뜻이다.
- [ ] 부수 효과: 잔고 조회가 성공하기 시작하면 `risk_gate_state.risk_engine`이
      `account_tracker_not_ready`에서 실제 평가 결과로 바뀌는 분이 는다 — **의도된 결과**다.

### Fix#3 (메타 라벨 실입력)

- [ ] **HIGH_CONVICTION 0건 / STANDARD 증가** — 주장 지표. 08-05 전수 재계산 기준 13 → 0.
      **0건은 회귀가 아니라 정정이다** — GEX 음수 + 레짐 불안정인 날에 최고 등급이 나오던 것이
      결함이었다. GEX가 양수로 돌아서고 `stability_flag`가 True가 되는 날에는 다시 나와야 하며,
      **그날에도 0이면** 배선이 과하게 조인 것이니 재검토할 것.
- [ ] **Fix#1과 반드시 함께 읽을 것** — `short_gamma_requires`는 `highest_confidence`를 요구한다.
      Fix#3으로 HIGH_CONVICTION이 사라지면 overpriced 분의 `limited_premium_sell`이 **확정적으로**
      게이트에 걸린다. 두 fix의 효과를 분리하려면 `signal_decisions.vrp`(어느 열)와
      conviction(어느 등급)을 나눠 볼 것.

### Fix#4 (느린 호출 임계)

- [ ] **`slow_calls.count >= 15`** — 주장 지표. 08-05는 ReadTimeout 21건인데 slow_calls 0건이었다.
- [ ] **새 불변식: `slow_calls.count >= qualitative.read_timeout`** — 타임아웃은 이제 임계와
      무관하게 반드시 느린 호출 줄을 남긴다. 이 부등식이 깨지면 배선이 끊긴 것이다.
- [ ] **로그 볼륨 100~300줄 예상**(사람이 읽는 줄의 4~10%). **1,000줄을 넘으면 임계를 3.5초로**
      올린다 — 단 4.0초 미만은 유지해야 한다(불변식 테스트가 막는다).

### Fix#5 (하루 전략 상한) / Fix#6 (WS 등록·해제 구분)

- [ ] **`WS 구독 확립` 줄 수 ≈ `WS 구독 요청` 줄 수**, `WS 구독 해제 확인` ≈ `WS 구독 해제`.
      08-05에는 확립 1,249 / 해제확인 0줄이었다(해제 1,218건이 전부 "확립"으로 기록됨).
      자동 리포트가 파싱하지 않는 줄이라 **수기 확인**이다.
- [ ] **하루 distinct 진입 전략 ≤ 2** — `hypotheses.yaml` 2026-08-05-p5.
      **ENTER가 3회 이상, 그것도 서로 다른 전략으로 나기 전까지는 공허하게 참이다** —
      "확인"이 떠도 상한이 동작한다는 증거가 아니다. 판정 근거는 단위테스트에 있다.
- [ ] `오늘 사용 전략 조회 실패` WARNING이 뜨는지 — 뜨면 DB 조회 쪽 문제이지 판단 문제가
      아니다(그 사이클은 상한 없이 진행된다).

## [MW0601] 2026-08-05 신규 발견 — 아직 안 고친 것

- [x] **(2026-08-05 구현) `MetaLabelContext()` 전 필드 기본값 — Fix#3.**
      `gamma_regime_stable`을 `GEX>=0 & stability_flag`로 배선(`_build_meta_label_context()`).
      **`event_proximity_minutes`는 여전히 None** — 매크로 이벤트 캘린더 소스가 프로젝트에
      없다. 이것만은 "사실"이 아니라 **미지**이므로 아래에 별도 항목으로 남긴다.
- [x] **(2026-08-05 구현) `already_used_strategies_today` 미전달 — Fix#5.**
      `db.entry_strategies_used_today()` 신설 + 배선. **구현 중 더 근본적인 결함이 나왔다**:
      `enforce_daily_strategy_cap`이 `(continuing+fresh)[:cap]`으로 이번 호출의 길이만 잘랐는데
      §11.4 매트릭스 셀이 전부 1개짜리라 `[:2]`가 자를 일이 없었다 — 인자를 제대로 넘겼어도
      상한은 무력이었다. 남은 슬롯(`cap - len(already_used)`) 계산으로 정정.
- [x] **(2026-08-05 구현) slow-call 임계 > read 타임아웃 — Fix#4.**
      임계 5.0 → 3.0초 + 타임아웃은 임계와 무관하게 무조건 로깅 + **불변식 테스트**로
      `임계 < min(모든 read 타임아웃)`을 기계적으로 강제
      (`test_slow_call_threshold_must_stay_below_every_read_timeout`).
- [x] **(2026-08-05 구현) WS UNSUBSCRIBE ACK를 "구독 확립"으로 기록 — Fix#6.**
      `SubscriptionAck.is_unsubscribe`(msg1 접두사 판정) 신설. 문구뿐 아니라
      **`ws_status.market_op_subscribed_at`(CB 감지 생존 신호)이 해제 응답으로 갱신되던
      잠복 결함**도 함께 닫았다.
- [x] **(2026-08-05 구현) `_record_http_latency()` 국내/해외 `inquire-price` 혼합.**
      `endpoint_label()` 신설(`overseas-inquire-price`) + **모든 `PATH_*`가 서로 다른 라벨을
      내는지 테스트로 강제**. 섞이면 §9-1 위에 얹힌 자동 대응 규칙(p5)이 **항상 실패하는
      해외선물 지연 때문에 엉뚱한 폴러를 줄이게 된다**는 것이 실제 위험이었다.
- [x] **(2026-08-05 구현) 장 개시 전 stale 스팟.** 09:00 이전 적재 중단 +
      신호 경로 신선도 경계 5분. **조사 중 더 큰 것이 나왔다** — `compute_gap_zscore()`
      (v6 §16.1 WARMUP ②)가 "오늘 첫 스팟 − 전일 종가"인데 오늘 첫 스팟이 곧 전일 종가라
      **개시 이래 모든 거래일의 갭이 정확히 0.0000**이었다(8일 전수 + 라이브 호출 확인).
      08-05 실제 갭 +4.29%를 레짐 엔진이 한 번도 못 봤다.
  - [ ] **(08-06) 갭이 실제로 0을 벗어나는지 확인** — 09:00 정각 폴링이 지수 첫 체결보다
        앞서면 그 1분은 여전히 전일 종가일 수 있다(08-05가 그랬고 08-04는 09:00에 이미 실값).
        90분 → 최대 1분으로 줄지만 0은 아니다. **0이 계속되면 09:00 행을 더 걸러야 한다** —
        그때 `compute_gap_zscore()`를 손댈지 결정한다(숫자를 보기 전에 규칙을 더 얹지 않는다).
  - [ ] **(08-06) §12의 `underlying_spot_1m` 행 감소(~90행)를 유실로 읽지 말 것.**
        `option_analysis_1m`은 그대로여야 정상이다 — 둘 다 줄면 그건 진짜 결손이다.
- [ ] **ATM 히스테리시스(08-04 Fix#6) 판정 보류** (08-05 §2 이상점 5) — 즉시 왕복률이
      36.1% → **42.9%로 악화**했다. 다만 08-04에 왕복이 일으키던 피해(체인 50레그/최고령 444초)는
      **신선도 창 축소**가 이미 해소했다(08-05 중앙 10레그/최고령 252초). 08-06 ATM 정합률이
      84.6% 수준을 유지하면 **왕복은 무해로 판정하고 지표에서 강등**할 것. 손대려면 데드밴드
      확대가 아니라 **최소 체류시간(dwell)** 이다 — 08-05 왕복은 미세 지터가 아니라 개장 직후
      실제 스팟 이동(1분에 4.65p)이 만든 것이라 대칭 데드밴드로는 억제되지 않는다.
- [ ] **가설에 "주장 지표" 필수화** (08-05 고도화#2) — 08-04 `p4`는 "왕복이 사라진다"고
      주장하면서 등록 지표가 `chain_age_seconds_max`/`log_volume.human_lines`였다. 자기 주장을
      검정하지 않는 지표로 **"확인" 판정**이 났다. 주장 지표가 없으면 §0이 "판정 불가"로
      표시하게 할 것.
- [ ] **계측 감사에 "임계-물리한계 정합성" 규칙 추가** (08-05 고도화#3) — 현재 감사는
      *"0건 보고인데 로그에 실재하는가"* 한 방향만 본다. 이상점 4는 **로그에도 없어서** 통과했다.
      지표의 발동 임계가 시스템의 물리적 상한(타임아웃·예산·창 크기)보다 크면 경고할 것.
- [x] **(2026-08-05 구현) `event_proximity_minutes` — 사용자 결정 (a) 수기 YAML.**
      `mahdi/config/event_calendar.yaml` + `mahdi/fusion/event_calendar.py`.
      `covered_through`로 **"모른다"(경고)와 "없다"(조용)를 가른다** — 그 구분이 (a)를 고른
      유일한 이유다. 미기입 경고는 `_QUALITATIVE_MARKERS`에 등록돼 리포트 §11에 매일 찍힌다.
  - [ ] **(08-06) 만기 15분 전 페널티 확인** — 08-06은 위클리(목) 만기(15:35 등재)라
        15:20~15:34 확신도가 절반이 돼야 한다. **배선 여부는 이 페널티로 판정한다** —
        `event_calendar_not_covered` 0건으로 판정하려 들면 08-04에 `slow_calls 0건`을
        개선으로 읽은 것과 같은 실수가 된다(covered_through가 08-13이라 0이 정상이다).
  - [ ] **(2026-08-14부터) 매일 미기입 경고가 뜬다** — 끄지 말고 **채울 것**.
        다음 만기·매크로 일정을 넣고 `covered_through`를 옮긴다. 경고가 뜨는 것이 설계 의도다.
  - [ ] **이벤트 직후 구간은 페널티 밖이다**(P2, 별건) — v6 정의가 "다음 이벤트까지 남은 분"
        이라 15:35 만기 직후 15:35~15:45는 안 걸린다. 변동성이 가장 큰 구간일 때가 많다.
        덮으려면 필드 의미를 "가장 가까운 이벤트까지의 거리"로 바꿔야 하는데, 그것은 v6 §11.2
        입력 정의 변경이라 **사용자 결정 사항**이다.
- [ ] **멤버별 점수를 `signal_decisions`에 남길 것** (08-05 고도화#1) — 지금은 가용성만 있고
      점수가 없어, **`OPTIONS_FLOW_GAMMA_WALL_FALLBACK` 유지 여부(사용자 결정 대기)를 데이터로
      답할 수 없다.** 08-05의 방향 ±0.692가 어느 멤버에서 왔는지 DB로 역산이 불가능하다.

---

## 2026-08-05(수) 검증 항목 — 08-04 구현한 Fix 8종

> 예측치는 `docs/동작점검/hypotheses.yaml`의 `2026-08-04-p1~p4`에 등재돼 있고 08-05 자동 리포트
> §0이 대조한다. 상세는 `docs/동작점검/2026-08-04_마흐디_운영점검보고서.md` §8.
> **08-03 5건은 손으로 확정 완료**(refuted 2 / inconclusive 1 / untested 1 / confirmed 1).

- [ ] **계측 감사 줄을 먼저 볼 것** — 리포트 §11 끝의 `✅ 계측 감사` / `⚠ 계측 감사 실패`.
      ⚠가 뜨면 **그날 리포트의 해당 지표를 믿으면 안 된다.** 08-04에 이게 없어서
      "느린 REST 호출 0건(▼933 ✅)"을 개선으로 읽었다(실제 362건).
- [ ] **`available_member_count`가 오전 3 / 오후 4** 인지 — Charm 경로는 14:00부터 열린다.
      4가 안 나오면 `risk_gate_state.member_unavailable`에 사유가 남아 있다(고도화#2).
- [ ] **`rows` 20 미만 사이클이 생기는지** — Fix#8의 **의도된 대가**다. 하나도 없으면 예산이
      안 걸린 것이고, 사이클 절반을 넘으면 임계(50초)가 너무 낮은 것이다.
- [ ] **`qualitative.read_timeout` 증가** — read 10→4초의 의도된 결과. 회귀가 아니다.
      `overrun.count` 감소와 **반드시 나란히** 읽을 것.
- [ ] **ATM 롤링 건수를 손으로 셀 것**(자동 지표에 없다) — 예측 130±20회, 즉시 왕복 ≤ 3회.
- [ ] **`gex_expiry`가 매 분 먼슬리(최근월)로 찍히는지** — 위클리가 섞이면 먼슬리 식별 실패.
- [ ] **`market_op_subscribed_at`** — `3d51fbf`(08-04 07:45 커밋, 그날 07:30 기동이라 미반영)
      검증. 기동 후 60초 내 값 존재. 없으면 마이그레이션 021 미적용.
- [ ] **COCKPIT 육안(브라우저)** — 08-05 장전 기동으로 새로 뜬 프로세스에서:
  - [ ] CB 배지에 "구독확립 HH:MM:SS"(오늘 아침 오경보의 원인이 사라졌는지)
  - [ ] 15:40~15:45 선물 시세 배지가 초록 유지(오경보 재발 — 아직 한 번도 못 봤다)
  - [ ] "관측 품질" 신호 도달률 배지가 앙상블 3~4멤버로 바뀌는지

## 2026-08-05 사용자 확인 대기 — Fix#3(신호 정의 변경)

- [ ] **`options_flow`의 감마월 폴백을 유지할지 결정** — 08-04 보고서 §7 #1.
      "Fix 8종 전부 구현" 지시에 따라 **권장안 (a)로 구현**했으나, 이것은 인프라가 아니라
      v6 §11.3 해석 변경이다. 되돌리려면 상수 하나만 바꾸면 된다:
      `mahdi/fusion/signal_layer.py`의 `OPTIONS_FLOW_GAMMA_WALL_FALLBACK = False`.
      08-05 하루치로 이 멤버가 실제로 유의미한 신호를 내는지 보고 결정한다.

## 판단 파이프라인 회복 로드맵 (2026-08-04 전면 갱신 — 고도화#6)

> 07-31에 세운 로드맵은 **"레짐 엔진이 살아나면 판단이 살아난다"** 는 단일 가설 위에 있었다.
> 08-04에 그 가설이 **불완전**하다는 것이 드러났다: 레짐(`regime_hmm`)은 이미 살아 있었고
> (08-04에 가용 419분), 죽어 있던 것은 **옵션 축과 주문흐름 축**이었다. 레짐만 다양해져도
> 앙상블 멤버가 2개면 판단은 거의 안 바뀐다. 그래서 축을 나눠 다시 적는다.

### 축 A — 앙상블 멤버 (판단의 **분자**)

| 멤버 | 08-04 상태 | 이후 |
|---|---|---|
| `flow_position` | ✅ 494분 가용 | — |
| `regime_hmm` | ✅ 419분 가용(장전 75분만 미가용) | 축 B로 품질 개선 |
| `options_flow` | ❌ **전 이력 0분** | Fix#3(감마월 폴백)+#4(Charm)로 08-05부터 |
| `orderflow_ofi_vpin` | ❌ **전 이력 0분**(데이터는 있었다) | Fix#2 배선으로 08-05부터 |
| `xgboost_tabular` / `lstm_temporal` | ❌ 미학습 | Phase 3 (`trade_history` 0건이라 착수 조건 미달) |

- [x] **(2026-08-05 달성) 가용 멤버 2 → 4.** 494분 중 **409분이 4멤버**(82.8%),
      판단 형태 전이 2줄 → **83줄**, 확신도 4종(NO_TRADE/STANDARD/HIGH_CONVICTION/SMALL_TEST).
      실제 모양은 예상과 달랐다 — "오전 3 / 오후 4"가 아니라 **08:46부터 종일 4**였다
      (`options_flow`가 감마월 폴백으로 상시 활성화됐기 때문이고, Charm 경로는 그 위에 얹혔다).
- [ ] **(08-06~) `options_flow`가 유의미한 신호를 내는지** — 감마월 폴백이 방향을 뒤집어
      노이즈만 더하면 켜지 않는 게 낫다. `OPTIONS_FLOW_GAMMA_WALL_FALLBACK=False`로 즉시 되돌린다.
      **08-05 고도화#4로 이제 데이터로 답할 수 있다**: §14-3의 `options_flow` 부호 분포와
      다른 멤버와의 쌍별 일치율을 본다. 일치율이 극단(≈100% 또는 ≈0%)이면 이 멤버는 다른
      멤버의 복사본이거나 반대부호 복사본이라 **가중치만 바꾸는 셈**이다.
- [ ] Phase 3(XGBoost/LSTM)은 **`trade_history`가 쌓이기 전에는 착수하지 않는다** — 지금
      착수하면 학습 데이터가 없어 또 하나의 영구 None 멤버를 만든다.

### 축 B — 레짐 품질 (판단의 **분모**)

- [x] **(2026-08-04 달성)** 6개 피처 전부 활성 — `rv_ratio` 09:00:49, `cross_asset_stress`
      08:45:50, `book_thinning` 08:47:51에 첫 중립 탈출.
- [ ] **(2026-08-10 월) `feature_store` 8,000샘플 도달** — 08-05 **7,032건(87.9%)**, 일 409건 페이스.
      **⚠ 종전 표기 "(~2026-08-08 금)"은 두 번 틀렸다**: 2026-08-08은 **토요일**이고,
      잔여 968건 ÷ 409 = 2.37일이라 **08-06(목) 7,441 → 08-07(금) 7,850 → 08-10(월) 8,259**다.
- [ ] **(08-07 금 저녁) 드라이런 필수** — 임계 도달 **전 마지막 영업일**이다.
      `--dry-run`으로 (a) 잠재상태 방문 수가 8/8인지 (b) `rv_ratio` 분산이 0을 벗어났는지
      확인한 **뒤에** 저장할 것. `fit()`이 예외를 안 던진다는 것은 성공 기준이 아니다
      (08-03 드라이런에서 비수렴 결함 2건을 그렇게 잡았다).
- [ ] 성공 기준은 **`regime_state`에 RANGE_BALANCED 외 값이 나오는 것**
      (08-05에도 409분 전부 단일 레짐 — **23영업일째 고정**).
      **순서 주의(이제는 충족됐다)**: 축 A가 2개인 상태에서 이 축만 개선되면 판단이 거의 안
      바뀌므로 멤버 4개 확인이 선행 조건이었다. **08-05에 그 조건이 충족됐으므로**
      08-10 이후의 레짐 학습 결과는 판단 변화로 곧장 귀속시켜 읽어도 된다.

### 축 C — 실행 (판단이 살아난 **뒤에** 필요한 것)

- [ ] **`base_size` 하드코딩(1.0)·단위 미정의** — 2026-07-29 기록, **8일째 미해결**.
- [ ] **재진입 방지 로직 부재** — 같은 날짜 기록, 같은 상태.
- [ ] 위 둘은 **실거래(CONFIRM/FULL_AUTO) 전환의 선행 조건**이다. 지금은 ADVISORY 전용이라
      드러나지 않지만, 축 A·B가 회복되어 ENTER가 실제로 나기 시작하면 곧바로 문제가 된다.

### 관측 (축 A·B·C가 진짜로 나아졌는지 재는 눈)

- [x] **(2026-08-04) 지표 사이의 빈 칸 3개를 메웠다** — §14-1 멤버별 가용성(어느 멤버가 왜
      죽었나), §14-2 행사가 창 품질(수집한 행사가가 맞는 행사가였나), §9-1 KIS 응답시간
      (밀림의 90%가 어디서 오나).
- [ ] **(08-05) 새 세 절이 실제 값을 내는지 확인** — §14-1과 §9-1은 08-04 로그에 계측이 없어
      "계측 전"으로 표시된다. 08-05부터 값이 나와야 한다. **안 나오면 배선 실패다.**
- [x] **(2026-08-05 저녁) 관측의 층이 하나 올라갔다 — "지표를 늘리는 것"에서 "지표끼리 맞춰보는 것"으로.**
      §17 교차 점검(고도화#3)이 08-05 데이터에서 **모순 3건**을 자동으로 집어냈고, 그중
      §14↔§15(광폭은 flip 없음인데 라이브는 22분)는 §2-5를 **사람이 22건 전수 대조하기 전에**
      잡을 수 있었던 것이다. 지표는 이제 17개 절로 충분히 많다 — 다음 진화는 대조 규칙이다.
- [ ] **(08-06) §14-3(멤버 점수)·§17(교차 점검)·§14-2 소스 간 격차가 값을 내는지.**
      §14-3은 08-05에 `member_scores` 키가 없어 "계측 전"이다. 08-06부터 값이 나와야 한다.
- [ ] **(08-06~) 쌍별 부호 일치율의 정상 범위를 모른다** — 며칠 쌓기 전에는 임계를 정하지 않는다
      (08-05 Fix#6에서 "0.5% 괴리" 임계를 정상 베이시스에 걸었다가 폐기한 것과 같은 실수를
      반복하지 않기 위함). **값만 보고, 임계는 사람이 나중에 정한다.**

## 이월 — 기타

- [ ] **`CURRENT_STATE.md`가 2026-07-10 이후 미갱신** — 4주째 이월.

## 폐기·종결된 안건 (다시 올리지 말 것)

- [x] **「GEX 광폭 체인」 — 2026-08-04 폐기.** 결정 규칙(`7716dd4`, 숫자 보기 전 확정) 적용
      결과 (A)(B) 모두 불통과이고, **더 근본적으로는 광폭 체인이 감마플립을 살리지 못한다는 것이
      실측으로 확인됐다**: 08-04 ATM 지터가 만든 25행사가(±3%)와, 구현 후 `find_gamma_flip`을
      방문 행사가 전 폭(±4.75%)으로 돌린 결과 **세 북 모두 flip 없음**.
      원인은 행사가 창이 아니라 **딜러 포지션이 전 구간 한 방향**인 것이다.
      → 재개 조건: 자동 리포트 §15의 **광폭 감마플립이 "없음"에서 벗어나는 날**.
      그 지표는 이제 매일 자동으로 남는다(추가 REST 0건).
- [x] **예산 규칙 (B)의 개정** — 08-04 §4. `백오프 최대`(하루 최대값, 445분 중 2분만 적자
      임계 초과)를 쓰면 어떤 안도 통과 못 한다. **다음 결정 전에** `백오프 p95 < 적자 임계`
      + `적자 초과 사이클 비율 < 2%`로 바꿀 것 — 숫자를 보기 전에 확정하는 규약 유지.
- [x] **Slack 알림 — 2026-08-01 결정, 보류 유지.** 실거래(CONFIRM/FULL_AUTO) 전환 검토
      시점에 자동으로 재검토 대상이 된다. **매 점검 보고서에서 다시 올리지 말 것.**
- [x] **폴러별 전용 페이서 분리 — 2026-08-01 기각.** 재개 조건: 밀림이 30건/일 이상 남고
      그 원인이 국소 충돌이 아니라 **총수요로 귀속**될 때. 2026-08-04 §2-6으로 원인이
      **KIS HTTP 지연(90%)** 으로 귀속됐으므로 이 조건은 더 멀어졌다.
- [x] **httpx INFO 로그 축소 — 보류 확정(2026-08-04).** 로그의 76%지만, §2-6과 고도화#5가
      이 로그에 의존하게 됐다. `rate_limit_total_calls` 기반 대체 계측을 먼저 갖출 것.

---

## (이하 2026-08-03 이전 기록 — 아카이브)

## 2026-08-03(월) — 자동 집계 도구 최초 실행일 (⚠ 검증일과 겹친다)

- [ ] **자동 집계 vs 수작업 대조** — 08-03은 §4 Fix 5종 판정일이자 `daily_ops_report.py`의 최초
      실행일이다. **도구 버그와 지표 변화를 구분할 수 없는 날**이므로, 그날만 자동 집계와 수작업
      집계를 나란히 놓고 대조한다(도구 검증 겸). 어긋나면 그 자리에서 파서를 고치고, **08-04부터
      수작업을 뗀다.**
- [ ] 산출물 확인: `docs/동작점검/auto/2026-08-03_지표.{md,json}`이 장마감 훅으로 자동 생성됐는지.
      리포트 §0에 `hypotheses.yaml`의 예측 6종이 실측과 나란히 나오는지.
- [ ] **`hypotheses.yaml`의 `상태`를 손으로 확정할 것** — 자동으로 바뀌지 않는다(설계 의도).
- [ ] COCKPIT "오늘의 점검 요약"이 2행(인프라/관측 품질)으로 보이는지, 관측 품질 5종이 장중에
      실제 값을 표시하는지 **브라우저 육안 확인**(07-06부터 이월 중인 항목과 함께).
- [ ] 마이그레이션 018/019/020이 장전 기동 스크립트로 자동 재적용되는지(이미 라이브 수동 적용됨).

## 2026-08-03(월) 검증 항목 — 08-01 구현한 Fix 5종 (07-31 운영점검 후속)

> 근거 표는 `docs/동작점검/2026-07-31_마흐디_운영점검보고서.md` §4. **"가설 → 예측치 → 판정
> 기준"을 구현 시점에 적어두는** 방식이 07-31 §2-0 검정을 가능하게 했으므로 이번에도 같은 형태로 남긴다.

- [ ] **우선순위 1 (만기유동성 버스트 분할)** — 예측: 시작분 mod10=2 구간의 "창 안 만기유동성
      호출" 평균 13.4 → **1.0 이하**, 그 구간 밀림 17 → **5건 이하**. `expiry_liquidity_1m`는
      43~44행 유지하되 **북별 timestamp가 1/3/5분대로 갈려야** 한다(같은 분에 3행이면 미적용).
- [ ] **우선순위 2 (먼슬리 캐치업)** — 예측: 먼슬리(2026-08-13 등 최근월) 분 커버리지
      90.5% → **95% 이상**, 총 REST 0.436 → **0.46건/초 이하**. 로그의 `옵션체인 결손 회수` 건수가
      밀림 건수와 비슷해야 한다. **회수 로그가 적게 나오는 건 실패가 아니라 성공 신호**다
      (우선순위 1이 밀림 자체를 줄이면 발동 횟수도 준다) — 두 지표를 함께 읽을 것.
- [ ] **우선순위 3 (REST 지연 분리 계측)** — `느린 REST 호출 N초 = 페이서대기 A + HTTP B` 로그를
      mod10=0 구간에서 수집해 **§2-1(b) 8~9초 정체의 원인을 확정**한다.
      페이서대기 크면 → 예약 큐 경합 / HTTP 크면 → KIS 서버·커넥션 풀(`httpx.Client` 공유,
      `max_connections` 미지정) / 둘 다 작으면 → 이벤트 루프·스레드풀 블로킹.
      **결과를 보기 전에는 아무것도 손대지 않는다.**
- [ ] **우선순위 4 (CB 생존 신호 분리)** — 예측: `market_halt_status.updated_at`이 15:44 근처까지
      300초 간격으로 갱신(≈98회). `last_message_at`은 09:00 부근에서 멈춰 있어도 **정상**이다.
      COCKPIT 배지에 "관측루프 HH:MM:SS · 최근 장운영정보 HH:MM:SS" 두 시각이 나오는지 육안 확인.
- [ ] **우선순위 5 (벽시계 위상 앵커)** — 예측: 실측 위상이 설계 표와 **±2초 이내** 일치.
      로그 타임스탬프의 "분 안의 초" 분포로 확인 — 옵션체인 0초 / 만기유동성 15초(1·3·5분) /
      투자자수급 40초 / 매크로 48초(2·7분) / 계좌잔고 48초(4·9분).
- [ ] **종합** — 밀림 46건 → ? / 결손 47분 → ? / EGW00201 64건(0.49%) → ? /
      백오프 최대 2.25배 → ? 를 07-31과 나란히 집계한다.

## 이월 검증 항목 (07-30 Fix 6종 중 미완)

- [ ] **Fix#5 자연 해소 시점(2026-08-04 예상)** — 정규장 기준 유효 종가가 **19개**(07-31 기준) →
      21개가 되는 날부터 `feature_store.rv_ratio`가 1.0이 아닌 값을 갖기 시작해야 한다.
      08-03(월)·08-04(화)를 채우면 충족. 실제로 바뀌는지 확인.
- [ ] **HMM 학습 임계(2026-08-10경)** — `feature_store` 5,803건(07-31) → 8,000건.
      `scripts/fit_regime_engine.py` 최초 실행 → `data/models/regime_engine.pkl` 생성 →
      자기참조 폴백 탈출. **성공 기준은 `regime_state`에 RANGE_BALANCED 외 값이 나오는 것**
      (19영업일 100% 고정 중).
  - [ ] **선행 준비**: `fit_regime_engine.py`를 지금 데이터로 **드라이런**해 샘플 부족 외의
        오류(스키마 불일치, 피처 순서, NaN 처리)를 미리 걷어낼 것 — 08-10에 처음 돌려서 실패하면
        또 하루를 잃는다.
- [x] **(2026-07-31 확인 완료) Fix#1** — ENTER 419 → **0건**, `strategy_palette:wait_only` 418건.
- [x] **(2026-07-31 확인 완료) Fix#6** — `risk_snapshots` 495행(사이클과 1:1).
      읽기 함수 추가 시 `loss_buffer`(DECIMAL → Decimal) `float()` 변환 잊지 말 것.
- [x] **(2026-07-31 확인 완료) Fix#4 최초 관측** — 수신 로그가 남았고, 그 결과 **H0UNMKO0은 세션
      전이 시에만 온다**(하루 2건)는 사실이 확인됐다. 그래서 "N분 미수신 = 경고" 임계는
      **넣지 않기로 확정**했다(넣으면 상시 오경보). 대신 생존 신호를 독립 하트비트로 분리(08-01 구현).

## 2026-07-31 사용자 결정 필요 (07-30 운영점검 §4/§5-2)

- [x] **(2026-08-01 결정) Slack 알림 — 별도 지침 전까지 보류 유지.**
      `slack_alert_settings.enabled = false`(2026-07-28부터). 사용자 판단: **개발 단계이므로
      개발자가 항상 프로그램을 모니터링 중**이라 별도 푸시 경로가 필요하지 않다.
      → **이 항목을 매 점검 보고서에서 "사용자 결정 대기"로 다시 올리지 말 것.**
      실거래(CONFIRM/FULL_AUTO) 전환 검토 시점에 자동으로 재검토 대상이 된다 —
      그때는 사람이 화면을 안 보고 있는 시간이 생기기 때문이다([[DECISION_LOG]] 2026-08-01 참고).
- [x] **(2026-07-31 결정·구현 완료) 총 REST 수요 축소안 — 사용자가 (a)+(c) 조합 승인**.
      (a) 위클리 2북 격분(2분) 폴링 + (c) 만기유동성 300→600초. 07-30 실측 기준 재계산 결과
      **0.663 → 0.451건/초(용량의 45%), 적자 시작 배율 1.51배 → 2.22배, 총 -32%**.
      (b) ATM±2→±1은 유일하게 신호 계산 입력을 훼손하는 안이라 **마지막 수단으로 보류** —
      판단 무반응 문제를 푸는 중에 판단 입력을 좁히는 건 방향이 반대다.
- [x] **(2026-07-31 추가 구현) 매크로 항목별 갱신 주기 분리** — ZN·ES의 KIS 시도 1시간,
      US10Y·USDKRW·MOVE 6시간, ES 값/VIX/USDCNH는 5분 유지. 누적 **0.663 → 0.438건/초(43.8%),
      적자 임계 2.28배**. ZN/ES 실패 로그 198 → 18건/일이라 "트레이스백 축약" 안은 폐기했다.
  - [ ] **다음 거래일 검증**: `macro_snapshot_5m`에서 zn_front/us10y_yield/usdkrw/move_index가
        의도한 주기(1시간/6시간)에만 non-null인지, es_front/vix/usdcnh는 매 사이클 채워지는지.
        COCKPIT 매크로 패널이 LOCF 덕에 빈칸 없이 보이는지 육안 확인.
  - [ ] ZN 이중실패 알림의 벽시계 의미가 10분 → 2시간으로 바뀌었다(스트릭은 조회 사이클에서만
        증감). 실제로 오경보 없이 동작하는지 확인.
  - [ ] **다음 거래일 검증**: 초당 수요가 실제로 0.44 근처인지(로그 HTTP Request 수 ÷ 관측 분),
        백오프 배율이 2.22배를 넘는 구간이 있는지, 위클리 `option_analysis_1m` 행이 정확히
        격분에만 쌓이는지(먼슬리는 매분), 옵션체인 사이클 유실이 25분에서 줄었는지.
  - [ ] **(a)+(c)로도 07-30 최대치 2.61배는 못 덮는다**(적자 임계 2.22배). 그 피크 자체가 적자
        악순환의 결과라 Fix#2·#3과 함께 내려갈 것으로 보지만, 다음 거래일에도 2.22배를 상시
        넘으면 (b) 또는 페이서 분리를 다시 논의해야 한다.
- [x] **(2026-07-31 해결) ZN/ES 실패 로그** — 로그를 축약하는 대신 **호출 주기 자체를 1시간으로**
      바꿔 198 → 18건/일이 됐다. 진단 정보(트레이스백)는 그대로 남기고 잡음만 사라졌다.
      부수 정정: "하루 12.8MB"는 오류였다(실제 4.77MB, 11:52 회전은 07-28부터 이틀치 누적 때문).
      로그 용량의 실제 주범은 **httpx 요청 로그 87.1%(4.16MB, 19,610건)** 다.
  - [ ] **httpx INFO 로그 축소 검토(신규)** — REST 호출을 한 건씩 전부 INFO로 남기는 게 로그의
        87%다. `logging.getLogger("httpx").setLevel(WARNING)`으로 끄면 하루 4.16MB가 사라지지만,
        지금까지의 조사(엔드포인트별 호출 수 집계, 타폴러동시호출 검증)가 전부 이 로그에 의존했다
        — 대체 계측(`rate_limit_total_calls` 등)을 먼저 갖춘 뒤에 검토할 것.

## 2026-07-31 판단 파이프라인 회복 로드맵 (07-30 운영점검 §5-1 — 최우선)

레짐 엔진이 개시 이래 18영업일 100% RANGE_BALANCED이고 `rv_ratio`도 전 이력 중립값이라, 마흐디의
"판단"은 현재 시장 반응성이 사실상 0이다. Phase 2(Execution/Backtest)를 더 쌓기 전에 이 축부터
회복시키는 것이 순서다.

- [ ] (~2026-08-04) `rv_ratio`가 21일 임계를 넘어 실제 값으로 전환 — 6개 피처가 전부 살아있는 첫 시점.
- [ ] (~2026-08-11) `feature_store` 8,000샘플 도달 → `scripts/fit_regime_engine.py` 최초 실행 →
      `data/models/regime_engine.pkl` 생성 → 자기참조 폴백 탈출.
      (2026-07-31 현재 5,394건/14영업일 = 67%)
- [ ] 그 시점의 성공 기준은 **레짐 분포가 실제로 여러 값을 갖는지**(RANGE_BALANCED 100%가 깨지는지).
- [ ] `base_size` 하드코딩(1.0)·단위 미정의, 재진입 방지 로직 부재 — 07-29에 기록된 "실행 전 필수
      해결" 2건은 여전히 미해결. Fix#1로 잘못된 ENTER는 사라졌지만 이 둘은 별개 항목이다.

## 2026-07-31 관측성/기록 후속

- [ ] **`scripts/daily_ops_report.py` 신설 검토**(§5-5) — 이번 보고서의 근거(시간대별 REST/밀림 표,
      mod-5 클러스터 표, 데이터 결손 표, 레짐/판단 분포)는 전부 재현 가능한 SQL·로그 집계다.
      장마감 후 자동 실행하면 9회째 반복되는 수작업 전수 분석이 "표를 읽는" 작업으로 줄어든다.
- [ ] **`CURRENT_STATE.md`가 2026-07-10 이후 미갱신**(§5-6) — 그 사이 Phase 2 Risk Engine /
      Signal Fusion / Execution Engine / Backtest / 계좌 추적기 / CB 감지가 전부 추가됐다.
- [ ] **COCKPIT 브라우저 육안 검증** — 07-29 신규 패널(판단현황/계좌현황)과 CB 배너가 실제로
      어떻게 보이는지 아직 확인 안 됨(07-06부터 반복 이월). 07-31에 판단현황이 "진입 없음 +
      wait_and_see (관망)"으로 바뀌는지 함께 볼 것.

---

## 2026-07-28 ZN 13:01 패턴 근본원인 조사 + fix 구현 완료 — 다음 재발 여부로 가설 최종 확인 필요

**배경**: KIS ZN 조회는 CBOT 미구독으로 하루 종일 항상 실패(시간 무관) — "13:01 이중실패"의 실제
방아쇠는 전적으로 yfinance 쪽이었다. `yfinance_fallback.fetch_last_close()`가 `interval="1m"`으로
ZN=F/ES=F(거의 24시간 거래)를 조회해 매 5분 사이클마다 1,000행 넘는 1분봉을 통째로 다시 받아온 걸
확인(실측 ZN 1,046행/ES 1,153행) — 이 반복적 과다 요청이 Yahoo의 비공식 레이트리밋을 매일 비슷한
누적 시점(부팅 후 약 5.5시간=13:01 전후)에 건드렸을 가능성이 유력한 가설. `interval="1m"`을 제거해
일봉 기본값(당일 1행, 같은 Close값 확인)으로 줄이는 fix를 구현·라이브 검증까지 완료(DB에서
`zn_front`/`es_front`가 `yfinance_fallback` 소스로 정상 적재됨을 확인 — [[DECISION_LOG]] 2026-07-28
항목 참고). 부가로 `PriceHistory._last_error`를 읽어 yfinance 내부 실패 사유(레이트리밋 status_code
등)를 mahdi 로그에 노출하는 진단 계측도 추가.

- [ ] **다음 정규 거래일에 13:01 패턴이 재현되는지가 가설의 최종 확인 기준** — 재현 안 되면(또는
      재현되더라도 새 진단 로그에 `_last_error`가 "(Yahoo status_code = 429)" 같은 레이트리밋
      증거를 남기면) 가설이 최종 확정된다. 반대로 여전히 재현되면서 `_last_error`가 다른 사유를
      가리키면(예: 네트워크, 진짜 데이터없음) 이 fix로는 부족하다는 뜻이니 원인을 다시 좁혀야 한다.
      오늘(07-28)은 개발 세션 중 재시작 4회가 껴 있어 자연스러운 하루 흐름으로 볼 수 없다 — 반드시
      다음 "온전한" 거래일 기준으로 판단할 것.
- [ ] ZN 외 다른 곳에서도 `history(period="1d", interval="1m")`처럼 필요 이상으로 넓은 범위를
      조회하는 yfinance/외부 API 호출 패턴이 더 있는지 점검(현재는 ZN/ES/MOVE 세 심볼 모두
      `yfinance_fallback.fetch_last_close()` 하나를 공유해 이번 fix로 셋 다 함께 가벼워짐 —
      MOVE는 원래도 1행만 오던 심볼이라 영향 없음).

---

## 2026-07-28 스케줄 밀림 fix 후속 — 스태거링 하루치 재확인 + 보류 후보 2개 지속 관찰

**배경**: 07-27 재수사로 스케줄 밀림의 실제 원인이 4개 폴러(poll_option_chain/poll_investor_flow/
poll_expiry_liquidity/poll_macro_snapshot)가 공유하는 단일 `_RateLimiter` 동시 접촉임을 확정하고,
같은 날 poll_investor_flow(+15초)/poll_macro_snapshot(+45초) 시작 지연(스태거링)을 구현·라이브
재시작으로 반영했다(정상 상태 타폴러동시호출 3건→0건, REST수집 32.0초→29.0초로 개선 확인, 3연속
사이클 실측 — [[DECISION_LOG]] 2026-07-28 항목 참고). fix 후보 3개 중 나머지 2개는 위험/스코프
이유로 이번엔 미구현.

- [ ] **스태거링 효과를 하루치 데이터로 재확인** — 07-27 보고서 기준 스케줄 밀림 101건 대비 다음
      정규 거래일에 실제로 줄었는지, 밀림 WARNING의 "타폴러동시호출추정"이 여전히 0에 가깝게
      유지되는지 확인 필요(오늘 07-28은 개발 세션 중 재시작 3회로 데이터가 불연속이라 온전한 하루치
      비교 불가 — 다음 완전한 거래일 기준으로 봐야 함).
- [x] **폴러별 전용 페이서 분리 — 2026-08-01 "기각"으로 종결** (07-31 §5-3, [[DECISION_LOG]] 참고).
      07-28에 "스태거링만으로 부족함이 하루치로 확인되면 재검토"라는 조건을 걸었고 표면적으로는
      충족된 듯 보였으나, **07-31 하루치 실측이 반대 방향의 답을 줬다**: 총수요 43.6%로 용량은
      남고(백오프 최대 2.25배 < 적자 임계 2.28배 — 하루 종일 구조적 적자에 안 들어감), 남은 밀림
      46건은 전부 국소 원인 2개(만기유동성 버스트 17건 + mod10=0 정체 16건)로 귀속되며 **둘 다
      페이서를 나눠서 해결되는 문제가 아니다**. 2026-07-08 페이서 분리 → 500 폭주 → 203분 유실
      전례의 위험은 그대로다.
  - [ ] **재개 조건(새로 명시)**: §4 우선순위 1·3 적용 후에도 **밀림이 30건/일 이상** 남고,
        그 원인이 국소 충돌이 아니라 **총수요로 귀속**될 때. 그때는 KIS 실제 한도가 앱키 총합인지
        TR ID/엔드포인트별인지 확인하는 선행 조건도 함께 다룬다(그 확인 없이 라이브 적용 금지).
- [ ] **ATM±2→±1 축소(콜 수 30→18) — 사용자 결정 필요, 보류 중** — 순수 인프라 조치가 아니라
      실제 수집 데이터 범위(외곽 행사가 2개, GEX/Vanna/Charm 신호에 영향)를 줄이는 제품 결정이라
      코드만 보고 임의로 진행하지 않는다. 스태거링 효과가 부족하다고 판명되고 페이서 분리도
      부적절하다고 결론 나면, 이 옵션을 사용자에게 명시적으로 물어보고 진행할 것.

---

## 2026-07-24 fix 후속 확인 (다음 거래일 2026-07-27 월)

- [x] **`RATE_LIMITER_STATUS_WRITE_SLOW_SECONDS`(0.2초) 경고 확인 완료(2026-07-27)** — 07-27
      실전 첫 검증에서 경고 0건 확인, 스케줄 밀림 원인 가설(동기 DB 쓰기)이 반증됨. 근본원인은
      이후 2026-07-28 세션에서 공유 `_RateLimiter` 동시 접촉으로 최종 확정([[DECISION_LOG]]
      2026-07-28 항목 참고).
- [x] **ZN 이중 실패 Slack 알림 연속 2회 조건 확인 완료(2026-07-27)** — 3사이클 연속 실패 중
      알림이 정확히 두 번째(13:06:38)에 뜸을 확인, 스트릭 조건이 설계대로 알림 시점을 지연시킨
      직접 증거. ZN 13:01 패턴 자체의 근본원인은 2026-07-28 세션에서 yfinance 과다요청으로
      확정·fix 완료([[DECISION_LOG]] 참고).
- [x] **COCKPIT 레이트리밋 근접도 배지 브라우저 픽셀 렌더링 확인 완료(2026-07-28)** — 사용자가
      실제 COCKPIT 스크린샷으로 확인: 10번째 배지가 `09:34:00 기준 배율 1.00배, 직전 사이클
      밀림 0.0초`로 정상 출력, 배율 1.00 ≤ 임계값(1.01)이라 ok(녹색) 렌더링도 코드와 일치.
      CBOT(4번)·레짐 stability_flag(7번)·레짐 엔진 학습 데이터(8번) 배지도 코드상 info 상태와
      실제 파란색 렌더링이 일치함을 사용자가 직접 확인(특히 CBOT 배지 색상 재확인 요청 후
      "파란색으로서 정상" 확답 받음). 07-23부터 4번 이월됐던 항목 종결 — 배율이 1.0을 넘는
      warning 색 전환 자체는 이번에 백오프가 없어 못 봤으니, 다음에 실제 백오프 중 우연히
      보게 되면 완전 검증으로 추가 기록.

---

## 2026-07-22 fix 후속 확인 (다음 세션)

- [x] **Docker Desktop 기동 후 라이브 DB 직접 대조 완료(같은 날 3차 세션)** — `option_analysis_1m`
      부모 테이블뿐 아니라 **14개 청크(하이퍼테이블 chunk) 전부** `atttypmod` 직접 조회로
      `NUMERIC(14,4)`(917512) 일치 확인 — migration 012가 과거분 청크까지 완전히 전파됐고
      잔존 오버플로우 위험 없음을 확정(1차 세션에서 로그로 "오늘 0건"까지만 봤던 걸 스키마
      레벨로 재확인). `shutdown_check_log`엔 07-22 15:45:05 기록 1건(remaining_process_count=0)
      — 오늘 장마감 종료가 정상 기록됨. 오늘 옵션 데이터도 근접 위클리(07-23·07-27) 둘 다
      월물(08-13)과 비슷한 행수(~4,800)로 정상 적재 확인 — 07-21 보고서가 지적했던 "근접
      만기 전량 유실"이 완전히 해소됐음을 실측으로 재확인.
- [x] **"기동 전 잔존 프로세스 정리" 블록 실전 확인 완료(2026-07-24)** — `premarket_startup.log`
      07-24 07:30:00.44에 정상 출력, 잔존 프로세스 없음. 2일 연속(07-23/07-24) 정상.
- [x] **레이트리미터 회복 임계값(8) 재조정 후 며칠 더 실측(2026-07-24 완료·정정)** — 07-23
      하루치 실측 결과 8로 낮춘 게 오히려 역효과였음을 확인(EGW00201 비율·스케줄 밀림·평균/최대
      지연 전부 07-22보다 악화, 운영점검보고서 2026-07-23 §2-1). 20으로 원복 + 백오프 상태전이
      로깅 신규 추가([[DECISION_LOG]] 2026-07-23 항목 참고).
  - [x] **다음 거래일(07-24) 확인 완료**: 20으로 원복한 뒤 EGW00201 비율이 정확히 07-22 수준
        (0.38%)으로 복귀함을 확인. 단, **스케줄 밀림 건수는 오히려 3일 연속 최고치(115건)**를
        기록 — 임계값과 무관한 별도 원인 의심(운영점검보고서 2026-07-24 §2-1, `record_rate_
        limiter_status()` 동기 DB 쓰기를 후보로 지목해 계측 로깅 추가, 위 2026-07-24 절 참고).
        신규 추가한 "레이트리밋 백오프 확대/회복" 로그도 실전에서 정상 출력 확인(확대 70건·
        회복 280건, 최대 배율 2.25배).
  - [x] **COCKPIT "레이트리밋 근접도" 배지(2026-07-24 신규, 10번째 배지) 브라우저 렌더링 확인
        완료(2026-07-28)** — 위 "2026-07-24 fix 후속 확인" 절 참고, 사용자 스크린샷으로 최종
        확인.
  - [x] **`db/migrations/014_rate_limiter_status.sql` 라이브 적용 확인 완료(2026-07-24)** —
        07-24 07:30:31.02 `premarket_startup.log`에 "적용 완료 - 014_rate_limiter_status.sql"이
        `CREATE TABLE`(최초 생성, "already exists" 아님)로 정상 출력됨.
- [x] **ZN(10년 국채선물) KIS·yfinance 이중 실패 13:01 패턴 3일 연속 재현 확정(2026-07-24)** —
      07-22 13:01:29 / 07-23 13:01:24 / 07-24 13:01:28로 3일 연속 5초 오차 내 재현돼 우연이
      아닌 것으로 확정(운영점검보고서 2026-07-24 §2-3). 근본 원인(KIS/yfinance 쪽 정기 요인)은
      여전히 미확인 — 완화책으로 알림 조건을 연속 2회(10분) 실패로 강화함(위 2026-07-24 절
      Fix#3 참고). 근본 원인 조사는 계속 열린 항목.
- [x] **COCKPIT 기동 마커(`logs/.last_cockpit_start.txt`) 실전 확인 완료(2026-07-24)** — 07-24
      07:30:37.728 Uvicorn 기동 직후 "직전 COCKPIT 기동: 2026-07-23 07:30:47 (24.0시간 전)"
      정상 출력(마커 도입 후 2번째 실행에서 기대한 "경과시간 포함" 형태로 정상 동작 확인).

---

## 레짐(Regime) 실데이터 파이프라인 — 2026-07-10 배선 완료, 재시작 및 후속 확인 필요

- [x] 관측 루프 재시작 — 2026-07-10 중 Cross-asset stress 작업으로 여러 차례 재시작됨(아래 새 절 참고).
- [ ] 큰 갭이 있는 날 `compute_gap_zscore` 기반 TREND_UP/DOWN·VOL_EXPANSION·CRISIS_DEFENSE
      전환이 실제로 동작하는지 확인. 갭이 작은 평상시엔 전일 마감 레짐을 그대로 물려받는 게 정상 동작.
- [ ] `feature_store`에 §7.3 6개 피처가 매분 실제로 쌓이는지 DB로 확인(`symbol="KOSPI200"`,
      `feature_version="v1"`).
- [ ] `feature_store`가 20영업일 이상(대략 8,000행) 쌓이면 `python scripts/fit_regime_engine.py` 실행 →
      `data/models/regime_engine.pkl` 생성 → 다음 재시작부터 `RegimeStateMachine`이 자동으로 predict()
      모드로 전환되는지 확인.
- [x] **`cross_asset_stress` 실계산 배선 완료(2026-07-20)** — 더 이상 0.0 고정 스텁이 아니다.
      `mahdi/features/regime_features.py`의 `cross_asset_stress(usdkrw_daily_series,
      usdcnh_recent_series, us10y_daily_series)`가 세 시퀀스 각각에 `book_thinning`과 동일한
      z-score(`_series_zscore`로 추출·공유)를 구해 절대값 평균을 낸다. `RegimeStateMachine.step`이
      `db.recent_usdkrw_daily_series`(일봉, 최근 10거래일)·`db.recent_usdcnh_series`(5분 스냅샷,
      최근 24개=2시간)·`db.recent_us10y_daily_series`(일봉, 최근 10거래일)를 매 선물봉마다 조회해
      넘긴다. USDKRW/US10Y는 거래일당 값이 하나뿐이라 "급변"이 거래일 단위 day-over-day 변화로
      측정되고, USDCNH는 5분 주기라 인트라데이 변동을 그대로 반영 — 이 refresh 주기 차이를 그대로
      살린 설계다. `feature_store`에 실제로 유의미한 값이 쌓이는지는 정규장 운영 후 확인 필요.
- [x] **`macro_score`(`compute_macro_score_proxy`) 복합 신호로 확장 완료(2026-07-20)** — 함수
      이름·시그니처는 [[DECISION_LOG]] 2026-07-10 결정("시그니처는 유지하고 내부만 교체")대로
      유지, 내부 계산만 교체함. 기존 외국인 순매수 부호(K-market 수급)에 4개 신호 추가:
      VIX 기간구조 부호(백워데이션=위험회피/콘탱고=위험선호), USDKRW/USDCNH 추세(상승=자국통화
      약세=위험회피로 부호 반전), ES(S&P500 선물) 추세(상승=위험선호). 각 신호는 -1/0/+1로
      정규화해 "존재하는 신호만" 평균(데이터 없는 신호는 분모에서도 제외). US10Y·MOVE는 방향이
      위험선호/회피로 명확히 매핑되지 않아(수익률·변동성 급등이 맥락에 따라 다름) 제외 —
      그 "급변 크기"는 이미 `cross_asset_stress()`가 별도로 포착 중. `db.recent_es_front_series`
      신규 추가(`recent_usdcnh_series`와 동일 패턴). `mahdi/engines/regime_pipeline.py`에
      `_directional_sign`/`_trend_sign` 헬퍼 추가.
- [ ] `rv_ratio`(RV5d/RV20d)는 선물 심볼이 분기 롤오버될 때마다 일별 종가 이력이 끊긴다(현재 심볼
      기준으로만 `daily_closes` 조회) — 롤오버 연속성 처리 필요 여부 검토.

## Cross-asset stress 매크로 스냅샷 — 2026-07-10 구현, ZN은 2026-07-20 yfinance 폴백으로 전환

- [x] **CBOT EGW00552 원인 확정** — 2026-07-20 HTS [7936](해외선물옵션 거래소 실시간시세신청/조회)
      실측 확인: CME|CBOT는 **API(무료) 탭에 아예 없고 API(유료) 탭에만 있음** — 기간이용료
      **월 228.8불**. "2026-07-10 신청 완료"로 믿었던 것은 무료 탭 기준이었을 가능성이 높고, 실제로는
      유료 구독이 성립한 적이 없었던 것으로 보임(그래서 6일 넘게 EGW00552가 계속 남).
- [x] **당분간 KIS CBOT 유료 구독 안 함 — yfinance 폴백으로 대체(2026-07-20)** — 모의투자 개발
      단계에서 월 228.8불을 지불하는 건 시기상조라는 사용자 결정. `mahdi/data/yfinance_fallback.py`
      신규(`ZN=F` yfinance), `main.py._collect_macro_snapshot_cycle`이 KIS 조회 실패 시에만 호출.
      `macro_snapshot_5m.zn_front_source`("kis"|"yfinance_fallback"|NULL, migration 010)로 출처
      구분 — COCKPIT [_cbot_status_check](mahdi/dashboard/data_source.py)·
      [macro_panel](mahdi/dashboard/panels/macro_panel.py)에 "(폴백)" 표시로 실제 CBOT 체결가와
      혼동되지 않게 함. **정식 운영 전환 시(또는 안정화 후 유료 구독 결정 시) 재검토 필요** — KIS가
      나중에 성공하기 시작하면 코드 변경 없이 자동으로 KIS 우선 사용된다.
- [x] **v6 "글로벌 확인 신호" 나머지 항목(S&P500 선물·MOVE·USDKRW) 점검 및 수집 배선(2026-07-20)** —
      마스터파일 실측으로 셋 다 KIS 수집 가능 여부를 확정함:
      - **USDKRW**: KIS `frgn_code.mst` 실측 확인 — 환율구분 `FID_COND_MRKT_DIV_CODE="X"`,
        심볼 `FID_INPUT_ISCD="FX@KRW"`로 US10Y와 **완전히 동일한 무료 엔드포인트**
        (`inquire-daily-chartprice`)에서 계좌 게이트 없이 바로 얻어짐 → KIS로 즉시 구현
        (`macro_snapshot_5m.usdkrw`, us10y_yield와 동일하게 일봉 LOCF).
      - **ES(S&P500 E-mini 선물)**: KIS `ffcode.mst` 실측 확인 — 상품코드 `ES`/거래소코드 `CME`
        (ZN의 CBOT와 다른 서브거래소지만 HTS [7936]상 "CME|CME"도 동일하게 유료 월 228.8불) →
        ZN과 동일한 yfinance 폴백 패턴(`ES=F`) 적용(`macro_snapshot_5m.es_front`/`es_front_source`).
      - **MOVE(ICE BofA MOVE Index)**: 마스터파일 어디에도 없음(장외 파생 인덱스라 거래소 상장
        상품 자체가 아님) → KIS 경로 없음, yfinance(`^MOVE`) 전용 폴백만 가능
        (`macro_snapshot_5m.move_index`/`move_index_source`, 항상 "yfinance_fallback").
      `zn_fallback.py`는 3개 심볼(ZN/ES/MOVE)을 공용으로 다루는 `mahdi/data/yfinance_fallback.py`
      (`fetch_last_close(symbol)`)로 일반화함, migration 011.
      **참고**: `cross_asset_stress()` 실계산 배선과 `macro_score`(`compute_macro_score_proxy`)에
      ES 추세 반영까지 같은 날 바로 이어서 완료함(위 레짐 절 항목 참고). MOVE는 방향성이
      위험선호/회피로 명확히 매핑되지 않아 `macro_score`에는 의도적으로 넣지 않음(설계 결정,
      TODO 아님) — `cross_asset_stress()`의 급변 크기 계산에만 반영.
- [ ] yfinance 폴백은 비공식 스크래핑이라 운영 신뢰도가 낮다(레이트리밋/스키마 변경 가능) — 정식
      운영 전 재점검 필요. **실거래 전환 시 항목별 필요 조치는 [[DECISION_LOG]] 2026-07-20
      "실거래 전환 시 데이터 소스 재검토 필요 항목 정리" 표 참고**(ZN/ES는 KIS CME 계열 유료
      구독 월 228.8불 또는 Databento GLBX.MDP3 월 $179로 대체 가능, MOVE는 KIS로 근본적으로
      해결 안 됨).
- [ ] `poll_macro_snapshot`이 정규장 하루 운영 후 실제로 5분 간격을 안정적으로 지키는지, 다른 폴러들과의
      레이트리밋 경합으로 유실되는 사이클이 있는지 DB로 확인(다른 세 폴러는 2026-07-09에 이미 이 문제를
      겪고 고정틱 스케줄링으로 고쳤음 — `poll_macro_snapshot`도 같은 패턴을 이미 쓰고 있지만 실운영
      검증은 아직 안 함).
- [x] (2026-07-21 장전 점검·수정 완료) **마이그레이션 010/011 라이브 DB 미적용으로 매크로 스냅샷
      적재 전면 중단 + COCKPIT 종일 합성 폴백** — `db/migrations/010_macro_snapshot_zn_fallback_source.sql`
      (`zn_front_source`)·`011_macro_snapshot_es_move_usdkrw.sql`(`usdkrw`/`es_front`/`es_front_source`/
      `move_index`/`move_index_source`) 둘 다 파일만 커밋됐을 뿐 `docker exec psql < file.sql`로 라이브
      컨테이너에 적용된 적이 없었음(`docker-entrypoint-initdb.d`는 볼륨 최초 생성 시 1회만 실행되고
      기존 볼륨엔 재적용 안 됨 — 008/009와 달리 이번엔 적용을 깜빡함). 오늘 07:30 장전 기동 직후부터
      `poll_macro_snapshot`의 INSERT가 매 사이클 `UndefinedColumn: usdkrw`로 전부 실패
      (`macro_snapshot_5m` 신규 행 0건, 전날 15:42에 멈춰있었음). 더 심각한 건
      [mahdi/dashboard/data_source.py](mahdi/dashboard/data_source.py) `_load_from_db`의 매크로 조회
      `except` 블록에 `conn.rollback()`이 빠져있던 기존 버그(2026-07-10 커밋부터 잠복, 오늘 처음 발동)—
      같은 커넥션의 후속 쿼리(`get_active_futures_symbol`)가 `InFailedSqlTransaction`으로 연쇄 실패해
      `_load_from_db` 전체가 예외로 빠지고 **COCKPIT이 07:30~07:53 사이 125회 "합성 리플레이"로 폴백**
      (실시간 데이터가 아니라 가짜 데이터를 보여주고 있었음). 조치: (1) 마이그레이션 010/011을
      `docker exec -i mahdi_timescaledb psql -U mahdi -d mahdi < ...`로 라이브 적용, 14개 컬럼 전부
      존재 확인. (2) `data_source.py`의 누락된 `conn.rollback()` 추가(같은 파일 다른 체크 함수들과
      패턴 통일). 전체 테스트 335개 통과. COCKPIT 프로세스를 재시작(Streamlit이 임포트 모듈
      변경을 자동 반영 안 하는 기존에 확인된 특성 때문 — 2026-07-06/07-20 항목 참고) 후
      `load_snapshot()` 직접 호출로 `is_live=True` 정상 복구 확인.
  - [x] (2026-07-21 같은 날 정정) **"관측 루프는 무중단 유지" 서술 정정** — 위 항목을 처음 적을
        때 관측 루프(`mahdi.main`)는 재시작하지 않았다고 기록했으나, 오늘자 운영점검보고서
        (§1-2) 작성 중 실측으로 **이 서술이 틀렸음**을 확인했다. 근거 셋: ① 현재 살아있는
        `mahdi.main` 프로세스의 OS 생성 시각(`Get-CimInstance Win32_Process`)이 08:40:09로,
        배치스크립트가 07:30:32에 새 창을 띄운 시각과 70분 차이난다. ② `observation_loop.log`
        자체에 `_log_startup_gap_since_last_run()`이 남긴 "직전 정상 기동" 줄이 오늘 **두 번**
        있다 — 07:30:56(정상 예약 기동)과, 그로부터 1.2시간 후(≈08:43~08:45)의 두 번째 기동.
        ③ `observation_loop_crash.log`는 오늘 갱신되지 않아(마지막 07-19) 비정상 크래시가
        아니라 창을 닫고 수동으로 재시작한 것으로 보인다. 즉 이 사고를 복구하며 COCKPIT뿐 아니라
        관측 루프도 재시작됐다 — 다만 그 재시작이 `start_mahdi_premarket.bat`의 명명 규약
        (`start "Mahdi COCKPIT" ...` / `start "Mahdi Observation Loop" ...`)을 거치지 않아
        창 제목이 달라졌고, 이게 §3-1(15:45 자동 종료 taskkill이 "No tasks running"만 남기고
        실패)의 원인으로 이어졌다. 재발 방지로 `stop_mahdi_marketclose.bat`에 창 제목 대신
        실행 커맨드라인(`mahdi.main`/`mahdi/dashboard/app.py` 포함 여부) 기준 PowerShell
        fallback kill을 추가하고, `scripts/log_marketclose_stop.py`에 종료 후 잔존 프로세스를
        재확인해 Slack 경고하는 로직을 배선함(같은 날 후속 커밋).
  - [x] (2026-07-21 같은 세션에서 4개 고도화 항목 전부 구현·검증 완료) **재발 방지 구조 개선**:
        (1) `scripts/start_mahdi_premarket.bat`에 `docker compose up -d` 직후 `db/migrations/*.sql`을
        파일명 오름차순(`dir /b /on`)으로 매 기동마다 전부 재적용하는 단계 신설(`:apply_migration`
        서브루틴, `psql -v ON_ERROR_STOP=1`, 개별 파일 실패는 경고만 남기고 기동은 계속 진행) —
        001~011 전 파일이 `CREATE TABLE IF NOT EXISTS`/`ADD COLUMN IF NOT EXISTS`/
        `create_hypertable(if_not_exists=>TRUE)`/`COMMENT ON COLUMN`으로만 작성돼 매일 재실행해도
        안전함을 전수 확인 후 적용. cmd.exe 로직(for/call/goto, 괄호 블록 없음)은 영어 주석
        버전으로 별도 격리 실행해 검증(한글 REM 주석이 이 세션 도구 셸에서 깨지는 현상은
        2026-07-20 항목에 이미 문서화된 기존 현상 — 실제 Windows 작업 스케줄러 실행엔 영향 없음).
        (2) `mahdi/dashboard/data_source.py`에 `_schema_integrity_check()` 신규 — `db.macro_snapshot_columns()`
        (신규 공개 함수, `_MACRO_SNAPSHOT_5M_COLUMNS`를 그대로 노출해 INSERT/헬스체크가 같은
        목록을 공유)와 `information_schema.columns`를 대조해 "오늘의 점검 요약"에 8번째 배지로
        노출, 라이브 재검증(`get_health_summary()` 직접 호출)으로 "ok" 확인. (3) `data_source.py`
        전체 재점검 결과 이 케이스 외 다른 rollback 누락은 없음을 확인(슬랙 설정 조회/저장 두
        함수는 커넥션을 재사용하지 않아 애초에 문제 없음). (4) `mahdi/main.py`
        `poll_macro_snapshot`에 `MACRO_SNAPSHOT_INSERT_FAILURE_ALERT_STREAK`(=2, 10분) 연속
        INSERT 실패 시 Slack 경고 + 복구 시 별도 알림(gap_alerted와 동일 패턴) 신규 배선. 신규
        테스트 9개 추가(연속실패 알림/복구 알림/스키마체크 ok·warning·조회실패/컬럼목록 단일소스
        고정) — 전체 341개 통과. COCKPIT 재시작 후 브라우저 미확인(다음 실제 접속 시 8개 배지
        정상 렌더링 확인 필요).

## 관측 인프라(Phase 1) 마무리

- [x] (2026-07-19 로깅 추가, 2026-07-21 근본원인 확정·수정 완료) **NumericValueOutOfRange 진단 로깅
      및 수정** — 2026-07-16 점검에서 특정 10개 행사가(1087.5~1097.5, 1160~1170 두 클러스터)의
      IV 등이 DECIMAL(8,6) 범위를 계속 넘어 레그 삽입이 실패(3,416회) 중임을 발견.
      `_parse_option_quote()`가 원본 `output1`을 row에 `_raw_kis_output1`로 함께 실어 나르고,
      삽입 실패 로그에 그대로 찍히도록 수정함([[SESSION_LOG]] 2026-07-19 항목 참고).
  - [x] (2026-07-21 확정) 로그의 `raw_kis_output1`을 실제로 확인 — **범인은 `theta` 단독**.
        KIS `theta` 필드는 원화 단위 하루 시간가치 감소분이라 얇은/근접만기 옵션일수록
        절댓값이 수백~수천대(실측 최대 |9625.4268|)까지 커지는데, `theta` 컬럼이
        `DECIMAL(8,6)`(절댓값 99.999999까지만 허용)이라 상시 오버플로우했다. 같은 로그로
        `delta_val`(최대 |0.886|)·`gama`(최대 |0.0571|)·`vega`(최대 61.3252)는 전부 기존
        범위 안임을 확인(전수 재검토 완료). 위클리 옵션이 만기에 가까울수록 theta 절댓값이
        커지므로, 이 버그는 만기 임박일수록 유실이 심해지다가 만기 전날엔 근접 만기 데이터가
        사실상 전량 빠지는 형태로 나타났다(DB 실측: 15:45 시점 근접 두 만기 데이터가 통째로
        사라지고 월물만 남음 — 운영점검보고서 2026-07-21 §2-1). `db/migrations/012_option_analysis_theta_scale.sql`로
        `theta`를 `NUMERIC(14,4)`로 확장, 라이브 DB에도 즉시 적용·검증 완료. 두 클러스터가
        서로 다른 북인지는 여전히 미확인(우선순위 낮음, 근본원인 해결로 실익 감소).
  - [ ] 두 클러스터(약 70pt 간격)가 정말 서로 다른 북(먼슬리/위클리월/위클리목)의 ATM 근방인지
        DB(`option_analysis_1m`은 실패라 안 남으므로 subscription_manager 로그/각 북의 desired_strikes
        스냅샷 등으로)로 교차 확인.
- [x] `nearest_expiry_chain()`으로 얻은 심볼 목록을 순회하며 `rest_client.get_quote()` 반복 호출 →
      `option_analysis_1m`(IV/Greeks/OI/GEX 등) 적재 루프를 `main.py`에 연결 — 2026-07-06 `poll_option_chain()`으로
      구현 완료([[SESSION_LOG]] 참고). 단, 범위는 `nearest_expiry_chain()`(체인 전체)이 아니라 WS와 동일한
      ATM±3(`subscription_manager.desired_strikes`)로 한정 — 레이트리밋/지연 우려로 의도적으로 좁힘.
- [x] `main.py`가 옵션 체인(선물 1건이 아니라 여러 옵션 심볼)에 대해 WS 구독을 실제로 검증 —
      2026-07-06 실데이터로 검증 중 심볼 혼입 버그 + WS 헤더 파싱 버그 둘 다 발견·수정함([[SESSION_LOG]] 참고)
- [ ] 심볼별 aggregator 분리 수정 이후, 정규장 중 각 종목(콜/풋 개별)의 봉이 실제로 합리적인 OHLC 범위를
      유지하는지(더 이상 다른 종목과 안 섞이는지) 추가 관찰 필요 — 오늘은 수정 직후라 확인 시간이 짧았음
- [x] (2026-07-19 해소) **WS 재연결 로직** — `run_observation_loop_forever()`(신규, `mahdi/main.py`)가
      `run_observation_loop()`을 감싸 WS 단절(OSError/websockets.WebSocketException) 시 죽지 않고
      지수 백오프(5초→최대 60초, 연결 성공 시 리셋)로 재연결한다. 재연결마다
      `RollingSubscriptionManager.rebind()`(신규)로 새 클라이언트로 교체 + `desired_strikes`를
      리셋해, 서버 쪽 구독이 재연결로 사라진 것과 무관하게 ATM±N 전체를 새 연결에 처음부터
      다시 구독한다([[SESSION_LOG]] 2026-07-19 항목 참고).
  - [ ] **실운영 확인 필요**: approval_key를 재연결마다 재발급하지 않고 재사용하도록 구현함(REST
        접속키 발급 엔드포인트에 불필요한 부하를 주지 않기 위한 판단) — 실제 장시간 운영 중
        재연결이 발생했을 때 KIS가 오래된 approval_key로도 재연결을 계속 승인하는지 확인 안 됨.
        거부되면(예: 인증 오류로 즉시 재끊김 반복) `ApprovalKeyIssuer(kis_settings).issue()`를
        재연결 경로에도 추가해야 함.
  - [ ] 실제 네트워크 단절(예: Wi-Fi 재시작, KIS 서버 점검)로 재연결이 실전에서 발동하는지,
        재연결 후 몇 분 내로 정상 관측이 재개되는지 로그로 확인 필요(지금까지는 단위테스트로만 검증됨).
- [x] (2026-07-19 문서화만 완료, 스키마/데이터는 그대로) **타임스탬프 정책 명문화** — 2026-07-16
      점검(§3-4)에서 발견: `datetime.now()`(naive, 서버 로컬=KST)를 TIMESTAMPTZ 컬럼에 그대로 써서
      실제로는 KST 벽시계 시각인데 "+00"(UTC)으로 잘못 라벨링된 값이 저장되고 있었음. 사용자에게
      세 가지 방향(문서화만 / TIMESTAMP로 컬럼 타입 변경 / tz-aware 전환+과거데이터 마이그레이션)을
      제시했고 **"문서화만" 선택**(하이퍼테이블 스키마·기존 데이터는 건드리지 않음, 2026-07-19).
      `mahdi/data/db.py`에 `local_now()`(datetime.now()와 동일 동작) 신설 + 큰 docstring으로 이
      규약을 설명, `mahdi/main.py`/`mahdi/engines/regime_pipeline.py`/`mahdi/dashboard/data_source.py`의
      DB 관련 `datetime.now()` 호출을 전부 `db.local_now()`로 교체(동작 변경 없음, 단일 소스화만).
      `db/migrations/008_timestamp_policy_docs.sql`(신규)이 관련 TIMESTAMPTZ 컬럼 15개에
      `COMMENT ON COLUMN`으로 같은 설명을 남김([[SESSION_LOG]] 2026-07-19 항목 참고).
  - [x] (2026-07-19 적용 완료) 마이그레이션을 실행 중인 컨테이너에 직접 적용
        (`docker exec -i mahdi_timescaledb psql -U mahdi -d mahdi < db/migrations/008_timestamp_policy_docs.sql`
        — Docker Desktop이 꺼져있어 먼저 기동, `restart: unless-stopped`로 컨테이너 자동 기동 후
        적용). `pg_catalog.pg_description` 조회로 15개 컬럼 전부 코멘트 반영 확인, 데이터/스키마
        타입은 무변경.
  - [ ] "TIMESTAMP로 컬럼 타입 변경"·"tz-aware 전환+과거데이터 보정" 두 대안은 이번에 보류만
        됐을 뿐 기각된 게 아님 — Phase2 착수 전이나 해외선물 교차분석이 실제로 필요해지는 시점에
        다시 검토할 것(비용: 전자는 TimescaleDB 하이퍼테이블 7개의 파티션 컬럼 타입 ALTER, 후자는
        과거 데이터 9시간 일괄 보정 + 전환 시점 불연속 처리).
- [x] (2026-07-19 구현+라이브 DB 적용 완료) **Slack 능동 알림** — WS 연결 끊김/재연결, option_analysis_1m
      5분 이상 결손+복구, CBOT(zn_front) 미승인 3가지를 Slack으로 알린다(운영점검보고서 §5-4).
      `C:\Users\82108\PycharmProjects\futures`(미륵이)의 utils/notify.py+slack_queue.py 큐+워커 패턴을
      asyncio로 이식(`mahdi/notify.py`, 신규). COCKPIT에 🔔 체크박스(대시보드 상단) + `mahdi/config/settings.py`
      `SlackSettings`(.env: SLACK_BOT_TOKEN/SLACK_CHANNEL_ID/SLACK_ALERTS_ENABLED_DEFAULT) 추가.
      On/Off는 COCKPIT(Streamlit)과 mahdi.main(관측 루프)이 별도 프로세스라 메모리 공유가 안 돼
      `slack_alert_settings` 싱글턴 테이블(DB, `db/migrations/009_slack_alert_settings.sql`)로 공유—
      라이브 컨테이너에 이미 적용 완료(빈 테이블, 아직 아무도 안 건드려 SlackSettings 기본값 True로
      동작 중)([[SESSION_LOG]] 2026-07-19 항목 참고).
  - [x] (2026-07-19 검증 완료) **실제 Slack 메시지 발송 확인** — 실제 봇 토큰으로 채널
        (C0BJ7R4MZ9B)에 테스트 메시지 발송 후 사용자가 정상 수신 확인. 첫 시도에서 콘솔에 한글이
        깨져 보여 httpx json= 편의 파라미터가 charset을 안 붙이는 게 원인인 줄 알고
        `mahdi/notify.py`를 명시적 UTF-8 바이트+`charset=utf-8` 헤더로 고쳤으나, Slack
        `conversations.history`로 재확인해보니 **원래도 정상 전송되고 있었음**(깨져 보인 건 이
        Windows 콘솔(cp949)의 표시 문제였을 뿐) — 그래도 더 견고한 방식이라 되돌리지 않고 유지.
  - [ ] COCKPIT 체크박스를 브라우저에서 실제로 토글해 DB(`slack_alert_settings.enabled`)에 반영되고,
        관측 루프가 재시작 없이 그 값을 바로 따르는지 실운영 확인 필요(단위테스트로는 로직만 검증됨).
- [x] (2026-07-19 구현 완료) **로그 위생** — `logs/observation_loop.log`가 로테이션 없이 105MB까지
      누적된 문제(운영점검보고서 §5-5). `mahdi/main.py`에 `_configure_logging()`(신규) 추가 —
      `logging.handlers.RotatingFileHandler`(파일당 10MB, 최근 10개=최대 약 110MB)로 이 파일을
      Python이 직접 소유하도록 바꿈. `scripts/start_mahdi_premarket.bat`도 함께 수정해 stdout
      리다이렉트(`>> logs\observation_loop.log`)를 제거(안 걷어내면 Python이 회전시킨 파일을 이
      리다이렉트가 계속 원래 경로에 append해 로테이션이 무의미해짐) — 콘솔 창엔 여전히 실시간으로
      보이고, stderr만 별도 회전 없는 크래시 전용 로그(`logs\observation_loop_crash.log`)로 남김.
      `mahdi/logutil.py`(신규) `WarningThrottle`로 §3-1 NumericValueOutOfRange(한 사이클 안에서
      레그마다 반복 재발)와 WS 재연결 반복 실패 경고를 60초당 최초 1건만 로깅하도록 억제(억제된
      건수는 다음 로깅 때 요약으로 붙음)([[SESSION_LOG]] 2026-07-19 항목 참고).
  - [ ] **기존 104MB 로그 파일은 그대로 둠** — 다음 실제 관측 루프 실행 시 RotatingFileHandler가
        열자마자 maxBytes 초과를 감지해 즉시 `.1`로 회전시키고 새 빈 파일로 시작한다(Python
        표준 동작, 별도 조치 불필요) — 다만 그 첫 `.1` 백업 자체는 여전히 104MB이므로 디스크
        공간이 급하면 수동으로 지워도 됨.
  - [ ] 실제 관측 루프를 재시작해 콘솔 창에 로그가 여전히 실시간으로 보이는지, `logs/observation_loop.log`가
        실제로 10MB 근방에서 회전되는지 실운영 확인 필요(단위테스트는 tmp_path로 격리해 검증함).
- [x] (2026-07-19 구현+라이브 DB로 검증 완료) **COCKPIT "오늘의 점검 요약" 패널** — 운영점검보고서
      §1-B 장중 체크리스트 중 SQL로 자동화 가능한 항목(§5-6): 옵션체인/선물 데이터 결손(장중에만
      판단, §5-4 Slack 알림과 동일한 5분 기준), CBOT(zn_front) 승인 상태, series/symbol 화이트리스트
      위반(화석 데이터), 오늘 레짐 stability_flag 비율 — 5개를 COCKPIT 제목 바로 아래 배지로
      상시 노출. `mahdi/dashboard/data_source.py`에 `HealthCheck`/`get_health_summary()`(+ 5개
      개별 체크 함수) 신규, `mahdi/data/db.py`에 `expiry_liquidity_fossil_series()` 신규(화이트리스트
      밖 series 존재 여부 — `latest_expiry_liquidity()`는 이미 걸러서 반환하므로 "숨긴 것"과
      "없는 것"을 구분하려면 별도 조회 필요). 항목별로 독립적으로 DB에 접근해(하나 실패해도
      rollback 후 나머지는 계속 보여줌) 라이브 DB로 직접 조회해 정상 동작 확인함(장중 아님/CBOT
      미승인/화석 데이터 없음/오늘 레짐 데이터 없음 — 현재 시각 기준 전부 예상대로 표시)
      ([[SESSION_LOG]] 2026-07-19 항목 참고).
  - [ ] 실제 장중(평일 09:00~15:45)에 COCKPIT을 열어 옵션체인/선물 배지가 "정상"으로 정확히
        전환되는지, 결손을 실제로 유도했을 때(예: 폴링 중단) "결손" 경고로 바뀌는지 실측 확인 필요
        (지금까지는 장외시간 실측 + 단위테스트로만 검증됨).
- [x] (2026-07-19 구현+라이브 DB로 검증 완료) **20영업일 도달 카운트다운** — `feature_store` 축적
      현황을 "오늘의 점검 요약"(§5-6) 6번째 배지로 노출(§5-7). `_regime_fit_progress_check()`
      (`data_source.py`)가 `feature_store`에서 `count(*)`(총 행수)와
      `count(DISTINCT timestamp::date)`(실제 데이터 쌓인 날짜 수)를 함께 세어, 목표(8,000행/20영업일,
      `scripts/fit_regime_engine.py`의 `DEFAULT_MIN_SAMPLES`와 맞춤) 대비 진행률 + 하루 평균
      누적 속도 기반 잔여 영업일 추정치를 함께 보여줌. 론치일(07-05)부터 달력으로 계산하지 않고
      "실제로 데이터가 쌓인 날짜 수"를 직접 세는 방식을 택함 — 스케줄러가 쉬거나 실패한 날이
      있어도 자동으로 정확함(하드코딩된 론치일+영업일 계산보다 항상 실제 축적 상태를 정확히
      반영). 라이브 DB로 확인 결과 현재 1,764/8,000행(5/20영업일), 하루 평균 353행 기준 약
      18영업일 남음으로 표시됨 — 5영업일인 이유는 feature_store 실적재가 론치일(07-05)이 아니라
      `RegimeStateMachine` 실배선일(2026-07-10)부터 시작됐기 때문으로 추정(달력 계산이 아니라
      실측 카운트를 쓴 설계가 이 차이를 자동으로 반영함)([[SESSION_LOG]] 2026-07-19 항목 참고).
  - [ ] 목표(8,000행) 도달 시 실제로 "ok" 배지+`scripts/fit_regime_engine.py` 실행 안내가 뜨는지,
        그 시점에 실제로 그 스크립트를 실행해 `data/models/regime_engine.pkl`이 생성되고
        `RegimeStateMachine`이 predict() 모드로 전환되는지까지 이어서 확인 필요(대략 8월 초 예상).
- [ ] `_option_symbol` 그리드(고정 2.5 간격 ATM±N)와 실제 상장 행사가가 어긋나는 구간을
      실거래로 확인(현재는 `option_symbol()`이 None 반환 시 조용히 스킵만 함)
- [ ] `poll_option_chain()` 범위를 `nearest_expiry_chain()`(체인 전체, ATM±3보다 훨씬 많은 종목)로 넓힐지 결정 —
      넓히면 레이트리밋/폴링 사이클 소요시간 검토 필요
- [ ] `skew_25d`/`spread_state`(`option_analysis_1m`)는 아직 NULL — 25델타 스큐 계산(체인 전체 IV 곡선 필요),
      스프레드 상태 분류 설계 필요
- [ ] `rv_5d`는 KIS `hist_vltl`(과거변동성) 근사치를 그대로 씀 — 정확한 5일 realized vol 재계산으로 교체 검토
- [x] 수급(외국인/기관/개인 순매수) — 2026-07-06 "시장별 투자자매매동향(시세)" API로 `poll_investor_flow()`
      구현 완료([[SESSION_LOG]] 참고, `investor_flow_1m`). 재시작 후 실운영 데이터로 값 범위/추세가 그럴듯한지
      확인 필요.
- [ ] 투자자매매동향 응답(`*_ntby_tr_pbmn`)의 정확한 화폐 단위(원/천원)를 공식 문서/실거래 규모 비교로 확인 —
      확인되면 `position_panel.py`의 "순매수대금" 라벨에 단위를 되돌림
- [x] VPIN(`market_raw_1m.vpin`) — 2026-07-06 선물(H0IFCNT0) 실시간체결가 신규 구독 + `VolumeBucketAggregator`로
      구현, 이후 사용자 요청으로 **옵션에도 종목 구분 없이 통일 적용**([[SESSION_LOG]], [[DECISION_LOG]] 참고).
      재시작 후 실운영 데이터로 선물/옵션 각각 VPIN 값이 0.5(중립) 근처에서 벗어나는 유의미한 변동을
      보이는지 확인 필요.
- [ ] `VPIN_BUCKET_SIZE=50`(등거래량 버킷 크기, 선물·옵션 공용)은 미보정 추정치 — 실거래량 패턴을
      며칠 관찰한 뒤 "일평균거래량/50" 같은 근거로 재조정 검토(특히 옵션은 선물보다 훨씬 얇아 같은
      크기가 적절한지 재검토 필요)
- [x] Flow Radar가 선물만 계속 대표 종목으로 뽑는 문제(VPIN 도입의 부작용) — 2026-07-06 선물/옵션
      계열 분리로 해결, 이후 UI 배치(옵션이 위)/x축 통일/옵션 VPIN 차트까지 2차 개편 완료
      ([[SESSION_LOG]], [[DECISION_LOG]] 참고)
- [x] `find_gamma_flip`(options_intel.py)이 COCKPIT 리런마다 vollib에서 `RuntimeWarning: divide by
      zero`/`invalid value encountered` 출력 — 2026-07-09 원인 규명(vollib.ref_python.d1()의 디버그용
      `print('')`, iv/t_years=0 경계) 및 `redirect_stdout`+`catch_warnings`로 수정 완료([[SESSION_LOG]],
      [[DECISION_LOG]] 참고). 이게 COCKPIT 하루 로그 99.9%를 차지하던 빈 줄의 정체였음.
- [ ] Flow Radar x축 `rangebreaks`(전일 장마감~당일 개장 공백 제거) — 2026-07-07 코드 수정 완료,
      테스트 통과([[SESSION_LOG]] 참고). **COCKPIT 재시작 후 브라우저에서 실제로 공백이 압축돼
      보이는지 육안 확인 아직 안 함**.

## 운영 검증

- [x] 2026-07-06(월) 07:30 자동 기동 스케줄 실제 동작 확인 (Mahdi-PreMarket-Startup) — 실행은 됐으나 Docker Desktop 미기동으로 DB/Redis 없이 COCKPIT/관측루프만 뜸(수동으로 Docker 기동해 당일 대응, 배치파일에 자동 기동/대기 로직 추가함)
- [x] 2026-07-07(화) 07:30 기동 시 새로 추가한 Docker 자동 기동/폴링 로직이 실제로 동작하는지 확인 — **실패**: schtasks 반환코드 255로 즉시 종료(PC가 07:25:42에 막 부팅돼 Docker 꺼진 채 트리거되며, `start_mahdi_premarket.bat`의 IF 블록 내 미이스케이프 괄호로 인한 cmd.exe 파싱 버그가 처음 실제로 실행되어 노출됨). `^(...^)`로 수정 후 수동 재실행해 당일분 기동 완료([[SESSION_LOG]]/[[DECISION_LOG]] 참고). 2026-07-08 07:30 자동 기동은 정상 동작 확인(2026-07-09 로그 점검, 크래시성 재시작 없이 하루 종일 안정).
- [x] 2026-07-08(수) 15:45 자동 종료 확인 (Mahdi-MarketClose-Shutdown) — 정상 동작 확인(2026-07-09 로그 점검, COCKPIT/관측루프 프로세스 트리 에러 없이 종료, DB/Redis는 유지).
- [x] 정규장 시간 중 `market_raw_1m`/`regime_state`에 실제 1분봉이 쌓이는지 확인 — 2026-07-09 DB 쿼리로 확인. 단, `option_analysis_1m`은 정규장 405분 중 203분(50%)치가 REST 500 대량 유실로 통째로 비어 있었음([[SESSION_LOG]], [[DECISION_LOG]] 참고) — 원인으로 추정되는 레이트리밋 미대응을 2026-07-09 레이트리미터+재시도로 수정.
  - [x] (2026-07-20 재확인 완료) "사이클 전체 유실"(위 203분 문제)은 재발 안 함 — 대신 지금까지
        계측된 적 없던 **레그 단위 결손**(콜은 거의 항상 성공, 풋만 계속 500)을 오늘 로그·DB로
        새로 발견함(콜 18~19건 vs 풋 3건/8분, 5개 행사가 전부 동일 경향) — 공유 `_RateLimiter`가
        행사가마다 콜→풋 순서로 호출하는데 2건/초(0.5초 간격)로도 KIS 모의투자 실제 한도(1건/초로
        재추정)를 넘겨 매 쌍의 두 번째 호출만 걸리는 패턴이었다. `DEFAULT_MIN_REQUEST_INTERVAL_SECONDS`를
        1.0초로 상향 + 적응형 백오프(EGW00201 감지 시 자동 확대/서서히 복귀) 추가로 수정,
        재시작 직후 실측으로 콜:풋 비율이 18:3(~17%) → 120:75(~63%)로 정상화됨을 확인
        ([[SESSION_LOG]] 2026-07-20 항목, [[DECISION_LOG]] 참고). **다음 확인 필요**: 정규장
        하루 운영 후에도 이 비율이 유지되는지, 적응형 백오프가 불필요하게 자주(레이트리밋이
        아닌 다른 500 때문에) 발동하지 않는지 DB/로그로 재확인.
- [ ] **2026-07-09 2차**: 위 대량 유실 수정 후에도 남아있던 5분 간격 잔여 유실(09:03/09:08/09:13/09:18 패턴, 405분 중 4분)을 사용자가 Gamma Wall에서 발견 — `poll_option_chain`/`poll_expiry_liquidity`/`poll_investor_flow`를 절대시각 고정 틱 스케줄링으로 전환 + `poll_expiry_liquidity` 시작 오프셋(30초) 추가로 수정 완료([[SESSION_LOG]], [[DECISION_LOG]] 참고). **다음 확인 필요**: 정규장 하루 운영 후 이 5분 간격 패턴이 실제로 사라졌는지 DB로 재확인.
- [ ] 정규장 시간 중 `logs/observation_loop.log`에 에러 없이 insert가 찍히는지 확인
- [ ] COCKPIT의 `st.rerun()` 10초 폴링이 브라우저에서 실제로 갱신되는지, 장시간(하루 종일) 방치 시 메모리/연결 누수 없는지 확인
- [x] `mahdi/dashboard/` 하위 모듈 수정 후 COCKPIT을 재시작해야 반영된다는 사실을 실제로 겪고 확인함
      (2026-07-06, [[DECISION_LOG]] 참고) — 앞으로 대시보드 코드를 고치는 세션에서는 체크리스트에 "COCKPIT
      재시작" 항목을 빠뜨리지 않을 것
- [ ] KIS 토큰 발급 레이트리밋(분당 1회 추정) 실운영 중 재현 여부 관찰 — 오늘 테스트 중 반복 호출로 403 재현됨

## 2026-07-20 점검·고도화 — 다음 확인 필요 (코드 변경 아님, 실운영 확인 대상)

- [ ] **사용자 확인 필요**: 사용자가 실전 선물옵션계좌(계좌번호 44833081, 근거계좌 -01/온라인
      개설 -03)를 신규 개설함 — 현재 `.env`에 설정된 KIS 모의투자 계좌(60045705,
      `KIS_ENV=vps`)와는 계좌번호 체계가 다른 별개 계좌로 보인다(모의투자 계좌는 보통 실전
      계좌와 독립적으로 발급됨). 이 신규 계좌가 (1) CBOT(해외선물옵션 SUB거래소) 미승인 문제를
      해소하는 것과 관계가 있는지, (2) 마흐디가 앞으로 이 계좌를 쓰도록 `.env`를 바꿔야 하는지
      (모의→실전 전환은 매우 신중해야 할 결정 — Phase 2 주문 실행 로직은 아직 미구현이라 지금
      당장은 데이터 수집에만 영향), (3) 아니면 완전히 별개 용도(예: 다른 프로젝트)인지 사용자
      확인 필요. `.env`는 이번 세션에서 건드리지 않았음([[SESSION_LOG]] 2026-07-20 3차 항목 참고).

- [x] (2026-07-20 3차 세션에서 CLI로 확인 완료) COCKPIT "오늘의 점검 요약"에 신규 추가한
      "옵션체인 콜/풋 균형" 배지가 정규장(09:00~15:45)에 실제로 ok/warning을 정확히 오가는지 —
      정규장 11시대에 `get_health_summary()`를 직접 호출해 7개 배지 전부(콜/풋 균형 포함)
      정상 표시 확인. **단, 이 확인 과정에서 별도의 심각한 버그(`_freshness_check`의
      naive/aware datetime TypeError로 헬스체크 전체가 다운돼 있었음)를 발견·수정함
      ([[SESSION_LOG]] 2026-07-20 3차 항목, [[DECISION_LOG]] 참고).**
  - [x] (2026-07-20 완료) 버그 수정 후 COCKPIT(Streamlit)만 별도 재시작(관측 루프는 무중단
        유지) → 사용자가 브라우저 스크린샷으로 7개 배지 정상 렌더링 확인(옵션체인/선물 "95초
        전 갱신", 콜/풋 균형 "콜 45건/풋 90건", CBOT "미승인" 등 기대한 그대로).
- [ ] `scripts/start_mahdi_premarket.bat`의 cockpit.log 회전(10MB 임계값, 기동 시점 1회 체크)이
      실제로 트리거되는 날이 오면(현재 3.7MB대) `.1` 파일이 정상 생성되는지 확인.
- [ ] `mahdi/main.py`의 `_log_startup_gap_since_last_run()`(직전 정상 기동 경과 로그)과
      `scripts/log_marketclose_stop.py`(직전 정상 종료 경과 로그)가 내일(2026-07-21) 07:30/15:45
      정규 스케줄 실행에서도 정상적으로 마커를 갱신하는지 확인 — 오늘은 수동 재시작으로만 검증함.
- [ ] `scripts/start_mahdi_premarket.bat`/`stop_mahdi_marketclose.bat` 실행 시 콘솔에 한글 REM
      주석 일부가 깨진 명령어처럼 보이는 현상을 이번 세션 도구 환경에서 재현함(이 저장소의
      수정 전 원본 배치파일도 동일 환경에서 동일하게 재현되는 것을 별도로 확인해, 이번 수정으로
      새로 생긴 문제는 아닌 것으로 판단) — Docker/COCKPIT/관측루프 실제 기동에는 영향 없음을
      확인했지만, 정식 Windows 작업 스케줄러 실행 로그에서도 부작용이 전혀 없는지는 아직
      확인 안 됨([[DECISION_LOG]] 2026-07-20 항목 참고).
- [ ] `mahdi/broker/rest_client.py`의 적응형 `_RateLimiter`(EGW00201 감지 시 간격 1.5배 확대,
      최대 4배, 연속성공 20회마다 서서히 복귀)가 실제 장중 트래픽에서 의도대로 동작하는지 —
      백오프가 걸렸다가 정상적으로 복귀하는 사례를 로그로 한 번 이상 확인할 것.

## Phase 2(판단·실행) — Risk Engine(07-28)에 이어 Signal Fusion/Execution/Backtest 최초 구현(07-28 2차)

- [x] **Risk Engine(Kelly 사이징, 한도, Circuit Breaker, Kill Switch) 최초 구현 완료(2026-07-28)**
      — `mahdi/risk/{sizing,limits,circuit_breaker,engine}.py`, v6 §12.1~12.3 전부 구현, 신규
      테스트 45개(`tests/test_risk_*.py`) 전부 통과([[SESSION_LOG]] 2026-07-28 4차 항목 참고).
      순수 결정 함수 라이브러리로만 구현 — `main.py` 라이브 루프 배선은 아직 없음(아직 신호/
      주문 자체가 없어 배선할 대상이 없음).
  - [ ] Portfolio Greeks 한도(`portfolio_greeks_limits`)는 `risk_limits.yaml`에 실제 수치가
        없어 옵션 인자 미제공 시 그 체크를 건너뛴다(`unconfigured_checks`로 드러남) — 실제
        Delta/Gamma/Vega 상한값을 계좌 자본 규모 기준으로 정해 `risk_limits.yaml`에 추가할지
        사용자 결정 필요(Execution Engine이 실제 포지션 Greeks를 계산하기 시작하는 시점에
        같이 정하는 게 자연스러움).
  - [ ] `risk_snapshots` 테이블(스키마는 이미 존재) 영속화 — main.py 라이브 배선 시점에 추가.
- [x] **Signal Fusion(Core Engine 6) 최초 구현 완료(2026-07-28 2차)** — `mahdi/fusion/
      {signal_layer,ensemble,conflict_resolution,meta_label,strategy_palette,engine}.py`,
      v6 §11.1~§11.4 전부 구현, 신규 테스트 43개 통과. **학습된 ML 모델이 아직 없어(trade_history
      0건) 앙상블 6개 멤버 중 `xgboost_tabular`/`lstm_temporal`은 항상 None(가중치 0, 나머지
      4개로 재정규화) — Meta-Label도 실제 RandomForest 대신 §11.2 입력 목록을 그대로 받는
      결정론적 프록시**(`meta_label.classify()`, 임계값은 `strategy_params.yaml`의
      `meta_label_thresholds`). Phase 3(Champion-Challenger)에서 `trade_history`가 쌓이면
      실제 학습된 분류기로 교체 예정 — 이 사실이 코드 docstring에 명시돼 있음.
  - [ ] `xgboost_tabular`/`lstm_temporal` 실학습(피처는 이미 존재) 및 Meta-Label을 실제
        RandomForest/XGBoost + Triple Barrier + Purged K-Fold로 교체 — `trade_history` 축적 필요.
  - [ ] `options_flow`(GEX/Gamma Flip 반전 로직)·`orderflow_ofi_vpin`(OFI+잔량 불균형 부호
        평균)의 부호 기반 방향 추정이 실제 시장에서 의미 있는지 실거래 데이터로 검증 필요
        (현재는 결정론적 프록시일 뿐, 계수 자체를 백테스트/실운영으로 검증한 적 없음).
- [x] **Execution Engine(Core Engine 8) + 하이브리드 3모드 최초 구현 완료(2026-07-28 2차)** —
      `mahdi/execution/{hybrid_mode,entry,exit_stack,forced_flat,order_manager,engine}.py`,
      v6 §13.1~§13.5 전부 구현, 신규 테스트 46개 통과. Forced Flat은 [[DECISION_LOG]]
      2026-07-21 "종료 시퀀스 자기검증 없이는 배포 금지" 조건대로 `verify_forced_flat()`이
      FILLED가 아닌 모든 주문(PENDING/PARTIAL/CANCELLED/REJECTED)을 잔존으로 취급. `ExecutionEngine`
      파사드는 진입/재평가 양쪽에서 `RiskEngine.evaluate_entry()`/`evaluate_ongoing()`을 항상
      경유(§12 "독립 거부권" 배선 요구사항 충족). **순수 오케스트레이션 라이브러리로만 구현
      — Risk Engine과 같은 이유로 `main.py` 라이브 루프 배선은 이번에도 하지 않음**(Signal
      Fusion이 검증 안 된 휴리스틱이라 실시간으로 실제 주문을 계속 내보내는 건 시기상조라는
      판단, [[DECISION_LOG]] 참고).
  - [ ] `forced_flat.py`/`order_manager.py`가 실제 KIS 브로커(모의계좌)로 제출→폴링→자기검증
        전체 사이클을 도는지 실거래 검증 필요 — 지금은 목 브로커로만 테스트됨.
  - [x] **Signal Fusion만 main.py 라이브 배선 완료(2026-07-28 6차, ADVISORY 전용)** —
        `poll_signal_fusion_cycle()`이 매 사이클 `signal_decisions`에 기록. Risk/Execution
        Engine은 여전히 라이브 미배선(계좌 손익/포지션 추적 인프라가 없어 `RiskEngine.
        evaluate_entry()`를 의미 있게 호출할 수 없음 — 아래 새 항목 참고). **main.py 코드는
        작성했으나 실제 관측 루프 재시작은 아직 안 함** — 사용자 확인 후 진행할 것.
    - [x] **계좌 추적기 설계를 위한 실측 완료(2026-07-28 7차)** — `get_balance()`가
          `MGNA_DVSN`/`EXCC_STAT_CD`/`CTX_AREA_FK200`/`CTX_AREA_NK200` 필수 파라미터 누락으로
          **호출될 때마다 항상 실패하고 있던 버그**를 발견·수정(`msg_cd=OPSQ2001`). 라이브
          모의계좌 재확인 결과 현재 현금예수금 5,000만원/포지션 0건/손익 0(깨끗한 시작 상태).
          필요한 손익 필드가 이미 응답에 다 있어(`prsm_dpast`=추정예탁자산으로 일간/주간
          손익·드로우다운 계산, `evlu_pfls_amt_smtl`/`trad_pfls_amt_smtl`=평가/실현손익합계,
          `output1[].sll_buy_dvsn_name`=종목별 매수/매도 방향) 별도 API 없이 `get_balance()`
          하나로 계좌 추적기를 만들 수 있음을 확인([[DECISION_LOG]] 2026-07-28 7차 항목에
          필드 매핑 전체 기록).
    - [x] **계좌 손익/포지션 상태 추적기 실제 구현 완료(2026-07-28 8차)** —
          `db/migrations/015_account_balance_snapshots.sql`(신규 테이블, 라이브 DB 적용 완료) +
          `mahdi/data/db.py`(`insert_account_balance_snapshot`/`latest_account_balance_snapshot`/
          `account_balance_snapshot_before`/`max_account_balance_ever`/
          `daily_trade_counts_by_strategy`) + `mahdi/execution/account_tracker.py`
          (`parse_balance_response`/`build_account_state`) + `mahdi/main.py`
          `poll_account_balance_cycle()`(신규, `get_balance()` 주기 폴링) 신규. **라이브 DB
          왕복 스모크 테스트 중 실제 버그 하나 더 발견·수정**: `latest_account_balance_snapshot`/
          `account_balance_snapshot_before`가 psycopg의 `Decimal` 타입을 float으로 변환하지
          않아, `max_account_balance_ever()`(float 반환)와 섞어 연산하면 `TypeError`가 나는
          걸 실측으로 확인(단위테스트는 순수 float/int 픽스처만 써서 못 잡았음) — 두 함수 모두
          DECIMAL 컬럼을 float 변환하도록 수정, 재검증 완료.
    - [x] **`poll_signal_fusion_cycle()`이 이제 진입 후보일 때 `RiskEngine.evaluate_entry()`를
          실제로 호출(2026-07-28 8차)** — `signal_decisions.risk_gate_state.risk_engine`에
          승인여부/승인사이즈/거부사유 기록. **여전히 실주문은 안 함**(ExecutionEngine의
          order_manager/hybrid_mode는 안 씀) — v6 §12 "독립 거부권"을 실제로 라이브에서
          작동시키는 첫 단계. 계좌 추적기가 아직 스냅샷을 못 쌓았으면(기동 직후 등)
          `risk_gate_state.risk_engine == "account_tracker_not_ready"`로 명시.
    - [ ] `PositionSizingInput`의 `target_vol`/`realized_vol`/`liquidity_score`/
          `portfolio_capacity_remaining_pct`가 아직 중립값(라이브 계산기 없음) — 실현/내재
          변동성 비율과 호가 두께 기반 유동성 점수를 실제로 계산하는 파이프라인 필요.
    - [ ] **(2026-07-29 COCKPIT "판단 현황" 패널 사용자 문의 중 발견) `base_size`가 어디에도
          설정되지 않고 `mahdi/main.py:1573`에 리터럴 `1.0`으로 하드코딩돼 있음** — 더 근본적인
          문제는 이 "1.0"이 1계약인지, 계좌 자산 대비 비율인지, 명목가치 1단위인지조차 정의된
          적이 없다는 것. `RiskEngine.evaluate_entry()`가 반환하는 `approved_size`(예:
          0.06)를 실제 계약 수/주문 수량으로 변환하는 로직이 `mahdi/execution/entry.py`
          (`EntryPlan` 생성 담당)에 전혀 없음 — 지금 COCKPIT에 보이는 0.06은 순수
          "위험조정 사이즈 비율"일 뿐, "그래서 몇 계약을 살지"에 대한 답이 아직 없는 상태.
          Execution Engine을 실제로 라이브 배선하기 전에 반드시 정의해야 할 항목(계좌 자본
          규모 기준 base_size 단위 결정 + entry.py의 사이즈→계약수 변환 로직 추가) — 위
          Portfolio Greeks 한도 항목(2026-07-28)과 같은 시점에 함께 정하는 게 자연스러움.
    - [ ] **[반드시 해결 — Execution Engine 배선 전 필수] "이미 같은 신호로 진입했으면 재진입
          안 함" 로직이 어디에도 없음(2026-07-29 COCKPIT 판단 현황 패널 사용자 문의 중 발견)** —
          `poll_signal_fusion_cycle()`은 60초마다 상태 없이 완전히 새로 평가하므로, 신호(레짐·
          확신도)가 안 바뀌면 "진입 후보"가 매분 반복해서 뜬다. 지금은 ADVISORY라 무해하지만,
          **이 상태 그대로 FULL_AUTO/CONFIRM에 연결하면 매분 같은 신호로 반복 진입을 시도하게
          된다.** 이걸 막을 것처럼 보이는 한도가 `risk_limits.yaml`에 있지만 둘 다 사실상
          무력화돼 있음을 확인:
          - `max_same_direction_positions: 3`(설정됨) — 하지만 이 값이 세는
            `same_direction_buy_count`/`sell_count`는 KIS `get_balance()`의 **실제 보유 포지션**
            에서만 나오는데, ADVISORY라 실주문이 없어 항상 0 → 실행에 연결되기 전까지는 이
            한도가 절대 작동할 수 없는 죽은 코드.
          - `max_daily_trades_per_strategy: null` — 아예 미설정(주석은 "strategy_params.yaml에서
            세팅"이라 하나 실제로는 그 파일 어디에도 없음, `check_limits()`가 `unconfigured_
            checks`로도 표시 안 하고 조용히 스킵함 — portfolio_greeks와 달리 투명성도 없음).
          - 실행에 연결된 이후에도 타이밍 공백 존재: 계좌 잔고 폴러(오늘 300초로 완화, Fix#1)와
            신호평가(60초) 주기가 달라, 실제 첫 진입 체결 후 최대 5분간은 포지션 증가가
            반영 안 돼 그 사이 추가 중복 진입이 가능하다.
          - 근본 해결책(둘 다 필요해 보임): (1) `max_daily_trades_per_strategy`를 실제 값으로
            설정하고 `daily_trade_counts_by_strategy()`가 `trade_history` 대신(또는 함께)
            `signal_decisions`의 "같은 전략·같은 방향으로 이미 ENTER+승인된" 최근 기록을 보게
            하거나, (2) Execution Engine에 "이 신호(전략+방향+대략적 만기/행사가 조합)로 이미
            열려있는 포지션이 있으면 스킵" 판단을 명시적으로 추가. Execution Engine 라이브
            배선 논의 시 위 base_size 항목과 함께 반드시 다뤄야 함.
    - [x] **관측 루프 기동 완료·라이브 검증 완료(2026-07-28 8차)** — 이전엔 프로세스가 아예 안
          떠 있었음(확인 결과 "재시작"이 아니라 최초 기동). `uv run python -m mahdi.main`을
          백그라운드로 기동 후 로그+DB로 직접 확인: `poll_account_balance_cycle`의
          `get_balance()` 호출이 (7차에서 고친) 4개 필수 파라미터 전부 포함해 200 OK,
          `poll_signal_fusion_cycle`도 매 사이클 정상 동작(에러 로그 0건) —
          `signal_decisions`/`account_balance_snapshots`에 실제로 행이 쌓이는 것까지 실측
          확인. 현재 장외시간이라 신호는 전부 NO_TRADE/REJECT로 나오는데, 이는 실데이터 부재
          상황에서 기대되는 정상 동작.
    - [ ] COCKPIT이 열려있지 않은 상태에서 기동했다 — COCKPIT은 이번 요청 범위 밖이라 별도로
          기동 필요(사용자 요청 시).
    - [ ] `signal_decisions`/`account_balance_snapshots`를 COCKPIT에서 확인할 방법(직접 쿼리
          또는 신규 패널) 아직 없음.
    - [ ] **사소한 발견**: `scripts/start_mahdi_premarket.bat`를 거치지 않고 `python -m
          mahdi.main`을 직접 실행하면 `PYTHONUTF8=1`이 설정 안 돼 콘솔(cp949)에 특수문자(예:
          "—" em-dash)가 포함된 로그 줄에서 `UnicodeEncodeError`가 난다(ZN CBOT 실패 로그에서
          발생 확인) — Python 로깅 모듈이 내부적으로 잡아 프로세스는 안 죽고, 실제
          `logs/observation_loop.log`(UTF-8 명시)는 영향 없음(직접 확인). 정식 배치파일 경로로
          기동하면 애초에 발생 안 함 — 별도 조치 불필요, 기록만.
    - [ ] **거래일 정규장(09:00~15:45)에 실제로 유의미한 ENTER/HIGH_CONVICTION 신호가 나오는지,
          RiskEngine 승인/거부가 실제로 갈리는지 확인 필요** — 오늘은 장외시간 기동이라
          NO_TRADE만 확인됨.
- [x] **백테스트 엔진 프레임워크 최초 구현 완료(2026-07-28 2차)** — `mahdi/backtest/
      {simulated_broker,metrics,engine}.py`, 신규 테스트 15개 통과. `BacktestEngine`이
      `SignalFusionEngine`→`ExecutionEngine`(내부에서 `RiskEngine` 경유)을 실거래와 동일하게
      재사용해 합성 봉 시퀀스를 재생 — 실행 경로 이원화 없음.
  - [x] **실데이터 어댑터 + WFO/MC/DSR 검증 스택 구현 완료(2026-07-28 6차)** —
        `mahdi/backtest/data_adapter.py`(`load_backtest_steps_from_db()`, DB에 신규 "as-of"
        조회 3종 추가) + `mahdi/backtest/validation.py`(`walk_forward_splits`/
        `monte_carlo_resample`/`deflated_sharpe_ratio`). **알려진 단순화**: 어댑터가 만드는
        `AccountState`/`MarketConditions`는 항상 중립 기본값(과거 손익 재구성 인프라 없음,
        위 계좌 추적기 항목과 동일 제약)이라 실행 경로 검증용이지 실제 과거 손익 재현이
        아니다. 레짐도 라벨만 복원(원-핫 근사, 과거 prob_vector 미저장).
  - [ ] 실거래 이력이 아직 3주 남짓이라 `load_backtest_steps_from_db()`로 실제 돌려봐도
        통계적으로 유의미한 백테스트 결과는 아니다 — 데이터가 몇 달치 쌓인 뒤 실제로 실행해
        `deflated_sharpe_ratio()`로 다중 시행 편향까지 확인하는 절차 필요.
  - [ ] 계좌 추적기가 생기면 `AccountState`/`MarketConditions`를 과거 시점 실제값으로
        재구성하는 것도 검토(현재는 항상 중립 — 위 항목 참고).
- [x] **ML 학습 스캐폴딩 구현 완료(2026-07-28 6차)** — `mahdi/fusion/trainer.py` +
      `scripts/fit_signal_fusion_meta_label.py`(`scripts/fit_regime_engine.py`와 동일 패턴),
      `db.get_trade_history()` 신규(읽기 전용). `xgboost_tabular` 멤버는 이미 설치된
      scikit-learn `GradientBoostingClassifier`로 구현(신규 의존성 없음). **`trade_history`가
      0건이라 오늘 스크립트를 실행하면 "데이터 없음" 에러만 남기고 종료 — 기대된 동작.**
      `xgboost_tabular`가 이 학습 결과를 실제로 로드해 신호에 반영하도록 배선하지 않음
      (신호 생성 경로 자체는 이번에 변경 안 함).
  - [ ] `trade_history`에 실제로 쓰는 곳이 아직 없다(실주문 실행이 없어서) — 계좌 추적기 +
        Execution Engine 라이브 배선이 생기면 그때 `trade_history` INSERT도 함께 추가해야
        스크립트가 실제로 학습할 데이터가 쌓인다.
  - [ ] `lstm_temporal` 멤버는 여전히 미착수 — 딥러닝 프레임워크(torch 등) 도입 여부는 별도
        사용자 결정 필요, 이번에도 다루지 않음.

## 기타

- [ ] KIS 토큰 폐기(`/oauth2/revokeP`) 호출 경로 없음 — 필요 시 `token_daemon.py`에 추가
- [x] (2026-07-06 해소) 위클리옵션(N/O) 조회 — `symbol_master.py`의 `options()`/`nearest_expiry_chain()`/
      `option_symbol()`이 `series="weekly"` 인자를 받도록 확장 완료(만기일의 월/목 여부는 이름만으로 확정
      불가라 파싱 안 함 — `main.py`가 실제 `get_quote()` 응답의 `futs_last_tr_date`로 확정).

## 만기·종목 선발 체계 (2026-07-06 조사 완료 — [[RESEARCH_EXPIRY_SELECTION_v1]] 참고)

- [x] Phase 1.5-①: `symbol_master`에 위클리(N/O) 조회 추가 — 2026-07-06 완료, 테스트 12개 통과
      (실측 상품종류가 공식 문서(L/M)와 다르고 N/O임을 확인, `symbol_master.py` 주석 참고).
      **정정(2026-07-10)**: "L/M은 없다"는 결론이 틀렸음— L/M도 실제 위클리 코드풀로 존재해
      N/O와 함께 조회하도록 확장함(바로 아래 항목, [[DECISION_LOG]] 참고).
- [x] Phase 1.5-②: 관측 북 2개(최근월 먼슬리 + 최근접 위클리) 동시 구독 — 2026-07-06 완료.
      `main.py`의 `run_observation_loop`가 `subscription_managers: list[...]`를, `poll_option_chain`이
      `books: list[tuple[manager, series]]`을 받도록 시그니처 변경. `main()`이 `monthly_manager`/
      `weekly_manager` 두 개를 만들어 같은 `ws_client`에 동시 구독(슬롯 29/41). 테스트 148개 전체 통과.
      **아직 재시작 안 함** — 반영하려면 관측 루프 프로세스 재시작 필요(사용자 확인 후 진행).
- [x] Phase 1.5-③: `expiry_liquidity_1m` 테이블 신설 — 2026-07-06 완료. 사용자가 "정식 스펙대로"
      선택(%스프레드 포함) — `poll_expiry_liquidity()`가 북(regular/weekly)마다 ATM±2×(C,P)에
      `get_asking_price()`로 Cao-Wei %스프레드·깊이·거래량을 집계, 만기는 ATM 1건만 `get_quote()`로
      확인(북당 사이클당 1건). 레이트리밋 우려 때문에 폴링 주기를 옵션체인(60초)의 5배인 300초로
      완화([[DECISION_LOG]] 참고). 마이그레이션 `005_expiry_liquidity.sql` 적용 완료, 테스트 6개 통과.
      **아직 재시작 안 함** — 실운영 데이터로 레이트리밋 회피 여부/값 범위 확인 필요.
- [x] Phase 1.5-④: COCKPIT 만기별 유동성 비교 패널 — 2026-07-06 완료. `expiry_liquidity_panel.py`
      (Plotly Table, 먼슬리/위클리 %스프레드·깊이·거래량·잔존일수 나란히 표시)를 `app.py`의
      "만기 유동성 비교" 섹션에 배치, `data_source.py`에 `expiry_liquidity` 필드/`db.latest_expiry_liquidity()`
      추가. 테스트 4개 통과. **아직 COCKPIT 재시작 안 함**(모듈 캐싱 — [[DECISION_LOG]] 참고).
- [x] **재시작 완료(2026-07-06 14:2x)**: Phase 1.5-①~④ 전체가 실운영에 반영됨. 재시작 과정에서
      기존 mahdi.main/COCKPIT이 각각 2중 프로세스(.venv python.exe가 anaconda python.exe 자식을
      스폰하는 구조 — 이 PC의 venv 특성으로 보이며 정상, 의도치 않은 중복 세션은 아니었음)로 떠
      있던 것을 정리하고 `uv run` 표준 경로로 1개씩 재시작함. 재시작 직후 위클리 도입으로
      NumericValueOutOfRange 크래시가 실제로 발생 → 바로 위 DECISION_LOG 항목대로 수정 후 재재시작,
      정상화 확인함(`market_raw_1m`에 BAFB*/CAFB*(위클리)와 A01609(선물)/B0160*·C0160*(먼슬리)
      전부 최신 봉 적재 확인, `expiry_liquidity_1m`에 regular 1행 적재 확인, weekly 행은 다음
      폴링 사이클(5분) 대기 중).
- [ ] 다음 확인: (1) `expiry_liquidity_1m`에 weekly series 행도 실제로 쌓이는지 (2) 레이트리밋
      (403/500) 발생 빈도가 300초 주기로 완화됐는지 — 여전히 잦으면 주기를 더 늘리거나 ATM±2를
      ±1로 좁히는 것 검토 (3) COCKPIT "만기 유동성 비교" 패널이 브라우저에서 실제로 두 북을
      보여주는지 육안 확인 (4) 오늘 이전에 있었던 mode="lines+markers" Flow Radar 수정도 이번
      COCKPIT 재시작으로 함께 반영됐으니 별도 재시작 불필요.
- [x] Flow Radar "가장 활발한 옵션" 종목이 리런마다 다른 종목으로 튀던 문제 — 2026-07-06 완료
      ([[DECISION_LOG]] 참고). 룩백윈도 누적거래량+`symbol ASC` 타이브레이커로 교체, COCKPIT
      재시작 반영함. **브라우저에서 실제로 안정화됐는지 육안 확인 아직 안 함** — 다음에 COCKPIT을
      열면 Flow Radar 옵션 종목 라벨이 몇 분간 유지되는지 확인할 것.
- [ ] Phase 2: 장전 선발 스코어러 + 일중 강등 트리거 + 북별 파라미터 세트 + 섀도우 30일 검증
      (핵심 규칙: 만기 북은 하루 고정·행사가만 ATM 롤링, 0DTE 방향성 네이키드 매수 금지 — 근거는 연구 문서 참고)
- [x] (2026-07-10 해소) **위클리(목) 확인**: 2026-07-06 당시 상장 전이라 미확인이었던 "위클리(목)"
      체인 — 2026-07-10 마스터파일 재확인 결과 상품종류 `L`(콜)/`M`(풋)에 "위클리C/P" 행이 실제로
      존재함을 확인. 처음엔 "N/O와 L/M이 같은 위클리 상품의 교대 코드풀"로 보고 병합 조회했으나,
      COCKPIT 만기유동성비교 패널이 실제로 표시한 위클리 만기(2026-07-13=월요일)가 그때 더 가까웠던
      N/O 풀에서 나온 것으로 확인돼 **N/O=위클리(월), L/M=위클리(목)인 별개 상품**으로 정정, 사용자
      요청대로 `symbol_master.py`의 series를 `"weekly_mon"`(N/O)/`"weekly_thu"`(L/M)로 분리하고
      `main.py`도 두 북으로 나눠 COCKPIT/Flow Radar에서 월/목이 각각 표시되도록 완료
      ([[DECISION_LOG]]/[[SESSION_LOG]] 2026-07-10 항목 참고).
  - [x] (2026-07-10 교차검증 완료) N/O=월·L/M=목 매핑 — 위클리 분리 반영 재시작 후 실제
        `poll_expiry_liquidity()`가 `weekly_thu`(L/M) 행을 적재했고, 만기가 2026-07-16(목요일,
        달력으로 직접 확인)·잔존일수 6(정확)으로 나와 REST `get_quote()` 기반 실데이터로도
        N/O=위클리(월)/L/M=위클리(목)이 확정됐다.
  - [ ] 위클리를 3번째 북으로 분리하며 `STRIKES_EACH_SIDE`를 3→2로 낮췄다(세 북 모두 ATM±2,
        31/41 슬롯) — 재시작 후 실운영에서 ATM±2로도 관측 품질(감마/유동성 판단)이 충분한지,
        아니면 먼슬리만 ±3으로 복원하는 차등 배분이 필요한지 확인.
- [x] (2026-07-10 원인 수정 완료, 결과 확인은 남음) **위클리(목) 누적거래량이 계속 0**: 사용자가
      COCKPIT에서 발견 — `_parse_asking_price_leg`가 호가(ask/bid)가 없으면 `acml_vol`이 정상
      파싱됐어도 레그 전체를 버리던 결합 버그를 찾아 스프레드/거래량을 독립 파싱하도록 수정
      ([[DECISION_LOG]]/[[SESSION_LOG]] 참고), 관측 루프 재시작 완료.
  - [ ] 재시작 후 다음 몇 사이클에서 위클리(목) 누적거래량이 실제로 0이 아닌 값을 보이는지
        확인 — 계속 0이면 그건 버그가 아니라 "오늘 그 행사가들에 정말 체결이 없다"는 뜻.
- [x] (2026-07-10 해소) **화석 `series='weekly'` 행이 COCKPIT에 영구 노출**: 위클리 분리 반영
      재시작 직후 사용자가 COCKPIT에서 "먼슬리/weekly/위클리(월)" 3행을 보고 정체 확인 요청 —
      `series='weekly'`는 분리 전 구코드가 남긴 화석 데이터(재시작 전 마지막 폴링에서 멈춤)였다.
      `db.latest_expiry_liquidity()`에 `_VALID_EXPIRY_LIQUIDITY_SERIES` 화이트리스트 필터를 추가해
      차단([[DECISION_LOG]] 참고).
  - [x] (2026-07-10 완료) DB에 남아있던 옛 `series='weekly'` 179건을 사용자 확인 후
        `DELETE FROM expiry_liquidity_1m WHERE series='weekly'`로 완전 삭제 —
        `expiry_liquidity_1m`엔 이제 regular/weekly_mon/weekly_thu 세 series만 존재.
