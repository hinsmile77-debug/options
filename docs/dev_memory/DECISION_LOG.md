# DECISION_LOG — 설계 결정 이유 기록

_최신 항목이 위에 오도록 역순 정렬_

---

## [2026-07-09] REST 폴링 4개 루프에 공유 레이트리미터 도입 — TPS 초과로 정규장 절반 데이터 유실

**결정:** `KISRestClient`에 스레드 안전 `_RateLimiter`(최소 호출 간격 0.5초=2건/초)를 추가하고, 모든 GET/POST가 이를 거치는 `_get`/`_post` 단일 진입점으로 리팩터링했다. `min_request_interval` 생성자 인자로 조정 가능.

**Why:** 2026-07-08 하루치 로그/DB를 실측한 결과, 정규장 405분 중 203분(50%)치 `option_analysis_1m`이 통째로 비어 있었다. 원인은 `poll_option_chain`(60초 주기, ~28콜)·`poll_investor_flow`(60초, 3콜)·`poll_expiry_liquidity`(300초, ~11콜) 세 루프가 `asyncio.gather`로 동시에 돌면서 각자는 순차 호출이라도 서로 다른 `asyncio.to_thread` 스레드가 겹치는 순간 KIS 앱키의 초당 호출 한도를 넘겨 500 Internal Server Error가 대량 발생했기 때문(전체 REST 호출의 38%가 실패, 그중 500 에러는 평균 2.5회·최대 17회 연속으로 뭉쳐서 발생 — 순수 랜덤 장애가 아니라 버스트 패턴). 공식 문서에 모의투자 TPS 한도가 명시돼 있지 않아 보수적으로 2건/초로 잡았다 — 사이클당 필요한 최대 호출(옵션체인+수급=31건)도 15.5초면 끝나 60초 주기 안에 여유 있게 들어간다.

**How to apply:** 새로운 REST 폴링 루프를 추가할 때는 반드시 기존 `rest_client` 인스턴스를 공유해서 쓴다(레이트리미터가 인스턴스 단위 상태이므로 별도 `KISRestClient()`를 새로 만들면 페이싱이 깨진다). 레이트리밋 여전히 잦으면 `min_request_interval`을 늘리거나, `main()`에서 인스턴스 생성 시 값을 낮춰(예: 0.3초=3.3TPS) 대역폭을 넓히는 것부터 검토 — 다만 KIS 쪽 실측 없이 낮추면 다시 500이 늘어날 수 있음.

---

## [2026-07-09] 옵션체인/수급 폴링, 사이클 전체 실패 시 짧은 backoff 후 1회 재시도

**결정:** `poll_option_chain`/`poll_investor_flow`에서 그 사이클의 모든 종목/세그먼트가 실패해 적재할 게 하나도 없으면, 정규 주기(60초)를 기다리지 않고 `CYCLE_RETRY_BACKOFF_SECONDS`(5초) 후 그 사이클을 한 번만 재시도한다. 재시도까지 실패하면 조용히 포기하고 다음 정규 사이클로 넘어간다.

**Why:** 위 레이트리미터로 TPS 버스트는 완화되지만, KIS 서버 자체의 일시적 500(레이트리밋과 무관한 장애)까지 없앨 수는 없다 — 그 경우 사이클 하나가 완전히 비면 그 1분치 데이터가 영구 유실된다. 60초 뒤 다음 정규 사이클을 기다리는 대신 5초 뒤 짧게 재시도하면 일시적 장애 구간을 대부분 건너뛸 수 있다. 무한 재시도가 아니라 1회로 제한한 이유는, 진짜 장애(서버 다운 등)일 때 재시도 루프가 정규 주기를 계속 잠식하지 않게 하기 위함.

**How to apply:** 재시도는 `_collect_option_chain_cycle`/`_collect_investor_flow_cycle` 헬퍼를 그대로 재호출하는 방식이라, 두 함수의 시그니처를 바꿀 때는 재시도 호출부도 함께 수정해야 한다. `poll_expiry_liquidity`에는 이 재시도를 적용하지 않았다 — 이 지표는 20거래일 기준선 축적용이라 한 사이클 유실이 실시간 판단에 영향을 안 주고, 폴링 주기도 이미 300초로 넉넉해 우선순위가 낮다고 판단.

---

## [2026-07-09] vollib `find_gamma_flip` stdout/RuntimeWarning 국소 억제

**결정:** `find_gamma_flip`이 그리드 스캔 중 `vollib.gamma()`를 호출하는 구간만 `contextlib.redirect_stdout(io.StringIO())` + `warnings.catch_warnings()`(RuntimeWarning ignore)로 감쌌다. vollib 패키지 자체(`.venv/site-packages`)는 건드리지 않았다.

**Why:** COCKPIT `cockpit.log` 하루치(667,663줄)의 99.9%(667,651줄)가 빈 줄이었던 원인을 `sys.stdout.write`에 콜스택 트레이싱을 붙여 직접 추적한 결과, `vollib.ref_python.black_scholes.d1()`에 `if not denominator: print('')`(디버그 잔재로 추정)가 있었다 — `denominator = sigma*sqrt(t)`가 0이 되는 경우(레그의 `iv`나 `t_years`가 0에 가까운, 얇거나 만기 임박 종목)마다 실행된다. `find_gamma_flip`은 스팟 그리드(기본 41점)마다 이 값을 재계산하므로, 그런 레그가 하나만 섞여 있어도 COCKPIT 리런 1회당 수십~수백 개의 빈 줄이 나온다. 같은 조건이 `RuntimeWarning: divide by zero`/`invalid value encountered`도 함께 유발한다(계산 자체는 nan/inf를 반환할 뿐 GEX 부호 비교 로직에는 영향 없음 — 억제해도 안전).
site-packages를 직접 고치는 대신 호출부에서 흡수한 이유: 벤더 패키지 수정은 git에 안 잡히고 `uv sync`/재설치 시 사라진다.

**How to apply:** vollib를 호출하는 새 코드를 추가할 때(예: 다른 Greeks 계산 함수) 동일한 `iv=0`/`t_years=0` 경계에서 같은 문제가 재발할 수 있다 — 새 호출부도 필요하면 같은 패턴(`redirect_stdout` + `catch_warnings`)으로 감쌀 것. 테스트에서 이 조건을 검증할 때는 반드시 `iv=0.0, t_years=0.0` 같은 경계값 레그를 써야 한다 — 정상 레그(`iv=0.18, t_years=0.05`)로는 `denominator != 0`이라 print/warning이 애초에 안 나서 회귀를 못 잡는 거짓 통과 테스트가 된다(실제로 이번에 이 실수를 했다가 고쳤음).

---

## [2026-07-09] 배치파일에 `chcp 65001` 추가 — premarket_startup.log 한글 mojibake 수정

**결정:** `start_mahdi_premarket.bat`/`stop_mahdi_marketclose.bat` 둘 다 `setlocal` 직후 `chcp 65001 >nul`을 추가했다.

**Why:** 두 배치파일은 BOM 없는 UTF-8로 저장돼 있는데, cmd.exe는 기본 시스템 코드페이지(한국어 Windows 기준 949)로 배치파일을 읽고 콘솔에 출력한다 — UTF-8 바이트를 949로 잘못 해석해 `echo`의 한글 문구가 부분 mojibake로, `taskkill` 등 OS 자체 출력은 완전히 깨진 바이트로 `premarket_startup.log`에 남았다(예: `????: PID 1234의 프로세스...`). 배치파일이 CRLF+UTF-8(BOM 없음)인 것은 이미 확인된 전제라 `chcp 65001`만 추가하면 별도 재인코딩 없이 바로 해결됨.

**How to apply:** 두 스크립트를 직접 실행해 검증함 — 수정 전엔 taskkill 출력이 `????...`로 깨졌고, 수정 후엔 `SUCCESS: The process...`로 정상 출력됨(이 시스템은 코드페이지 65001에서 taskkill 리소스 문자열이 영어로 나옴 — 한글이 아니어도 mojibake만 없으면 목적 달성). 앞으로 이 두 파일에 새 `echo` 문구를 추가할 때는 UTF-8 인코딩만 유지하면 되고 별도 조치 불필요. 다른 새 배치파일을 추가할 때도(특히 한글 문구가 콘솔에 출력되는 경우) 상단에 `chcp 65001 >nul`을 습관적으로 넣을 것.

---

## [2026-07-07] 배치파일 IF 블록 안 echo에 괄호를 그대로 쓰면 cmd.exe가 파싱 실패 — `^()` 이스케이프로 수정

**결정:** `scripts/start_mahdi_premarket.bat`의 `if not exist "%DOCKER_DESKTOP_EXE%" ( ... )` 블록 안 echo 문에서 `(%DOCKER_DESKTOP_EXE%)`처럼 괄호를 그대로 썼던 것을 `^(...^)`로 이스케이프했다.

**Why:** 07-07 07:30 자동 기동이 실패(schtasks 반환코드 255, 로그엔 시작 줄 하나만 남고 끝)했고, 사용자 요청으로 수동 재현해도 `cmd.exe`가 `- was unexpected at this time` 파싱 에러로 즉시 죽는 걸 확인했다. 원인은 이 echo 줄이 `if not exist (...)` 블록 안에 있는데, 블록 안에서 괄호를 이스케이프 없이 쓰면(설령 개수가 짝이 맞아도) cmd.exe 파서가 블록 경계를 잘못 인식해 블록이 조기 종료되고 이어지는 토큰(`- 수동 확인 필요`, 닫는 `)`)이 블록 밖 명령으로 잘못 해석돼 구문 에러가 난다 — 전형적인 cmd.exe IF/FOR 블록 내 괄호 지뢰. 이 분기는 Docker가 이미 켜져 있으면 아예 실행되지 않으므로 07-06엔 안 걸렸다가(그날은 docker_ready 분기를 탔음), 07-07 아침 PC가 막 부팅돼(`LastBootUpTime` 07:25:42) Docker가 꺼진 채로 트리거되면서 이 분기가 처음 실제 실행되어 버그가 드러났다.

**How to apply:** 배치파일에서 `if (...)`/`for (...)` 블록 안의 echo·set 등에 괄호나 특수문자(`&`,`|`,`<`,`>`,`^`)를 넣어야 하면 반드시 `^`로 이스케이프한다. 이런 "에러 처리/경고 로그" 분기는 평소(정상 상태)엔 안 타다가 장애 상황에서만 처음 실행되는 경우가 많아, 정상 경로만 검증하고 넘어가면 방치되기 쉽다는 점을 유의 — 배치 스크립트의 실패-분기(fallback branch)도 최소 한 번은 강제로 타보고 검증할 것.

---

## [2026-07-06] Flow Radar "가장 활발한 옵션" 선정을 "최근 틱 1개"에서 "룩백 윈도 누적거래량"으로 변경

**결정:** `data_source.py`의 옵션 계열 선정 쿼리를 `ORDER BY max(timestamp) DESC LIMIT 1`(타이브레이커 없음)에서 `ORDER BY sum(volume) DESC, max(timestamp) DESC, symbol ASC LIMIT 1` + `WHERE timestamp >= 최근 10분`으로 바꿨다. 룩백 기준 시각은 `datetime.now()`가 아니라 스냅샷 자체의 `regime_state.timestamp`를 쓴다(리플레이 시나리오에서도 윈도가 항상 데이터 시각 기준으로 맞게).

**Why:** 사용자가 스크린샷으로 "Flow Radar 차트가 순간순간 극적으로 다른 모양(다른 종목)으로 올라온다"고 지적. 실측(`market_raw_1m`)해보니 위클리 북 추가 후 여러 위클리 옵션 종목이 같은 1분봉 timestamp로 동시에 찍혀("14:37:00"에 5개 종목 동률), 타이브레이커 없는 `ORDER BY max(timestamp) DESC`가 COCKPIT 10초 리런마다 동률 처리를 사실상 무작위로 바꿔 완전히 다른 종목(다른 가격대·OFI·VPIN)이 뽑히고 있었다. 2북(먼슬리+위클리) 동시 구독으로 후보 종목 수가 거의 2배가 되면서 동률 빈도가 크게 늘어 이 문제가 눈에 띄게 심해진 것 — Phase 1.5 2북 작업의 직접적인 부작용.

**How to apply:** "가장 최근에 활동한 것"을 고르는 로직에 후보가 여러 개고 동률이 흔하면, 단일 최근 이벤트가 아니라 윈도 집계(여기선 거래량 합)로 바꾸고 마지막에 `symbol ASC` 같은 완전 결정론적 타이브레이커를 반드시 넣는다 — 그래야 데이터가 정말 안 바뀌었는데 화면만 깜빡이는 걸 막는다. 리런마다 다시 계산하는 대시보드 쿼리는 "지금 이 순간의 승자"가 조회할 때마다 바뀔 수 있다는 걸 항상 의심할 것.

---

## [2026-07-06] Phase 1.5 재시작 중 실측 버그: 위클리 도입 후 옵션 레그 DB 삽입 실패가 관측 루프 전체를 죽임 — DB insert에 개별 try/except+rollback 추가

**결정:** `poll_option_chain()`/`poll_expiry_liquidity()`의 DB 삽입 루프(`db.insert_option_analysis_1m`, `db.insert_expiry_liquidity_1m`)를 각각 레그/북 단위 `try/except`로 감쌌다. 실패 시 `conn.rollback()`을 반드시 호출한 뒤 다음 레그로 계속 진행한다.

**Why:** 2북(먼슬리+위클리) 동시 구독을 실제로 재시작해 켰더니, 얇은(거래 없는) 종목의 IV 등이 `option_analysis_1m.iv DECIMAL(8,6)`(절댓값 100 미만 제약) 범위를 넘는 값을 반환해 `psycopg.errors.NumericValueOutOfRange`가 발생했다. 이 예외가 `asyncio.gather()` 안에서 잡히지 않고 전파돼 `run_observation_loop`/`poll_expiry_liquidity`/`poll_investor_flow`까지 전부 강제 종료됐다(선물 틱 수신까지 멈춤) — 옵션 레그 하나의 데이터 이상이 전체 파이프라인을 죽인 것. `poll_option_chain`의 기존 docstring은 이미 "개별 종목 실패는 건너뛰고 계속 처리한다"고 명시했었지만, 그 원칙이 REST 조회 실패에만 적용되고 DB 삽입 실패에는 적용돼 있지 않았던 설계 공백이었다.

**How to apply:** `conn.rollback()`을 빼먹으면 안 된다 — psycopg는 실패한 트랜잭션에서 커밋 없이 다음 `execute()`를 거부하므로, rollback 없이 `continue`만 하면 그 사이클의 남은 레그가 전부 연쇄로 실패한다. 앞으로 "레그/북 단위로 실패를 흡수해야 하는" 루프를 새로 추가할 때는 REST 호출뿐 아니라 DB 삽입까지 같은 원칙(개별 실패 격리)을 적용해야 한다는 선례로 남긴다. 실운영 데이터(특히 신규 진입한 위클리처럼 검증 안 된 데이터 소스)는 예상 못 한 범위의 값을 낼 수 있다는 점도 재확인.

---

## [2026-07-06] Phase 1.5-③ 만기 유동성(expiry_liquidity_1m)은 %스프레드 포함 정식 스펙대로, 폴링 주기로 부하 완화

**결정:** `expiry_liquidity_1m` 테이블을 신설하고 `poll_expiry_liquidity()`를 추가했다. 축소판(거래량·OI만)과 정식 스펙(%스프레드 포함) 중 사용자에게 물었더니 "정식 스펙대로" 선택 — `get_asking_price()`를 북(regular/weekly)당 ATM±2×(C,P)=10건씩 신규 호출해 Cao-Wei 상대(%) 스프레드·호가잔량 합(깊이)·누적거래량을 집계한다. 만기일(`expiry`)은 레그마다 다시 구하지 않고 북당 ATM 종목 1건만 `get_quote()`로 확인해 재사용한다(REST 부하 최소화).

**Why:** 이날 이미 1배 부하(옵션체인 폴링 60초 주기)에서 403/500 레이트리밋이 관찰된 상태였고, 2북(먼슬리+위클리) 동시 구독까지 켜져 REST 호출량이 늘어난 상황이었다. 그런데도 사용자가 정식 스펙(%스프레드 포함)을 선택했으므로, 호출을 줄이는 대신 **폴링 주기**로 부하를 낮췄다 — 이 지표의 용도가 실시간 판단이 아니라 "장전 선발 점수의 20거래일 기준선 축적"(연구문서 참고)이라 60초씩 촘촘히 볼 필요가 없다는 점에 근거해, 옵션체인(60초)보다 5배 긴 300초(5분) 주기로 잡았다.

**How to apply:** 사용자가 레이트리밋 위험을 알고도 "정식대로"를 택하면, 그 선택을 존중하되 위험 완화 수단은 호출 자체를 줄이는 대신 다른 축(주기·캐싱 등)에서 찾는다. `LIQUIDITY_ATM_EACH_SIDE=2`(WS 구독 범위 ATM±3보다 좁음)는 연구문서가 권고한 "ATM±2 집중, 스캘핑에 최적 %스프레드 구간"을 그대로 따른 것 — WS 구독 폭(ATM±N)과 유동성 지표 집계 폭(ATM±M)을 항상 같게 둘 필요는 없다는 선례로 참고. 재시작 전이라 실운영 데이터로 폴링 주기가 실제로 레이트리밋을 피하는지는 아직 확인 안 됨.

---

## [2026-07-06] (아래 결정을 뒤집음) VPIN을 옵션에도 종목 구분 없이 통일 적용

**결정:** 바로 아래 "[2026-07-06] VPIN은 개별 옵션이 아니라 선물(기초자산)에 적용한다" 결정을 뒤집는다. `run_observation_loop`의 선물 전용 특수 분기를 없애고, `aggregators`/`volume_buckets`/`vpin_returns`/`vpin_volumes`를 전부 종목별 dict로 통일해 선물이든 옵션이든 봉이 완성되면 그 종목의 VPIN을 계산해 적재한다.

**Why:** Flow Radar를 선물/옵션 두 계열로 분리한 뒤, 사용자가 "옵션 섹션에도 VPIN 차트가 빠져있다"고 지적했다. 얇은 거래량 때문에 VPIN이 자주 0.5(중립)에 머물 수 있다는 트레이드오프를 다시 설명했더니, 사용자가 "그래도 실제로 계산해서 보여달라"고 명시적으로 선택 — "일부 계열에서만 신뢰도가 낮을 수 있는 지표"보다 "화면에 아예 안 보이는 것"이 더 나쁘다는 판단으로 이해했다.

**부작용과 대응:** 옵션에도 vpin이 채워지면서, Flow Radar 분리에 쓰던 "`vpin IS NOT NULL` = 선물" 휴리스틱이 깨졌다 — `active_futures_symbol` 레지스트리 테이블(신규)로 교체해 선물 식별을 명시적으로 만들었다.

**How to apply:** VPIN처럼 "특정 조건에서만 신뢰도가 낮은" 지표를 놓고 "아예 안 보여줄지 vs 낮은 신뢰도를 감수하고 보여줄지" 갈림길에 서면, 기본값으로 후자(보여주되 한계를 설명)를 우선 제안한다 — 이번에 사용자가 그렇게 선택했다. 다만 코드 구조상 "vpin 유무로 선물/옵션을 구분"처럼 어떤 컬럼의 존재 여부에 암묵적으로 의미를 싣는 설계는, 나중에 그 컬럼을 다른 곳에도 채우게 되면 깨지기 쉽다는 교훈도 남겼다 — 애매하면 처음부터 명시적 레지스트리/플래그를 두는 편이 낫다.

---

## [2026-07-06] VPIN은 개별 옵션이 아니라 선물(기초자산)에 적용한다

**결정:** `calculate_vpin()`(BVC 기반, 이미 구현·테스트돼 있었지만 아무도 안 부르던 함수)의 입력을 만드는 등거래량 버킷을, 지금 WS로 구독 중인 14개 개별 옵션 종목이 아니라 KOSPI200 선물(기초자산) 실시간체결가(H0IFCNT0)를 새로 구독해서 만든다.

**Why:** VPIN은 Easley-Lopez de Prado-O'Hara가 유동성이 충분한 단일 종목(전형적으로 지수선물)을 전제로 설계한 지표다. 오늘 실측한 개별 옵션 거래량은 분당 1~10계약 수준으로 얇아서, 등거래량 버킷이 완성되는 데 몇 시간씩 걸리거나 표준편차가 0에 가까워 VPIN이 대부분 0.5(중립)로 정체될 위험이 컸다. KOSPI200 선물은 옵션보다 훨씬 유동성이 높아 VPIN의 설계 의도에 맞고, 버킷이 합리적인 빈도로 완성된다.

**How to apply:** 대가로 선물 실시간체결가 WS 구독을 새로 추가해야 했다(`main.py`의 `run_observation_loop`가 옵션 ATM±3 구독과 별개로 `H0IFCNT0`/`futures_symbol`을 구독). 필드 순서가 옵션(H0IOCNT0)과 다르므로 별도 파서(`_parse_futures_tick`, `_FUT_IDX_*`)가 필요했다 — 공식 문서(시트 "지수선물 실시간체결가", API 실시간-010)로 실측 검증함. COCKPIT Flow Radar의 "가장 활발한 실제 종목 자동선택" 로직은 안 건드렸다 — 선물이 옵션보다 훨씬 자주 체결되므로 자연스럽게 대표 종목으로 뽑혀 OFI/price/microprice/VPIN이 전부 일관되게 선물 기준으로 표시된다.

---

## [2026-07-06] `data_source.py`(또는 그 하위 모듈)를 고치면 COCKPIT(Streamlit) 프로세스를 반드시 재시작해야 함

**결정:** `mahdi/dashboard/app.py`가 아니라 그 아래에서 `import`되는 모듈(`data_source.py`, `panels/*.py` 등)을 수정했을 때는, COCKPIT 프로세스를 재시작해서 완전히 새로 띄운다. 프로그램적으로 넣어둔 `st.rerun()` 폴링이나 브라우저 새로고침만으로는 부족하다.

**Why:** Streamlit은 최상위 엔트리 스크립트(`app.py`)만 매 리런마다 디스크에서 다시 읽어 실행하고, `import`된 하위 모듈은 파이썬의 `sys.modules` 캐시에 남아 재사용된다. 오늘 `data_source.py`를 여러 차례(기초자산 스팟 재설계, 옵션 체인, 수급 연동) 고쳤는데도 COCKPIT 화면이 계속 "합성 리플레이" 경고를 띄웠던 사고가 있었다 — 원인은 07:30에 뜬 COCKPIT 프로세스를 하루 종일 한 번도 재시작하지 않아서, 가장 오래된(고정 라벨 `"KOSPI200_OPT"`로 조회하던) `data_source.py`를 계속 메모리에 캐싱한 채 실행하고 있었기 때문. 그 라벨은 심볼 분리 수정 이후 아무도 안 써서 09:09에 멈춘 화석 데이터였고, 그래서 항상 빈 결과 → `None` → 합성 폴백이 나왔던 것.

**How to apply:** 관측 루프(`mahdi.main`)와 COCKPIT(`streamlit`)은 서로 다른 프로세스이므로 재시작 필요 여부도 따로 판단한다 — 관측 루프 코드(`main.py`, `db.py` 등)만 고쳤으면 COCKPIT은 안 건드려도 되지만, `mahdi/dashboard/` 아래 아무 파일이나 고쳤으면 COCKPIT도 반드시 재시작한다. 원인 진단을 쉽게 하려고 `data_source.py`의 `except Exception` 폴백 분기에 `logger.exception(...)`을 추가해뒀다(예전엔 조용히 `None`만 반환해 사후 추적이 불가능했음).

---

## [2026-07-06] 투자자매매동향(수급) API는 "모의투자 미지원" 문서에도 불구하고 모의 앱키로 실전 도메인 호출이 성공함

**결정:** `rest_client.get_investor_flow()`는 `is_mock` 분기 없이 항상 `tr_codes.REAL_REST_DOMAIN`을 호출한다.

**Why:** KIS 문서(시트 "시장별 투자자매매동향(시세)")에 "모의 TR_ID: 모의투자 미지원", "모의 Domain: 모의투자 미지원"이라고 명시돼 있어 처음엔 이 데이터를 모의투자에서 못 쓴다고 생각했다. 하지만 지수옵션 실시간 시세(WS)도 똑같은 문구가 있었는데 실제로는 "계좌 무관 공개 데이터라 모의투자 전용 도메인이 아예 없다"는 뜻이었던 선례가 있어([[DECISION_LOG]] "KIS WS 실시간 프레임" 항목 이전 커밋 참고), 같은 논리가 적용될지 실측했다 — 살아있는 모의투자 토큰으로 `REAL_REST_DOMAIN`을 직접 호출해보니 200 OK로 실제 수급 수치가 돌아왔다. 투자자매매동향은 계좌 포지션이 아니라 시장 전체 통계라 계좌 종류와 무관한 공개 데이터이기 때문.

**How to apply:** KIS 문서에 "모의 미지원"이라고 적힌 API를 만나면, 그게 "계좌별 데이터라 정말 안 되는 것"인지 "계좌 무관 공개 데이터라 그냥 모의용 도메인이 없을 뿐 실전 도메인 호출 자체는 되는 것"인지 먼저 실측으로 구분한다. 후자라면 굳이 기능을 포기하지 않아도 된다.

---

## [2026-07-06] COCKPIT "기초자산 현재가"는 옵션 체결가가 아니라 별도 지수 스팟 테이블에서 가져와야 함

**결정:** `underlying_spot_1m` 테이블을 신설해 REST 폴링이 얻은 KOSPI200 지수 자체(`output3.bstp_nmix_prpr`)를 저장하고, `data_source.py`는 여기서 스팟을 읽는다. `market_raw_1m`(옵션 종목별 틱 집계)에서 스팟을 유추하지 않는다.

**Why:** 사용자가 공유한 COCKPIT 스크린샷에서 "기초자산 현재가 35.95"가 이상하다고 지적 — 확인해보니 `data_source.py`가 고정 라벨 `symbol="KOSPI200_OPT"`로 `market_raw_1m`을 조회해 그 심볼의 옵션 체결가(close)를 그대로 "기초자산"으로 표시하고 있었다. 심볼 혼입 버그를 고치기 전에는 여러 종목이 섞여있어 이 오류가 덜 티났을 뿐, 원래부터 잘못된 설계였다. 심볼 분리 수정 이후로는 그 고정 라벨에 아무도 안 써서 데이터가 아예 멈춰버려(화석 데이터) 문제가 명확히 드러났다.

**How to apply:** 대시보드에 새 지표를 추가할 때, "이 값이 실제로 어느 테이블/어느 종목에서 왔는가"를 항상 확인한다. 옵션 체결가와 기초자산 스팟은 근본적으로 다른 개념이라 절대 같은 컬럼/쿼리로 대체하면 안 된다. [[mahdi-project-overview]] 참고.

---

## [2026-07-06] KIS get_quote() 응답에 그릭스/IV/OI가 이미 포함되어 있음 — 별도 Greeks API 불필요

**결정:** 옵션 체인 폴링(`poll_option_chain`)은 기존 "선물옵션 시세"(`get_quote`, inquire-price) 응답 하나로 delta/gamma("gama")/theta/vega/IV(`hts_ints_vltl`)/OI(`hts_otst_stpl_qty`)/만기(`futs_last_tr_date`)/기초자산 스팟(`output3.bstp_nmix_prpr`)을 전부 얻는다. 별도 Greeks 조회 엔드포인트를 찾거나 만들지 않는다.

**Why:** `NEXT_TODO.md`에 "option_analysis_1m 연동은 옵션 체인 수집기 가동 이후"로 미뤄져 있었는데, KIS 공식 문서(xlsx)가 인코딩 문제로 파싱이 어려워서 대신 살아있는 모의투자 연결로 실제 옵션 심볼(`get_quote("B01607B38", ...)`)을 직접 호출해 응답 JSON을 실측했다. 결과: 이미 그릭스/IV/OI가 output1에, 기초자산 지수가 output3에(어느 옵션을 조회하든 동일하게) 들어있었다 — 추가 API 조사/구현이 필요 없었다.

**How to apply:** KIS 응답 필드를 추정하지 말고, 모의투자 토큰이 살아있으면 실제로 한 번 호출해서 `response.json()`을 찍어보는 쪽이 xlsx/PDF 문서를 파싱하는 것보다 훨씬 빠르고 정확하다. `output1.gama`처럼 필드명이 직관과 다른 경우가 있으니(gamma가 아니라 gama) 항상 실측 우선.

---

## [2026-07-06] KIS WS 실시간 프레임은 파이프(|) 헤더를 벗긴 뒤에 캐럿(^) 파싱해야 함

**결정:** `mahdi.main._parse_tick`은 `raw.split("^")` 전에 `raw.split("|", 3)[-1]`로 `암호화유무|TR_ID|데이터건수` 헤더를 먼저 제거한다.

**Why:** ATM±3(콜/풋 최대 14종목) 구독 중 서로 다른 종목의 체결가가 한 봉에 뒤섞이는 버그를 고치려고 틱의 종목코드(0번 필드)를 처음 읽었더니, `market_raw_1m.symbol VARCHAR(20)`을 넘겨 실거래 중 `StringDataRightTruncation`으로 반복 크래시. 원인은 KIS WS 프레임이 `"0|H0IOCNT0|001|<실제데이터^...>"` 형태로 파이프 헤더를 앞에 붙여 오는데, 이를 벗기지 않고 전체를 `"^"`로만 쪼개면 0번 필드에 헤더 전체 + 실제 0번 필드가 합쳐져버림. idx1(BSOP_HOUR) 이후 필드는 헤더 뒤에 있어 우연히 정상 정렬됐던 것이라 지금까지 안 들켰음(가격/거래량/호가는 전부 정상으로 보였음).

**How to apply:** KIS WS 실시간 데이터를 새로 파싱하는 코드를 추가할 때는 항상 파이프 헤더 제거를 먼저 하고, 0번 필드(대개 종목코드)를 쓰기 전에는 길이/포맷을 실측으로 재확인한다. `raw.split("|", 3)[-1]`은 헤더가 없는 입력(테스트 픽스처 등)에도 안전하게 통과한다(전체 문자열을 그대로 반환).

---

## [2026-07-06] 커밋되는 스크립트/설정에 절대경로 하드코딩 금지 (멀티 PC 이식성)

**결정:** `scripts/*.bat`를 비롯해 저장소에 커밋되는 모든 스크립트는 실행 시점에 자기 위치 기준으로
경로를 계산한다. `start_mahdi_premarket.bat`/`stop_mahdi_marketclose.bat`의
`C:\Users\82108\PycharmProjects\options` 하드코딩을 `for %%I in ("%~dp0..") do set "PROJECT_DIR=%%~fI"`
(배치파일 자기 위치의 상위 디렉터리를 정규화한 절대경로)로 교체.

**Why:** 사용자가 이 저장소를 여러 PC에서 pull/배포해 쓸 수 있어야 한다고 명시적으로 요청. 절대경로를
하드코딩하면 다른 PC나 다른 클론 경로에서는 그대로 깨진다.

**How to apply:** 새 스크립트를 작성할 때마다 "다른 PC의 다른 경로에서 pull해도 동작하는가"를 커밋 전에
점검한다. Windows 작업 스케줄러의 Action처럼 도구 자체가 절대경로를 요구하는 예외적인 경우만 PC별 1회
설정 단계로 분리하고(스케줄러 등록 명령 자체는 커밋 대상이 아님), 스크립트 본문은 항상 이식 가능하게 유지한다.

---

## [2026-07-06] 배치파일은 CRLF 줄바꿈 필수

**결정:** Windows 작업 스케줄러용 `.bat` 파일은 반드시 CRLF(`\r\n`) 줄바꿈으로 저장한다.

**Why:** LF만 있는 배치파일을 `cmd.exe`로 실행하면 `'FILE' is not recognized...`, `&& was unexpected at this time` 같은 파싱 에러가 나며, 어느 줄이 깨지는지도 실행마다 다르게 나타난다(같은 파일인데 증상이 바뀜). 원인은 cmd.exe 배치 파서가 줄 경계 인식에 CR을 사실상 요구하기 때문. Write 도구/heredoc으로 만든 파일은 기본이 LF라 매번 변환이 필요했다.

**How to apply:** `.bat` 파일을 새로 만들거나 수정한 뒤에는 `sed -i 's/$/\r/' file.bat` 등으로 CRLF 변환 후 반드시 실행 테스트를 한다. PowerShell `Out-File`/`Set-Content`는 기본이 CRLF라 이 문제가 없다.

---

## [2026-07-06] taskkill WINDOWTITLE 필터는 뒤쪽 와일드카드만 지원

**결정:** `taskkill /FI "WINDOWTITLE eq Title*"` (뒤쪽 와일드카드만) 사용. `*Title*`(앞뒤 모두)은 사용 금지.

**Why:** `*Title*` 형태는 "The search filter cannot be recognized" 에러로 무조건 실패. 실측으로 확인(더미 창 띄워서 검증).

**How to apply:** 창 제목으로 프로세스를 찾아 종료하는 스크립트를 작성할 때는 `start "정확한제목" ...`으로 제목을 고정해두고, `taskkill /F /T /FI "WINDOWTITLE eq 정확한제목*"`로 매칭한다.

---

## [2026-07-06] 옵션 만기 판별은 월물구분코드가 아니라 한글종목명에서 추출

**결정:** KIS 종목코드 마스터파일(`fo_idx_code_mts.mst`)에서 옵션 콜/풋의 최근월(가장 가까운 만기) 판별은 `월물구분코드` 컬럼이 아니라 `한글종목명`(예: "C 202607   545.0")에서 정규식(`^[CP]\s*(\d{6})`)으로 뽑은 YYYYMM을 쓴다. 선물은 그대로 `월물구분코드`를 신뢰한다.

**Why:** 실제 파일을 내려받아 확인한 결과, KOSPI200 콜옵션 2664행에 대해 `월물구분코드`는 단 3개 값(1/2/3)만 존재하는데 실제 만기월은 11개(202607~202812)였다 — 즉 이 컬럼은 옵션에서는 만기 순번이 아니라 다른 분류(유동성 등급 추정)를 나타낸다. 선물은 7개 행에 월물구분코드 1~7이 만기 순서와 정확히 일치해 문제없이 사용 가능.

**How to apply:** `symbol_master.py`의 `options()`/`nearest_expiry_chain()`/`option_symbol()`은 모두 `한글종목명` 파싱 결과(`만기YYYYMM` 컬럼)를 기준으로 동작한다. 새로운 조회 메서드를 추가할 때도 옵션 만기 필터링에는 절대 `월물구분코드`를 쓰지 않는다.

---

## [2026-07-06] KIS 종목코드 마스터파일 실제 컬럼 순서는 공식 참고 스크립트와 다름

**결정:** `symbol_master.py`의 `_COLUMNS`는 KIS 공식 GitHub 참고 스크립트(`domestic_index_future_code.py`)가 명시한 순서(`...,ATM구분,행사가,월물구분코드,...`)가 아니라 실측 순서(`...,월물구분코드,행사가,ATM구분,...`)를 따른다.

**Why:** 실제 파일을 내려받아 raw 필드를 직접 `split('|')`해서 확인한 결과, 동일 만기의 여러 행사가에서 5번째 필드가 계속 '2'로 고정되고(=월물구분코드류) 6번째 필드가 행사가별로 다르게 변하는(=행사가) 패턴을 확인. 참고 스크립트의 컬럼명 순서를 그대로 믿었다가 처음엔 만기/행사가가 전부 어긋난 체인이 나왔다.

**How to apply:** KIS가 제공하는 참고 스크립트/문서는 "출발점"으로만 쓰고, 실제 파일을 받아 값을 확인하기 전에는 신뢰하지 않는다. 마스터파일 포맷이 KIS 쪽에서 바뀌면(가능성 낮지만) `symbol_master.py`의 `_COLUMNS` 주석에 있는 실측 근거부터 다시 검증해야 한다.

---

## [2026-07-06] 지수옵션 실시간 시세(WS)는 모의투자 전용 도메인이 없음

**결정:** `H0IOCNT0`/`H0IOASP0`(지수옵션 실시간체결가/호가) 구독은 계좌가 모의투자든 실전이든 항상 `ws://ops.koreainvestment.com:21000`(실전 도메인)에 접속한다. `is_mock` 여부로 도메인을 분기하지 않는다.

**Why:** 공식 KIS API 문서에 이 두 TR ID는 "모의 TR_ID: 모의투자 미지원", "모의 Domain: 모의투자 미지원"으로 명시되어 있다 — 시세는 계좌와 무관한 공개 데이터라 모의투자용 별도 채널 자체가 없다. 반대로 "선물옵션 실시간체결통보"(내 주문 체결 알림, H0IFCNI0/H0IFCNI9)는 계좌를 특정해야 해서 모의/실전 TR_ID와 도메인이 둘 다 분리되어 있다.

**How to apply:** `tr_codes.py`의 `MARKET_DATA_WS_DOMAIN`은 시세 구독 전용으로 항상 실전 도메인을 가리킨다. Phase2에서 주문체결통보(계좌별 알림)를 붙일 때는 `WS_TR_ORDER_NOTICE`처럼 `is_mock` 기준으로 도메인/TR_ID를 분기해야 한다 — 이 둘을 같은 로직으로 착각하지 않는다.

---

## [2026-07-06] 옵션 체인 조회는 모의투자에서 REST 1회 호출로 불가능

**결정:** "선물옵션 시세"/"시세호가"(inquire-price/inquire-asking-price, TR `FHMIF1000/1001 0000`) 단일종목 조회 + KIS 종목코드 마스터파일 기반 체인 구성 조합으로 대체한다. "국내옵션전광판_옵션월물리스트"(display-board-option-list)류는 쓰지 않는다.

**Why:** display-board 계열 REST는 공식 문서에 "모의투자 미지원"으로 명시되어 있고, TR ID도 처음에 잘못 추정했었다(FHPIF05030100은 실제로는 콜풋 전광판용). 모의투자에는 체인 전체를 한 번에 주는 API가 존재하지 않는다.

**How to apply:** 옵션 체인이 필요한 곳(대시보드 Gamma Map, 피처 계산 등)은 `symbol_master.nearest_expiry_chain()`으로 심볼 목록을 얻고, 각 심볼에 대해 `rest_client.get_quote()`를 반복 호출해 실시간 값을 채우는 2단계 패턴을 유지한다.

---

## [2026-07-05] .gitignore의 `data/`는 `/data/`로 고정

**결정:** `.gitignore`의 `data/` 패턴을 `/data/`(저장소 루트 고정)로 수정.

**Why:** 고정하지 않은 `data/`는 깊이에 상관없이 이름이 "data"인 모든 디렉터리를 무시한다 — 결과적으로 `mahdi/data/`(실제 소스 패키지: collector.py/db.py/subscription_manager.py)가 첫 커밋부터 git에 전혀 안 잡히고 있었다.

**How to apply:** 앞으로 `.gitignore`에 일반적인 이름(data, build, dist, cache 등)의 디렉터리를 추가할 때는 의도한 위치(보통 루트)에만 적용되도록 항상 `/` 접두사로 고정한다. 새 gitignore 규칙을 추가한 뒤에는 `git status`로 예상치 못하게 무시되는 소스 파일이 없는지 확인한다.

---

## [2026-07-05] Regime Engine 테스트는 실제 HMM 적합이 아니라 stub 모델로 검증

**결정:** `RegimeEngine`의 라벨 매핑/확률벡터 재정렬/상위 레짐 충돌 해소 로직은 가짜(stub) 모델을 주입해 결정론적으로 테스트하고, 실제 `GaussianHMM.fit()`은 느슨한 어서션의 스모크 테스트로만 확인한다.

**Why:** 8개 군집을 인위적으로 잘 분리해도 EM 수렴 결과가 실행할 때마다 달라졌다(같은 코드, 같은 시드인데 BLAS 스레딩 등 실행 환경 차이로 다른 지역해에 도달). GaussianHMM.fit()의 정확한 군집-라벨 매칭을 강하게 단언하는 테스트는 근본적으로 비결정적이라 CI에 부적합.

**How to apply:** 새로운 통계적/수치적 적합(fit) 로직을 테스트할 때는 "우리가 작성한 로직"(라벨 매핑, 후처리)과 "라이브러리의 수치 최적화 결과"를 분리해서, 후자는 구조적 검증(예외 없음, 확률합=1)만 하고 전자는 stub으로 결정론적으로 검증한다.
