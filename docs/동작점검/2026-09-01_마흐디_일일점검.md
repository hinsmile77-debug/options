# 마흐디 일일점검 — 2026-09-01 (화)

- **국면**: 장전 (pre) · **실행 08:01~08:02 KST** — 예정 08:00, **정시 실행**(밀리지 않았다)
- 보고서 작성 08:05~08:25 · **관측 범위: 07:30:00 ~ 08:02:14**
- 증거 다이제스트: `docs/동작점검/auto/2026-09-01_증거_pre.md` (08:02:05 생성, 40.4KB)
- 지연창 원자료: `docs/동작점검/auto/2026-09-01_지연창.tsv` (6창)
- HEAD `cb6c28b` · 브랜치 `master` · 미커밋 2건(`.claude/` · `docs/Dev_md/dashboard_build_prompt_1.md` — 둘 다 08-27부터 미결정으로 이월 중인 항목, 새 것 아님)
- ⛔ **이 회차는 코드 · 설정 · `hypotheses.yaml`의 `상태` · dev_memory를 한 줄도 바꾸지 않는다.**

---

## §0. 오늘 한 장

> 이 절은 전문용어 없이 쓴다. 용어가 필요한 설명은 §1 이후에 있다.

**1. 지금 프로그램은 정상이다.** 아침 7시 30분 자동 기동이 한 번에 성공했고, 재기동 없이 08:02까지 32분 동안 1분마다 시세를 빠짐없이 가져오고 있다. 오류(ERROR) 0건, 시세 요청 응답시간은 0.02초(참고 못 참고 끊는 한계선 4.0초의 0.5%)로 매우 빠르다. 어젯밤(08-31) 사람이 직접 켠 「시세 조회를 덜 하게 만드는 장치」(레버 E)가 **오늘 아침 기동에 실제로 반영된 것**을 확인했다 — 이것이 오늘 아침 가장 중요하게 확인해야 했던 것이다.
※ 이것은 **07:30~08:02 사이에 관측된 것**이다. 개장 후는 아직 모른다.

**2. 사람이 지금 해야 할 일 — 없다.** 어제 저녁에 이미 핵심 결정(레버 E 발동)이 끝났고 오늘 아침 그 값이 실제로 켜진 채 기동한 것까지 확인됐다. 개장 전에 새로 정해야 할 것은 없다.

**개장 전 사람 조치: 불필요** (관측 범위 07:30~08:02).

**3. 문제 5건 (심각 0 / 보통 1 / 경미 4). 그중 새로 찾은 것 1건.**

| 심각도 | 건수 | 번호 |
|---|---|---|
| P0(오늘 관측·거래에 직접 위험) | **0건** — 단 이것은 「07:30~08:02 사이에 P0가 관측되지 않았다」이지 「오늘 P0가 없다」가 아니다 | — |
| P1(정확성·관측이 훼손된다) | 0건 | — |
| P2(운영 부담·기술부채·확인 필요) | 5건 | 1-1 · 1-2 · 1-3 · 1-4 · 1-5 |
| 그중 **오늘 새로 찾은 것** | 1건 | **1-2**(가설 하나가 판정 예정일보다 5일 늦도록 안 닫혔다) |

나머지 4건(1-1·1-3·1-4·1-5)은 전부 **어제 이전부터 있던 것을 오늘도 확인**했거나(만성), 이미 결정된 것을 오늘 자리에서 재확인한 것이다. 새 결함이 아니다.

**12:35 갱신(장중① 회차).** 개장(09:00) 이후에도 계속 정상이다. 오전 내내 오류(ERROR) 0건, 재기동 0회, 3분 넘는 관측 공백 0건 — 프로그램이 07:30에 한 번 켜진 채로 지금까지 끊김 없이 돌고 있다. **사람이 지금 해야 할 일은 없다.** 장전이 지적한 문제 5건은 전부 그대로 지속 중이며(만성 3건 + 결정 대기 2건), 이 회차에서 심각한(P0·P1) 새 문제는 찾지 못했다. 다만 자동 점검기가 "판단을 내리는 4개 축이 47분째 안 보인다"고 경보를 냈는데, 확인해 보니 이것은 죽은 것이 아니라 **"바뀐 것이 없어서 로그를 안 찍은" 정상 동작**이었다(자세한 근거는 §1-6). 자세한 내용은 「제2부 장중①」참조.

**14:40 갱신(장중② 회차, 14:50 신규진입 컷오프 직전 마지막 점검).** 여전히 정상이다. **사람이 지금(14:50 전) 해야 할 일은 없다.** 12:35 이후 2시간 5분 동안 시세 요청 응답이 느려지는 속도(오후 열화 게이지, §제2부②)를 10~15분 단위로 살펴봤는데, 위험 수준까지 나빠지는 곳은 없었다 — 시세 조회 응답시간의 중앙값은 하루 내내 참고 못 참는 한계선(4.0초)의 71%를 넘은 적이 없다. 다만 14:30 딱 1분 동안 옵션 시세가 통째로 안 들어온 순간이 한 번 있었다(→ 신규 1-7, 다음 분에 바로 정상화됨 — P2). 12:30이 지적한 문제 5건은 전부 그대로 지속 중이고(전부 만성이거나 결정 대기, 새 것 아님), 12:30의 "4개 축이 안 보인다"는 오탐은 이번 회차엔 아예 다시 뜨지 않았다(재발 없음으로 재확인). **14:50 컷오프까지 사람이 개입할 이유 없음 — 오늘 남은 시간은 그대로 관측만 계속한다.**

---

# 제1부 — 장전 (07:30 ~ 09:00)

## 1-0. 전일 이월 — 직전 거래일 08-31은 종합 완성본으로 닫혀 있다

08-31 파일은 이상점 1-1~1-17, 사용자 조치 1~20번이 전부 처분된 채 닫혔다(제6부 「사용자 몫으로 남은 것」이 그 시점의 미해결 전체 목록이다). 오늘 파일은 **번호를 1-1부터 새로 쓴다**(하루 단위 채번). 08-31이 남긴 것 중 오늘 아침 시점에서 아직 열려 있는 항목을 아래로 옮긴다.

| 08-31이 남긴 것 | 오늘 처분 | 근거 |
|---|---|---|
| ⏰ 레버 E(혼잡 시간대 위클리 폭주 억제) 발동 여부 — 내일 07:30 기동 전이 시한 | ✅ **해소** — 어제 저녁 18:45에 사람이 이미 켰고, 오늘 07:30 기동이 그 값을 그대로 실었다 | 증거 §7 `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` **켜짐** · `git log cb6c28b` 08-31 18:45:31 · `NEXT_TODO.md:30` `[x]` → **1-1**(판정) |
| ⏰ 사전 대응 규칙(`2026-08-04-p5`) 발동 여부 — 레버 E와 같은 손잡이 | ✅ **해소** — 레버 E와 함께 닫혔다(08-31 저녁 DECISION_LOG) | 위와 동일 손잡이 |
| 예측 파일 닫기(도래 13 확정 + 미등재 10 inconclusive) | ✅ **해소** — 어제 저녁 커밋 `3c1cdfe`로 완료 | `hypotheses.yaml` mtime 2026-08-31 · 증거 §8-1 「직전 보고서가 언급한 id는 전부 yaml에 있다」 |
| 08-27·08-28 결번 회차 사후 재구성 | ✅ **해소** — 어제 저녁 커밋 `bd1f65e`로 완료 | `docs/동작점검/2026-08-27_마흐디_일일점검.md`·`2026-08-28_마흐디_일일점검.md` 존재 확인 |
| 이벤트 캘린더 `covered_through`를 9월분으로 연장 | 🔄 **지속** — 실측 만기 1건만 추가됐고(`d0a126f`) `covered_through`는 안 옮겨졌다. **9월 일정 확인은 사람 몫**이라 자동조치가 못 끝냈다 | 증거 §5 07:31:10 경고 오늘도 발화 → **1-3**(만성) |
| 결번 재발방지(MISSING_CHECK 표식 파일) 미구현 | 🔄 **지속** — `NEXT_TODO.md:54~60`에 계획만 있고 착수 전이다. 오늘 관측 범위에서 결번은 없었으므로 급하지 않다 | 이월(다음 fix 착수 회차) |
| GEX 광폭 체인 재개 여부 — 08-04 폐기, 재개 조건이 08-31에 처음 성립 | 🔄 **지속 — 사람 결정 대기.** 08-31 회차 권고("며칠 더 본다")가 아직 유효하다 | `NEXT_TODO.md:39~40` |
| `.claude/` 커밋 여부 · `dashboard_build_prompt_1.md` 커밋 여부 | 🔄 **지속 — 미결정.** 오늘 `git status`에도 그대로 남아 있다(미커밋 2건) | 위 헤더 참고 |
| Claude 앱 예약 4종이 08-27~08-28 왜 안 떴는가 | 🔄 **지속 — 저장소 밖이라 이 회차가 답할 수 없다.** 오늘도 4종 중 첫 회차(장전)가 정시(08:01)에 떴다는 사실만 보탠다 | — |
| 레버 `use_effective_member_count`(판단에 참여한 축만 세는 스위치)의 실제 발동일을 코드 주석에 적기 | 🔄 **지속** — `strategy_params.yaml:94` 위 주석에 아직 발동일이 없다. `hypotheses.yaml` 쪽 날짜는 어제 저녁 정정됐다(08-25→08-23) | → **1-4**(만성) |
| 만성 — ZN/ES 해외선물 조회 권한 결손 | 🔄 **지속(만성)** | → **1-5** |

---

## 장전 체크리스트 (`phases.md` A절)

### A-1. 기동 — 7항목 전부 확인됨

| 항목 | 확인 |
|---|---|
| 07:30 `start_mahdi_premarket.bat` 실행 | ✅ `premarket_startup.log` 07:30:00.84 「Mahdi 장전 기동 시작」 |
| docker(redis/postgres) 기동 성공 | ✅ 07:30:13 Docker Desktop 실행 → 07:30:32 데몬 준비 완료 → `docker compose up -d`, 실패 문구 없음 |
| COCKPIT(streamlit) 기동 | ✅ `.last_cockpit_start.txt` = 2026-09-01T07:31:20 |
| 관측 루프 기동 — `직전 정상 기동` 표식 1회 | ✅ 07:31:00 1회만. 2회 이상(재기동)이 아니다 |
| `.last_successful_start.txt`가 오늘 시각인가 | ✅ 2026-09-01T07:31:00 |
| 마스터 파일·토큰·WS 구독 완료, 실패·재시도 횟수 | ✅ 로거 상위 목록에 실패·재시도 관련 ERROR 없음(ERROR 0건) |
| 워치독 스케줄 무장 — 감시 창 개시 후 OK | ✅ 당일 3행, OK 3 / 비-OK 0. `.watchdog_last_check.json` 08:02:01 `action: ok` |

### A-2. 오늘의 계획

| 항목 | 확인 |
|---|---|
| `hypotheses.yaml` 검증예정일 오늘(2026-09-01)인 pending | **13건** — 증거 §8 표. 대부분 08-31에 새로 등재된 것들이라 실측(장중·장후 데이터)이 있어야 판정 가능. 이 회차는 목록만 확인한다 |
| 예정일이 지난 pending 항목 | **1건** — `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`(예정일 08-27, **5일 지남**) → **1-2** |
| `NEXT_TODO.md`의 「켤 것」 레버가 실제로 켜졌는가 | ✅ 레버 E — `NEXT_TODO.md:30` `[x]`, 증거 §7에서 코드값 확인. 위 1-0 표 참고 |
| 전일 커밋이 오늘 프로세스에 실렸는가(커밋 시각 < 기동 시각) | ✅ HEAD `cb6c28b` 커밋 08-31 18:45:31, 오늘 기동 07:31:00보다 한참 앞선다. 오늘 도는 코드와 커밋된 코드가 같다 |
| 전 거래일 지표·증거에 `🔔 사전 대응 규칙 조건 성립`이 있는가 | ✅ 있었고, **어제 저녁에 이미 발동으로 처리됐다**(레버 E와 같은 손잡이). 오늘 아침에 새로 결정할 것이 없다 |
| 전일 보고서를 `--verify-report`로 통과 | ✅ 실행함(아래) |

**`--verify-report` 결과** (대상: `docs/동작점검/2026-08-31_마흐디_일일점검.md`):

```
(나) 폐기·종결 목록과 부딪히는가 → ⚠ 2줄 걸림(755·1395행, 키워드 "광폭 체인")
(다) 지목한 파일이 리포에 실재하는가 → ⚠ 2개 경로 부재(148행 mahdi/ops/alerting.py · tools/collect_evidence.py)
```

**판정 — 둘 다 오탐이다.** (나)의 두 줄은 08-31 본문이 "광폭 체인을 열지 않는다"고 **폐기 사실을 인용하며 설명한 문장**이지, 되살리는 문장이 아니다(직접 원문 확인). (다)의 두 경로는 08-27과 같은 패턴의 오탐이다 — `mahdi/ops/alerting.py`는 실재 모듈 `mahdi/notify.py`를 가리키는 "앞으로 만들 파일" 서술이고, `tools/collect_evidence.py`는 실재 경로 `docs/동작점검/tools/collect_evidence.py`를 줄여 적은 것이다. **새 이상점으로 올리지 않는다.**

### A-3. 전일 이월 — 위 1-0에서 처리 완료

---

## 1-1. 레버 E(혼잡 시간대 위클리 폭주 억제)가 어제 저녁 지시대로 오늘 기동에 실렸다 ✅ **판정(정상) · P2**

**증상 아님 — 확인 항목.** 어제(08-31) 저녁 18:45, 사람이 대화로 직접 지시해 `mahdi/main.py`의 `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`를 빈 값에서 `{10: 4, 11: 4, 12: 4, 13: 4, 14: 4}`로 바꿨다(프로세스가 안 떠 있는 상태에서 값만 바꿔 재기동은 강제하지 않았다). 오늘 아침 정규 07:30 기동이 그 코드를 그대로 집어드는지가 이 회차가 확인해야 할 첫 번째 항목이었다.

**근거.** 증거 §7 레버 표: `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` = `{10: 4, 11: 4, 12: 4, 13: 4, 1…` · **켜짐** · 무조건발동일 `2026-09-01(오늘)`. `git --no-optional-locks log -1 cb6c28b` → `2026-08-31 18:45:31 +0900`, 07:31:00 기동보다 앞선다. `NEXT_TODO.md:30` 체크박스도 `[x]`로 닫혀 있다.

**기준.** `docs/동작점검/tools/phases.md` A-2 — "전일 커밋이 오늘 프로세스에 실렸는가(커밋 시각 < 기동 시각)". 오늘은 충족.

**영향.** 위클리 옵션을 혼잡한 시간대(10~14시)에 2분 대신 4분 간격으로만 조회하게 되어, 시세 요청이 하루 약 700건 줄어들 것으로 기대된다. 대신 그 시간대 위클리 시세를 보는 빈도가 절반(50%→약25%)으로 준다. **이 대가가 실제로 나타나는지는 오늘 장중·장후 관측이 답한다**(→ 오늘 관측이 답해야 할 것 ①).

---

## 1-2. 🆕 가설 하나가 판정 예정일보다 **5일** 늦도록 pending으로 남아 있다 — P2

**증상.** `hypotheses.yaml`의 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`(검증예정일 **2026-08-27**)가 오늘도 상태 `pending`이다. 이 가설은 "사전 대응 규칙의 발동 시점이 다음 거래일 **장전(07:30 기동 전)**에 온다"는 것을 주장한다.

**근거.** 증거 §8 표 — 이 id의 검증예정일란에 `2026-08-27 (5일 지남)`으로 표시됨. `hypotheses.yaml:5857~5882`의 원문에도 상태가 아직 `pending`으로 남아 있다.

**기준.** `docs/동작점검/tools/phases.md` A-2 — "예정일이 지난 pending 항목 — 손 판정이 밀리고 있다는 뜻이다". 같은 문서 C-3 — "소급 적용 금지. 예정일이 지난 항목에 예측을 새로 붙이지 않는다. `inconclusive`로 닫는다."

**참고 — 왜 이 가설이 붕 떴는지.** 이 가설이 말하는 "발동"은 실제로 **어제(08-31) 저녁**에 일어났다(레버 E와 같은 손잡이, → 1-1). 다만 그 발동은 이 가설이 예정한 시각(다음 거래일 **장전**, 즉 오늘 아침 점검 회차)이 아니라 **그 전날 저녁**에, 정규 점검 회차 밖에서 이뤄졌다. 그리고 이 가설 자체의 검증예정일(08-27)은 그보다 훨씬 전에 이미 지나 있었다 — 08-27부터 오늘까지 다섯 거래일 동안 아무도 이 가설의 상태를 건드리지 않았다.

**영향.** 판정 창이 지난 가설을 그대로 열어 두면, 나중에 누군가 오늘 이후의 숫자로 그때를 "확인됐다"거나 "틀렸다"고 소급 판정할 위험이 생긴다. 그러면 무엇이 무엇을 증명했는지 더는 못 가른다.

**제안(확정은 사람 몫).** 이 가설을 `inconclusive`로 닫는다 — 사유: "검증예정일(08-27)을 5거래일 넘겼고, 그사이 08-27~08-28 점검 결번(6회차)까지 겹쳐 그 창 안에서 판정할 기회 자체가 없었다. 실제 발동은 08-31 저녁, 정규 장전 회차 밖에서 일어나 이 가설이 주장한 '다음 거래일 장전' 형태와도 다르다." (→ 사용자 조치 1번)

---

## 1-3. 만성 — 이벤트 일정표가 여전히 만료된 채다 · 오늘도 경고 발화 🔄 **지속 · P2**

**증상.** `mahdi/config/event_calendar.yaml`의 `covered_through: 2026-08-28`가 오늘(09-01)보다 앞서 있어, 오늘도 기동 직후 경고가 났다.

**근거.** `logs/observation_loop.log` 07:31:10 `WARNING:mahdi.main:이벤트 캘린더 미기입: not_covered — ... covered_through(2026-08-28)가 오늘(2026-09-01)보다 앞선다`. 08-31 저녁 커밋 `d0a126f`가 실측 만기 1건(09-03)만 추가했고 `covered_through` 자체는 "날짜만 미는 것은 금지"라는 원칙 때문에 사람이 9월 일정(금통위·FOMC·CPI·고용지표)을 확인한 뒤에만 옮기기로 되어 있다.

**기준.** `NEXT_TODO.md:51~53` "이벤트 캘린더 covered_through를 옮긴다 — 실제로 확인해 넣은 뒤에만".

**영향.** 오늘 금리 발표 같은 큰 일정이 코앞이어도 프로그램이 평소보다 몸을 사리지 않는다. 일정이 없는 날이면 아무 일도 안 일어나고, 있는 날이면 평소 크기로 베팅한다. (→ 사용자 조치 2번)

---

## 1-4. 만성 — 이미 켜진 레버의 발동일이 코드 주석에 아직 없다 🔄 **지속 · P2**

**증상.** `use_effective_member_count`(판단에 참여한 축만 세는 스위치)는 이미 켜져 있고 실제 발동일도 2026-08-23으로 밝혀졌는데(git blame `f673b29`), 그 사실이 `strategy_params.yaml:94` 위 주석에는 아직 안 적혀 있다.

**근거.** 증거 §7 "레버 `use_effective_member_count`는 이미 켜져 있다(현재 줄: `use_effective_member_count: true`). 무조건발동일(2026-08-23)만 9일 지난 채 남았다 — 실제로 켠 날짜를 그 줄 주석에 적을 것." `strategy_params.yaml:91~94` 주석은 아직 "켠 날짜가 여기 없으면 또 git log를 뒤져야 한다"고만 적혀 있다.

**기준.** 08-31 증거 §7의 동일 지적(그때도 "정리 항목"으로 분류, 새 발견 아님).

**영향.** 다음에 이 레버의 발동일을 확인할 사람이 또 `git log`를 뒤져야 한다. 동작에는 영향이 없다 — 순수하게 기록 정리 문제다. (→ 제4부 Fix P2-1)

---

## 1-5. 만성 — ZN/ES 해외선물 근월물 조회 실패 🔄 **지속 · P2**

**증상.** 오늘도 기동 직후 미국 국채선물(ZN)과 E-mini S&P500선물(ES) 근월물 조회가 실패했다.

**근거.** `logs/observation_loop.log` 07:32:35 `ZN(10년 국채선물) 근월물 조회 실패: ZNU6 — CBOT SUB거래소 신청 계좌가 아닙니다` · 07:32:48 `ES(E-mini S&P500) 근월물 조회 실패: ESU6 — CME SUB거래소 신청 계좌가 아닙니다`.

**기준.** 08-31 증거·보고서에서 이미 "거래소 이용 신청이 안 된 계좌라 코드 문제가 아니다"로 확정된 만성 항목(`NEXT_TODO.md` §「08-13 이후로 넘긴 것」 — ZN/ES 권한).

**영향.** 여러 시장(주식·채권·환율 등)이 동시에 흔들리는 정도를 재는 지표(cross-asset stress — 국채·주가지수 선물 등 여러 자산의 움직임을 한데 모아 보는 위험 신호) 계산에서 이 두 자산이 계속 빠진 채로 간다. 계좌 권한 신청 여부는 사람이 결정할 일이라 코드로 못 고친다. (→ 사용자 조치 「결정이 필요한 것」에 이미 이월 중 — 새로 올리지 않음)

---

## 오늘 관측이 답해야 할 것 — 장중·장후가 닫는다

| 번호 | 물음 | 어떻게 답이 나오는가 | 언제 |
|---|---|---|---|
| **①** | 레버 E의 대가(위클리 커버리지 50%→약25% 하락, 예산 초과 건수 감소)가 실제로 나타나는가 | `auto/2026-09-01_지표.json`의 `db.book_coverage` · `budget_exceeded.count`(08-31 이전 기준선과 비교) | 장후(`2026-08-12-eE-on-congested-hours` 첫 실측 판정) |
| **②** | 오늘 검증예정일이 도래한 가설 13건이 각각 확인/반증/판정불가/미실행 중 무엇인가 | 증거 §8 목록, 장중·장후 데이터 필요 | 장후 |
| **③** | 08:30 만기유동성 버스트(북별 홀수분 슬롯)가 예정대로 도는가 | 이 회차의 관측 범위(08:02)는 08:30 이전이라 아직 「아직이다」다 | 장중 ① |
| **④** | 1-2 가설(`p1-3-preemptive-rule-fires-at-premarket`)을 `inconclusive`로 닫을지 | 사람 확정 필요 — 제안은 §1-2에 적어 뒀다 | 장후(사람 확정) |
| **⑤** | 09:00 개장 이후에도 ERROR 0건·재기동 0회가 유지되는가 | 관측 루프 상태, 장중 두 회차가 확인 | 장중 ①·② |

---

## Fix 구현계획 (초안) — 적용은 전부 장후

> ⛔ 이 회차는 코드를 고치지 않는다. 아래는 계획이다. 착수 전에 `hypotheses.yaml`에 예측치를 등재한다.

### P2-1. 이미 켜진 레버(`use_effective_member_count`)의 발동일을 코드 주석에 남긴다 (1-4)

- 대상: `mahdi/config/strategy_params.yaml:91~94`
- 바꿀 것: 91~93행 주석 끝에 `# 실제로 켠 날짜: 2026-08-23 (git f673b29)` 한 줄 추가
- 회귀 위험: 없음(주석만)
- 검증: 다음 증거 §7 조회에서 이 항목이 "정리 필요" 목록에서 빠지는가

### P2-2(결정 대기). 가설 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`을 어떻게 닫을 것인가 (1-2)

- §1-2의 제안(`inconclusive`, 사유 명시)대로 닫을지, 아니면 08-31 저녁 발동을 "장전" 발동으로 인정해 다른 방식으로 판정할지 — **사람이 정할 일**이다. 이 회차는 제안만 하고 `상태`를 바꾸지 않았다.

---

## 고도화 방안 (초안) — 오늘 관측에서 나온 것만

### 고도화 1. 정규 회차 밖에서 일어난 레버 발동을 증거 §7 표에서 구분한다 (근거: 1-1 · 1-2)

- **관측 근거**: 레버 E는 이번에도(레버 F가 08-23에 그랬듯) 정규 장전 점검 시각이 아니라 **저녁 대화 중 직접 지시**로 켜졌다. 이 자체는 규약 위반이 아니지만(phases.md A-2가 "발동은 사람이 누른다"고만 요구하지 시각을 못박지 않는다), 이 발동 시점이 관련 가설(`p1-3-...-fires-at-premarket`)이 예정한 시점과 어긋나면서 그 가설의 판정을 5일째 붕 뜨게 만들었다.
- **제안**: `collect_evidence.py` §7 레버 표에 "마지막으로 값이 바뀐 시각"과 "그 시각이 정규 점검 회차(08:00/12:30/14:30/16:00) 안인가 밖인가"를 한 열 추가한다.
- **기대 효과**: 다음에 비슷한 "발동 시점" 가설을 쓸 때, 그 가설이 실제로 무엇을 재는지(코드 값이 바뀐 시각인가, 정규 회차가 그것을 보고한 시각인가)를 미리 갈라 쓸 수 있다.
- **비용·위험**: 낮다 — git log 조회 1줄 추가.
- **우선순위**: 다음 단계(급하지 않음).

---

## dev_memory 반영 초안 — 장후 세션이 실제로 반영한다

> 이 회차(자동 예약, 장전)는 dev_memory를 직접 수정하지 않는다.

- **DECISION_LOG 추가 후보**: "2026-09-01 — 레버 E가 정규 장전 회차 밖(08-31 저녁)에서 켜지면서 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` 가설의 판정 창을 붕 뜨게 만들었다. 다음에 이런 '중간 발동'이 있으면 그 즉시 관련 가설의 검증예정일도 함께 옮길 것."
- **NEXT_TODO 추가 후보**: `mahdi/config/strategy_params.yaml:94` 위 주석에 실제 발동일(2026-08-23) 기입 — P2, 5분 이내 작업.

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (개장 전)

**사람 조치 없음.** 어제 저녁에 핵심 결정(레버 E 발동)이 끝났고, 오늘 기동에 반영된 것까지 이 회차가 확인했다.

### 오늘 중 (급하지 않음, 전부 이월 중인 항목 — 새로 생긴 것 아님)

1. **가설 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`을 어떻게 닫을지 정한다.** (근거: 1-2) — 이 회차의 제안: `inconclusive`로 닫고 사유는 §1-2에 적어 둔 문장을 쓴다. 안 정해도 오늘 거래에는 영향 없다.
2. **이벤트 캘린더 `covered_through`를 9월분으로 확인해 옮긴다.** (근거: 1-3) — 9월 금통위·FOMC·미국 CPI·고용지표·옵션 만기(09-03·09-10)를 실제로 넣은 뒤 그 마지막 날로 옮긴다. 날짜만 미는 것은 금지.
3. **`.claude/`를 커밋할지 `.gitignore`에 넣을지 정한다.** (08-27부터 이월) — 정하고 나면 `docs/Dev_md/dashboard_build_prompt_1.md`와 함께 한 커밋으로 정리한다.

### 결정이 필요한 것 (자동으로 못 하는 것)

4. **「GEX 광폭 체인」 재개 여부.** (08-31 이월) — 재개 조건이 08-04 폐기 이후 처음 성립했지만, 08-31 회차의 권고("며칠 더 본다")가 아직 유효하다. 이 회차는 열지도 닫지도 않는다.
5. **ZN/ES 해외선물 거래소 이용 신청 여부.** (만성, 1-5) — (A) CME/CBOT SUB거래소 신청 (B) 매크로 축에서 공식 제외. 코드로 못 고치는 계좌 문제다.
6. **Claude 앱 예약 4종이 08-27~08-28에 왜 안 떴는가.** (다섯 회차째 이월) — 저장소 밖이라 이 회차가 답할 수 없다. 오늘도 장전 회차가 정시(08:01)에 떴다는 사실만 보탠다.

---

# 제2부 — 장중 ① (12:30 회차)

- **국면**: 장중① (intra) · **예정 12:30 / 실행 12:35~12:39 KST — 정시 실행**(5분 이내, 밀리지 않았다)
- **관측 범위: 07:30:00 ~ 12:35:24**(장전 관측 범위 07:30~08:02를 이어받아 개장 후 3시간 35분을 더 본다)
- 증거 다이제스트: `docs/동작점검/auto/2026-09-01_증거_intra.md` (12:35:24 생성, 57.6KB)
- HEAD `cb6c28b`(변동 없음) · 브랜치 `master` · 미커밋 3건(`.claude/` · `dashboard_build_prompt_1.md` · 이 일일점검 파일 자신 — 전부 이월 중인 항목, 새 것 아님)
- ⛔ **이 회차는 코드 · 설정 · `hypotheses.yaml`의 `상태` · dev_memory를 한 줄도 바꾸지 않는다.**

## 2-0. 장전 이월 처분 — 5건 전부 처분 + 관측 항목 5건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 레버 E(혼잡시간대 위클리 4분 격분)가 오늘 기동에 실렸다 | 🔄 **지속(대가 아직 미판정)** | 오늘도 09~12시 위클리 관련 예산초과 69건 발생 중 — 대가가 실제로 줄었는지는 하루치 델타가 다 모여야 답이 난다. 최종 판정은 장후(→ 관측①) |
| 1-2 가설 `2026-08-26-p1-3-…`이 예정일보다 5일(오늘 기준도 그대로) 늦게 pending | 🔄 **지속 — 사람 결정 대기** | 증거 §8 표에도 동일하게 `pending`·`(5일 지남)`으로 남아 있다. `hypotheses.yaml` mtime 08-31 그대로(안 바뀜) |
| 1-3 이벤트 캘린더 `covered_through` 만료 · 경고 계속 발화 | 🔄 **지속** | 오늘도 07:31:10부터 12:34:11까지 **6회** 같은 경고(매시 정각 직후 1회, 최근 3600초 내 억제 적용) — 만료 상태 그대로 |
| 1-4 레버 `use_effective_member_count` 발동일 주석 미기재 | 🔄 **지속** | `strategy_params.yaml:91~94` 무변경 확인 |
| 1-5 ZN/ES 근월물 조회 실패(만성) | 🔄 **지속** | 오늘도 07:32:35·07:32:48에 이어 11:32:40·11:32:42에도 같은 실패(각 5건) — 장중에도 반복되는 만성 |
| (관측①) 레버 E의 대가가 실제로 나타나는가 | ➡️ **이월 — 장후로.** 오전 데이터만으로는 델타를 못 낸다(전일 `_지표.json` 대비 비교가 하루치 다 있어야 함) | §5-1 증거: 사전 대응 규칙 조건이 오늘도 12구간에서 성립 — 4분 격분에도 혼잡시간대 지연은 여전하다 |
| (관측②) 오늘 검증예정일 도래 가설 13건 | ➡️ **이월 — 장후로.** 실측 데이터가 하루치 다 필요하다 | 증거 §8 — 13건 전부 `pending` 그대로 |
| (관측③) 08:30 만기유동성 버스트가 예정대로 도는가 | ✅ **판정 — 정상.** | `logs/observation_loop.log` 08:35:41 `만기 유동성 버스트 완료: 북 3개(regular,weekly_mon,weekly_thu) · 30레그 · 적재 3행 (08:31~08:35)` — 예정대로 돌았다 |
| (관측④) 1-2 가설을 `inconclusive`로 닫을지 | ➡️ **이월 — 사람 확정 필요.** 이 회차는 판단 권한이 없다 | 장전 §1-2의 제안이 아직 유효 |
| (관측⑤) 09:00 이후에도 ERROR 0건·재기동 0회가 유지되는가 | ✅ **판정 — 유지됨.** | 증거 §4 「ERROR 이상 — 0건」(레벨 집계 INFO 10291 / WARNING 235, ERROR 0) · §3 「프로세스 기동 1회」· 「3분 이상 공백 0건」 |

## 장중 체크리스트(`phases.md` B절) — 전체 통과

| 항목 | 확인 |
|---|---|
| **B-1 생존** — 관측 루프 마지막 레코드와 현재 거리 | ✅ 최종 레코드 12:35:24, 조회 시각과 사실상 동시. 07:30~15:45 구간 3분 이상 공백 **0건** |
| **B-1 워치독** — 마지막 기록 이후 무기록 구간 | ✅ 당일 30행(OK 30 / 비-OK 0). 감시 창 끝(12:35)까지 무기록 **5분**(정상 주기 10분 이내) |
| **B-1 크래시** — `observation_loop_crash.log` | ✅ 당일 기동(07:30:56) **이후** 트레이스백 없음. 파일 최종수정도 07:30 기동 표식뿐 — mtime만 보고 오판하지 않도록 표식 이후만 확인함 |
| **B-2 수집** — 예산초과/연속타임아웃/실패예산 3분류 | 예산초과 **69건**(전체 사이클 대비 정상 범위) · 연속타임아웃 조기포기 **23건** · 실패예산 소진 **0건(계측 경로 살아있음 — 08-26에 36건 찍힌 전례로 확인)** |
| **B-2 WS 단절** — 횟수·시간대 집중도 | 5회, **전부 매시 정각(08:00:01·09:00:01·10:00:01·11:00:01·12:00:01)** — 가설 `2026-08-31-wiring2`가 주장한 "정각 외 끊김만 사건"과 정확히 일치. 정각 외 끊김 0건 |
| **B-2 ATM 롤링** | 159건, 08~12시 고르게 분포(08시6·09시51·10시63·11시30·12시9) — 급격한 왕복 쏠림 없음 |
| **B-3 판단 도달 — 앙상블 멤버** | 비영 멤버(0점 아닌 축) 최소 1 / 최대 3, 최근 30분은 4/6 가용 유지 — 자세한 것은 §1-6 |
| **B-3 레짐 갱신** | 09:15 WARMUP 종료 이후 분마다 갱신, 12건 전이. WARMUP 재진입 없음 |
| **B-3 체인 신선도** | 로그 축 없음(정상 — DB 축이 담당, 장후 지표가 답한다) |
| **B-3 이벤트 캘린더 미기입** | 6건(매시 1회, 1-3과 동일 사안) |

---

## 1-6. 🆕 자동 적신호 "앙상블 멤버 4개가 47분째 미관측"은 오탐이다 — 확인 결과 정상 · P2

**증상(자동 적신호 원문).** 증거 §12의 5~8번이 `flow_position`·`options_flow`·`orderflow_ofi_vpin`·`regime_hmm` 네 축이 **11:48:11 이후 47분째 미관측**이라고 경보했다. 문면만 보면 "판단에 참여하는 축 넷이 동시에 죽었다"로 읽힌다.

**근거 — 직접 원본을 확인한 결과, 죽은 것이 아니다.**

1. `mahdi/fusion/engine.py:243~244`의 로깅 코드: `가용멤버·허가·사유·전략`을 하나의 "형태"로 묶어, **직전 사이클과 같으면 로그를 아예 안 찍는다**(`if shape == self._last_shape: return`). 이 함수 자체의 주석(228~236행)이 "전이만 남기면 정상일에는 수 건"이라고 설계 의도를 밝히고 있다 — **매분 계산은 하되, 안 바뀌면 안 찍는 것이 원래 설계다.**
2. 그런데도 **매분 계산이 실제로 돌고 있다는 증거**: `mahdi.main`의 "주문 미제출" 로그가 11:48부터 12:35까지 **분 단위로 끊김 없이** 찍혔다(`12:30:11` · `12:31:11` · `12:32:11` · `12:33:11` · `12:34:11` · `12:35:11` — 전부 확인). 이 로그는 퓨전 엔진의 판단 결과(`실행 경로 전제 미충족: mode_not_auto_submit,reentry_cooldown_not_configured,no_entry_plan`)를 매분 그대로 인용한다 — **엔진이 죽었으면 이 줄도 안 나온다.**
3. 사유 문구(`mode_not_auto_submit,reentry_cooldown_not_configured,no_entry_plan`)가 11:44~12:35 내내 **한 글자도 안 바뀌었다** — "형태가 바뀌지 않아서 안 찍혔다"는 설명과 정확히 들어맞는다.

**기준.** `docs/동작점검/tools/phases.md` 규약 C·D 취지 — "건수 0은 진짜 없었거나, 계측이 없거나 둘 중 하나다. 어느 쪽인지 로그로 구분한다." 이번 경우는 셋째 유형이다: **계측은 있고, 값도 정상 계산되지만, dedup 설계상 "안 바뀌면 안 찍는다."** 자동 수집기(§8 자동 적신호 로직)가 이 셋째 유형을 아직 구분 못 해 "미관측=위험"으로만 읽는다.

**영향.** 오늘은 오탐이라 실질적인 피해는 없다. 하지만 이 문구를 그대로 믿으면, 다음에 정말 축 하나가 죽었을 때도 "또 그 오탐이겠지"라며 넘길 위험이 생긴다 — **양치기 소년 문제**다. (→ 아래 고도화 1로 자동 점검기 자체를 고칠 것을 제안)

---

## 14:30 회차가 이어받을 것

- **관측①(레버 E 대가)**: 12시대까지의 예산초과 69건·사전 대응 규칙 12구간 성립을 이어서, 13~14시 구간도 같은 속도인지 확인. 최종 판정은 장후 지표(`_지표.json`)가 나온 뒤.
- **관측②**: 가설 13건 pending 그대로 — 14:30도 데이터 부족으로 판정 불가할 가능성이 높다. 넘겨도 된다.
- **관측④**: 1-2 가설 `inconclusive` 처리 여부 — 사람 확정 대기 계속.
- **1-6 관련**: 14:30 회차도 같은 자동 적신호(멤버 미관측)가 뜨면, 이번 회차의 판정 방법(주문 미제출 로그로 생사 교차확인)을 그대로 반복 적용할 것 — 매번 원본을 다시 파고들 필요 없이 이 절을 인용하면 된다.
- **새로 볼 것**: 14:50 신규 진입 컷오프 직전이므로, 13~14시대 REST 지연(p95/timeout 비율)이 더 악화되는지, ATM 롤링 왕복이 급증하는지 주시할 것.

---

## Fix 구현계획 — 이 회차는 신규 없음

> 장전 Fix 계획(P2-1: `use_effective_member_count` 발동일 주석 추가)이 그대로 유효하다. 적용은 장후.
> ⛔ 이 회차는 코드를 고치지 않는다.

## 고도화 방안 — 오늘 관측에서 나온 것만

### 고도화 2. 자동 적신호가 "미관측=위험"과 "미관측=dedup 정상"을 구분하게 한다 (근거: 1-6)

- **관측 근거**: 오늘 §12 적신호 5~8번이 실제로는 오탐이었다. 판별 방법은 있다 — 같은 시간대에 `mahdi.main`의 "주문 미제출"(또는 그에 준하는 매분 로그)이 계속 찍히고 있으면 엔진은 살아 있는 것이고, 그 로그마저 끊기면 진짜 죽은 것이다.
- **제안**: `collect_evidence.py`의 앙상블 멤버 미관측 적신호 로직에, "같은 구간 `mahdi.main` 매분 로그(주문 미제출/사이클 소요 분해 등)가 계속 찍히고 있는가"를 교차 확인하는 조건을 추가한다. 계속 찍히고 있으면 문구를 `"N분째 미관측(형태 무변화로 추정 — 교차확인: 엔진 생존)"`으로, 안 찍히면 지금처럼 그대로 경보로 남긴다.
- **기대 효과**: 다음에 축이 정말 죽었을 때 이 경보의 신뢰도가 지금보다 높아진다(양치기 소년 문제 완화).
- **비용·위험**: 낮음 — 기존 로그 파싱에 카운터 하나 추가.
- **우선순위**: 다음 단계(급하지 않음, P2).

---

## dev_memory 반영 초안 — 장후 세션이 실제로 반영한다

> 이 회차(자동 예약, 장중①)는 dev_memory를 직접 수정하지 않는다.

- **DECISION_LOG 추가 후보**: "2026-09-01 12:30 — 앙상블 멤버 미관측 자동 적신호가 47분 구간에서 오탐으로 확인됐다(퓨전 엔진 dedup 로깅 설계 때문). 판별법: 같은 구간 `mahdi.main` 매분 로그 생존 여부로 교차확인."
- **NEXT_TODO 추가 후보**: `collect_evidence.py`의 멤버 미관측 적신호에 엔진 생존 교차확인 로직 추가 — P2, 고도화 2 참고.

---

# 제2부 — 장중 ② (14:30 회차)

- **국면**: 장중② (intra 14:30) · **예정 14:30 / 실행 14:40:23~14:44 KST — 정시 실행**(10분 이내, 밀리지 않았다)
- **관측 범위: 07:30:00 ~ 14:40:44**(장중① 관측 범위 07:30~12:35를 이어받아 개장 후 2시간 5분을 더 본다)
- 증거 다이제스트: `docs/동작점검/auto/2026-09-01_증거_intra_1430.md` (14:40:45 생성, 58.5KB)
- 지연창 원자료: `docs/동작점검/auto/2026-09-01_지연창.tsv` (86창, 07:36~14:41, 5분 간격)
- HEAD `cb6c28b`(변동 없음, 08-31 18:45:31 커밋) · 브랜치 `master` · 미커밋 3건(`.claude/` · `dashboard_build_prompt_1.md` · 이 일일점검 파일 자신 — 전부 이월 중인 항목, 새 것 아님)
- ⛔ **이 회차는 코드 · 설정 · `hypotheses.yaml`의 `상태` · dev_memory를 한 줄도 바꾸지 않는다.**

## N-0. 장중① 이월 처분 — 6건 + 관측 항목 5건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 레버 E(혼잡시간대 위클리 4분 격분)가 오늘 기동에 실렸다 | 🔄 **지속(대가 아직 미판정)** | 13~14시대에도 같은 속도로 예산초과가 쌓였다(오늘 누적 107건, §5-1-1 표) — 대가 판정은 하루치가 다 모여야 나온다(→ 관측①, 장후로 이월 유지) |
| 1-2 가설 `2026-08-26-p1-3-…`이 예정일보다 늦게 pending | 🔄 **지속 — 사람 결정 대기** | `hypotheses.yaml` mtime 08-31 그대로, 상태 변화 없음 |
| 1-3 이벤트 캘린더 `covered_through` 만료 · 경고 계속 발화 | 🔄 **지속** | 12:34:11 이후에도 13:34:11·14:35:10 두 차례 더 발화(매시 정각 직후 1회 패턴 유지) — 만료 상태 그대로 |
| 1-4 레버 `use_effective_member_count` 발동일 주석 미기재 | 🔄 **지속** | `strategy_params.yaml:91~94` 무변경 확인 |
| 1-5 ZN/ES 근월물 조회 실패(만성) | 🔄 **지속** | 오늘 13:42:42·13:42:43에도 같은 실패 반복(누적 7건씩) — 장중 내내 반복되는 만성 |
| 1-6 앙상블 멤버 "47분 미관측" 오탐 | ✅ **판정 유지 — 재발 없음으로 재확인** | 이 회차 증거 다이제스트(§12 자동 적신호)에 "미관측" 문구가 **한 건도 없다**(12:30 회차 때와 달리 이번엔 그 적신호 자체가 안 떴다). 대신 14:04~14:24 사이 판단 형태가 여러 차례 정상 전이했다(4/6·비영3 유지, 뒤 §참고) — 12:30의 판정("dedup 정상 동작")이 이번에도 그대로 맞았다 |
| (관측①) 레버 E의 대가가 실제로 나타나는가 | ➡️ **이월 — 장후로.** 13~14시대도 09~12시와 같은 속도로 예산초과가 누적됐다(오늘 총 107건, 시간대별 09:20·10:21·11:17·12:17·13:21·14:11 — 고르게 분포, 특정 시간대 급증 없음). 최종 판정은 장후 `_지표.json`이 나온 뒤 | §5-1-1 |
| (관측②) 오늘 검증예정일 도래 가설 13건 | ➡️ **이월 — 장후로.** 여전히 13건 전부 `pending`. 실측 데이터가 하루치 다 필요하다 | 증거 §8 — 변화 없음 |
| (관측④) 1-2 가설을 `inconclusive`로 닫을지 | ➡️ **이월 — 사람 확정 필요.** 이 회차는 판단 권한이 없다 | 장전 §1-2·장중① 2-0의 제안이 아직 유효 |
| (관측) 「1-6 관련」 — 14:30 회차도 같은 미관측 적신호가 뜨면 판정법을 반복 적용 | ✅ **판정 완료 — 적신호 자체가 재발하지 않았다.** 반복 적용할 필요가 없었다 | 위 1-6 처분 참고 |
| (관측) 「새로 볼 것」 — 13~14시대 REST 지연 악화 여부, ATM 롤링 왕복 급증 여부 | ✅ **판정 — 악화 없음.** REST 지연(p50÷타임아웃)은 12:30까지의 최대 0.61배에서 14:00 창 0.71배로 소폭 올랐다가 14:30 창엔 다시 0.11~0.56배로 내려갔다 — 추세적 악화가 아니라 잡음 범위. ATM 롤링은 195건이 08~14시에 고르게 분포(08시6·09시51·10시63·11시30·12시12·13시24·14시9)해 특정 시간대 쏠림 없음 | 아래 「오후 열화 게이지」 표 참고 |

## 오후 열화 게이지 (창 단위 시계열 — 12:30 이후, 30분 창)

> 판정은 **창 최대**로 한다. p50÷타임아웃(4.0초)이 1.0에 닿으면 그 창 호출의 절반 이상이 시간 안에 못 돌아온다는 뜻이고, 0.8을 넘으면 경고선이다.

| 시간창 | 사이클 | rows=0 비율 | p50÷타임아웃(창 최대) | 비영 멤버(최소~최대/6) | 관측 공백 | WS 단절(누적) |
|---|---|---|---|---|---|---|
| 12:30~13:00 | 30 | 0/30 = 0% | 0.56배 | 3~3 | 0분 | 5회(변화 없음) |
| 13:00~13:30 | 30 | 0/30 = 0% | 0.64배 | 3~3 | 0분 | 6회(13:00:01 +1) |
| 13:30~14:00 | 30 | 0/30 = 0% | 0.68배 | 3~3 | 0분 | 6회(변화 없음) |
| 14:00~14:30 | 30 | 0/30 = 0% | 0.71배 | 3~3 | 0분 | 7회(14:00:01 +1) |
| 14:30~14:40 | 12 | 1/12 = 8.3% | 0.56배 | 3~3 | 0분 | 7회(변화 없음) |

- **경고선(rows=0 비율 ≥30%, p50÷타임아웃 ≥0.8)을 넘은 창은 하나도 없다.** 오늘 하루 전체(07~14시)로 넓혀도 p50÷타임아웃의 창 최대는 14:00창의 0.71배가 가장 높았다 — 경고선 0.8배에 못 미친다.
- **WS(웹소켓, 실시간 시세를 밀어 넣는 연결) 단절**은 12:30 이후 두 번(13:00:01, 14:00:01) 더 있었지만 **전부 매시 정각**이다 — 정각 외 끊김은 0건으로, 08-31에 세운 가설(`2026-08-31-wiring2` — "정각 1회는 KIS쪽 세션 경계이고 사건이 아니다")과 그대로 들어맞는다. 증가 추세 아님.
- **비영 멤버(판단에 실제로 점수를 낸 축) 수는 12:30부터 14:40까지 계속 3/6에 머물렀다.** 판단 형태 전이 로그는 "형태가 바뀌면만 찍는다" 설계라, 12:00~14:04 사이 로그가 아예 없다는 것 자체가 "그 사이 형태가 한 번도 안 바뀌었다"는 뜻이다(1-6에서 확인한 것과 같은 판독법).
- 참고 수치(경고선 대상은 아니나 자동 적신호에 늘 뜨는 값): 시세 응답시간의 **상위 5%(p95)** ÷ 타임아웃 비율은 10:01 창에서 이미 1.01배로 처음 1.0을 넘었고(장전·장중① 관측 범위 밖 — 12:30 이전) 이후 0.72~1.01배 사이를 오르내리며 **더 나빠지지도 더 좋아지지도 않은 채** 오후까지 이어졌다. 이 창은 「일부(5%)가 이미 막힌다」는 신호이지 「전체가 느려진다」는 신호는 아니다(경고선 대상은 중앙값 p50이다).

---

## 1-7. 🆕 옵션체인이 14:30 한 분 동안 통째로 비었다 — 다음 분에 바로 정상화 · P2

**증상.** 14:30 분에 옵션체인 20레그 중 10레그를 수집 시도했으나 그 10레그 전부(적재 0행)가 예산(28.5초) 안에 안 들어왔다. 오늘 첫 ERROR 로그다(12:30 회차까지는 ERROR 0건).

**근거.**
```
14:30:15 [WARNING] 옵션체인 연속 타임아웃 3회(리셋=아니오) — 적재 0행 · 컷당한북=regular
14:30:34 [WARNING] 옵션체인 연속 타임아웃 3회(리셋=아니오) — 적재 0행 · 컷당한북=regular
14:30:34 [ERROR] 옵션체인 이번 분 전멸 — 수집 10레그 중 적재 0행(예산 28.5초 소진, 재시도 함). 이 분의 체인은 비었다 — 판단은 신선도 창 안의 직전 스냅샷을 쓴다
14:30:34 [INFO] 옵션체인 사이클 소요 분해: REST수집 28.50초 (rows=0, 재시도함) 분=14:30
14:31:41 [INFO] 옵션체인 사이클 소요 분해: REST수집 40.64초 (rows=10) 분=14:31
```
바로 다음 분(14:31)부터 14:39까지 rows=10·14·14·10·10·16·20·10·10으로 **정상 회복**됐다. 오늘 하루 428사이클 중 rows=0은 이 1건뿐이었다(최장 연속 1분, 경보 임계 20분에 크게 못 미친다).

**기준.** `phases.md` B-2 — "실패 3분류(예산 초과/연속 타임아웃 조기 포기/실패 예산 소진)". 이 사건은 "연속 타임아웃 조기 포기"에 해당하고, 로그 자체가 "판단은 신선도 창 안의 직전 스냅샷을 쓴다"고 설계된 대응을 명시한다 — **정의된 폴백이 정의된 대로 작동한 사례**다. 같은 종류의 ERROR는 로테이션된 로그 전체에서 396건 나와 처음 겪는 패턴도 아니다.

**영향.** 이 1분 동안 옵션 시세 기반 판단(GEX·감마플립 등)은 그 직전 분의 스냅샷을 그대로 썼다. 사람이 체감할 손실은 없었다 — 다음 분에 바로 새 시세로 돌아왔고, 이 1분 동안 이 프로그램이 새로 주문을 내지도 않았다(§1-6에서 확인한 "실행 경로 전제 미충족" 상태가 이 구간에도 계속 유지됐다).

**제안.** 조치 불필요. 다만 하루 안에 이런 "1분 전멸"이 여러 번 겹쳐 뜨는지는 장후가 총 건수로 다시 본다(→ 관측 이월 없음, 장후 통상 점검 범위에 포함).

> ↩️ **"오늘 하루 428사이클 중 rows=0은 이 1건뿐"이라는 위 문장은 뒤집혔다 — §3-8(장후) 참조.** 14:40 이후(이 회차의 관측 범위 밖) 15:00~15:02·15:15~15:21에 같은 종류의 전멸이 10분 더 있었다. (본문은 14:44 당시 관측 범위 안에서는 사실이었다 — 그대로 보존한다)

---

## 14:50 컷오프 권고

- **지금(14:44 기준) 컷오프까지 6분 남음.**
- **개입 불필요.** 오늘 이 시각까지 P0·P1 없음, rows=0 비율·시세지연·앙상블·WS단절·관측공백 모두 경고선 아래. 1-7(14:30 전멸 1분)은 이미 스스로 회복됐고 재발 조짐도 없다.
- **재시동 손익 비교(참고용, 오늘은 해당 없음)**: 재시동 없이 얻는 것 — 오늘 관측 연속성과 남은 6분. 재시동으로 얻는 것 — 없음(고칠 것이 없다. 수집 경로가 이미 정상이다). 결정할 것이 없으므로 손익 비교 자체가 불필요하다.

---

## Fix 구현계획 — 이 회차는 신규 없음

> 장전 Fix 계획(P2-1: `use_effective_member_count` 발동일 주석 추가)이 그대로 유효하다. 적용은 장후.
> 1-7은 설계된 폴백이 정상 작동한 사례라 Fix 대상이 아니다.
> ⛔ 이 회차는 코드를 고치지 않는다.

## 고도화 방안 — 오늘 관측에서 나온 것만

### 고도화 3. "1분 전멸" ERROR가 다음 분에 회복됐는지를 증거 다이제스트가 스스로 표시하게 한다 (근거: 1-7)

- **관측 근거**: 오늘 §12 자동 적신호는 "ERROR 이상 1건"이라고만 찍었다. 사람이 로그를 15분 들여다봐야 "다음 분에 바로 회복됐다"는 것을 알 수 있었다. 이 확인은 매번 같은 절차(다음 분 rows 값 조회)라 자동화할 수 있다.
- **제안**: `collect_evidence.py`의 ERROR 집계에, "옵션체인 이번 분 전멸" 종류의 ERROR가 뜬 분(分) 바로 다음 분의 `rows=` 값을 같이 붙인다. 회복됐으면 `(다음 분 rows=N — 회복됨)`, 아직 0이면 `(다음 분도 rows=0 — 미회복, 주의)`를 자동으로 붙인다.
- **기대 효과**: 사람이 매번 로그를 열어보지 않아도 "1분짜리 잡음"과 "연속 전멸의 시작"을 다이제스트 한 줄로 구분할 수 있다.
- **비용·위험**: 낮음 — 기존 사이클 로그 파싱에 다음 줄 조회 1건 추가.
- **우선순위**: 다음 단계(급하지 않음, P2).

---

## dev_memory 반영 초안 — 장후 세션이 실제로 반영한다

> 이 회차(자동 예약, 장중②)는 dev_memory를 직접 수정하지 않는다.

- **DECISION_LOG 추가 후보**: "2026-09-01 14:30 — 오늘 첫 ERROR(옵션체인 이번 분 전멸)가 14:30 한 분에 떴고 14:31에 바로 회복됐다. 설계된 신선도 폴백(직전 스냅샷 사용)이 정상 작동한 사례로 확인, 조치 불필요."
- **NEXT_TODO 추가 후보**: `collect_evidence.py` ERROR 집계에 "다음 분 rows 회복 여부" 자동 표시 추가 — P2, 고도화 3 참고.

---

## 사용자 조치 (14:50 컷오프 반영)

> 이 절에 없는 것은 사람이 안 해도 되는 일이다. 아래는 장전·장중①이 이미 올린 항목의 최신 상태다 — 새로 생긴 항목 없음.

### 지금 (14:50 전)

**사람 조치 없음.** 개입할 이유가 없다(위 「14:50 컷오프 권고」 참고).

### 오늘 중 · 결정이 필요한 것

기존 목록(제1부 「사용자 조치」 1~6번)이 그대로 유효하다. 이 회차에서 새로 추가되거나 앞당겨진 항목은 없다.

---

# 제3부 — 장후 (15:20 ~ )

- **국면**: 장후 (post) · **예정 15:45 / 실행 15:46 KST — 정시 실행**
- **관측 범위: 07:30:00 ~ 15:45:00**(장중② 관측 범위 07:30~14:40을 이어받아 마감까지 전부 본다)
- 증거 다이제스트: `docs/동작점검/auto/2026-09-01_증거_post.md`(15:46:23 생성) · 자동 지표 `docs/동작점검/auto/2026-09-01_지표.{md,json}`(15:46 생성, 74.5KB/64.3KB) · 지연창 원자료 `auto/2026-09-01_지연창.tsv`(98창)
- HEAD `cb6c28b`(변동 없음, 08-31 18:45:31 커밋) · 브랜치 `master` · 당일 커밋 **0건** · 미커밋 3건(`.claude/` · `dashboard_build_prompt_1.md` · 이 일일점검 파일 자신 — 전부 이월 중, 새 것 아님)
- ⛔ **이 회차는 코드 · 설정 · `hypotheses.yaml`의 `상태` · dev_memory를 한 줄도 바꾸지 않는다.**

## 3-0. 장중② 이월 처분 — 6건 + 관측 항목 5건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 레버 E(혼잡시간대 위클리 4분 격분)가 오늘 기동에 실렸다 | 🔄 **지속(대가 참고값만 — 공식 판정은 09-02)** | `hypotheses.yaml`의 이 가설(`2026-08-12-eE-on-congested-hours`) **검증예정일은 2026-09-02**다(오늘이 아니다 — 소급 적용 금지 규약을 앞으로도 지키려면 **너무 이른 판정도 금지**해야 한다). 오늘 실측은 참고값으로만 남긴다: `budget_exceeded.count` 123건(08-12 기준선 74건 대비 **▲49**, 방향이 예측과 반대) · `db.signal_reach.chain_input_source.stale_pct` **98.8%**(기대 `<=70.0`, 큰 폭으로 미달) · `db.book_coverage` weekly_mon 32.0%·weekly_thu 32.8%(기대 대가 "50%→약25%"에 대략 근접, 정확히는 안 닿음) · `db.signal_reach.gex_input_missing_minutes` **6분**(기준선 4분 대비 ▲2, "먼슬리는 그대로여야" 조건에 살짝 못 미침). ⚠ **오늘 수치를 그대로 반증으로 읽지 않는다** — 오늘은 KIS 응답 자체가 하루 종일 나빴다(→ 1-8, 규약 F: 건수는 그날 KIS 상태에 비례). **09-02 장전 회차가 공식 판정한다.** → 사용자 조치 7 |
| 1-2 가설 `2026-08-26-p1-3-…`이 예정일보다 늦게 pending | 🔄 **지속 — 사람 결정 대기, 오늘도 안 닫혔다** | `hypotheses.yaml` mtime 여전히 08-31(오늘 한 줄도 안 바뀜 — 증거 §12 적신호 10번). 이 회차의 제안(변경 없음): `inconclusive`로 닫는다, 사유는 §1-2 원문 그대로 → 사용자 조치 1 |
| 1-3 이벤트 캘린더 `covered_through` 만료 · 경고 계속 발화 | 🔄 **지속** | 14:35:10 이후에도 15:35:10에 한 차례 더 발화(오늘 총 **9회**, 매시 정각 직후 1회 패턴 유지, 최종 15:35:10) — 만료 상태 그대로, 마감까지 안 고쳐짐 |
| 1-4 레버 `use_effective_member_count` 발동일 주석 미기재 | 🔄 **지속** | `strategy_params.yaml:91~94` 무변경(증거 §7 "정리 항목", 무조건발동일 2026-08-23 — 오늘로 **9일** 지남) |
| 1-5 ZN/ES 근월물 조회 실패(만성) | 🔄 **지속** | 오늘 마지막 실패 15:42:33(ZN)·15:42:34(ES) — 하루 내내(07:32~15:42) 반복, 각 9건. 여전히 만성, 계좌 권한 문제 |
| 1-6 앙상블 멤버 "47분 미관측" 오탐(1-6 판정법) | ✅ **판정 유지 — 하루 내내 재발 없음으로 최종 확인** | 오늘 전체 증거(§12)에 "미관측" 계열 적신호가 한 번도 다시 뜨지 않았다. 판단 형태 전이는 하루 90건, 최장 무기록 구간도 없었다(§3 「07:30~15:45 구간 3분 이상 공백 0건」) |
| 1-7 옵션체인 14:30 1분 전멸 · "다음 분 회복" 판정 | ↩️ **정정 — 「1건뿐」은 뒤집혔다. 전체 판정은 1-8로 승격** | 14:40 이후(장중② 관측 범위 밖) 같은 종류의 전멸이 **10분 더** 있었다(15:00~15:02, 15:15~15:21). 14:30 건 자체의 "다음 분 회복" 판정(14:31 rows=10)은 그대로 유효하다 — 다만 "하루 428사이클 중 1건뿐"이라는 하루 전체 주장은 그 시점(14:44)의 관측 범위 한계였다. → **1-8**(장후 신규, 승격) |
| (관측①) 레버 E의 대가가 실제로 나타나는가 | ✅ **판정 — 참고값으로 답했고, 공식 판정은 09-02로 이월** | 위 표 참고. 오늘은 **정식 검증예정일이 아니다** — 이 항목의 원래 문구("장후가 답한다")는 요구시점을 09-01로 가정했는데 `hypotheses.yaml`의 실제 예정일은 09-02였다(→ **1-9**, 새 이상점) |
| (관측②) 오늘 검증예정일 도래 가설 13건 | ✅ **판정 — 13건 전부 아래에서 처리 완료(§3-2)** | 증거 §8 목록 그대로 처리했다 |
| (관측④) 1-2 가설을 `inconclusive`로 닫을지 | ➡️ **이월 — 사람 확정 필요, 오늘도 미확정** | 제안은 §1-2·2-0·N-0에 반복해서 남겼다. 이 회차도 상태를 바꾸지 않는다(규약: 사람이 손으로 확정) → 사용자 조치 1 |
| (관측) 14:50 컷오프 이후 진입 여부 | ✅ **판정 — 컷오프 이후 ENTER 0건, 강제 평탄화 이후도 0건** | §13 "진입 컷오프(14:50): 차단 21분 · 컷오프 이후 ENTER 0건" — 정상(진입 배선 자체가 ADVISORY라 원천 차단) |

## 3-1. 종료 시퀀스 (15:20~15:45)

| 항목 | 확인 |
|---|---|
| 15:20 정규장 마감(옵션) | ✅ 앵커 §3 — 15:20 창 안 관측 11분, 공백 없음 |
| 15:45 `stop_mahdi_marketclose.bat` 실행 | ✅ `.last_marketclose_stop.txt` = 2026-09-01T15:45:03.474362 |
| taskkill 후 잔존 프로세스 | ✅ `shutdown_check_log` 잔존 프로세스 **0** |
| `scripts/daily_ops_report.py`가 종료 절차 안에서 돌았는가 | ✅ `auto/2026-09-01_지표.md`(74.5KB)·`_지표.json`(64.3KB) 둘 다 15:46 생성 |
| 관측 루프 최종 레코드 시각이 종료 시각과 맞는가 | ✅ 최종 레코드 15:45:00(HTTP inquire-price) — 15:45 종료 배치와 사실상 동시. 그보다 훨씬 이르게 죽지 않았다 |
| 워치독 마지막 판정 | ✅ 15:40:01(마지막 판정) → 감시 창 끝(15:45)까지 5분 무기록(정상 주기 10분 이내) |
| 관측 루프 재기동 횟수(하루 전체) | ✅ **0회**(프로세스 기동 1회만, 07:31:00) — 07:30에 한 번 켜진 채로 하루 종일 끊김 없이 돌았다 |
| 관측 루프 크래시 | ✅ `observation_loop_crash.log` — 당일 기동(07:30:56) 이후 트레이스백 없음 |

## 3-2. 오늘 검증예정일 도래 가설 13건 — 판정 제안 (확정은 사람 몫)

> ⛔ 아래는 **제안**이다. `hypotheses.yaml`의 `상태`는 이 회차가 바꾸지 않는다. 근거는 증거 §8·지표 §0-1과 이 회차가 직접 로그를 되짚어 확인한 것을 함께 적는다.

| id | 오늘 실측(요지) | 제안 판정 | 근거 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | `REGIME_RESTORE_SESSION_WINDOW` 레버 **꺼짐**, 오늘 재기동 **0회**(경로 자체가 안 돌았다) | **미실행(규약 H)** — pending 유지, 검증예정일을 "다음 재기동이 있는 날"로 재설정(구체 날짜는 못 박지 않는다 — 재기동은 예정된 사건이 아니다) | 지표 §0 레버표 OFF·§0-1 "미실행" 자동판정과 일치 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | `execution_logs` 0행(오늘도 주문 0건) | **확인(공허하게)** — 표본이 여전히 0이라 이 fix 자체는 아직 한 번도 진짜 검정되지 않았다. pending 유지, 검증예정일을 "첫 실주문이 나가는 날"로 재설정 | §16-4 "그날 나간 주문이 0건" |
| `2026-08-21-p1-latency-recovers-with-the-session` | 위험선(1.0배) 최초 돌파 **15:06**, 최종 돌파 **15:21**, **15:26** 창에 비율 **0.01**로 복귀(15:30 단일가 종료 -4분, ±5분 창 안) | **확인** | `지연창.tsv` 15:06/15:21/15:26 행 직접 대조. 08-21 실측(11:10 돌파 → 15:31 회복)과 같은 패턴 재현 |
| `2026-08-25-fix8-v2-watchdog-cost-is-cadence-not-count` | `watchdog.recovered_episodes` 0(>=0), `watchdog.silence_over_cadence_ratio` 1.0(<=1.5) | **확인** | 오늘 DEGRADED 사건 자체가 없어(§11-1 전부 0) 이 fix가 실제로 시험된 날은 아니다 — 조건 미충족이라 "위반 없음"으로 확인되는 셈 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 예정일 08-27, 오늘로 **6일 지남**(어제 5일 → 오늘 6일) | **`inconclusive` 제안 유지**(변경 없음) | §1-2 원문 사유 그대로 |
| `2026-08-26-adv7-cliff-episodes-accumulate` | 오늘이 **세 번째 절벽 에피소드**다(15:06~15:26, KIS 귀속 §17) — `cliff_episodes.md`는 아직 08-26 두 번째 건까지만 있고 오늘 것이 **안 실렸다** | **미실행(대가) / 주장은 조건 성립** — `cliff_episodes.md` 갱신은 사람 몫(코드 아님, 이 회차는 편집 안 함) → 제4부 Fix P1-A. 그 파일이 스스로 남긴 "세 번째가 나오면 물을 것" 두 질문에 오늘 답이 나온다(§3-8) | 지연창 15:06→15:26 직접 대조 |
| `2026-08-31-wiring2-v3-stream-down-is-off-the-hour` | 스트림 끊김 8회, 시각 08:00:01·09:00:01·10:00:01·11:00:01·12:00:01·13:00:01·14:00:01·15:00:01 — **전부 정각(:00:0x), 정각 외 0건** | **확인** | `logs/observation_loop.log.1` 직접 grep(로테이션된 파일에 있었다 — 15:31 로테이션으로 오늘 새벽~15:31분 기록이 `.log.1`로 넘어감) |
| `2026-08-31-p1-1-uncomputable-flip-says-what-it-was-fed` | `gamma_flip_uncomputable` 5건(>=0), `gamma_flip_out_of_leg_range` 1건(>=0) | **확인** | 지표 §0-1 자동판정과 일치. 문구 형식(직전 분 적재 N행)은 대표 사례 하나로 육안 확인 |
| `2026-08-31-p1-2-censoring-and-p95-reach-the-red-flags` | 증거 §12 적신호 3번(검열률 30%↑ 창 2개)·4번(`p95/timeout>=1.0` 창 45/98개)이 **p50 적신호(2번)와 별개 줄**로 실제로 올라왔다 | **확인** | 증거 §12 원문 직접 확인 — 두 줄이 분리돼 있다 |
| `2026-08-31-p1-3-a-missing-round-is-reconstructed-not-erased` | 증거 §8-1 "직전 보고서를 찾았다"(못 찾았다 아님), 지시 이행 표 공란 아님 | **확인** | 증거 §8-1 원문 |
| `2026-08-31-p1-4-a-denominator-of-one-says-so` | 오늘 "⚠ 분모 1" 꼬리표 **1건**, 시각 **15:36:10 — 14:50 컷오프 이후**(08-31 3건도 전부 컷오프 뒤였던 것과 동일 패턴) | **확인(주장) / 확인(대가 — 문구 안 바뀜, 파서 정상)** | `logs/observation_loop.log` 15:36:10 직접 grep. "14:50 이전에 붙는 날이 오는가"는 오늘도 **아니오** |
| `2026-08-31-p2-3-a-silent-cap-leaves-a-reason` | `strategy_daily_cap` 사유가 오늘 **20건**(§13 REJECT 표) DB에 남았고, 그 사유가 붙은 사이클의 `allowed`·확신도는 다른 REJECT와 같은 규칙으로 계산됨(별도 회귀 없음) | **확인(수기로 직접 grep — 등록된 자동 지표는 틀렸다, → 1-10)** | `logs/observation_loop.log*` "사유 ['strategy_daily_cap']" grep 6건(dedup 로그) — DB 축 20건과 자릿수 다른 것은 §1-6과 같은 "형태 불변시 미기록" 정상 동작 |
| `2026-08-31-adv1-the-close-does-our-ab-test-every-day` | `close_transition.ratio` **0.042**(마감 전 2.20초/586건 → 마감 후 0.09초/390건) | **확인** | 지표 §9-1 원문. 08-31 값(0.0423)과 거의 동일한 비 — 계측이 매일 안정적으로 재현된다 |
| `2026-08-31-adv4-uncomputable-and-rejected-are-counted-apart` | 증거 §5-3에 "산출 불가" 5건·"기각(범위 밖)" 1건이 **각각 한 줄**로 분리돼 있다 | **확인** | 증거 §5-3 원문 |

## 3-3. 절벽 에피소드(cliff) — 오늘이 세 번째다

`docs/동작점검/cliff_episodes.md`는 08-14·08-26 두 건을 기록하고 "세 번째가 나오면 물을 것" 두 질문을 남겨 뒀다. 오늘 그 답이 나왔다.

- **Q1. 언제나 마감과 함께 풀리는가?** → **그렇다, 세 번째도 같았다.** 위험선(1.0배) 최종 돌파 15:21, **15:26**에 비율 0.01로 즉시 복귀, 15:30~15:45 구간은 내내 근접 0(마감 전후 비 0.042). 이걸로 이틀이 아니라 **사흘**이 같은 패턴이다.
- **Q2. 경고선 → 위험선 간격이 20분 안팎으로 일정한가?** → **아니다, 오늘은 달랐다.** 오늘은 경고선(0.8배)과 위험선(1.0배)이 **같은 창(15:06)에서 동시에** 뚫렸다(08-14는 24분, 08-26은 20분 간격이었다). 세 건이 20분·24분·0분으로 흩어져 있어 **"일정한 선행 시간"이라는 가설은 이 시점에서 기각 쪽으로 기운다** — 표본 셋으로 확정하지는 않는다.

> 이 절 자체는 판정이 아니라 **`cliff_episodes.md`에 실릴 원재료**다. 그 파일에 열을 더하는 것은 이 회차가 하지 않는다(코드·데이터 파일 편집 최소화 원칙) — 제4부 Fix P1-A로 넘긴다.

## 오후 열화 게이지 — 마감까지 이어서 (장중② 표 이어받음)

| 시간창 | 사이클 | rows=0 비율 | p50÷타임아웃(창 최대) | 비영 멤버(최소~최대/6) | 관측 공백 | WS 단절(누적) |
|---|---|---|---|---|---|---|
| 14:40~15:00 | 20 | 0/20 = 0% | 0.53배 | 2~3 | 0분 | 7회(변화 없음) |
| 15:00~15:15 | 13 | 3/13 = 23.1% | 1.00배 ⛔ | 2~3 | 0분 | 8회(15:00:01 +1) |
| 15:15~15:20(정규장 마감) | 5 | 5/5 = 100% ⛔ | 1.01배 ⛔ | 2~3 | 0분 | 8회(변화 없음) |
| 15:20~15:26(단일가) | 6 | 2/6 = 33.3% | 1.01배 ⛔(15:21 창) | 2~3 | 0분 | 8회(변화 없음) |
| 15:26~15:45(마감 이후) | 25 | 0/25 = 0% | 0.07배 | — | 0분 | 8회(변화 없음) |

- **경고선을 넘은 창은 오늘 처음으로 여럿 나왔다** — 15:00~15:20 구간에 집중(3개 창 연속 ⛔). 09~14시 내내 한 번도 안 넘었던 것과 대비된다(→ 1-8, P1).
- 15:15~15:20(정규장 마감 직전 5분)은 rows=0 비율 **100%** — 이 5분간 옵션체인 판단 입력은 전부 직전 스냅샷(신선도 폴백)이었다.
- 15:26 이후로는 다시 09~14시 수준(0.07배)보다도 낮게 안정됐다 — 마감과 함께 KIS 쪽 부하가 통째로 빠진 것으로 읽힌다(§17 교차점검과 같은 결론).

---

## 1-8. 🆕 정규장 마감 직전 KIS 응답이 절벽처럼 무너져 옵션체인이 15분 중 11분 비었다 — 오늘 세 번째 절벽 에피소드 · **P1**

**증상.** 15:00~15:02, 15:15~15:21 두 구간(총 10분)에 옵션체인 수집이 매 분 전멸했다(적재 0행). 14:30 1분(1-7)까지 합치면 오늘 하루 **11분**이 통째로 비었다 — 전일(08-31) 같은 값은 1분이었다(▲10분).

**근거.**
```
지표 §1 판단 입력 표: 최장 연속 0행 구간 7분(15:15~15:21, 전일 1분) · GEX 입력 없던 분 6분(전일 1분)
증거 §5-1: 위험선(비율 1.00) 최초 돌파 15:06(p50 4.00초/62건) · 검열률 30% 초과 창 2개(최악 15:21 100%, 45/45건)
지연창.tsv: 15:06:06(1.00배) 15:11:06(0.52배) 15:16:06(0.53배) 15:21:06(1.01배·검열100%) 15:26:06(0.01배)
```
15:01~15:11 사이(경고선 아래로 한 번 내려갔다 15:06 위험선을 찍은 것)와 15:15~15:21(연속 5분 정규장 마감을 관통) 두 파도로 왔다 — 단일 사건이 아니라 **두 번 반복**됐다.

**기준.** `docs/동작점검/cliff_episodes.md`가 08-14·08-26 두 건을 "절벽"으로 정의한 것과 같은 모양(§17 교차점검 — 백오프 최대 2.25배로 낮은데 ReadTimeout·지연 경고만 튀는 조합 = **KIS 귀속**). 규약 F(귀속은 그날 KIS 상태에 비례) 적용 — 우리 쪽 백오프·수요는 오히려 전일보다 낮았다(§1 한눈에: 총 REST 호출 ▼1,060건, 백오프 최대 배율 변화 없음).

**영향.** 이 11분 동안 GEX(감마 익스포저)·감마플립 계산은 그 직전 분의 오래된 시세를 그대로 썼다. 오늘은 진입 배선이 ADVISORY(설정만으로 주문이 안 나가는 상태)라 실제 손실로 이어지지는 않았지만, 실거래 전환 이후 같은 일이 생기면 이 11분 동안의 판단은 최대 4분 늙은 시세로 내려진 것이다(§14 체인 스냅샷 최고령 레그 4.2분).

**제안.** 조치 불필요(설계된 신선도 폴백이 정상 동작) — 다만 `cliff_episodes.md`에 세 번째 열을 추가하는 것은 사람 몫으로 남긴다(→ 제4부 Fix P1-A). **가설 `2026-08-26-adv7-cliff-episodes-accumulate`가 예정한 "세 번째 표본"이 바로 이것**이다.

## 1-9. 🆕 레버 E 가설의 공식 검증예정일(09-02)과 어제 저녁 결정 기록의 "09-01 첫 실측" 서술이 어긋난다 — **P2**

**증상.** `hypotheses.yaml`의 `2026-08-12-eE-on-congested-hours`는 **검증예정일: 2026-09-02**로 등재돼 있다. 그런데 어제(08-31) 저녁 `DECISION_LOG.md`에 적힌 결정문은 "**09-01 장후 회차가 첫 실측으로 판정한다**"라고 썼고, 오늘 장전 보고서(§1-1)도 "오늘 관측이 답한다(→ 관측①)"고 이어받았다.

**근거.** `docs/동작점검/hypotheses.yaml:3296` `검증예정일: "2026-09-02"`. `DECISION_LOG.md` 08-31 저녁 항목 "**검증.** `hypotheses.yaml`의 `2026-08-12-eE-on-congested-hours`(09-01 장후가 첫 실측 판정)". 오늘 §1-1(장전) "이 대가가 실제로 나타나는지는 오늘 장중·장후 관측이 답한다".

**기준.** `docs/동작점검/tools/phases.md` C-3 — "검증예정일이 도래한 가설을 그 예정일 당일의 실측으로 판정한다(오늘 숫자로 이틀 전 항목을 판정하지 않는다)". 이 규약은 **소급**(늦게 판정)만 언급하지만, 대칭적으로 **조기 판정**(하루 이르게 확정)도 같은 문제를 만든다 — 오늘은 09-02가 아니다.

**영향.** 이 어긋남을 못 보고 지나가면, 오늘 나온 실측값(예: `stale_pct` 98.8% vs 기대 70% 이하)이 "가설이 반증됐다"는 성급한 결론으로 굳어질 뻔했다. 실제로는 하루 이른 참고값이라 아직 결론이 아니다 — 내일(09-02) 장전 회차가 공식 판정한다는 사실을 놓치면 그 판정이 통째로 하루 당겨져 틀릴 위험이 있었다.

**제안.** 09-02 장전 회차가 이 가설을 공식 판정한다는 것을 사용자 조치에 명시적으로 남긴다(→ 사용자 조치 7). `DECISION_LOG.md`의 08-31 문장 자체는 지나간 기록이라 고치지 않는다(dev_memory는 append 전용) — 대신 내일 회차가 이 사실을 알고 시작하도록 이 절을 남긴다.

## 1-10. 🆕 가설 `2026-08-31-p2-3-a-silent-cap-leaves-a-reason`의 등록된 예측 지표가 주장과 무관한 값을 가리킨다 — **P2**

**증상.** 이 가설은 "전략 상한 컷(`strategy_daily_cap`)이 걸리는 사이클에 그 사유가 `risk_gate_state`에 남는가"를 주장하는데, `hypotheses.yaml`에 등록된 예측 지표 두 줄(주장·대가) 모두 `metric: db.signal_reach.gamma_flip_out_of_range_count`로 적혀 있다 — 감마플립과 전략 상한 컷은 서로 다른 메커니즘이다.

**근거.**
```
hypotheses.yaml:6647   metric: db.signal_reach.gamma_flip_out_of_range_count   (주장)
hypotheses.yaml:6650   metric: db.signal_reach.gamma_flip_out_of_range_count   (대가, 위와 완전히 동일)
```
오늘 이 metric의 실측값은 0인데, 정작 주장하는 현상(`strategy_daily_cap` 사유)은 오늘 실제로 **20건**(§13 REJECT 표) DB에 남았다 — 즉 가설 자체는 오늘 참인 것으로 보이지만, **등록된 지표로는 이 사실을 전혀 확인할 수 없다.**

**기준.** `docs/동작점검/README.md`의 예측치 규약 — 지표 경로가 실재해야 하고, "주장이 실제로 재는 것"과 일치해야 한다(부모만 맞고 잎이 틀린 경로가 특히 위험하다는 08-19 §1-2와 같은 계열의 문제, 이번엔 잎이 아예 다른 가지다).

**영향.** 다음에 자동 판정기가 이 가설을 "확인" 또는 "반증"으로 자동 표기해도 그 표기는 우연의 일치일 뿐 실제로는 아무것도 확인하지 못한 것이다. 등록 당시(08-31) 복붙 실수로 보인다 — `enforce_reentry_cooldown`류 항목의 metric을 그대로 옮기다 안 고친 것으로 추정된다(추론).

**제안.** metric을 실제로 `strategy_daily_cap` 사유가 붙은 사이클 수(예: `db.signal_reach.risk_gate_reason_counts.strategy_daily_cap` 같은 신규 지표, 없으면 `scripts/daily_ops_report.py`에 추가 필요)로 정정한다(→ 제4부 Fix P2-B). 오늘 실측(수기 확인)으로는 **확인**으로 제안한다 — grep 결과 "사유 ['strategy_daily_cap']" 6건(dedup 로그), DB 축 20건, 둘 다 정상 범위(§1-6과 같은 dedup 설명).

---

## 종합 판정표 — 국면별 판정이 어떻게 바뀌었는가

| 항목 | 장전(08:02) | 장중①(12:35) | 장중②(14:44) | 장후(15:46) |
|---|---|---|---|---|
| 시스템 전반 정상 여부 | ✅ 정상 | ✅ 정상 | ✅ 정상 | ✅ 정상(마감까지 재기동 0회·ERROR는 13건이나 전부 설명됨) |
| 1-1 레버 E 대가 | 판정 대상 등록 | 대가 미판정(이월) | 대가 미판정(이월) | 참고값만 기록, **공식 판정 09-02로 재이월**(→1-9) |
| 1-2 가설 예정일 초과 | 🆕 발견(5일 지남) | 지속 | 지속 | 지속(6일 지남, 사람 결정 대기) |
| 1-3 이벤트 캘린더 만료 | 지속(만성) | 지속 | 지속 | 지속(오늘 9회 발화) |
| 1-4 레버 주석 미기재 | 지속(만성) | 지속 | 지속 | 지속 |
| 1-5 ZN/ES 조회 실패 | 지속(만성) | 지속 | 지속 | 지속(만성) |
| 1-6 멤버 미관측 오탐 | — | 🆕 발견·판정(오탐) | ✅ 재발 없음 재확인 | ✅ 하루 내내 재발 없음 최종 확인 |
| 1-7 옵션체인 1분 전멸 | — | — | 🆕 발견(1건, 회복) | ↩️ **정정 — 실제로는 11분(3파도), §1-8로 승격** |
| 1-8 15시대 절벽(세 번째 에피소드) | — | — | — | 🆕 신규(P1) |
| 1-9 가설 예정일 불일치 | — | — | — | 🆕 신규(P2) |
| 1-10 가설 metric 등록 오류 | — | — | — | 🆕 신규(P2) |

## 당일 이상점 통합 대장

| 번호 | 제목 | 최초 발견 국면 | 최종 심각도 | 최종 처분 | 근거 위치 |
|---|---|---|---|---|---|
| 1-1 | 레버 E가 어제 저녁 지시대로 오늘 기동에 실렸다(확인 항목) | 장전 | P2 | ✅ 판정(정상) — 대가는 09-02 공식 판정으로 이월 | §1-1, 3-0 |
| 1-2 | 가설 하나가 판정 예정일보다 늦게 pending | 장전 | P2 | 🔄 지속 — 사람 결정 대기(오늘 6일 지남) | §1-2, 3-0 |
| 1-3 | 이벤트 캘린더 만료(만성) | 장전 | P2 | 🔄 지속 | §1-3, 3-0 |
| 1-4 | 레버 발동일 주석 미기재(만성) | 장전 | P2 | 🔄 지속 | §1-4, 3-0 |
| 1-5 | ZN/ES 근월물 조회 실패(만성) | 장전 | P2 | 🔄 지속 | §1-5, 3-0 |
| 1-6 | 앙상블 멤버 "47분 미관측" 오탐 | 장중① | P2 | ✅ 판정(오탐 확인) — 하루 내내 재발 없음 | §1-6, 2-0, N-0, 3-0 |
| 1-7 | 옵션체인 14:30 1분 전멸 | 장중② | P2 | ↩️ 정정 — 실제 규모는 11분(§1-8로 통합) | §1-7, 3-0 |
| 1-8 | 정규장 마감 직전 절벽(세 번째 에피소드, 11분 전멸) | 장후 | **P1** | 🆕 신규 — 조치 불필요(설계된 폴백 정상), `cliff_episodes.md` 갱신은 Fix P1-A | §1-8, 3-3 |
| 1-9 | 레버 E 가설 예정일(09-02) vs 결정기록(09-01) 불일치 | 장후 | P2 | 🆕 신규 — 09-02로 공식 판정 이월 명시 | §1-9 |
| 1-10 | 가설 `p2-3` 예측 metric 등록 오류 | 장후 | P2 | 🆕 신규 — Fix P2-B로 정정 제안 | §1-10 |

## 종가 기준 최종 손익 / 절대원칙 판정

| 항목 | 값 | 판정 |
|---|---|---|
| 실현 손익 | 0원 | ✅ (거래 없음) |
| 미실현 손익 | 0원(보유 포지션 0개) | ✅ |
| 자본 대비 손익률 | 0.0% | ✅ |
| 고점 대비 낙폭 | 0.0% | ✅ |
| 보유 포지션 수 | 0개(§16-1 "그날 보유 포지션 기록이 없다") | ✅ |
| 계약 수 | 0계약 | ✅ |
| 나간 주문 | 0건(§16-4) — 진입 판단 346건 중 221건이 `mode_not_auto_submit`·`reentry_cooldown_not_configured`로 막힘 | ✅ 정상(ADVISORY 설계대로) |
| 레짐일당 전략 상한(2개) 위반 여부 | `strategy_daily_cap` 사유 20건 발생 — **컷이 실제로 작동해 상한을 지켰다** | ✅ (컷이 조용히 아니라 사유를 남기며 작동, →1-10) |
| 하드 리밋(포지션·계약 한도) | 거래 자체가 없어 해당 없음 | ✅ (해당 없음) |
| 금지 사항(자동매매 무단 발동 등) | 당일 커밋 0건, 레버는 사전 승인된 것만 발동 | ✅ |

**거래 없음.** 오늘은 ADVISORY 모드(주문 미제출 설계)라 체결·포지션·손익이 구조적으로 전부 0이다. 이것은 이상이 아니라 현재 배선의 정상 상태다(§16-4 "코드 배선만으로는 안 풀리는 것이 이 설계의 요점").

---

# 제4부 — Fix 구현계획 (P0/P1/P2)

> ⛔ 이 회차는 코드를 고치지 않는다. 적용은 사람이 다음에 이어서 지시할 때.

## P1-A. `cliff_episodes.md`에 오늘을 세 번째 절벽 에피소드로 추가한다 (근거: 1-8, 3-3)

- 대상: `docs/동작점검/cliff_episodes.md`
- 바꿀 것: 기존 표(08-14·08-26 두 열)에 **09-01** 열을 추가. 값: 경고선 최초 돌파 15:06 · 위험선 최초 돌파 15:06(간격 0분) · 최장 연속 rows=0 7분(15:15~15:21) · 회복 15:26(마감 -4분) · 우리 쪽 압력(백오프 최대 2.25배, 전일보다 낮음) · KIS 귀속(§17). 파일 하단 "세 번째가 나오면 물을 것" 두 질문에 §3-3의 답을 그대로 옮겨 적는다.
- 회귀 위험: 없음(기록 파일, 코드 아님)
- 검증: 다음 절벽(네 번째) 발생 시 이 파일이 세 표본으로 "경고→위험 간격이 일정한가"를 스스로 답할 수 있는가

## P2-B. 가설 `2026-08-31-p2-3-a-silent-cap-leaves-a-reason`의 예측 metric을 정정한다 (근거: 1-10)

- 대상: `docs/동작점검/hypotheses.yaml:6647`·`6650`, 필요하면 `scripts/daily_ops_report.py`
- 바꿀 것: `db.signal_reach.gamma_flip_out_of_range_count`를 `strategy_daily_cap` 사유가 붙은 사이클 수를 실제로 세는 지표로 교체. 그런 지표가 아직 없으면 `daily_ops_report.py`에 `db.signal_reach.risk_gate_reason_counts` 같은 신규 집계를 추가한다.
- 회귀 위험: 낮음(신규 지표 추가, 기존 값 안 건드림) — 다만 `daily_ops_report.py` 변경이면 `tests/test_ops_hypotheses.py` 범위에서 회귀 확인 필요
- 검증: 정정 후 다음 판정에서 이 가설의 「확인/반증」이 실제 로그와 같은 방향으로 나오는가

## P2-1. (이월, 1-4) 이미 켜진 레버 `use_effective_member_count`의 발동일을 코드 주석에 남긴다

- 대상: `mahdi/config/strategy_params.yaml:91~94`
- 바꿀 것: 91~93행 주석 끝에 `# 실제로 켠 날짜: 2026-08-23 (git f673b29)` 한 줄 추가
- 회귀 위험: 없음(주석만)

## P2-C. (이월, 1-2) 가설 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`을 `inconclusive`로 닫는다

- 대상: `hypotheses.yaml`의 해당 항목 `상태`
- 사유(제안): "검증예정일(08-27)을 6거래일 넘겼고, 08-27~08-28 점검 결번(6회차)까지 겹쳐 그 창 안에서 판정할 기회 자체가 없었다. 실제 발동은 08-31 저녁, 정규 장전 회차 밖에서 일어나 이 가설이 주장한 '다음 거래일 장전' 형태와도 다르다."
- 실행 주체: 사람(→ 사용자 조치 1)

---

# 제5부 — 고도화 방안 (당일 관측에서 출발한 것만)

### 고도화 4. 절벽 에피소드가 세 번째부터는 "경고→위험 간격"·"마감 복귀 여부"를 지연창 TSV에서 자동으로 뽑아 `cliff_episodes.md` 초안 행을 만든다 (근거: 1-8, 3-3)

- **관측 근거**: 오늘도 사람이 지연창.tsv를 손으로 훑어 15:06(동시 돌파)·15:26(복귀)을 찾았다. 이 계산(경고선·위험선 최초 돌파 시각, 회복 시각, 간격)은 이미 존재하는 `_지연창.tsv`만으로 기계가 뽑을 수 있다.
- **제안**: `collect_evidence.py`(또는 별도 스크립트)에 "그날 위험선(1.0배)을 넘은 창이 있으면, 경고선·위험선 최초 돌파 시각과 0.1배 이하로 복귀한 첫 시각을 자동 계산해 `cliff_episodes.md` 형식의 한 행으로 출력"하는 기능을 추가한다.
- **기대 효과**: 다음 절벽부터는 사람이 표를 채우는 게 아니라 검토만 하면 된다. 오늘처럼 "간격이 0분"인 특이 케이스도 놓치지 않는다.
- **비용·위험**: 낮음 — 기존 지연창 계산 로직 재사용.
- **우선순위**: 다음 단계(P2).

### 고도화 5. `hypotheses.yaml` 등록 시 metric 키가 가설 본문과 무관한지 최소한으로 걸러낸다 (근거: 1-10)

- **관측 근거**: `2026-08-31-p2-3`이 복붙 실수로 감마플립 지표를 가리킨 채 열흘 가까이 아무도 몰랐다 — 자동 판정기는 그 틀린 지표로도 "확인"을 냈을 것이다.
- **제안**: `tests/test_ops_hypotheses.py`에 "같은 가설 안에서 서로 다른 역할(주장/대가)의 두 예측이 완전히 동일한 metric 키를 쓰면 경고"하는 약한 검사를 추가한다(강제 실패는 아니고 lint 수준 — 08-19 §1-2처럼 정말 같은 지표를 두 각도로 볼 수도 있으므로 강제 금지는 과하다).
- **기대 효과**: 다음 복붙 실수를 등록 즉시 잡는다.
- **비용·위험**: 낮음 — 신규 테스트 1건.
- **우선순위**: 다음 단계(P2).

---

## dev_memory 반영 초안 — 사람이 실제로 반영한다

> ⛔ 이 회차(자동 예약, 장후)는 `dev_memory`를 직접 수정하지 않는다. 아래는 초안이다.

- **DECISION_LOG 추가 후보**: "2026-09-01 — 세 번째 절벽 에피소드(15:06~15:26, KIS 귀속). 경고선·위험선이 같은 창(15:06)에서 동시에 뚫려 앞선 두 건(20분·24분 간격)과 다른 패턴을 보였다 — '일정한 선행 시간' 가설은 표본 3으로는 기각 쪽. `cliff_episodes.md`에 아직 미반영, 사람이 다음에 추가."
- **DECISION_LOG 추가 후보**: "2026-09-01 — `2026-08-12-eE-on-congested-hours`의 실제 검증예정일은 2026-09-02인데 08-31 저녁 결정문이 '09-01 첫 실측'이라고 잘못 적었다. 09-02 장전 회차가 공식 판정할 것."
- **NEXT_TODO 추가 후보 1**: `docs/동작점검/hypotheses.yaml:6647,6650` — `2026-08-31-p2-3` metric을 `strategy_daily_cap` 실측 지표로 정정(P2-B, `daily_ops_report.py` 신규 집계 필요할 수 있음).
- **NEXT_TODO 추가 후보 2**: `docs/동작점검/cliff_episodes.md`에 09-01을 세 번째 열로 추가(P1-A, 값은 제4부에 다 적어 뒀다 — 옮기기만 하면 된다).
- **NEXT_TODO 추가 후보 3**: `mahdi/config/strategy_params.yaml:91~94` 주석에 실제 발동일(2026-08-23) 기입(P2-1, 5분 이내 작업, 여러 날째 이월 중).
- **CURRENT_STATE 갱신 후보**: 없음(오늘은 구조 변경이 없었다 — 레버 상태·코드 전부 08-31 그대로).

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 / 오늘 중

1. **가설 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`을 어떻게 닫을지 정한다.** (근거: 1-2) — 제안: `inconclusive`로 닫고 사유는 제4부 P2-C에 적어 둔 문장을 쓴다. 오늘로 6거래일째 미확정.
2. **이벤트 캘린더 `covered_through`를 9월분으로 확인해 옮긴다.** (근거: 1-3) — 9월 금통위·FOMC·미국 CPI·고용지표·옵션 만기(09-03·09-10)를 실제로 넣은 뒤 그 마지막 날로 옮긴다. 날짜만 미는 것은 금지. 오늘까지 경고 9회 발화.
3. **`.claude/`를 커밋할지 `.gitignore`에 넣을지 정한다.** (08-27부터 이월, 6일째)
4. **`cliff_episodes.md`에 오늘(세 번째 절벽)을 추가한다.** (근거: 1-8, →Fix P1-A) — 값은 제4부에 다 정리돼 있다. 옮겨 적기만 하면 된다.
5. **가설 `2026-08-31-p2-3-a-silent-cap-leaves-a-reason`의 metric을 정정한다.** (근거: 1-10, →Fix P2-B)
6. **레버 `use_effective_member_count` 발동일을 주석에 적는다.** (근거: 1-4, →Fix P2-1, 5분 작업)

### 결정이 필요한 것 (자동으로 못 하는 것)

7. **레버 E(`OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`)의 대가(위클리 커버리지 하락 등)를 공식 판정할 날은 내일(09-02) 장전이다.** (근거: 1-9) — 오늘 참고값(`stale_pct` 98.8%, `budget_exceeded` 123건 등)을 그대로 최종 결론으로 쓰지 말 것. 09-02 장전 회차가 이 파일을 이어받아 판정한다.
8. **「GEX 광폭 체인」 재개 여부.** (08-31 이월, 오늘도 미결정) — 2026-09-10 북의 "광폭 감마플립 가능 여부"가 전일 대비 가능→불가로 바뀐 것을 참고. 아직 재개도 폐기도 하지 않는다(며칠 더 본다는 08-31 권고 유지).
9. **ZN/ES 해외선물 거래소 이용 신청 여부.** (만성, 1-5) — (A) CME/CBOT SUB거래소 신청 (B) 매크로 축에서 공식 제외.
10. **Claude 앱 예약 4종이 08-27~08-28에 왜 안 떴는가.** (이월, 저장소 밖이라 이 회차가 답할 수 없음) — 오늘도 4개 회차(08:01·12:35·14:40·15:46) 전부 정시 실행됐다는 사실만 보탠다.
