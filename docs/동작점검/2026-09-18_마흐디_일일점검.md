# 마흐디 일일점검 — 2026-09-18 (금)

- 국면: 장전(pre) · 예정 08:00 · 실행 08:01 — **정시 실행**(밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-18_증거_pre.md`
- 리포 HEAD: `2190593`(브랜치 `master`) · 당일 커밋 **0건** · 미커밋 **27건**(§1-3에서 다룬다)

---

## §0. 오늘 한 장

**오늘 프로그램은 정상 작동했다.** 07:30 장전 기동(도커·DB 마이그레이션 37개·관측 루프)이 문제 없이 끝났고, 07:30~08:01 사이 오류(ERROR) 로그는 **0건**, 시세 수집 지연도 없었다(응답시간이 제한시간의 1~2% 수준). 로그가 끊긴 구간도 없다.

**사람이 지금 당장 해야 할 일은 없다.** 다만 미뤄져 온 결정 두 가지(응답지연에 대한 안전장치를 켤지 말지, 커밋 안 된 파일 정리)가 오늘도 그대로 남아 있어 → 「사용자 조치」에 정리했다.

**문제 5건 — 전부 이미 알려진 문제이고, 새로 찾은 것은 0건이다(심각 0 / 보통 2 / 경미 3).** 오늘 판정 시한을 맞은 예측(가설) 2건이 따로 있는데, 둘 다 어젯밤에 막 구현된 것이라 아직 판단할 재료가 없다(§1-5) — 오늘 장 마감 뒤에 답한다.

(관측 범위: 07:30~08:01. 이 범위 밖에서 무슨 일이 있었는지는 이 회차가 답할 수 없다 — 「없다」가 아니라 「모른다」다.)

**[12:30 갱신 — ⚠ 지금 사람이 볼 것]** 개장(09:00) 이후 옵션(파생상품) 가격 정보를 받아오는 것이 여러 번 끊겼다. 짧게 세 번(4~11분씩, 10:29·10:54·11:24) 끊겼다 풀리기를 반복하다가 11:37부터는 계속 안 풀리고 있다 — 이 회차가 증거를 모은 12:34 시점 기준 57분째, 방금 다시 확인한 12:36 기준으로는 60분을 넘겼다. 프로그램 자체는 살아 있고 멈추지 않았지만, 최근 시세를 하나도 못 가져오고 있다. 자동으로 다시 켜는 장치는 없다 — 켤지 말지는 사람이 정한다. 다만 장중에는 설정을 바꾸지 않는 규칙이라(재기동 대가는 약 30분 공백) 지금 당장 취할 조치는 없고, 과거 비슷한 사례들은 대체로 장 마감 무렵 저절로 풀렸다. 이 상황이 14:30 회차로 이어진다(→ 사용자 조치, 아래 §2-6). 그 밖에 새로 찾은 문제가 1건 더(판단 근거가 줄어드는 것) 있고, 어제 미뤄졌던 미커밋 파일 27건은 26건이 정리돼 1건만 남았다.

**[14:30 갱신 — ⚠ 참고만 하면 됨, 급한 조치 없음]** 12:30이 "60분 넘게 안 풀린다"고 본 그 끊김은 실제로는 **12:40에 풀렸다**(총 63분). 다만 그걸로 끝이 아니었다 — 그 뒤로 짧게 두 번(3분·11분) 더 끊겼고, 13:27~14:27에는 **61분짜리**로 한 번 더 끊겼다가 풀렸고, 14:39~14:44에 **8번째**(6분)가 또 있었다가 이 회차가 보고서를 쓰는 중에 풀렸다. 오늘 이런 끊김이 벌써 8번이다. 원인으로 보이는 증권사(한국투자증권) 쪽 시세 응답 지연은 09:56에 처음 위험 수준을 넘은 뒤로 지금까지 **4시간 넘게 단 한 번도** 안전한 수준으로 안 돌아왔다 — 짧은 회복 구간에도 이 지연 자체는 계속 위험선 위였다. 지금 당장 사람이 할 일은 없다 — 다만 오늘 하루 프로그램이 보는 가격이 자주 최신이 아니었다는 점만 알아 두면 된다.

---

## 제1부 — 장전 (07:30 ~ 09:00)

### 1-0. 전일 이월

오늘이 이 파일을 새로 만드는 첫 회차라 「본문을 고치지 않고 이월 처분한다」는 규칙이 적용될 앞 국면 절이 없다. 대신 **전일(2026-09-17) 보고서가 남긴 것**을 아래에서 확인한다(phases.md A-3).

| 전일이 남긴 것 | 오늘 아침 확인 | 판정 |
|---|---|---|
| 전일 `auto/2026-09-17_지표.json` 존재 여부 | 있음(64.1KB, 09-17 15:46 생성) | ✅ 전일 델타 계산 가능 |
| 미커밋 26건(09-17 장전 기준) 정리 결정 | 오늘 27건(§1-3) — 실질 내용은 동일, 어젯밤 주석 1건만 추가 | 🔄 지속 → §1-3 |
| ZN/ES 해외선물 조회 실패 — 만성 | 오늘도 동일 사유로 실패 | 🔄 만성 → §1-2 |
| 이벤트 캘린더 9월분 미갱신 — 20일째 | 오늘도 그대로(21일째) | 🔄 지속 → §1-1 |
| 가설 4건(08-12·08-19·08-26·09-15) 판정 시한 초과, 손 판정 대기 | 오늘도 pending, 시한 초과 일수만 늘었다 + 신규 시한초과 2건(09-16 두 건) 합류 | 🔄 지속·확대 → §1-4 |
| 사전 대응 규칙(2026-08-04-p5) 발동 여부 — 09-17 장후가 「22거래일째 미결」로 재확인 | 오늘 아침 지표(09-17 15:46 생성)에도 🔔 재성립 — 결정 없이 하루 더 지남 | 🔄 지속 → §1-6 |
| Fix B·Fix C(09-17 밤 커밋 `8c71d3d`) — 검증예정일 오늘(09-18) | 오늘 관측 대상, 실측은 장후 | ➡️ 이월 → 「오늘 관측이 답해야 할 것」 |
| `hypotheses.yaml` 언급-실재 불일치(`2026-08-26-p1-2-1`) — 09-17이 "내일부터 소멸" 예고 | 오늘 증거 §8-1에서 **사라짐 확인** | ✅ 해소 |

### 장전 체크리스트 (phases.md A절)

**A-1. 기동 — 전부 정상**
- 07:30:00 `start_mahdi_premarket.bat` 실행, 07:30:06 도커 데스크톱 기동, 07:30:23 도커 데몬 준비, 마이그레이션(DB 구조 갱신) 37개 전부 적용 완료(07:30:27~07:30:35대)
- `.last_successful_start.txt`: 2026-09-18T07:30:58.9 — 오늘 시각 ✅
- 관측 루프 기동 표식 **1회만** 확인(`직전 정상 기동: 2026-09-17 07:31:21`) — 밤새 재기동 없었다는 뜻
- COCKPIT(대시보드) 07:30:50 기동, `.last_cockpit_start.txt` 07:31:18 — 정상
- 워치독(watchdog — 관측 루프가 죽었는지 1분마다 확인하고 되살리는 감시 프로세스): 당일 3행 전부 OK, 이상(DEGRADED/RESTART) 0건. 호출 40회(idle 18·ok 22), 5분 이상 공백 없음
- WS(웹소켓 — 실시간 시세를 밀어 넣는 연결) 구독: 07:31:03부터 41개 종목 구독 요청 순차 완료, 실패 로그 없음

**A-2. 오늘의 계획**
- 검증 시한이 오늘인 가설 **2건**: `2026-09-17-fixb-latency-streak-counts-past-two-days`, `2026-09-17-fixc-chain-wipeout-shows-in-the-headline` — 둘 다 어젯밤 커밋(`8c71d3d`)으로 구현되고 코드보다 먼저 등재(`001d263`)됐다. 실측은 오늘 하루 데이터가 쌓인 뒤(장후)에나 가능 → 「오늘 관측이 답해야 할 것」
- 시한이 지난 pending 가설 **6건**(08-12·08-19·08-26 각 1건 + 09-15 1건 + 09-16 2건, §1-4)
- `NEXT_TODO.md`의 「켤 것」 레버(lever — 코드 안에 미리 심어 둔 On/Off 설정 스위치): 오늘 새로 켤 레버 없음. 기존 레버(회원수 보정, 신호융합 위상차 55초, 9~14시 혼잡 시간대 완화) 전부 이미 켜진 지 오래(발동 완료 각각 08-23·09-02·08-31)
- 전일 커밋(가장 최근 `2190593`, 09-17 저녁) < 관측 루프 오늘 기동 시각(07:30:58) — 조건 충족, 어젯밤 Fix B·C가 오늘 프로세스에 실려서 돈다
- **전 거래일(09-17) 지표에 🔔 사전 대응 규칙 조건 성립이 있었다** — phases.md A-2 규정상 오늘 아침이 검토 창이었으나, 이 규칙의 발동 시점 자체가 "다음 거래일 **07:30 기동 전**"으로 못박혀 있어(09-17 지표 §0-1) 이 세션(08:01 실행)이 시작됐을 때 이미 그 창은 지나 있었다 → §1-6·사용자 조치로 넘긴다
- **`--verify-report`로 전일(09-17) 보고서 자기검증**: 폐기 목록과 부딪히는 줄 0건, 지목한 `*.py` 경로 10개 전부 실재 — 이상 없음

**A-3. 전일 이월** — 위 1-0 표에서 처리함

**개장 전 사람 조치: 불필요** (관측 범위 07:30~08:01)

### 1-1. 이벤트 캘린더 9월분 미갱신 — 의도적 방치, 21일째

**증상**: 07:31:55 "이벤트 캘린더 미기입: not_covered — `covered_through`(2026-08-28)가 오늘(2026-09-18)보다 앞선다. 이벤트 근접 페널티가 걸리지 않는 상태다".

**근거**: `mahdi/config/event_calendar.yaml:70` `covered_through: "2026-08-28"`. 08-28부터 오늘까지 21일 지났다. `docs/동작점검/auto/2026-09-18_증거_pre.md` §5.

**기준**: `docs/동작점검/README.md` 규약 및 여러 회차 보고서가 "20일째"로 이미 언급한 만성 항목(09-17 §1-4가 20일째로 기록, 오늘은 21일째).

**영향**: 금리 발표 같은 굵직한 이벤트가 9월에 있어도, 프로그램이 "이벤트 근접이니 몸을 사려야 한다"는 판단을 못 내린다. 채워지지 않은 것은 방치가 아니라 "채울 때까지 경고를 계속 울리게 두자"는 의도된 설계다.

**심각도**: P2(만성, 의도적). 새 발견 아님. 9월 이벤트 목록을 채우는 것은 코드 밖의 일(외부 일정 확인)이라 사람이 해야 한다.

### 1-2. ZN·ES 해외선물 조회 실패 — 만성

**증상**: 07:32:35 "ZN(미국 10년 국채선물) 근월물 조회 실패: ZNUN — CBOT SUB거래소 신청 계좌가 아닙니다", 07:32:47 "ES(E-mini S&P500 선물) 근월물 조회 실패: ESUN — CME SUB거래소 신청 계좌가 아닙니다".

**근거**: `docs/동작점검/auto/2026-09-18_증거_pre.md` §4 WARNING 표.

**기준**: 증권사 계좌에 해당 해외거래소 권한이 아직 신청돼 있지 않다는 뜻 — 코드 문제가 아니라 계좌 설정 문제로 여러 회차에 걸쳐 이미 확인된 만성 항목.

**영향**: 매크로(거시경제) 스트레스 지표 중 미국 채권·주가지수 선물을 보는 축 두 개가 오늘도 비어 있다. 그 두 축이 지금 당장 옵션 매매 판단에 필요한 것은 아니라 거래 자체에는 영향이 없다.

**심각도**: P2(만성). 새 발견 아님. 증권사 계좌 설정(코드 밖의 일)이라 사람이 신청해야 풀린다.

### 1-3. 미커밋 27건 — 사람 결정 대기, 전일 대비 내용은 거의 무변화

**증상**: `git --no-optional-locks status --porcelain` 27줄. 수정 5건(`mahdi/dashboard/app.py`·`data_source.py`·`scripts/daily_ops_report.py`·`scripts/start_mahdi_premarket.bat`·`scripts/stop_mahdi_marketclose.bat`) + 미추적 22건(연구 문서 5개, `premarket_levels`(맥점 예측) 계열 스크립트·모듈 12개, `.claude/`, 개인 메모 1건, 테스트 1건).

**근거**: 앞 4개 수정 파일의 최종 수정 시각은 전부 **2026-09-06**(`stat` 확인) — 오늘 새로 생긴 변경이 아니다. 다섯 번째(`stop_mahdi_marketclose.bat`)만 어젯밤 **09-17 17:54**(09-17 장후 보고서 완성 이후) 수정됐는데, diff를 보면 `taskkill` 필터가 COCKPIT 창 제목과 우연히 안 겹친다는 것을 설명하는 **주석만** 추가됐고 실행 로직은 한 글자도 안 바뀌었다. 09-17 장전 보고서는 "미커밋 26건, 9일째로 추정"이라고 기록했다(`docs/dev_memory/NEXT_TODO.md`).

**기준**: `scripts/start_mahdi_premarket.bat`의 07:30 기동 로그에 "당일 맥점 예측 준비"가 실제로 찍힌다(`logs/premarket_startup.log` 07:30:47) — 즉 **미추적 상태인 `scripts/premarket_levels_publish.py`가 매일 자동 실행되고 있다.** 여러 회차가 이미 "다른 PC에서 이 배치를 돌리면 파일이 없어 실패할 수 있다"고 지적한 항목이다.

**영향**: 지금 이 PC에서 도는 프로그램은 정상이다. 다만 이 상태로 다른 컴퓨터에 옮겨 돌리면 장전 기동 배치가 「당일 맥점 예측 준비」 단계에서 조용히 실패할 수 있고(기동 자체는 안 막힘 — 배치가 실패해도 진행하도록 설계돼 있다), 그 실패 원인이 "커밋 안 된 파일을 불렀다"라서 로그만 봐서는 바로 안 잡힐 것이다.

**심각도**: P2(만성, 9일째 이상으로 추정 — 정확한 시작일은 diff 미대조로 확정하지 못함). 새 발견이 아니다.

### 1-4. 검증 시한 초과 가설 6건 — 손 판정이 밀리고 있다 (전일 4건 → 오늘 6건)

**증상**: `hypotheses.yaml`의 pending 가설 중 6건이 검증 시한을 넘겼다. 전일(4건) 대비 09-16에 시한이 도래했던 2건이 새로 합류했다.

| id | 무엇을 확인하려던 가설인가 | 시한 | 오늘까지 지난 일수 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | 재기동해도 시세 판단이 처음부터(WARMUP — 예열 상태) 다시 시작하지 않게 하는 개선 | 2026-09-01 | 17일 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 주문 왕복(발주→체결확인) 실측이 DB에 증거로 남는가 | 2026-09-01 | 17일 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 혼잡 시간대 사전 대응 규칙이 정해진 시점(다음 거래일 장전)에 실제로 발동하는가 | 2026-08-27 | 22일 |
| `2026-09-15-eE-adds-nine-to-congested-hours` | 9시를 혼잡 시간대에 추가한 효과가 실제로 있는가 | 2026-09-16 | 2일 |
| `2026-09-16-recovery-hint-carries-its-sample-count` | 워치독 회복 힌트 문구가 표본 수를 같이 인쇄하는가 | 2026-09-17 | 1일 |
| `2026-09-16-cliff-day-marks-the-hypothesis-table-itself` | 절벽이 난 날 가설 판정표에 「오염」 열이 뜨는가 | 2026-09-17 | 1일 |

**근거**: `docs/동작점검/auto/2026-09-18_증거_pre.md` §8. 뒤의 두 건(09-16 자)은 09-17 하루 동안 판정 조건(워치독 no_ingest 경보 / 20분 이상 절벽)이 한 번도 발생하지 않아 09-17 장후도 판정하지 못한 채 시한을 넘겼다(09-17 파일 §5-4/제6부 참조).

**기준**: `docs/동작점검/README.md` 소급 적용 금지 규약 — 시한이 지난 항목은 오늘 숫자로 판정하지 않고 `inconclusive`(판정 불가)로 닫거나 시한만 옮긴다. 이 판단은 사람이 손으로 확정해야 한다.

**영향**: 개선했다고 믿는 것들의 효과가 언제까지고 "아직 확인 안 됨" 상태로 쌓인다. 특히 혼잡 시간대 사전 대응 규칙 하나는 발동 여부를 결정할 시각(장전 07:30 전) 자체가 매일 사람 개입 없이 지나가는 구조적 문제라, 판정이 늦어지는 게 아니라 **판정할 수 있는 절차 자체가 비어 있는 상태**다(→ §1-6과 같은 뿌리).

**심각도**: P1(관측·정확성 훼손 — 개선 효과 검증이 막혀 있다).

### 1-5. 오늘 판정 시한인 가설 2건 — 어젯밤 막 구현돼 실측 재료가 아직 없다

**증상**: `2026-09-17-fixb-latency-streak-counts-past-two-days`(응답지연 연속일수를 실제로 세는 기능)와 `2026-09-17-fixc-chain-wipeout-shows-in-the-headline`(옵션체인 전멸 건수를 §1 표에 노출)의 검증예정일이 오늘이다.

**근거**: `docs/동작점검/auto/2026-09-18_증거_pre.md` §8. 두 가설 모두 커밋 `8c71d3d`(09-17 밤)로 구현되고 `001d263`으로 코드보다 먼저 등재됐다. 코드 커밋 시각이 오늘 관측 루프 기동(07:30:58)보다 이르므로 오늘 프로세스에는 실려 있다 — 다만 실측 대상(오늘 하루치 지연 패턴 · 오늘 옵션체인 전멸 여부)은 아직 안 쌓였다.

**기준**: `docs/동작점검/tools/phases.md` C-3 — 검증예정일이 도래한 가설은 **그 예정일 당일의 실측으로** 판정한다.

**영향**: 지금 판단할 수 있는 것이 없다 — 나쁜 신호가 아니라 아직 이르다는 뜻이다. 장후가 오늘자 `_지표.md`가 생성된 뒤 판정한다.

**심각도**: 이상점 아님(관측 항목) — 아래 「오늘 관측이 답해야 할 것」 1·2번으로 넘긴다.

### 1-6. 사전 대응 규칙(2026-08-04-p5) 발동 여부 — 22거래일 넘게 미결, 오늘도 결정 창을 못 잡았다

**증상**: `inquire-price`(시세 조회) 응답시간이 제한시간(4.0초)의 상당 부분(2.5초)을 넘는 시간대가 이틀 연속 나타나면 그 시간대 위클리 폴링 간격을 넓히는 「사전 대응 규칙」이 있다. 전일(09-17) 지표가 이 조건을 다시 확인했다 — 🔔 "연속 판정 성립: 18개 구간"(9~15시 여러 조회 종류, 그중 `inquire-price`는 9·10·11·12·13·14시 전부 포함, 오늘=09-17 기준). 09-17 장후 보고서(제6부 §6-1)는 이 조건이 "나흘째"가 아니라 **22거래일째** 서 있다고 재확인했다.

**근거**: `docs/동작점검/auto/2026-09-17_지표.md` §0-1(🔔 라인) · 09-17 일일점검 파일 §6-1(정정). `mahdi/main.py`의 현재 `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS = {9:4, 10:4, 11:4, 12:4, 13:4, 14:4}`는 이미 9~14시를 전부 덮고 있어(09-17 §1-6 확인) "새로 켤 시간대"는 없다 — 그런데도 그 커버 범위 **안에서** 위험선 근접이 반복되는 이유는 아직 미해결이다.

**기준**: `docs/동작점검/tools/phases.md` A-2 — "연속 3거래일 이상이면 그 자리에서 (A) 켠다 / (B) 유예한다 중 하나를 명시적으로 고른다 — 유예 재연장 금지". 그리고 규칙 자체의 발동 시점은 "조건이 성립한 **다음 거래일 장전(07:30 기동 전)**"으로 규약 문구에 박혀 있다(09-17 지표 §0-1).

**영향**: 오늘 이 세션(08:01 실행)이 시작됐을 때 이미 07:30 기동이 끝나 있었다 — 그래서 오늘도 "결정할 수 있는 시각"이 없었다. 이 구조가 계속되는 한 이 결정은 **누구의 잘못도 아니게 영원히 미뤄질 수 있다.** 실제로 비슷한 조건에서 09-08에 28분짜리 시세 장애가 난 전례가 있다.

**심각도**: P1(반복되는 구조적 위험 + 규약 위반 소지 — 유예 재연장 금지 조항을 사실상 매일 어기고 있다). 새 발견은 아니다(09-17 §6-1이 이미 지적) — 다만 "07:30 전"이라는 창 자체가 이 예약 세션 구조와 안 맞는다는 점은 이 회차가 처음 명시한다.

### 오늘 관측이 답해야 할 것

1. **`2026-09-17-fixb-latency-streak-counts-past-two-days`(검증 시한: 오늘)** — 오늘자 `_지표.md` §5-1에 실제 연속일수 표시가 정상적으로 찍히는지, 기존 🔔 문구·임계(2.5초)·규칙 ID가 그대로인지 장후가 확인한다.
2. **`2026-09-17-fixc-chain-wipeout-shows-in-the-headline`(검증 시한: 오늘)** — 오늘 옵션체인 전멸(ERROR, rows=0) 건수가 §1 「한눈에」 표에 행으로 뜨는지, 0건인 날에도 그 행 자체는 나타나는지(규약 C — 점검 규약집의 원칙 중 하나로, "0건"이 "진짜 없었다"인지 "잴 도구가 없었다"인지 반드시 구분한다) 장후가 확인한다.
3. **§1-4의 6건 가설** — 시한을 더 미룰지, `inconclusive`로 닫을지 장후가 제안하고 사람이 확정한다.
4. **§1-6 사전 대응 규칙** — "다음 거래일 07:30 기동 전"이라는 결정 창을 이 예약 세션 구조로는 못 잡는다는 문제 자체를 오늘 장후가 dev_memory에 명시적으로 남길지 검토.
5. **§1-3 미커밋 27건** — 09-17 대비 diff(주석 1건 추가만인지) 장후가 재확인.

### Fix 구현계획 (초안)

오늘 아침 관측만으로는 새로 착수할 코드 수정 항목이 없다(§1-1·1-2·1-3·1-4·1-6 전부 사람 결정 대기이거나 코드 밖 작업, §1-5는 아직 실측 불가). 장전·장중은 코드를 바꾸지 않으므로 이 절은 장후 회차가 실제 계획을 세운다.

**다만 §1-6에 새로 짚은 구조적 문제(결정 창이 "07:30 전"인데 예약 세션은 08:00에 도는 미스매치)에 대한 계획 제안**: `docs/동작점검/tools/phases.md` A-2의 해당 항목에 "이 창은 실질적으로 **전일 장후 세션**이 채워야 한다(다음날 07:30 전에 사람이 볼 수 있는 마지막 기회)"는 문구를 추가하는 것을 장후가 검토할 것. 코드 변경이 아니라 문서 정정이라 장후가 바로 판단 가능하다.

### 고도화 방안 (초안)

오늘 관측에서 새로 제안할 코드 고도화 항목은 없다. 09-17 밤 Fix B·C(`8c71d3d`)가 이미 어제 구현·커밋됐고, 그 효과 검증은 위 「오늘 관측이 답해야 할 것」 1·2번이 이어받는다.

### dev_memory 반영 초안

이 회차(자동 장전)는 dev_memory를 직접 수정하지 않는다. 장후 세션이 반영할 후보:
- §1-6 — 사전 대응 규칙의 발동 창("다음 거래일 07:30 기동 전")이 예약된 장전 점검 세션(08:00 실행)보다 이르다는 구조적 미스매치를 `NEXT_TODO.md`에 명시적으로 남길 것. 이 미스매치가 있는 한 "22거래일 미결"은 사람 태만이 아니라 **결정할 기회가 애초에 없었다**는 뜻이라는 점도 함께.

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (개장 전 / 장중 즉시)

**사람 조치 없음.** 관측 범위(07:30~08:01) 안에서 개장 전에 처리해야 할 P0 항목이 없다.

### 오늘 중 (장 마감 후)

1. **§1-4의 가설 6건** — 시한 재설정 또는 `inconclusive` 전환을 확정한다. 장후 회차가 근거와 함께 제안한다.
2. **§1-5의 가설 2건(Fix B·C) 판정** — 오늘자 지표가 나온 뒤 장후가 실측으로 판정한다. 사람이 할 일은 그 판정을 확인하는 것뿐이다.

### 결정이 필요한 것 (자동으로 못 하는 것)

3. **사전 대응 규칙(2026-08-04-p5) — 다음 조치를 어떻게 할 것인가.** (§1-6) 이미 9~14시는 레버가 덮고 있어 "새로 켤 시간대"는 없다. 남은 선택은 (A) 그 커버 범위 안에서도 위험선 근접이 반복되는 원인을 별도 조사한다(NEXT_TODO Fix 9 트랙, 이미 열려 있음) / (B) "07:30 전 결정 창"이 이 세션 구조와 안 맞는다는 점을 인정하고 phases.md·NEXT_TODO에 그 사실을 명시해 "미결정"이 아니라 "결정 불가 구조"로 재분류한다. 어느 쪽이든 사람이 골라야 (A)/(B) 사이의 소모적 반복(같은 항목이 22거래일째 "미결"로만 찍히는 것)이 끝난다.
4. **미커밋 27건 정리 여부(§1-3)** — 특히 `premarket_levels`(맥점 예측) 관련 핵심 파일들(2026-09-06 최종 수정, 이후 줄곧 미추적)이 `start_mahdi_premarket.bat`을 통해 **매일 자동 실행되고 있다**. 이 PC가 아니면 그대로 재현되지 않는다. (A) 커밋할 것 / `.gitignore`에 뺄 것 / 폐기할 것 3분류를 정한다 vs (B) 당분간 그대로 둔다.
5. **이벤트 캘린더 9월분 채우기 — 21일째.** 외부 일정(금리 발표 등)을 사람이 확인해 `mahdi/config/event_calendar.yaml`에 채워야 한다(코드 밖의 일).
6. **ZN/ES 해외선물 거래소 권한 신청** — 증권사 계좌 설정(코드 밖의 일, 만성).

### 이번 회차에 사람이 할 일이 없으면

해당 없음(위 항목 참조). 다만 **지금 당장(개장 전) 처리해야 할 것은 없다** — 3~6번은 모두 장 마감 후에 처리해도 늦지 않는다.

---

## 제2부 — 장중 ① (12:30 회차)

- 국면: **장중(intra)** · 예정 12:30 · 실행 12:34 KST — **정시 실행**(밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-18_증거_intra.md`(12:34:24 수집) + 실시간 재확인(12:36, 로그 직접 조회)
- 리포 HEAD: `486bc61`(브랜치 `master`) · 당일 커밋 **5건**(장전 세션 이후 사용자가 직접 커밋한 것으로 보임 — 이 회차는 코드·저장소를 건드리지 않았다) · 미커밋 **1건**(`.claude/`만 남음)
- 이 절 위(제1부 장전)의 본문은 고치지 않았다. 정정이 필요한 판정은 아래 이월 처분표에 적는다.

### 2-0. 장전 이월 처분 — 6건 전부 처분 + 관측 항목 5건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 이벤트 캘린더 9월분 미갱신(21일째) | 🔄 지속(만성, 의도적 방치) | 12:32:55에도 같은 WARNING("이벤트 캘린더 미기입: not_covered", `covered_through`는 여전히 2026-08-28) — 07~12시 매시 1회씩 총 6회 반복(증거 §5) |
| 1-2 ZN·ES 해외선물 조회 실패 | 🔄 지속(만성) | 오늘 07:32·08:32·09:32시에도 동일 사유(`CBOT/CME SUB거래소 신청 계좌가 아닙니다`)로 반복, 당일 누적 73건(원본 로그 직접 확인) — 계좌 권한 문제, 코드 밖의 일 |
| 1-3 미커밋 27건 | ✅ **거의 해소 — 27건 중 26건 정리됨** | 장전(08:01) 이후 사용자가 5개 커밋(`b0855e7`·`dd23de4`·`6105748`·`e177b47`·`486bc61`)을 올렸다. 그중 `b0855e7`(당일 맥점 예측 운용 경로) · `dd23de4`(맥점 연구 8종 + 리서치 문서 3건)이 바로 1-3이 지목했던 `premarket_levels` 관련 미추적 파일들이다. 지금 `git status --porcelain`은 `?? .claude/` **한 줄**뿐이다. 이번 회차는 diff 내용까지 검증하지 않았다 — 장후가 "정말 그 파일들이 커밋됐는가"를 확인하는 것을 권고 |
| 1-4 검증 시한 초과 가설 6건 | 🔄 지속 | 동일 6건 그대로(증거 §8) — `hypotheses.yaml` mtime이 09-17 그대로라 상태 변화 없음. 시한 초과 일수만 하루씩 늘었다 |
| 1-5 오늘 판정 시한 가설 2건(Fix B·C) | ➡️ 이월(장후) | `auto/2026-09-18_지표.md`·`.json`이 아직 **없다**(증거 §9, 종료 배치가 15:45에 만든다) — 실측 재료가 여전히 없어 판정 불가. 다만 오늘의 66분+ 체인 전멸 사건(아래 2-1)이 두 가설을 검증할 좋은 실측 재료가 될 것이다 |
| 1-6 사전 대응 규칙(2026-08-04-p5) 미결 22거래일째 | ⬆️ **격상 — 조건 성립 구간이 어제 8개 → 오늘 12개로 늘었고, 오늘 실제 절벽(아래 2-1)과 시간이 겹친다** | 증거 §5-1: 오늘 09~12시 `inquire-price`·`inquire-balance` 등 12구간이 위험 기준(2.5초)을 넘었다(어제 8구간). 다만 **발동 창은 설계상 "다음 거래일 07:30 기동 전"으로 못박혀 있어 장중인 지금은 발동할 수 없다** — 이 결정 구조 자체는 새 문제가 아니다(장전 §1-6에서 이미 지적) |
| (관측) fixb — "이틀"이 실제 연속일수를 인쇄하는가 | ➡️ 이월(장후) | 오늘자 `_지표.md`가 아직 없어 확인 불가 |
| (관측) fixc — 체인 전멸 건수가 「한눈에」 표에 뜨는가 | ➡️ 이월(장후) | 같은 이유로 이월. 오늘 144건(증거 §4)이라는 뚜렷한 숫자가 나왔으니 장후 판정에 좋은 재료다 |
| (관측) §1-4 6건 시한 재설정/inconclusive 전환 제안 | ➡️ 이월(장후) | 사람 손 판정 필요, 이 회차는 제안하지 않는다(장후 몫) |
| (관측) §1-6 구조적 문제 dev_memory 반영 검토 | ➡️ 이월(장후) | 오늘 조건 성립 구간이 12개로 늘고 실제 절벽과 겹쳤다는 사실을 추가해 반영할 것을 권고(아래 dev_memory 반영 초안) |
| (관측) §1-3 미커밋 27→? diff 재확인 | ✅ 판정(위 1-3에서 처리) | 26건이 사라졌다 — 상세 diff는 장후가 마저 확인 |

### 장중① 체크리스트 (phases.md B절)

| 항목 | 결과 |
|---|---|
| B-1 생존 | ✅ 관측 루프 최종 레코드 12:34:28(수집 시각과 거의 동시). 07:30~12:34 구간 3분 이상 공백 **0건**(증거 §3) — 프로세스 자체는 죽지 않았다 |
| B-1 워치독 | ⚠ 당일 102행(OK 22 / 비-OK **80**). 기록 간격 25분 이상 공백 0건(워치독 자신은 정상 주기로 계속 기록 중). 마지막 기록 12:34 → 지금까지 무기록 0분 |
| B-1 크래시 | ✅ `observation_loop_crash.log` 8.0KB, 당일 07:30 기동 이후 트레이스백 없음(mtime만으로 판단하지 않고 기동 표식 이후 줄을 확인) |
| B-2 수집(예산초과/조기포기/실패예산) | 예산 초과 13건 · 연속 타임아웃 조기 포기 **313건**(전례 없이 많다) · 실패 예산 소진 1건. `chain_total_failure`(옵션 체인 폴링 전체 실패) 152건도 이례적으로 많다(증거 §4) |
| B-2 WS 단절 | ✅ 0건(2026-09-09에 8건 찍힌 전례 있어 「진짜 0」, 증거 §5-3) |
| B-2 먼슬리 되살리기 | ⚠ 실패 12사이클(09시 5·10시 4·11시 3) 중 **완전 미회복 9건** · 부분회복 3건 — 전일(09-17) 회복률 79.1%(레그 기준) 대비 오전에 유독 몰렸다(증거 §5-1-1). 해당 분의 GEX·감마플립은 핵심 레그가 빈 채로 계산됐다 |
| B-3 판단 도달(앙상블) | ⚠ 비영 멤버(0점이 아닌 축) 최대 **3/6**, 최소 **0/6**(증거 §5-2). 12:24~12:36 최근 구간은 `options_flow`가 목록에서 아예 빠진 채 **비영 2개**(`orderflow_ofi_vpin`·`flow_position`)로 판단 중(원본 로그 직접 확인) — 아래 2-2 참조 |
| B-3 레짐 갱신 | ✅ 분마다 갱신, WARMUP 복귀 없음(08:46 최초 전이, 09:15 예열 종료 이후 `predict` 모델 유지) |
| B-3 체인 신선도 | ⚠ 아래 2-1 참조. 신선도 창(5분)을 훨씬 넘겨 오래된 스냅샷으로 판단 중인 구간이 있다 |
| B-3 이벤트 캘린더 | ⚠ 6건, 1-1에서 처분(만성) |
| B-4 코드 변경 | ✅ **없음**(이 회차는 관찰·기록만 했다) |

> 1-1~1-6과 관측 항목 5건은 위 이월 처분표에서 전부 처분했다. 아래부터 이 회차의 신규 항목이다 — **이상점 번호는 하루 안에서 이어진다**(장전 1-1~1-6 → 이 회차 1-7부터).

### 1-7. 옵션체인(파생상품 가격표) 적재가 반복적으로 끊기다가 12:36 현재도 안 풀리고 있다 🆕 ⚠ P0(현재진행형)

**증상**: 09:00 개장 이후 시세(옵션 20종목 가격) 수집이 여러 차례 완전히 비었다. 워치독(감시 프로세스)이 「10분 동안 적재 0분」(no_ingest, DEGRADED) 경보를 오늘 네 번 냈다 — 10:29~10:33(4분) → 회복 → 10:54~11:05(11분) → 회복 → 11:24~11:28(4분) → 회복 → **11:37~(현재까지 지속)**. 이 회차가 증거를 모은 12:34:24 시점 기준 이 마지막 구간은 **57분째**였고, 보고서 작성 중 실시간으로 다시 확인한 **12:36 기준으로는 60분을 넘겼다**(원본 `logs/watchdog.log` 12:36:01 줄: "연속 60분째(11:37부터)"). 같은 시각 원본 관측 로그에도 "옵션체인 이번 분 전멸"(그 분 적재 0행) ERROR가 12:35:35·12:34:33에도 계속 찍히고 있다.

**근거**:
- 워치독 비-OK 80행(당일 102행 중), 증거 §6 인용 구간 전체
- 「rows=0(그 분 수집 0행)」 최장 연속 구간: **66분**(11:28~12:33, 증거 §5-1) — 임계 20분을 3배 넘었다. 단발성 0행 분(앞뒤 정상)은 2건뿐이라 이번은 단발 사고가 아니라 **지속형 절벽**이다
- 원본 로그로 실시간 재확인(12:36): DEGRADED 상태가 12:36:01에도 이어지고 있고("연속 60분째"), 같은 시각 옵션체인 수집도 계속 0행(12:35:35, 12:36:32 ERROR/WARNING)
- 참고로 지금까지 기록된 「절벽」 5건(`cliff_episodes.md`)은 전부 **14:00~15:01에 시작**해 장 마감 무렵 풀렸다. 오늘은 **10:29부터 시작해 정오를 넘겨서도 안 풀리고 있다** — 시작 시각과 누적 발생 횟수(네 번) 둘 다 과거 기록에 없는 패턴이다
- 같은 시간대에 지연 위험선(`inquire-price` p50÷제한시간 ≥ 1.0) 돌파 창이 41/60개, 그중 **34개 창이 09:46~12:31 연속으로 이어졌다** — 이 역시 과거 최다 기록(09-17 최장 4창, 09-16 최장 8창)을 크게 웃돈다(증거 §5-1)
- 자동 재기동 장치는 없다(설계 의도 — "원인이 우리 쪽이 아닐 수 있다")

**기준**: `docs/동작점검/tools/phases.md` B-1("정규장 안의 로그 공백은 3분이면 사건") · `cliff_episodes.md`("20분 이상 연속 rows=0"이 절벽 등재 기준) · README 규약 C(0건과 계측부재 구분 — 이번은 계측이 살아 있고 진짜로 못 가져온 것을 여러 축이 함께 확인)

**영향**: 지금 이 순간에도 프로그램이 보고 있는 옵션 가격이 최소 한 시간 가까이 안 새로 고쳐지고 있다. 판단은 신선도 창(5분)이 지나면 오래된 스냅샷을 그대로 쓰도록 설계돼 있어 완전히 멈추지는 않지만, 시장이 그사이 움직였다면 그 변화를 전혀 못 본 채로 판단을 이어가고 있는 것과 같다. 급하게 대응해야 할 가격 변화가 지금 나더라도 이 프로그램은 그걸 모른다.

**심각도**: **P0**(현재진행형, 오늘 관측에 직접 위험). 다만 원인이 증권사(KIS) 쪽 응답 지연(같은 시간대 지연 위험선 돌파와 정확히 겹친다)으로 보이고, 프로세스 자체는 죽지 않았다(워치독 박동 22초 전 확인, 관측 루프 스스로도 매 분 사이클을 계속 돌고 있다) — 우리 쪽 장애로 판단되는 크래시나 프로세스 다운은 아니다.

**지금 조치(손익 비교, 재기동은 이 회차가 실행하지 않는다)**:
- **재기동 안 함(권고)** — 얻는 것: 오늘 관측 연속성 유지, 장전 커밋(`8c71d3d` Fix B·C 등)이 계속 검증 대상으로 남음. 잃는 것: 지금 이 순간도 데이터 공백이 계속됨. 과거 비슷한 다섯 사례 중 세 번은 사람 개입 없이 장 마감 무렵(15:25~15:26) 저절로 풀렸다(나머지 둘은 +9~10분 늦게 풀림) — 이번도 원인이 KIS 쪽이라면 재기동이 문제를 해결할 가능성은 낮다.
- **재기동함** — 얻는 것: 혹시 우리 쪽 세션·연결 문제라면 즉시 회복. 잃는 것: 레짐(시장 상태 판단)이 WARMUP(예열)으로 되돌아가 약 30분 판단을 못 한다. 오늘 새로 실린 Fix B·C 코드가 검증되지 않은 채로 재시작된다.
- 이 회차는 규칙상(장중 코드/설정 변경 금지) 재기동을 실행하지 않았다. **14:30 회차가 그 시점까지도 안 풀려 있으면 이 판단을 다시 검토할 것.**

### 1-8. 앙상블(여러 판단 축을 모아 결론 내는 장치) 판단 근거가 최근 한 시간 동안 2개 축으로 줄었다 🆕 P1

**증상**: 옵션 관련 판단 축(`options_flow`)이 11:30:55 이후 판단 형태 전이 로그의 「가용멤버」 목록에서 더 이상 나타나지 않는다. 12:24~12:36 구간(원본 로그 직접 확인)에는 가용멤버가 `regime_hmm`·`orderflow_ofi_vpin`·`flow_position` 3개인데, 그중 `regime_hmm`은 매번 "0점축"으로 찍혀 실제로 점수를 보태는 축은 **2개**(`orderflow_ofi_vpin`·`flow_position`)뿐이다.

**근거**:
- 증거 §5-2: `options_flow` 최초 편입 09:00:55 / 마지막 관측 **11:30:55**(그 뒤로 한 번도 안 나타남)
- 원본 로그(12:24:55~12:35:55) 5개 판단 전이 기록 모두 가용멤버 3/6, "0점축=['regime_hmm']"으로 동일
- 시간상 위 1-7의 체인 전멸 구간(11:28~12:33)과 정확히 겹친다 — `options_flow`는 옵션체인 데이터가 입력이라, 체인이 안 들어오면 이 축이 빠지는 것은 설계상 자연스러운 결과로 보인다

**기준**: `docs/동작점검/tools/phases.md` B-3("앙상블 멤버 가용성 — 죽은 멤버가 있으면 언제부터 죽었는가") · README 규약 C(형태가 같다는 것과 그 축이 살아 있다는 것은 다른 말 — DB `member_scores` 교차검증이 정본이나 이번 회차는 로그만으로 1차 확인)

**영향**: 지금 이 프로그램이 내리는 판단은 평소보다 적은 근거(보통 최대 3~4개 축인데 지금은 실질 2개)로 내려지고 있다. 근거가 줄면 판단의 확신도가 실제보다 부풀려지거나(축이 적을수록 의견 충돌이 덜 드러난다), 반대로 기회를 놓칠 수 있다.

**심각도**: P1(정확성·판단 근거 훼손). 새 발견이지만 원인은 1-7과 같은 뿌리(체인 전멸)로 보인다 — 별도 조치보다 1-7이 풀리면 함께 회복될 가능성이 높다. DB `member_scores` 교차검증은 이 회차가 하지 않았다 — 장후가 확인할 것을 권고.

### 14:30 회차가 이어받을 것

1. **§1-7(옵션체인 적재 정지, 현재진행형)이 14:30 시점에 풀렸는지부터 확인한다.** 안 풀렸으면 60분을 훌쩍 넘긴 것이라 재기동 손익 재검토가 필요하다(14:50 신규 진입 컷오프 직전이라는 점도 고려). 풀렸으면 정확한 회복 시각과 총 지속 시간을 기록한다.
2. **§1-8(`options_flow` 이탈)이 §1-7과 함께 회복되는지 확인한다.** 같이 회복되면 인과 추정이 맞았다는 뜻이고, 안 되면 별도 원인을 봐야 한다.
3. **§1-6(사전 대응 규칙, 오늘 12구간 성립)이 14:30까지 더 늘어나는지 본다.** 오늘이 실제 절벽과 겹친 첫 사례라 dev_memory에 남길 근거가 더 쌓일 수 있다.
4. `2026-09-17-fixb`·`fixc` 두 가설, §1-4의 6건은 계속 장후 몫으로 이월.
5. 새 P0/P1: 이 회차는 **P0 1건(§1-7, 현재진행형) · P1 1건(§1-8) · P2 0건(신규)**으로 마감. 1-3은 사실상 해소.

### Fix 구현계획 (초안 — 장중이라 실행하지 않음)

- **§1-7**: 코드 수정 대상이 아니다(원인이 KIS 쪽으로 보임). 장후가 오늘 하루 전체의 `rows=0` 총량과 지속 패턴을 `_지표.md`로 확정하면, 그 숫자를 `2026-09-17-fixc-chain-wipeout-shows-in-the-headline` 판정에 그대로 쓸 것을 제안한다.
- **§1-8**: 장후가 `member_scores` 테이블로 `options_flow` 이탈을 교차검증하고, 만약 체인 전멸과 무관하게 이탈이 이어진다면(즉 1-7이 풀렸는데도 1-8이 안 풀리면) 별도 P1 항목으로 승격해 원인을 조사한다(파일: `mahdi/fusion/engine.py` 관련 멤버 가용성 판정 로직).

### 고도화 방안 (초안)

오늘 관측에서 출발한 제안 1건: **워치독 no_ingest 경보에 「오늘 몇 번째 발생인지」와 「오늘 누적 지속 분」을 함께 인쇄**하는 것을 검토할 것(현재는 이번 구간 기준 연속 분만 보인다). 오늘처럼 네 번 끊겼다 풀리기를 반복하는 날, 각 경보는 "60분째"만 보여줄 뿐 "오늘 벌써 네 번째"라는 누적 맥락을 안 보여준다 — 그 맥락이 있었으면 이 회차가 §1-7을 훨씬 빨리 「이례적 패턴」으로 판단했을 것이다. 장후가 `NEXT_TODO.md`에 후보로 올릴 것을 제안(기존 §8-4/Fix D 트랙과 유사한 결의 문제).

### dev_memory 반영 초안 (이번 회차는 직접 수정하지 않는다 — 장후가 반영)

- §1-7 — 2026-09-18은 옵션체인 적재 정지가 오전~정오에 **네 차례**(10:29·10:54·11:24·11:37~) 반복되고 마지막 구간이 60분을 넘겨 진행 중이었던 날로 `cliff_episodes.md`에 새 표본으로 등재할 것을 권고. 기존 5건은 전부 14:00 이후 시작한 단발 절벽이었는데, 오늘은 **시작 시각(10:29)과 반복 횟수(4회)** 둘 다 다른 패턴이라 별도 열이 필요할 수 있다.
- §1-6 — 사전 대응 규칙 조건 성립 구간이 12개로 늘고 실제 절벽과 겹친 첫 사례라는 점을 `NEXT_TODO.md` 관련 항목에 추가할 것.
- §1-8 — `options_flow` 이탈과 체인 전멸의 시간적 일치를 `member_scores` 교차검증 결과와 함께 기록할 것.

---

## 제2부 — 장중 ② (14:30 회차)

- 국면: **장중(intra)** · 예정 14:30 · 실행 14:40 KST — **정시 실행**(10분 지연이지만 밀린 실행 아님. `phases.md` 기준 09:00~15:20 구간)
- 증거 파일: `docs/동작점검/auto/2026-09-18_증거_intra_1430.md`(14:40:04 수집) + 실시간 재확인(14:41·14:43, 원본 로그 직접 조회)
- 리포 HEAD: `486bc61`(장중①과 동일, 변화 없음) · 당일 신규 커밋 **0건**(이 회차는 코드·저장소를 건드리지 않았다) · 미커밋 2건(이 보고서 파일 자체 + `.claude/`, 둘 다 정상)
- 이 절 위(제1부 장전 · 제2부 장중①)의 본문은 고치지 않았다. 정정이 필요한 판정은 아래에 쓰고, 해당 항목은 이 절에서 처리한다.

### 2-4-0. 장중① 이월 처분 — 8건 전부 처분 + 이어받을 것 5건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 이벤트 캘린더 9월분 미갱신 | 🔄 지속(만성) | 13:32:55·14:32:55에도 동일 WARNING 반복(당일 누적 8건, 최종 14:32:54) — `covered_through`는 여전히 2026-08-28 |
| 1-2 ZN·ES 해외선물 조회 실패 | 🔄 지속(만성) | 오후에도 동일 사유로 계속 실패, 계좌 권한 문제라 코드 밖의 일 |
| 1-3 미커밋 27→1건 | ✅ 해소 유지 | `git status --porcelain` 여전히 `.claude/` 한 줄 + 이 보고서 파일 자체(M, 정상 — 장중①이 이 파일에 쓴 내용이 아직 커밋 전이라는 뜻일 뿐 새 문제 아님) |
| 1-4 검증 시한 초과 가설 6건 | 🔄 지속 | `hypotheses.yaml` mtime 여전히 2026-09-17 — 한 줄도 안 바뀌었다. 시한 초과 일수만 더 늘었다 |
| 1-5 오늘 판정 시한 가설 2건(Fix B·C) | ➡️ 이월(장후) | `auto/2026-09-18_지표.md`·`.json` 여전히 없음(증거 §9) — 종료 배치가 15:45에 만든다. 오늘 8차례 반복된 절벽(아래 1-9)이 두 가설 모두에 풍부한 실측 재료가 될 것 |
| 1-6 사전 대응 규칙(2026-08-04-p5) 미결 22거래일째 | ⬆️ **격상 — 조건 성립 구간이 12:30 시점 12개 → 지금 20개로 늘었다. 그리고 위험선 자체가 09:56부터 지금까지 한 번도 안 풀렸다(아래 1-9)** | 증거 §5-1: 오늘 09~14시 20구간이 위험 기준(2.5초)을 넘었다(어제 15구간). 발동 창은 여전히 "다음 거래일 07:30 기동 전"으로 못박혀 있어 지금은 발동할 수 없다는 점은 그대로다 |
| 1-7 옵션체인 적재정지, 12:30 시점 "60분+ 현재진행형" | ↩️ **정정 + 🔄 지속(패턴으로 재규정, 아래 1-9 참조)** | 12:30이 "안 풀린다"고 본 그 삽화는 **12:40에 풀렸다**(총 63분, 11:37~12:39). 다만 그 뒤로도 12:53~12:55(3분)·13:07~13:17(11분)·13:27~14:27(**61분**) 세 차례 더 끊겼고, **지금(14:39부터, 14:43 기준 5분째)도 다시 끊긴 상태**다. "하나의 긴 사고"가 아니라 "반복되는 짧고 긴 삽화들"이었다는 것이 정정의 핵심 — 상세는 §1-9 |
| 1-8 앙상블 판단 근거 축소(options_flow 이탈) | 🔄 지속 | 원본 로그(`판단 축 이탈/복귀`) 확인 결과 오늘 `options_flow`는 하루 종일 수십 차례 이탈·복귀를 반복했다(10~15분 주기가 정상, 체인 전멸 구간에는 65~67분씩 장기 이탈). **지금도 14:34:54부터 이탈 중**이며 위 1-7의 8번째 삽화와 시간이 겹친다 — 인과 추정(체인이 안 들어오면 이 축이 빠진다)이 오늘 하루 종일 재확인됐다. 비영 멤버는 오후 내내 2~3/6 사이(§5-2, 아래 게이지 표) |
| (이어받음) §1-7이 14:30까지 풀렸는지 | ✅ 판정 — **부분적으로만.** 12:40·12:56·13:18·14:28에 네 번 풀렸다가 14:39에 다시 끊겼다. "14:30 시점에 풀려 있었는가"에 답하면 **풀려 있었다**(14:28~14:39, 11분간), 다만 그 직후 다시 끊겼다 | §1-9 시계열 |
| (이어받음) §1-8이 §1-7과 함께 회복되는지 | ✅ 판정 — **그렇다, 매번.** `options_flow` 이탈·복귀 시각이 no_ingest DEGRADED/RECOVERED 구간과 반복적으로 겹친다(원본 로그 대조) — 별도 원인이 아니라 같은 뿌리라는 12:30의 추정이 확인됐다 | 원본 로그 직접 대조(위 표) |
| (이어받음) §1-6이 14:30까지 늘어나는지 | ✅ 판정 — **늘었다(12→20구간)** | 위 1-6 처분란 |
| (이어받음) 가설 2건·§1-4 6건 | ➡️ 이월(장후) | 변화 없음, 계속 장후 몫 |
| (이어받음) 신규 P0/P1 마감 카운트 | ✅ 판정 — 이 회차는 **P0 0건(신규, 1-7은 지속 재분류) · P1 1건(1-9, 신규) · P2 0건**으로 마감 | 아래 참조 |

### 오후 열화 게이지 (15분 창 단위 시계열, 12:00~14:41)

수집 예산 50초 · `inquire-price` read timeout 4.0초. **판정은 창 최대로 한다.**

| 창 | rows=0 사이클 비율 | inquire-price p50÷timeout(창 최대) | 비영 멤버(최소/최대, 분모 6) | 관측루프 공백 | WS 단절(누적) |
|---|---|---|---|---|---|
| 12:00~12:14 | 15/15 = 100% ⛔ | 1.01배 ⛔ | 2/2 | 0분 | 0 |
| 12:15~12:29 | 15/15 = 100% ⛔ | 1.01배 ⛔ | 2/2 | 0분 | 0 |
| 12:30~12:44 | 12/15 = 80% ⚠(12:40 회복 반영) | 1.01배 ⛔ | 2/3 | 0분 | 0 |
| 12:45~12:59 | 13/15 = 87% ⛔ | 1.01배 ⛔ | 2/3 | 0분 | 0 |
| 13:00~13:14 | 15/15 = 100% ⛔ | 1.01배 ⛔ | 2/3 | 0분 | 0 |
| 13:15~13:29 | 14/15 = 93% ⛔(13:17 회복 반영) | 1.01배 ⛔ | 2/3 | 0분 | 0 |
| 13:30~13:44 | 15/15 = 100% ⛔ | 1.01배 ⛔ | 2/2 | 0분 | 0 |
| 13:45~13:59 | 15/15 = 100% ⛔ | 1.01배 ⛔ | 2/2 | 0분 | 0 |
| 14:00~14:14 | 15/15 = 100% ⛔ | 1.01배 ⛔ | 2/2 | 0분 | 0 |
| 14:15~14:29 | 13/15 = 87% ⛔(14:28 회복 반영) | 1.01배 ⛔ | 2/2 | 0분 | 0 |
| 14:30~14:41(현재까지) | 11/11 = 100% ⛔ | 1.01배 ⛔ | 2/2 | 0분 | 0 |

경고선(0.3/0.8) · 위험선(0.7/1.0) 기준(스킬 표): rows=0 비율은 12:00부터 지금까지 **한 창도 빠짐없이 위험선(0.7) 이상**이었다. inquire-price 지연 비율도 마찬가지로 **한 창도 빠짐없이 위험선(1.0) 이상**이었다. 앙상블 비영 멤버는 **한 창도 빠짐없이 위험선(3/6) 이하**였다(최대 3, 대부분 2). 관측루프 공백과 WS 단절은 하루 종일 0으로 정상.

**경고선을 처음 넘은 창의 시각(오늘 하루 기준, 12:30 이전 포함)**: rows=0/no_ingest 위험 신호는 **10:29**(장중①이 이미 특정), inquire-price 위험선(1.0배)은 **09:56**(오늘 §5-1, 이번 회차가 처음 확인) — 즉 **12:30 회차 훨씬 이전에 이미 넘었고, 그 뒤로 지금(14:43)까지 4시간 47분 동안 단 한 번도 안전선 아래로 안 내려왔다.** 이번 회차가 새로 밝힌 것은 "언제 처음 넘었는가"가 아니라 **"넘은 채로 얼마나 오래 버티고 있는가"**다.

### 1-9. 옵션체인 적재정지가 오늘 벌써 8차례 — "긴 사고 1건"이 아니라 "반복되는 삽화"였다 🆕 P1

**증상**: 워치독 DEGRADED(no_ingest, 직전 10분 적재 0분)~RECOVERED 사이클이 오늘 총 **8차례** 발생했다. 지속시간 순으로: 10:29~10:32(4분) → 10:54~11:04(11분) → 11:24~11:27(4분) → **11:37~12:39(63분)** → 12:53~12:55(3분) → 13:07~13:17(11분) → **13:27~14:27(61분)** → **14:39~현재(5분째, 14:43 기준 미해결)**.

**근거**:
- `logs/watchdog.log` DEGRADED/RECOVERED 8쌍(위 표에 시각 전부 인용) — 원본 로그 직접 대조
- 12:30 회차(§1-7)는 7번째 삽화(63분)가 "현재진행형, 언제 풀릴지 모름"이라고 봤지만 실제로는 **회차 직후인 12:40에 풀렸다** — 자연 회복 시점이 그 회차의 관측 창(12:36)에서 불과 4분 뒤였다
- 근본 원인으로 보이는 `inquire-price` 지연은 **회복 구간에도 안 풀렸다** — 위 게이지 표에서 rows=0 비율이 100%가 아닌 창(12:30·12:45·13:15·14:15)에서도 p50÷timeout은 여전히 1.01배로 동일하다. 즉 "회복"은 "지연이 사라졌다"가 아니라 "타이밍이 맞아 그 분에는 몇 개 레그가 겨우 들어왔다"는 뜻에 가깝다
- 8차례의 삽화 사이 간격(첫 삽화 종료~다음 삽화 시작)은 22분·20분·10분·14분·11분·10분·12분으로 **비교적 규칙적**이다 — 순수 무작위 장애라기보다 어떤 주기성(리밋 윈도우 재설정 등)이 있을 가능성을 시사하나, 이 회차는 그 원인까지는 확인하지 않았다(장후·다음 조사 몫)

**기준**: `cliff_episodes.md`(20분 이상 연속 rows=0을 절벽으로 등재) — 기존 등재 5건은 전부 **14:00 이후 시작한 단발 사고**였다. 오늘은 (a) 10:29라는 이른 시작 시각, (b) 하루 8회라는 반복 횟수, (c) 60분대 삽화가 두 번이라는 점에서 기존 표본과 질적으로 다르다.

**영향**: 오늘 프로그램이 실시간 가격을 "계속 최신으로" 보고 있던 시간은 사실상 없다고 봐야 한다. 옵션 가격 정보가 잠깐씩만 들어오고 대부분 시간은 오래된 값으로 판단을 이어가고 있어서, 오늘 하루 전체의 판단 품질이 평소보다 낮았을 가능성이 높다. 급한 시장 변화에 제때 반응하지 못했을 위험이 하루 종일 이어졌다는 뜻이다.

**심각도**: P1(정확성·관측 훼손, 하루 누적). 개별 삽화 하나하나는 프로세스 다운이 아니라 증권사 응답 지연으로 보이지만, **반복 횟수와 지속시간의 합**이 오늘 하루 전체 판단 품질에 미치는 영향은 개별 P0 삽화보다 크다고 판단해 종합 심각도는 P1으로 매긴다. 다만 지금(14:43) 이 순간은 8번째 삽화가 진행 중이라 **현재 시점 상태만 보면 P0(관측 위험 현재진행형)**이다 — 아래 「14:50 컷오프 권고」로 넘긴다.

### 14:50 컷오프 권고 — 남은 시간과 손익

**지금(14:43) 상태**: 8번째 삽화 진행 중, 14:39부터 5분째. 신규 진입 컷오프(14:50)까지 **7분** 남았다.

- **개입 안 함(권고)** — 얻는 것: 오늘 관측 연속성 유지, 이미 여러 번 확인된 대로 짧으면 3~4분 안에 자연 회복(오늘 8건 중 3건이 5분 이내 회복)한 전례가 있다. 잃는 것: 남은 7분 동안 이 상태가 안 풀리면 오늘 신규 진입 판단 전체가 오래된 가격 위에서 이뤄진 채로 컷오프를 맞는다.
- **재기동함** — 얻는 것: 원인이 혹시 우리 쪽이라면 즉시 회복 가능성(다만 오늘 8건 전부 증권사 쪽 지연과 시간이 겹쳐 우리 쪽 원인일 가능성은 낮아 보인다). 잃는 것: 레짐이 WARMUP으로 돌아가 약 30분 판단 불능 — **컷오프(14:50)까지 7분밖에 안 남아, 재기동하면 오늘 남은 장 전체(14:50~15:20)를 사실상 예열 상태로 흘려보내는 것과 같다.**
- **이 회차는 규칙상(장중 코드·설정 변경 금지) 재기동을 실행하지 않았다.** 남은 시간이 짧아 재기동의 기대이익이 확실히 마이너스로 보이므로, **오늘 남은 시간에는 개입하지 않는 것을 권고한다.** 16:00(장후) 회차가 이 삽화의 최종 지속시간과 오늘 총 절벽 시간을 확정한다.

> **실시간 후속 확인(14:45)**: 이 절을 쓰는 동안 8번째 삽화가 **14:44에 끝났다**(`[2026-09-18 14:45:01] RECOVERED — 적재 정지 6분 지속 후 회복(14:39~14:44)`) — 지속 6분, 컷오프(14:50) 5분 전이었다. 위 손익 비교와 「개입 안 함」 권고는 이 회차가 판단을 내린 시점(14:43) 기준으로 유효했고, 결과적으로도 재기동 없이 6분 만에 풀렸다. **다만 이 회복이 「끝」이라는 보장은 없다** — 오늘 이미 두 번(11:37→63분, 13:27→61분) 짧게 시작했다가 나중에 길어진 전례가 있으므로, 14:50 컷오프 이후에도 9번째 삽화가 생기는지는 16:00 회차가 확인한다.

### Fix 구현계획 (초안 — 장중이라 실행하지 않음)

- **§1-9**: 코드 수정 대상이 아니다(원인이 KIS 쪽 응답 지연으로 보임, 우리 쪽 재현 경로 없음). 장후가 오늘 8개 삽화의 시작·종료 시각·지속시간을 `cliff_episodes.md`에 신규 표본으로 등재하고, 삽화 간 간격(22·20·10·14·11·10·12분)의 주기성 여부를 조사 후보로 `NEXT_TODO.md`에 올릴 것을 제안.
- **§1-8**: 여전히 `member_scores` 테이블 교차검증 미실시(로그 대조로 1차 확인만 했다) — 장후가 DB로 확정할 것.

### 고도화 방안 (초안)

오늘 관측에서 출발한 제안 1건(장중①의 제안을 구체화): **워치독 no_ingest 경보에 "오늘 몇 번째 삽화인지"와 "직전 삽화 종료 후 경과 분"을 함께 인쇄**하는 것을 검토할 것. 오늘 8번째 삽화가 시작된 14:39 시점에 이 정보가 있었다면 "또 시작됐다, 오늘 벌써 8번째, 직전 삽화 종료로부터 12분 만"이라는 맥락이 바로 보였을 것이다 — 지금은 매번 "연속 N분째"만 보여줘 반복 여부를 사람이 로그를 뒤져야 안다. 장후가 `NEXT_TODO.md`(기존 §8-4/Fix D 트랙)에 반영할 것을 제안.

### dev_memory 반영 초안 (이번 회차는 직접 수정하지 않는다 — 장후가 반영)

- §1-7/§1-9 — 2026-09-18을 `cliff_episodes.md`에 **"반복형 절벽"**이라는 새 유형으로 등재할 것을 권고. 기존 5건(단발, 14:00 이후 시작)과 달리 오늘은 8회 반복(10:29 첫 시작, 63분·61분짜리 두 건 포함)이었고, 삽화 사이에도 근본 지연(p50/timeout≥1.0)이 안 풀렸다는 점이 핵심 차이다.
- §1-6 — 사전 대응 규칙 조건 성립 구간이 하루 동안 12개(12:30 시점) → 20개(14:30 시점)로 늘었다는 것과, 위험선 자체가 09:56~14:43(그리고 그 이후로도) 한 번도 안 풀렸다는 사실을 `NEXT_TODO.md`에 추가 근거로 남길 것.
- §1-8 — `options_flow`가 오늘 하루 수십 차례 이탈·복귀를 반복했고, 체인 전멸 삽화와 이탈 구간이 로그상 반복적으로 일치한다는 것을 `member_scores` 교차검증과 함께 기록할 것.

### 16:00 회차가 이어받을 것

1. **§1-9(8번째 삽화, 14:39~)가 언제·어떻게 끝났는지 확정한다.** 컷오프(14:50) 전에 풀렸는지, 풀렸다면 몇 분짜리 9번째 기록인지.
2. **오늘 절벽 삽화의 최종 개수·총 지속시간·최장 기록을 확정**해 `cliff_episodes.md` 신규 표본 등재안을 완성한다.
3. **§1-6 성립 구간이 장 마감까지 몇 개로 최종 늘었는지** 확정(다음 거래일 장전 발동 판단의 근거 자료).
4. `2026-09-17-fixb`·`fixc` 두 가설과 §1-4의 6건은 계속 장후 몫으로 이월. 오늘의 8회 반복·61~63분 삽화 데이터가 fixc(체인 전멸 건수 노출) 판정에 좋은 재료가 될 것.
5. **`member_scores` DB 교차검증**(§1-8)을 장후가 실시.

---

## 제3부 — 장후 (15:20 ~ )

- 국면: **장후(post)** · 예정 15:45 · 실행 15:46 KST — **정시 실행**
- 증거 파일: `docs/동작점검/auto/2026-09-18_증거_post.md`(15:46:29 수집) + `auto/2026-09-18_지표.md`·`.json`(15:46 생성, `scripts/daily_ops_report.py` 자동 실행) + 원본 로그 직접 재확인(`logs/watchdog.log` 전량 대조)
- 리포 HEAD: `486bc61`(장중②와 동일) · 당일 신규 커밋 **0건**(이 회차는 코드·저장소를 건드리지 않았다) · 미커밋 **3건**(이 보고서 파일 자체 `M` · `auto/2026-09-18_지표.json` `??` · `.claude/` `??` — 셋 다 정상. 지표 사이드카는 매일 생기는 미추적 산출물, 보고서 파일은 지금 쓰고 있는 그 파일이다)
- 이 절 위(제1부 장전 · 제2부 장중①② )의 본문은 **한 글자도 고치지 않았다.** 정정이 필요한 판정은 전부 아래 이월 처분표에 쓴다.

### 3-0. 장중② 이월 처분 — 9건 전부 처분 + 「이어받을 것」 5건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 이벤트 캘린더 9월분 미갱신(21일째) | 🔄 지속(만성, 의도적 방치) | 증거 §5: `covered_through` 여전히 2026-08-28, 07~15시 매시 반복(당일 누적 **9건**, 최종 15:32:54). 변화 없음 |
| 1-2 ZN·ES 해외선물 조회 실패 | 🔄 지속(만성) | 하루 종일 동일 사유(`CBOT/CME SUB거래소 신청 계좌가 아닙니다`)로 반복. 계좌 권한 문제, 코드 밖의 일 |
| 1-3 미커밋 27→1건 | ✅ **해소 확정 — diff 재확인 완료** | 장중①이 미룬 diff 검증을 이 회차가 했다. `b0855e7`(당일 맥점 예측 **운용 경로** — `premarket_levels.py` 등 핵심 4파일)·`dd23de4`(맥점 연구 8종 + 리서치 문서 3건)를 `git show --stat`으로 직접 열어 확인 — 커밋 메시지가 "09-18 장전 점검 §1-3 · 사용자 조치 4번이 그 결정을 요구했다"고 스스로 인용하고 있다. **이 보고서의 지적이 실제로 사람의 결정과 커밋을 이끌어 낸 사례다.** 남은 `.claude/` 1건은 `.gitignore` 편입 여부 결정 대기(만성, `NEXT_TODO:1403`) — 이건 해소가 아니라 **원래도 결함이 아니었던 것**(도구 설정 폴더)이라 이상점 목록에서 뺀다 |
| 1-4 검증 시한 초과 가설 6건 | 🔄 지속 + 이 회차가 6건 전부 개별 제안 작성(아래 「가설 판정 제안」) | `hypotheses.yaml` mtime 09-17 그대로, 상태 변경 없음. 이 회차는 상태를 **바꾸지 않고** 근거와 함께 제안만 남긴다(사용자 조치 1번) |
| 1-5 오늘 판정 시한 가설 2건(Fix B·C) | ✅ **판정 완료**(아래 「가설 판정 제안」 fixb·fixc) | `auto/2026-09-18_지표.md`·`.json`이 15:46에 생성됨 — 실측 재료 확보, 오늘 90분+ 절벽 사건이 풍부한 실측치를 남겼다 |
| 1-6 사전 대응 규칙(2026-08-04-p5) 미결 | ⬆️ **최종 격상 유지 — 23거래일째, 23구간으로 마감** | 증거 §5-1: 오늘 최종 `inquire-price` 등 위험선(2.5초) 돌파 구간 **23개**(장중② 시점 20개에서 3개 더 늘었다). 발동 창("다음 거래일 07:30 기동 전")은 여전히 장중엔 발동 불가 — 이 결정은 **내일 아침 회차가 받는다**(증거 §5-1 마지막 줄이 명시) |
| 1-7 옵션체인 적재정지(최초 60분+ 삽화) | ↩️ 정정 유지 — **1-9(반복형 절벽)로 완전히 흡수, 개별 번호로는 더 안 다룬다** | 장중②가 이미 정정했다(하나의 긴 사고가 아니라 반복 삽화). 이 회차는 그 판단을 재확인만 한다 — 아래 1-9·1-10 |
| 1-8 앙상블 판단 근거 축소(options_flow 이탈) | ✅ **DB 교차검증 완료 — 인과관계 확정** | 로그 대조(장중②)에 이어 이 회차가 `auto/2026-09-18_지표.md` §14-1(DB `risk_gate_state.member_unavailable` 집계, 분모 494분)을 열어봤다: `options_flow` 가용률 **30.0%**(148/494분, 그중 「구조적」 114분 제외하면 실질 미가용은 그 나머지)이고, 미가용 대표 사유가 정확히 **"입력 없음: gex, spot, 기준선(flip/wall)"** — 옵션체인이 안 들어오면 이 축이 빠지는 것이 로그 추정이 아니라 **DB에 남은 사실**로 확인됐다. 비교: `regime_hmm` 84.8% · `orderflow_ofi_vpin` 83.0% · `flow_position` 100.0% — options_flow만 유독 낮다 |
| 1-9 옵션체인 적재정지 — 반복 삽화(장중② 시점 8회) | 🔄 지속 — **9번째 삽화 추가 확인, 아래 1-10에서 하루 총계로 마감** | `logs/watchdog.log` 원본 재확인: 14:44 회복 이후 **14:54~15:26(33분)**에 9번째 DEGRADED~RECOVERED 사이클이 새로 있었다(원문: `[2026-09-18 15:27:01] RECOVERED — 적재 정지 33분 지속 후 회복(14:54~15:26)`). 컷오프(14:50) 이후, 정규장 마감(15:20) 전후로 걸쳐 있다 |
| (이어받음) §1-9(8번째 삽화)가 언제·어떻게 끝났는지 | ✅ 판정 — **14:44에 끝났다(6분 지속), 이미 장중②가 실시간으로 확인함(14:45 후속 확인 문단)** | 장중② §1-9 마지막 문단 참조(본문 불변경) |
| (이어받음) 절벽 최종 개수·총 지속시간·최장 기록 확정 | ✅ 판정 — 아래 1-10 | — |
| (이어받음) §1-6 성립 구간 장 마감까지 최종 몇 개인지 | ✅ 판정 — **23개**(위 1-6 처분란) | — |
| (이어받음) 가설 2건(Fix B·C)·§1-4 6건 | ✅ 판정 완료(아래 「가설 판정 제안」) | — |
| (이어받음) `member_scores` DB 교차검증(§1-8) | ✅ 판정 완료(위 1-8 처분란) | — |

### 3-1. 종료 시퀀스 (15:20~15:45) — phases.md C-1 체크리스트

| 항목 | 결과 |
|---|---|
| 15:45 `stop_mahdi_marketclose.bat` 실행 | ✅ `.last_marketclose_stop.txt` = `2026-09-18T15:45:03.012352`, `.intentional_stop` = `intentional stop 2026-09-18 15:45:00.86` — 의도된 정상 종료, 크래시 아님 |
| taskkill 후 잔존 프로세스 0 | ✅ 워치독 15:30·15:40 `OK — 박동 정상 · 적재 감시 창 밖`으로 정상 유휴 전환, 15:46:02 `.watchdog_last_check.json` `action: idle · detail: 감시 창 밖` |
| `daily_ops_report.py`가 종료 절차 안에서 돌았는가 | ✅ `auto/2026-09-18_지표.md`(76.2KB)·`.json`(127.8KB) 둘 다 15:46 생성 — 산출물 존재 |
| 관측 루프 최종 레코드 시각이 종료 시각과 맞는가 | ✅ 최종 레코드 15:45:00(`inquire-price` HTTP 요청) — 종료 배치(15:45:03)와 3초 차이, 그 전에 죽은 것이 아니다 |
| 15:20~15:45 구간 자체 이상 | ⚠ **9번째 절벽 삽화가 이 구간(14:54~15:26)에 걸쳐 있었다** — 마감 직전~직후까지 데이터 공백이 이어졌다는 뜻. 다만 15:27 RECOVERED 이후 15:30·15:40 워치독은 정상(유휴)이라 종료 자체는 깨끗했다 |

이 구간에서 새로 발견된 것은 없다 — 위 표가 곧 이 구간의 전부다. 아래부터 이 회차의 신규 항목이다. **이상점 번호는 하루 안에서 이어진다**(장중②가 1-9까지 씀 → 이 회차는 **1-10부터**).

### 1-10. 오늘 하루 총계 확정 — 절벽 9회·누적 196분, 정규장 절반 가까이가 데이터 공백이었다 🆕 P1

**증상**: 오늘 옵션체인 적재정지(no_ingest DEGRADED~RECOVERED) 삽화는 하루 총 **9회**였다. 지속시간 순: 10:29~10:32(4분) → 10:54~11:04(11분) → 11:24~11:27(4분) → 11:37~12:39(63분) → 12:53~12:55(3분) → 13:07~13:17(11분) → 13:27~14:27(61분) → 14:39~14:44(6분) → 14:54~15:26(33분). **합계 196분.**

**근거**:
- `logs/watchdog.log`의 DEGRADED/RECOVERED 9쌍 전부를 원본에서 직접 대조(위 표에 시각 전부 인용, 9번째는 이 회차가 처음 확인)
- 정규장(09:00~15:20, 380분) 기준으로 겹치는 시간만 세면(9번째 삽화 중 마감 이후 6분은 제외) **최소 189분(49.7%)** — 정규장의 **절반 가까이**가 옵션 가격 공백이었다
- DB 축(`db.chain_minute_coverage.zero_row_longest_run.length`, `auto/2026-09-18_지표.md` §0-1 상단 배너)은 최장 연속 71분(11:28~12:38)으로 워치독의 같은 삽화(63분, 11:37~12:39)와 **8분 차이**가 난다 — 워치독은 "10분 창 안에 적재 0분"을 감지 로직상 1분 지연으로 보고, DB는 분 단위 `rows=0`을 그대로 센다. **둘은 같은 사건을 다른 자로 잰다**(규약 C와 같은 원리) — 어느 쪽도 틀린 게 아니라 정의가 다르다
- 판단 입력 손상도 같은 표에서 확인된다(§1 「한눈에」): **먼슬리 절대 커버리지 37.3%**(전일 99.6%, ▼62.3pt) · **GEX 입력 없던 분 239분**(전일 1분, ▲238) · **옵션체인 전멸(ERROR) 309건**(전일 2건, ▲307) · **먼슬리 완전미회복 14건**(전일 3건, ▲11)
- `cliff_episodes.md`에 등재된 기존 5건(08-14·08-26·09-01·09-03·09-16)은 전부 **단발 사고**이고 최장 기록은 09-16의 89분이다. 오늘은 (a) 시작이 10:29로 가장 이르고, (b) 하루 9회 반복이라는 점에서, (c) 60분대 삽화가 두 번(63분·61분)이라는 점에서 기존 표본 어느 것과도 다른 **질적으로 새로운 패턴**이다

**기준**: `cliff_episodes.md`(20분 이상 연속 `rows=0`을 절벽으로 등재, 지금까지 「몇 시에 시작해서 몇 분 갔다가 마감 무렵 풀렸다」라는 **단일 사고** 틀로만 설계돼 있다) · README 규약 C(같은 사건을 다른 축이 다르게 재는 것과 사건 자체가 다른 것을 구분)

**영향**: 오늘 프로그램이 실시간 옵션 가격을 믿고 판단한 시간은 절반이 채 안 된다. 나머지 절반 가까운 시간은 몇 분에서 최대 한 시간 넘게 오래된 가격으로 판단을 이어갔다. 다행히 오늘은 실제 진입(매수·매도)이 한 건도 없어 이 손상이 실제 손익으로 이어지지는 않았지만(→ 아래 「종가 기준 최종 손익」), **만약 오늘 진입 조건이 맞아 거래가 났다면 그 판단의 절반 가까이는 낡은 가격 위에서 내려졌을 것**이다.

**심각도**: **P1**(정확성·관측 훼손, 하루 누적 — 개별 삽화는 증권사 응답 지연으로 보이고 프로세스 다운이 아니라 P0으로 격상하지 않는다. 다만 **반복 횟수·누적 시간이 기존 최악 표본을 압도한다**는 점에서 dev_memory에 새 유형으로 반드시 등재해야 하는 사안이다).

**원인 귀속**: `auto/2026-09-18_지표.md` §17 교차 점검(§6↔§9-1)이 오늘 자동으로 잡은 모순 — "우리 쪽 압력은 낮은데(백오프 최대 1.64배, 전일 2.25배보다 오히려 낮다) KIS 지연 경고는 23건·ReadTimeout은 3,077건"이라는 조합은 08-04 §2-6이 미리 적어둔 판정표대로 **KIS(증권사) 쪽 귀속**이다. 우리 쪽 코드 변경은 0건(당일 신규 커밋 없음), 재기동도 0회.

### 가설 판정 제안 — 이 회차는 `hypotheses.yaml`의 `상태`를 바꾸지 않는다. 아래는 근거를 붙인 **제안**이고 확정은 사람 몫이다

| id | 이 회차의 제안 | 근거 |
|---|---|---|
| `2026-09-17-fixb-latency-streak-counts-past-two-days`(오늘 시한) | **확인(confirmed)** | 증거 §5-1 원문에 `📅 23거래일째(2026-08-19부터)` 줄이 실제로 인쇄됐다. 대가 축(`rest_latency.p95_warn_threshold`=2.5, 🔔 줄 문구)도 지표.md §0-1이 스스로 "판정 무변경"이라 확인했다 — 기존 기능을 안 깬 채 새 줄만 추가됐다는 뜻 |
| `2026-09-17-fixc-chain-wipeout-shows-in-the-headline`(오늘 시한) | **주장은 확인 · 대가 예측치는 재설계 필요(오늘 판정 보류)** | 주장: `auto/2026-09-18_지표.md` §1 「한눈에·판단 입력」 표에 **「옵션체인 전멸(ERROR)」행이 실제로 새로 생겼다**(오늘 309건 vs 전일 2건). 대가: 지표.md는 `db.chain_minute_coverage.zero_row_count`(309)를 예측 범위 `0~2`와 비교해 **「반증」**으로 냈지만, 지표.md §0-1 최상단 배너가 스스로 경고하듯 **오늘은 절벽 발생일**이라 이 수치는 fixc의 대가가 아니라 절벽 자체의 크기를 잰 것이다(규약 E 오염). `0~2`라는 예측 범위 자체가 평상시 기준값으로 보이는데 절벽일과 평상일을 가르지 않고 걸려 있다 — **대가 예측치를 「절벽 없는 날」 기준으로 다시 세울 것을 제안**(2026-09-03 고도화 3와 같은 원리) |
| `2026-09-16-recovery-hint-carries-its-sample-count`(1일 지남) | **확인(confirmed)** | 오늘 10:29:01 첫 DEGRADED 줄 원문을 직접 대조: `"같은 유형(no_ingest)의 과거 표본 중 **회복이 관측된 5건에서 3건**(08-26·09-01·09-03)이 45~85분 만에 15:25~15:26에 자연 회복했다. **나머지 둘은 어긋났다** — 09-08 15:30(마감+10분)·09-16 15:29:51(+9분51초)로 최근 두 번 연속이다"` — 예측이 요구한 「표본 수 + 이탈 사례」가 정확히 다 있다 |
| `2026-09-16-cliff-day-marks-the-hypothesis-table-itself`(1일 지남) | **확인(confirmed)** | 오늘 `auto/2026-09-18_지표.md` §0-1 최상단에 정확히 "⚠ 오늘은 절벽 발생일 — 대가 지표 해석 주의"가 인쇄됐고, 표 마지막 열(오염)에 모든 행이 `⚠`로 채워져 있다 — 오늘이 그 가설이 겨눴던 정확한 시나리오(절벽일)라 실측 그 자체가 확인이다 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`(22일 지남) | **`inconclusive`로 닫되, 판정 축을 좁혀 재도전을 제안** | 발동 여부 자체는 "다음 거래일 07:30 기동 전"이라는 창이 예약 장전 세션(08:00 실행)과 구조적으로 안 맞아(장전 §1-6이 이미 지적) 영원히 판정 불가다. 제안: 가설을 "발동했는가"가 아니라 "**다음 거래일 장전 보고서가 그 시간대를 인용했는가**"로 좁히면(지표.md 자신의 「수기 판정」 문구가 이미 이 기준을 쓰고 있다) 판정 가능해진다 — 이번 주 안에 이 좁힌 기준으로 재시도할 것 |
| `2026-08-12-fix7-regime-session-window-restore`(17일 지남) | **레버 ON 전까지 `inconclusive` 유지를 제안(변경 없음)** | 전제 레버 `REGIME_RESTORE_SESSION_WINDOW`가 오늘도 OFF — 규약 H. 레버를 켤지는 사람 결정(→ 사용자 조치) |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence`(17일 지남) | **검증예정일을 날짜가 아니라 "다음 실제 진입 발생일"로 재설정할 것을 제안** | `execution_logs` 행 0건인 이유가 오늘도 **주문 자체가 0건**이었기 때문(아래 「종가 기준 최종 손익」)이다 — 진입이 안 나는 한 이 가설은 어떤 날짜를 잡아도 영원히 판정 불가하다. `NEXT_TODO.md`(§8-3 `:444`)가 이미 같은 제안을 걸어 두고 있다 |

### 오늘 관측이 답했다 (장전 §1-5가 남긴 것 — 판정 완료)

1. `2026-09-17-fixb` — 확인. 위 표 참조.
2. `2026-09-17-fixc` — 주장 확인, 대가 축은 판정 보류(절벽일 오염). 위 표 참조.
3. §1-4 6건 — 6건 전부 근거와 함께 제안 완료(위 표). 상태 변경은 사람 몫.
4. §1-6 구조적 문제(결정 창이 "07:30 전"인데 예약 세션은 08:00에 돈다) — 아래 dev_memory 반영 초안에서 남긴다.
5. §1-3 미커밋 27건 — ✅ diff까지 재확인 완료(위 3-0 표). 실제로 이 보고서 연쇄의 지적이 커밋으로 이어졌다.

---

## 종합 판정표 — 하루 전체, 국면별 판정이 어떻게 바뀌었는가

| 번호 | 제목 | 장전 | 장중① | 장중② | 장후(최종) |
|---|---|---|---|---|---|
| 1-1 | 이벤트 캘린더 9월분 미갱신 | P2 만성 | 🔄 | 🔄 | 🔄 **P2 만성, 21일째, 변화 없음** |
| 1-2 | ZN·ES 해외선물 조회 실패 | P2 만성 | 🔄 | 🔄 | 🔄 **P2 만성, 변화 없음** |
| 1-3 | 미커밋 27건 | P2 사람결정대기 | ✅ 26건 해소 | ✅ 유지 | ✅ **해소 확정(diff 검증 완료), 잔여 1건은 애초 결함 아님** |
| 1-4 | 검증 시한 초과 가설 6건 | P1(4→6건) | 🔄 | 🔄 | 🔄 **P1, 6건 전부 제안 작성, 확정은 사람 몫** |
| 1-5 | 오늘 시한 가설 2건(관측 항목) | (이상점 아님) | ➡️ 이월 | ➡️ 이월 | ✅ **판정 완료**(fixb 확인 · fixc 주장확인/대가보류) |
| 1-6 | 사전 대응 규칙 22거래일 미결 | P1 | ⬆️ 12구간 | ⬆️ 20구간·23거래일 | ⬆️ **P1 유지, 최종 23구간·23거래일째로 마감, 내일 장전이 받는다** |
| 1-7 | 옵션체인 적재정지(최초 60분+) | (없음, 장전엔 없었다) | 🆕 P0 현재진행형 | ↩️ 정정(63분 종료) | **1-9/1-10에 완전 흡수, 개별 판정 종료** |
| 1-8 | 앙상블 판단축소(options_flow 이탈) | (없음) | 🆕 P1 | 🔄 지속 | ✅ **P1 확정 — DB 교차검증으로 인과관계까지 확인** |
| 1-9 | 반복 삽화(장중② 시점 8회) | (없음) | (없음) | 🆕 P1(현재시점 P0) | 🔄 **9회로 갱신, 1-10에서 하루 총계로 마감** |
| 1-10 | 하루 총계(9회·196분·정규장의 절반) | — | — | — | 🆕 **P1, dev_memory 신규 유형 등재 제안** |

---

## 당일 이상점 통합 대장 — 1-1부터 1-10까지, 전부

| 번호 | 제목 | 최초 발견 국면 | 최종 심각도 | 최종 처분 | 근거 위치 |
|---|---|---|---|---|---|
| 1-1 | 이벤트 캘린더 9월분 미갱신 — 21일째 | 장전 | P2 | 🔄 만성 지속, 사람 조치 대기(외부 일정 확인) | §1-1, 3-0 |
| 1-2 | ZN·ES 해외선물 조회 실패 | 장전 | P2 | 🔄 만성 지속, 사람 조치 대기(계좌 권한 신청) | §1-2, 3-0 |
| 1-3 | 미커밋 27건 | 장전 | P2 | ✅ 해소 확정(diff 검증 완료) | §1-3, 3-0 |
| 1-4 | 검증 시한 초과 가설 6건 | 장전(4건, 이후 6건) | P1 | 🔄 지속, 6건 전부 제안 작성·확정은 사람 몫 | §1-4, 「가설 판정 제안」 |
| 1-5 | 오늘 시한 가설 2건(관측 항목) | 장전 | (이상점 아님) | ✅ 판정 완료 | §1-5, 「가설 판정 제안」 |
| 1-6 | 사전 대응 규칙 22거래일 미결 | 장전 | P1 | ⬆️ 23거래일·23구간으로 마감, 내일 장전 이월 | §1-6, 3-0 |
| 1-7 | 옵션체인 적재정지 — 최초 삽화(60분+) | 장중① | P0→흡수 | ↩️ 1-9/1-10에 흡수 | §1-7(장중①), 3-0 |
| 1-8 | 앙상블 판단 근거 축소(options_flow 이탈) | 장중① | P1 | ✅ DB 교차검증으로 확정 | §1-8(장중①), 3-0 |
| 1-9 | 반복 삽화(장중② 8회 → 장후 9회) | 장중② | P1 | 🔄 1-10에서 최종 마감 | §1-9(장중②), 3-0 |
| 1-10 | 하루 총계 — 절벽 9회·196분·정규장 절반 | 장후 | **P1** | 🆕 dev_memory 신규 유형 등재 제안 | §1-10 |

---

## 종가 기준 최종 손익 / 절대원칙 판정

**계좌 구분**: `openapivts.koreainvestment.com` — **모의투자(KIS VTS) 계정**. 실거래 계좌 아님.

| 항목 | 값 | 판정 |
|---|---|---|
| 실현손익 | 0원 | ✅ (거래 없음) |
| 미실현손익 | 0원 | ✅ (거래 없음) |
| 실현손익(자본 대비 %) | 0.00% | ✅ |
| 실현손익(고점 대비 %) | 0.00% | ✅ |
| 보유 포지션 수 | 0 | ✅ |
| 계약 수 | 0계약 | ✅ |
| 나간 주문 수 | 0건 | ✅ |
| 체결통보 수 | 0건(스트림 구독 성립 9회, 끊김 8회 — 구독 자체는 살아 있었다) | ✅ |
| 절대원칙(하드 리밋·금지 사항) 위반 | 해당 사항 없음 | ✅ |

**거래 없음 — 이유**: `auto/2026-09-18_지표.md` §16-1~16-4를 직접 확인했다. 오늘 "진입 판단은 섰는데 살 종목이 없다"(사유=`no_strike_match`/`no_eligible_book`)가 09:00~14:44 사이 총 **23회** 찍혔지만 전부 종목 매칭 단계에서 탈락했고, 그 이전 단계인 **주문 자체가 이 시스템에서 아직 배선되지 않은 상태**다(§16-4: "그날 나간 주문이 0건이다… 그날 ENTER 판단 자체가 없었다는 뜻이다"·§16-2: "주문 경로가 배선되기 전에는 이것이 정상이다"). 즉 오늘 손익이 0인 것은 **오늘 하루의 결과가 아니라 지금 시스템이 관측·판단 전용(ADVISORY/CONFIRM 미통과) 단계**이기 때문이다. 절대원칙 위반도 같은 이유로 "해당 없음"이다 — 애초에 자금을 움직이는 경로가 없다.

---

## 제4부 — Fix 구현계획 (P0/P1/P2)

**P1-1. `docs/동작점검/cliff_episodes.md`에 2026-09-18을 신규 표본으로 등재, 표 구조를 「단발 사고」 전제에서 확장**
- 파일: `docs/동작점검/cliff_episodes.md`
- 현재 축 표(`## 축` 아래 표)는 열마다 "전멸 시작(단일 시각)"·"최장 연속 rows=0(단일 값)"을 적게 돼 있다 — 오늘처럼 9회 반복인 날은 이 틀에 못 넣는다.
- 변경: 09-18 열을 추가하되 "전멸 시작"에 "10:29(1회차) — 이하 총 9회, 간격 22·20·10·14·11·10·12·10분"처럼 **다회차 요약 서식**을 쓰고, 표 아래에 "## 2026-09-18 — 반복형 절벽(하루 9회, 신규 유형)" 산문 섹션을 08-26 절과 같은 형식으로 추가한다.
- 회귀 위험: 없음(문서 변경, 코드 아님).
- 검증: 사람이 읽고 "이 표 하나로 09-18이 재구성되는가"로 확인.

**P1-2. 워치독 `no_ingest` DEGRADED 알림에 「오늘 몇 번째 삽화」·「직전 삽화 종료 후 경과 분」 필드 추가**
- 파일: `scripts/watchdog_observation_loop.py` (DEGRADED 메시지를 조립하는 함수 — 현재 "연속 N분째(HH:MM부터)"·회복 표본 참고 문구를 붙이는 자리)
- 변경: DEGRADED 진입 시 당일 이전 삽화 횟수를 세어 "오늘 N번째 삽화, 직전 삽화(HH:MM~HH:MM) 종료로부터 M분"을 문구에 추가. 이미 장중①·②가 두 차례 제안한 항목(§8-3 `NEXT_TODO:238:246` 계열, 기존 Fix D 트랙)을 구체 파일·자리까지 좁힌 것.
- 회귀 위험: 낮음 — 문구 추가만, `mahdi.ops.watchdog_metrics`의 `startswith` 집계 로직은 건드리지 않는다(2026-09-16 recovery-hint 가설이 이미 이 집계 방식의 안전성을 확인했다).
- 검증: `tests/test_watchdog_observation_loop.py`에 "오늘 N번째" 문구 검증 케이스 추가 후 회귀 실행.

**P2-1. `hypotheses.yaml`의 `2026-08-19-fix2-roundtrip-leaves-db-evidence`·`2026-08-12-fix7-regime-session-window-restore` 검증예정일을 날짜에서 사건 기반으로 재설정**
- 파일: `docs/동작점검/hypotheses.yaml`(스키마) + `docs/동작점검/tools/collect_evidence.py`의 `due_hypotheses()`(판정 로직)
- 변경: 두 가설 모두 "다음 날짜"가 아니라 "다음 실제 진입 발생일" / "다음 레버 ON 전환일"을 조건으로 갖도록 필드 추가 검토. `NEXT_TODO.md`(§8-3 `:444`)가 이미 이 방향을 제안해 뒀다.
- 회귀 위험: 중간 — `due_hypotheses()`가 날짜 비교에 의존하는 다른 가설에 영향 없게 조건부 분기로 넣어야 한다.
- 검증: `tests/test_ops_hypotheses.py` 확장.

**P2-2. `docs/동작점검/tools/phases.md` A-2에 사전 대응 규칙 결정 창 문구 정정**
- 파일: `docs/동작점검/tools/phases.md`
- 변경: "이 창은 실질적으로 **전일 장후 세션**이 채워야 한다(다음날 07:30 전에 사람이 볼 수 있는 마지막 기회)"라는 문구 추가 — 장전이 이미 제안한 것을 이 회차가 실행 확정.
- 회귀 위험: 없음(문서).

---

## 제5부 — 고도화 방안

**고도화 1 — 지연 위험선 돌파를 `no_ingest`의 「선행 경보」로 승격 검토.** 오늘 §17 교차 점검이 자동으로 잡아낸 모순(우리 압력은 낮은데 KIS 지연·타임아웃만 높다)과, 절벽 9회 전부가 `inquire-price` p50/timeout ≥ 1.0 구간과 시간적으로 겹친다는 사실(오늘 09:56부터 15시대까지 이 비율이 단 한 번도 안전선 아래로 안 내려왔다, 증거 §5-1)이 오늘 함께 확인됐다. 위험선 돌파 신호가 이미 살아 있는데도 `no_ingest` 경보는 "이미 10분간 적재 0분"이 된 뒤에야 뜬다 — 위험선 돌파를 별도의 "곧 절벽이 온다" 조기 신호로 승격하면, 사람이 재기동 여부를 판단할 시간을 지금보다 몇 분 더 벌 수 있다. `NEXT_TODO.md`의 「지연 경고선을 선행 신호로 재정의할 것인가」(§8-3 `:998`) 항목과 같은 결의 — 이번엔 오늘 데이터(23구간, 9회 삽화 전부 겹침)가 근거로 추가됐다.

**고도화 2 — `options_flow` 가용성(§14-1)을 대시보드에 실시간 노출.** 오늘 이 지표가 DB에 이미 원인별로(입력없음: gex/spot/기준선) 정확히 남고 있다는 것을 확인했다(위 1-8). 지금은 장후 보고서를 봐야만 보이는데, 장중에 이 숫자가 COCKPIT에 노출되면 "지금 판단이 몇 개 축으로 내려지고 있는가"를 사람이 실시간으로 알 수 있다. `NEXT_TODO.md` §8-3 `:254`(Fix 6)이 이미 이 방향을 열어 두고 있다 — 오늘 데이터가 그 우선순위를 뒷받침한다.

---

## dev_memory 반영 초안 (이 회차는 직접 수정하지 않는다 — 사람 확인 후 다음 세션이 반영)

- **CURRENT_STATE.md / cliff_episodes.md**: 2026-09-18을 「반복형 절벽」 신규 유형으로 등재(위 Fix P1-1). 기존 5건(단발, 14:00 이후 시작)과 달리 10:29 첫 시작·9회 반복·196분 누적이라는 점이 핵심 차이.
- **NEXT_TODO.md**: (1) 사전 대응 규칙 조건 성립이 23거래일째·23구간으로 최댓값을 경신했다는 사실 추가. (2) 워치독 DEGRADED 문구에 "오늘 N번째 삽화" 필드 추가(Fix P1-2, 구체 파일 위치 명시). (3) `hypotheses.yaml`의 08-12·08-19 두 가설을 사건 기반 검증예정일로 재설정하는 안(Fix P2-1). (4) `.claude/`의 `.gitignore` 편입 여부 — 09-17 §1403 항목, 오늘도 미결.
- **DECISION_LOG.md**: 오늘(09-18) `options_flow` 이탈 원인을 DB `risk_gate_state.member_unavailable`로 확정한 것 — "입력 없음: gex, spot, 기준선(flip/wall)"이 옵션체인 전멸과 시간적으로 정확히 겹친다는 사실을 판정 근거로 기록.
- **hypotheses.yaml**(사람이 손으로 확정): 위 「가설 판정 제안」 표의 6개 판정안 그대로 반영 검토 요청.

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (장 마감 직후)

**사람 조치 없음.** 장이 끝났고 포지션도 없다(0건) — 지금 당장 취해야 할 행동이 없다.

### 오늘 중 (또는 다음 점검 전까지)

1. **`hypotheses.yaml` 8건 상태 확정** — 이 회차가 근거와 함께 제안한 대로:
   - `2026-09-17-fixb` → 확인 / `2026-09-16-recovery-hint` → 확인 / `2026-09-16-cliff-day` → 확인 (3건은 오늘 실측이 뚜렷해 그대로 반영해도 될 것으로 보인다)
   - `2026-09-17-fixc` → 주장만 확인, 대가 예측치(`0~2`)는 절벽일 오염이라 재설계 필요
   - `2026-08-26-p1-3` → `inconclusive`로 닫고 판정 축을 좁혀 재도전(제안 내용 위 표 참조)
   - `2026-08-19-fix2` → 검증예정일을 "다음 실제 진입일"로 재설정(사건 기반)
2. **dev_memory 3파일 반영** — 위 「dev_memory 반영 초안」 그대로 사람이 확인 후 다음 세션이 append.
3. **미커밋 3건**(이 보고서 파일·`auto/2026-09-18_지표.json`·`.claude/`) 커밋 — 앞 둘은 일상적 산출물이라 그냥 커밋하면 되고, `.claude/`만 아래 5번과 함께 결정.

### 결정이 필요한 것 (자동으로 못 하는 것)

4. **사전 대응 규칙(2026-08-04-p5) — 23거래일째 미결.** 오늘도 발동 못 함(장중엔 발동 불가한 구조). 선택지: (A) 이미 9~14시를 레버가 덮고 있는데도 그 안에서 절벽이 반복되는 근본 원인을 별도 조사(`NEXT_TODO` Fix 9 트랙, 이미 열려 있음) / (B) phases.md에 "이 결정은 전일 장후가 채운다"는 문구를 박아(제4부 P2-2) "미결"이 아니라 "결정 불가 구조"로 재분류. 둘 중 하나를 고르지 않으면 이 항목은 내일도 23→24거래일째로 계속 늘어난다.
5. **미커밋 `.claude/` 정리** — `.gitignore`에 넣을지 그대로 둘지(코드/설정 폴더라 커밋 대상은 아님, 무시 목록에 넣기만 하면 매일 뜨는 `?? .claude/` 줄이 사라진다).
6. **이벤트 캘린더 9월분 채우기 — 21일째.** 외부 일정(금리 발표 등)을 사람이 확인해 `mahdi/config/event_calendar.yaml`에 채워야 한다(코드 밖의 일).
7. **ZN/ES 해외선물 거래소 권한 신청** — 증권사 계좌 설정(코드 밖의 일, 만성).
8. **오늘의 반복형 절벽(1-10)을 보고 재기동 자동화를 고려할지** — 오늘은 규칙상(장중 설정 변경 금지) 개입하지 않았고 결과적으로 매번 자연 회복했다. 다만 하루 196분(정규장의 절반 가까이)이라는 숫자가 이 방침을 계속 유지할지 재검토할 근거가 될 수 있다. 급한 결정은 아니다 — 표본이 더 쌓인 뒤 판단해도 늦지 않는다.


---

## 제6부 — 장후 자동조치 구현 결과 (16:26~17:0x · 자동)

> 예약 작업 `mahdi-postmarket-autofix`. ⛔ **앞 회차 본문은 한 글자도 고치거나 지우지 않았다** —
> 정정이 필요한 것은 이 절 안에서만 적는다(아래 §6-6).

### 6-0. 가드 4종

| 가드 | 결과 |
|---|---|
| 장후 절 존재 | ✅ `## 제3부 — 장후`(398행) · `## 제4부 — Fix 구현계획`(531행) |
| 완결 확인 | ⚠ **예외 통과 — 여드레째**(아래) |
| 중복 실행 방지 | ✅ 상태파일 날짜 `2026-09-17` ≠ 오늘, 해시도 다르다 |
| 장중 금지 | ✅ 16:26 실행 · 마흐디 프로세스 **0개** · 브랜치 `master` |

**가드 2 예외.** 스킬 §1-2가 요구하는 완결 표기(「이 파일은 여기서 닫힌다」·
「사용자 조치 — …최종본」)가 **오늘 파일에도 둘 다 없다.** 09-09 · 10 · 11 · 14 · 15 · 16 ·
17과 같고, 09-10 회차가 확인한 대로 **「최종본」 표기는 09-09부터 템플릿에서 빠졌다** —
규칙대로면 이 작업은 영영 안 돈다. 진행 근거 넷: ① 「사용자 조치」가 「결정이 필요한 것」
절까지 갖춰 하루를 닫는다 ② 「종합 판정표」·「당일 이상점 통합 대장」·「종가 기준 최종 손익」
세 종합 절이 전부 있다 ③ 제4부·제5부가 다 있다 ④ 마흐디 프로세스 0개.
⚠ **상시화 금지 — 여드레째다.** `NEXT_TODO.md`에 사용자 몫으로 다시 올렸다.

**병행 세션 확인** — 당일 커밋 5건(11:07~11:09)은 전부 **장전 회차와 사람의 작업**이고
장후 자동조치 항목과 겹치지 않는다. 같은 항목을 두 번 구현하지 않았다.

### 6-1. 구현 완료 — 2건

**P1-1. `cliff_episodes.md`에 일곱 번째 표본 등재** (커밋 `6fa2c48`)

- **왜** — 기존 여섯 표본은 전부 *「몇 시에 시작해 몇 분 갔다가 마감 무렵 풀렸다」*는
  **단일 사고**다. 그래서 표의 축이 「전멸 시작(한 시각)」·「최장 연속(한 값)」·
  「회복(한 시각)」으로 돼 있고 **오늘은 그 틀에 안 들어간다.** 세 칸 전부 다회차 서식으로
  적었고, 「적는 법」에 그 서식 규칙을 박았다.
- **걸린 시간** — 약 25분(대부분 표 열두 칸의 값을 원본에서 직접 확인하는 데 썼다).
- **안 하면** — 오늘이 표본에서 빠진다. 그러면 여덟 번째 날 *「하루에 여러 번은 처음인가」*를
  물을 수 없고, 09-16이 「일곱 번째가 나오면 물을 것」으로 걸어 둔 세 질문이 답 없이 남는다.

09-16의 세 질문에 오늘이 낸 답:

| 질문 | 오늘의 답 |
|---|---|
| 회복 지연이 세 번 연속인가 | **질문 자체가 어긋났다.** 아홉 중 **여덟이 장중에** 저절로 풀렸다(10:33 · 11:05 · 11:28 · 12:40 · 12:56 · 13:18 · 14:28 · 14:45). 마지막만 15:27(마감 +7분)이다 |
| 89분을 넘는 표본이 나오는가 | **단일 삽화로는 아니다**(최장 71분). **하루 누적으로는 196분**, 종전 최악(09-16 89분)의 **2.2배**다 |
| 15시가 레버에 들어간 뒤 첫 절벽인가 | **아니다.** 15시는 오늘도 레버 밖이고, 9회 중 **여덟이 레버가 덮은 10~14시 안**에서 시작했다 |

**P1-2. DEGRADED 줄이 「오늘 몇 번째 삽화인가」를 말한다** (커밋 `89df1fa`)

- **왜** — 오늘 삽화가 9회였는데 **DEGRADED 196줄 어디에도 순번이 없다.** 그래서 「9회」는
  로그가 말한 것이 아니라 **장중②(8회)와 장후(9회)가 사람 손으로 비-OK 줄을 묶어** 낸 수다.
  08-21에 16분/3구간을 손으로 묶었던 것과 같은 자리이고, 그때 만든 답이
  `track_degraded_episode()`였다 — 다만 그것은 삽화 **안**의 분을 센다. 이 축은 그 바깥,
  **삽화들 사이**다.
- **걸린 시간** — 약 40분(구현 15분 · 회귀 테스트 18건 20분 · 실측 재생 5분).
- **안 하면** — 다음 반복형 절벽일에도 「총 N회」를 사람이 또 손으로 묶는다. 그리고 그 수는
  회차마다 달라진다 — 오늘 장중②가 8, 장후가 9로 적은 것이 그 증거다.

⚠ **함정 게이트에서 하나 갈렸다.** 리포트는 판정 로직을 `mahdi/ops/liveness.py`로 부를 법한
자리에 두었으나 실물은 **`mahdi/liveness.py`**다(`mahdi/ops/liveness.py`는 **없다**).
그리고 **대장을 `track_degraded_episode()`의 상태에 얹을 수 없다** — 그 상태는 삽화가 닫히는
순간 호출측이 **파일째 지운다**(`_write_degraded_episode(None)` → `unlink`). 얹었으면 이
카운터가 **영원히 1에서 멈춘다.** `_MISSING_CHECK_STATE` 주석이 `next_state()`에 대해 이미
똑같은 함정을 적어 뒀고 `_DEGRADED_EPISODE_STATE`가 별도 파일이 된 이유도 같다 —
**같은 함정에 세 번째로 걸리지 않으려고** 별도 파일 + 별도 순수 함수로 갔다.

**실측 재생** — 오늘 `watchdog.log`의 no_ingest DEGRADED **196줄을 그대로** 먹였다:

```
삽화 9회 · 순번 줄 8줄
  오늘 2번째 삽화 · 직전 삽화(10:29~10:32, 4분) 종료로부터 22분
  오늘 3번째 삽화 · 직전 삽화(10:54~11:04, 11분) 종료로부터 20분
  …
  오늘 9번째 삽화 · 직전 삽화(14:39~14:44, 6분) 종료로부터 10분
```

### 6-2. 억제와 판정 무변경 — B등급 항목이 지불한 값

**억제**: 순번 문구는 **두 번째 이후** 삽화의 **첫 분**에만 나온다. 첫 삽화엔 「직전」이 없고
「오늘 1번째」는 아무것도 말해 주지 않으면서 평범한 하루마다 한 줄을 더한다.
→ **삽화 1회당 최대 1줄**이고, 역대 최다인 오늘 재생으로도 **8줄**이다. 같은 줄에 붙는 회복
참고 문구 14줄과 합쳐도 **하루 30줄 예산 안**이고, 둘 다 기존 줄 **끝**에 붙으므로
**새 줄은 0이다.**

**판정 무변경**: 대장은 `liveness.next_state()`·`decision.should_alert`·재기동 임계
**어디에도 입력되지 않는다.** 임계도 경보 조건도 한 줄 안 건드렸다 — 바꾼 것은
**「무엇을 세는가」**이지 **「얼마부터 위험한가」**가 아니다.
회귀 테스트 18건 중 **판정 무변경 축이 5건**이다.

**규약 준수**: 규약 C — `episode_sequence_notes`는 **0인 날에도 키가 실린다**(키 없음 =
「그날은 이 문구 자체가 없던 버전이다」). 규약 E — **전용 축**이고
`degraded_checks`(아팠던 분 196) · `recovered_episodes`(닫힌 사건 9)에 얹지 않았다.
규약 G — 삽화 수는 그날 KIS 상태에 비례한다(수가 줄었다고 **우리가** 좋아진 것이 아니다).

### 6-3. 가설 — 확정 3건 · 신규 등재 1건

**확정**(리포트 「가설 판정 제안」 표의 제안·근거 그대로, 커밋 `72e378e`):

| id | 확정 | 근거 실물 |
|---|---|---|
| `2026-09-16-recovery-hint-carries-its-sample-count` | **confirmed** | `logs/watchdog.log` 2767행(10:29:01)에 표본 수와 이탈 두 건이 원문대로 인쇄 |
| `2026-09-16-cliff-day-marks-the-hypothesis-table-itself` | **confirmed** | `auto/2026-09-18_지표.md` 38행이 「오염」 열을 달고 전 행 `⚠` |
| `2026-09-17-fixb-latency-streak-counts-past-two-days` | **confirmed** | `auto/2026-09-18_증거_post.md` **290행**에 `📅 23거래일째(2026-08-19부터)` |

🔴 **fixb의 예측에 적힌 「4」는 안 나왔다 — 23이 나왔다. 사후에 맞추지 않고 그대로 남겼다.**
「4」의 출처는 09-17 리포트가 인용한 09-15 §1-7인데 그 절이 09-14까지만 거슬러 올랐다.
즉 **틀린 것은 계산이 아니라 근거의 기억 길이**이고, **축소 보고를 고치려던 항목의 예측치
자체가 축소 보고였다.** 구현한 계산기는 09-17 당일에 이미 22거래일째를 냈고 오늘 23으로
하루만큼 늘었다 — **계산기는 내내 일관됐다.** 판정은 「줄이 실렸는가」에 준다.

⛔ **손대지 않은 것 넷**: `2026-09-17-fixc`(대가 축이 절벽일 오염이라 리포트가 **판정 보류**) ·
`2026-08-26-p1-3` · `2026-08-12-fix7`(둘 다 `inconclusive` 제안 — 스킬 §3이 자동 확정에서
**명시적으로 제외**) · `2026-08-19-fix2`(검증예정일 재설정 제안이지 **상태 판정이 아니다**).
pending **8 → 6**, 항목 **190 → 191**.

**신규 등재 1건** — `2026-09-18-p1-2-degraded-line-says-which-episode-of-the-day`
(구현일 09-18 · 검증예정일 **09-21** · 상태 `pending`). ⚠ **코드보다 먼저 커밋했다**
(`72e378e` → `89df1fa`). 주장·대가는 리포트 제4부 P1-2의 「변경」·「회귀 위험」 줄에서
그대로 옮겼다. metric 경로 둘 다 오늘 `지표.json`에 **실재 확인**(`recovered_episodes` 9 ·
`degraded_checks` 196).
⚠ **새 전용 축 `watchdog.episode_sequence_notes`는 예측 경로로 못 썼다** — 오늘 사이드카에
아직 없기 때문이다(이 fix가 만든다). 그 사실을 가설의 `주의:`에 적었다.

### 6-4. C등급으로 미룬 것 — 4건

| 항목 | 사유 |
|---|---|
| **제4부 P2-1** (가설 2건 검증예정일을 사건 기반으로 재설정) | `hypotheses.yaml` **스키마 변경** + `due_hypotheses()` **판정 로직** 변경이고 리포트 자신이 「필드 추가 **검토**」·「회귀 위험 **중간**」이라 적었다. 09-17 Fix A와 **같은 항목이 이틀째** 제안만 쌓인다 |
| **제4부 P2-2** (`phases.md`에 결정 창 문구) | 🔴 리포트 안에서 **충돌한다** — 제4부는 「이 회차가 실행 확정」이라 적었으나, 「사용자 조치」 **`### 결정이 필요한 것` 4번**이 같은 것을 (A)/(B) **선택지로 사람에게 넘겼다**. 스킬은 「사용자 조치」가 **우선순위의 정본**이고 그 절 항목은 ⛔ **절대 손대지 않는다**고 못 박는다. **폐기 목록이 리포트를 이기듯, 이 자리는 사용자 조치가 제4부를 이긴다** |
| **제5부 고도화 1** (지연 위험선을 `no_ingest` 선행 경보로 **승격**) | 리포트 자신이 「승격 **검토**」로 적었고 **신호 재정의는 임계 변경**이다. 예측치(주장·대가)도 없다 |
| **제5부 고도화 2** (`options_flow` 가용성 COCKPIT 노출) | 예측치가 없다 — 규약상 **방안이 아니라 아이디어**다. 예측치만 적으면 착수 가능하다 |

**고도화 착수 0건** — 하루 상한 3건에 못 미친다. **넘쳐서 줄인 것이 아니라 자격을 못 갖췄다.**

### 6-5. 폐기 목록 위반으로 기각한 것 — **0건**

착수 전 `NEXT_TODO.md`의 「폐기·종결된 안건」(2961행)과 「⚠ 다시 올리지 말 것」(1720행)을
통째로 읽고 시작했다. 오늘 리포트는 폐기 항목(**Slack 토글** · GEX 광폭 체인 · 예산 규칙 (B)
개정 · 폴러별 전용 페이서 · httpx INFO 축소 · `handle_message` DB 가드)을 **하나도 올리지
않았다.** ⛔ **Slack 토글은 켜지 않았다.**

### 6-6. 정정 — 앞 회차 본문은 안 고쳤다. 여기서만 적는다

1. 🔴 **§1-10 「우리 쪽 코드 변경은 0건(당일 신규 커밋 없음)」은 사실이 아니다.**
   당일 커밋은 **5건**이다(11:07~11:09, `4b91b52`→`486bc61`, `mahdi/` 코드 1,043줄 포함).
   **그래도 결론(KIS 귀속)은 바뀌지 않는다**: 첫 삽화(10:29)보다 **38분 뒤**이고
   **재기동 0회**라 실행 중이던 프로세스는 옛 코드 그대로였다(`process_starts` 1건 ·
   약 08:15 기동). ⚠ 다만 **「커밋이 없었다」와 「돌던 코드가 안 바뀌었다」는 다른
   문장이다**(규약 C). `cliff_episodes.md`의 「우리 쪽 조치」 칸은 후자로 적었고,
   「적는 법」에 **이 축은 커밋 이력이 아니라 `process_starts`와 재기동 횟수로 읽으라**고 박았다.
2. **§1-10의 삽화 간격 다섯 번째는 11분이 아니라 12분이다**(12:55 → 13:07).
   원본 로그 재생 결과 전체는 22 · 20 · 10 · 14 · **12** · 10 · 12 · 10분이다.
3. **§1-10이 적은 「기존 5건(08-14·08-26·09-01·09-03·09-16)」은 여섯이고 09-08이 빠졌다.**
   `cliff_episodes.md` 축 표의 열은 08-14 · 08-26 · 09-01 · 09-03 · **09-08** · 09-16
   **여섯**이다(그래서 오늘이 **일곱** 번째다). 「최장 기록은 09-16의 89분」은 맞다.

### 6-7. 테스트

**2,358 → 2,376 passed / 0 failed** (신규 18건 — `tests/test_watchdog_episode_sequence.py`).
그중 **판정 무변경 축 5건**. 기존 테스트는 **하나도 고치지 않았고 완화하지도 않았다**
(`test_liveness.py` · `test_watchdog_observation_loop.py` · `test_watchdog_recovery_hint.py` ·
`test_ops_watchdog_metrics.py` · `test_ops_watchdog_invocation_trail.py` **138건 무수정 통과**).
`hypotheses.yaml` 무결성: **191항목** 파싱 · pending 6 · `test_ops_hypotheses.py` 69건 통과 ·
CRLF 0건. ruff는 `.venv`에 미설치라 못 돌렸다(**열두 회차 연속**).

### 6-8. 커밋

| 해시 | 무엇 |
|---|---|
| `a733672` | 당일 점검 산출물 — 09-18 보고서 + `_지표.json` |
| `72e378e` | 가설 3건 확정 + P1-2 예측치 선등재 (**코드보다 먼저**) |
| `6fa2c48` | P1-1 — `cliff_episodes.md` 일곱 번째 표본 |
| `89df1fa` | P1-2 — 순번 문구 + 전용 축 + 회귀 테스트 18건 |

⛔ `git add .`을 쓰지 않았고 **사람의 미결 미커밋을 하나도 건드리지 않았다.**
⛔ `.claude/` · `docs/Dev_md/dashboard_build_prompt_1.md`는 **손대지 않았다**(미결정 사항).

### 6-9. 🔴 사용자 몫으로 남은 것

1. 🔴 **사전 대응 규칙 `2026-08-04-p5` 발동 여부 — 23거래일째 미결**(사용자 조치 4번).
   오늘 (A)/(B) 선택이 **또 안 됐다**. 발동 창은 **다음 거래일 장전(07:30 전)**뿐이고,
   ⛔ 「내일 아침」 버킷은 자동조치가 손대지 않는다(등재 창 · 소급 금지).
   ⚠ 판단 재료가 바뀌었다 — 조건이 「나흘째」가 아니라 **23거래일째** 서 있다.
2. **`hypotheses.yaml` 나머지 5건 확정**(사용자 조치 1번) — fixc의 **대가 예측치 재설계**
   (절벽일 오염) · `2026-08-26-p1-3`을 `inconclusive`로 닫고 판정 축 좁혀 재도전 ·
   `2026-08-19-fix2` 검증예정일 사건 기반 재설정(= 제4부 P2-1, 승인 필요).
3. **제4부 P2-2 (A)/(B) 선택** — 위 6-4 참조. 리포트 안에서 충돌해 손대지 않았다.
4. 🔴 **워치독 회복 참고 문구가 오늘로 낡았다.** `_NO_INGEST_RECOVERY_HINT`는 *"회복이 관측된
   **5건에서 3건**"*이라 적는데 오늘 표본(일곱 번째)이 그 분모에 없다. 그 상수 주석이
   *"새 표본이 나오면 `cliff_episodes.md`의 표와 **이 상수를 함께** 고칠 것"*을 **두 번**
   (09-03 · 09-16) 적어 뒀고, **09-08에 못 고쳐 09-16에 고쳤던 일이 반복될 자리다.**
   ⛔ 오늘 리포트 제4부가 항목으로 올리지 않아 자동조치가 손대지 않았다 — `NEXT_TODO.md`에 올렸다.
5. **이벤트 캘린더 9월분 — 21일째** · **ZN/ES 해외선물 권한 신청**(둘 다 코드 밖).
6. **`.claude/` `.gitignore` 편입 여부 — 25일째 미결정.**
7. **이 예약 작업의 스킬 §1-2 완결 판정 — 여드레째.** ⚠ 상시 예외 처리는 금지.
8. **ruff 미설치 — 열두 회차 연속** 린트 못 돌림.

### 6-10. 주의

⚠ 저장소 `python`은 3.7(경로 우선)이라 파일 쓰기·테스트는 `.venv/Scripts/python.exe`(3.12)로
했다. ⚠ 패치는 전부 스크립트 파일로 만들어 실행했다(Bash heredoc 이스케이프 회피 — 09-07).
⚠ 콘솔이 cp949라 `PYTHONIOENCODING=utf-8`을 붙였다. ⚠ 한글 경로 문서 읽기·검색은 PowerShell
절대경로로 했다. ⚠ 줄바꿈은 `read_bytes().count(b"\r\n")`으로 확인했다 — 손댄 파일 전부 **LF**.
⚠ **당일 산출물을 재생성하지 않았다**(09-04 「재생성은 절반만 복구다」) — 그래서 순번 줄은
**다음 삽화부터**, `episode_sequence_notes` 축은 **09-19 사이드카부터** 보인다.
오늘 값은 **0**이고 그것은 「안 붙었다」가 아니라 **「그날 코드가 안 돌았다」**이다.
⚠ 모든 읽기 전용 git에 `--no-optional-locks`를 썼고 `.git/index.lock` 잔재 없음.
