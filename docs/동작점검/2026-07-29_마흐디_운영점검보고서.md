# 마흐디(Mahdi) 운영 점검 보고서 — 2026-07-29 (진행 중)

**점검 범위**: 07-29(수) 장전 07:30:00 최초 기동 ~ 보고 작성 시점(DB `now()` 기준 09:09 KST,
정규장 개장 09:00 대비 약 9분 경과). **주의**: 이 보고서는 하루 전체가 아니라 장전+장중 초반
약 99분만 다룬다 — 15:45 장마감 이후 재작성 또는 후속 갱신 필요.
**작성 기준**: 설계문서 v6, `docs/dev_memory/{NEXT_TODO,DECISION_LOG,SESSION_LOG}.md`(단, 이
세 파일은 07-28 8차 세션 내용까지만 담겨 있고 아직 커밋되지 않은 상태 그대로임 — 오늘자 신규
기록 없음), 최근 커밋(`25c0af9`까지), 직전 보고서(07-28), 07-29자 로그
(`logs/{premarket_startup,observation_loop,cockpit}.log`) 전수 분석 + 라이브 Docker DB
(`rate_limiter_status_log`/`signal_decisions`/`account_balance_snapshots`) 직접 조회 대조.

---

## 0. 한 줄 요약

07-29는 장전 기동 자체는 6일 연속 완전히 깨끗했지만, **어제(07-28) 세션에서 신규 추가한 계좌
잔고 폴러(`poll_account_balance_cycle`)가 07-27/07-28에 어렵게 확정한 스태거링 설계를 다시
깨뜨려, 스케줄 밀림·레이트리밋 백오프가 100분도 안 되는 사이 07-24 "하루 전체" 최악 수준에
근접하는 규모로 재발했다**(REST수집 평균 41.8초·최대 133.19초, 밀림 최대 73.4초, 백오프 최대
배율 2.69배 — 역대 최고 경신). 원인은 명확하다: 어제 추가된 계좌 잔고 폴러(실제 KIS REST 호출)가
기존 옵션체인(t=0)·투자자수급(t=15초)과 함께 **60초 주기 REST 폴러 3개를 20초 폭 안에** 몰아넣는
설계였고, 07-27/07-28에 검증했던 "4개 폴러 스태거링"에 이 5번째 폴러가 반영되지 않았다. 반면
**계좌 추적기·Signal Fusion·Risk Engine 라이브 배선 자체는 기능적으로 완전히 정상 동작 중**(99건
잔고 스냅샷, 99건 신호결정 — ENTER 24건 전부 RiskEngine 승인, 계좌 현금 5천만원 그대로 유지)이고,
ZN 13:01 패턴은 오늘도 재현 없음, 콜:풋 비율도 1:0.97 안정권을 유지했다. 부가로 이번 조사 중
`observation_loop.log`에 타임스탬프가 아예 없다는 것과 `cockpit.log`가 23일째 무회전 상태라는
두 가지 관측성 결함을 새로 확인했다.

---

## 1. 장전(07:30~09:00) — 이상점 없음

`premarket_startup.log` 07:30:00.70 "기동 전 잔존 프로세스 정리" → 창 제목/커맨드라인 매칭
둘 다 "없음"(정상, 7일 연속). Docker 데몬 준비 07:30:22.90(실행 07:30:07.08 대비 15.8초).
마이그레이션 001~015 전체 재적용 — 015(`account_balance_snapshots`, 어제 신규)도 하이퍼테이블
포함 정상 재확인. COCKPIT/관측 루프 07:30:27.68~27.73 기동. 개발 세션 재시작 없이 이 한 번의
정규 기동만으로 지금까지 이어지고 있다(07-28과 달리 재시작 이력 없음 — 지표 해석에 유리).

---

## 2. 장중(09:00~현재, 약 9분 경과) — 이상점

### 2-1. [핵심/신규] 계좌 잔고 폴러 추가로 스태거링 설계가 재붕괴

**배경**: 07-27/07-28 세션에서 옵션체인(t=0)·투자자수급(t=15초)·만기유동성(t=30초)·매크로(t=45초)
4개 REST 폴러의 스타트업 오프셋을 벌려 스케줄 밀림을 완화했고, 07-28 아침 재시작 직후 4사이클
동안 "REST수집 29~40초·타폴러동시호출 0건"으로 검증됐다(다만 07-28 보고서 §2-2가 이미 지적했듯
이 검증 자체가 재시작 직후 짧은 창에 근거해 과장됐었다).

**어제(07-28) 8차 세션 변경점**: 계좌 잔고 폴러(`poll_account_balance_cycle`, 실제
`rest_client.get_balance()` REST 호출 — DB 조회만 하는 `poll_signal_fusion_cycle`과 다름)를
`ACCOUNT_BALANCE_STARTUP_OFFSET_SECONDS=20.0`·`ACCOUNT_BALANCE_POLL_INTERVAL_SECONDS=60.0`으로
신규 추가(`mahdi/main.py`). 그 결과 **60초 주기 REST 폴러가 옵션체인(t=0)·투자자수급(t=15초)·
계좌잔고(t=20초) 3개로 늘었고, 전부 20초 폭 안에 몰려있다** — 기존 4폴러 스태거링 재설계 없이
새 폴러를 끼워넣은 형태.

**오늘 실측(07:30~09:09, 약 99분, 398사이클)**:

| 지표 | 07-28 "정상화" 검증 당시(재시작 직후 4사이클) | 07-24 하루 전체(기존 최악 기준) | 오늘(07-29, 약 99분) |
|---|---|---|---|
| REST수집 평균 | ~29~40초 | (미집계) | **41.8초** |
| REST수집 최대 | 40초대 | (미집계) | **133.19초** |
| 60초 초과 사이클 비율 | 0/4 | (미집계) | **53/398(13.3%)** |
| 스케줄 밀림 최대 | 0초 | 76.2초(07-23 기준) | **73.4초** |
| 타폴러동시호출추정 최대 | 0건 | (미집계) | **40건** |
| 백오프 최대 배율 | (미측정) | 2.25배 | **2.69배(역대 최고 경신)** |
| 백오프 확대/회복 건수 | (미측정) | 70건/280건(하루 전체) | **64건/253건(99분 만에)** |

99분(장전 90분+장중 9분) 만에 07-24 하루 전체(390분)치와 맞먹거나 넘어서는 백오프 이벤트가
발생했다는 것은, 스케줄 밀림 문제가 07-27/07-28에서 확정한 원인(공유 `_RateLimiter` 동시
접촉)이 이번에는 **원래 폴러들의 우연한 겹침이 아니라 새 폴러 추가로 인한 구조적 재악화**임을
시사한다. 다행히 `_MAX_INTERVAL_MULTIPLIER`(4.0배) 상한에는 아직 여유가 있고 EGW00201 비율도
0.36%(60/16,794)로 07-28(0.383%)과 비슷한 수준을 유지해 즉각적인 장애로 번지진 않았지만, 장이
길어질수록(오전장 내내, 장중반 유동성 몰릴 때) 07-28처럼 악화가 누적될 개연성이 높다.

### 2-2. [기능 검증 — 정상] 계좌 추적기·Signal Fusion·Risk Engine 라이브 배선, 오늘 첫 정규장 데이터로 정상 동작 확인

07-28 8차 세션에서 최초 기동한 파이프라인이 오늘 정규장에서도 이어서 정상 동작 중임을 DB로
직접 확인:
- `account_balance_snapshots`: 오늘 99건 적재(60초 간격과 일치), 현금예수금 5,000만원·손익 0
  그대로(ADVISORY 모드, 실주문 없음 — 예상대로).
- `signal_decisions`: 오늘 99건(ENTER 24건 / REJECT 75건), **ENTER 24건 전부
  `risk_gate_state.risk_engine.approved=true`**로 RiskEngine 게이트를 실제로 통과 — v6 §12
  "독립 거부권" 배선이 라이브에서 처음으로 정규장 데이터를 상대로 작동함을 확인.
- 계좌 잔고 폴러 자체의 REST 실패 로그는 오늘 0건(§2-1의 부하는 다른 폴러의 지연으로 나타났을
  뿐, 계좌 잔고 조회 자체가 실패하지는 않음).

### 2-3. [양호] ZN 13:01 패턴 — 오늘도 재현 없음(2일 연속)

KIS ZN 조회는 여전히 매 사이클 EGW00552(CBOT 미승인, 기지 이슈)로 실패하지만, **yfinance
폴백 실패는 오늘 0건** — 07-28에 구현한 `interval="1m"` 제거 fix가 이틀째 안정적으로 유지되고
있다. 다만 09:09 현재까지는 아직 13:01 시간대 자체에 도달하지 않았으므로, NEXT_TODO가 요구하는
"다음 온전한 거래일 13:01 재현 여부" 최종 판정은 오늘 오후 장중 재확인이 필요하다(오늘은 재시작
없는 "온전한" 하루라 판정 조건 자체는 충족).

### 2-4. [양호] 콜:풋 비율 — 안정권 유지

오늘 REST 요청 기준 콜:풋 7,575:7,325(≈1:0.967) — 07-28의 1:0.97과 거의 동일한 균형 유지.

### 2-5. [신규 발견/관측성 결함] `observation_loop.log`에 타임스탬프 없음

`mahdi/main.py:1657-1659`의 `logging.basicConfig(level=logging.INFO, handlers=[...])` 호출에
`format=` 인자가 없어 기본 포맷(`%(levelname)s:%(name)s:%(message)s`)이 적용된다 — **로그 각
줄에 발생 시각이 전혀 남지 않는다.** 이번 조사에서 73.4초/49.5초 지연이 정확히 몇 시에
발생했는지 로그만으로 특정하지 못하고 파일 내 줄 위치(전체 대비 비율)로 추정해야 했다. DB의
`rate_limiter_status_log`도 히스토리 테이블이 아니라 단일행 UPSERT(스키마상 `id boolean not
null default true`)라 과거 시점 값을 되짚어볼 수 없다. 07-28 보고서(§5-2)가 이미 "핵심 계측
지표의 일별 누적치를 COCKPIT에 노출"을 제안했는데, 그 전제인 "시각 정보 자체가 로그에 있어야
한다"는 부분이 지금 빠져 있었다는 게 이번에 처음 드러났다.

### 2-6. [정정: 이미 완화책 있음] `cockpit.log` — 최초 조사에서 "무회전"으로 오판, 실제로는 아침 기동 시 10MB 임계 회전 존재

`cockpit.log`가 2026-07-06 최초 실행 이후 계속 누적(현재 1,871,680줄/5.2MB)되어 있는 것은
맞지만, **Fix 구현 단계 재조사 결과 `scripts/start_mahdi_premarket.bat:91-107`에 2026-07-20에
이미 추가된 "기동 시점 10MB 초과 확인 → `.1`로 이동" 로직이 있음을 확인** — 이 보고서 최초
작성 시 로그 파일만 보고 배치 스크립트를 확인하지 않아 "07-19 로그 위생 개선 대상에서
빠졌다"고 오판했다(정정). `observation_loop.log`의 `RotatingFileHandler`처럼 실시간 감지는
아니고 아침 1회 확인 + 단일 세대 백업이라는 차이는 있지만, 현재 성장 속도(23일 5.2MB ≈ 하루
230KB)로는 40일 이상 걸려야 10MB에 닿아 당장 위험하지 않다 — **코드 변경 없이 이번 조치에서
제외**. 조사 중 발견한 naive/aware datetime `TypeError`(`get_health_summary`→
`_option_chain_freshness_check`, 971건)는 **traceback의 줄 번호(149/132)가 현재 소스(167/150
— tzinfo 방어 코드 이미 존재)와 달라, 전부 그 방어 코드가 추가되기 전(2026-07-20 수정 이전)
시점의 과거 기록임을 확인** — 마지막 발생 줄 위치(1867211)가 오늘 기동 마커 위치(1871680)보다
한참 앞이라 오늘 새로 발생한 문제가 아니다.

---

## 3. 장후 — 해당 없음(진행 중)

보고 작성 시점이 09:09이라 아직 장마감 전이다. 15:45 이후 §2-1(스케줄 밀림 재발이 오후까지
누적되는지)과 §2-3(ZN 13:01 패턴) 최종 확인을 위한 후속 갱신이 필요하다.

---

## 4. Fix 작업 (우선순위 순) — 전부 구현 완료(2026-07-29 같은 세션)

| 순위 | 항목 | 상태 | 내용 |
|---|---|---|---|
| 1 | 계좌 잔고 폴러 스태거링 재조정 | ✅ 완료 | `ACCOUNT_BALANCE_POLL_INTERVAL_SECONDS` 60→300초, `ACCOUNT_BALANCE_STARTUP_OFFSET_SECONDS` 20→60초(`mahdi/main.py`) — 60초 REST 그룹에서 계좌 잔고를 빼내 옵션체인(0초)·투자자수급(15초) 2개만 남김. |
| 2 | `observation_loop.log`에 타임스탬프 추가 | ✅ 완료 | `_configure_logging()`에 `format="%(asctime)s %(levelname)s:%(name)s:%(message)s"` 추가(`mahdi/main.py`). |
| 3 | `rate_limiter_status_log` 히스토리화 | ✅ 완료 | 마이그레이션 `016_rate_limiter_status_history.sql`(라이브 DB 적용 완료) + `db.append_rate_limiter_status_history()`/`rate_limiter_status_history_since()` 신규 + `poll_option_chain`에서 매 사이클 함께 기록. |
| 4 | `cockpit.log` 회전 | ⏭️ 코드 변경 없음(§2-6 정정) | 재조사 결과 `scripts/start_mahdi_premarket.bat`에 이미 아침 기동 시 10MB 임계 회전이 있어 당장 불필요. |

**부가 구현(사용자 요청)**: COCKPIT에 "마흐디 판단 현황"(ADVISORY 진입 판단 카드 5장 + 최근
이력 표 — RiskEngine 승인/거부, 청산 단계는 ExecutionEngine 미배선이라 정직하게 "미배선" 표시)
+ "계좌 현황"(예탁자산/현금/평가·실현손익/일간·주간 수익률/최대낙폭 카드 7장) 패널을 "3초 룰"
로 최상단(점검 요약 배지 바로 아래)에 신규 추가. `mahdi/dashboard/panels/{decision_panel,
account_panel}.py` 신규, `mahdi/data/db.py`에 `recent_signal_decisions()` 신규,
`mahdi/dashboard/data_source.py`에 `get_latest_decision_context()`/`get_account_status_view()`
신규(둘 다 DB 실패 시 지어내지 않고 "데이터 없음"으로 폴백). 카드는 label/value/status/help
dict 리스트 패턴이라 향후 지표 추가는 리스트에 dict 하나만 붙이면 됨(`get_health_summary()`와
동일한 확장 패턴). 신규 테스트 24개 포함 전체 627개 테스트 통과, ruff 통과.

**미반영(사용자 승인 필요)**: 관측 루프(`mahdi.main`) 재시작 — 장중이라 스케줄/로깅 변경이
아직 실제 프로세스에 반영 안 됨. COCKPIT도 재시작해야 신규 패널이 브라우저에 보임.

---

## 5. 고도화 방안 (실행 점검 결과 기반)

1. **인프라 변경 시 "레이트리밋 공유 자원 영향도" 체크리스트화** — 오늘 회귀의 근본 원인은
   기능적으로는 옳은 작업(계좌 추적기 추가)이 스케줄링 설계 검토 없이 기존 스태거링 값에
   끼워넣어진 것이다. 앞으로 REST를 호출하는 폴러를 추가/변경할 때는 "현재 공유 `_RateLimiter`를
   쓰는 폴러 목록과 각 오프셋·주기"를 먼저 나열하고 재배치까지 끝낸 뒤 커밋하는 절차를 두면,
   같은 유형의 회귀(07-27→07-28→07-29 세 번째)를 예방할 수 있다.
2. **"재시작 직후 몇 사이클"을 fix 검증의 최종 근거로 삼지 않는다(07-28 보고서 교훈 재확인)** —
   07-28 보고서가 이미 지적한 문제인데, 07-28 아침 fix 검증도 결과적으로 짧은 창 근거였고
   그 다음날(오늘) 새 변경이 겹치며 지속 관찰 없이도 문제가 재현됐다. 최소 1~2시간 연속 관측
   (또는 새 폴러가 추가된 날은 특히) 후 결론 내리는 절차를 표준화할 것을 다시 제안한다.
3. **레이트리밋 공유 자원 자체의 구조적 해법 재검토 시점 도래** — 07-28 NEXT_TODO가 "페이서
   분리는 스태거링만으로 부족함이 하루치 데이터로 확인되면 재검토"라는 조건을 걸어뒀는데,
   오늘 100분 만에 07-24 하루치와 맞먹는 악화가 나왔다는 것은 그 조건에 근접했다는 신호다. 다만
   페이서 분리는 2026-07-08 500 폭주 사고 전례가 있으므로, 먼저 (a) KIS 실제 한도가 앱키당
   총합인지 TR ID/엔드포인트별인지 확인, (b) 계좌 잔고 폴러 주기를 늘리는 저위험 조치(Fix #1)의
   효과부터 먼저 재보고, 그래도 부족하면 이때 페이서 분리를 검토하는 단계적 접근을 권장한다.
4. **dev_memory 3종 파일이 07-28 8차 세션 이후 커밋되지 않은 채 워킹트리에 남아있음** — 코드
   변경(Phase 2 Risk Engine, 계좌 추적기 등)과 함께 커밋 여부를 사용자에게 확인할 필요가 있다
   (본 보고서 작성 중 발견한 부수 사항, 기능적 문제는 아님).

---

*이 보고서는 git 커밋(`25c0af9`까지), `docs/dev_memory/{NEXT_TODO,DECISION_LOG,SESSION_LOG}.md`
(07-28 8차 세션까지, 미커밋), 직전 보고서(`2026-07-28_마흐디_운영점검보고서.md`), 07-29자 로그
(`logs/{premarket_startup,observation_loop,cockpit}.log`) 전수 분석과 라이브 Docker DB
(`rate_limiter_status_log`/`signal_decisions`/`account_balance_snapshots`, DB `now()`=2026-07-29
00:08:51 UTC=09:08:51 KST 기준) 직접 조회 대조를 근거로 작성됨. 09:09 시점 스냅샷이라 15:45
장마감 이후 §2-1/§2-3 최종 확인을 위한 후속 갱신을 권장한다.*
