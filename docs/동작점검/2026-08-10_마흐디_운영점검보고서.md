# 마흐디 운영점검 보고서 — 2026-08-10 (월)

> 종일 완결(07:30:48~15:45, 494분) 기준. 자동 집계: `docs/동작점검/auto/2026-08-10_지표.{md,json}`
>
> **오늘은 인프라 점검보다 레짐 엔진 쪽이 압도적으로 크다.** §1~§3이 장전·장중 점검이고,
> §4부터가 HMM 재학습에서 나온 것이다.

---

## 요약 — 레짐 엔진을 처음 켜려다, 그것이 못 켜지고 있던 이유 넷을 찾았다

`feature_store` 8,217행으로 HMM 임계(8,000)에 도달해 25영업일 만에 재학습을 돌렸다.
**저장까지 성공했다. 그리고 검증에서 라이브 상수 출력이 나왔다.**

고치니 다음 결함이 나오고, 그것을 고치니 또 나왔다 — 결국 넷이다. 공통점 하나:
**24영업일간 WARMUP 폴백이었기에 전부 드러날 수 없었다.** 그중 가장 깊은 것은 모델이 아니라
**피처**였다 — ATM IV가 폴링 격자를 타고 매분 진동하고 있었고, HMM이 그 격자를 레짐으로 배웠다.

동시에 08-07에 미뤄 뒀던 질문 하나가 닫혔다: **KIS WS는 「유지 구독」에 아이들 타임아웃을 걸지
않는다.** 리플레이로 못 잡던 그 하나다.

---

## §1. 장전 검증 창(07:30~09:00) — 3항목 전부 판정

커밋 `04077ee`가 "리플레이로 못 잡는 것 하나"로 남긴 항목이 오늘 닫혔다.

| 항목 | 판정 | 근거 |
|---|---|---|
| **KIS WS가 「유지 구독」에 계속 데이터를 보내는가** | **보낸다 — 서버측 아이들 타임아웃 없음** | `BAFBRW990` 08:46~15:14 **367행**, `CAFBRW980` 365행, `BAFBRW985` 327행. 스팟이 990→975로 내려가는 동안 ATM 창 밖에 있었는데도 봉이 안 끊겼다. `market_raw_1m` **4,405행 / 44심볼**(08-07 1,073행 / 20심볼) |
| `retention_pool` 경로 실주행 | 확인 | 07:30:51 22구독 확립 → 08:45 첫 롤. 끊긴 구독 67건 / (롤 73회 × 북 2) = **롤·북당 0.46건**(풀 없을 때 2.42) |
| 마이그레이션 028 멱등 재적용 | 확인 | 07:30:26 |

`2026-08-07-e1`의 `market_raw_1m.rows` 예측을 **내릴 필요가 없어졌다.** 이득은 "WS 트래픽
감소뿐"이 아니라 봉 연속성 그 자체다.

> NEXT_TODO의 *"07:31 첫 롤에서 해제 로그가 안 나오는 것이 정상 신호"* 는 **검증 불가**였다 —
> 07:31에 롤 자체가 없었고 첫 롤은 08:45다. 그 문장은 판정 규칙으로 못 쓴다.

## §2. 인프라 — 조용하다

밀림 0건, 결손 0분(§3-1 참고), 백오프 최대 1.50배, 비200 20건(0.21%), 페이서 용량 33.8%,
프로세스 재시작 0회. 장마감 자동 종료·집계 정상(15:45).

`options_flow.structural_minutes` **110분**(장전 89 + 현물마감 21) — 08-07 Fix#2의 예측
구간(89 초과 ~ 140 미만)에 정확히 들어왔다. **새 경로가 실제로 탔다.**

## §3. 이상점

### 3-1. 로그축 결손 지표가 사이클을 한 분 앞으로 귀속한다 (A급 — 계측이 거짓말)

리포트 §4가 「인프라 결손 1분 = 11:09」를 냈는데 **허위**다. 로그에는 `분=11:09` 라벨이 붙은
사이클이 **있다**.

[log_metrics.py:803](../../mahdi/ops/log_metrics.py#L803)이 `seen = {_hhmm(c["start"])}`로
**파생 start**(종료시각 − 소요 합)를 쓴다. 11:09 사이클은 소요 33.45초에 11:09:33.446 종료 →
파생 시작 **11:08:59.996** → `11:08`로 귀속. 11:08 사이클도 `11:08`이라 11:09가 결손으로 남는다.

08-07 Fix#3이 `분=` 라벨이라는 진실 소스를 만들었는데 `_duplicate_poll_minutes()`만 그것을 쓰고
**결손 지표는 옛 축에 남아 있다.** 파급이 하나 더 있다 — §4가 이 불일치를 보고
*"두 축이 어긋난다 … `rows=0`으로 끝난 사이클은 로그 축에 안 잡힌다"* 는 **틀린 설명**을 냈다.
원인은 DB 쪽이 아니라 파서 쪽이다.

**미수정 — 다음 세션 fix 대상.**

### 3-2. REJECT 대조군 격차 전 지평 음수 — 이틀 연속 (B급)

| 지평 | 08-07 | 08-10(종일) |
|---|---|---|
| 5m | −1.4pt | **−7.6pt** |
| 15m | −2.5pt | **−13.8pt** |
| 30m | −11.3pt | **−9.3pt** |

6개 셀(3지평 × 2일) **전부 음수** — 우리가 거른 판단이 더 잘 맞혔다. 15m는 z=−2.63(p=0.0086).

다만 커밋 `53cc63e` §7-4가 적어둔 경계를 지킨다: 지평 셋은 강하게 상관하므로 실질 독립 시행은
하루 1회에 가깝고 **2/2 음수의 우연 확률은 25%** 다. **아직 결론이 아니다.** `shared_hours` 6개로
비교 성립 조건은 충족.

### 3-3. `flow_position ↔ options_flow` 19.2% — 이틀 연속 20% 밑 (B급)

08-06 74.6% → 08-07 13.1% → 08-10 **19.2%**(+6.1pt). 고도화#B의 판정축이 "전일 대비 변화폭"이고
*"흔들린다는 것이 앙상블이 살아 있다는 증거"* 였는데 **오늘은 안 흔들렸다.** 멤버 원값도 상시
반대다(`options_flow` 평균 −0.6423 / `flow_position` +0.7870).
**3영업일째 20% 밑이면 `signal_layer` 부호 규약을 본다.**

### 3-4. KIS 지연 — 사전대응 규칙 발동 조건 성립, 그러나 발동 안 함 (C급)

`inquire-price` p95 2.5초 초과가 10~15시 6구간, **이틀 연속 같은 시간대**. 08-07에
"변수를 하나만 바꾼다"는 원칙으로 08-11 재판단하기로 이미 결정했으므로 오늘 발동하지 않는다.
새 사실: 15시 `inquire-asking-price` p95 **6.50초**(08-07엔 없던 구간).

### 3-5. 해석 주의 — 이상 아님

- **옵션체인 REST 9,241 → 6,860은 fix 효과가 아니다.** 오늘은 **북이 3→2개**다(08-13이 먼슬리
  만기이자 목요일이라 위클리(목) 미상장). `2026-08-07-e1`의 대가 지표 "확인"을 유지 풀의 공로로
  읽으면 안 된다 — **규약 F가 잡으라고 만든 유형**이다.
- 감마플립 0% — 두 북 모두 광폭 '없음'. 시장 구조이고 정상.
- ATM 롤 로그가 이벤트당 3줄인데 북은 2개 — 위클리(목) 매니저가 먼슬리와 같은 만기를 잡아
  심볼이 겹친다. 카운터는 이벤트 단위라 정상.
- Flow Radar 옵션 패널이 **오늘 만기** 위클리(`BAFBRW980`)를 뽑는다. 화면과 판단이 다른 북을
  보지만 신호 경로 결함은 아니다 — `orderflow_ofi_vpin`의 OFI는 선물에서 온다.

---

## §4. HMM 재학습 — 결함 넷

상세 근거는 `docs/dev_memory/DECISION_LOG.md` 2026-08-10, 격리 기록은
`data/models_quarantine/README.md`(gitignore라 이 저장소엔 안 올라간다).

### 4-1. `startprob_` one-hot 붕괴 (R1 + R3)

첫 산출물이 라이브에서 **전 이력 8,241분을 단일 상태**로 판정했다.

    startprob_ = [0, 0, 0, 0, 0, 1.0, 0, 0]   # 7개가 부동소수 정확히 0.0

- **R1** `fit()`이 `lengths=` 없이 25세션을 한 시퀀스로 학습 → startprob 표본이 1개 → 붕괴.
- **R3** `step()`이 **길이 1**을 넘김 → 길이 1의 사후확률은 `normalize(startprob ⊙ emission)`이라
  **전이행렬이 통째로 안 쓰임** → startprob이 답을 지배.

`predict()`의 docstring은 처음부터 *"최근 윈도우 (n,6)"* 를 받도록 적혀 있었고 내부도 전방
필터링이다 — **설계는 맞았고 호출부가 계약을 어겼다.**

### 4-2. 가드 둘이 둘 다 다른 것을 재고 있었다 (R4)

- **방문 상태 검사**: 배치 Viterbi로 "8/8 방문" → 라이브 축으로 재면 **1/8**. 규약 F의 코드판.
- **수렴 가드**(`monitor_.converged`): hmmlearn 0.3.3 구현상 `iter == n_iter`(비수렴)와
  **음수 delta**(발산)에서 정확히 True. 그날 발산 후보 둘이 restart **패자**였기에 저장물이
  우연히 멀쩡했다.

재구축한 3겹이 각자 무엇을 지키는지 **음성 대조로 확인**했다 — 붕괴 모델은 `check_startprob`이
잡고, R3 회귀(`window=1`)는 `check_live_diversity`가 잡는다. **어느 하나로 다른 쪽을 대신할 수
없다.**

### 4-3. ATM IV가 폴링 격자를 타고 있었다 (R6) — **가장 깊다**

R1·R3 수정 후 재학습본은 게이트 3겹을 **전부 통과**했다. 그런데 `RegimeStateMachine`에 직접 태워
08-10을 재생하니 **분당 99.2%가 레짐 전이**였고 `stability_flag`는 한 번도 안 걸렸다 — 모델이
매분 **확률 1.000으로 확신하며** 뒤집혔기 때문이다.

원인은 피처였다. `_update_atm_iv()`가 **모든 북의 IV를 평균**하는데 위클리는 격분에만 조회된다:

| | 북 구성 | 평균 IV |
|---|---|---|
| 짝수분 | 위클리(08-10) + 먼슬리(08-13) | 0.5285 |
| 홀수분 | 먼슬리 단독 | 0.7387 |

`iv_chg` 차분 lag1 자기상관 **−0.900**(완벽한 구형파). HMM이
**"짝수분=TREND_UP_STRONG / 홀수분=VOL_COMPRESSION"을 94% 일치**로 재현했다.
**레짐이 시장이 아니라 폴링 격자를 따라갔다.**

`_books_due_this_cycle()` docstring은 이미 *"먼슬리를 먼저 조회해 ATM IV가 항상 주력 북 기준으로
잡히도록"* 이라고 적고 있었다 — **의도는 맞았고 구현이 안 지켰다.** 2026-08-06 Fix#4가 체인 나이
지표에서 고친 것과 **같은 계열**이고 이 함수만 빠져 있었다.

### 4-4. `fit()`이 발산한 후보를 승자로 뽑았다 (R7)

재계산 피처로 학습하니 승자가 **delta=−0.32로 발산한 후보**였다(점수만 보고 골랐다).
게이트가 런 전체를 거부했다 — **멀쩡한 후보가 9개 있었는데도.**

---

## §5. 결정 — 오염된 이력을 어떻게 할 것인가

R6은 앞으로 쌓일 값만 고친다. 기존 8,223행은 그대로 학습하면 같은 것을 배운다.
**(b) 학습 시점 재계산**을 골랐다(사용자 결정).

**DB는 고치지 않는다.** `reconstruct_iv_chg()`가 원본 체인에서 읽기만 하고
`build_feature_matrix()`가 **학습 입력에서만** 갈아 끼운다(대체율 7,832/8,242행, 95.0%).

증거: 08-10 DB의 `iv_chg`는 **지금도 구형파**다(짝수분 −0.26420 / 홀수분 +0.30419).

대체 사실은 **세 곳**에 남는다 — 학습 로그 / 모델 pickle의 `metadata.iv_chg_source` /
운영 리포트 §13. pickle에 넣은 이유: 로그는 지워지지만 모델 파일은 남는다.

---

## §6. 배포 판정 — 게이트 통과가 승인이 아니다

게이트 3겹 통과 모델이 99.2% 채터링이었던 것이 §4-3이다. 저장 뒤 `RegimeStateMachine`에
**직접 태워** 5영업일을 재생하는 것을 판정 조건으로 삼았다.

| 날짜 | 예측분 | 전이 | 전이율 | 방문 |
|---|---|---|---|---|
| 08-04 | 381 | 3 | 0.8% | VOL_EXPANSION 374 / RANGE_BREAK_PREP 7 |
| 08-05 | 380 | 6 | 1.6% | VOL_EXPANSION 359 / RANGE_BREAK_PREP 21 |
| 08-06 | 361 | 18 | 5.0% | VOL_EXPANSION 332 / RANGE_BREAK_PREP 29 |
| 08-07 | 381 | 2 | 0.5% | VOL_COMPRESSION 371 / RANGE_BREAK_PREP 10 |
| 08-10 | 381 | 2 | 0.5% | VOL_COMPRESSION 380 / RANGE_BREAK_PREP 1 |

99.2% → **0.5~5.0%**. 08-06/08-07 사이에 지배 상태가 바뀌는 것이 **날짜 축의 실제 레짐 전환**이다.

**배포함** — `data/models/regime_engine.pkl`(16:56). 08-11 07:30 기동부터 WARMUP 폴백이 끝난다.

> **재생 검사를 게이트로 승격하지 않았다.** 무엇이 정상 전이율인지 아직 모른다(5영업일치가
> 전부). 정상 범위를 모르는 상태에서 임계를 먼저 정하면 그 임계가 곧 결론이 된다.

---

## §7. 구현 결과

| Fix | 파일 | 내용 |
|---|---|---|
| R1 | `engines/regime.py` | `fit(features, lengths)` — 세션별 학습. 합 불일치는 ValueError |
| R2 | `scripts/fit_regime_engine.py` | `build_feature_matrix` → `(X, lengths)`. **필터 생존 행만** 날짜로 묶는다 |
| R3 | `engines/regime_pipeline.py` | `step()`이 세션 누적 창(`maxlen=120`)을 넘긴다 |
| R4 | `scripts/fit_regime_engine.py` | 저장 게이트 3겹. 리플레이는 `regime_pipeline`에 둬 라이브와 축 동기화 |
| R5 | `engines/regime.py` | `predict()` 길이 1 경고(인스턴스당 1회) |
| R6 | `main.py` + `options_intel.py` | ATM IV를 먼슬리 단독으로. 라이브·재계산이 **같은 함수** |
| R7 | `engines/regime.py` | `fit()`이 **수렴한 후보 중** 최고점을 고른다 |
| (b) | `db.py` + `fit_regime_engine.py` + `ops/` | `iv_chg` 학습 시점 재계산 + 3중 명시 |

테스트 **1,271개 통과**(신규 43개). 린트 제 변경분 0건. 12파일 / +967 −89.

핵심은 **길이 1로 되돌아가면 즉사하는** 회귀 테스트다.

---

## §8. 다음 (NEXT_TODO에 이관)

1. **08-11 검증** — `2026-08-10-r1r3` / `r6` / `r4r7` 예측치 대조.
2. **§3-1 로그축 결손 지표 fix** — `분=` 라벨 축으로 이전.
3. **확신도 분모 레버는 08-12 이후** — 기준일이 08-10에서 08-11로 밀렸다.
4. **`iv_chg`는 08-11을 경계로 모집단이 다르다** — 경계를 넘어 시계열 비교 금지.
5. `2026-08-07-e4`는 **`inconclusive`로 닫았다** — 소급 수정 없이 새 게이트를 `r4r7`로 등재.
