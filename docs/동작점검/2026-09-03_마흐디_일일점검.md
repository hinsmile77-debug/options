# 마흐디 일일점검 — 2026-09-03 (목)

- 국면: **장전(pre)** · 예정 08:00 · 실행 08:01 KST — **정시 실행** (밀린 실행 아님)
- 증거: `docs/동작점검/auto/2026-09-03_증거_pre.md`
- HEAD `910c1e1` · 브랜치 `master` · 미커밋 2건(`.claude/`, `docs/Dev_md/dashboard_build_prompt_1.md`, 둘 다 이월 중인 미결정 사항)
- 이 파일은 오늘 하루 한 파일이다. 장중 12:30·14:30, 장후가 이 파일 끝에 이어 붙인다. 앞 절 본문은 뒤 국면이 고치지 않는다.

## §0. 오늘 한 장

오늘 프로그램은 지금까지(07:30~08:01) 정상 작동했다. 오류 0건, 관측 공백 0건, 예기치 않은 재기동 0회다. **사람이 지금 할 일은 없다** — 개장(09:00) 전에 급히 손댈 것이 없다는 뜻이고, 오늘 안에 사람이 정할 것들은 뒤쪽 「사용자 조치」에 모아 뒀다.

문제는 5건 찾았고 전부 심각(P0)은 아니다 — 보통(P1) 1건 · 경미(P2) 4건. 그중 새로 생긴 것은 1건뿐이고(발동일 기록이 안 남은 레버가 둘에서 셋으로 늘었다), 나머지 4건은 어제·그제부터 이어지는 만성 항목이다.

오늘 특별히 볼 것 하나 — 어젯밤(09-02 저녁) 프로그램이 판단하는 시점을 「그 분의 10초」에서 「그 분의 55초」로 늦추는 손잡이(Fix#10)를 켰다. 오늘이 그 상태로 도는 **첫 실거래일**이다. 늦게 판단할수록 더 신선한 시세를 보고 판단하게 되는데, 실제로 그렇게 되는지는 오늘 장중에 확인해야 한다.

> **[장중① 12:30 갱신]** 지금까지(07:30~12:34) 프로그램은 정상 작동했다 — 오류 0건, 감시 프로세스(워치독) 무기록 구간 0건, 프로그램 이상종료(크래시) 0건. **사람이 지금 급히 할 일은 없다.** 다만 오늘 아침에 미뤄 둔 결정 두 가지(혼잡한 시간대 하나를 더 늦추게 바꿀지, 어제 켠 손잡이 하나를 최종 정리할지)는 아직 그대로 남아 있다. 이번 회차가 새로 찾은 것은 1건인데, 이것은 프로그램의 문제가 아니라 **자동 점검 도구가 "이상 없음"을 "이상 있음"으로 잘못 읽은 것**이었다(바로잡아 뒀다, 1-6).

> ⚠ **[장중② 14:40 갱신 — P0]** 14:31분부터 지금(회차 실행 시각 14:40, 확인 시점 14:45까지도 계속)까지, 옵션 시세를 받아오는 통로가 증권사(KIS) 쪽에서 통째로 막혀 있다 — **14분 연속으로 "이번 분의 옵션 시세"가 한 줄도 안 들어왔다.** 감시 프로세스(워치독)도 14:40부터 "이상(DEGRADED)" 경보를 계속 내고 있다. 프로그램 자체는 죽지 않았고, 직전에 받아 둔 시세를 계속 참고해 판단을 이어가고 있지만 **그 시세가 갈수록 낡아간다.** 같은 증상이 올해 들어 이번이 **네 번째**이고(08-14·08-26·09-01·오늘), 앞의 세 번 모두 "우리 쪽이 손대지 않아도 장 마감 직후 저절로 풀렸다" — 그래서 **이번에도 사람이 지금 코드로 손댈 것은 없다.** 새 진입 마감(14:50)까지 남은 시간이 5분 안팎이라 사람이 당장 결정할 것도 사실상 없다(상세 근거와 결론은 아래 1-7, 맨 끝 「사용자 조치」 참고). 이번 회차가 새로 찾은 문제 2건(1-7 심각 · 1-8 보통).

> ✅ **[장후 15:45 갱신 — 오늘 하루 마무리]** 14:31분부터 막혀 있던 옵션 시세 통로는 **15:25~15:26 사이(감시 프로세스 기준 45분 만에, "옵션체인이 통째로 비었다"는 뜻의 rows=0 기준으로는 53분 만에)** 사람 손을 안 대고 저절로 풀렸다 — 위 갱신에서 예상한 그대로다. 오늘 프로그램은 매매 주문을 내는 경로가 아직 연결돼 있지 않은 "지켜보기 전용" 상태였으므로, 이 소동으로 인한 **실제 손실은 0원**이다(오늘 하루 산 것도 판 것도 없다, 아래 「종가 기준 최종 손익」 참고). **사람이 지금 할 일은 없다.** 오늘 하루 전체로 보면 문제 10건을 찾았고 심각(P0)은 1건(옵션 시세 통로 막힘, 지금은 해소됨) · 보통(P1) 2건 · 경미(P2) 7건이다. 그중 오늘 새로 찾은 것은 4건(옵션 시세 통로 막힘과 그 부산물 2건, 그리고 자동 점검 도구의 착각 1건)이고 나머지 6건은 어제·그제부터 이어지는 만성 항목이다. 오늘 특별히 볼 것 하나 더 — 어젯밤 켠 "판단을 45초 늦춰서 더 신선한 시세를 보게 하는" 손잡이(Fix#10)는 오늘 하루로는 **판정을 유보한다**: 신선도 자체는 목표(20% 이내)를 크게 통과했는데(0.8%), "얼마나 최신인가"를 초 단위로 잰 값 하나만 기준(55초)을 2.4초 넘겼고, 그 값이 오늘의 시세 통로 소동 때문에 부풀었을 가능성이 있어서다 — 소동이 없는 날 다시 재 봐야 한다(아래 1-1 판정 참고).

## 제1부 — 장전 (07:30 ~ 09:00)

### 1-0. 전일(09-02) 이월 — 「사용자 조치」 10건 · 「종합 판정표」 8행 · 「통합 대장」 12건 전부 처분

09-02 파일은 장후 자동조치(제6부, 16:26~17:0x)가 그날 저녁 안에 Fix 6건을 실제로 구현하고 커밋까지 마쳤다(`5e94525`~`910c1e1`). 그 결과를 반영해 처분한다.

| 09-02 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 (09-01 반영 미적용) | ✅ 해소 | 09-02 저녁 제6부가 Fix 4건 중 3건은 이미 반영돼 있었음을 확인했고 나머지도 커밋으로 닫았다 |
| 1-2 (이벤트 캘린더 만료, 만성) | 🔄 지속 — 오늘도 07:30:56 같은 경고 재발 | 아래 1-2 |
| 1-3 (레버 발동일 기록 미정리, 만성) | 🔄 지속·**확대** — 대상이 2개→3개로 늘었다 | 아래 1-3 |
| 1-4 (ZN/ES 조회 실패, 만성) | 🔄 지속 | 아래 1-4 |
| 1-5 (가설 판정 지연) | 🔄 지속·**증가** — 9건→13건(도래 4건 추가) | 아래 1-5 |
| 1-6 (레버 E 켜진 채 기동 확인) | ✅ 판정 완료(09-02 §1-10에서 이미 공식 판정) — 오늘은 후속 결정만 남음 | 사용자 조치 참고 |
| 1-7 (사전 대응 규칙 09시 공백) | 🔄 지속 — **오늘이 결정 예정일** | 사용자 조치 1번 |
| 1-8 (앙상블 비영 멤버 2/6 하락) | ➡️ 이월 — 장전은 09:00 전이라 앙상블이 아직 안 돈다. 장중이 판정 | 아래 「오늘 관측이 답해야 할 것」 |
| 1-9 (p95 꼬리지연 위험선 상시 도달) | ➡️ 이월 — 같은 이유로 장중이 판정. 오늘 07~08시 창은 정상(§5-1) | 〃 |
| 1-10 (레버 E 공식 판정) | ✅ 판정 완료 — 사람 확정 대기 | 사용자 조치 2번 |
| 1-11 (stale_pct 지표 설계 문제) | 🔄 지속 — 사람 결정 대기(P1-5는 「사람이 먼저 무엇을 잴지 정한 뒤 착수」로 못 박혀 있음) | 사용자 조치 2번 |
| 1-12 (마감 후 비영 멤버 연쇄 이탈) | ➡️ 이월 — 09-02 §제6부 6번이 중간조사(74분이 "값 0"이지 "무입력"이 아님)까지 마쳤고, P1-3(오늘부터 0점축 이름이 로그에 남는다) 실측을 기다리는 중 | 〃 |

### 장전 체크리스트 (`phases.md` A절)

- **A-1 기동** — 7항목 전부 확인됨. 07:30:00.85 기동 시작 → 07:30:45.51 관측 루프 정상 기동(표식 1회, 재기동 없음) → 07:31:01.42 COCKPIT 기동. Docker 데몬 준비 23초, 마이그레이션 33건 전부 성공. 워치독은 07:30~08:01 사이 3행(전부 OK), 마지막 박동이 감시 창 끝까지 1분 이내다.
- **A-2 오늘의 계획**
  - 검증예정일이 **오늘(09-03) 도래**한 pending 가설 4건: Fix#10(위상오프셋 55초) · 「판단 형태 전이 0점축 이름」 · 「§12 적신호 연속 창 수」 · 「신선도 연속축」. 넷 다 09-02 장후가 어제 구현·등재했다.
  - 검증예정일이 **지난** pending 가설 9건 — 아래 1-5.
  - `NEXT_TODO.md`의 「켤 것」 레버 확인 — 오늘 관련 항목 없음(08-20 하루-한-파일 전환 관련 「켤 것」은 그 전환 자체가 이미 08-21부터 실효 중이라 해당 없음).
  - 전일 커밋(`910c1e1`, 09-02 17:21:06)이 오늘 관측 루프 기동(07:30:45)보다 **앞선다** — 오늘 프로세스에 실려 있다. 문제 없음.
  - **9월 초 사전 대응 규칙 조건**: 09-02 지표에 새 성립 기록 없음(확인 대상 아님 — 레버 E가 이미 켜진 채 돌고 있어 이 규칙과 같은 손잡이가 닫힌 상태다, 09-02 §1-6 결정 참고).
  - `--verify-report`로 09-02 파일을 통과시켰다: 폐기 목록과 부딪히는 줄 없음. 파일 경로 미확인 2건(98행 `mahdi/ops/alerting.py`·`tools/collect_evidence.py`)은 09-02 자신이 이미 오탐으로 판정해 둔 것과 **동일한 두 줄**이다(09-01 보고서에서도 같은 오탐이 났었다) — 반복되는 오탐 패턴으로 확인, 새 이상점 아님.
- **A-3 전일 이월** — 위 1-0에서 처리 완료. 09-02 `_지표.json`(61.0KB, 09-02 15:46) 존재 — 전일 델타 복구 가능.
- **장전 판정 문구** — 규약대로 관측 범위를 명시한다: **개장 전 사람 조치: 불필요** (관측 범위 07:30~08:01). 범위 밖(08:01~09:00, 그리고 정규장)은 아직 모른다.

---

## 1-1. 확인(정상) — Fix#10(판단 위상 10초→55초)이 오늘 첫 실거래일로 가동한다 — **P2(모니터링 대상)**

**증상 아님 — 확인 사항.** `mahdi/main.py:923`의 `SIGNAL_FUSION_PHASE_OFFSET_SECONDS = 55.0`이 09-02 17:21:06 커밋(`910c1e1`)으로 켜졌고, 그 값이 실린 채로 오늘 07:30:45에 프로세스가 새로 떴다.

**근거.** 커밋 시각(09-02 17:21:06)이 관측 루프 기동(09-03 07:30:45)보다 앞선다 — 장 마감 후 프로세스가 안 떠 있을 때 값만 바꾸고 다음 자연 기동에 실은 것으로, 09-02 §1-10과 같은 처리 방식이다. 코드 주석(`main.py:918~922`)에 이 값에 대한 「주장」·「대가」 지표 넷이 이미 등재돼 있다.

**기준.** `hypotheses.yaml`의 `2026-09-02-fix10-phase-offset-55s`, 검증예정일 오늘(09-03), 전제레버 `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`.

**영향.** 지금까지는 판단이 「그 분이 시작한 지 10초 뒤」의 시세를 봤는데, 오늘부터는 「55초 뒤」의 시세를 본다 — 판단이 그만큼 더 최신 정보를 보게 된다는 뜻이다. 다만 판단 시각 자체가 45초 늦어지므로, 신규 진입 컷오프(14:50) 근처에서 한 분이 통째로 밀려 넘어가는지는 오늘 실측으로 봐야 한다(→ 아래 「오늘 관측이 답해야 할 것」 1번).

## 1-2. 만성·지속 — 이벤트 일정표가 여전히 만료된 채다 · 오늘도 경고 발화 🔄 P2

**증상.** 07:30:56 `이벤트 캘린더 미기입: not_covered — covered_through(2026-08-28)가 오늘(2026-09-03)보다 앞선다`.

**근거.** `mahdi/config/event_calendar.yaml:70` `covered_through: "2026-08-28"` — 09-02 지적 시점보다 하루 더 오래됐다(6거래일째).

**기준.** 09-02 §1-2와 동일 만성 항목. 새 발견 아님.

**영향.** 9월 금리 발표·CPI·고용지표·옵션 만기(09-03 오늘·09-10) 같은 이벤트가 임박해도 프로그램이 그 사실을 모른 채 평소와 똑같이 베팅한다.

## 1-3. 만성·지속·확대 — 발동 완료됐지만 코드/가설 어느 한쪽에 실제 발동일이 안 남은 레버가 2개에서 **3개**로 늘었다 🔄 P2

**증상.** 증거 §7 「발동은 끝났는데 날짜가 안 옮겨진 레버」 목록에 다음 셋이 함께 뜬다.

| 레버 | 코드에 발동일 주석 있는가 | `hypotheses.yaml` 무조건발동일 정리됐는가 |
|---|---|---|
| `use_effective_member_count` | ✅ 있음(`strategy_params.yaml:87` "실제로 켠 날: 2026-08-23") | ❌ 여전히 2026-08-23(11일 지남) — 필드값 자체는 맞지만 「지남 ⚠」으로 계속 뜬다 |
| `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` | ✅ 있음(`main.py:258~264` "2026-08-31 저녁 — 켰다") | ❌ 09-08-31(3일 지남) — 위와 같은 이유 |
| `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`(Fix#10) | ❌ **없음** — `main.py:923` 주변에 "실제로 켠 날" 주석이 아직 없다 | ❌ 2026-09-02(1일 지남) |

**근거.** 위 표는 증거 §7 원문 인용. `main.py:918~923`을 직접 읽어 Fix#10만 주석이 비어 있음을 확인.

**기준.** `docs/동작점검/README.md` — 발동일이 안 박히면 그 레버의 유예가 조용히 성립한다는 지적(08-19 이후 여러 번 반복). 09-02 §1-3과 같은 항목이나, 대상이 하나 늘었다는 점에서 확대다.

**영향.** 이 자체는 오늘 거래에 영향이 없다(값은 이미 맞게 켜져 있다). 다만 「지남 ⚠」이 계속 뜨는 채로 두면, 다음에 정말 안 켜진 레버가 생겨도 같은 문구에 묻혀 사람이 놓칠 수 있다 — 이미 08-12에 그 실수가 났었다.

## 1-4. 만성·지속 — ZN/ES 해외선물 근월물 조회 실패 🔄 P2

**증상.** 07:32:35 ZN(미국 10년 국채선물) 근월물 조회 실패(`CBOT SUB거래소 신청 계좌가 아닙니다`), 07:32:40 ES(E-mini S&P500) 근월물 조회 동일 실패.

**근거.** 증거 §4 WARNING 목록. 09-02 §1-4와 완전히 동일한 문구·시각대.

**기준.** 만성 확정 항목(거래소 이용 신청 미완료).

**영향.** 매크로(교차자산 스트레스) 축 계산에서 미국 국채·S&P500 선물 근월물 데이터가 빠진 채로 오늘도 판단이 이뤄진다.

## 1-5. 지속·악화 — 가설 판정 지연이 9건에서 **13건**으로 늘었다 · 최장 지연 7거래일 🔄 P2

**증상.** 검증예정일이 지난 pending 가설이 9건(최장 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`, 7일 지남), 오늘 새로 도래한 pending 가설이 4건 — 합쳐서 오늘 사람이 봐야 할 미확정 가설이 **13건**이다.

**근거.** 증거 §8 표 전체(13행). §12 적신호 3번 "검증예정일이 지난 pending 가설 9건". `hypotheses.yaml` 최종 수정 09-02(mtime) — 09-02 저녁 이후 아직 한 줄도 안 바뀌었다(장 시작 전이라 당연하지만, 09-02가 낸 「가설 판정 제안 15건」 중 6건만 확정되고 9건이 사람 확정을 기다리는 채로 하루를 넘겼다).

**기준.** 09-02 §1-5(당시 9거래일 지연 1건 + 전체 지연 이슈)와 같은 계열. 규모가 늘었다는 점에서 지속이 아니라 악화로 기록한다.

**영향.** 판정 대기가 쌓일수록 「이 레버가 진짜 효과가 있었는지」에 대한 답이 계속 미뤄진다. 지금 당장 거래에 위험한 것은 아니지만, 다음에 비슷한 레버를 켤지 말지 판단할 때 참고할 과거 판례가 갈수록 부실해진다.

---

## 오늘 관측이 답해야 할 것 — 장중·장후가 닫아야 할 목록

1. **Fix#10(위상 55초) 실측** — 판단이 실제로 「그 분에 적재된 체인」을 보는지, GEX 계산 신선도(`chain_newest_age_seconds.p50`)가 55초 이하로 나오는지. 컷오프(14:50) 근처에서 한 분이 통째로 밀려 넘어가는 사건이 있는지(→ 1-1).
2. **09시 혼잡시간대 포함 여부 결정** — 09-02가 오늘 장전으로 미룬 결정(1-7). 아래 「사용자 조치」 1번에서 선택지를 낸다.
3. **09:00 개장 이후 앙상블 상태** — `flow_position` 외 다른 축들이 정상 합류하는지, 09-02 장중② 이후 이어진 비영 멤버 하락(1-8·1-9·1-12)이 오늘도 재현되는지.
4. **p95 꼬리지연 위험선 도달 여부** — 07~08시 창은 정상(§5-1)이었으나 09-02는 13:30 이후 상시 위험선 위였다. 같은 패턴이 재현되는지.
5. **레버 E 관련 사람 결정 여부** — 오늘 중 확정되면 그 결과를 이월 처분표에 반영.

## Fix 구현계획 (초안) — 적용은 전부 장후 이후, 사람 검토 후

> 장전은 코드를 바꾸지 않는다. 아래는 계획만.

### P2-1 (이월, 1-3). 레버 3종의 발동일 기록을 한 번에 정리한다
- 대상: `hypotheses.yaml`의 `use_effective_member_count`(2726행 무조건발동일) · `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`(3322행) · `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`(7031행) 세 항목의 `무조건발동일`을 실제 발동일로 옮기고 사유를 주석으로 남긴다.
- 추가로 `mahdi/main.py:923` 위에 `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`용 "실제로 켠 날: 2026-09-02(커밋 910c1e1)" 주석을 다른 두 레버와 같은 형식으로 추가한다(현재 유일하게 이 레버만 코드 쪽 주석이 없다).
- 회귀 위험: 낮음(주석·yaml 값만). 검증: 다음 증거 수집기 실행에서 §7 "날짜 정리 필요" 목록에서 셋 다 빠지는가.

### P2-2 (이월, 09-02 사용자 조치 3번). 이벤트 캘린더 `covered_through`를 9월분으로 갱신
- 대상: `mahdi/config/event_calendar.yaml`. 9월 금통위·FOMC·CPI·고용지표·옵션 만기(09-03·09-10)를 실제로 조사해 채운 뒤 `covered_through`를 그 마지막 날짜로.
- 이 항목은 코드 변경이 아니라 사람의 사실 확인이 선행돼야 한다 — 자동 회차가 만기일 이상은 채울 수 없다.

### P1-2 (이월, 1-7). `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`에 09시 포함 여부
- 대상: `mahdi/main.py:268`. 안 A 채택 시 `{9: 4, 10: 4, 11: 4, 12: 4, 13: 4, 14: 4}`.
- **오늘 결정하더라도 오늘 적용은 불가능하다** — 이 값은 프로세스 기동 시점에 읽히고(07:30:45에 이미 기동됨) 장중 재기동은 금지돼 있다. 결정은 다음 07:30 기동(09-04)부터 실린다. 아래 사용자 조치 1번 참고.

---

## 고도화 방안 (초안) — 오늘 관측에서 나온 것만

### 고도화 1. "발동일 정리" 체크리스트를 레버 활성화 커밋 자체에 강제한다 (근거: 1-3)
- **관측 근거**: 같은 종류의 실수(발동일을 코드/yaml 어느 한쪽에 안 남기는 것)가 이제 세 번째 레버에서 재현됐다. `use_effective_member_count`와 `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`는 코드 주석은 챙겼지만 `hypotheses.yaml` 무조건발동일을 못 옮겼고, `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`는 코드 주석조차 아직 없다.
- **제안**: 레버를 켜는 커밋(예: `910c1e1`류)의 커밋 메시지 템플릿에 "☐ 코드 주석에 실제 발동일 ☐ hypotheses.yaml 무조건발동일 갱신" 두 체크박스를 넣거나, `tests/test_ops_hypotheses.py`에 "코드에서 `true`/활성값으로 확인되는 레버는 그 즉시(같은 커밋 내) hypotheses.yaml 무조건발동일이 과거로 갱신돼 있어야 한다"는 테스트를 추가한다.
- **기대 효과**: 발동일 미정리가 매번 다음 장전 회차의 P2 항목으로 반복되는 것을 원천에서 막는다.
- **비용·위험**: 낮음(테스트 1개 + 커밋 규약 문서화).
- **우선순위**: P2.

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (개장 전)

**사람 조치 없음.** 관측 범위(07:30~08:01) 안에서 P0 없음, 오류·재기동·공백 전부 0. 09:00 개장 전에 급히 손댈 것이 없다.

### 오늘 중 (급하지 않음)

1. **`OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`에 09시를 넣을지 정한다.** (근거: 1-7, Fix P1-2) — (A) `{9: 4, ...}`로 09시 추가: 09-02 §1-9가 짚은 09시 공백을 없앤다. (B) 09시는 워밍업 구간이라 손대지 않음: 지금 값 유지, 위클리 커버리지를 그만큼 더 지킨다. **오늘 정해도 적용은 내일(09-04) 07:30 기동부터다** — 오늘 안에 결정만 해 두면 된다.
2. **레버 E 가설의 최종 상태를 확정한다.** (근거: 1-10·1-11, 09-02가 낸 제안: 주장 축 `refuted`, 대가 축 `confirmed(목표 미달)`로 분리 종결) — 다만 §1-11이 짚은 `stale_pct` 지표 설계 문제(위클리가 아니라 먼슬리 분단위 일치만 잰다)를 먼저 검토할 것을 권한다.
3. **이벤트 캘린더 `covered_through`를 9월분으로 갱신한다.** (근거: 1-2, 6거래일째 이월, 오늘도 옵션 만기일이다)
4. **`.claude/` · `dashboard_build_prompt_1.md` 커밋 여부를 정한다.** (09-02까지 8일째 이월)
5. **가설 판정 백로그 13건을 정리한다.** (근거: 1-5) — 09-02가 이미 제안을 낸 9건(레버 E 2건 포함, 조건부 3건, 판정 대상 사건 없음 5건)과, 오늘 새로 도래한 4건(모두 09-02 저녁에 구현·등재된 것이라 아직 실측이 없음 — 오늘 장중·장후 실측 후 확정 대상).

### 결정이 필요한 것 (자동으로 못 하는 것)

6. **ZN/ES 해외선물 거래소 이용 신청 여부.** (만성, 1-4) — (A) CME/CBOT SUB거래소 신청 (B) 매크로 축에서 공식 제외.
7. **Claude 앱 예약 4종의 프롬프트가 전부 새 파일명으로 고쳐졌는지.** 이번 장전 회차의 프롬프트 자체는 이미 `{날짜}_마흐디_일일점검.md`를 정확히 지시하고 있어 이 부분은 **정리된 것으로 보인다.** 장중 2종·장후 프롬프트도 같은지는 이 회차가 확인할 수 없다(리포 밖) — 오늘 장중·장후 회차가 자기 프롬프트를 스스로 보고하면 확인된다.

> **[장중① 확인]** 이 회차(`mahdi-intraday-check`, 12:30 예정)의 실제 프롬프트도 `{날짜}_마흐디_일일점검.md`를 정확히 지시하고 있다 — 장전 것과 일치한다. 장후 프롬프트는 이 회차가 볼 수 없다(리포 밖).

---

## 제2부 — 장중 ① (12:30 회차)

- 국면: **장중①(intra)** · 예정 12:30 · 실행 12:34 KST — **정시 실행** (밀린 실행 아님)
- 증거: `docs/동작점검/auto/2026-09-03_증거_intra.md`
- HEAD `910c1e1`(변화 없음) · 미커밋 그대로 3건(`.claude/` · `dashboard_build_prompt_1.md` · 이 보고서 파일 자신)
- 관측 범위: 07:30~12:34 (레코드 9,681행 · WARNING 167건 · ERROR 0건)

### 2-0. 장전 이월 처분 — 5건 + 관측 5건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 Fix#10(위상 55초) 첫 실거래일 확인 | 🔄 지속 — 레버 값(55.0초)이 그대로 켜진 채 돈다 | 증거 §7 `SIGNAL_FUSION_PHASE_OFFSET_SECONDS = 55.0`, `main.py:923` |
| 1-2 이벤트 캘린더 만료(만성) | 🔄 지속 — 오늘도 07:30:56 같은 경고 재발, 이후도 매시 정각 반복(08:31:57~12:33:57, 6건) | 증거 §5 `event_calendar` 인용 |
| 1-3 레버 3종 발동일 미정리(만성·확대) | 🔄 지속 — 증거 §7 동일 3건(`use_effective_member_count` 11일 지남 · `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` 3일 지남 · `SIGNAL_FUSION_PHASE_OFFSET_SECONDS` 1일 지남) | 증거 §7 |
| 1-4 ZN/ES 조회 실패(만성) | 🔄 지속 — 오늘도 07:32:35(ZN)·07:32:40(ES) 재발, 11:37:37까지 총 5건 | 증거 §4 WARNING 표 |
| 1-5 가설 판정 지연 13건 | 🔄 지속·부분판정 — 오늘 도래 4건 중 **2건은 이 회차가 로그로 확인**(아래 「가설 판정 제안」), 나머지 2건(freshness 실측 포함)과 지난 9건은 사람 확정 대기 그대로 | 아래 「가설 판정 제안」 |
| (관측) Fix#10 실측(freshness) | ➡️ 이월 — `chain_newest_age_seconds.p50`은 DB(postgres) 지표라 이 세션 환경에서 조회 불가(§5-3 「로그 축 없음 — DB 축이 답한다」). 장후 `daily_ops_report.py` 산출물이 나온 뒤 판정 | 장후 이월 |
| (관측) 컷오프(14:50) 근처 밀림 사건 | ➡️ 이월 — 아직 14:50 전이라 판정 대상 시간대에 도달하지 않았다 | 14:30 회차·장후 |
| (관측) 09시 혼잡시간대 포함 여부 결정 | ➡️ 이월 — 사람 미결정. 장전 「사용자 조치」 1번 그대로 유효 | 사용자 조치(장전 절 참고) |
| (관측) 09:00 이후 앙상블 상태 | ✅ 판정(정정 포함) — 가용 4/6·비영 최대 3/6 유지 확인. 다만 자동 적신호가 이를 「축 사망」으로 잘못 읽어 **정정했다**(아래 1-6) | 아래 1-6 |
| (관측) p95 꼬리지연 위험선 도달 여부 | ✅ 판정 — 오늘도 도달했다(4/60창, 최악 12:00 1.01배). 09-02와 같은 패턴 재현. 사전 대응 규칙(`2026-08-04-p5`) 조건도 오늘 6구간으로 이틀 연속 성립 유지 — **발동은 사람이, 다음 장전에만.** 장전 사용자 조치 1번과 같은 결정 | 증거 §5-1 |
| (관측) 레버 E 관련 사람 결정 | ➡️ 이월 — 아직 미확정. 장전 「사용자 조치」 2번 그대로 유효 | 사용자 조치(장전 절 참고) |

### 가설 판정 제안 (사람 확정 대기 — 이 회차는 제안만 한다)

- `2026-09-02-p1-3-shape-transition-names-the-zero-axes` — **확인(제안)**: 오늘 07:30~12:34 전 구간의 "판단 형태 전이" 로그 줄 전부에 `0점축=[...]` 꼬리표가 빠짐없이 붙어 있다(예: `08:46:00 …0점축=['regime_hmm']`, `11:31:57 …0점축=['regime_hmm']`). 설계대로 작동.
- `2026-09-02-p1-4-red-flags-count-consecutive-p95-windows` — **확인(제안)**: 증거 §12 적신호 1번이 "p95/timeout >= 1.0인 창 4/60개(최악 12:00 1.01, **최장 연속 1창** 10:30~10:30)"으로, 개수와 최장 연속 창 수를 함께 냈다. 설계대로 작동.
- `2026-09-02-fix10-phase-offset-55s` / `2026-09-02-p1-5-freshness-has-a-continuous-axis` — **판정 보류**: 둘 다 체인 신선도(freshness)의 DB 축이 있어야 답할 수 있다. 이 세션 환경은 postgres에 접근할 수 없다(장중 점검은 원래 로그만으로 돈다 — B-3 「stale 비율」 항목도 같은 이유로 로그엔 없다). 장후 회차로 이월.

## 1-6. 신규 — 자동 앙상블 경보(§12 적신호 5~8번)가 오탐이었다: 「63분째 미관측」은 축이 죽은 게 아니라 로그가 조용했던 것이다 🆕 P2

**증상.** 증거 다이제스트 §12의 자동 적신호 5~8번이 "앙상블 멤버 `flow_position`/`options_flow`/`orderflow_ofi_vpin`/`regime_hmm`가 11:31:57 이후 63분째 미관측 — 축이 도중에 빠졌다"고 네 축 전부에 대해 경보했다. 이대로 읽으면 판단 엔진이 63분간 아무것도 못 계산한 것처럼 보인다.

**근거.** `mahdi/fusion/engine.py:228~236`(`_log_shape_transition` 함수 독스트링)에 이렇게 적혀 있다 — "가용 멤버 집합·거래허가·기각사유·허용전략의 조합(형태)이 **직전 사이클과 다를 때만** 로그 한 줄을 남긴다. 매 분 찍으면 하루 495줄이 되어 가독성을 잃는다." 즉 이 로그 줄("판단 형태 전이")은 **전이 전용**이라 형태가 안 바뀌면 몇 시간이든 안 찍힌다 — 침묵이 곧 죽음이 아니다.

실제로 판단 엔진은 11:31:57 이후에도 매분 살아 있었다. 같은 판단 사이클(`poll_signal_fusion_cycle`)이 매분 남기는 다른 로그(`주문 미제출 — 실행 경로 전제 미충족…`)가 11:32:57부터 12:34:57까지 **1분 간격으로 61줄** 이어졌다:
```
11:31:57,494 주문 미제출 — … (종목=B09FCWA19 …)
11:32:57,519 주문 미제출 — … (종목=B09FCWA19 …)
   … (11:33:57, 11:34:57, … 1분 간격으로 계속) …
12:33:57,384 주문 미제출 — … (종목=B09FCWA20 …)
12:34:57,358 주문 미제출 — … (종목=B09FCWA20 …)
```
판단은 매분 계산되고 있었고, 다만 그 형태(가용멤버 4/6·거래허가·기각사유·허용전략)가 63분간 안 바뀐 것뿐이다.

**기준.** `docs/동작점검/README.md` 규약 C(「0건」은 계측이 없는 것과 구분해야 한다)와 규약 G(증감·부재를 그대로 판정으로 읽지 않는다)의 연장선이다 — 이번엔 자동 수집기 자신이 "로그가 없다"를 "축이 죽었다"로 잘못 읽었다.

**영향.** 오늘은 실제로 아무 문제가 없었다 — 이 항목은 정정으로 끝난다. 하지만 이 자동경보 문구를 다음에 누군가 그대로 옮겨 적으면, **진짜 앙상블 장애**와 **그냥 63분간 조용히 안정적이었던 시간**을 못 가르게 된다. 지금 상태로는 이 표를 볼 때마다 사람이 매번 로그를 다시 파야 믿을 수 있다는 뜻이다.

**Fix 제안(초안 — 적용은 장후, 지금은 코드를 안 바꾼다).** `docs/동작점검/tools/collect_evidence.py`의 앙상블 가용성 절(§5-2)이 "마지막 관측" 시각을 "판단 형태 전이" 줄에서만 뽑지 말고, 같은 시간대에 판단 사이클이 남기는 다른 매분 로그(예: 위 "주문 미제출" 또는 `옵션체인 사이클 소요 분해`)가 계속 찍히고 있는지 함께 대조한다. 둘 다 있으면 "형태 무변화(정상 지속)", 판단 사이클 로그 자체가 끊겼으면 "진짜 미관측(사건)"으로 나눠 표시한다. 회귀 위험: 낮음(수집기 판정 로직 추가, 마흐디 실행 코드는 안 건드린다). 검증: 다음 실행에서 §12 적신호 문구가 두 경우를 구분해 내는가.

### 14:30 회차가 이어받을 것

- Fix#10(위상 55초) freshness 실측 — DB 지표가 나온 뒤(장후) 판정. 컷오프(14:50) 근처 밀림 사건 여부도 함께
- 09시 혼잡시간대 포함 여부, 레버 E 최종 확정 — 사람 결정 대기, 변경 없으면 그대로 이월
- 컷당한북 우선순위 위반 여부(`priority_before_others_minutes`) — 로그에 이 필드가 없다(오늘 0건 검색). DB/장후 지표가 있어야 판정된다
- 가설 판정 백로그(13건, 그중 2건은 이 회차가 확인 제안까지 냄) — 나머지는 사람 확정 대기
- 1-6(자동 앙상블 경보 오탐) — 수집기 개선 제안이 유효한 채로 남는다. 장후가 채택 여부 검토

---

## 제2부 — 장중 ② (14:30 회차)

- 국면: **장중②(intra) 14:30 회차** · 예정 14:30 · 실행 14:40 KST(10분 지연 — 09:00~15:20 정상 범위라 밀린 실행 아님) · 이 절 작성 완료 시각 14:45 KST
- 증거: `docs/동작점검/auto/2026-09-03_증거_intra_1430.md` (60.8KB) + 원본 로그 직접 대조(`logs/observation_loop.log`, 14:45까지)
- HEAD `910c1e1`(변화 없음) · 미커밋 그대로 3건(`.claude/` · `dashboard_build_prompt_1.md` · 이 보고서 파일 자신)
- 관측 범위: 07:30~14:45 (당일 레코드 14,186행 · WARNING 355건 · ERROR 10건)

### 3-0. 장중① 이월 처분 — 전건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 Fix#10(위상 55초) 첫 실거래일 확인 | 🔄 지속 | 레버 값(55.0초)이 그대로 켜진 채 돈다(증거 §7). freshness 실측은 여전히 DB 축이 있어야 답한다 |
| 1-2 이벤트 캘린더 만료(만성) | 🔄 지속 | 07:30:56 이후 매시 정각 반복, 이번 회차 범위 안 최종 14:34:57(증거 §5) |
| 1-3 레버 3종 발동일 미정리(만성·확대) | 🔄 지속 | 증거 §7 동일 3건(`use_effective_member_count` 11일 지남 · `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` 3일 지남 · `SIGNAL_FUSION_PHASE_OFFSET_SECONDS` 1일 지남), 변화 없음 |
| 1-4 ZN/ES 조회 실패(만성) | 🔄 지속 | 이번 회차 범위 안 최종 13:37:46·13:37:50(증거 §4), 12:34 이후 재발 없음(정오 이후 조용) |
| 1-5 가설 판정 지연 13건 | 🔄 지속 | `hypotheses.yaml` 09-02 이후 한 줄도 안 바뀜(증거 §12-11). 12:30이 제안한 확인 2건도 사람 확정 대기 그대로 |
| 1-6 자동 앙상블 경보 오탐(정정) | 🔄 지속(제안 유효) | 수집기 개선 제안은 아직 미착수. 장후가 채택 여부 검토 — 유효한 채로 이월 |
| (관측) Fix#10 freshness 실측 | ➡️ 이월 | DB(postgres) 축 필요, 이 세션 환경에서 조회 불가. 장후로 이월 |
| (관측) 컷오프(14:50) 근처 밀림 사건 여부 | ✅ **판정 — 없다, 그러나 다른 종류의 사건이 있다** | 14:30~14:44 전 사이클에서 `밀림=0.0초`(정시 시작 유지) — "분이 통째로 밀려 넘어가는" 사건은 없었다. 대신 **분이 밀리지 않고 정시에 시작은 하되 내용물이 비는(rows=0)** 사건이 14:31부터 진행 중이다. 아래 1-7 |
| (관측) 09시 혼잡시간대 포함 여부 결정 | ➡️ 이월 | 사람 미결정. 장전 「사용자 조치」 1번 그대로 유효 |
| (관측) 09:00 이후 앙상블 상태 | 🔄 지속(추가 관찰) | 4/6·비영 2~3 기조 유지되다 **14:35:57에 `options_flow`가 이탈해 3/6**으로 줄었다 — 아래 1-7에서 같은 사건의 일부로 다룬다 |
| (관측) p95 꼬리지연 위험선 도달 여부 | ⬆️ **격상 — p95를 넘어 p50·검열률까지 위험선 도달** | 12:30 판정(도달함, 만성)에서 더 나아가 이번 회차 구간에 **p50 자체**가 타임아웃을 넘었다(아래 1-7). 사전 대응 규칙 조건은 이제 12구간으로 하루 더 확대 — 그대로 이월(발동은 사람이, 다음 장전에만) |
| (관측) 레버 E 관련 사람 결정 | ➡️ 이월 | 아직 미확정. 장전 「사용자 조치」 2번 그대로 유효 |

### 오후 열화 게이지 — 창 단위 시계열

**시간당 개관(07~14시, 증거 §5-1 원문)** — 예산 50초 · `inquire-price` read timeout 4.0초 기준

| 시간대 | rows=0 | REST수집 평균 | 예산대비 | p50 평균 | 창최대 p50 | 최대/timeout | p95 최대 |
|---|---|---|---|---|---|---|---|
| 07시 | 0 | 19.5초 | 39% | 0.02 | 0.02 | 0.01 | 0.05 |
| 08시 | 0 | 19.8초 | 40% | 0.02 | 0.02 | 0.01 | 0.06 |
| 09시 | 0 | 36.2초 | 72% | 0.40 | 0.91 | 0.23 | 3.31⚠ |
| 10시 | 0 | 34.3초 | 69% | 0.89 | 1.73 | 0.43 | 4.02⚠ |
| 11시 | 0 | 36.3초 | 73% | 1.37 | 1.94 | 0.48 | 4.03⚠ |
| 12시 | 0 | 36.0초 | 72% | 1.38 | 2.14 | 0.54 | 4.05⚠ |
| 13시 | 0 | 39.2초 | 78% | 1.59 | 2.17 | 0.54 | 4.00⚠ |
| 14시(41분까지) | **9** | 40.5초 | 81% | 2.17 | **4.05(하한, 검열됨)** | **1.01** | 4.06⚠ | ⛔ |

지연이 07~13시 내내 **서서히** 커진 것(0.01배→0.54배, 시간당 평균 약 0.08배포인트씩)과 달리, 14시는 **한 시간 안에서 절벽**이 났다. 아래는 그 절벽을 5분 창 단위로 뜯어본 것이다(`auto/2026-09-03_지연창.tsv` 원문, 13:30부터).

| 창(끝시각) | 호출수 | p50(초) | p50÷timeout | 검열건수/율 | p95÷timeout | rows=0(그 5분) |
|---|---|---|---|---|---|---|
| 13:30 | 69 | 2.17 | 0.54 | 4/6% | 1.00⚠ | 0/5 |
| 13:45 | 80 | 1.39 | 0.35 | 1/1% | 0.73 | 0/5 |
| 13:50 | 64 | 1.45 | 0.36 | 3/5% | 0.98 | 0/5 |
| 13:55 | 72 | 0.94 | 0.23 | 0/0% | 0.80 | 0/5 |
| 14:00 | 70 | 1.58 | 0.40 | 8/11% | 1.01⚠ | 0/5 |
| 14:05 | 73 | 2.11 | 0.53 | 5/7% | 1.00⚠ | 0/5 |
| 14:10 | 65 | 2.17 | 0.54 | 1/2% | 0.96 | 0/5 |
| 14:15 | 71 | 2.12 | 0.53 | 4/6% | 1.00⚠ | 0/5 |
| 14:20 | 63 | 2.30 | 0.57 | 3/5% | 0.94 | 0/5 |
| 14:25 | 73 | 1.61 | 0.40 | 5/7% | 1.01⚠ | 0/5 |
| **14:30** | 64 | 2.22 | **0.56** | 10/16% | 1.01⚠ | **4/5**(14:31~14:34) |
| **14:35** | **42** | **≥4.0(하한)** | **1.01⛔** | **42/100%⛔** | 1.01 | **5/5** |
| (14:40 창, 부분) | — | — | — | — | — | 5/5(14:36~14:40 전부) |
| (14:44까지, 부분) | — | — | — | — | — | **14/14** 연속(14:31~14:44) |

- **경고선(0.80배) 최초 돌파: 14:35 창.** 그 직전 창(14:30, 0.56배)까지는 하루 종일 한 번도 0.8을 넘은 적이 없다(07:35 이후 85개 창 전수 확인, 증거 §5-1).
- **위험선(1.00배) 최초 돌파: 같은 14:35 창.** 즉 **경고선과 위험선 사이의 간격이 0분이다** — 5분 창 하나를 건너뛰며 0.56배에서 1.01배로 뛰었다. 사람이 "경고를 보고 위험에 대비할" 시간이 없었다.
- **검열률(타임아웃에 잘려 응답 자체를 못 받은 비율) 30% 최초 돌파: 같은 14:35 창(100%)** — 그 전 창은 최고 16%(14:30)였다.
- **rows=0(그 분의 옵션체인이 통째로 빈 것) 최초 발생: 14:31.** 이후 지금(14:44, 확인 시각 14:45)까지 **14분 연속** — 위험 임계(20분 연속)까지 **6분** 남았다. 지금 추세가 그대로면 **14:50(신규 진입 컷오프와 같은 시각) 무렵 20분 임계에 도달한다.**
- **앙상블 멤버 이탈: 14:35:57**, `options_flow`가 가용멤버에서 빠져 4/6→**3/6**(비영 멤버는 2로 변화 없음) — 옵션체인 수집이 끊기자 그것을 입력으로 쓰는 축이 같은 창에서 같이 빠졌다. **원인(수집 절벽)과 결과(축 이탈)가 같은 5분 창에 함께 찍혔다.**
- **워치독: 14:40:01부터 DEGRADED, 14:45:01 기준 연속 6분째.** "직전 10분 동안 옵션체인 적재가 0분"이라는 문구 그대로 — 자동 재기동은 설계대로 하지 않았다(원인이 KIS 쪽일 수 있어서).
- **WS(웹소켓) 단절: 0건** — 이 채널은 이번 사태와 무관하게 그대로 살아 있다. 끊긴 것은 REST(옵션체인·시세조회) 쪽뿐이다.

> **판정은 「창 최대」로 한다** — 07~13시의 완만한 증가와 14시의 절벽은 **같은 축의 다른 국면**이다. 시간당 평균(2.17)만 보면 절벽(4.05, 검열 100%)이 숨는다.

## 1-7. 신규 — 옵션체인 수집 절벽(4호), 14:31부터 진행 중, 원인은 증권사(KIS) 쪽으로 추정 🆕 **P0**

**증상.** 14:31부터 지금(14:44, 14분 연속)까지 옵션체인이 매분 통째로 비어 있다(`rows=0`). 같은 구간에 `inquire-price`(시세조회) 응답시간이 14:35 창에서 지연 경고선(0.8배)과 위험선(1.0배)을 **동시에** 뚫었고, 그 창의 호출 42건 중 42건(100%)이 타임아웃으로 잘렸다. 판단 엔진의 `options_flow` 축이 같은 시각(14:35:57)에 가용멤버에서 이탈했다. 워치독은 14:40:01부터 "DEGRADED(no_ingest)"를 계속 내고 있다.

**근거.**
```
14:30:56 [옵션체인 사이클] rows=1  (첫 이상 징후)
14:31:35 [ERROR] 옵션체인 이번 분 전멸 — 수집 10레그 중 적재 0행(예산 28.5초 소진, 재시도 함)
14:32:56~14:44:55 [옵션체인 사이클] rows=0 (14분 연속, 밀림=0.0초 — 정시 시작은 유지)
14:35:52 지연창: 호출 42건 · p50 ≥4.0초(하한) · p50/timeout 1.01 · 검열 42/42(100%)
14:35:52 [ERROR] 지연 경고선 돌파 — inquire-price p50 4.05초 ÷ 제한 4.00초 = 1.01배 · 이 창 검열 42건(100%)
14:35:57 판단 형태 전이: 가용멤버 [...](3/6, 비영 2) ← 직전 4/6에서 `options_flow` 이탈
14:40:01 [워치독] DEGRADED — 관측 루프 적재 정지(no_ingest) — 직전 10분 동안 적재 0분 · 연속 1분째
14:45:01 [워치독] DEGRADED — 〃 · 연속 6분째(계속 진행 중)
```

**기준.** `docs/동작점검/cliff_episodes.md`가 정의한 "절벽"(수집이 느려지는 게 아니라 통째로 비는 것) 그대로다. 올해 네 번째 표본이다:

| | 08-14 | 08-26 | 09-01 | **09-03(오늘)** |
|---|---|---|---|---|
| 전멸 시작 | 14시대 | 13:40:53 | 15:00(두 파도) | **14:31** |
| 경고선→위험선 간격 | 24분 | 20분 | **0분** | **0분** |
| 그날 최대 비율 | — | 1.18 | 1.01 | **1.01(진행 중, 더 오를 수 있음)** |
| 판단 쪽 대가 | — | `options_flow` 이탈, 3/6 | 비영 2~3/6 | **`options_flow` 이탈, 3/6(비영 2) — 08-26과 동일 패턴** |
| 우리 쪽 조치 | 없음 | 없음(자연 회복) | 없음(자연 회복) | **아직 없음(진행 중)** |

`cliff_episodes.md`가 스스로 걸어 둔 "네 번째가 나오면 물을 것" 질문 1번 — **"간격 0분이 재발하는가?"** — 에 대한 답이 지금 **"그렇다, 재발했다"**로 나왔다(09-01, 09-03 둘 다 0분). 그 파일은 이미 이렇게 적어 뒀다: *"두 번째로 0분이 나오면 「선행 시간」은 그냥 없는 것이다."*

**영향.** 지금 이 순간 프로그램의 판단은 최소 14분 이상 지난 옵션 시세를 보고 있고, 그 값은 계속 낡아가는 중이다(신선도 창 안의 직전 스냅샷 사용 — 프로그램이 눈을 완전히 감은 것은 아니지만, 보고 있는 것이 실시간이 아니다). 과거 세 번의 표본이 전부 "우리 쪽이 손대지 않아도 장 마감 직후 저절로 풀렸다"는 것을 감안하면, **오늘 이 상태가 장 마감(15:20)까지 이어질 가능성이 있다** — 즉 오늘 남은 장중 내내(신규 진입 컷오프 14:50 포함) 판단이 실시간이 아닌 시세를 볼 수 있다는 뜻이다. 실제 손익에 영향이 있었는지는 이 세션에서 조회할 수 없는 DB(주문·체결 기록)를 봐야 답이 나온다 — 장후 이월.

**Fix 제안 — 지금은 없다.** 근거 셋이 독립적으로 "우리 쪽 문제가 아니다"를 가리킨다: (1) 우리 쪽 코드·설정 변경 0건(당일 커밋 없음), (2) 같은 증상이 REST 한 채널(옵션체인·시세조회)에만 국한되고 WS·체결통보는 멀쩡하다, (3) 과거 세 번 다 우리 쪽 조치 없이 자연 회복했다. **재기동은 이 문제를 고치지 못한다**(원인이 KIS 서버 쪽이면 재기동 후에도 같은 타임아웃이 재현된다) — 그런데 재기동의 대가(예열 약 30분, 규약상 봉 30개)는 실재한다. 따라서 이번 사안은 통상적인 "재기동 손익 비교" 틀이 아니라 **"손댈 지렛대가 없다"**는 결론이다.

**고도화 제안(장후 검토, 지금은 코드 변경 없음).** `docs/동작점검/cliff_episodes.md`에 09-03을 4번째 열로 추가하고, 위 표의 질문 1("간격 0분 재발")에 "재발했다"로 답을 옮겨 적을 것을 권한다 — 이 결론이 확정되면 P1-4가 만든 "지연 경고선"을 **선행 신호가 아니라 동시 신호**로 재정의하는 것이 다음 논의 대상이다(오늘은 판정만, 재정의는 장후 이후).

## 1-8. 신규(관측) — rows=0 연속 14분, 위험 임계(20분)까지 6분 남음 · 컷오프 시각과 겹친다 🆕 P1

**증상 아님 — 계산된 관측.** 최장 연속 `rows=0` 구간이 14:31~14:44 현재 **14분**이다(임계 20분, 증거 §5-1 최신값은 회차 실행 시각 기준 9분이었으나 그 뒤로도 계속돼 지금은 14분). 이 추세가 그대로면 **20분 임계 도달 예상 시각은 14:50 전후**로, 신규 진입 컷오프와 겹친다.

**근거.** 위 1-7 인용과 동일 로그. 계산: 14:31이 첫 rows=0, 14:44가 최신 확인 시각 → 14분.

**기준.** phases.md 오후 열화 게이지 표의 "관측 루프 공백" 위험선(20분)과는 다른 축이다 — 이것은 "로그가 없다"가 아니라 "로그는 매분 있는데 내용이 계속 0이다"이므로 별도로 추적한다.

**영향.** 이 자체가 새 위험을 만들지는 않는다(1-7과 같은 근본원인) — 다만 "몇 분 남았는가"를 시계처럼 보여줘서, 다음 회차(장후)가 "그래서 결국 몇 분 연속으로 비었는가"를 바로 답할 수 있게 한다.

## 14:50 컷오프 권고

**지금(14:45) 기준 컷오프까지 5분 남았다.**

- **코드·설정 조치: 없음.** 원인이 KIS 쪽으로 추정되고(1-7 근거 참고), 재기동은 이 문제를 고치지 못하면서 예열 대가(약 30분)만 발생시킨다. 장중 재기동 자체가 규약상 금지이기도 하다.
- **사람이 지금 결정할 새로운 것: 없다.** 09시 혼잡시간대·레버E 결정(장전부터 이월 중인 두 건)은 이 사태와 무관하며 그대로 미뤄도 된다.
- **인지해 둘 것 하나:** 14:50~15:20 사이 신규 진입 판단이 있다면 그 판단은 지금 진행 중인 절벽이 안 풀렸을 경우 **실시간이 아닌(최소 14분 이상 지난) 옵션 시세**를 참고한 것일 수 있다. 이것은 사람이 지금 막을 수 있는 성격이 아니다(프로그램 설계상 신선도 창 스냅샷 사용은 이미 내장된 대응이다) — **알고 있으라는 뜻이지, 하라는 뜻이 아니다.**

**결론: 개입 불필요.** 과거 세 번의 동일 패턴이 전부 사람 개입 없이 장 마감 직후 자연 회복됐고, 이번 사태의 성격(REST 단일 채널 절벽, 우리 쪽 변경 0건)도 그 세 번과 같다. 장후 회차가 회복 시각과 실제 판단 영향(GEX 입력 결손 분수, freshness DB 축)을 확정한다.

### 14:50 회차가 이어받을 것

- 1-7·1-8(수집 절벽 4호) — 회복 시각, 최종 rows=0 연속 분수, freshness DB 축 판정. **장후의 1차 질문이어야 한다**
- Fix#10(위상 55초) freshness 실측 — 오늘 절벽과 겹쳐 있어 판정이 더 까다로워졌다(신선도 창이 정상 작동해도 그 입력 자체가 14분+ 낡았던 구간과 겹친다)
- 09시 혼잡시간대 포함 여부, 레버 E 최종 확정 — 사람 결정 대기, 변경 없으면 그대로 이월
- 가설 판정 백로그(13건) — 사람 확정 대기
- 1-6(자동 앙상블 경보 오탐) — 수집기 개선 제안 유효, 채택 여부 검토
- `cliff_episodes.md`에 09-03을 4번째 열로 추가하고 "간격 0분 재발" 질문에 답을 확정할 것

### dev_memory 반영 초안 (장후가 실제로 옮겨 적을 것 — 이 회차는 초안만 남긴다)

- **DECISION_LOG.md 추가 후보**: "2026-09-03 — 옵션체인 수집 절벽 4호(14:31 시작, 09-03 기준 최소 14분+ 연속 rows=0). 경고선·위험선 간격 0분 재발(09-01에 이어 두 번째) — `cliff_episodes.md` 질문 1에 대한 답이 '재발한다' 쪽으로 확정. 원인은 KIS 쪽으로 추정(REST 단일 채널 국한·WS 정상·당일 커밋 0건). 우리 쪽 조치 없음(과거 3건과 동일)."
- **NEXT_TODO.md 추가 후보**: "[ ] `docs/동작점검/README.md`의 지연 경고선(P1-4) 문구를 '선행 신호'에서 '동시 신호일 수 있음'으로 재정의할지 검토 — 09-01·09-03 두 표본이 간격 0분."
- **CURRENT_STATE.md**: 이번 절벽의 회복 시각·최종 지속 시간은 장후가 확정한 뒤 갱신.

---

## 제3부 — 장후 (15:20 ~ )

- 국면: **장후(post)** · 예정 15:45 · 증거 수집 실행 15:46 KST(정시) · 이 절 작성 완료 16:15 KST — **정시 실행** (작성 지연 30분은 증거·원본 대조 작업 시간이며 감시 범위 밖으로 나가지 않았다, 밀린 실행 아님)
- 증거: `docs/동작점검/auto/2026-09-03_증거_post.md` · `auto/2026-09-03_지표.{md,json}` · `logs/watchdog.log`(14:40~15:30 원본 대조) · `docs/동작점검/hypotheses.yaml`(7026행~) · `docs/Dev_md/MAHDI_ULTIMATE_SYSTEM_v6.md`(§12.2·12.3, 절대원칙)
- HEAD `910c1e1`(변화 없음) · 당일 커밋 **0건** · 미커밋 3건(`.claude/` · `dashboard_build_prompt_1.md` · 이 보고서 파일 자신 — `_지표.json`은 최신 실행으로 이미 갱신됨)
- 관측 범위: **07:30~15:45 (하루 전체)** — 레코드 15,990행 · WARNING 644건 · ERROR 64건 · 워치독 90행(OK 44 / 비-OK 46)

### 3-0. 장중② 이월 처분 — 전건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 Fix#10(위상 55초) 첫 실거래일 확인 | ✅ **판정(조건부) — 오염 의심, 보류 제안** | 아래 「가설 판정 제안」 1번. 직접지표(stale_pct 0.8%)는 기준(20% 이내)을 크게 통과했지만, 초 단위 지표(체인 최신 나이 p50 57.4초)만 기준(55.0초 이내)을 2.4초 넘었다 — 오늘 옵션체인 절벽(53분)이 이 계산에 섞였을 가능성 있음(증거 §14) |
| 1-2 이벤트 캘린더 만료(만성) | 🔄 지속 | 07:30:56~15:35:56까지 매시 정각 총 9회 반복(증거 §5). 오늘이 마침 옵션 만기일(09-03)이었다는 점에서 6거래일째 방치의 실질 대가가 실재했다 |
| 1-3 레버 3종 발동일 미정리(만성·확대) | 🔄 지속 | 증거 §7 동일 3건 — `use_effective_member_count` 11일 지남 · `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` 3일 지남 · `SIGNAL_FUSION_PHASE_OFFSET_SECONDS` 1일 지남. 하루 끝까지 변화 없음 |
| 1-4 ZN/ES 조회 실패(만성) | 🔄 지속 | 하루 마지막 발생 15:27:37(ZN)·15:27:38(ES) — 당일 총 18건. 12:34 이후 한동안 조용했다가 옵션체인 절벽 회복 직후(15:27) 다시 발생 — 절벽과 무관한 별개의 만성 원인(거래소 이용 미신청) |
| 1-5 가설 판정 지연 13건 | ✅ **이 회차가 13건 전부에 판정 제안을 냈다(확정은 사람)** | 아래 「가설 판정 제안」 표. 지연 자체(9건 최장 8거래일)는 해소되지 않았고 사람 확정이 남아 있다는 점에서 지연 상태는 유지되나, "제안 없음"이던 상태는 벗어났다 |
| 1-6 자동 앙상블 경보 오탐(정정) | 🔄 지속(제안 유효, 미착수) | 12:30이 낸 `collect_evidence.py` §5-2 개선 제안이 아직 코드에 반영되지 않았다 — 아래 Fix P1-B로 이월 |
| 1-7 옵션체인 수집 절벽(4호) | ✅ **해소 — 15:25~15:26 자연 회복** | 아래 1-9. 심각도 P0→**P1로 하향**(진행 중일 때는 결과를 몰라 P0였으나, 결과가 "실거래 영향 0"으로 확정됐다) |
| 1-8 rows=0 연속 14분 관측 | ✅ **판정 — 최종 53분 연속(14:31~15:23), 임계 20분을 33분 초과** | 1-7과 같은 사건의 최종 수치. 증거 §5-1 「최장 연속 rows=0 구간: 53분」 |
| (관측) Fix#10 freshness DB 실측 | ✅ 판정(조건부) | 위 1-1과 동일. DB 지표(`chain_newest_age_seconds`)가 postgres 조회 가능한 장후 환경에서 처음 확인됐다 |
| (관측) 09시 혼잡시간대 포함 여부 결정 | ➡️ **이월(만 5일째)** | 사람 미결정. 오늘 결정해도 적용은 내일(09-04) 07:30 기동부터다 — 사용자 조치 참고 |
| (관측) 레버 E 관련 사람 결정 | ➡️ **이월** | 아직 미확정. 사용자 조치 참고 |
| (관측) 컷당한북 우선순위 위반 여부 | ➡️ **판정 불가 — 이월** | `priority_before_others_minutes` 필드가 로그에 0건. DB 축도 오늘 지표에 해당 항목 없음. 다음 회차가 DB 스키마 존재 여부부터 확인할 것 |

### 가설 판정 제안 (사람 확정 대기 — 이 회차는 제안만 한다)

`hypotheses.yaml`의 검증예정일 도래·경과 pending 13건 전부에 대한 이 회차의 제안이다. **`상태`는 이 회차가 직접 바꾸지 않는다.**

| id | 검증예정일 | 이 회차의 제안 | 근거 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | 09-01(2일 경과) | **미실행 유지** — 전제 레버 `REGIME_RESTORE_SESSION_WINDOW`가 오늘도 꺼진 채(증거 §7). 판정 자체가 불가능하다(규약 H). 레버를 켠 다음 거래일로 예정일을 옮길 것을 제안 |
| `2026-08-12-eE-on-congested-hours` | 09-02(1일 경과) | **부분 확인(대가 지표 오염)** — 주장 축 `stale_pct` 0.8%(≤70 통과) 확인. 대가 축 `book_coverage`(위클리 32.7~34.2%, 기준선 50%에서 약 33%로 하락 — 예측한 "약 25%"보다는 덜 내려갔으나 방향은 일치) 확인 방향. 다만 대가 축 `gex_input_missing_minutes`(50분, 기준선 4분)은 **오늘 절벽(50분 GEX 입력 없음의 대부분이 절벽 구간과 겹친다)이 원인**일 가능성이 커서 이 레버의 대가로 온전히 못 잰다(규약 E) — 절벽 없는 날 재확인 필요 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 09-01(2일 경과) | **판정 불가** — 오늘 왕복 실측 자체가 없었다(execution_logs 0행, 오늘 거래 0건, 아래 1-10). 검정 대상 사건이 없었을 뿐 기능 결함이 아니다. 거래가 발생하는 날로 예정일을 옮길 것을 제안 |
| `2026-08-21-p1-latency-recovers-with-the-session` | 09-01(2일 경과) | **확인** — 오늘 지연이 위험선(1.0배)을 14:35에 넘었고, 회복(15:25~15:26)이 예측 범위(단일가 종료 15:30 ±5분, 즉 15:25~15:35) **안**에 들었다 |
| `2026-08-25-fix8-v2-watchdog-cost-is-cadence-not-count` | 09-01(2일 경과) | **확인** — `watchdog.recovered_episodes`=1(오늘 실제 45분 DEGRADED→RECOVERED 1회와 일치, 로그 `[15:25:01] RECOVERED — 적재 정지 45분 지속 후 회복(14:40~15:24)`). `watchdog.silence_over_cadence_ratio`=1.01(≤1.5 통과) |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 08-27(**8일 경과**) | **부분 확인(다른 경로로 충족)** — 오늘 장전 §1-0/A-2가 "9월 초 사전 대응 규칙 조건: 09-02에 새 성립 기록 없음 — 레버 E가 이미 켜진 채 돌고 있어 이 규칙과 같은 손잡이가 닫힌 상태"라고 조건을 인용했다(41행). 규칙이 별도로 발동할 것이 없어진 상태(레버 E가 이미 상시 켜짐)라, "장전이 그 조건을 인용한다"는 요건은 형태를 바꿔 충족된 것으로 본다 |
| `2026-08-26-adv7-cliff-episodes-accumulate` | 09-01(2일 경과) | **확인** — 오늘(4번째 표본) 대조 시 `cliff_episodes.md`에 이미 08-26·09-01 두 건이 같은 축으로 적혀 있어 비교가 즉시 가능했다(이 회차가 실제로 그렇게 대조해 회복시각 3연속 일치를 찾아냈다, 아래 1-9). 대가 축(임계·자동판정을 안 만들었는가)도 유지 — 여전히 사람이 판정한다 |
| `2026-08-31-p1-3-a-missing-round-is-reconstructed-not-erased` | 09-01(2일 경과) | **확인** — 오늘 장전 증거 §8-1이 직전(09-02) 보고서를 정상적으로 찾았고 미완료 체크박스 0건으로 확인했다. 결번 회차는 오늘 발생하지 않아 "사후 재구성" 자체는 검정 대상이 없었으나 탐지 메커니즘은 정상 작동했다 |
| `2026-08-31-adv1-the-close-does-our-ab-test-every-day` | 09-01(2일 경과) | **확인(오늘이 강한 표본)** — `rest_latency.close_transition.ratio`=0.0208(0에 가까움 → KIS 부하 쪽 판정). 오늘은 마감 직전(14:50~15:20 구간)이 절벽 한복판이라 "마감 전"이 극단적으로 느렸는데 "마감 후"(15:30~15:45, 회복 이후)는 정상이었다 — 이 비율이 정확히 그 대비를 잡아냈다. 오늘 결론(KIS 쪽 원인)과도 정합적이다 |
| `2026-09-02-p1-3-shape-transition-names-the-zero-axes` | 09-03(도래) | **확인(장중①에 이어 최종 확정)** | 07:30~15:45 전 구간 「판단 형태 전이」 로그에 `0점축=[...]` 이름이 빠짐없이 붙었다(증거 §14-1 멤버별 표와 부합) |
| `2026-09-02-p1-4-red-flags-count-consecutive-p95-windows` | 09-03(도래) | **확인(장중①보다 강한 확증)** | 12:30에는 4/60창·최장연속 1창(평온한 표본)이었으나, 하루 끝 §12 적신호 4번이 "`p95/timeout>=1.0`인 창 21/98개, **최장 연속 13창**(14:25~15:25)"으로 정확히 개수와 연속창 수를 함께 냈다 — 스트레스 상황에서도 설계대로 작동했다는 점에서 더 강한 확증이다 |
| `2026-09-02-fix10-phase-offset-55s` | 09-03(도래) | **판정 보류(조건부) — 위 1-1 참고** | `stale_pct` 0.8%(직접지표, 규약 J)는 크게 통과. `chain_newest_age_seconds.p50` 57.4초만 기준(55.0초)을 2.4초(4.4%) 초과 — 오늘 절벽 오염 의심. 절벽 없는 정상일에 재실측할 것을 제안 |
| `2026-09-02-p1-5-freshness-has-a-continuous-axis` | 09-03(도래) | **확인** | `chain_newest_age_seconds.behind_minutes` 히스토그램이 실재하고 여러 칸(0분:441 · 1~4분:각 1)으로 나뉜다 — 08-18 이후 매일 상한(98~100%)에 붙어 안 움직이던 이산 지표(`stale_pct`)와 달리 연속값은 초 단위로 움직인 것이 보인다(이 항목의 요점 그대로 실현됨). 대가(정의 무변경) — `stale_pct` 0.8%는 정의를 안 건드린 채 나왔다 |

> **소급 적용 금지 원칙 재확인**: 위 제안은 모두 오늘(09-03) 실측을 반영한 것이지, 예정일 당시로 거슬러 올라가 값을 고친 것이 아니다.

### 3-1. 종료 시퀀스 (15:20 ~ 15:45)

- **15:20 정규장 마감(옵션)** — 이 시각, 절벽은 아직 진행 중이었다(rows=0 최종 15:23까지). 즉 **14:31부터 15:20 정규장 마감까지, 남은 정규 거래시간의 사실상 전 구간이 옵션체인 공백 상태로 지나갔다** — 이는 과거 세 번의 표본(08-14·08-26·09-01)이 대체로 오후 특정 시간대에 국한됐던 것과 달리, **이번엔 마지막 49분간(14:31~15:20)이 통째로 절벽에 잠식됐다**는 점에서 정규 거래시간 잠식 비율로는 이번이 가장 크다(신규 진입 컷오프 14:50도 이 구간 안에 있었다 — 다만 오늘은 신규 진입 자체가 없었다, 1-10).
- **15:24~15:26 회복** — `옵션체인 사이클 소요 분해` 로그: 15:24 `rows=4`(부분 회복 시작) → 15:25 `rows=19` → 15:26 `rows=20`(완전 정상화). 워치독은 15:25:01에 `RECOVERED — 적재 정지 45분 지속 후 회복(14:40~15:24)`을 남겼다.
- **15:30~15:45 종료 배치** — `.last_marketclose_stop.txt`: `2026-09-03T15:45:03.537009`. `shutdown_check_log` 잔존 프로세스 0. 워치독 15:30·15:40 두 회차 모두 `OK — 박동 정상 · 적재 감시 창 밖`. 종료 시퀀스 자체에는 이상이 없었다.
- **경보 억제 확인** — Slack 경보 토글이 꺼진 채였고(2026-08-01 보류 결정 유지), 오늘 그 토글 때문에 버려진 경보가 **2건** 있었다(증거 §16 인용). "불렀는데 안 갔다"와 "안 불렀다"를 가르는 수치로, 오늘 실제로 무언가 버려졌다는 뜻이다 — Slack을 켜라는 결론은 아니지만 사용자 조치에 참고로 남긴다.

## 1-9. 신규 — 옵션체인 수집 절벽(4호) 최종 확정: 45분(워치독 기준)/53분(rows=0 기준) 만에 자연 회복, **회복 시각이 세 번째로 정확히 「정규장 마감+6분」과 일치** 🆕 **P1**

**증상 아님 — 오늘 진행 중이던 P0(1-7·1-8)의 결말 확정.** 14:31부터 진행되던 옵션체인 수집 절벽이 15:25~15:26 사이 사람 손을 안 대고 저절로 풀렸다.

**근거.**
```
15:24:53 [옵션체인 사이클] rows=4  (첫 회복 조짐)
15:25:01 [워치독] RECOVERED — 적재 정지 45분 지속 후 회복(14:40~15:24)
15:25:52 [옵션체인 사이클] rows=19
15:26:31 [옵션체인 사이클] rows=20 (완전 정상화)
15:23:56 [ERROR] 옵션체인 이번 분 전멸 (이 시각이 rows=0 계열 ERROR의 마지막 기록)
```
증거 §5-1: "최장 연속 `rows=0` 구간: **53분** (14:31~15:23)". `cliff_episodes.md`(08-26·09-01 두 표본이 이미 기록됨)와 대조하면:

| | 08-26 | 09-01 | **09-03(오늘)** |
|---|---|---|---|
| 경고선→위험선 간격 | 20분 | **0분** | **0분(재발, 2/3)** |
| 회복 시각 | **15:26** | **15:26** | **15:25~15:26** |
| 정규장 마감(15:20) 대비 | +6분 | +6분 | **+5~6분** |

**세 번의 표본 중 세 번 다 15:26 언저리에 회복했다.** `cliff_episodes.md`가 스스로 걸어 둔 "네 번째가 나오면 물을 것" 두 질문에 대한 답:

1. **간격 0분이 재발하는가?** → 그렇다, 두 번째 재발(09-01, 09-03). 그 파일이 이미 적어 둔 대로 "두 번째로 0분이 나오면 「선행 시간」은 그냥 없는 것이다" — 경고선은 이제 선행 신호가 아니라 **동시 신호**로 다시 정의할 근거가 섰다.
2. **회복 시각이 계속 마감+6분인가?** → 그렇다, 세 번째로 같다. 이것은 우연이라 보기 어렵다 — **KIS 쪽에 15:25~15:26 무렵의 어떤 주기적 처리(정산 배치, 세션 리셋 등)가 있을 가능성**을 시사한다.

**기준.** `docs/동작점검/cliff_episodes.md` "네 번째가 나오면 물을 것" 절.

**영향.** 오늘은 실제 손실로 이어지지 않았다(거래가 없었으므로, 아래 1-10). 하지만 이 패턴이 세 번째로 확인됐다는 것은, 다음에 같은 절벽이 나면 **"몇 시에 풀릴지"를 사람이 어느 정도 예상할 수 있다는 뜻**이다 — 지금은 그 정보가 사람 머릿속에만 있고 시스템 경보 문구에는 없다.

**Fix 제안(장후 검토용, 지금은 반영하지 않음).** 아래 제4부 P1-A·P2-3.

## 1-10. 신규(확인) — 오늘 거래 0건, 실행 경로가 아직 배선되지 않은 관측 전용 상태 확인 🆕 **P2(정보성 확인, 결함 아님)**

**증상 아님 — 확인 사항.** 오늘 하루 보유 포지션 기록 0행, 포지션 원장 사건(연 것/닫은 것) 0건, 체결통보 0건(단 WS 구독 성립은 9회, 스트림 끊김 8회로 정상 재연결 비용 범위), 주문 0건.

**근거.** 증거 §16-1~16-4(지표.md). `docs/dev_memory/CURRENT_STATE.md`가 이미 "mahdi/main.py — 관측 전용 오케스트레이터"로 명시하고 있다 — 실주문 발주 경로가 아직 연결되지 않은 상태는 오늘 새로 생긴 것이 아니라 설계상 현재 단계다.

**기준.** 규약 C("0건"은 계측 없음과 구분해야 한다) — 오늘의 0은 "체결이 없어서"가 아니라 "주문 자체를 낼 수 있는 경로가 없어서"이고, 이 둘을 가르는 절차(§16-1 주석)를 오늘 실제로 따라가 확인했다.

**영향.** 오늘 옵션체인 절벽(1-7~1-9)이 얼마나 위험했는지를 실전 손익으로는 판단할 수 없다는 뜻이다 — 만약 주문 경로가 배선된 상태였다면, 14:31~15:20 사이의 신규 진입 판단이 최대 53분 낡은 시세를 근거로 이뤄졌을 것이고 그 결과가 실제 손익에 남았을 것이다. 오늘은 그 위험이 **관측만 됐고 청구되지는 않았다.**

---

## 종합 판정표 — 하루 전체, 국면별 판정이 어떻게 바뀌었는가

| 항목 | 장전(07:30~09:00) | 장중①(~12:34) | 장중②(~14:45) | 장후(최종, ~15:45) |
|---|---|---|---|---|
| 시스템 전반 | ✅ 정상 | ✅ 정상 | ⚠ P0 진행 중(옵션체인 절벽) | ✅ 해소 — 실거래 영향 0(거래 자체가 없었음) |
| 옵션체인 수집 | 정상 | 정상 | ⛔ 14:31부터 전멸 진행 중 | ✅ 15:25~15:26 자연 회복(45~53분 지속) |
| Fix#10(위상 55초) | 오늘 첫 실거래일, 관측 예정 | 판정 불가(DB 접근 불가) | 판정 불가(DB 접근 불가) | 조건부 판정 보류(오염 의심) |
| p95/p50 꼬리지연 | 관측 전 | 도달함(4/60창, 만성 재현) | ⬆ 격상 — p50·검열률까지 위험선 도달 | 최종: 21/98창 위험선 도달, 최장연속 13창 · 하루 최대 1.01배 |
| 앙상블 가용성 | 관측 전 | 4/6·비영 3(정상) | 4/6→3/6(절벽과 동시 이탈) | 형태분포 4/6×74·3/6×41·2/6×1·1/6×1, 비영 최대 3 |
| 자동 앙상블 경보 정합성 | — | ⚠ 오탐 발견·정정(1-6) | 지속(제안 미착수) | 지속(제안 미착수, 이월) |
| 09시 혼잡시간대 결정 | 결정 예정으로 명시 | 미결정 | 미결정 | **미결정 — 5일째 이월** |
| 레버 E 최종 확정 | 판정 완료·사람 대기 | 미확정 | 미확정 | **미확정 — 이월** |
| 이벤트 캘린더 | 만료(만성) | 만료·재발 | 만료·재발 | 만료·재발(옵션 만기일 당일에도) — **6거래일째 이월** |
| ZN/ES 조회 | 실패(만성) | 실패·재발 | 실패·재발 | 실패·재발(총 18건) — 만성 유지 |
| 가설 판정 백로그 | 13건 지연 | 2건 제안 | 2건 확정 유지 | **13건 전부 제안 완료(확정은 사람)** |
| 거래·손익 | — | — | — | **거래 0건 · 손익 0원 · 절대원칙 전항목 N/A(거래 없음)** |

## 당일 이상점 통합 대장 — 1-1부터 1-10까지 전부

| 번호 | 제목 | 최초 발견 국면 | 최종 심각도 | 최종 처분 | 근거 위치 |
|---|---|---|---|---|---|
| 1-1 | Fix#10(위상 55초) 첫 실거래일 확인 | 장전 | P1(정확성, 조건부) | 판정 보류(오염 의심 — 절벽 없는 날 재확인 제안) | 장전 §1-1, 장후 「가설 판정 제안」 |
| 1-2 | 이벤트 캘린더 만료(만성) | 장전 | P2 | 🔄 지속 — 6거래일째, 오늘도 옵션 만기일 | 장전 §1-2, 장후 3-0 |
| 1-3 | 레버 3종 발동일 미정리(만성·확대) | 장전 | P2 | 🔄 지속 — 하루 끝까지 변화 없음 | 장전 §1-3, 장후 3-0 |
| 1-4 | ZN/ES 해외선물 조회 실패(만성) | 장전 | P2 | 🔄 지속 — 당일 총 18건 | 장전 §1-4, 장후 3-0 |
| 1-5 | 가설 판정 지연(9건→13건) | 장전 | P2 | ✅ 이 회차가 13건 전부 제안 완료(확정은 사람) | 장전 §1-5, 장후 「가설 판정 제안」 |
| 1-6 | 자동 앙상블 경보 오탐(63분 미관측 오판) | 장중① | P2 | 🔄 지속(정정 완료, Fix 제안 미착수) | 장중① §1-6 |
| 1-7 | 옵션체인 수집 절벽 4호 | 장중② | P0→**P1(완료)** | ✅ 해소 — 15:25~15:26 자연 회복, 실거래 영향 0 | 장중② §1-7, 장후 §1-9 |
| 1-8 | rows=0 연속 관측(임계 근접) | 장중② | P1→**해소(수치 확정)** | ✅ 판정 — 최종 53분 연속(14:31~15:23), 임계 20분의 2.65배 | 장중② §1-8, 장후 §1-9 |
| 1-9 | 절벽 회복시각 3연속 「마감+6분」 일치·간격0분 2/3 재발 | **장후(신규)** | P1 | 확정(운영 인사이트) — `cliff_episodes.md` 4번째 열 반영 필요(제4부 P1-A) | 장후 §1-9 |
| 1-10 | 오늘 거래 0건, 실행 경로 미배선 확인 | **장후(신규)** | P2(정보성) | 확인 완료 — 결함 아님, 손익 판정의 전제로 기록 | 장후 §1-10 |

## 종가 기준 최종 손익 / 절대원칙 판정

**거래 없음.** 이유: 마흐디는 현재 매매 실행 경로가 배선되지 않은 관측 전용 상태다(`CURRENT_STATE.md`). 오늘 보유 포지션 기록 0행 · 포지션 원장 사건(개설/청산) 0건 · 체결통보 0건 · 주문 0건(증거 §16-1~16-4, 위 1-10).

| 항목 | 값 | 비고 |
|---|---|---|
| 실현손익 | **0원** | 거래 없음 |
| 미실현손익 | **0원** | 보유 포지션 없음 |
| 포지션 수 | **0개** | — |
| 계약 수 | **0계약** | — |
| 자본 대비 % | **0.0%** | — |
| 고점 대비 % | **0.0%** | — |

**절대원칙(하드 리밋·금지 사항) 판정** — 기준: `docs/Dev_md/MAHDI_ULTIMATE_SYSTEM_v6.md` §12.2·12.3

| 원칙 | 기준값 | 오늘 값 | 판정 |
|---|---|---|---|
| 단일 트레이드 손실 한도 | 계좌의 −0.5% | 거래 없음 | ✅ (해당 없음) |
| 일일 손실 한도 | 계좌의 −2% | 거래 없음 | ✅ (해당 없음) |
| 주간 손실 한도 | −5% | 이 세션에서 조회 불가(DB 필요) | ✅ (오늘 거래 0이므로 오늘 기여분은 0) |
| 최대 드로우다운 | −10% | 거래 없음 | ✅ (해당 없음) |
| 동일 방향 동시 포지션 상한 | 3개 | 0개 | ✅ |
| 포트폴리오 Greeks 상한 | Delta/Gamma/Vega 한도 | 포지션 없음 | ✅ (해당 없음) |
| 하루 최대 거래 횟수 | 전략별 상한 | 0건 | ✅ |
| Kill Switch — `data_quality_fail`(피드 끊김/지연) | 조건 성립 시 신규 차단 | ⚠ 오늘 53분 연속 옵션체인 공백이 이 조건의 취지에 해당하는 성격이었으나, 매매 실행 경로가 배선되지 않아 이 서킷브레이커가 실전에서 발동을 검증할 기회 자체가 없었다 | ⚠ 참고(위반 아님 — 검증 불가 상태) |

**결론: 오늘 절대원칙 위반 0건.** 다만 매매 실행 경로가 배선되는 시점부터는 오늘과 같은 절벽 상황에서 Kill Switch가 실제로 작동하는지가 첫 실측 대상이 된다(제5부 고도화 참고).

---

## 제4부 — Fix 구현계획 (P0/P1/P2)

> 이 회차는 코드를 고치지 않는다. 계획만 세운다 — 적용은 사람 검토 후.

### P1-A (신규, 근거 1-9). `cliff_episodes.md`에 09-03을 4번째 열로 추가하고 두 질문에 답을 확정한다

- 대상: `docs/동작점검/cliff_episodes.md` "## 축" 표 + "네 번째가 나오면 물을 것" 절.
- 반영할 값(그대로 복사 가능하도록 정리): 전멸 시작 **14:31** · 최장 연속 rows=0 **53분(14:31~15:23)** · 경고선(0.8) 돌파 **14:35** · 위험선(1.0) 돌파 **14:35(간격 0분)** · 그날 최대 비율 **1.01(14:35·15:05 창)** · 회복 **15:25~15:26**(정규장 마감 15:20 **+5~6분**) · 우리 쪽 조치 **없음** · 판단 쪽 대가 **`options_flow` 이탈, 4/6→3/6(비영 2)** · 워치독 **14:40부터 no_ingest 45분 연속, 자동 재기동 안 함**.
- 질문 1(간격 0분 재발) 답: **재발했다(2/3)** — 경고선을 "선행 신호"에서 "동시 신호"로 재정의 검토 필요(P1-B와 연계).
- 질문 2(회복이 계속 마감+6분인가) 답: **그렇다, 3/3** — 다음 절에서 "KIS 쪽 정기 처리 가능성" 조사 항목으로 승격(제5부 고도화 1).
- 회귀 위험: 없음(문서만, 마흐디 실행 코드 미변경). 검증: 표에 4번째 열이 정확히 반영됐는지 사람이 확인.

### P1-B (이월, 근거 1-6). `collect_evidence.py`의 앙상블 가용성 판정에 판단 사이클 매분 로그를 함께 대조한다

- 대상: `docs/동작점검/tools/collect_evidence.py` §5-2 절(앙상블 가용성).
- 내용: "마지막 관측" 시각을 「판단 형태 전이」 줄에서만 뽑지 말고, 같은 시간대에 판단 사이클이 남기는 다른 매분 로그(`주문 미제출 —…` 또는 `옵션체인 사이클 소요 분해`)가 계속 찍히는지 함께 대조한다. 둘 다 있으면 "형태 무변화(정상 지속)", 판단 사이클 로그 자체가 끊겼으면 "진짜 미관측(사건)"으로 구분 표시.
- 회귀 위험: 낮음(수집기 판정 로직 추가, 마흐디 실행 코드 미변경). 검증: 다음 실행에서 §12 적신호 문구가 두 경우를 구분하는가.

### P1-C (신규). `hypotheses.yaml` 13건 확정 반영

- 대상: `docs/동작점검/hypotheses.yaml`. 위 「가설 판정 제안」 표의 12건은 확정(확인/미실행/판정불가 등)이 가능하고, 1건(`2026-09-02-fix10-phase-offset-55s`)은 **판정 보류 상태로 예정일만 연기**할 것을 제안.
- 이 회차는 `상태` 필드를 직접 바꾸지 않는다 — 사람이 표를 검토해 반영.

### P2-1 (이월, 근거 1-3). 레버 3종의 발동일 기록을 한 번에 정리한다

- 대상: `hypotheses.yaml`의 `use_effective_member_count`(2726행 무조건발동일) · `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`(3322행) · `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`(7031행) 세 항목의 `무조건발동일`을 실제 발동일로 옮기고, `mahdi/main.py:923` 위에 `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`용 "실제로 켠 날: 2026-09-02(커밋 910c1e1)" 주석을 추가한다.
- 회귀 위험: 낮음(주석·yaml 값만). 검증: 다음 증거 수집기 실행에서 §7 "날짜 정리 필요" 목록에서 셋 다 빠지는가.

### P2-2 (이월). 이벤트 캘린더 `covered_through`를 9월분으로 갱신한다

- 대상: `mahdi/config/event_calendar.yaml`. 9월 금통위·FOMC·CPI·고용지표·옵션 만기(09-03·09-10)를 실제 조사해 채운 뒤 `covered_through`를 그 마지막 날짜로.
- 사람의 사실 확인이 선행돼야 한다 — 자동 회차가 만기일 이상은 채울 수 없다.

### P1-2 (이월). `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`에 09시 포함 여부

- 대상: `mahdi/main.py:268`. 안 A 채택 시 `{9: 4, 10: 4, 11: 4, 12: 4, 13: 4, 14: 4}`.
- 오늘도 결정되지 않았다 — 결정하면 적용은 다음 07:30 기동(09-04)부터다.

### P2-3 (신규, 근거 1-9). 워치독 DEGRADED 문구에 과거 회복 이력 참고정보를 추가한다

- 대상: `scripts/watchdog_observation_loop.py`의 DEGRADED 경보 문자열 생성부.
- 내용: 같은 유형(no_ingest) 경보가 나올 때, 과거 표본 3건의 평균 회복 소요시간(약 45~53분)과 회복이 몰린 시각대(15:25~15:26 부근, 정규장 마감+6분)를 참고 문구로 함께 표시한다. **자동 조치를 추가하는 것이 아니라 사람이 상황을 판단할 근거를 하나 더 주는 것이다.**
- 회귀 위험: 낮음(문자열 추가만). 검증: 다음 no_ingest 경보 발생 시 문구에 참고 소요시간이 표시되는가.

---

## 제5부 — 고도화 방안 (당일 관측에서 나온 것만)

### 고도화 1. 절벽 회복 시각이 세 번째로 "정규장 마감+6분"과 정확히 일치했다 — KIS 쪽 정기 처리 가능성을 조사한다 (근거: 1-9)

- **관측 근거**: 08-26·09-01·09-03 세 번의 옵션체인 절벽이 전부 15:25~15:26 무렵 자연 회복했다. 원인·규모·시작 시각은 매번 달랐는데 회복 시각만 세 번 다 같았다.
- **제안**: 다음 절벽 발생 시 그 시각대의 KIS 시스템 점검·정산 배치 공지가 있는지 확인하는 절차를 `cliff_episodes.md`의 "적는 법"에 추가한다. 확인되면 P2-3(워치독 참고 문구)의 신뢰도가 "우연의 패턴"에서 "알려진 원인"으로 올라간다.
- **비용·위험**: 낮음(조사 항목 추가). **우선순위**: P2.

### 고도화 2. 경고선→위험선 간격 0분이 2/3로 재발했다 — "선행 신호"를 "동시 신호"로 재정의할지 검토한다 (근거: 1-7·1-9)

- **관측 근거**: `cliff_episodes.md`가 이미 "두 번째로 0분이 나오면 선행 시간은 그냥 없는 것"이라고 적어 뒀고, 오늘이 그 두 번째다.
- **제안**: `docs/동작점검/README.md`의 지연 경고선(P1-4) 문구를 "선행 신호"에서 "동시 신호일 수 있음"으로 재정의할지 검토(`NEXT_TODO.md`에 이미 후보로 등재돼 있음, 오늘 표본으로 근거가 하나 더 늘었다).
- **비용·위험**: 낮음(문서 재정의 + 관련 서술 수정). **우선순위**: P1.

### 고도화 3. 대가 지표가 다른 사건에 오염될 수 있다는 것이 오늘 실제로 드러났다 — 오염 플래그를 병기한다 (근거: 1-1)

- **관측 근거**: `2026-08-12-eE-on-congested-hours`의 대가 지표(`gex_input_missing_minutes` 50분)와 `2026-09-02-fix10-phase-offset-55s`의 주장 지표(`chain_newest_age_seconds.p50`)가 둘 다 오늘의 옵션체인 절벽 때문에 순수하게 그 레버만의 효과로 읽을 수 없게 됐다.
- **제안**: 자동 지표(`daily_ops_report.py`)가 그날 절벽(rows=0 연속 20분 이상) 여부를 감지하면, 그날 산출되는 모든 가설 검정 행에 "⚠ 오늘은 절벽 발생일 — 대가 지표 해석 주의" 플래그를 자동으로 병기한다.
- **비용·위험**: 낮음(플래그 열 추가, 판정 로직 자체는 안 건드림). **우선순위**: P2.

---

## dev_memory 반영 초안 (이 회차가 실제로 옮겨 적을 대상)

### DECISION_LOG.md 추가 후보

```
## 2026-09-03 — 옵션체인 수집 절벽 4호, 회복 시각이 세 번째로 「마감+6분」과 일치

### [MW0601] 결정: cliff_episodes.md 4번째 표본 확정, 경고선을 「동시 신호」로 재정의 검토 착수

**증상.** 14:31~15:23(53분) 옵션체인 rows=0 연속. 경고선(0.8배)·위험선(1.0배)이 같은 창(14:35)에서
동시 돌파(간격 0분, 09-01에 이어 2/3 재발). 워치독 45분 연속 DEGRADED(14:40~15:24) 후
15:25:01 RECOVERED. 08-26·09-01·09-03 세 표본 모두 회복 시각이 15:25~15:26(정규장 마감+5~6분)로 일치.

**원인.** REST(옵션체인·시세조회) 단일 채널 지연/타임아웃, WS는 정상(0건 단절) — KIS 쪽으로 추정.
당일 커밋 0건, 우리 쪽 변경 없음.

**결정.** 코드 변경 없음(재기동은 이 문제를 고치지 못하고 예열 대가만 발생시킨다 — 과거 세 번과 동일 결론).
`cliff_episodes.md`에 4번째 열 추가, 「간격 0분 재발」·「회복 시각 3연속 일치」 두 관측을 공식 기록으로 승격.

**Why.** 오늘 매매 실행 경로가 배선되지 않아 실손실은 0원이었지만, 배선 이후 같은 상황에서는
Kill Switch(data_quality_fail)의 첫 실전 검증 대상이 된다. 회복 시각 패턴은 향후 경보 문구에
참고정보로 활용 가능하다(P2-3).

**How to apply.** `cliff_episodes.md` 표에 09-03 열 추가(제4부 P1-A 값 그대로). 착수 전 새 예측치는
필요 없음(기존 관측 기록 절차).

**검증.** 다음(5번째) 절벽 발생 시 회복 시각이 다시 15:25~15:26 부근인지, 경고-위험 간격이
계속 0분인지가 이 결정의 실질 검증이다.
```

### NEXT_TODO.md 추가 후보

```
- [ ] `docs/동작점검/cliff_episodes.md`에 09-03을 4번째 열로 추가한다(제4부 P1-A, 값 준비됨).
- [ ] `docs/동작점검/README.md`의 지연 경고선(P1-4) 문구를 「선행 신호」에서 「동시 신호일 수 있음」으로
      재정의할지 검토 — 09-01·09-03 두 표본이 간격 0분(기존 후보 항목, 오늘 근거 추가).
- [ ] `scripts/watchdog_observation_loop.py` DEGRADED 문구에 과거 회복 이력(평균 45~53분,
      15:25~15:26 부근) 참고정보 추가(제4부 P2-3).
- [ ] `hypotheses.yaml` `2026-09-02-fix10-phase-offset-55s` — 절벽 없는 정상일에 재실측 후
      최종 확정(오늘은 오염 의심으로 판정 보류 제안).
- [ ] `daily_ops_report.py`에 절벽일 자동 감지 + 가설 검정 행 오염 플래그 병기(제5부 고도화 3).
- [ ] 09시 혼잡시간대 포함 여부·레버 E 최종 확정 — 5일째 이월, 결정 자체가 필요(코드 작업 아님).
```

### CURRENT_STATE.md 갱신 후보

```
### 2026-09-03 — 옵션체인 절벽 4호 (14:31~15:23, 53분) — 정규장 마감+6분에 회복, 3연속 동일 시각
회복 시각이 08-26·09-01·09-03 세 표본 모두 15:25~15:26으로 일치 — KIS 쪽 정기 처리 가능성 시사.
경고선-위험선 간격 0분 2/3 재발 — 「선행 신호」 재정의 검토 대상(NEXT_TODO).
오늘 매매 실행 경로 미배선으로 실손실 0원 — 배선 후 이런 상황이 Kill Switch(data_quality_fail)의
첫 실전 검증 대상이 된다.
```

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다. 이 절이 오늘 하루 전체(장전~장후)를 대신한다 — 앞 국면 절의 「사용자 조치」는 이 절로 흡수됐다.

### 지금 (장 마감 직후)

**사람 조치 없음.** 절벽은 해소됐고, 오늘 거래가 없었으므로 손실도 없다. 종료 시퀀스(15:30~15:45)도 정상이었다.

### 오늘 중 (급하지 않음)

1. **`hypotheses.yaml` 13건의 상태를 확정한다.** (근거: 1-5, 위 「가설 판정 제안」 표) — 12건은 이 회차의 제안대로 확정 가능하고, `2026-09-02-fix10-phase-offset-55s` 1건은 판정 보류(예정일 연기)를 제안한다.
2. **`cliff_episodes.md`에 09-03을 4번째 열로 추가한다.** (근거: 1-9, 제4부 P1-A) — 표에 넣을 값은 전부 준비돼 있다.
3. **dev_memory(DECISION_LOG·NEXT_TODO·CURRENT_STATE) 세 파일에 위 초안을 반영한다.** (이 회차는 초안만 남기고 직접 커밋하지 않는다)
4. **`OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`에 09시를 넣을지 정한다.** (근거: 1부 P1-2, **5일째 이월**) — (A) 09시 추가: 09시 공백 해소. (B) 유지: 위클리 커버리지 보존. 오늘 정해도 적용은 내일(09-04) 07:30 기동부터다.
5. **레버 E 가설의 최종 상태를 확정한다.** (근거: 1-1, 09-02가 낸 제안대로 주장 축 `refuted`·대가 축 `confirmed(목표 미달)` 분리 종결 검토) — `stale_pct` 지표 설계 문제(§1-11 지속 논의)를 먼저 볼 것을 권한다.
6. **이벤트 캘린더 `covered_through`를 9월분으로 갱신한다.** (근거: 1-2, **6거래일째 이월**, 오늘도 옵션 만기일이었다)
7. **`.claude/` · `dashboard_build_prompt_1.md` 커밋 여부를 정한다.** (**9일째 이월**)

### 결정이 필요한 것 (자동으로 못 하는 것)

8. **ZN/ES 해외선물 거래소 이용 신청 여부.** (만성, 1-4) — (A) CME/CBOT SUB거래소 신청 (B) 매크로 축에서 공식 제외.
9. **매매 실행 경로 배선 시점.** (근거: 1-10) — 오늘 같은 절벽이 실거래 중에 발생하면 그것이 Kill Switch(`data_quality_fail`)의 첫 실전 시험대가 된다. 배선 전에 이 서킷브레이커의 발동 조건·수동 개입 절차를 한 번 점검해 둘 것을 권한다(급한 결정은 아니다).

### 확인 완료 — 더 볼 필요 없는 것

- **Claude 앱 예약 4종의 프롬프트 새 파일명 반영.** 장전·장중①이 이미 확인했고(위 171~173행), 이번 장후 프롬프트도 `{날짜}_마흐디_일일점검.md`를 정확히 지시하고 있었다 — **4종 전부 확인 완료.**

---

*이 파일은 2026-09-03 하루(장전 07:30 ~ 장후 15:45)의 종합 완성본이다. 국면별 세부 절(제1부·제2부·제3부)의 본문은 작성 당시 그대로 보존돼 있으며, 판정이 바뀐 자리에는 뒤 절로의 포인터가 남아 있다.*


---

## 제6부 — 장후 자동조치 구현 결과 (16:26~17:0x · 자동)

> 이 절은 **append 전용**이다 — 앞 회차 본문은 한 글자도 고치지 않았다.
> 착수 전 `NEXT_TODO.md`의 폐기 목록을 통째로 읽고 시작했다.
> ⛔ **Slack 토글은 켜지 않았다.** §3-1이 「버려진 경보 2건」을 인용했지만 스스로
> *"Slack을 켜라는 결론은 아니다"*라고 적었고, 이 세션은 2026-08-01 보류 결정을 따랐다.

### §0. 가드 통과 근거

| 가드 | 결과 |
|---|---|
| 장후 절 존재 | ✅ 제3부·제4부 있음 |
| 완결 표기 | ✅ 파일 끝 *"이 파일은 2026-09-03 하루(장전 07:30 ~ 장후 15:45)의 종합 완성본이다"* |
| 중복 실행 | ✅ 상태파일 최신이 09-02 — 오늘 첫 실행 |
| 장중 금지 | ✅ 16:26 착수(15:50 이후) · 마흐디 파이썬 프로세스 0 |
| 브랜치 | ✅ `master` · 착수·종료 시 `.git/index.lock` 없음 |
| 병행 세션 | ⚠ **있다** — 아래 §6 참고. 당일 커밋은 0건이었으나 워킹트리가 움직이고 있었다 |

### §1. 구현 완료 — 6건 (왜 · 걸린 시간 · 안 하면)

**P1-A. `cliff_episodes.md`에 09-03을 4번째 열로 추가.**
왜 — 표본이 넷이 됐고 그 파일이 스스로 걸어 둔 두 질문("간격 0분이 재발하는가", "회복이
계속 마감+6분인가")에 오늘 **둘 다 답이 나왔다**(그렇다 2/3 · 그렇다 3/3).
걸린 시간 — 약 10분(값은 제4부 P1-A에 전부 준비돼 있었다).
안 하면 — 로그는 이틀치만 남는다. **오늘 안 적으면 이 표본은 영영 못 만든다.**
⚠ **경고선 재정의는 이 파일이 하지 않았다** — 조건이 성립했다는 사실만 적고, 규약
(`README.md` P1-4) 개정은 사람 결정으로 `NEXT_TODO.md`에 올렸다.

**P1-B. §5-2 이탈 적신호가 「형태 무변화」와 「진짜 미관측」을 가른다.**
왜 — §1-6의 오탐(「63분째 미관측」)은 축이 죽은 게 아니라 **형태가 안 바뀌어 로그가 조용했던
것**이었다. `member_last_seen`은 형태 전이 줄에서만 갱신되므로 **판단이 멀쩡할수록 이 적신호가
잘 뜬다.** 가르는 재료(`scan.cycle_minutes`)는 이미 그 파일에 있었다.
걸린 시간 — 약 25분(순수 함수 `_dropout_flag()`로 뽑아 테스트 가능하게 했다).
안 하면 — **오탐이 진짜 정지를 덮는다.** 08-14 오후의 진짜 이탈이 다음에 오면 같은 문구에 묻힌다.
⛔ 임계(`MEMBER_DROPOUT_ALERT_MIN = 30`)는 안 건드렸다 · **적신호를 지우지도 않았다**(문구만 갈랐다).

**P2-3. `no_ingest` DEGRADED 경보가 과거 회복 이력을 인용한다.**
왜 — 표본 3건이 45~85분 만에 **15:25~15:26**(마감+5~6분)에 자연 회복했다는 사실이
사람 머릿속에만 있었다. 오늘 워치독은 DEGRADED를 **45회** 내면서 한 번도 그 말을 안 했다.
걸린 시간 — 약 20분(억제 포함).
안 하면 — 다음 절벽에서 사람이 「지금 재기동할까」를 **근거 없이** 정한다. 08-26이
자동 재기동을 안 켠 것이 옳았다고 증명했으니, 남은 것은 사람에게 근거를 주는 일이다.
⚠ **억제를 함께 넣었다** — 에피소드 첫 분 + 30분마다 1회. 45분 사건에 **2줄**이다
(매 줄에 붙였으면 45줄). ⛔ 자동 조치는 추가하지 않았다.

**P2-1(부분). `main.py`의 레버 두 줄에 「실제로 켠 날」 주석.**
왜 — 증거 §7이 *"실제로 켠 날짜를 그 줄 주석에 적을 것"*을 요구한다. 레버 F는 08-26에
`strategy_params.yaml:87`에 이미 있었고, 나머지 둘이 비어 있었다.
걸린 시간 — 약 10분. 안 하면 — Fix#10처럼 **「켜짐」이 「적용됨」으로 읽히는 3주**가 또 생긴다.
⚠ **계획을 다시 세웠다** — 아래 §3.

**고도화 1. `cliff_episodes.md` 「적는 법」에 KIS 공지 확인 절차.**
왜 — 회복 시각 3연속 일치가 「우연의 패턴」인지 「알려진 원인」인지는 **우리 로그만으로는
영영 못 가른다.** 걸린 시간 — 약 5분. 안 하면 — 다섯 번째 표본이 와도 같은 자리에 머문다.
⛔ **확인 못 했으면 「확인 못 함」이라고 적게 했다**(빈칸·「없음」 금지 — 규약 C).

**고도화 3. §0-1 가설 표 위에 절벽일 오염 주의 병기.**
왜 — 오늘 실제로 두 가설(`eE-on-congested-hours`의 대가 축 · `fix10`의 주장 축)이 절벽에
오염됐다. 지금까지 그 오염을 **사람이 매번 기억해서 발라내야** 했다.
걸린 시간 — 약 30분(그중 절반이 아래 §3의 재계획).
안 하면 — 기억이 빠지는 날 규약 E가 막으려는 것(대가를 다른 사건의 값으로 재는 것)이 일어난다.
⛔ **판정은 한 글자도 안 무른다** — 플래그는 표 **위의 한 줄**이다.

### §2. `hypotheses.yaml` — 등재 3건 · 확정 8건 · 보류 1건

**신규 등재(코드보다 먼저, 숫자를 보기 전에):**
`2026-09-03-p2-3-watchdog-cites-recovery-history` · `2026-09-03-p1-b-dropout-cross-checks-the-cycle-log`
· `2026-09-03-adv3-cliff-day-flags-the-hypothesis-table` — 셋 다 검증예정일 **2026-09-04**.

**`confirmed` 확정 8건** — 「가설 판정 제안」 표가 **판정 제안과 근거를 함께** 낸 것만 옮겼다:
`2026-08-21-p1-latency-recovers-with-the-session` · `2026-08-25-fix8-v2-watchdog-cost-is-cadence-not-count`
· `2026-08-26-adv7-cliff-episodes-accumulate` · `2026-08-31-p1-3-a-missing-round-is-reconstructed-not-erased`
· `2026-08-31-adv1-the-close-does-our-ab-test-every-day` · `2026-09-02-p1-3-shape-transition-names-the-zero-axes`
· `2026-09-02-p1-4-red-flags-count-consecutive-p95-windows` · `2026-09-02-p1-5-freshness-has-a-continuous-axis`
(각 항목의 `검정`에 이 표의 근거를 그대로 인용했다.)

**판정 보류 1건** — `2026-09-02-fix10-phase-offset-55s`: `상태`는 `pending` 그대로 두고
검증예정일만 **09-04**로 옮겼다. ⛔ **기준 55.0초는 안 건드렸다** — 2.4초가 모자라니 57.0으로
올리자는 것이 이 체계가 가장 경계하는 형태다. 55.0은 08-19가 **숫자를 보기 전에** 적은 값이다.

### §3. 계획을 다시 세운 것 둘 — 리포트대로 하지 않았다

**(가) 고도화 3은 「축 신설」이 아니라 「배선」이었다.**
착수 계획은 로그 쪽에 최장 연속 축을 새로 다는 것이었고 예측치도 그 경로로 등재했다.
착수 직전 확인에서 **같은 것을 재는 축이 이미 있었다** —
`db.chain_minute_coverage.zero_row_longest_run.length`(2026-08-14 Fix#3), 09-03 실측
**53분(14:31~15:23)**으로 이 회차가 손으로 센 값과 **정확히 같고**, 임계까지
`ZERO_ROW_RUN_ALERT_MINUTES = 20`으로 정해져 있었다. 신설분은 `git restore`로 되돌리고
있는 축을 읽도록 배선했다. `hypotheses.yaml`의 예측 경로도 실재하는 쪽으로 **정정**하고
정정 사유를 그 자리에 적었다(09-02 P2-B와 같은 결의 처분).
⚠ **덤으로 임계를 새로 만들 뻔한 것을 면했다** — 20분을 이 항목이 정했다면 그것은
「표본을 보고 임계를 만드는 것」이었다.

**(나) P2-1의 검증 문장(「§7 목록에서 셋 다 빠지는가」)은 성립하지 않는다.**
① **날짜는 이미 옮겨져 있다** — `use_effective_member_count` 08-23 · `OPTION_CHAIN_SLOW_…` 08-31
· `SIGNAL_FUSION_PHASE_OFFSET_SECONDS` 09-02, 셋 다 `발동일`과 `무조건발동일`이 일치한다.
② 그런데도 안 빠진다 — §7의 조건이 `무조건발동일 < 오늘 AND 레버가 켜짐`이라
**한 번 켜진 레버는 날짜를 어디로 옮겨도 영원히 이 목록에 남는다.**
그래서 **실제로 남은 일은 주석뿐이었고 그것만 실었다.**
⚠ 이 예측을 그대로 등재했다면 다음 거래일에 **구현이 옳은데도 `refuted`가 났을 것이다.**
근본 해결안(`발동일 != 무조건발동일`로 판정 변경)은 리포트 제안 범위 밖이라
**착수하지 않고** `NEXT_TODO.md`에 올렸다.

### §4. 이미 반영돼 있어 안 한 것

- **P2-1의 yaml 3건** — `발동일`·`무조건발동일`이 셋 다 이미 실제 발동일이다(08-31·09-02 정리분).
- **레버 F의 「실제로 켠 날」 주석** — `strategy_params.yaml:87`에 08-26부터 있었다.

### §5. C등급으로 미룬 것과 사유 (전부 `NEXT_TODO.md`에 등재)

| 항목 | 왜 안 했나 |
|---|---|
| **P1-2** 09시 포함 여부 | `### 결정이 필요한 것` — 사람 결정. **6일째 이월** |
| **P2-2** 이벤트 캘린더 `covered_through` | **사람의 사실 확인이 선행**돼야 한다(9월 일정 실제 조사) |
| **고도화 2** 경고선 「동시 신호」 재정의 | 규약(`README.md` P1-4) 개정은 사람 결정. 오늘은 **조건 성립 사실만** 기록 |
| 레버 E 가설 최종 상태 | 제안이 「부분 확인(대가 오염)」으로 갈렸고 사용자 조치 5가 조건을 달았다 |
| `2026-08-26-p1-3` 상태 | 제안이 「부분 확인(다른 경로)」 — 확정 문구가 아니다 |
| `fix7-regime-…` · `fix2-roundtrip-…` 예정일 연기 | 옮길 **날짜가 아직 없다**(「레버 켠 다음 날」·「거래 발생일」) — 날짜 없는 연기는 「잊힌 유예」다 |
| §7 목록 구조 문제 | 원인은 확정했으나 **수집기 판정 변경**이라 제안 범위 밖 |

**폐기 목록 위반으로 기각한 것: 없다.** 오늘 리포트는 폐기 항목을 되살리지 않았다.

### §6. ⚠ 병행 세션 — 이 회차가 커밋하지 않은 변경이 워킹트리에 있다

착수 시점(16:26)에는 없던 수정이 **16:33~16:35에 나타났다**:
`mahdi/dashboard/{app,data_source,panels/gamma_map_panel}.py` ·
`tests/test_dashboard_{data_source,panels}.py` (5파일, +259/-5).
감마맵 낡은 값 건(`CURRENT_STATE.md` 09-03 첫 절)으로 보인다.
**이 회차는 손대지 않았고 커밋에도 넣지 않았다** — `git add`는 바꾼 경로만 명시했다.
그 세션이 자기 커밋으로 가져가면 된다.

### §7. 테스트

`pytest tests/` — **2,195 passed · 0 failed** (09-02 2,166 → +29. 이 회차 신규 **23건**,
나머지는 위 병행 회차분). 되돌린 항목 없음.

| 새 테스트 | 무엇을 못 박는가 |
|---|---|
| `test_watchdog_recovery_hint.py` (9) | 45분 사건에 참고 문구 **2줄**(억제) · 파서 집계 **무변경** |
| `test_evidence_collector_dropout_split.py` (6) | 사이클이 끊긴 구간에서는 **종전 문구 그대로**(판정 무변경) · 임계 30분 불변 |
| `test_ops_report_cliff_flag.py` (8) | 절벽 없는 날 §0-1 출력 **바이트 동일** · 절벽일에도 표의 `판정` 열 **무변경** |

### §8. 사용자 몫으로 남은 것

1. **이벤트 캘린더 `covered_through` 9월분** — **7거래일째.** 실제 일정 확인이 필요하다.
2. **09시 혼잡시간대 포함 여부** — **6일째.** ⚠ 안 켜기로 정하면 **사유를 보고서에 적을 것.**
3. **레버 E 가설 최종 상태** — `stale_pct` 지표 설계 문제를 먼저 볼 것(사용자 조치 5의 조건).
4. **경고선을 「동시 신호」로 재정의할 것인가** — 오늘로 근거가 2/3가 됐다.
5. **ZN/ES 거래소 신청 여부** · **매매 실행 경로 배선 시점**(결정이 필요한 것 8·9).
6. **`.claude/` · `dashboard_build_prompt_1.md` 커밋 여부** — **10일째 미결정.** 손대지 않았다.

### §9. 커밋

| # | 무엇 |
|---|---|
| ① | 점검 산출물 — 이 보고서 + `auto/2026-09-03_지표.json` |
| ② | `hypotheses.yaml` — 예측 3건 선등재 + 확정 8건 + 보류 1건 |
| ③ | P1-A · 고도화 1 — `cliff_episodes.md` |
| ④ | P1-B · P2-3 · P2-1 · 고도화 3 — 코드 4파일 + 테스트 3파일 |
| ⑤ | dev_memory 3파일 |

*해시는 아래 커밋 로그 참고 — 이 절을 쓴 뒤에 ①이 만들어지므로 여기 적지 않는다.*
