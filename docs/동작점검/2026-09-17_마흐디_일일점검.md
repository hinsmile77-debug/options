# 마흐디 일일점검 — 2026-09-17 (목)

- 국면: 장전(pre) · 예정 08:00 · 실행 08:02 — **정시 실행** (10분 이내, 밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-17_증거_pre.md`
- 리포 HEAD: `4b91b52`(브랜치 `master`) · 당일 커밋 **0건** · 미커밋 **26건**(§1-2에서 다룬다)

---

## §0. 오늘 한 장

**오늘 프로그램은 정상 작동했다.** 07:30 장전 기동(도커·DB 마이그레이션 42개·관측 루프)이 문제 없이 끝났고, 07:30~08:02 사이 오류(ERROR) 로그는 **0건**, 시세 수집 지연도 없었다(응답시간이 제한시간의 1~2% 수준). 로그가 끊긴 구간도 없다.

**사람이 지금 해야 할 일은 없다.** 켜야 할 레버도 없다(오늘 필요한 레버는 이미 전부 켜져 있다).

**문제 5건 — 전부 이미 알려진 만성 문제이고, 새로 찾은 것은 0건이다.** 오늘 판정을 기다리는 가설(예측)이 6건 있는데, 그중 4건은 판정 시한이 이미 지났고(사람이 아직 결론을 못 내렸다는 뜻), 2건은 오늘이 판정 시한이지만 장중에 특정 상황(감시 경보·거래량 급감)이 실제로 발생해야만 판정할 수 있다 — 장중·장후 회차가 이어받는다.

(관측 범위: 07:30~08:02. 이 범위 밖에서 무슨 일이 있었는지는 이 회차가 답할 수 없다 — 「없다」가 아니라 「모른다」다.)

**[12:30 장중 갱신]** 정오까지 프로그램은 정상 작동 중이고, 지금 당장 사람이 해야 할 일은 없다(관측 범위 07:30~12:35). 오류(ERROR)는 오늘 0건, 시세가 아예 끊기는 사고(과거 최장 89분)도 없었다. 다만 오전 내내 시세 조회 응답 속도가 여러 차례 제한시간(4.0초)에 바짝 붙었다 — 이 자체는 최근 며칠째 반복되는 패턴인데, 자동 계산기는 "어제와 오늘, 이틀째 같은 현상"이라고 세지만 실제로는 그제(9/15)부터 **사흘째**다(§1-6, 세는 도구가 하루씩만 비교해서 생긴 차이). 그 시간대는 이미 속도 조절 장치가 켜져 있어 당장 추가로 켤 것은 없다. 새로 찾은 문제는 경미한 것(P2) 1건, 심각한 것은 0건이다.

**[14:30 장중 갱신]** 14:40 시점까지도 프로그램은 정상 작동 중이고, 멈추거나 끊긴 적이 없다. 다만 오후 들어 시세 조회 응답 속도가 오늘 처음으로 "경고 수준"에 닿았다 — 14:26분 구간에서 응답시간의 중앙값이 제한시간의 84%까지 올라갔고(그 앞뒤로는 한 뼘 아래), 같은 시각 옵션 시세 조회가 통째로 한 번 실패했다가 5초 뒤 재시도로 회복됐다. 그래도 실제로 시세가 통째로 끊긴 순간(0건 적재)은 오늘 하루 동안 한 번도 없었다. **개장 마감(14:50, 신규 진입 마감 기준) 전에 사람이 지금 당장 해야 할 일은 없다** — 다만 이 흐름이 계속되면 장후 회차가 재점검할 필요가 있다(§14:50 컷오프 권고 참조). 새로 찾은 문제는 경미한 것(P2) 2건, 심각한 것은 0건이다.

**[장후 마감]** 오늘 프로그램은 07:30 기동부터 15:45 종료 배치까지 **한 번도 끊기거나 죽지 않고 정상 작동했다.** 오늘 하루 실제 거래는 **0건**이다 — 주문을 넣는 경로 자체가 아직 배선되지 않은 "관측 전용" 단계라 이것은 정상이다. 개장 마감 직후(15:00~15:01) 옵션 시세가 2분간 통째로 안 들어온 순간이 딱 한 번 있었지만, 설계된 대로 직전 시세를 대신 써서 판단이 멈추지는 않았다. **사람이 지금 해야 할 일은 없다.** 오늘 하루 전체로는 문제 10건(심각 0 / 보통 8 / 정확성 훼손 2)을 확인했고, 그중 2건(옵션체인 2분 전멸, 오후 응답지연이 위험선에 더 바짝 붙은 것)이 이 장후 회차에서 새로 찾은 것이다. 판정을 기다리던 가설 2건은 오늘도 그 일이 일어나지 않아 판정하지 못했다 — 나쁜 소식이 아니라 "잴 상황 자체가 없었다"는 뜻이다.

---

## 제1부 — 장전 (07:30 ~ 09:00)

### 1-0. 전일 이월

오늘이 이 파일을 새로 만드는 첫 회차라 「본문을 고치지 않고 이월 처분한다」는 규칙이 적용될 앞 국면 절이 없다. 대신 **전일(2026-09-16) 보고서가 「다음 거래일 관측 예정」으로 남긴 것**을 아래에서 확인한다(phases.md A-3).

| 전일이 남긴 것 | 오늘 아침 확인 | 판정 |
|---|---|---|
| 전일 `auto/2026-09-16_지표.json` 존재 여부 | 있음(85.2KB, 09-16 15:46 생성) | ✅ 전일 델타 계산 가능 |
| 미커밋 26건(09-16 기준) 정리 결정 — 8일째 | 오늘도 미결정, 26건 그대로 | 🔄 지속 → §1-2 |
| ZN(미국 10년 국채선물)·ES(E-mini S&P500 선물) 조회 실패 — 만성 | 오늘도 동일 사유로 실패 | 🔄 만성 → §1-3 |
| 이벤트 캘린더 9월분 미갱신 — 20일째 | 오늘도 그대로(의도적 방치, §1-4 참조) | 🔄 지속 → §1-4 |
| 가설 4건(2026-08-12·08-19·08-26 등) 판정 시한 초과, 손 판정 대기 | 오늘도 pending, 시한 초과 일수만 늘었다 | 🔄 지속 → §1-5 |
| `hypotheses.yaml` 언급-실재 불일치 1건(`2026-08-26-p1-2-1`) — 09-16 보고서는 "내일(09-17)부터 자동 소멸"이라고 예고 | **예고가 틀렸다** — 오늘도 그대로 뜬다 | ↩️ 정정 → §1-1 |
| `2026-09-16-recovery-hint-carries-its-sample-count` · `2026-09-16-cliff-day-marks-the-hypothesis-table-itself` 두 가설 — 검증 시한이 오늘(09-17) | 오늘 아직 판정 상황(감시 경보·절벽) 미발생 | 🔄 이월 → 「오늘 관측이 답해야 할 것」 |

### 장전 체크리스트 (phases.md A절)

**A-1. 기동 — 전부 정상**
- 07:30:00 `start_mahdi_premarket.bat` 실행, 07:30:07 도커 데스크톱 기동, 07:30:42 도커 데몬 준비, 마이그레이션(DB 구조 갱신) 42개 전부 적용 완료(07:30:45~07:30:54대)
- `.last_successful_start.txt`: 2026-09-17T07:31:21 — 오늘 시각 ✅
- 관측 루프(시세를 계속 받아 판단하는 프로그램) 기동 표식 **1회만** 확인 — 장중 재기동 없었다는 뜻
- 워치독(watchdog — 관측 루프가 죽었는지 1분마다 확인하고 되살리는 감시 프로세스): 당일 3행 전부 OK, 이상(DEGRADED) 0건. 호출 189회(idle 166 · ok 23), 5분 이상 공백 없음

**A-2. 오늘의 계획**
- 검증 시한이 오늘인 가설 2건, 시한이 지난 pending 가설 4건 확인함 — §1-5, 「오늘 관측이 답해야 할 것」 참조
- `NEXT_TODO.md`의 「켤 것」 레버: 오늘 새로 켤 레버 없음. 기존 레버(회원수 보정, 신호융합 위상차 55초, 9~14시 혼잡 시간대 완화) 모두 이미 켜진 지 오래(발동 완료 각각 08-23·09-02·08-31)
- 전일 커밋 시각(가장 최근 09-16 16:28) < 관측 루프 오늘 기동 시각(07:31) — 조건 충족, 최근 fix들이 오늘 프로세스에 실려서 돈다
- 사전 대응 규칙(9~14시 혼잡 완화)이 아직 안 덮은 시간대: **판정 불가**(규약 C — 점검 규약집이 정한 원칙 중 하나로, "0건"이라는 숫자가 나오면 그것이 "진짜 없었다"인지 "잴 재료가 아직 없다"인지 반드시 구분한다) — 08:02 시점엔 오늘 자체 지연 계측이 07~08시분밖에 없어 비교 대상이 부족하다. 매일 이 시각엔 나오는 정상적인 「모름」이지 이상점이 아니다
- **`--verify-report`로 전일(09-16) 보고서 자기검증**: 폐기 목록과 부딪히는 줄 0건, 지목한 파일 경로(6개) 전부 실재 — 이상 없음

**A-3. 전일 이월** — 위 1-0 표에서 처리함

**개장 전 사람 조치: 불필요** (관측 범위 07:30~08:02)

### 1-1. `hypotheses.yaml` 언급-실재 불일치 재현 — 어제의 "내일 소멸" 예고가 틀렸다

**증상**: 오늘 증거 §8-1이 "직전 보고서가 언급했는데 `hypotheses.yaml`에 없는 id 1개: `2026-08-26-p1-2-1`"을 다시 낸다. 09-16 보고서는 이 재현을 "예상된 것이며 내일(09-17)부터 자동으로 사라진다"고 명시적으로 예고했다.

**근거**: `docs/동작점검/auto/2026-09-17_증거_pre.md` §8-1. 이 검사기는 **직전 보고서 파일 전체 텍스트**에서 가설 id 패턴을 찾는데(`docs/동작점검/tools/collect_evidence.py:2794` `report_hypothesis_ids(text)`), 09-16 보고서 본문 안에 옛 id `2026-08-26-p1-2-1`이 설명문 형태로 **네 곳**(§1-6 절 등, 09-16 보고서 145·149·238·357·505행)에 그대로 인용돼 있다. 검사기는 미완료 체크박스뿐 아니라 본문 전체를 훑으므로, 09-16 보고서가 09-17의 "직전 보고서"로 남아 있는 한 이 재현은 계속된다.

**기준**: 09-16 보고서 §1-6은 "내일(09-17) 장전부터 이 적신호는 자동으로 사라진다"고 적었다(09-16 보고서 153행). `docs/동작점검/tools/collect_evidence.py:2794`의 실제 스캔 범위(본문 전체)와 그 예고가 어긋난다.

**영향**: 실질적인 피해는 없다. 사람이 매일 이 목록을 한 번 더 눈으로 걸러야 하는 수고가 하루 더 늘어날 뿐이다. 다만 "왜 아직도 뜨는가"를 오늘 이 회차가 짚어 두지 않으면 내일도 "왜 안 없어지지"로 반복해서 헷갈릴 수 있다.

**심각도**: P2. 이 보고서(오늘, 2026-09-17) 본문에서는 그 옛 id를 굳이 다시 인용하지 않았으므로, **내일(09-18)부터는 정말로 사라질 것으로 예상한다.**

### 1-2. 미커밋 26건 — 사람 결정 대기, 9일째로 추정

**증상**: `git --no-optional-locks status --porcelain` 26줄. 수정 4건(`mahdi/dashboard/app.py`·`data_source.py`·`scripts/daily_ops_report.py`·`scripts/start_mahdi_premarket.bat`) + 미추적 22건(연구 문서 5개, `premarket_levels` 계열 스크립트·모듈 다수, `.claude/`, 개인 메모 1건).

**근거**: 위 4개 수정 파일의 최종 수정 시각은 전부 **2026-09-06**로, 오늘 새로 생긴 변경이 아니다(`stat` 확인). 09-16 보고서 §1-5가 "미커밋 27건, 8일째"로 기록했다(`docs/dev_memory/NEXT_TODO.md:58,86,100`). 오늘 26건으로 구성은 동일(09-16 보고서 자신이 미커밋 대상이었던 만큼 그 파일이 커밋되며 27→26으로 자연 감소한 것으로 보인다 — diff 대조는 이번 회차에서 하지 않았다).

**기준**: `scripts/start_mahdi_premarket.bat`의 수정분이 **미추적 상태인** `scripts/premarket_levels_publish.py`를 부른다는 것이 이미 여러 회차에 지적돼 있다 — 다른 PC에서 이 배치를 돌리면 파일이 없어 실패할 수 있다.

**영향**: 지금 이 PC에서 도는 프로그램은 정상이다. 다만 이 상태로 다른 컴퓨터에 옮겨 돌리면 장전 기동 배치가 중간에 실패할 수 있고, 그 실패 원인이 "커밋 안 된 파일을 불렀다"라서 로그만 봐서는 바로 안 잡힐 것이다.

**심각도**: P2(만성, 9일째로 추정 — 정확한 시작일은 diff 미대조로 확정하지 못함). 새 발견이 아니다.

### 1-3. ZN·ES 해외선물 조회 실패 — 만성

**증상**: 07:32:35 "ZN(미국 10년 국채선물) 근월물 조회 실패: ZNUN — CBOT SUB거래소 신청 계좌가 아닙니다", 07:32:39 "ES(E-mini S&P500 선물) 근월물 조회 실패: ESUN — CME SUB거래소 신청 계좌가 아닙니다".

**근거**: `docs/동작점검/auto/2026-09-17_증거_pre.md` §4 WARNING 표.

**기준**: 증권사 계좌에 해당 해외거래소 권한이 아직 신청돼 있지 않다는 뜻 — 코드 문제가 아니라 계좌 설정 문제로 여러 회차에 걸쳐 이미 확인된 만성 항목이다.

**영향**: 매크로(거시경제) 스트레스 지표 중 미국 채권·주가지수 선물을 보는 축 두 개가 오늘도 비어 있다. 그 두 축이 프로그램의 주된 판단(옵션 매매 신호)에 지금 당장 필요한 것은 아니라 거래 자체에는 영향이 없다.

**심각도**: P2(만성). 새 발견 아님. 조치는 증권사 계좌 설정(코드 밖의 일)이라 사람이 신청해야 풀린다.

### 1-4. 이벤트 캘린더 9월분 미갱신 — 의도적 방치, 20일째

**증상**: 07:31:55 "이벤트 캘린더 미기입: not_covered — `covered_through`(2026-08-28)가 오늘(2026-09-17)보다 앞선다. 이벤트 근접 페널티가 걸리지 않는 상태다".

**근거**: `mahdi/config/event_calendar.yaml:70` `covered_through: "2026-08-28"`. 같은 파일 62행 주석: "`covered_through`는 08-28 그대로 두고, 경고가 계속 울리게 남긴다" — **의도적으로 경고를 계속 울리게 남긴 설계**다. 08-28부터 오늘까지 20일 지났다.

**기준**: `docs/동작점검/README.md` 규약 및 여러 회차 보고서가 "20일째"로 이미 언급한 만성 항목.

**영향**: 금리 발표 같은 굵직한 이벤트가 9월에 있어도, 그 날짜가 캘린더에 없으면 프로그램이 "이벤트 근접이니 몸을 사려야 한다"는 판단을 못 내린다. 다만 이것은 실수로 방치된 것이 아니라 "채워질 때까지 경고를 계속 울리게 하자"는 의도된 설계다.

**심각도**: P2(만성, 의도적). 9월 이벤트 목록을 실제로 채우는 것은 코드 밖의 일(외부 일정 확인)이라 사람이 해야 한다.

### 1-5. 검증 시한 초과 가설 4건 — 손 판정이 밀리고 있다

**증상**: `hypotheses.yaml`의 pending 가설 중 4건이 검증 시한을 넘겼다.

| id | 무엇을 확인하려던 가설인가 | 시한 | 오늘까지 지난 일수 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | 재기동해도 시세 판단이 처음부터(WARMUP — 시장 상황을 아직 못 익힌 예열 상태) 다시 시작하지 않게 하는 개선 | 2026-09-01 | 16일 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 주문 왕복(발주→체결확인) 실측이 DB에 증거로 남는가 | 2026-09-01 | 16일 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 혼잡 시간대 사전 대응 규칙이 정해진 시점(다음 거래일 장전)에 실제로 발동하는가 | 2026-08-27 | 21일 |
| `2026-09-15-eE-adds-nine-to-congested-hours` | 9시를 혼잡 시간대에 추가한 효과가 실제로 있는가 | 2026-09-16 | 1일 |

**근거**: `docs/동작점검/auto/2026-09-17_증거_pre.md` §8. `2026-08-12-fix7...`은 전제 레버(`REGIME_RESTORE_SESSION_WINDOW`)가 계속 꺼져 있어(§7) 다섯 번째 미실행 판정을 받은 상태이고, `2026-08-19-fix2...`는 그 사이 실주문이 없어 판정 재료 자체가 안 생겼다. `2026-08-26-p1-3...`은 발동 여부를 정할 시각(장전)이 3주 넘게 사람 결정 없이 지나간 경우다.

**기준**: `docs/동작점검/README.md` 소급 적용 금지 규약 — 시한이 지난 항목은 오늘 숫자로 판정하지 않고 `inconclusive`(판정 불가)로 닫거나 시한만 옮긴다. **이 판단(닫을지 옮길지)은 사람이 손으로 확정해야 한다** — 이 회차는 제안만 한다.

**영향**: 개선했다고 믿는 것들의 효과가 언제까지고 "아직 확인 안 됨" 상태로 남는다. 특히 혼잡 시간대 사전 대응 규칙 하나는 3주 동안 발동 여부를 결정할 시각 자체가 하루에 한 번도 없었다는 구조적 문제라, 판정이 늦어지는 게 아니라 **판정할 수 있는 절차 자체가 없는 상태**다.

**심각도**: P1(관측·정확성 훼손 — 개선 효과 검증이 막혀 있다). 전일 보고서가 이미 그 항목을 판정 불가 전환 대상으로 제안했다(→ 사용자 조치 3번).

### 오늘 관측이 답해야 할 것

1. **`2026-09-16-recovery-hint-carries-its-sample-count`(검증 시한: 오늘)** — 워치독의 "no_ingest"(시세 미유입) 경보가 오늘 실제로 뜨면, 그 경보 문구에 "회복 표본 5건 중 3건만 정시 회복"이라는 새 표현이 제대로 찍히는지 확인한다. 오늘 그 경보가 한 번도 안 뜨면 판정 불가(`untested`)로 남긴다.
2. **`2026-09-16-cliff-day-marks-the-hypothesis-table-itself`(검증 시한: 오늘)** — 오늘 옵션체인 시세가 20분 이상 끊기는 "절벽"이 나면, 장후 지표표에 "오염" 표시가 실제로 뜨는지 확인한다. 절벽이 안 나면 이 항목도 판정 불가로 남긴다(그 자체가 나쁜 소식은 아니다 — 절벽이 없었다는 뜻이므로).
3. **§1-1 재확인** — 내일(09-18) 장전 회차에서 `2026-08-26-p1-2-1` 불일치 적신호가 실제로 사라지는지 확인한다.
4. **§1-2 미커밋 건수 diff** — 09-16 27건 → 오늘 26건, 무엇이 줄었는지 아직 대조 안 함. 장후 회차가 확인한다.

### Fix 구현계획 (초안)

오늘 아침 관측만으로는 새로 착수할 코드 수정 항목이 없다(§1-1~1-5 전부 사람 결정 대기이거나 코드 밖 작업). 장전·장중은 코드를 바꾸지 않으므로 이 절은 장후 회차가 실제 계획을 세운다.

### 고도화 방안 (초안)

오늘 관측에서 새로 제안할 고도화 항목 없음. 09-16 보고서 제5부(회복 힌트 표본 수 인쇄, 절벽일 가설표 오염 열)가 이미 어제 구현·커밋됐고(`7467d07`), 그 효과 검증은 위 「오늘 관측이 답해야 할 것」 1·2번이 이어받는다.

### dev_memory 반영 초안

이 회차(자동 장전)는 dev_memory를 직접 수정하지 않는다. 장후 세션이 반영할 후보:
- §1-1(hypotheses.yaml 언급-실재 불일치 예고 실패) — 검사기가 본문 전체를 스캔한다는 사실을 `collect_evidence.py` 주석에 이미 적어 두었으나(2813행), 향후 보고서 작성 시 "직전 보고서가 언급했는데 없는 id" 문구를 **다시 인용하지 않는 편이** 하루 더 빨리 소멸시킨다는 점을 팁으로 남길 만하다.

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (개장 전 / 장중 즉시)

**사람 조치 없음.** 관측 범위(07:30~08:02) 안에서 개장 전에 처리해야 할 P0/P1 항목이 없다.

### 오늘 중 (장 마감 후)

1. **미커밋 26건 중 09-16 대비 무엇이 줄었는지 diff 확인** — 장후 회차가 처리 예정(→ §1-2).

### 결정이 필요한 것 (자동으로 못 하는 것)

2. **미커밋 26건 정리 — 9일째로 추정.** (A) 커밋할 것 / `.gitignore`에 뺄 것 / 폐기할 것 3분류를 정한다 vs (B) 당분간 그대로 둔다(단, `start_mahdi_premarket.bat`이 미추적 파일에 의존하는 위험은 남는다). (→ §1-2)
3. **가설 2건을 `inconclusive`(판정 불가)로 전환할지 확정** — `2026-08-19-fix2-roundtrip-leaves-db-evidence`·`2026-08-26-p1-3-preemptive-rule-fires-at-premarket`. 근거는 §1-5에 있다. 사람이 확정만 하면 된다.
4. **이벤트 캘린더 9월분 채우기 — 20일째.** 외부 일정(금리 발표 등)을 사람이 확인해 `mahdi/config/event_calendar.yaml`에 채워야 한다(코드 밖의 일).
5. **ZN/ES 해외선물 거래소 권한 신청** — 증권사 계좌 설정(코드 밖의 일, 만성).

### 이번 회차에 사람이 할 일이 없으면

해당 없음(위 항목 참조).

---

## 제2부 — 장중 ① (12:30 회차)

- 국면: **장중(intra)** · 예정 12:30 · 실행 12:35 KST — **정시 실행**(밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-17_증거_intra.md`(12:35:19 수집)
- 리포 HEAD: `4b91b52`(브랜치 `master`) · 당일 커밋 **0건** · 미커밋 **27건**(아래 2-0의 1-2 처분 참조)
- 이 절 위(제1부 장전)의 본문은 고치지 않았다. 정정이 필요한 판정은 아래 이월 처분표에 적는다.

### 2-0. 장전 이월 처분 — 5건 전부 처분 + 관측 항목 4건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 `hypotheses.yaml` 언급-실재 불일치(`2026-08-26-p1-2-1`) | 🔄 **지속 — 예상대로, 내일(09-18) 소멸 예정** | 증거 §8-1이 오늘도 같은 1건을 낸다. 장전이 이미 "09-16 보고서 본문의 인용 때문에 오늘 한 번 더 뜬다"고 설명했고, 그 설명대로다. 오늘(09-17) 이 파일 본문은 그 옛 id를 다시 인용하지 않았으므로 내일은 정말 사라질 것으로 예상한다 |
| 1-2 미커밋 26건, 사람 결정 대기 | 🔄 **지속 — 26건→27건, 실제 증가 아니다** | 증가분 1건은 **이 일일점검 파일 자신**이다(`?? docs/동작점검/2026-09-17_마흐디_일일점검.md` — 08:08에 장전이 막 만든 파일이 12:35 시점엔 아직 미커밋 상태로 잡힌다). 나머지 구성(수정 4건 + 미추적 22건)은 장전과 동일(증거 §1) — 내용상 새 드리프트 없음 |
| 1-3 ZN·ES 해외선물 조회 실패 | 🔄 **지속(만성)** | 오늘도 07:32:35(ZN)·07:32:39(ES) 조회 실패, 이후 11:32:38·11:32:40까지 같은 사유로 반복(증거 §4). 계좌 권한 문제, 코드 밖의 일 |
| 1-4 이벤트 캘린더 9월분 미갱신 | 🔄 **지속(20일째, 의도적 방치)** | 오늘도 07:31:55~12:33:54 사이 6회 `이벤트 캘린더 미기입: not_covered` WARNING, `covered_through`는 여전히 `2026-08-28`(증거 §5). 방치는 설계 의도(경고를 계속 울리게 둠) |
| 1-5 검증 시한 초과 가설 4건 | 🔄 **지속** | 동일 4건 그대로(증거 §8) — `2026-08-12-fix7...` 16일 지남, `2026-08-19-fix2...` 16일 지남, `2026-08-26-p1-3...` 21일 지남. 시한 초과 일수만 늘었을 뿐 사람 손 판정은 아직 없다 |
| (관측) `2026-09-16-recovery-hint-carries-its-sample-count`(오늘 시한) | ➡️ **이월(14:30·장후로)** | 판정 조건(워치독 `no_ingest` 경보)이 오늘 아직 한 번도 안 떴다 — 당일 워치독 30행 전부 OK, 비-OK 0건(증거 §6). 안 뜨면 오늘도 판정 불가(`untested`)로 남을 수 있다 |
| (관측) `2026-09-16-cliff-day-marks-the-hypothesis-table-itself`(오늘 시한) | ➡️ **이월(14:30·장후로)** | 판정 조건(20분 이상 연속 시세 끊김, 「절벽」)이 오늘 아직 없다 — 최장 연속 `rows=0` 구간 없음, 3분 이상 로그 공백 0건(증거 §3·§5-1). 나쁜 소식이 아니다(절벽이 없었다는 뜻) — 안 나면 오늘도 판정 불가로 남을 수 있다 |
| (관측) §1-1 내일(09-18) 재확인 | ➡️ **이월(내일 장전 몫)** | 그대로 |
| (관측) §1-2 미커밋 09-16→09-17 diff | ➡️ **이월(장후 몫)** | 아직 대조 안 함. 오늘 26→27 증가는 위에서 이미 설명(자기참조), 09-16 대비 실질 변화는 장후가 확인 |

### 장중① 체크리스트 (phases.md B절)

| 항목 | 결과 |
|---|---|
| B-1 생존 | ✅ 관측 루프 최종 레코드 12:35:22(수집 시각과 거의 동시), 07:30~12:35 구간 3분 이상 공백 **0건** |
| B-1 워치독 | ✅ 당일 30행(OK 30/비-OK 0), 기록 간격 25분 이상 공백 0건. 마지막 기록 12:30 → 지금(12:35)까지 5분 무기록(정상, 주기 10분) |
| B-1 크래시 | ✅ `observation_loop_crash.log` 8.0KB, **당일 07:31:14 기동 이후 트레이스백 없음**(파일 자체 mtime은 09-17 07:31이지만 그 표식 이후로 추가된 줄이 없다 — mtime만으로 오늘 죽었다고 읽지 않았다) |
| B-2 수집(예산초과/조기포기/실패예산) | 예산 초과 **60건** · 연속 타임아웃 조기 포기 **2건** · 실패 예산 소진 **0건**(계측 살아 있음, 2026-09-16엔 54줄 찍힌 전례가 있어 「진짜 0」으로 판정, 증거 §5-3). 컷당한 북 분포는 weekly_thu·weekly_mon·regular 혼재 — 위반 판정 축(먼슬리를 두고 위클리가 먼저 잘렸는가)은 로그에서 확인 안 됨 |
| B-2 WS 단절 | ✅ **0건**(2026-09-09엔 8건 찍힌 전례가 있어 경로는 살아 있음, 「진짜 0」) |
| B-2 REST 실패 분해 | ReadTimeout(BNAN·CNAN 등) 다수, EGW00201(초당 거래건수 초과) 13건 — 기존 분류 범위 안, `other` 급증 없음 |
| B-2 먼슬리 되살리기 | 완전 미회복 **0건** · 부분 회복 **2건**(11시 1건·10시 1건, 증거 §5-1-1) — 전일(09-16) 실패 12건 대비 대폭 개선(전일 회복률 79.2%). 다만 이 증감은 그날 KIS 상태에 비례하는 축이라 판정 근거로 쓰지 않는다(규약 G) |
| B-3 판단 도달(앙상블) | 비영 멤버 최대 **3/6**(가용 목록 4종: `flow_position`·`options_flow`·`orderflow_ofi_vpin`·`regime_hmm`; 나머지 2종 `xgboost_tabular`·`lstm_temporal`은 애초에 미학습·미구현 상태로 만성, `NEXT_TODO.md` 기존 항목이지 오늘 새 문제 아님). 확신도 HIGH_CONVICTION 19·STANDARD 16·SMALL_TEST 3·NO_TRADE 1. `options_flow` 마지막 관측 12:26:54 — 살아 있음 |
| B-3 레짐 갱신 | ✅ 분마다 갱신, WARMUP 복귀 없음. 전이 2건 중 최초(08:46)만 개장 전 워밍업, 09:15부터 `predict` 모델 |
| B-4 코드 변경 | ✅ **없음**(이번 회차는 관찰만) |

> 1-1·1-2·1-3·1-4·1-5와 관측 항목 4건은 위 이월 처분표에서 전부 처분했다(근거 인용 포함).
> 아래부터 이 회차의 신규 항목이다 — **이상점 번호는 하루 안에서 이어진다**(장전 1-1~1-5 → 이 회차 1-6부터).

### 1-6. KIS 응답지연 위험선 근접 — 자동 카운터는 「이틀째」라 하지만 실제로는 「사흘째」 연속 🆕

**증상**: 오전~정오 내내 REST(시세·잔고 조회) 응답시간이 제한시간(4.0초, `inquire-price` 기준 read timeout)에 바짝 붙어 있었다. 시간대별 최댓값(그 시간대 안에서 가장 나쁜 순간의 p95 — 응답시간을 순서대로 줄 세웠을 때 상위 5% 지점)이 09시 4.03초, 10시 4.03초, 11시 3.97초, 12시 4.00초로 전부 제한시간과 사실상 같았다(비율 1.0 근처).

**근거**:
- 증거 §5-1 시간대별 표: 09~12시 「p95 최대」열이 각각 4.03·4.03·3.97·4.00(전부 ⚠ 표시)
- 사전 대응 규칙(`2026-08-04-p5`)이 정한 위험 기준(2.5초)을 넘은 시간대·조회 종류 조합이 **11개**(10시 `inquire-asking-price` 2.76초·11시 2.74초·12시 2.84초·09시 `inquire-balance` 2.76초·10시 3.36초·11시 3.47초·12시 3.01초·09시 `inquire-price` 3.08초·10시 3.30초·11시 3.40초·12시 3.48초, 증거 §5-1)
- 자동 계산기는 이 조건이 "이틀 연속(어제 09-16과 오늘)" 성립했다고 센다(증거 §5-1 원문). 그런데 **09-16 보고서 자신의 §1-7이 "09-15도 같은 구간에서 조건이 성립했다"고 이미 적어 놓았다**(09-16 파일 270행: "이틀 연속(09-15도) 같은 구간에서 p95 2.5초를 넘었다"). 09-15·09-16·09-17을 이으면 **사흘 연속**인데, 자동 계산기는 매번 "어제 하루"하고만 비교해서 "이틀"로만 보고한다 — 연속 일수를 실제로 세는 도구(사이드카)가 없다(증거 §8-4: "지연 계측이 실린 사이드카를 못 찾았다")
- 다행히 오늘은 응답시간이 제한시간을 실제로 **넘긴** 창(p95/timeout ≥ 1.0)이 4/60개뿐이었다(최악 09:31, 비율 1.01) — 09-16의 21/60개(최장 연속 8창)보다는 훨씬 덜 심각했다

**기준**: `docs/동작점검/tools/phases.md` A-2 — "연속 3거래일 이상이면 그 자리에서 (A) 켠다 / (B) 유예한다 중 하나를 명시적으로 고른다 — 유예 재연장 금지". 다만 오늘 조건이 성립한 시간대(10·11·12시)는 전부 기존 혼잡 레버(`OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`, 9~14시 커버)가 이미 덮고 있다 — 09-16 보고서가 같은 상황에서 이미 확인한 대로 "새로 켤 시간대는 없다". 남는 질문은 "레버가 덮은 시간대 안에서도 왜 위험선 근접이 반복되는가"이고, 이건 별도로 이미 열려 있는 조사 항목(NEXT_TODO Fix 9, 09-15 §3-5)의 몫이다.

**영향**: 지금 당장 막혀 있는 조치는 없다(레버는 이미 켜져 있고, 새로 켤 시간대도 없다). 다만 응답시간이 매일 제한시간 턱밑까지 가는 패턴이 사흘째 반복되고 있는데, 그것을 "사흘째"로 정확히 세는 도구가 없어서 사람이 매번 전날 보고서를 손으로 펼쳐 봐야 실제 연속 일수를 알 수 있다 — 이 수고를 건너뛰면 매일 "이틀째"로만 보여 실제보다 덜 급해 보일 수 있다.

**심각도**: P2(관측·도구 결손 — 규약 C: 「0건」이 아니라 「세는 도구가 하루씩만 비교한다」는 문제). 새 Fix는 아니다 — 기존 §8-4/Fix D(연속 일수 사이드카)의 연장선. 장후 회차가 dev_memory에 "09-15~09-17 사흘 연속"을 명시적으로 적어 두는 것을 권고한다.

### 14:30 회차가 이어받을 것

1. **§1-6의 「사흘 연속」 확정** — 14:30·장후 시점에도 조건이 이어지는지, 그리고 이 사흘 연속이 09-18 장전의 결정(레버 재검토 여부)에 근거로 쓰일 수 있도록 dev_memory에 날짜와 함께 명시적으로 남길 것
2. **`2026-09-16-recovery-hint-carries-its-sample-count`·`2026-09-16-cliff-day-marks-the-hypothesis-table-itself` 두 가설** — 트리거(no_ingest 경보 / 20분+ 절벽)가 여전히 안 뜨면 장후가 최종적으로 `untested`로 닫는 것을 검토
3. **미커밋 27건 diff(09-16→09-17)** — 여전히 장후 담당, 이번 회차는 자기참조 증가만 설명(§2-0)
4. **새 P0/P1 없음** — 이 회차는 P0 0건·P1 0건·P2 1건(신규 1-6)으로 마감

### dev_memory 반영 초안 (이번 회차는 직접 수정하지 않는다 — 장후가 반영)

- §1-6(KIS 응답지연 사흘 연속) — 자동 계산기가 "이틀"로 보고하는 것과 실제 연속 일수(09-15부터 사흘)가 다르다는 점, 그리고 그 차이가 §8-4/Fix D의 사이드카 부재 때문이라는 점을 `NEXT_TODO.md`의 관련 항목(§8-2·§8-3 기존 Fix D 트랙)에 한 줄로 추가할 것을 권고. 새 Fix 항목 신설은 불필요 — 기존 트랙 보강이면 충분하다.

---

## 제2부 — 장중 ② (14:30 회차)

- 국면: **장중(intra)** · 예정 14:30 · 실행 14:40 KST — **정시 실행**(밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-17_증거_intra_1430.md`(14:40:32 수집) + 지연창 원본 `docs/동작점검/auto/2026-09-17_지연창.tsv`(5분 창)
- 리포 HEAD: `4b91b52`(브랜치 `master`) · 당일 커밋 **0건** · 미커밋 **27건**(변동 없음)
- 이 절 위(제1부·제2부 장중①)의 본문은 고치지 않았다. 정정이 필요한 판정은 아래 이월 처분표에 적는다.

### 3-0. 장중① 이월 처분 — 전건 처분(1-1~1-6) + 관측 항목 4건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 `hypotheses.yaml` 언급-실재 불일치(`2026-08-26-p1-2-1`) | 🔄 **지속 — 예상대로** | 증거 §8-1 동일하게 1건. 09-18(내일) 장전에 소멸 예상이라는 판단 그대로 유지 |
| 1-2 미커밋 27건 | 🔄 **지속 — 변동 없음** | `git --no-optional-locks status --porcelain` 27줄, 12:30과 구성 동일. 이번 회차가 만든 증거 파일(`증거_intra_1430.md`)이 아직 다음 스캔에 안 잡혔을 뿐 새 드리프트 아님 |
| 1-3 ZN·ES 해외선물 조회 실패 | 🔄 **지속(만성)** | 오늘 마지막 실패 13:37:39(ZN)·13:37:42(ES, 증거 §4) — 계좌 권한 문제, 코드 밖의 일 |
| 1-4 이벤트 캘린더 9월분 미갱신 | 🔄 **지속(20일째)** | 14:33:54까지 8회째 WARNING(증거 §5), `covered_through` 여전히 2026-08-28 |
| 1-5 검증 시한 초과 가설 4건 | 🔄 **지속** | 동일 4건, 시한 초과 일수만 늘었다(증거 §8). 사람 손 판정 여전히 대기 |
| 1-6 KIS 응답지연 위험선 근접(사흘 연속) | 🔄 **지속 — 오늘 오후 실제로 경고선을 넘었다(세부는 「오후 열화 게이지」)** | 12:30 회차가 "위험선까지는 안 갔다"고 적은 전제가 14:26 구간에서 무너졌다. 새 이상점(1-8)으로 갈라 적는다 — 세는 도구 문제(§8-4/Fix D)와는 다른 사건이라 번호를 나눈다 |
| (관측) `2026-09-16-recovery-hint-carries-its-sample-count`(오늘 시한) | ➡️ **이월(장후로)** | 워치독 `no_ingest` 경보가 14:40까지도 0건(증거 §6, 당일 43행 전부 OK) — 아직 판정 조건 미충족. 장 마감(15:20)까지 조건이 안 뜨면 장후가 `untested`로 닫는 것을 검토 |
| (관측) `2026-09-16-cliff-day-marks-the-hypothesis-table-itself`(오늘 시한) | ➡️ **이월(장후로)** | 최장 연속 `rows=0` 구간 여전히 없음(증거 §5-1, 0행 사이클 0개) — 절벽 자체가 없어 판정 불가 지속. 나쁜 소식 아님 |
| (관측) §1-1 내일(09-18) 재확인 | ➡️ **이월(내일 장전 몫)** | 그대로 |
| (관측) §1-2 미커밋 09-16→09-17 diff | ➡️ **이월(장후 몫)** | 이번 회차도 대조 안 함. 27건 그대로라 diff 시급성 변화 없음 |

### 오후 열화 게이지 (창 단위 시계열)

**요약**: 시세 조회가 얼마나 오래 걸리는지 보여주는 다섯 계기판 중, **넷은 오늘 하루 종일 바닥에 붙어 있었고(정상)**, **하나(응답 지연)만 오후 들어 처음으로 "경고등"이 켜졌다.** 아래에서 그 하나를 자세히, 나머지 넷은 짧게 확인한다.

**① rows=0 사이클(옵션 시세가 그 1분 동안 아예 하나도 안 들어온 경우) 비율** — 07:30~14:40 전체 구간에서 **0건/0%**. 경고선(30%)·위험선(70%) 둘 다 하루 종일 미달. 이상 없음.

**② 관측 루프 공백(로그가 끊긴 시간)** — 07:30~14:40 구간 3분 이상 공백 **0건**. 이상 없음.

**③ WS(웹소켓 — 실시간 시세를 밀어 넣는 연결) 단절** — 12:30 이후 누적 **0건**(당일 전체도 0건). 이상 없음.

**④ 앙상블(여러 판단 모형을 합쳐 최종 신호를 내는 장치) 비영 멤버(0점이 아니라 실제로 의견을 낸 모형 수)** — 가용 모형 자체가 4종뿐이고(나머지 2종은 애초에 미구현, 만성) 그중 몇 개가 실제로 점수를 냈는지를 30분 단위로 보면:

| 시간대 | 비영 멤버 수(가용 4종 중) | 비고 |
|---|---|---|
| 12:30~13:00 | 3 (변화 없음) | |
| 13:00~13:30 | 3 | |
| 13:30~14:00 | 3 (변화 없음) | |
| 14:00~14:15 | **2** | 이 구간만 4종 중 2종만 점수를 냈다 |
| 14:15~14:25 | 2 (변화 없음) | |
| 14:25~14:40 | 2~3(오르내림) | 14:34부터 3으로 복귀 |

14:00~14:25 사이 잠깐 2종으로 줄었다가 14:25 이후 3종으로 되돌아왔다. 08-19에 실측 2.36(가용 3.43 대비)까지 떨어졌던 것과 비교하면 이번은 그보다 얕고 짧다 — **0종(전멸)은 없었다.** 경고선(가용 대비 과반 이하)에 걸리는 구간이긴 하나 이 앙상블 자체가 만성적으로 4/6 천장(NEXT_TODO 기존 항목)이라 매일 나오는 흔들림 범위 안이다. 새 이상점으로 올리지 않는다.

**⑤ 시세 조회 응답 지연 — 오늘 처음 경고선을 넘었다 (핵심 게이지)**

기준: 시세 조회 한 번의 제한시간(read timeout)은 4.0초. 그 분(창) 안에서 응답시간을 순서대로 줄 세웠을 때 정중앙(p50 — 중앙값)이 제한시간의 몇 %인지를 5분 창마다 잰다. 80%(0.8배)를 넘으면 경고, 100%(1.0배)를 넘으면 위험(그 창의 호출 절반 이상이 시간 초과라는 뜻).

| 창 시각 | p50(초) | p50÷제한시간 | 판정 | p95(상위5% 지점)÷제한시간 |
|---|---|---|---|---|
| 12:31 | 2.31 | 0.58 | 정상 | 1.00 ⚠(위험) |
| 12:56 | 2.30 | 0.57 | 정상 | 0.96 |
| 13:01 | 2.42 | 0.60 | 정상 | 1.01 ⚠ |
| 13:11 | 2.58 | 0.65 | 정상 | 1.00 ⚠ |
| 13:21~13:31 | 2.36~2.48 | 0.59~0.62 | 정상 | 1.00~1.01 ⚠(3창 연속) |
| 13:41~14:01 | 2.11~2.72 | 0.53~0.68 | 정상(p50 기준) | **1.00~1.01 ⚠ — 5창 연속(13:41~14:01)** |
| 14:16~14:21 | 2.41~2.80 | 0.60~0.70 | 정상 | 1.00~1.01 ⚠ |
| **14:26** | **3.36** | **0.84** | **🔶 경고선(0.8) 최초 돌파** | 1.01 ⚠ |
| 14:31~14:36 | 2.28~2.86 | 0.57~0.71 | 정상(경고선 아래로 복귀) | 1.00~1.01 ⚠ |

- **경고선(p50÷제한시간 0.8) 최초 돌파: 14:26 창, 비율 0.84.** 이 창 하나만 경고선을 넘었고 앞뒤 창은 그 아래(0.57~0.71)로 돌아갔다 — **지속적 악화가 아니라 순간 스파이크**다.
- **위험선(p50÷제한시간 1.0)은 오늘 한 번도 안 넘었다.**
- 다만 더 예민한 지표인 p95(상위 5% — 가장 느린 호출들)는 **13:41~14:01 사이 5개 창 연속으로 1.0을 넘었다**(최장 연속 기록, 최악 13:01·13:31 등에서 1.01). 이는 "느린 호출의 꼬리"가 이미 자주 제한시간을 넘기고 있다는 뜻이고, 12:30 회차가 "위험선까지는 안 갔다"고 적은 것보다 한 단계 더 나쁜 신호다.
- **검열(제한시간에 잘려 실패로 끝난 호출) 건수**: 14:26 창에서 15건/58건(26%)로 하루 중 최댓값. 그 직전 14:25:28에는 옵션 체인 조회가 통째로 실패했다가 5초 뒤 재시도로 회복(증거 §4 `chain_total_failure`×1, 오늘 처음이자 유일).
- **그래도 rows=0(적재 완전 실패)은 없었다** — ②에서 본 대로 이 지연이 아직 "체인이 통째로 빈다"는 결과로는 안 이어졌다.

### 1-7. 먼슬리(월물) 되살리기 완전 미회복 — 오전 0건 → 오후 2건 🆕

**증상**: 옵션 월물(만기가 먼 종목) 시세를 예산 안에서 되살리는 절차가, 그 분의 레그(옵션 한 종목)를 **하나도** 못 되살린 경우가 오후 들어 두 번 났다.

**근거**: `logs/observation_loop.log` 13:00:51 "먼슬리 레그 재시도: 2개 중 0개 회복(남은 예산 0.0초) — 되살리기 실패", 14:00:50 "먼슬리 레그 재시도: 3개 중 0개 회복(남은 예산 0.0초) — 되살리기 실패". 12:30 회차 보고서(증거 §5-1-1)는 09~12시 완전 미회복 **0건**으로 적었다 — 이번 회차 증거 §5-1-1은 13시·14시 각 1건씩 완전 미회복을 낸다.

**기준**: `docs/동작점검/README.md` 규약 G(증감 화살표를 그 자체로 판정하지 않는다) — 이 건수는 그날 KIS(한국투자증권 API) 혼잡 상태에 비례하는 축이라 단독으로 회귀 판정 근거로 쓰지 않는다. 다만 완전 미회복은 "일부만 비었다"보다 나쁜 등급이라 존재 자체는 기록한다.

**영향**: 그 두 번의 분(13:00·14:00)에는 해당 월물의 감마·GEX(옵션 시장이 지수를 특정 가격대에 붙드는 힘) 계산에 들어가는 입력이 통째로 비어 그 순간의 판단이 평소보다 얇은 정보로 내려졌다. 매매를 막을 정도는 아니고, 다음 분에는 정상으로 돌아왔다.

**심각도**: P2. 새 Fix는 아니다 — 이미 열려 있는 조사 항목(NEXT_TODO Fix 9, "사전 대응 규칙이 켜진 시간대 안에서도 절벽이 나는 원인 조사")에 시각(13:00:51·14:00:50)과 함께 보탤 근거로 남긴다.

### 1-8. 시세 조회 지연 — 오늘 처음 경고선을 넘었다 (14:26, §1-6의 후속) 🆕

**증상**: 「오후 열화 게이지」⑤에서 본 대로, 14:26 창에서 시세 조회 응답시간의 중앙값이 제한시간의 84%까지 올라 오늘 처음 경고선(80%)을 넘었다. 같은 창 앞뒤로 더 예민한 지표(p95)는 13:41~14:01 사이 5개 창 연속으로 제한시간을 넘겼고, 14:25:28에는 옵션 체인 조회가 통째로 한 번 실패했다.

**근거**: `docs/동작점검/auto/2026-09-17_지연창.tsv` 14:26:24행(p50 3.36초, 비율 0.84, 검열 15/58건=26%) · `docs/동작점검/auto/2026-09-17_증거_intra_1430.md` §4 `chain_total_failure` 14:25:28 · §5-1 "경고선(비율 0.80) 최초 돌파: 14:26".

**기준**: `docs/동작점검/tools/phases.md` 오후 열화 게이지 표 — p50÷제한시간 ≥0.8이 경고. 위험선(1.0)은 안 넘었고, 스파이크는 그 창 하나로 끝나 다음 창(14:31)부터 0.57로 복귀했다.

**영향**: 이 순간 시세 조회의 절반 가까이가 평소보다 훨씬 오래 걸렸지만, 옵션 시세가 아예 안 들어온 분(rows=0)은 없었다 — 판단이 조금 느려졌을 뿐 완전히 멈추지는 않았다. 다만 이 스파이크가 방금 한 번으로 끝났는지, 앞으로 더 잦아질 신호의 시작인지는 이 회차만으로는 알 수 없다.

**심각도**: P2(오늘 아직 실질 피해 없음 — rows=0·완전 절벽 없음). §1-6과 같은 근본 원인(KIS 혼잡)이지만 "세는 도구가 틀렸다"는 §1-6의 문제와 달리 이건 **실제로 위험선에 근접한 사건**이라 번호를 나눴다. 장후가 15:20까지 이 패턴이 재발하는지, 특히 p95 위험선 연속 창이 더 길어지는지 확인할 것을 권고한다.

### 14:50 컷오프 권고

**지금(14:42) 기준 14:50까지 남은 시간: 약 8분.**

- **개입 불필요.** 코드 변경·재기동 없이 남은 장을 마친다.
- 근거: 오늘 하루 rows=0(시세 완전 유실) **0건**, 워치독 이상(DEGRADED) **0건**, ERROR 로그 **0건**, WS 단절 **0건** — 지연 스파이크(1-8) 하나를 빼면 나머지 네 계기판은 하루 종일 정상 범위였다. 그 스파이크도 한 창(14:26)으로 끝났고 다음 창부터 정상으로 복귀했다.
- 손익 비교(재시동을 검토할 경우를 대비해 기록): 재시동 없이 얻는 것 = 오늘 관측 연속성(끊기면 절벽·회복 가설 판정 기회 자체가 사라진다) + 남은 약 38분(15:20 마감까지). 재시동으로 얻는 것 = 수집 경로를 강제로 새로고침(그러나 지금 수집 경로 자체는 안 죽었다 — rows=0 없음). 대가는 레짐 워밍업 약 30분(봉 30개) — 오늘 남은 장 시간 대부분을 워밍업으로 날린다. **재시동을 정당화할 근거가 없다.**
- 남는 일은 관찰뿐이다: 14:26 스파이크가 재발하는지, 특히 p95 위험선(1.0) 연속 창이 5창보다 길어지는지 — 장후 회차가 확인한다.

### dev_memory 반영 초안 (이번 회차는 직접 수정하지 않는다 — 장후가 반영)

- §1-6/§1-8 — "KIS 응답지연 사흘 연속"(세는 도구 문제, §8-4/Fix D)과 "14:26 경고선 최초 돌파"(오늘 실제 사건)는 **서로 다른 사실**이니 dev_memory에도 분리해서 적을 것. 후자는 p95 위험선 5창 연속(13:41~14:01)이라는 구체 수치와 함께 남긴다 — 다음에 비슷한 패턴이 나오면 "13:41부터 몇 창 연속인가"로 바로 비교할 수 있게.
- §1-7 — 먼슬리 완전 미회복 시각(13:00:51·14:00:50)을 NEXT_TODO Fix 9 조사 항목에 사례로 추가할 것.

---

## 제3부 — 장후 (15:20 ~ )

- 국면: **장후(post)** · 예정 15:45 · 실행 15:46 KST — **정시 실행**(밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-17_증거_post.md`(15:46:56 수집) · 자동 지표: `auto/2026-09-17_지표.{md,json}`
- 리포 HEAD: `4b91b52`(브랜치 `master`) · 당일 커밋 **0건** · 미커밋 **27건**(증거 수집 시점 15:46 기준. 이 회차 작성 중 재확인하니 28건 — §3-2에서 다룬다)
- 이 절 위(제1부·제2부 장중①·②)의 본문은 고치지 않았다. 정정이 필요한 판정은 아래 이월 처분표에 적는다.

### 3-0. 장중② 이월 처분 — 전건 처분(1-1~1-8) + 관측 항목 4건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 `hypotheses.yaml` 언급-실재 불일치(`2026-08-26-p1-2-1`) | 🔄 **지속 — 예상대로, 내일(09-18) 소멸 예정 그대로** | 증거 §8-1 동일하게 1건. 오늘(09-17) 이 파일 본문이 그 옛 id를 다시 인용하지 않았으므로 내일 장전이 실제로 사라지는지 확인할 것 |
| 1-2 미커밋 27건, 사람 결정 대기 | 🔄 **지속** — 아래 §3-2에서 09-16 대비 diff를 **처음으로 확정**한다 | 증거 §1. 구성은 09-16과 자기참조 파일(그날그날의 일일점검·지표 산출물) 2줄만 빼고 전부 동일 — 실질 드리프트 0건 |
| 1-3 ZN·ES 해외선물 조회 실패 | 🔄 **지속(만성)** | 오늘 마지막 실패 15:42:32(ZN)·15:42:33(ES, 증거 §4·§5) — 하루 총 9회, 계좌 권한 문제 |
| 1-4 이벤트 캘린더 9월분 미갱신 | 🔄 **지속(20일째, 의도적 방치)** | 오늘 마지막 경보 15:33:54, 하루 총 9회(증거 §5). `covered_through` 여전히 `2026-08-28` |
| 1-5 검증 시한 초과 가설 4건 | 🔄 **지속 — 시한 초과 일수만 늘었다** | `2026-08-12-fix7...` 16일 지남 · `2026-08-19-fix2...` 16일 지남 · `2026-08-26-p1-3...` **21일 지남** · `2026-09-15-eE...` 1일 지남(증거 §8). 사람 손 판정 여전히 없음(→ 사용자 조치 2번) |
| 1-6 KIS 응답지연 — 세는 도구가 "이틀"로 보고하는 문제 | 🔄 **지속 — 오늘도 "이틀 연속"(실제로는 나흘째, 09-14부터)** | 증거 §5-1: "사전 대응 규칙 조건 성립 — 이틀 연속(직전 거래일 2026-09-16) 같은 구간 18개 / 오늘 성립 19개". 09-15 §1-7이 "09-14도 성립"이라 적었으므로 09-14·09-15·09-16·09-17 **나흘 연속**인데 계산기는 매번 전날 하루와만 비교한다. 세는 도구 부재 문제는 그대로 — dev_memory에 "09-14~09-17 나흘 연속"으로 갱신할 것(§3-6) |
| 1-8 시세 조회 지연 — 경고선 최초 돌파(14:26) | ⬆️ **격상 위험 신호 — 마감 직전 p95 위험선(1.0) 연속 창이 오늘 최장인 7창(14:51~15:21)으로 늘었다** | 증거 §5-1: "p95/timeout ≥1.0인 창 26/98개 — 최악 13:01 비율 1.01 · 최장 연속 7창(14:51~15:21)". 12:30 회차가 "위험선까지는 안 갔다", 14:30 회차가 "5창 연속"으로 적은 것보다 한 단계 더 나쁘다. 다만 **p50 기준 위험선(1.0)은 오늘 하루 한 번도 안 넘었다** — "호출 절반 이상이 타임아웃"까지는 아직 안 갔다. 심각도는 P2 유지(아래 1-9에서 실제 피해 1건을 별도로 다룬다) |
| (관측) `2026-09-16-recovery-hint-carries-its-sample-count`(오늘 시한) | ➡️ **판정 불가로 마감 — 트리거 자체가 오늘 하루 한 번도 안 떴다** | 워치독 `no_ingest`/DEGRADED 경보 당일 **0건**(증거 §6, 49행 전부 OK). 검증예정일(오늘)이 지났지만 판정 재료가 없다 — §3-5에서 처리 제안 |
| (관측) `2026-09-16-cliff-day-marks-the-hypothesis-table-itself`(오늘 시한) | ➡️ **판정 불가로 마감 — 절벽(20분+) 자체가 오늘도 없었다** | 최장 연속 `rows=0` 구간 **2분**(임계 20분에 크게 못 미침, 증거 §5-1). 나쁜 소식 아님 — §3-5에서 처리 제안 |
| (관측) §1-1 내일(09-18) 재확인 | ➡️ **이월(내일 장전 몫)** | 그대로 |
| (관측) §1-2 미커밋 09-16→09-17 diff | ✅ **판정 — §3-2에서 확정** | 아래에서 처음으로 실제 대조함 |

### 3-1. 종료 시퀀스 (15:20 ~ 15:45)

| 확인 | 결과 |
|---|---|
| 정규장(옵션) 마감 15:20 전후 사이클 | 정상 지속. 15:20:43까지 옵션체인 연속 타임아웃 조기 포기 경보(WARNING) 있었으나 이는 낮부터 이어진 혼잡 패턴의 연장(§1-6/§1-8)이지 마감 자체의 이상이 아님 |
| `stop_mahdi_marketclose.bat` 실행 | `.last_marketclose_stop.txt`: **2026-09-17T15:45:03** — 예정(15:45)과 거의 동시, 정상 |
| taskkill 이후 `daily_ops_report.py` 호출 | 성공 — `auto/2026-09-17_지표.md`·`.json`, `auto/2026-09-17_증거_post.md`, `auto/2026-09-17_지연창.tsv` 전부 15:46에 생성됨(증거 §9) |
| 크래시 로그 | `observation_loop_crash.log` 8.0KB, **당일 기동(07:31:14) 이후 추가된 트레이스백 없음** — 종료 배치가 프로세스를 죽이는 과정에서 새 크래시를 만들지 않았다 |
| 워치독 마지막 기록 | 15:40(감시 창 끝 15:45까지 5분 무기록, 정상 주기 10분 안) |
| 앙상블 멤버 마감 후 이탈 — **정상적인 장 마감 후 쇠퇴로 판단** | 15:24:54 `options_flow` 이탈(384분 유지, 사유=원재료없음[gex·gamma_flip·wall·spot]) → 15:36:54 `orderflow_ofi_vpin` 이탈(410분 유지, 사유=원재료없음[ofi·queue_imbalance]). 둘 다 마감(15:20) **이후**에 일어났고 사유가 "옵션체인/체결 원재료가 더는 안 들어온다"이므로, 장이 닫혀 입력이 자연히 마르는 정상 패턴이다. 장중에 일어났다면 이상점이었을 것이나 오늘은 아니다 — 새 이상점으로 올리지 않는다 |

**개장 마감 후 사람 조치: 불필요.** 종료 시퀀스 5개 확인 전부 정상.

### 3-2. 미커밋 27→28건 — 09-16 대비 실질 변화, 처음으로 확정

**증상**: 09-16(27건)→09-17(27~28건)로 사람이 3회차째 "무엇이 줄었는지 diff 미대조"로 이월해 온 항목.

**근거**: 09-16 증거 다이제스트(`auto/2026-09-16_증거_post.md` §1)와 오늘 증거(`auto/2026-09-17_증거_post.md` §1)의 파일 목록을 줄 단위로 대조했다.

- 수정 4건(`mahdi/dashboard/app.py`·`data_source.py`·`scripts/daily_ops_report.py`·`scripts/start_mahdi_premarket.bat`) — **완전히 동일**
- 미추적 23건 중 21건 — **완전히 동일**(연구 문서 5개, `premarket_levels`·`mireuk_*` 계열 스크립트·모듈, `당일 맥점예측.txt`)
- 달라진 것은 **그날그날의 자기참조 파일 2줄뿐**: 09-16엔 `docs/동작점검/2026-09-16_마흐디_일일점검.md`·`auto/2026-09-16_지표.json`이 있었고, 오늘은 그 자리에 `2026-09-17_마흐디_일일점검.md`·`auto/2026-09-17_지표.json`이 있다 — 그날 만든 산출물이 다음날 스캔에서 사라지는 이유는 그 산출물이 **전전날치**가 되어 더는 미커밋 목록에 없어서가 아니라(둘 다 여전히 미커밋 상태), 단순히 파일명이 날짜로 바뀌었기 때문이다.
- 장중① 회차가 관찰한 "26→27 증가는 이 일일점검 파일 자신이 미커밋으로 잡힌 것"이라는 설명(§2-0)과 정합적이다.

**추가 발견 — 이 회차 자체 점검 중 28건 관측**: 이 보고서를 쓰는 도중 `git --no-optional-locks status --porcelain`을 다시 돌리니 **28줄**이 나왔다(15:46 스냅샷의 27줄에 `?? .claude/` 한 줄이 더 있다). `.claude/settings.local.json`의 최종 수정 시각은 **2026-08-31**로 오늘 새로 생긴 파일이 아니다 — 즉 오늘 무언가 새로 바뀐 게 아니라, 15:46 증거 수집이 이 디렉터리를 **놓쳤을 가능성**이 있다(수집기 코드는 일반 `git status --porcelain`을 그대로 쓴다 — `collect_evidence.py:1897`). 원인은 이번 회차에서 규명하지 못했다(재현 조건 불명 — git 인덱스 캐시 타이밍 차이로 추정하나 확정 아님).

**기준**: `docs/동작점검/README.md` 규약 G(증감 화살표를 그 자체로 판정하지 않는다) — 26→27→27→28의 오르내림은 실질 파일 증감이 아니라 **자기참조와 수집 시점 차이**로 전부 설명된다.

**영향**: 이번 회차로 "미커밋 건수가 매일 오르내리는 이유"가 **처음으로 완전히 해명**됐다 — 실질 드리프트는 3일째(09-15/16/17) **0건**이다. 다만 `.claude/` 누락 가능성은 증거 수집기의 신뢰도에 관한 별개 질문이라 남겨 둔다.

**심각도**: P2(정보성 — 새 결함 아님). §3-2가 이 관측 항목을 **판정 완료**로 닫는다.

### 3-3. `hypotheses.yaml` 미변경 — 09-16 이후 사흘째 한 줄도 안 바뀜

**증상**: 증거 §12-9 "`hypotheses.yaml`이 2026-09-16 보고서 이후 한 줄도 안 바뀌었다(mtime 2026-09-16)".

**근거**: 09-16 보고서가 등재·이동·확정을 지시한 항목들(§1-1의 `2026-08-26-p1-2-1` 등재 포함)이 사흘째 전부 무행동으로 남아 있다. 이 회차도 스킬 규약(§3 "하지 않는 것")에 따라 `hypotheses.yaml`의 `상태`를 직접 바꾸지 않는다 — 아래 §3-5·§3-6에서 **제안**만 적는다.

**기준**: 스킬 대원칙 — "제안은 근거와 함께 반드시 쓴다. 제안이 없으면 사람이 옮길 것도 없다."

**영향**: 판정 대기 가설이 계속 쌓여서, 다음에 사람이 한 번에 정리하려면 오늘까지 최소 6건(§1-5의 4건 + §3-5의 2건)을 검토해야 한다.

**심각도**: P1(관측·정확성 훼손 — 개선 효과 검증이 구조적으로 막혀 있다). 새 발견은 아니다.

### 3-4. 옵션체인 완전 전멸(ERROR) — 15:00:52~15:01:53, 2분간 판단이 어제 스냅샷을 대신 썼다 🆕 (1-9)

**증상**: 마감 40분 전, 옵션체인이 그 분(15:00·15:01) 동안 **20레그 중 0행**만 적재됐다. 오늘 유일한 ERROR 레벨 로그 2건이 이 사건이다.

**근거**:
- `docs/동작점검/auto/2026-09-17_증거_post.md` §4: `15:00:52 [ERROR] 옵션체인 이번 분 전멸 — 수집 20레그 중 적재 0행(예산 48.8초 소진, 재시도 함). 이 분의 체인은 비었다 — 판단은 신선도 창 안의 직전 스냅샷을 쓴다`, `15:01:53` 같은 메시지
- 같은 분에 WARNING `chain_total_failure` 2건 동반: `15:00:28 옵션 체인 폴링 전체 실패 — 5초 후 재시도`, `15:01:28` 동일(증거 §5)
- 증거 §5-1: "최장 연속 `rows=0` 구간: **2분**(15:00~15:01, 임계 20분)" · "단발 완전실패(앞뒤가 정상인 `rows=0` 분): **0건**" — 즉 이 2분은 **연속** 사건이지 별개의 두 사고가 아니다

**기준**: `docs/동작점검/README.md`·`mahdi/main.py`의 설계 — 그 분의 체인이 비면 판단은 신선도 창 안의 직전 스냅샷으로 대체한다(폴백 설계). 임계(`ZERO_ROW_RUN_ALERT_MINUTES` 20분)에는 한참 못 미쳐 "절벽"으로 분류되지 않는다.

**영향**: 15:00~15:01 두 분 동안 프로그램이 내린 판단은 그 순간의 실제 옵션 시세가 아니라 그 직전(대략 1분 전)의 스냅샷을 보고 내려졌다. 폴백이 정상 동작해서 판단 자체가 멈추지는 않았지만, 그 2분 동안은 시세가 실제로 얼마나 움직였는지 프로그램이 몰랐다는 뜻이다. 오늘은 그 순간 큰 가격 변동이 없어 실질적 피해는 없었던 것으로 보인다(§1-8의 14:26 스파이크와 마찬가지로 마감 직전 KIS 혼잡이 원인으로 보인다).

**심각도**: P2(설계된 폴백이 정상 작동 — 즉각 피해 없음). 다만 **오늘 처음으로 "완전 전멸"이 실제로 일어난 날**이라 §1-6/§1-8과 같은 뿌리(마감 시간대 KIS 응답지연 누적)의 연장선에서 기록해 둔다. 새 Fix는 제4부 Fix C로 제안한다.

### 3-5. 시한 도래 가설 2건 — 오늘도 트리거가 안 떠 판정 불가, "사건 기반 재설정" 제안

`2026-09-16-recovery-hint-carries-its-sample-count`·`2026-09-16-cliff-day-marks-the-hypothesis-table-itself` 두 가설 모두 오늘이 검증예정일이었다. 둘 다 트리거 조건(워치독 `no_ingest` DEGRADED 경보 / 20분 이상 옵션체인 절벽)이 **오늘 하루 한 번도 발생하지 않았다**(증거 §6: DEGRADED 0건 · §5-1: 최장 연속 0행 2분, 임계 20분 미달).

**판단**: 날짜 시한을 하루 더 미루는 것은 의미가 없다 — 이 두 가설은 애초에 "특정 사건이 일어난 날"에만 판정 가능한 조건부 가설이라, 달력 날짜로 시한을 걸면 그 사건이 안 일어나는 한 영원히 `pending`으로 남는다. **이미 `NEXT_TODO.md`(증거 §8-3, 923행 인근 ":360")가 정확히 이 문제를 지적하며 "위 두 가설의 검증예정일을 날짜가 아니라 사건으로 재설정할 것"을 제안해 두었다** — 이번 회차가 그 제안을 다시 확인하고 구체화한다.

**제안(사람 확정 필요, `hypotheses.yaml` 직접 수정 안 함)**:
- `2026-09-16-recovery-hint-carries-its-sample-count`: `검증예정일`을 고정 날짜 대신 "다음 워치독 `no_ingest`/DEGRADED 에피소드 발생일"로 재정의. 그날 장후 회차가 그 경보의 첫 줄 참고 문구를 직접 인용해 판정한다.
- `2026-09-16-cliff-day-marks-the-hypothesis-table-itself`: `검증예정일`을 "다음 `zero_row_longest_run.length >= 20분` 발생일"로 재정의.
- 두 항목 다 `상태`는 `pending` 유지(→ 사용자 조치 3번, 제4부 Fix A).

### 3-6. dev_memory 반영 — 이번 회차는 직접 수정하지 않는다 (스킬 표준 절차와 달리, 이번 예약 작업 지시가 명시적으로 금지함)

> 이 회차(자동 장후)는 스킬 §6 표준 절차(장후가 dev_memory에 직접 append)를 따르지 않는다.
> 이번 예약 작업의 지시문이 "dev_memory를 고치거나 커밋하지 않는다 — 초안만 이 파일 끝에 적어 둔다"를
> 명시적으로 요구하므로, 아래는 **다음에 사람(또는 다음 세션)이 반영할 초안**이다.

**`DECISION_LOG.md`에 추가할 항목(초안, `[MW0601]` 헤딩)**:
- 09-17: 미커밋 건수가 26→27→27→28로 오르내린 이유를 처음으로 완전히 해명(§3-2) — 실질 드리프트 09-15/16/17 사흘째 0건, 전부 자기참조 산출물 파일명 교체 + 증거 수집 시점의 `.claude/` 누락 추정. `mahdi-daily-check` 스킬의 "미커밋 diff 대조" 관측 항목을 여기서 판정 완료로 닫는다.
- 09-17: 옵션체인 완전 전멸(ERROR) 오늘 처음 발생(15:00~15:01, 2분) — 폴백 정상 동작, 임계(20분) 크게 미달. §1-6/§1-8과 같은 마감 직전 KIS 혼잡이 원인으로 추정.
- 09-17: KIS 응답지연 "이틀 연속" 계산기 문제 — 09-14부터 세면 **나흘 연속**(09-14/15/16/17)인데 계산기는 매번 "이틀"로만 보고. 기존 §8-4/Fix D 트랙에 날짜와 함께 추가.

**`NEXT_TODO.md`에 추가할 항목(초안)**:
- [ ] Fix A(P2, §4): `hypotheses.yaml`의 `2026-09-16-recovery-hint-carries-its-sample-count`·`2026-09-16-cliff-day-marks-the-hypothesis-table-itself` 두 가설의 `검증예정일`을 날짜 대신 사건 조건으로 재설정 — 이미 8-3(:360)이 지적한 항목, 09-17 장후가 제안 구체화.
- [ ] Fix B(P2, §4): KIS 응답지연 연속일수 사이드카(§8-4/Fix D 트랙) — `auto/{날짜}_지표.json`을 과거 N일치 순회해 실제 연속일수를 계산하는 헬퍼를 `collect_evidence.py`에 추가.
- [ ] Fix C(P2, §4): "옵션체인 이번 분 전멸"(ERROR, rows=0) 건수를 `daily_ops_report.py` §1 「한눈에」 표에 행으로 추가 — 현재는 §5-1 산문에만 있어 매일 한눈에 보이지 않는다.
- [ ] 증거 수집기(`collect_evidence.py`)의 `git status --porcelain` 스냅샷이 `.claude/`를 놓친 것으로 보이는 건(§3-2) 재현 조건 확인.

**`CURRENT_STATE.md` 갱신할 것**: 「알려진 자잘한 문제」 절에 "미커밋 건수 오르내림 — 자기참조 파일 교체가 원인, 실질 드리프트 없음(09-17 확정)" 한 줄 추가.

---

## 종합 판정표 — 오늘 하루, 국면별로 판정이 어떻게 바뀌었는가

| 시각 | 국면 | §0 한 줄 판정 | 새로 찾은 문제(P0/P1/P2) |
|---|---|---|---|
| 08:02 | 장전 | ✅ 정상. 사람 조치 불필요(관측 07:30~08:02) | 0/0/0 (전부 이월 항목) |
| 12:35 | 장중① | ✅ 정상 지속. 사람 조치 불필요(관측 07:30~12:35) | 0/0/1(§1-6) |
| 14:40 | 장중② | ✅ 정상 지속. 사람 조치 불필요(관측 07:30~14:40), 다만 오후 지연이 처음 경고선 근접 | 0/0/2(§1-7·§1-8) |
| 15:46 | 장후(이 회차) | ✅ **하루 전체 정상 마감.** 거래 0건(설계상 정상), 사람 조치 불필요 | 0/0/1(§3-4=1-9). §1-8은 ⬆️ 격상 위험 신호로 재평가(위험선 근접 폭 확대) |

**국면별 판정이 뒤집힌 곳은 없다** — 장전의 "정상" 판정이 장중·장후에 부정된 적은 하루 종일 한 번도 없었다. 유일하게 강도가 올라간 것은 §1-6→§1-8(응답 지연 패턴)로, 경고선 근접(14:26)에서 위험선 연속 7창(14:51~15:21)까지 오후 내내 서서히 나빠졌다가 15:20 마감과 함께 멈췄다.

## 당일 이상점 통합 대장 — 1-1부터 1-9까지, 빠짐없이

| 번호 | 제목 | 최초 발견 국면 | 최종 심각도 | 최종 처분 | 근거 위치 |
|---|---|---|---|---|---|
| 1-1 | `hypotheses.yaml` 언급-실재 불일치(`2026-08-26-p1-2-1`) | 장전 | P2 | 🔄 지속 — 내일(09-18) 소멸 예상 | §1-1, §3-0 |
| 1-2 | 미커밋 26~28건, 사람 결정 대기 | 장전 | P2 | 🔄 지속(정리 결정 대기) / diff는 ✅ 판정 완료 | §1-2, §3-2 |
| 1-3 | ZN·ES 해외선물 조회 실패 | 장전 | P2 | 🔄 지속(만성, 계좌 설정 문제) | §1-3, §3-0 |
| 1-4 | 이벤트 캘린더 9월분 미갱신 | 장전 | P2 | 🔄 지속(20일째, 의도적 방치) | §1-4, §3-0 |
| 1-5 | 검증 시한 초과 가설 4건 | 장전 | P1 | 🔄 지속(21일 지남 항목 포함, 손 판정 대기) | §1-5, §3-0 |
| 1-6 | KIS 응답지연 — "이틀"로 세는 도구가 실제론 나흘째를 놓친다 | 장중① | P2 | 🔄 지속(나흘째, 09-14~09-17) | §1-6, §3-0 |
| 1-7 | 먼슬리 되살리기 완전 미회복 오전0→오후 | 장중② | P2 | 🔄 지속(오늘 최종 완전미회복 3건, 전일 6건보다 개선) | §1-7 |
| 1-8 | 시세 조회 지연 — 경고선 최초 돌파(14:26) | 장중② | P2 | ⬆️ 격상 위험(위험선 연속 7창, 14:51~15:21) — 위험선 자체는 안 넘음 | §1-8, §3-0 |
| 1-9 | 옵션체인 완전 전멸(ERROR) 15:00~15:01, 2분 | 장후 | P2 | 🆕 신규 — 폴백 정상 동작, 즉각 피해 없음 | §3-4 |
| (관측) | `2026-09-16-recovery-hint-carries-its-sample-count` | 장전(등재는 09-16) | — | ➡️ 판정 불가로 마감, 사건 기반 재설정 제안(§3-5) | §3-5 |
| (관측) | `2026-09-16-cliff-day-marks-the-hypothesis-table-itself` | 장전(등재는 09-16) | — | ➡️ 판정 불가로 마감, 사건 기반 재설정 제안(§3-5) | §3-5 |

**심각도 집계**: P0 0건 · P1 1건(1-5) · P2 8건(1-1~1-4, 1-6~1-9) · 관측(미판정) 2건. 오늘 새로 찾은 것은 **1-9 한 건**(그 외 1-8은 기존 이상점의 심각도 재평가).

## 종가 기준 최종 손익 / 절대원칙 판정

**거래 없음.** 오늘 체결·포지션 **0건**(`db.tables.execution_logs.rows=0`, `db.positions.rows=0` — 증거 §16-1·§16-2, 지표 §16-1 "그날 보유 포지션 기록이 없다(행 0)"). 이유는 결함이 아니라 설계다 — **주문을 실제로 넣는 경로가 아직 배선되지 않은 관측 전용(ADVISORY) 단계**라, 프로그램이 아무리 강한 신호를 내도 실제 매수·매도는 애초에 일어나지 않는다(`mahdi/main.py` 헤딩 "관측 전용 오케스트레이터", `NEXT_TODO.md`: "`trade_history`에 실제로 쓰는 곳이 아직 없다 — 실주문 실행이 없어서").

| 항목 | 값 | 판정 |
|---|---|---|
| 실현 손익 | 0원 | 해당 없음(거래 없음) |
| 미실현 손익 | 0원 | 해당 없음(보유 포지션 없음) |
| 포지션 수 | 0 | ✅ 정상(설계상 관측 전용) |
| 계약 수 | 0계약 | ✅ 정상 |
| 절대원칙(하드 리밋·금지 사항) 위반 | 검사 대상 없음 | ✅ 위반 없음(거래가 없으므로 위반할 대상 자체가 없다) |

오늘 프로그램이 낸 판단(진입 시그널)은 여러 차례 있었으나("진입 판단은 섰는데 살 종목이 없다 — 사유=no_strike_match" 등) 전부 시뮬레이션·관측 목적이고 실제 주문으로 이어지지 않았다. 실주문 경로 배선은 `2026-08-19-fix2-roundtrip-leaves-db-evidence` 가설이 이미 그 완료 여부를 추적 중이며, 오늘도 `execution_logs` 행이 0인 것으로 보아 아직 배선 전이다(§1-5).

## 제4부 — Fix 구현계획 (P0/P1/P2)

### Fix A [P2] — 시한 도래 가설 2건, 검증예정일을 날짜에서 사건으로 재설정

`docs/동작점검/hypotheses.yaml`의 `2026-09-16-recovery-hint-carries-its-sample-count`·`2026-09-16-cliff-day-marks-the-hypothesis-table-itself` 두 항목.

- **원인**: 두 가설 모두 "특정 사건(워치독 DEGRADED 에피소드 / 20분+ 절벽)이 일어난 날"에만 판정 가능한데, `검증예정일`이 고정 달력 날짜(2026-09-17)로 박혀 있다. 그 사건이 계속 안 일어나면 시한만 매일 지나가고 판정은 영원히 못 한다.
- **바꿀 것**: `검증예정일` 필드를 고정 날짜 대신 조건부 표현으로 바꾸거나(예: `검증예정일: null` + `검증조건: "다음 watchdog.degraded_checks > 0 인 날"`), `docs/동작점검/README.md`의 가설 스키마 설명에 "사건 기반 가설은 날짜 시한을 걸지 않는다"는 규약을 추가한다.
- **회귀 위험**: `tests/test_ops_hypotheses.py`가 `검증예정일` 필드의 존재를 전제하고 있을 수 있다 — 필드를 없애기보다 `null` 허용으로 확장하는 편이 안전하다.
- **검증**: `pytest tests/test_ops_hypotheses.py` · 다음 no_ingest 에피소드/절벽 발생일에 실제로 판정이 되는지 관측.
- **적용 시점**: 사람 결정 후 다음 장전(→ 사용자 조치 3번).

### Fix B [P2] — KIS 응답지연 연속일수 사이드카 (기존 §8-4/Fix D 트랙 구체화)

`docs/동작점검/tools/collect_evidence.py`

- **원인**: §5-1의 "이틀 연속" 판정이 항상 전날 파일 하나와만 비교해서, 실제로는 09-14부터 나흘째 이어지는 패턴을 매일 "이틀"로 축소 보고한다(§1-6).
- **바꿀 것**: `collect_evidence.py`에 `_consecutive_latency_days(root, date, max_lookback=10)` 헬퍼를 추가 — `auto/{date}_지표.json`을 과거로 순회하며 같은 시간대·엔드포인트 조합이 연속으로 임계(2.5초)를 넘긴 날수를 센다. §5-1 문구를 "이틀 연속" 대신 이 함수의 반환값으로 교체.
- **회귀 위험**: 과거 `_지표.json`이 없는 날(사이드카 부재 기간)은 조회 실패로 처리하고 "N일 이상(과거 파일 없음)"으로 표기해야 한다 — 없는 파일을 0일로 잘못 셀 위험.
- **검증**: 09-14~09-17 네 날짜로 재실행해 "나흘"이 나오는지 단위 테스트.
- **예측치**: `hypotheses.yaml`에 새 id 등재, 주장 지표 `evidence.consecutive_latency_days` — 09-14~09-17 구간 재계산 시 4가 나와야 한다.
- **적용 시점**: 다음 착수 세션(장후).

### Fix C [P2] — 옵션체인 완전 전멸(ERROR, rows=0) 건수를 §1 한눈에 표에 노출

`scripts/daily_ops_report.py`

- **원인**: 오늘 1-9(15:00~15:01 전멸) 같은 사건이 지금은 §5-1 산문에만 있어, §1 요약표만 보는 사람은 놓칠 수 있다.
- **바꿀 것**: §1 「한눈에」 표(「판단 입력」 하위 표)에 "ERROR 전멸 분 수" 행을 추가 — `db.chain_minute_coverage.zero_row_minutes_error_level`(가칭)을 집계해 오늘/전일 나란히 인쇄.
- **회귀 위험**: 기존 "최장 연속 0행 구간"·"단발 완전실패"와 정의가 겹치지 않게 — 이 새 행은 **ERROR 레벨 로그 건수**(2건)를 세고, 기존 두 행은 **DB `rows=0` 분 수**(2분)를 센다는 것을 주석으로 명확히 구분한다.
- **검증**: `pytest tests/test_ops_daily_report.py`(있다면) · 09-17 재실행 시 "2건"이 나오는지 확인.
- **적용 시점**: 다음 착수 세션(장후).

## 제5부 — 고도화 방안 (당일 관측에서 출발한 것만)

1. **지연 경고선을 "선행 신호"에서 "동시 신호"로 재정의할지 검토(기존 NEXT_TODO 고도화 2 트랙)** — 오늘 14:26에 p50 경고선(0.8)을 넘은 뒤 24분 뒤(14:50)가 아니라 **35분 뒤(15:00~15:01)**에야 실제 전멸이 왔다. 08-14의 "0.77에서 24분 뒤 절벽" 전례와 오늘 "0.84에서 34분 뒤 전멸"을 나란히 놓으면, 경고선 돌파와 실제 사고 사이 간격이 24~34분으로 흔들린다 — 표본이 둘뿐이라 아직 결론은 못 내리지만, 세 번째 사례가 나오면 "선행 신호로서 몇 분 전에 우는가"를 확정할 수 있는 첫 데이터가 모이는 중이다.
2. **미커밋 건수 표시를 "총 건수"에서 "자기참조 제외 건수"로 바꾸는 것 검토** — §3-2에서 3일째 실질 드리프트 0건을 확인했다. 매일 파일명이 바뀌는 자기참조 2줄(그날의 일일점검·지표 파일)을 계산에서 빼면, 진짜 새로 생긴 미커밋만 남아 "26→27→27→28"처럼 매번 사람이 다시 확인해야 하는 헛수고가 줄어든다.
3. **§3-4(1-9)와 §1-8의 인과를 다음에 절벽이 나면 초 단위로 맞춰 볼 것** — 오늘은 경고선 돌파(14:26)와 전멸(15:00~15:01) 사이에 34분의 간격이 있었고 그사이 p95 위험선 연속 창이 계속 쌓였다(14:51~15:21). 다음에 비슷한 패턴이 나오면 "위험선 연속 몇 창째부터 전멸이 오는가"를 셀 수 있는 두 번째 표본이 된다.

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (장 마감 직후)

**사람 조치 없음.** 오늘 하루 P0 0건, 종료 시퀀스 전부 정상, 거래 0건(설계상 정상).

### 오늘 중 / 이번 주 중

1. **가설 2건의 검증예정일을 사건 기반으로 재설정할지 결정** — `2026-09-16-recovery-hint-carries-its-sample-count`·`2026-09-16-cliff-day-marks-the-hypothesis-table-itself`. 근거와 구체안은 §3-5·제4부 Fix A. 승인하면 다음 장전 회차가 `hypotheses.yaml`을 수정한다.

### 결정이 필요한 것 (자동으로 못 하는 것)

2. **검증 시한 초과 가설 4건을 `inconclusive`(판정 불가)로 전환할지 확정** — 특히 `2026-08-26-p1-3-preemptive-rule-fires-at-premarket`은 **21일** 지났다. (A) `inconclusive`로 닫고 시한을 다음 발동 기회로 재설정 vs (B) 그대로 pending 유지. (→ §1-5)
3. **사전 대응 규칙(`2026-08-04-p5`, 위클리 폴링 2분→4분 격분) 발동 여부 — 3주 넘게 미결정.** 오늘도 조건 성립(19개 구간, §5-1). (A) 다음 장전에 발동 vs (B) 계속 유예(단, 유예할수록 §1-6/§1-8/§3-4(1-9) 같은 사건이 반복될 가능성이 있다). 발동 창은 다음 거래일 장전(07:30 전)뿐이다.
4. **미커밋 27~28건 정리** — (A) 커밋할 것/`.gitignore`에 뺄 것/폐기할 것 3분류를 정한다 vs (B) 당분간 그대로 둔다(실질 위험은 §1-2에서 지적한 대로 `start_mahdi_premarket.bat`이 미추적 파일에 의존하는 것 하나뿐 — 오늘로 최소 3일째 실질 드리프트는 없었다). (→ §1-2, §3-2)
5. **이벤트 캘린더 9월분 채우기 — 20일째.** 외부 일정(금리 발표 등)을 확인해 `mahdi/config/event_calendar.yaml`의 `covered_through`를 갱신해야 한다(코드 밖의 일). (→ §1-4)
6. **ZN/ES 해외선물 거래소 권한 신청** — 증권사 계좌 설정(코드 밖의 일, 만성). (→ §1-3)
7. **Fix A/B/C(제4부) 착수 여부** — 착수 지시가 있으면 다음 세션이 예측치를 먼저 등재하고 시작한다.

### 이번 회차에 사람이 할 일이 없으면

해당 없음(위 항목 참조) — 다만 **P0/P1 성격의 긴급 사안은 없다.** 위 1~7번은 전부 "당장" 이 아니라 "결정이 나면 다음 세션이 처리" 범주다.
---

## 제6부 — 장후 자동조치 구현 결과 (16:26~17:0x · 자동)

> 예약 작업 `mahdi-postmarket-autofix`. **앞 회차(제1부~제5부·사용자 조치) 본문은 한 글자도
> 고치거나 지우지 않았다.** 정정이 필요한 것은 이 절 안에서만 적는다(§6-8).

### 6-0. 착수 전 가드

| 가드 | 결과 |
|---|---|
| 장후 절 존재 | ✅ 제3부 351행 · 제4부 508행 |
| 완결 표기 | ⚠ **없다 — 이레째.** 아래 §6-9 |
| 중복 실행 | ✅ 상태파일 날짜가 09-16이라 새 회차. 리포트 SHA256 `2242114B…D05`(71,445바이트 · 570행 · mtime 16:18:11) |
| 장중 금지 | ✅ 16:26 착수 · 마흐디 프로세스 **0개**(살아 있던 python 1개는 `py37_32` 환경의 pytest로 이 저장소 무관) |
| 브랜치 | ✅ `master` · 당일 커밋 0건이라 병행 세션 없음 |

착수 전 `NEXT_TODO.md`의 「폐기·종결된 안건」과 「⚠ 다시 올리지 말 것」을 통째로 읽었다.
**오늘 리포트는 폐기 항목을 하나도 올리지 않았다** — 기각한 항목 0건이다.
⛔ Slack 토글은 켜지 않았다.

### 6-1. Fix B — 「이틀 연속」이 실은 **22거래일째**였다 🔴 이 회차의 발견

**왜.** §1-6이 지적한 대로 §5-1의 🔔 줄은 조건이 며칠째 서 있든 언제나 「이틀 연속」이라
적는다 — 전날 사이드카 하나와만 대조하기 때문이다. 그래서 09-15·09-16·09-17 세 회차가
매번 「오늘도 그냥 이틀이네」로 읽었다.

**한 것.** `docs/동작점검/tools/collect_evidence.py`에 `consecutive_latency_days()`와
`_all_endpoint_breach_hours()`를 더했다. 기준일부터 거슬러 오르며 **그날과 그날의 직전
거래일**이 같은 `(엔드포인트, 시)`에서 함께 임계를 넘었는지 보고, 성립하면 하루 세고
직전 거래일로 옮겨 다시 본다. §5-1 🔔 줄 **뒤에** `📅 N거래일째(D부터)` 한 줄이 덧붙는다.

**🔴 등재한 예측치가 첫날 어긋났다 — 숨기지 않는다.**

리포트 제4부 Fix B는 *"09-14~09-17 네 날짜로 재실행해 **나흘**이 나오는지"* 를 검증으로
적었고, 나도 그대로 **4**를 `hypotheses.yaml`에 등재했다(코드보다 먼저, 커밋 `001d263`).
구현 후 실측은 **22거래일째 · 2026-08-19부터 · 사유 「끊김」**이다. 재현되는 수다:

| 기준일 | 직전 거래일 | 전 엔드포인트 겹침 | (참고) `inquire-price`만 |
|---|---|---|---|
| 09-17 | 09-16 | 18 | 6 |
| 09-14 | 09-11 | 17 | 6 |
| 09-01 | 08-31 | 16 | 7 |
| 08-21 | 08-20 | 22 | 7 |
| 08-19 | 08-18 | 16 | 6 |
| **08-18** | **08-17** | **0 — 여기서 끊긴다** | 0 |

리포트의 「나흘」이 틀린 이유는 계산이 아니라 **기억의 길이**다 — §1-6이 근거로 든
09-15 §1-7의 기록이 09-14까지만 거슬러 올라갔다. 09-13 이전을 아무도 이어 세지 않았고,
그래서 「나흘」 자체가 **이 fix가 고치려던 바로 그 형태의 축소 보고**였다.
⚠ 경계 08-17은 대체공휴일 형태(빈 사이드카)가 **아니다** — 482사이클 · 9시간대가 실린
정상 거래일이고, 08-18이 그날과 겹치는 시간대가 없어 정말로 끊긴 것이다.

⛔ **가설의 상태·문구는 손대지 않았다.** 사후에 예측을 고치는 것이 이 체계가 막는 바로
그것이다. `2026-09-17-fixb-…`는 `pending`으로 두고 **판정은 내일 사람이 한다.**

**안 하면.** 「이틀」이라는 글자가 **한 달째** 같은 무게로 인쇄되고, 사전 대응 규칙
`2026-08-04-p5`가 3주 넘게 미발동인 이유(=조건이 특별한 날에 서는 것이 아니라 **상시로
서 있다**)를 아무도 못 본다. 사용자 조치 3번의 (A)/(B) 판단이 달라질 수 있는 사실이다.

**걸린 시간.** 함정 게이트 포함 약 35분.

⛔ 임계(2.5초)·발동 조건(「이틀 연속」)·발동 창(다음 거래일 장전)·🔔 문구·`flags` 문장
전부 무변경. 바꾼 것은 **무엇을 재는가**이지 **얼마부터 위험한가**가 아니다.

### 6-2. Fix C — §1 「한눈에」에 옵션체인 전멸(ERROR) 행

**왜.** §3-4(1-9) — 15:00:52·15:01:53 두 분 동안 판단이 그 순간의 시세가 아니라 직전
스냅샷을 보고 났는데, 그 사실은 §5-1 **산문에만** 있었다. §1만 읽는 사람은 평범한 날로 읽는다.

**한 것.** `mahdi/ops/report.py`에 `HEADLINE_JUDGEMENT_LOG_METRICS`(한 줄)를 두고
`_render_headline()`이 판단 입력 표 **맨 뒤에** 붙인다. 그리고 규약 C —
`mahdi/ops/log_metrics.py`의 `_QUALITATIVE_ALWAYS_PRESENT`에 `chain_cycle_empty`를
**등록**했다. 등록이 없으면 전멸 0건인 날 칸이 `—`가 되고, `—`를 「좋았다」로 읽은 것이
08-14 사고다.

**첫 실측에서 바로 값을 했다** — 오늘 산출물로 다시 그리니:

| 지표 | 오늘 | 전일(09-16) | Δ |
|---|---|---|---|
| **최장 연속 0행 구간** | 2분 | 89분 | ▼87 ✅ |
| **단발 완전실패** | 0건 | 6건 | ▼6 ✅ |
| **옵션체인 전멸(ERROR)** | **2건** | **100건** | ▼98 ✅ |

09-16의 **100건**은 어느 §1에도 없던 수다. 두 축이 정의가 다르다는 것(ERROR 로그 건수
≠ DB `rows=0` 분 수)도 이 대비가 증명한다 — 09-16은 89분 절벽에서 전멸 선언이 100번
났다.

**안 하면.** 오늘 같은 「폴백이 잘 돌아서 조용한 날」의 2분이 계속 산문 속에 묻힌다.

**걸린 시간.** 약 25분.

⛔ 임계 없음(몇 건부터 위험한가는 긋지 않았다). 기존 DB 네 행은 **칸 단위로 무변경**이고
새 행은 맨 뒤에만 붙는다.

### 6-3. 착수하지 않은 것 — C등급과 사유

| 항목 | 사유 |
|---|---|
| **제4부 Fix A**(가설 2건 검증예정일을 사건 기반으로) | 「사용자 조치 1번」이 *"승인하면 다음 장전 회차가 수정한다"* 로 **승인 게이트**를 걸었고, 내용도 `검증예정일` 스키마를 바꾸는 **규약 변경**이다. `NEXT_TODO`에 등록만 했다 |
| **제5부 고도화 1**(지연 경고선을 선행→동시 신호로 **재정의**) | 리포트 자신이 *"표본이 둘뿐이라 아직 결론은 못 내린다"* 고 적었다. 그리고 경고선 **재정의**는 임계 변경이다 |
| **제5부 고도화 2**(미커밋 건수를 자기참조 제외로) | 「바꾸는 것 **검토**」이고 예측치(주장·대가)가 없다 — 규약상 방안이 아니라 아이디어다 |
| **제5부 고도화 3**(다음 절벽 때 초 단위 대조) | 코드 변경이 아니라 **다음 절벽일의 관측 지시**다. 오늘은 절벽이 없었다(최장 2분) |
| **사용자 조치 2~7번** | `### 결정이 필요한 것` 절 전부 — 자동조치가 손대지 않는다. `NEXT_TODO`에 등록 |

**고도화는 오늘 0건 착수했다.** 셋 다 「검토할지」·「맞춰 볼 것」이라 예측치를 적을 수
없었고, 예측치를 못 적는 항목은 착수하지 않는 것이 규약이다.

### 6-4. `hypotheses.yaml` — 등재 2건 · 상태 확정 **0건**

등재(코드보다 **먼저**, 커밋 `001d263`):

- `2026-09-17-fixb-latency-streak-counts-past-two-days` — 검증예정일 09-18 · `pending`
- `2026-09-17-fixc-chain-wipeout-shows-in-the-headline` — 검증예정일 09-18 · `pending`

⚠ **Fix C의 예측치 출처를 밝힌다.** 리포트 제4부 Fix C에는 「예측치」 항목이 **없다**.
주장은 그 항목의 「검증」 줄에서, 대가는 「회귀 위험」 줄에서 **그대로 옮겼다** — 지어낸
문장은 아니지만 리포트가 예측치로 적어 둔 것도 아니다. 그 사실을 가설의 `주의:`에
적어 뒀으니 **사람이 보고 무르면 무를 것.**

**상태 확정 0건.** 오늘 §3-5는 두 가설에 대해 「검증예정일 재설정」을 제안했을 뿐
**상태 판정을 제안하지 않았다**. §1-5의 시한 초과 4건은 제안이 `inconclusive`이고,
그것은 자동 확정에서 명시적으로 제외돼 있다. 판정 대기 `pending` **8건**(6 → 오늘 2건 추가).

### 6-5. 테스트

**2,337 → 2,358 passed / 0 failed.** 신규 **21건**:

- `tests/test_evidence_collector_latency_streak.py` **10건** — 연속 세기 3 · 규약 C 2 ·
  축과 임계 2 · **판정 무변경 3**(임계·규칙 id 불변 / 기존 🔔 문구 부분문자열 불변 /
  이 축에 임계 선언 없음)
- `tests/test_ops_report_chain_wipeout_row.py` **11건** — 행 게재 3 · 규약 C 3 ·
  정의 분리 2 · **판정 무변경 3**(기존 네 행 칸 단위 동일 / 임계 없음 / DB 없는 날에도 생존)

`hypotheses.yaml` 무결성: **190항목** 파싱 · `pending` 8 · `test_ops_hypotheses.py` 69건 통과 ·
CRLF 0건. 손댄 파일 5개 전부 LF 확인.
⚠ **ruff는 .venv에 미설치라 못 돌렸다 — 열한 회차 연속**(09-02·03·04·07·08·10·11·14·15·16·17).

### 6-6. 커밋

| 해시 | 내용 |
|---|---|
| `cf70715` | 당일 점검 산출물 — 09-17 보고서 + `_지표.json` |
| `001d263` | 예측치 선등재 2건 (**코드보다 먼저**) |
| `8c71d3d` | Fix B · Fix C + 회귀 테스트 21건 |
| (이 절) | dev_memory 3파일 + 리포트 제6부 |

⛔ `git add .`을 쓰지 않았고 **사람 미결 미커밋을 하나도 건드리지 않았다.** 오늘 커밋한
산출물 2건은 **오늘 생성된 것**이다.

### 6-7. 산출물을 재생성하지 않았다

§6-2의 새 행은 **09-18 `_지표.md`부터** 보인다. 09-17 `_지표.md`에는 없다 — 09-04가
적은 대로 **재생성은 절반만 복구**이고, 오늘 것을 다시 만들면 15:46의 실제 관측이
지금 시각의 재집계로 덮인다. §6-1의 `📅` 줄도 **다음 증거 수집부터** 보인다.

### 6-8. 정정 — 리포트가 지목한 파일이 틀렸다 (**여섯 번째**)

⛔ 앞 회차 본문은 고치지 않고 여기에만 적는다.

**제4부 Fix C**: 리포트는 자리를 `scripts/daily_ops_report.py`(§1 「한눈에」 표)로
적었으나, §1을 실제로 그리는 것은 **`mahdi/ops/report.py`의 `_render_headline()`**(551행)이고
`daily_ops_report.py`는 결과를 파일로 쓸 뿐이다. **이 구분이 또 결과를 갈랐다** —
`daily_ops_report.py`에는 사람의 미커밋 28줄이 있어 손대면 미결 변경을 같이 올리게
되지만(09-15 Fix 10 · 09-16 Fix 2가 막힌 자리), `report.py`는 워킹트리가 깨끗해
**착수할 수 있었다.** 확인 안 했으면 오늘도 「미커밋에 막혔다」로 닫았을 것이다.

**09-16과 같은 정정이 같은 파일 쌍에서 같은 이유로 또 났다.** 「없는 파일 지목 / 이미 있는
기능」 계열 **여섯 번째**이고 여섯 번 다 게이트가 걸렀다. 이 정도 반복이면 리포트를 쓰는
회차 쪽에 「`daily_ops_report.py`는 집계·파일쓰기, 렌더링은 `mahdi/ops/report.py`」를
박아 둘 때가 됐다 — `NEXT_TODO`에 올렸다.

**또 하나 — 세는 축은 이미 있었다.** Fix C가 필요로 한 `qualitative.chain_cycle_empty`는
2026-08-05부터 집계되고 있었다. **없던 것은 계측이 아니라 §1까지 오는 경로**다
(08-25 P1-1 ③과 같은 형태).

### 6-9. 완결 표기가 또 없다 — **이레째**

스킬 §1-2가 요구하는 「이 파일은 여기서 닫힌다」·「사용자 조치 — …최종본」이 오늘도
**둘 다 없다**(09-09·10·11·14·15·16과 같다 — 템플릿에서 빠진 것이고 규칙대로면 이 작업은
영영 안 돈다). 진행 근거 넷: ① 「사용자 조치」가 「이번 회차에 사람이 할 일이 없으면」
절까지 갖춰 하루를 닫는다 ② 「종합 판정표」·「당일 이상점 통합 대장」·「종가 기준 최종
손익」 세 종합 절이 전부 있다 ③ 파일이 16:18:11 이후 정지(확인 16:26) ④ 마흐디 프로세스 0개.
⚠ **상시화 금지** — `NEXT_TODO`에 사용자 몫으로 **이레째** 올렸다.

### 6-10. 사용자 몫으로 남은 것

1. 🔴 **사전 대응 규칙 `2026-08-04-p5` 발동 여부(사용자 조치 3번)** — §6-1이 판단 재료를
   하나 바꿨다: 조건은 「나흘째」가 아니라 **22거래일째** 서 있다. 3주 넘게 미결정인 이유가
   「아직 특별한 날이 아니어서」가 아니라 **「한 달 내내 특별해서」**일 수 있다.
   발동 창은 다음 거래일 장전(07:30 전)뿐이다.
2. **Fix A 승인 여부**(사용자 조치 1번) — 가설 2건의 검증예정일을 사건 기반으로 재설정.
3. **가설 4건 `inconclusive` 전환 확정**(사용자 조치 2번) — `2026-08-26-p1-3…`은 **21일** 지났다.
4. 🔴 **미커밋 정리 — 9일째.** `daily_ops_report.py`의 28줄이 여전히 그 파일의 항목을 막는다.
   `start_mahdi_premarket.bat`이 **미추적** `premarket_levels_publish.py`를 부르는 구조적
   위험(멀티 PC 이식성)도 그대로다.
5. **`.gitignore:52`** — `_지표.md`·`_증거_*.md`·`_지연창.tsv`를 전부 거른다. **09-08 제기, 열하루째.**
6. **이벤트 캘린더 `covered_through` 9월분 — 21일째**(외부 일정, 코드 밖).
7. **ZN/ES 해외선물 거래소 권한**(증권사 설정, 코드 밖).
8. **ruff 설치** — 열한 회차 연속 린트 못 돌림.
9. **이 예약 작업의 §1-2 완결 판정 수정 여부** — §6-9. **이레째.**
10. **`.claude/` · `docs/Dev_md/dashboard_build_prompt_1.md` 커밋 여부** — **24일째** 미결정(손대지 않음).

---
