# 마흐디 운영점검 보고서 — 2026-08-18 (화)

> 자동 집계: `docs/동작점검/auto/2026-08-18_지표.{md,json}` · 증거: `auto/2026-08-18_증거_post.md` (15:45:50 생성)
> 흡수한 국면 문서: `2026-08-18_점검_pre.md`(13:37) · `_점검_intra.md`(12:54 수동) · `_점검_intra_1340.md`(13:39 발화) · `_점검_intra_1430.md`(14:40 발화) — **넷 다 원본 보존**
> 오늘 커밋: **5건** (HEAD `ccdbeec`, 08-18자 13:41~14:24)
> 조사 범위: **07:30:15 기동 ~ 15:45:06 종료** (사이클 관측 494분, 프로세스 **1회 기동**)
> 이 세션(자동 실행)은 **코드·`hypotheses.yaml` 상태·`dev_memory`를 하나도 변경하지 않았다.**

---

## 요약 — 인프라가 하루 종일 초록이었는데, 그 초록 아래에서 세 가지가 처음 일어났다

**첫째, 실주문 왕복이 처음 성립했다.** 14:03:08 위클리 콜 `B09FAWA37` 1계약 지정가 15.40 제출 →
14:03:1x 조회 → 14:03:14 취소, **네 단계 전부 `rt_cd=0`**(§3-1). Block C가 넉 달 만에 라이브에서
닫혔다. 그런데 그 왕복은 **`execution_logs`에 한 행도 남기지 않았고**, 그 가설(`c1`)의 주장 지표인
`db.tables`는 `execution_logs`를 **애초에 세지 않는다** — `mahdi/ops/db_metrics.py::_DAILY_TABLES`에
그 테이블이 없다(§3-2). 왕복은 성공했는데 **성공을 증명하도록 설계된 지표가 그것을 못 본다.**
README가 경고한 「지표 경로가 실재하지 않아 가설이 조용히 무력화되는」 형태다.

**둘째, 우선순위 불변식이 오늘 처음 깨졌고, 14:30 회차가 본 2건이 전부가 아니었다.**
`우선순위위반=예`는 14:05:52 · 14:06:52 · **15:01:52** 세 건이다(§2-4). 셋째 건은 14:30 회차가
구조적으로 볼 수 없었던 시각에 있다. 세 건 다 `budget_exceeded`(순수 데드라인) 경로이고,
`timeout_abort` 경로 6건은 전부 `아니오`였다 — **Fix#4의 2단계 접기는 `abort_scope`만 보호하고
벽시계 데드라인 컷은 그 보호를 통과하지 않는다**(`mahdi/main.py:1996`).

**셋째, 판단 주입력이 끊긴 4분이 전부 정각·30분 위상에 있었다.** 감마플립 산출 불가는
13:01:10 · 13:31:10 · 14:01:10 · 15:01:10 **넉 분뿐이고 전부 `:01`** 이다(§2-5). 13:40 회차가
「관측 2회로는 단정 못 한다」고 유보한 그 추론을, 하루치 원본이 뒷받침한다: 정규장 `HTTP≥4.0초`
(= read timeout에 잘린 호출) **333건 중 103건(31%)이 `:00~:03`·`:30~:33` 여덟 분에 몰려 있다**
(균등이면 13.3%). 이것은 우리 레그 수 문제가 아니라 **위상 문제**다.

그리고 오늘의 자동 §1 델타 표는 **읽으면 안 된다** — 전일이 08-17, 즉 **광복절 대체휴일**이다.
「REST수집 평균 ▲18.7 ⚠」·「느린 REST 호출 ▲2,840 ⚠」·「사람이 읽는 로그 ▲5,770 ⚠」 세 개의
붉은 표시는 전부 *거래일과 휴장일을 뺀 값*이다(규약 G). 08-17 보고서가 P0로 남긴
「비거래일 배너 + 직전 거래일 기준 델타」가 미이행이라, **그 미이행이 오늘 정확히 예고된 대가를 냈다**(§3-3).

**P0 없음.** P1 5건 · P2 6건 · 만성 3건.

---

## §1. 장전 (07:30~09:00)

`_점검_pre.md`(13:37)를 흡수하고, 거기 지적된 것이 하루 끝에 어떻게 됐는지 결말을 붙인다.

### 1-1. 기동은 깨끗했다 — 그리고 하루 끝까지 깨끗했다

| 항목 | 장전 시점 판정 | **하루 끝 결말** |
|---|---|---|
| 07:30 배치·docker·마이그레이션 001~031 | 확인 | 종료까지 이상 없음 |
| `직전 정상 기동` 표식 | 1회 | **1회 유지** — 재기동 0회 (08-17은 5회였다) |
| ERROR 이상 | 0건(13:29까지) | **0건** (INFO 18,143 / WARNING 383) |
| 정규장 로그 3분↑ 공백 | 0건 | **0건** |
| `rows=0` 사이클 | 0개 | **0개** · 최장 연속 0분 |
| 워치독 | 36행 OK | **49행 전부 OK · 비-OK 0 · 25분↑ 무기록 0** |
| WS 단절 | 0건 | **0건** |
| 커밋 선후 | `abbe8d6` 08-17 17:37 < 기동 07:30 | 확인 — **08-17 커밋 3건은 오늘 프로세스에 실렸다** |

**오늘 커밋 5건(13:41~14:24)은 전부 기동 이후다** — 그 fix들은 오늘 관측 루프에서 돌지 않았다.
`refuted`가 아니라 `untested`다(2026-08-04 p4). 예외는 §3-1의 왕복 실측으로, 그것은 관측 루프가
아니라 별도 스크립트(`scripts/measure_order_roundtrip.py`)로 돌았으므로 실측이 유효하다.

### 1-2. [P1] 점검 예약 지연 — 부분 회복, 원인은 그대로

| 회차 | 예정 | 실발화 | 지연 |
|---|---|---|---|
| `mahdi-premarket-check` | 08:30 (cron `30 8 * * 1-5`) | **13:28:36** | **298분** |
| `mahdi-intraday-check` | 12:30 | **13:39** | 69분 |
| `mahdi-intraday-check-1430` | 14:30 | 14:40 | **10분 — 정시** |

장전본이 *"앱을 끄지 않으면 오늘은 정시에 뜬다"* 고 적었고 **그대로 됐다.** 즉 결함은 예약 자체가
아니라 **Claude 앱 기동 시각에 예약이 종속된 것**이고, 그 사실이 오늘 대조 실험으로 확정됐다
(같은 날 같은 스케줄러에서 298분 지연과 10분 정시가 함께 나왔다).

**결말: 부분 해소, 근본은 그대로.** 08-17 보고서가 남긴 미완료 항목
`[P1] 점검 예약 3회차가 오늘 전부 미발화 — 그날 안에 알아챌 경로가 없다`는 **오늘도 미이행**이다.

> 12:54본은 **예약이 아니라 사람이 손으로 돌린 것**이다(`mahdi-intraday-check`의 종전
> `lastRunAt`이 `2026-08-17T07:45:48Z`). 산출물만 보면 예약이 돈 것과 구별되지 않는다 — 이것이
> 이 결함의 진짜 위험이고, §5 고도화 2가 그것을 다룬다.

### 1-3. [P1] 레버 E — 유예 **9회째**, 사유는 여전히 없다

장전본이 「오늘 조건이 다시 성립했다」로 올렸고, **오늘도 켜지지 않았다.**
`NEXT_TODO.md:298`의 체크박스는 그대로이고 **유예 사유가 이번에도 적히지 않았다** —
증거 §7이 *"열 번 중 한 번도 사유가 적히지 않았다 — 결정된 유예가 아니라 잊힌 유예"* 라고
경고한 그 형태의 열 번째다. 무조건발동일 **2026-08-25 (D-7)**.

다만 오늘 실측은 **레버 E가 정확한 처방인지 자체를 흔든다** — §2-5를 볼 것.
예산 96%는 레그 수 때문이라고 장전본이 읽었는데, 정각 위상에 몰린 타임아웃은 레그 수를 줄여도
같은 분에 그대로 남을 수 있다.

### 1-4. [P2·이월] 가설 3건이 **5일째 미등재**

`2026-08-13-p1-chain-input-phase` · `2026-08-14-fix1-expired-chain-blocks-options-flow` ·
`2026-08-14-fix4-priority-cut-visible` — 08-14 보고서가 등재를 제안했고 `hypotheses.yaml`에
grep 결과 **0건**이다. 08-17 보고서가 「3일째」로 P1을 올렸고 오늘 **5일째**다.

**소급 등재는 금지다**(예측치 규약 — 숫자를 보고 예측을 고치는 것) — 오늘 것으로는 등재할 수 없다.
다만 `2026-08-14-fix4-priority-cut-visible`은 §2-4가 오늘 **실제로 그 축을 판정 가능하게 만들었다**
(위반 3건이 로그에 남았다). 미등재였기 때문에 그 관측이 어느 가설에도 귀속되지 않았다 —
**이것이 미등재의 대가다.**

### 1-5. 만성 — 새 발견으로 올리지 않는다

- **ZN/ES 권한 결손**: `ESU26` 9건(07:32:39~15:42:34) · `ZNU26` 8건(07:32:35~15:42:33),
  전부 `CME/CBOT SUB거래소 신청 계좌가 아닙니다`. `NEXT_TODO.md` 「ZN/ES 권한(§4 Fix#9)」에 이미 있다.
- **`chain_input_source` = `stale` 100%**: 490분 전부 `stale`, `current` **0분**.
  08-17 99.4% · 08-14 84.2%로 **만성**이다. 「판단이 그 분의 체인을 본 적이 하루에 0분」이라는
  뜻인데, 체인 나이 중앙값이 70초이므로 구조적이다(수집이 한 분을 통째로 쓴다).
  경보로 올리지 않되 §7-2에 질문으로 남긴다.
- **`regime_hmm` 점수가 419분 전부 정확히 0**: `member_score_quality` — `mean 0.0 · positive 0 ·
  negative 0 · zero 419`. 08-14도 419분 전부 0이었다. **가용 멤버 4/6의 분모에는 들어가는데
  분자에는 한 번도 기여하지 않는다** — 레버 F(`use_effective_member_count`)가 존재하는 이유가
  오늘 실측으로 다시 확인됐다(§5 고도화 2).

---

## §2. 장중 (09:00~15:20)

`_점검_intra.md`(12:54) · `_intra_1340.md`(13:39) · `_intra_1430.md`(14:40) 셋을 흡수한다.

### 2-1. [P1] `2026-08-17-p1` 반증 — 구독 창이 전략이 요구하는 델타에 닿지 않는다 (12:54본 §1)

| id | 주장 지표 | 예측 | 실측 | 판정 |
|---|---|---|---|---|
| `2026-08-17-p1` | `enter_minutes_with_candidate_pct` | ≥ 90 | **19.5** | **반증** |
| `2026-08-17-p2` | `expiry_day_leg_count` | 0 | 0 | 확인 |
| `2026-08-17-p3` | `order_submitted_true_count` | 0 | 0 (기록 293분) | 확인 |
| `2026-08-17-p4` | `overrun.count` | 0 | 0 | 확인 |

후보 없음 165건 중 163건이 `small_strangle_buy` / `delta_band[0.2,0.3]`. 그 북(08-20 위클리 목)의
분당 실측은 콜 26.5% · 풋 6.2%가 밴드를 보유했고 **양 날개 동시 보유는 그보다 훨씬 낮다**.

**결말: 하루 끝에도 (a)/(b)/(c) 미결이다.** 12:54본이 *"하루치로 결론 내지 않는다"* 로 남겼고
오늘 장후도 그 판단을 유지한다 — 다만 §2-5가 새 입력을 하나 준다(구독 창 ATM±2가 좁다는
같은 사실이 감마플립 축에서도 6건 나타났다: `감마플립 기각(레그 범위 밖)` 09:56:10~14:07:10).

### 2-2. [해소] 판단 4분이 통째로 사라졌다 (07:31~07:34) — 조치 완료

```
account_tracker.same_direction_positions():259
TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'
```

`Signal Fusion 사이클 실패` 4건(07:31:10~07:34:10). 마이그레이션 030이 `unknown_side_count`를
nullable로 추가했고, 08-15~08-17 사흘 휴장 때문에 오늘 첫 스냅샷 전까지 **08-14 15:44의 NULL 행**이
반환됐다. 실패 방식이 나쁘다 — 예외가 사이클 전체를 접어 **REJECT 행조차 안 남는다.**

**결말: 12:54본이 `db._account_balance_snapshot_row_to_dict()`에서 막았고, 09:00 이후 재발 0건이다.**
`unknown_side_count_max = 0` · `side_distribution = {}`(포지션 0행). §6에서 `b1`의 판정 근거가 된다.

### 2-3. 먼슬리(regular) 포함 컷 — 하루 총 **9건**, 셋으로 갈린다

| 시각 | 경로 | 적재 | 우선순위위반 |
|---|---|---|---|
| 13:00:37 · 13:30:33 · 14:00:50 | `timeout_abort` | 1 / 1 / 2행 | 아니오 |
| **14:05:52 · 14:06:52** | **`budget_exceeded`** | 9 / 9레그 | **예** |
| 15:00:34 · 15:01:29 · 15:02:51 | `timeout_abort` / 전체실패 | 1 / 0 / 5행 | 아니오 |
| **15:01:52** | **`budget_exceeded`** | 6레그 | **예** |

`timeout_abort` 경로의 `아니오`는 **정확하다** — `main.py:2004-2006`이 1단계에서 위클리를 이미
버렸는지 보고 판정한다. 13:40본이 이것을 옳게 못 박았다.

### 2-4. [P1·신규] 우선순위 불변식 위반 — 2건이 아니라 **3건**이다

14:30 회차는 2건으로 닫았다. 15:20까지 보면 하나 더 있다:

```
15:01:52,614 WARNING:mahdi.main:옵션체인 수집 예산(50초) 초과 — 남은 13레그를 포기하고 6레그로
             이번 분을 마감합니다 (…§2-6/Fix#8) 컷당한북=regular,weekly_thu · 우선순위위반=예
```

세 건 다 `budget_exceeded`다. `mahdi/main.py:1996`이

```python
if cut_by_scope or (deadline is not None and time.monotonic() >= deadline):
```

로 **`abort_scope` 분기와 벽시계 데드라인 분기를 같은 `if`에 합쳐 놓은 것**이 구멍의 자리다.
데드라인이 먼슬리 레그 인덱스에서 걸리면 뒤에 남은 위클리 레그(`non_priority_ahead[index+1]`)가
있어도 선제 강등(`dropped_non_priority_first`, :1978)이 발동하지 않는다.

**계측은 제 일을 했다.** 불변식 플래그가 스스로 `예`를 찍었다 — 2026-08-12 Fix#5의 성과다.
164건의 컷 로그 중 정확히 이 3건만 `예`다.

**영향은 두께이지 축의 사망이 아니다**: 14:05~14:06의 `rows`가 9로 반토막 났지만(정상 19~20)
14:07:10은 `산출 불가`가 아니라 `기각(레그 범위 밖)`이었다. 다만 15:01:52는 15:01:10의
`감마플립 산출 불가`와 같은 분대에 있다(§2-5).

### 2-5. [P1·신규] 판단 입력이 끊긴 넉 분이 **전부 `:01`** 이다 — 위상 문제다

오늘 `감마플립 산출 불가`는 **4건뿐이고 시각이 이렇다**:

```
13:01:10   14:01:10
13:31:10   15:01:10
```

13:40본이 *"13:00과 13:30, 정확히 30분 간격 — 관측 2회로는 위상 패턴이라 단정할 수 없다"* 고
유보했다. **오늘 하루치가 그 추론을 지지한다.** 정규장(09:00~15:45) `느린 REST 호출` 2,850건 중
HTTP 성분이 **4.0초 이상**인 것(= read timeout에 잘린 호출) 333건의 분 분포:

| 분 | :00 | :30 | :10 | :01 | :31 | 나머지 55분 |
|---|---|---|---|---|---|---|
| 건수 | **27** | **25** | 16 | 14 | 13 | 238 |

**`:00~:03` + `:30~:33` 여덟 분이 103건(31%)** 을 가진다 — 균등이면 13.3%다.
원본이 그 순간을 그대로 보여 준다:

```
13:00:09,585 느린 REST 호출 5.52초 = 페이서대기 1.50초 + HTTP 4.02초 (배율 1.50배, GET inquire-price)
14:00:10,165 느린 REST 호출 5.84초 = 페이서대기 1.83초 + HTTP 4.02초 (배율 1.82배, GET inquire-price)
15:00:08,831 느린 REST 호출 5.00초 = 페이서대기 1.00초 + HTTP 4.00초 (배율 1.00배, GET inquire-price)
   (대조) 11:00:09,575  … 페이서대기 1.22초 + HTTP 3.03초
   (대조) 12:00:12,943  … 페이서대기 1.08초 + HTTP 2.31초
```

**4.02·4.05·4.06은 실제 응답시간이 아니다** — 4.0초 타임아웃에 잘린 값이다(규약: 우측 검열).
그러므로 `inquire-price` p50 게이지는 이 분들을 **구조적으로 못 본다**. 14:30 회차가
「p50÷timeout 최대 0.74로 경고선 아래」로 닫은 것은 옳지만, **그 게이지가 못 보는 축에서
넉 분이 죽었다.**

> ⚠ **원인은 여기서 확정하지 않는다.** 정각·30분에 무엇이 겹치는지(만기유동성 폴러의
> `startup_offset` 30초, 5분 주기 매크로, 300초 창 REST 응답시간 인쇄 등)는 코드를 더 읽어야
> 한다. **관측된 사실은 셋이다** — 검열된 호출이 정각/30분에 2.3배 몰린다는 것, 감마플립
> 산출 불가 4건이 전부 `:01`이라는 것, 그 분들의 페이서 대기는 1.0~1.8초로 평범하다는 것
> (즉 우리 백오프가 아니라 KIS 응답이 그 분에 느리다).

**이것이 레버 E 판단에 주는 입력**: 위클리를 4분 격분화해도 정각 분의 먼슬리 호출은 그대로
그 창에 부딪힌다. 레버 E는 예산 96%(하루 평균)에는 듣지만 **`:01` 넉 분에는 안 들을 수 있다.**
두 문제는 같은 처방이 아니다.

### 2-6. [P2] 지연의 절반 이상이 우리 페이서 대기다 (14:30본 §4 유지)

```
14:04:04 느린 REST 호출 3.31초 = 페이서대기 3.30초 + HTTP 0.02초 (배율 3.32배)
```

HTTP 0.02초인데 총 3.31초다. `inquire-price` p50은 페이서 대기를 포함하고 read timeout(4.0초)은
HTTP에만 걸리므로 **두 값의 분모가 다르다.** 백오프 배율은 14:04:50 3.32 → 14:07:17 2.42로
자력 회복했다. §2-5와 합치면 **p50÷timeout 게이지는 위아래로 다 오염돼 있다** — 아래로는
페이서 대기가 부풀리고, 위로는 타임아웃이 자른다.

### 2-7. [P2] 장중 코드 변경이 규약을 벗어나 계속됐다

12:54본이 스스로 밝혔듯 사용자의 명시적 지시로 코드·테스트·`hypotheses.yaml`·`dev_memory`를
바꾸고 **라이브 DB에 마이그레이션 032를 적용**했다. 13:30:35~13:33:25에도 `mahdi/ops/report.py` ·
`scripts/daily_ops_report.py` · `mahdi/ops/campaign.py` 등이 바뀌었다.

`phases.md` B-4(*"장중에는 코드를 바꾸지 않는다"*) 위반이지만 **가동 중 프로세스에는 영향이 없다**
(07:30에 로드된 코드는 디스크 변경을 안 집는다). 다만 **오늘 `auto/2026-08-18_지표.{md,json}`은
하루 중간에 바뀐 `report.py`로 생산됐다** — 전일 델타를 읽을 때 함께 읽어야 한다.

마이그레이션을 **먼저** 적용한 순서는 옳았다(워치독 재기동 시 새 코드가 없는 컬럼에 INSERT하면
체인 적재가 통째로 죽는다). 오늘 재기동이 0회라 그 위험은 실현되지 않았다.

---

## §3. 장후 (15:20~15:45)

### 3-1. 종료 시퀀스는 정상 — 그리고 왕복 실측이 성립했다

```
[2026-08-18 15:45:06] 직전 정상 종료: 2026-08-17 15:45:03 (24.0시간 전)
15:45:37 판단 사후 평가 345건 계산
15:45:49 운영 지표 리포트 생성: auto/2026-08-18_지표.md
[collect_evidence] 저장: auto/2026-08-18_증거_post.md (50.3KB)
[2026-08-18 15:46:46.64] ===== 장마감 자동 종료 완료 (DB/Redis는 계속 실행) =====
```

산출물 전건 존재. **사람에게 도달한 경보 0건** — Slack 토글이 08-17 보고서에서 「보류(08-18~19
워치독 재기동 0회 확인 후)」였고, 오늘 재기동 0회로 그 조건이 **절반 충족**됐다(08-19가 남았다).

**왕복 실측 — `logs/order_roundtrip_20260818_140313.json`**

| 단계 | 결과 |
|---|---|
| `get_quote` `B09FAWA37`(위클리C 2608W3 1,090.0) | `rt_cd=0` · 현재가 22.00 |
| `submit_order` BUY 1계약 지정가 15.40 | `rt_cd=0` · `ODNO=0000007047` · `모의투자 매수주문이 완료 되었습니다` |
| `inquire_ccnl` | `rt_cd=0` · `odno="      7047"` · 지정가 · 미체결 |
| `cancel_order` | `rt_cd=0` · `ODNO=0000007050` · `ORGN_ODNO=0000007047` |

**두 번의 실패가 먼저 있었다**(13:57:26 · 13:59:40) — 둘 다 대상이 **선물 `A01609`** 였고
주문가 769.75(= 1099.65 × 0.7)가 선물 가격제한(1011.00~1186.80) 밖이라
`모의투자 상/하한가 오류`(`40270000`)로 거부됐다. 옵션(가격제한 0.01~99.30)으로 바꾼 셋째 시도가
성립했다. 커밋 `ccdbeec`의 *"예행연습이 두 번 제 일을 했다"* 가 이것이다.

**부수 발견 — 주문번호 패딩**: 제출 응답의 `ODNO`는 `"0000007047"`(왼쪽 0 패딩), 조회 응답의
`odno`는 `"      7047"`(왼쪽 공백 패딩)이다. 문자열 비교로 매칭하면 **같은 주문을 다른 주문으로
읽고 그 실패는 조용하다.** 커밋 `dfb2bd4`가 이것을 고쳤다 — **오늘 관측 루프에는 실려 있지 않다**
(14:24 커밋 > 07:30 기동).

### 3-2. [P1·신규] 성공한 왕복이 어떤 지표에도 안 잡힌다

`2026-08-16-c1`의 주장 지표는 **`db.tables` → `execution_logs` 행 ≥ 1**이다. 그런데:

1. `mahdi/ops/db_metrics.py::_DAILY_TABLES`(L57-74)에 **`execution_logs`가 없다.**
   오늘 `db.tables`는 14개 테이블을 세는데 그중 `execution_logs`는 한 줄도 아니다.
   자동 판정은 이것을 「실측 없음」이 아니라 **「수기 판정」** 으로 넘겼다.
2. `scripts/measure_order_roundtrip.py`는 `db.insert_execution_log()`(`mahdi/data/db.py:1801`)를
   **호출하지 않는다** — grep 결과 그 스크립트가 `db`에서 쓰는 것은 `db.local_now()` 한 곳뿐이다.
   증거는 `logs/order_roundtrip_*.json` 파일에만 남았다.

즉 왕복은 **성립했으나 DB 증거는 0행**이고, 그것을 재도록 설계된 지표는 **그 테이블을 보지도
않는다.** README의 *"지표 경로는 실재하는 것이어야 한다 — 오타는 「실측 없음」과 구별되지 않아
가설을 조용히 무력화한다"* 가 오타가 아닌 형태로 실현됐다: **경로는 맞는데 그 경로가 세는
목록에 대상이 없다.**

### 3-3. [P1·이월] 전일 델타의 기준이 **휴장일**이다 — 08-17 P0 미이행의 대가

자동 §1이 낸 붉은 표시 넷 중 셋은 사건이 아니다:

| 지표 | 오늘 | 전일(**08-17 = 광복절 대체휴일**) | 자동 판정 | 실제 |
|---|---|---|---|---|
| REST수집 평균 | 38.3초 | 19.5초 | ▲18.7 ⚠ | 거래일 vs 휴장일 |
| 느린 REST 호출 | 2,850건 | 6건 | ▲2.84e+03 ⚠ | 상동 |
| 사람이 읽는 로그 줄 | 6,728줄 | 955줄 | ▲5.77e+03 ⚠ | 상동 |
| 비200 응답 | 57건 | 22건 | ▲35 ⚠ | 상동 |

08-17 보고서 Fix#1이 **P0**로 *"점검 산출물에 「비거래일」 배너 + 직전 거래일 기준 델타"* 를
남겼고 미이행이다. 직전 **거래일**은 2026-08-14다 — 그날과 비교해야 오늘이 좋은 날인지 알 수 있다.

같은 오염이 `member_score_quality`에도 있다: 08-17의 `flow_position ↔ options_flow` 부호 일치율은
**0.0%**인데 그날은 시장이 없었다. 08-17 보고서가 남긴 질문 *"60% 복귀인가 30% 고착인가"* 의
답은 **08-14(32.4%) 대비 오늘 60.3%** — **60% 복귀다**(§6 참고).

### 3-4. 하루 전체 게이지 — 경고선 돌파 0

| 지표 | 경고 | 위험 | 오늘 최악 | 판정 |
|---|---|---|---|---|
| `rows=0` 비율 | ≥0.3 | ≥0.7 | **0.00** (0행 사이클 0개) | 돌파 없음 |
| p50 ÷ read timeout | ≥0.8 | ≥1.0 | **0.80** (15시 창 최대 3.20초) | **경고선 접촉** |
| 앙상블 멤버 | ≤5/6 | ≤3/6 | 4/6 (96분) · 3/6 11분 · 2/6 1분 · 1/6 1분 | 종일 4/6 기조 |
| 관측 루프 공백 | ≥3분 | ≥5분 | 0건 | 돌파 없음 |
| WS 단절 | 증가 | — | 0건 | 돌파 없음 |

15시 창 최대 p50/timeout이 **0.80으로 경고선에 정확히 닿았다**(자동 적신호 5). 다만 그 시각은
15:20 마감 이후 구간을 포함하고 rows=0은 0개이므로 **절벽은 오지 않았다.** 08-14는 0.77에서
24분 뒤 절벽이 왔지만, 그날은 rows=0이 이미 진행 중이었다 — **오늘은 대조군이다.**

### 3-5. `inquire-balance`(미검열 통로)와 `inquire-price`(검열 통로)가 갈렸다

`2026-08-16-p1`이 주장한 그대로다:

| 시 | `inquire-price` p50 | `inquire-balance` p50 | 비고 |
|---|---|---|---|
| 09 | 0.249 | 1.801 | |
| 10 | 0.588 | 2.741 | |
| 11 | 1.262 | 3.954 | |
| 12 | 1.200 | **4.206** | price는 4.0 천장에 눌리기 시작 |
| 13 | 1.377 | 3.927 | |
| 14 | 1.552 | 4.026 | |
| 15 | 1.560 | 2.651 | 마감 후 회복 |

`inquire-balance`(read timeout 10초) 최대 **7.62초** — 그 통로의 천장에는 닿지 않았다
(08-14는 12.72초로 닿았다). **두 행이 갈린다는 주장 축은 확인**이지만, **「전멸을 한 시간 앞서
예고한다」는 선행성은 오늘 전멸이 없어서 검정되지 않았다**(§6).

---

## §4. Fix 구현 계획

> 이 자동 실행은 **코드를 고치지 않는다.** 계획까지만 쓴다.
> **예측치는 착수 전에 `hypotheses.yaml`에 적는다**(숫자를 보기 전에). 아래 id는 제안이며
> 이 세션은 등재하지 않았다.

### Fix#1 [P1] — 데드라인 컷 경로에 우선순위 보호를 배선한다 (§2-4)

`mahdi/main.py:1996` `poll_option_chain()` 내부

```python
# 현재 — 두 분기가 한 if에 합쳐져 있다
if cut_by_scope or (deadline is not None and time.monotonic() >= deadline):
    ...
```

- **바꿀 것**: 데드라인 분기를 분리해, 자르기 **전에** 남은 비우선(위클리) 레그를 먼저 소거하고
  (`dropped_non_priority_first = True` 설정) 그래도 예산이 모자랄 때만 우선 레그를 자른다.
  `:1978`의 선제 강등 블록을 데드라인 경로에서도 통과하도록 호출 지점을 앞으로 옮긴다.
- **원인**: `abort_scope`(연속 타임아웃) 경로만 2단계 접기를 거치고, 벽시계 데드라인은
  그 보호를 우회한다. `timeout_abort` 6건은 전부 `아니오`, `budget_exceeded` 3건만 `예`인
  분포가 그 증거다.
- **회귀 위험**: 위클리 소거를 먼저 하면 위클리 적재가 더 줄어든다. **대가를 함께 잰다.**
- **검증**: `uv run pytest tests/test_main_option_chain*.py` + 신규 회귀 테스트 —
  「데드라인이 먼슬리 레그 인덱스에서 걸리고 뒤에 위클리가 남아 있는」 사이클을 합성해
  `priority_cut_before_others`가 `False`여야 함을 고정.
- **예측치(제안)**: `2026-08-18-p5-deadline-cut-respects-priority` ·
  주장 `db.…priority_violation_count == 0`(오늘 기준선 **3**) ·
  **대가** `db.tables.option_analysis_1m.rows` 가 8,583 근처 유지(위클리 소거로 총 적재가 줄면 여기가 준다).
- **적용 시점**: 장후(오늘 이후) · 08-19 07:30 기동에 싣는다.

### Fix#2 [P1] — `execution_logs`를 지표가 보게 하고, 왕복이 DB에 남게 한다 (§3-2)

두 곳을 **함께** 고쳐야 한다. 하나만 고치면 여전히 0행이거나 여전히 안 보인다.

1. `mahdi/ops/db_metrics.py::_DAILY_TABLES` (L57-74)에 한 줄 추가:
   `("execution_logs", "timestamp", "주문 실행 이력")` — 시각 컬럼명은 001 스키마로 확인할 것.
2. `scripts/measure_order_roundtrip.py`가 각 단계 후
   `db.insert_execution_log(conn, order_to_execution_log_row(order))`
   (`mahdi/data/db.py:1801` · `mahdi/broker/order_state_machine.py:102`)를 호출하게 한다.
   지금은 `logs/order_roundtrip_*.json`에만 남는다.
- **원인**: 가설의 주장 지표가 **실재하지 않는 집계 대상**을 가리켰다. 자동 판정은 이것을
  「실측 없음」으로 내지 않고 「수기 판정」으로 흘려보냈다 — 그래서 눈에 안 띄었다.
- **회귀 위험**: 낮음. 다만 `_DAILY_TABLES`는 `tests/test_ops_metric_conventions.py`가
  검사할 수 있으니 함께 돌린다.
- **검증**: `uv run pytest tests/test_ops_*.py` · 다음 왕복 실측 후 `db.tables`에 행이 뜨는가.
- **예측치(제안)**: `2026-08-19-fix2-roundtrip-leaves-db-evidence` ·
  주장 `db.tables[execution_logs].rows >= 1`(오늘 기준선: 키 자체 부재).

### Fix#3 [P1] — 비거래일 배너 + 직전 **거래일** 기준 델타 (§3-3, 08-17 P0 이월)

- **파일**: `mahdi/ops/report.py`(§1 델타 생성부) · `mahdi/market_calendar.py`(08-17 신설, 이미 있다)
- **내용**: 전일 사이드카를 고를 때 `market_calendar`로 **거래일만** 거슬러 올라간다. 전일이
  비거래일이면 §1 표 머리에 `⚠ 직전 거래일 2026-08-14 기준` 배너를 박고, 비거래일 자신의
  산출물에는 「비거래일」 배너를 박는다.
- **원인**: 08-17이 휴장인데 지표는 날짜만 보고 뺐다. 오늘 붉은 ⚠ 4개 중 4개가 거짓이다.
- **회귀 위험**: 캘린더 `covered_through`(현재 2026-12-31)를 넘어가면 판정 불가 — 그때는
  **「모름」으로 인쇄하고 0으로 접지 않는다.**
- **검증**: `uv run pytest tests/test_market_calendar.py tests/test_ops_report*.py` ·
  08-19 산출물의 §1이 08-18 기준인가.
- **예측치(제안)**: 주장 `report.delta_baseline_date == "2026-08-18"`(08-19 실행 시).

### Fix#4 [P1] — 예약 미발화를 **그날 안에** 알아채는 경로 (§1-2, 08-17·08-18 이월)

장전본 Fix#2를 그대로 유지한다. **사흘 연속 미이행**이므로 순위를 올린다.

- **파일**: `scripts/watchdog_observation_loop.py` — 10분마다 도는 유일한 상시 프로세스다.
- **내용**: 감시 창 안에서 「오늘 날짜의 `docs/동작점검/{날짜}_점검_pre.md`가 09:00까지 있는가」를
  한 번 보고, 없으면 `watchdog.log` 한 줄 + `notify.notify_sync` 1회.
  **예약이 안 뜬 것을 예약으로 감시할 수 없다.**
- **회귀 위험**: 오경보. 휴장일·수동정지 게이트(`stopped_at is None and holiday is None`) 재사용.
- **검증**: `uv run pytest tests/test_watchdog*.py`.
- **예측치(제안)**: 주장 `watchdog.missing_check_alerts` 키 **존재** ·
  대가 `watchdog.checks` 49 근처 유지.

### Fix#5 [P2] — 열화 게이지에 `우선순위위반` 독립 행 (§2-4, 14:30본 제안 유지)

지연 비율의 종속 지표로 두면 오늘처럼 **경고선(0.80) 아래에서 터질 때 표에 안 보인다.**
14:05~14:06의 p50÷timeout은 0.74였고, 먼저 울린 종은 **예산 대비 104%** 였다.

### Fix#6 [P2] — `REST 응답시간` 줄에 HTTP 성분 p50 분리 인쇄 (§2-6)

지금 게이지는 백오프가 깊어질수록 「KIS가 느려졌다」로 읽힌다. 페이서대기와 HTTP를 나눠 찍는다.
**§2-5의 검열 문제도 같은 줄에서 다뤄야 한다** — `HTTP ≥ read timeout`인 호출 수를 함께 센다
(검열된 값의 p50은 의미가 없고, **검열 건수**가 진짜 신호다).

### Fix#7 [P2] — 점검 회차 시각을 한 곳으로 모은다 (§1-2 / 장전 P2-1)

`phases.md` 08:00 · `collect_evidence.py:98` `PHASE_SCHEDULE["pre"]="08:00"` · 실제 cron `30 8 * * 1-5`.
**(a) cron을 `0 8 * * 1-5`로 옮기는 것을 권한다** — 그러면 문서·수집기가 진실이 되고 개장까지
60분이 확보된다. 지금은 정상인 날에도 매일 30분 거짓 지연을 인쇄한다.

### Fix#8 [P2] — 워치독 OK 줄에 적재 분 수를 실어 준다

`mahdi/liveness.py` `ACTION_OK` 분기: `정상 — 마지막 박동 3초 전` →
`… · 직전 10분 적재 9분`. `ingest_minutes_recent is None`이면 **`적재 모름`** —
「모른다」를 0으로 접지 않는다. 오늘 49행이 전부 같은 형태라 「적재를 쟀다」는 증거가 로그에 없다.

### Fix#9 [보류] — `.gitattributes` 줄바꿈 정규화 (08-17 Fix#4 이월)

미착수. 아직 없다(`ls .gitattributes` → 없음). 단독 커밋으로 먼저 하고 이후 작업 커밋을 쌓는
순서를 지켜야 diff가 읽힌다. **오늘 미커밋 파일이 60건 이상이라 지금 하면 충돌이 크다** —
작업 트리를 정리한 뒤 착수한다.

---

## §5. 고도화 방안

**당일 관측에서 출발한 것만 쓴다.**

### 고도화 1 — 「예산 대비」와 「검열 건수」를 한 줄로 붙여 판정 열을 만든다

- **관측 근거**: 오늘 두 지표가 반대 방향을 가리켰다. 하루 평균 예산 대비 96%(붉다)인데
  p50÷timeout은 최대 0.80(경고선 접촉)이고, **정작 판단이 죽은 넉 분은 둘 다 못 봤다**(§2-5).
- **제안**: `collect_evidence.py` §5-1에 판정 열을 더한다.

  | 예산 대비 | 검열 건수(HTTP≥timeout) | 판정 |
  |---|---|---|
  | 높음 | 낮음 | **우리 레그 수가 많다** → 레버 E |
  | 낮음 | 높음 | **KIS가 그 시각에 느리다** → 폴링을 줄여도 안 낫는다(결정 7) |
  | 둘 다 높음 | | 절벽 임박 |

  장전본이 제안한 판정 열의 둘째 축을 **p50 비율이 아니라 검열 건수**로 바꾼 것이다 —
  오늘 p50 비율은 그 축을 못 봤고 검열 건수는 봤다.
- **기대 효과**: 「레버 E를 켤 것인가」에 매일 자동으로 답이 나온다. **오늘은 첫 줄이 아니라
  둘째 줄이었을 가능성이 크다**(§1-3).
- **비용·위험**: 수집기 단독 변경, 낮음. 우선순위 **이번 주**.

### 고도화 2 — 「분모에만 들어가는 멤버」를 인쇄한다 → 레버 F의 근거

- **관측 근거**: `regime_hmm`이 419분 가용(85.5%)인데 점수는 **419분 전부 정확히 0**이다
  (`mean 0.0 · zero 419`). 08-14도 같았다. 확신도의 분모가 가용 멤버 수이므로
  **기여하지 않는 멤버가 확신도를 희석한다.**
- **제안**: `member_score_quality`에 `contributing_minutes`(|score| > 0인 분) 열을 더하고,
  §1 「판단 입력」 표에 **유효 멤버 수 = 기여 멤버 수**를 함께 인쇄한다.
  이것이 `use_effective_member_count`(레버 F)가 켜졌을 때 실제로 쓰는 값이다.
- **기대 효과**: 레버 F를 켜기 전에 **켰을 때의 값을 미리 볼 수 있다** — 지금은 켜야만 보인다.
- **비용·위험**: 낮음(읽기 전용 집계). 우선순위 **이번 주** — 레버 F 무조건발동일이 08-25다.

### 고도화 3 — 사람 손이 필요한 가설에 「마감 시각」을 단다

- **관측 근거**: `c1`/`c2`는 사용자 수동 주문이 있어야 판정된다. 오늘 14:03에 성공했지만
  그 전에 13:57·13:59 두 번 실패했고, 15:20까지 **77분** 남은 시점이었다. 하루만 어긋났으면
  또 밀렸다.
- **제안**: `hypotheses.yaml`에 `사람손: true` · `마감: "15:20"` 필드를 더하고, 수집기 §8이
  장전·장중에 그 항목만 **「오늘 사람이 해야 하는 것」** 블록으로 따로 올린다.
- **기대 효과**: 8건 표에 섞여 있던 것이 분리된다. 우선순위 **이번 주**.

### 고도화 4 [결정 대기] — 구독 창 ATM±2를 넓힐 것인가

- **관측 근거**: 같은 사실이 오늘 **세 축에서 따로 나타났다.**
  (1) `small_strangle_buy` 후보 없음 163건 — 0.20~0.30 밴드에 닿지 않는다(§2-1),
  (2) `감마플립 기각(레그 범위 밖)` 6건 — 탐색 그리드가 레그 범위 밖에서 부호 전환을 줍는다,
  (3) 먼슬리 관측 델타 범위 0.445~0.556(12:54본 §3).
- **선택지와 손익**:
  - **(a) 창을 ±3으로 넓힌다** — 레그 수 20 → 30, 예산 대비가 96%에서 더 오른다.
    §2-5가 보여주듯 정각 분은 이미 검열 중이라 **넓히면 그 분들이 먼저 무너진다.**
  - **(b) 먼슬리만 ±3, 위클리는 ±2로 차등** — 판단 입력만 두껍게 한다.
    `NEXT_TODO.md`에 이미 같은 질문이 07-10부터 열려 있다.
  - **(c) `small_strangle_buy`의 밴드를 관측 가능 범위로 재정의** — 그러면 v6 §11.5의 "≈0.25"를
    우리가 고치는 것이고 그 숫자가 전략의 정의가 된다.
- **권고**: **(b)를 먼저 재본다.** 다만 **레버 E와 같은 날 켜지 않는다** — 둘 다 레그 예산을
  움직여 원인 귀속이 불가능해진다. 순서는 레버 E → (관측 1일) → (b)다.
- **하루치로 결정하지 않는다.** 08-19·08-20 이틀 더 보고 정한다.

---

## §6. 오늘 확정할 판정 (사람이 손으로)

**8건 도래 — 이 세션은 `hypotheses.yaml`의 `상태`를 바꾸지 않았다. 아래는 제안이다.**

| id | **제안 판정** | 근거(실측) |
|---|---|---|
| `2026-08-14-fix2-watchdog-sees-emptiness` | **확인(존재) · 성능 판정 불가** | `watchdog.degraded_checks = 0`(키 존재·non-None) · 대가 `checks = 49`(08-14와 동일) · `max_silence_minutes = 10.0`. **판정 부하 없음이 확인됐다.** 다만 그 가설의 `주의`대로 `rows=0`이 오늘 0개라 「빈 것을 봤는가」는 검정되지 않았다 — 존재는 확인, 기능은 `pending` 유지 |
| `2026-08-14-fix3-longest-empty-run` | **확인(존재)** | `zero_row_longest_run = {length: 0, start: None, end: None}` — 키 존재, `length`가 non-None. `zero_row_count = 0`과 나란히 읽힌다. 값 자체는 조용한 날의 0이다 |
| `2026-08-14-fix6-p50-over-read-timeout` | **확인(존재) · ⚠ 한계 발견** | `rest_latency.p50_by_hour`에 `inquire-price` 행 9개 시각 전부 존재. **그러나 §2-5가 이 지표의 한계를 드러냈다** — 4.0초에 검열된 호출 333건을 p50이 못 본다. 확인하되 **§7-1을 후속 질문으로 연다** |
| `2026-08-16-p1-uncensored-latency-leads-the-cliff` | **부분 확인 · 선행성은 판정 불가** | 주장 축 확인: `inquire-balance` p50 1.80→4.21(9~12시)로 오르는 동안 `inquire-price` p50은 0.25→1.20에 머물렀고 p95는 4.0 천장에 눌렸다. `balance` max 7.62초(통로 타임아웃 10초 미접촉). **전멸이 없어서 「한 시간 앞서 예고」는 검정되지 않았다** — 오늘은 **대조군**이다(규약 C) |
| `2026-08-16-fix1-chain-famine-safety-is-locked-by-test` | **판정 불가 → `inconclusive` 후보** | 체인 기근이 오늘 없었다(`rows=0` 0개 · `gex_input_missing_minutes` 0분). 주장 지표 `db.signal_reach.member_availability`는 자동 리포트에서 **「실측 없음」** 으로 떴다(그 경로는 `db.member_availability`에 있고 `signal_reach` 아래엔 없다) — **지표 경로 오류다. §7-3** |
| `2026-08-16-b1-position-side-must-not-fail-silently` | **확인(존재) · 실측 미도달** | `unknown_side_count_max = 0`(키 존재·non-None) · `side_distribution = {}` · `positions.rows = 0`. **KIS가 실제로 무엇을 보내는지는 여전히 미실측** — 포지션이 0행이라 정규화가 검정될 기회가 없었다. 08-16 장중본이 잡은 `NoneType` TypeError는 **같은 컬럼의 다른 결함**이고 §2-2에서 해소됐다 |
| `2026-08-16-c1-order-roundtrip-is-measurable` | **왕복 성립(로그) · 주장 지표로는 반증** | 왕복 4단계 전부 `rt_cd=0`(§3-1) — **가설 본문이 말한 일은 일어났다.** 그러나 주장 지표 `db.tables → execution_logs ≥ 1`은 **그 테이블이 집계 목록에 없어서 잴 수 없다**(§3-2). 대가 지표 `db.positions.rows = 0` — **예측대로 체결되지 않았다**. 권고: **`확인(왕복) + fix 필요(계측)`** 로 닫고 Fix#2를 새 가설로 등재 |
| `2026-08-16-c2-fill-notice-decrypts` | **미실행 → `untested`** | 오늘 로그에 `H0IFCNI0`(체결통보) 구독이 **0건**이다 — `SUBSCRIBE SUCCESS` 41건은 전부 `H0IOCNT0`(체결가)다. 가설의 `주의`가 이미 *"`main.py` 배선은 이 커밋에 없다"* 고 적었다. **체결도 없었으므로 복호 경로는 손도 안 댔다** — 확인도 반증도 아니다(규약 H·C) |

**추가 — 08-17 보고서가 남긴 질문의 답**

| 질문 | 답 |
|---|---|
| `flow_position ↔ options_flow` 부호 일치율 — 60% 복귀인가 30% 고착인가 | **60.3% 복귀** (`both_nonzero 365분 / same_sign 220분`). 08-14는 32.4%. **08-17의 0.0%와 비교하지 말 것** — 휴장일이다(§3-3) |

---

## §7. 남은 질문

1. **정각·30분 위상에 무엇이 겹치는가** (§2-5) — 검열된 호출 333건 중 103건이 여덟 분에 몰렸고
   감마플립 산출 불가 4건이 전부 `:01`이다. 그 분에 우리가 무엇을 더 쏘는지(만기유동성 폴러의
   `startup_offset_seconds=30.0`? 5분 주기 매크로? 300초 창 인쇄?) 또는 KIS 쪽 정각 부하인지가
   갈리지 않았다. **무엇을 보면 갈리는가**: 그 분의 REST 호출을 엔드포인트별로 초 단위로 펼쳐
   우리 호출이 겹쳤는지 본다. 겹치지 않았는데 HTTP가 4.0초면 **KIS 쪽**이고, 그때 레버 E는
   이 문제에 듣지 않는다.
2. **`chain_input_source`가 하루 490분 전부 `stale`인 것은 설계인가 결함인가** (§1-4) —
   `current`가 0분이고 08-17·08-14도 같다. 수집이 한 분을 통째로 쓰므로 「그 분의 체인」이
   존재할 수 없는 구조로 보인다. 그렇다면 이 지표의 `stale_pct`는 **영원히 100%** 이고
   판정에 쓸 수 없다. **무엇을 보면 갈리는가**: `newest` 타임스탬프와 판단 분의 차이 분포를
   인쇄한다. 전부 정확히 1분이면 구조이고, 흩어지면 결함이다.
3. **`member_availability` 지표 경로가 두 곳에 있다** (§6) — `db.member_availability`에는 값이
   있는데 `db.signal_reach.member_availability`는 `null`이다. `2026-08-16-fix1`이 후자를
   가리켰다. 어느 쪽이 정본인지 정하고 가설의 경로를 고쳐야 한다. Fix#2와 **같은 종류의
   결함**이다 — 지표 경로가 실재를 안 가리키면 가설은 조용히 무력화된다.
4. **레버 E가 오늘 문제에 듣는가** (§1-3 · §2-5) — 예산 96%(하루 평균)에는 듣지만 `:01` 넉 분에는
   안 들을 수 있다. 무조건발동일 08-25(D-7)까지 질문 1이 먼저 답해야 켜는 근거가 선다.
   **미루려면 `NEXT_TODO.md`에 사유를 적을 것** — 이번이 열 번째다.

---

## dev_memory 반영 초안

> 이 자동 실행은 `dev_memory`를 고치지 않았다. 사람이 검토한 뒤 반영한다.

`DECISION_LOG.md` append 초안:

```
### [MW0601] 2026-08-18 — 성공한 왕복이 어떤 지표에도 안 잡혔다

증상: 14:03 실주문 왕복(제출→조회→취소) 4단계 전부 rt_cd=0으로 성립했는데
      execution_logs는 0행이고, 그 가설(2026-08-16-c1)의 주장 지표 db.tables는
      execution_logs를 애초에 세지 않는다(mahdi/ops/db_metrics.py::_DAILY_TABLES).
원인: (1) measure_order_roundtrip.py가 db.insert_execution_log()를 호출하지 않는다
          — 증거가 logs/order_roundtrip_*.json에만 남는다.
      (2) _DAILY_TABLES 목록에 execution_logs가 없다. 자동 판정은 이것을
          「실측 없음」이 아니라 「수기 판정」으로 넘겨 눈에 띄지 않았다.
결정: 두 곳을 함께 고친다(보고서 Fix#2). 하나만 고치면 여전히 0행이거나 여전히 안 보인다.
Why: README가 경고한 「지표 경로가 실재하지 않아 가설이 조용히 무력화되는」 형태가
     오타가 아닌 모양으로 나타났다 — 경로는 맞는데 그 경로가 세는 목록에 대상이 없다.
검증: 라이브 미검증. 다음 왕복 실측일에 db.tables에 행이 뜨는지 확인한다.

### [MW0601] 2026-08-18 — 벽시계 데드라인 컷은 Fix#4의 우선순위 보호를 통과하지 않는다

증상: 우선순위위반=예 3건(14:05:52 · 14:06:52 · 15:01:52), 전부 budget_exceeded 경로.
      같은 날 timeout_abort 경로 6건은 전부 아니오였다.
원인: main.py:1996이 cut_by_scope 분기와 벽시계 데드라인 분기를 같은 if에 합쳐 놓아,
      데드라인 컷은 :1978의 선제 강등(dropped_non_priority_first)을 거치지 않는다.
결정: 데드라인 분기를 분리해 비우선 레그를 먼저 소거한다(보고서 Fix#1). 대가 지표 필수.
Why: 계측(Fix#5, 08-12)은 제 일을 했다 — 불변식이 스스로 예를 찍었다. 없던 것은 보호다.
     그리고 그 위반은 p50÷timeout 0.74, 즉 경고선(0.80) 아래에서 일어났다 —
     불변식 위반은 지연 게이지의 종속 지표가 아니다.
검증: 라이브 미검증. 회귀 테스트 + 08-19 관측.

### [MW0601] 2026-08-18 — 판단이 끊긴 넉 분이 전부 :01이었다 (위상 관측)

증상: 감마플립 산출 불가 4건 = 13:01:10 / 13:31:10 / 14:01:10 / 15:01:10.
      정규장 HTTP>=4.0초(검열) 333건 중 103건(31%)이 :00~:03·:30~:33 여덟 분에 몰림.
      그 분들의 페이서 대기는 1.0~1.8초로 평범 — 우리 백오프가 아니라 KIS 응답이다.
결정: 원인 미확정. 보고서 §7-1을 질문으로 열어 두고 08-19에 엔드포인트별 초 단위로 편다.
Why: 13:40 회차가 관측 2회로 유보한 추론을 하루치 분포가 지지했다. 그러나 「무엇이 겹치는가」는
     아직 관측이 아니라 후보다 — 단정하면 2026-07-30의 실패를 반복한다.
검증: 08-19 정각/30분 분의 REST 호출 분해.
```

`NEXT_TODO.md` 추가 초안:

```
- [ ] [P1] 데드라인 컷 경로 우선순위 보호 배선 + 회귀 테스트 (Fix#1)
- [ ] [P1] execution_logs를 _DAILY_TABLES에 추가 + measure_order_roundtrip이 DB에 적재 (Fix#2)
- [ ] [P1] 비거래일 배너 + 직전 거래일 기준 델타 — 08-17 P0 3일째 이월 (Fix#3)
- [ ] [P1] 워치독에 「당일 점검 산출물 부재」 알림 — 사흘째 이월 (Fix#4)
- [ ] [P2] 열화 게이지에 우선순위위반 독립 행 (Fix#5)
- [ ] [P2] REST 응답시간에 HTTP 성분 p50 + 검열 건수 분리 (Fix#6)
- [ ] [P2] 예약 회차 시각 통일 — cron "0 8 * * 1-5" (Fix#7)
- [ ] [P2] 워치독 OK 줄에 적재 분 수 인쇄 (Fix#8)
- [ ] [질문] 정각/30분 위상에 무엇이 겹치는가 — 08-19 엔드포인트별 초 단위 분해 (§7-1)
- [ ] [질문] chain_input_source가 영원히 100% stale인 구조인가 (§7-2)
- [ ] [질문] member_availability 경로 이원화 — 가설이 가리키는 쪽이 null이다 (§7-3)
- [ ] [레버] E를 08-19 07:30 기동에 실을 것 — 미루면 **사유를 여기 적을 것**. 열 번째다. D-7(08-25)
- [ ] [Slack] 워치독 재기동 0회 — 08-18 충족. 08-19도 0회면 경보 토글을 켠다
```

---

## 자체 검증

- [x] 보고서를 `docs/동작점검/` **루트**에 저장(`auto/`가 아니다)
- [x] 장전·장중 파일을 루트와 `auto/` **양쪽**에서 찾음 — 넷 다 루트에 있었다
- [x] 흡수한 원본 4개(`_점검_pre.md` · `_intra.md` · `_intra_1340.md` · `_intra_1430.md`) **미훼손**
- [x] 코드·`hypotheses.yaml` 상태·`dev_memory` **변경 0건** (§6은 제안, dev_memory는 초안)
- [x] 모든 이상점에 로그 시각·건수·파일명 첨부
- [x] 자동 §0/§1 판정을 규약 **E**(대가 — Fix#1·#2·#4에 대가 지표 명시) · **F**(축 — c1은 총건수가
      아니라 execution_logs 축) · **G**(시장 — 전일이 휴장일, §3-3) · **H**(레버 — 레버 E·F 전부 OFF)로 걸렀다
- [x] 만성(ZN/ES · stale 100% · regime_hmm 0점)과 신규를 분리
- [x] 자동 적신호 8건을 전부 처리: 1~4(예산 90~96%)→§2-5·고도화1, 5(p50 0.80)→§3-4,
      6(가설 3건 미등재)→§1-2 계열 이월, 7(보고서 누락)→이 문서, 8(dev_memory 미갱신)→초안 절
