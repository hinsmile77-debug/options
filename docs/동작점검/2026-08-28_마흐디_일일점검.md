# 마흐디 일일점검 — 2026-08-28 (금)

> ⏰ **사후 작성 — 2026-08-31 저녁.** 이 파일 자체가 08-28에 뜨지 않았다(2026-08-31 §1-1의
> 회차 6개 결번 중 하나). 아래는 08-31 장후 자동조치가 그날 자동 생성된
> `auto/2026-08-28_증거_post.md`(15:46 정시 생성) · `auto/2026-08-28_지표.{md,json}`을 근거로
> 사후에 재구성한 것이다. **관측 자체가 없던 국면(장전·장중)은 「관측 없음」이라고 쓴다** —
> 있었던 척하지 않는다. `docs/동작점검/tools/phases.md` A절·B절이 요구하는 실시간 체크리스트
> (기동 직후 육안 확인, 장중 여러 차례의 스냅샷 비교)는 애초에 그 순간에만 할 수 있는 것이라
> 사후에는 채울 수 없다.
>
> ⚠ **로그 원문은 아직 살아 있었다**(`observation_loop.log`가 하루 단위 append이고, 이 파일이
> 쓰인 시점엔 롤링 전이었다). 그래도 이 절은 원문을 새로 훑지 않고 그날 이미 계산된 사이드카를
> 옮긴다 — 사후에 「그 순간 사람이 무엇을 봤어야 했는가」를 새로 판단하는 것은 재구성이 아니라
> 창작이기 때문이다.

---

# 제1부 — 장전 (07:30 ~ 09:00) — **관측 없음**

이 회차가 뜨지 않았다. 프로그램 자신이 그 사실을 알고 있었다 — 워치독이
`09:00:02 [MISSING_CHECK] 오늘(2026-08-28) 장전 점검 산출물이 09:00까지 없다`를 남겼다
(증거 §6). **예약이 안 뜬 것을 예약으로 감시할 수 없으므로 이 줄이 유일한 신호였다.**

`premarket_startup.log`(증거 §2)는 정상이다 — 07:30:01 기동 시작, 마이그레이션 001~003은
경고와 함께(이미 적용됨) 건너뛰고 004부터 완료, 07:30:38경 관측 루프 기동. **프로그램은
정상 기동했고, 안 뜬 것은 「사람이 쓰는 점검 파일」뿐이다.**

# 제2부 — 장중 (12:30 · 14:30 회차) — **관측 없음**

두 회차 모두 뜨지 않았다. `auto/2026-08-28_증거_intra_1430.md`가 없다(같은 날 §12 적신호
15번이 스스로 그렇게 지목했다). 그 시각들의 판단이 무엇을 보고 있었는지는 시각을 지나면
복원 대상이 아니다.

# 제3부 — 장후 (15:20 ~ ) — 사후 재구성

> 재료: `auto/2026-08-28_증거_post.md`(15:46 정시 생성) · `auto/2026-08-28_지표.{md,json}`.

## 하루 요약 — 08-27과 거의 같은 조용한 날

| 축 | 값 | 전일(08-27) 대비 |
|---|---|---|
| 프로그램 생존 | 재기동 0회 · 크래시 0건 · 공백 0분(07:30~15:44) | — |
| 시세 수집 | 옵션체인 사이클 493 · 60초 초과 0건 · 최대 밀림 0.0초 | ±0 |
| 판단 입력 | 진입 판단 335건 · 판단 다양성(risk_gate_state 고유값) 396종 | — |
| 초당 REST 수요 | 0.389건/초(용량의 38.9%) · 적자 시작 배율 2.57배 | ▼0.9pt · ▲0.06배 |
| 워치독 | 판정 50줄(OK 49 · 비-OK 1 — MISSING_CHECK) · 재기동 0 · DEGRADED 0 · 최장 무판정 10분(08:50~09:00, 정상 범위) | 비-OK 1건 신규(사유는 위 제1부) |
| WS 재연결 | 0회(조용한 날) | ±0 |
| 실질 멤버 수 | 가용 평균 3.45 vs 실질 평균 **2.4**(죽은 축 평균 1.05) | — |
| 주문 | 나간 주문 **0건** · `order_blocked` **329건**(ADVISORY·쿨다운 미설정) | ▼15 |

**08-28은 인프라·관측 축에서 08-27과 거의 같은 조용한 날이었다.** 유일한 새 사실은
워치독의 MISSING_CHECK 1건 — 그런데 그것은 시장 관측의 문제가 아니라 **이 점검 자체가
안 뜬 것을 프로그램이 스스로 알아챈 것**이다(그 사실이 지금 이 파일이 존재하는 이유다).

## 자동 적신호 (§12 그대로 — 판정은 아니고 출발점이다)

1. 10~14시 REST수집 평균이 예산의 **92~103%**(경고선 90%) — 08-27보다 소폭 더 높았다
2. 15시 창 최대 `p50/timeout` **0.93** — 경고선(0.8) 초과. 08-31의 위험선 도달(1.00)과
   같은 계열이지만 이날은 경고선에서 멈췄다
3. `2026-08-04-p5` 조건 성립(p95>2.5초 이틀 연속, 오늘 **22구간**) — **발동 창은 다음 거래일
   장전뿐**이고, 그 08-29(토)는 비영업일이라 실질적으로 다음 영업일(09-01) 아침으로 넘어갔다
   (§3-3(가)에서 08-31 §1-2가 이미 「내일이 무조건발동일」로 처리)
4. 먼슬리 되살리기 실패 6건 · 전일 8건 대비 **▼2** · 회복률 74.2%
5. 비영 멤버 최대 3/6
6. 워치독 비-OK 1행 — MISSING_CHECK(원인은 위 제1부)
7. 검증예정일이 지난 pending 가설 **13건** — 08-31 §3-6이 이후 전부 판정 제안·확정했다
8. `hypotheses.yaml` 08-26 이후 무변경 — 08-31이 처리
9. 등재 제안 10건 미등재 — 08-31이 `inconclusive`로 닫음
10. 장후 산출물 누락(이 파일 자체 · 장중 1430 증거) — 이 사후 재구성이 앞의 것을 메운다

## 손익 — 거래 없음(정상)

| 항목 | 값 | 근거 |
|---|---|---|
| 실현·미실현 손익 | 0원 — 거래 없음 | `order_submitted 0` · `execution_logs` 0행 |
| 포지션 | 0 | `orphan_position 0` · 원장 사건 없음(§16-2) |
| 왜 0인가 | ADVISORY 모드 + 재진입 쿨다운 미설정 | `order_blocked 329` |
| 참고 — 판단은 계속 났다 | 진입 판단 335건, 사후 평가 5분 지평 적중률 42.8% · 15분 45.4% | §13 |

## 절대원칙 판정

| 원칙 | 판정 | 근거 |
|---|---|---|
| 신규 진입 컷오프 14:50 이후 진입 금지 | ✅ | 지표 §13 `진입 컷오프(14:50): 차단 31분 · 컷오프 이후 ENTER 0건` |
| 15:10 이후 강제 평탄화 | ✅ | §13 `강제 평탄화 15:10 이후 0건` · `forced_flat_verified 1` |
| 자동 주문 제출 금지(ADVISORY 유지) | ✅ | `order_submitted 0` · `order_blocked 329` |
| 장중 설정·코드 변경 금지 | ✅ | 당일 커밋 0건(증거 §1) |
| 고아 포지션 0 | ✅ | `orphan_position 0` |
| 일일 손실 하드 리밋 | — 해당 없음 | 거래가 없어 손실 경로가 없었다 |
| 레짐일당 우선 전략 상한 2 | ⏸ 판정 불가 — 이 날짜엔 계측 축이 없었다 | 08-31 P2-3이 이후 그 축을 채웠다 |

---

# 종합 판정표

| 축 | 제1부 장전 | 제2부 장중 | 제3부 장후(사후) |
|---|---|---|---|
| 프로그램 생존 | 관측 없음(단, `premarket_startup.log`는 정상) | 관측 없음 | ✅ 재기동 0 · 공백 0분 |
| 이 점검 자체 | ⛔ **MISSING_CHECK**(09:00) | 관측 없음 | 이 사후 절이 메운다 |
| 시세 응답 압박 | 관측 없음 | 관측 없음 | ⚠ 15시 창 최대 p50/timeout 0.93(경고선 초과) |
| 거래 | — | 관측 없음 | ✅ 주문 0건 · 포지션 0(설계대로) |

# 당일 이상점 통합 대장

| 번호 | 제목 | 심각도 | 처분 |
|---|---|---|---|
| 1-1 | 이 점검 회차 자체가 09:00까지 안 떴다(MISSING_CHECK) | P1 | 08-31 §1-1이 08-27~08-28 결번 6개로 이미 통합 기록. 원인(Claude 앱 예약이 왜 이틀 안 떴는지)은 저장소 밖이라 **08-31 사용자 조치 9번으로 이월 — 아직 안 풀렸다** |
| 1-2 | 15시 창 p50/timeout 0.93 경고선 초과 | P2 | 08-31 §1-11(위험선 도달)과 같은 계열의 선행 사례로 남긴다. 별도 조치 없음(08-31 P1-2가 이후 검열률·p95 적신호를 보강) |

---

# 제4부 — Fix 구현계획 · 제5부 — 고도화 방안

⛔ **이 사후 절은 새 Fix·고도화를 제안하지 않는다.** 08-28에서 나온 관측(위 이상점 두 건)은
전부 이후 회차의 실측으로 이어받아 처리됐다(08-31 §1-1·§1-2·§1-3·§1-11, 제4부 P1-2).
지난 날짜를 근거로 지금 새 계획을 짓는 것은 그 실측을 무시하는 것이다.

---

# 사용자 조치

**이 사후 절이 낳는 새 사용자 조치는 없다.** 미해결로 이월되는 것은 하나뿐이다 —
**Claude 앱 예약(장전·장중·장후)이 08-27~08-28 이틀간 왜 안 떴는가**는 저장소 밖의 문제라
이 파일이 답할 수 없고, 08-31 사용자 조치 9번으로 이미 올라가 있다.

---

*제3부 장후(사후 재구성) 끝 — 이 파일은 여기서 닫힌다. 2026-08-28의 사후 완성본이다.*
*⛔ 이 절은 코드 · 설정 · `hypotheses.yaml`의 `상태` · dev_memory를 한 줄도 바꾸지 않았다.*
*⛔ 제1부·제2부는 「관측 없음」이라고만 썼다 — 있었던 척하지 않았다.*
