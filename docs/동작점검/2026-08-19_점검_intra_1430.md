# 마흐디 장중 2차 점검 (14:30 회차) — 2026-08-19 (수)

- 국면 **intra** · 실행 **14:40~14:42 KST** (예약 `mahdi-intraday-check-1430` 예정 14:30) — **정시 실행**(10분 지연, 15:20 이전)
- 조사 범위 **12:37 ~ 14:42** (12:30 회차 종료 이후 구간) · 증거: `auto/2026-08-19_증거_intra_1430.md` (14:40:26 생성, 56.3KB)
- 대조 기준: `2026-08-19_점검_intra.md` (12:30 회차) — **덮어쓰지 않았다**
- 리포 HEAD `cd594df` · 브랜치 `master`
- **코드·설정·`hypotheses.yaml`·`dev_memory`를 하나도 바꾸지 않았다.** 장중 규약대로 Fix는 계획만 세운다.

---

## 한 줄 요약

**12:30 회차가 예고한 절벽이 13시에 실제로 왔다 — `13:06` 창에서 p50/timeout이 **1.01**로
위험선을 넘었고 `13:01~13:06` 6분 연속 전멸이 났다. 그리고 **자기회복했다.**
14:42 현재 p50/timeout **0.37**, rows=0 30분 비율 **0.00**, 앙상블 4/6, 마지막 레코드
`14:42:04`(점검 14:42:06, 지연 **2초**).**

**⚠ P0 — 신규 0건.** 12:30 회차의 P0(§2-1 프로세스 2회 사망)는 **에스컬레이션되지 않았다** —
세 번째 사망 없음. P1 신규 0건 · P2 신규 1건.

> **이 회차가 존재한 값어치**: 12:30 회차는 12시 비율 0.68을 보고 *"13시·14시가 0.8을
> 다시 넘는가가 14:50 개입 판단의 재료"* 라고 인계했다. **넘었다 — 13:06에 1.01까지.**
> 12:30만 있었다면 이 사건은 16:00 장후 회차에서야 보였을 것이고, 그때는 "왜 13시에
> 6분이 비었나"를 사후에 물었을 것이다. **오늘의 답은 다행히 「개입 불필요」지만,
> 그 답이 나온 근거는 회복을 실시간으로 확인했기 때문이다.**

---

## §0. 12:30 회차 인계사항 5건 — 전건 답

12:30 회차 §5가 지목한 다섯 가지에 순서대로 답한다.

| # | 12:30이 물은 것 | 14:42 현재 답 | 판정 |
|---|---|---|---|
| **1** | **세 번째 사망이 있었는가** (기동 4회면 에스컬레이션) | **없다.** 기동 **3회 그대로**(`07:30:19`/`09:54:23`/`10:36:22`). `.watchdog_state.json` `{"restarts": 2}` 불변. `observation_loop_crash.log` mtime **09:51에서 안 움직였다**. 12:37 이후 3분+ 공백 **0건** | ✅ **에스컬레이션 없음** |
| **2** | **12시 기울기** — 13·14시가 0.8을 다시 넘는가 | **넘었다.** 경고선 0.8 돌파 창 3개(`13:01` 0.84 / `13:36` 0.90 / `13:41` 0.86), **위험선 1.0 돌파 1개(`13:06` 1.01)**. 그러나 **14시 전 창이 0.8 미만**, 최신 `14:41` 창 **0.37** | ⚠→✅ **돌파 후 회복** |
| **3** | **먼슬리 컷이 오후에 느는가** (12:30 현재 1건) | **3건으로 증가** — `10:11:51` · **`12:51:53`(신규)** · **`14:05:52`(신규)**. `=아니오` 275건 대비 3건 | ⚠ **증가 (§2-2)** |
| **4** | **레짐이 `predict`를 유지하는가** | **유지.** 11:06 이후 `warmup_fallback` 재진입 **0회**. 12:30 이후 전 전이가 `모델=predict, 창=120분`. 창이 30분→120분으로 자랐다 = 세션이 안 끊겼다는 증거 | ✅ **유지** |
| **5** | **이벤트 캘린더가 갱신됐는가** | **아니다.** `14:38:10`에도 동일 경고. 당일 9건(12:30 시점 6건 → +3) | ❌ **미조치 (§2-3)** |

---

## §1. 오후 열화 게이지 — 창 단위 시계열

**이 절이 이 회차의 본체다.** 시간대 평균은 절벽을 눌러 없앤다(증거 §5-1 주석). 아래는
`inquire-price` **5분 창**(`REST 응답시간(300초 창)` 로그) 원본을 창 단위로 편 것이다.

### 1-1. `inquire-price` 지연 — p50 ÷ read timeout(4.0초)

12:30 회차 이후 구간(12:36~)을 전량, 그 앞은 돌파 창만 인용한다.

| 창 시각 | 건수 | p50(초) | **p50/TO** | 판정 |
|---|---|---|---|---|
| `10:09:34` | 66 | 3.59 | **0.90** | ⚠ **당일 경고선 최초 돌파** |
| `10:14:34` | 65 | 3.23 | 0.81 | ⚠ |
| `10:29:34` | 67 | 3.64 | 0.91 | ⚠ |
| … (10:41~12:31 전 창 0.8 미만, 최대 `12:41` 0.71) | | | | |
| `12:36:36` | 81 | 1.94 | 0.48 | — |
| `12:41:36` | 65 | 2.83 | 0.71 | |
| `12:46:36` | 79 | 2.33 | 0.58 | |
| `12:51:36` | 68 | 2.77 | 0.69 | |
| `12:56:36` | 66 | 2.55 | 0.64 | |
| **`13:01:36`** | 66 | 3.38 | **0.84** | ⚠ **오후 경고선 최초 돌파** |
| **`13:06:36`** | **52** | **4.05** | **1.01** | ⛔ **위험선 돌파 — 당일 유일** |
| `13:11:36` | 74 | 2.78 | 0.69 | ↓ 회복 시작 |
| `13:16:36` | 62 | 2.69 | 0.67 | |
| `13:21:36` | 80 | 1.53 | 0.38 | |
| `13:26:36` | 78 | 2.06 | 0.52 | |
| `13:31:36` | 62 | 3.09 | 0.77 | ↑ 재상승 |
| **`13:36:36`** | 61 | 3.58 | **0.90** | ⚠ **2차 돌파** |
| **`13:41:36`** | 56 | 3.44 | **0.86** | ⚠ |
| `13:46:36` | 60 | 3.06 | 0.77 | ↓ |
| `13:51:36` | 69 | 2.69 | 0.67 | |
| `13:56:36` | 75 | 2.41 | 0.60 | |
| `14:01:36` | 67 | 2.72 | 0.68 | |
| `14:06:36` | 63 | 2.83 | 0.71 | |
| `14:11:36` | 71 | 2.08 | 0.52 | |
| `14:16:36` | 64 | 2.81 | 0.70 | |
| `14:21:36` | 67 | 2.98 | **0.74** | 14시 최대 |
| `14:26:36` | 79 | 2.20 | 0.55 | |
| `14:31:36` | 68 | 2.75 | 0.69 | |
| `14:36:36` | 79 | 2.20 | 0.55 | |
| **`14:41:36`** | 83 | **1.49** | **0.37** | ✅ **최신 — 09:50 이래 최저권** |

**경고선(0.80) 최초 돌파 시각 — `10:09:34` 창, 0.90.**
**위험선(1.00) 최초 돌파 시각 — `13:06:36` 창, 1.01.**
**오후(12:37 이후) 경고선 최초 돌파 — `13:01:36` 창, 0.84.**

> **창 건수가 절벽의 그림자다.** 정상 창은 65~105건인데 `13:06` 창은 **52건** — 당일 최소다.
> p50이 timeout에 닿으면 같은 5분에 호출을 덜 쏘게 되고, 그것이 다시 체인을 얇게 만든다.
> `14:41` 창은 83건으로 정상 상단이다.

### 1-2. rows=0 사이클 비율 (30분 롤링)

| 기준 시각 | 창내 사이클 | rows=0 | 비율 | 판정 |
|---|---|---|---|---|
| `12:50` | 30 | 0 | 0.00 | — |
| `12:55` | 30 | 1 | 0.03 | 첫 전멸 `12:55:50` |
| `13:00` | 30 | 3 | 0.10 | |
| `13:05` | 30 | 8 | 0.27 | |
| **`13:06`** | 30 | **9** | **0.30** | ⚠ **경고선 최초 돌파** |
| `13:07`~`13:24` | 30 | 9 | 0.30 | ⚠ **19분 연속 경고선 유지** |
| `13:25` | 30 | 8 | 0.27 | ↓ |
| `13:30` | 30 | 6 | 0.20 | |
| `13:35` | 30 | 1 | 0.03 | |
| `13:40` | 30 | 0 | 0.00 | |
| `13:45`~`14:10` | 30 | 2 | 0.07 | `13:41`·`13:42` 재발 2건 |
| **`14:15`~`14:40`** | 30 | **0** | **0.00** | ✅ **완전 해소** |

- **위험선(0.70)은 한 번도 넘지 않았다.** 최대 0.30.
- 최장 연속 `rows=0`: **6분** (`13:01`~`13:06`, 임계 20분).
- 당일 전멸 ERROR **12건** — `10:10:53`(1) · `12:55:50`~`12:57:50`(3) · `13:01:54`~`13:06:53`(6) · `13:41:54`~`13:42:54`(2).
  **12:30 회차 시점 1건 → 12건.** 즉 **11건 전부가 이 회차 관할 구간에서 났다.**

### 1-3. 앙상블 멤버 · 관측 공백 · WS

| 지표 | 12:30 | 14:42 | 경고선 | 판정 |
|---|---|---|---|---|
| 앙상블 최신 사이클 | 4/6 | **4/6** (`14:40:10`) | ≤5/6 경고 | ⚠ 상시 4/6 — **12:30과 동일, 열화 아님** |
| 형태 분포 | 4/6×90, 3/6×12, 1/6×1 | 4/6×157, 3/6×15, 1/6×1 | ≤3/6 위험 | ✅ **3/6이 3건만 추가** — 절벽 구간에도 축이 안 죽었다 |
| 4멤버 마지막 관측 | 12:37 | **전원 `14:40:10`** | — | ✅ |
| 관측 루프 공백 | 2건(09:50·10:32) | **12:37 이후 0건** | ≥3분 경고 | ✅ |
| 마지막 레코드 → 현재 | 4초 | **2초** (`14:42:04` → `14:42:06`) | ≥3분 경고 | ✅ |
| WS 단절 | 0건 (진짜 0) | **0건** (12:30 이후 0) | 증가 추세면 명시 | ✅ **추세 없음** |
| 워치독 무기록 | 0건 | **0건** · 최종 `14:40:01` `"정상 — 마지막 박동 21초 전"` | ≥25분 | ✅ |

> **앙상블 4/6은 오늘의 상수이지 오후 열화가 아니다.** 09:01 이후 `regime_hmm`·`options_flow`·
> `orderflow_ofi_vpin`·`flow_position` 4종이 한 번도 안 빠졌다. 절벽 구간(`13:01~13:06`)에도
> 3/6으로 떨어진 것이 15건 중 3건뿐이다 — **체인이 비어도 판단 축은 버텼다.**
> 08-14는 이 지점에서 3/6까지 무너졌다. 오늘은 다르다.

### 1-4. 수집 실패 3종 분류 (규약 C — 「0건」을 가른다)

| 분류 | 12:30 | 14:42 | 계측 |
|---|---|---|---|
| 예산 초과 | 117건 | **195건** (13시 37 / 14시 26) | 살아 있음 |
| 연속 타임아웃 조기 포기 | 26건 | **81건** (13시 **30** / 14시 12) | 살아 있음 |
| 실패 예산 소진 | 0건 | **0건** | **진짜 0** — 08-18에 19줄(경로 생존 확인) |
| 체인 폴링 전체 실패 | (미집계) | **18건** (10시 3 / 12시 5 / **13시 9** / 14시 1) | 살아 있음 |
| 먼슬리 레그 재시도 | — | **54건** (13시 13 / 14시 11) | 살아 있음 |

> **13시에 「연속 타임아웃 조기 포기」가 30건으로 폭증한 것이 회복의 기전이다.** 확정 실패
> 호출을 안 쏘고 다음 분을 정시에 시작하는 안전장치(`2026-08-11 Fix#1`)가 13시 창에서
> 30번 작동해 **호출을 스스로 줄였고**, 그 결과 `13:11` 창부터 p50이 내려왔다.
> **이것은 설계가 의도대로 동작한 것이다** — 08-14에 없던 안전장치다.

---

## §2. 이상점 (12:30 이후 신규만)

### 2-1. [P2·신규] 13시 절벽 — 위험선을 넘었고, 스스로 물러났다

**증상**: `13:06` 창에서 `inquire-price` p50이 **4.05초**로 read timeout(4.0초)을 **추월**했고,
`13:01`~`13:06` 6분 연속 체인 전멸이 났다.

**근거**

```
2026-08-19 12:55:50 ERROR:mahdi.main:옵션체인 이번 분 전멸 — 수집 20레그 중 적재 0행(예산 45.4초 소진, 재시도 함)
2026-08-19 12:56:54 ERROR:mahdi.main:옵션체인 이번 분 전멸 — … 49.4초 소진
2026-08-19 12:57:50 ERROR:mahdi.main:옵션체인 이번 분 전멸 — … 45.5초 소진
2026-08-19 13:01:54 ERROR:mahdi.main:옵션체인 이번 분 전멸 — … 49.7초 소진
2026-08-19 13:02:54 ERROR / 13:03:54 ERROR / 13:04:53 ERROR / 13:05:54 ERROR
2026-08-19 13:06:53 ERROR:mahdi.main:옵션체인 이번 분 전멸 — … 48.7초 소진
2026-08-19 13:41:54 ERROR / 13:42:54 ERROR                              ← 꼬리 재발
```

```
REST 응답시간(300초 창) — inquire-price
13:01:36  66건 3.38/…초   → p50/TO 0.84  ⚠
13:06:36  52건 4.05/…초   → p50/TO 1.01  ⛔  ← 위험선 돌파, 창 건수 당일 최소
13:11:36  74건 2.78/…초   → p50/TO 0.69      ← 회복
```

**기준**: 증거 §5-1 — *"창 최대 p50 ÷ read timeout이 1.0에 닿으면 그 창의 호출 절반 이상이
타임아웃이고, 20레그 순차 수집의 기대 성공 수는 0에 수렴한다."* 실제로 그 창에서 기대값대로
적재 0행이 6분 연속 났다 — **모델이 맞았다.**

**08-14와의 대비 — 이것이 P0가 아닌 이유**

| | 2026-08-14 | 2026-08-19 (오늘) |
|---|---|---|
| 경고선 최초 돌파 | `13:51` 창 0.90 | `13:01` 창 0.84 (**50분 이르다**) |
| 위험선 돌파 | `14:06` 창 1.01 | `13:06` 창 1.01 (**60분 이르다**) |
| 그 후 | **전멸이 30분/30분 전부** — 절벽 | **6분 후 회복**, 최장 연속 6분 |
| 앙상블 | **3/6까지 붕괴** | 4/6 유지 (3/6 3건) |
| 조기 포기 안전장치 | 미배선 | **13시 30회 작동** |

**영향**: 정규장 09:00~14:42 중 체인 전멸 **12분**. 그 분들의 GEX/감마플립 입력은 직전
스냅샷을 썼다(신선도 창 안). 감마플립 산출 불가 경고 23건(`10:06:10`~`14:31:10`).

**왜 P2인가**: 자기회복했고, 회복의 기전이 설계된 안전장치였다(§1-4). 14:42 현재 전 지표가
정상선 아래다. **다만 이것은 오늘 두 번째 경고다 — 10:09 창에서 이미 0.90이 있었다.**
`OPTION_CHAIN_READ_TIMEOUT_SECONDS` 레버(현재 `None` = 4.0초)와
`OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`(현재 `{}`, **무조건발동일 2026-08-25 = D+6**)가
정확히 이 문제를 겨냥한 레버인데 **둘 다 OFF다.** 장후 회차의 논점이다.

---

### 2-2. [P1·재발 2건 추가] 먼슬리 컷 — 12:30의 1건이 3건이 됐다

**증상**: 데드라인 컷이 먼슬리(`regular`)에서 끝나는 사례가 오후에 2건 더 났다.

**근거**

```
10:11:51 예산(50초) 초과 — 남은 17레그를 포기하고  1레그로 마감 · 컷당한북=regular,weekly_thu · 데드라인이먼슬리에서끝남=예
12:51:53 예산(50초) 초과 — 남은 16레그를 포기하고  2레그로 마감 · 데드라인이먼슬리에서끝남=예   ← 신규
14:05:52 예산(50초) 초과 — 남은 15레그를 포기하고  4레그로 마감 · 데드라인이먼슬리에서끝남=예   ← 신규
```

당일 `=예` **3건** / `=아니오` **275건**.

**기준**: `phases.md` B-2 — *"먼슬리(regular)가 먼저 잘렸으면 우선순위 불변식 위반"*.
`2026-08-18_마흐디_운영점검보고서.md` 미완료 체크박스 `- [ ] [P1] 데드라인 컷 경로 우선순위 보호 배선 (Fix#1)`.

**만성/신규 구분**: **신규 결함이 아니다** — 08-18에 P1으로 잡혀 이월된 미구현 항목의
**실물이 3건으로 늘었다**는 보고다. 12:30 회차가 이미 §2-3으로 올렸으므로
**여기서는 「증가」만 보고한다**(회차 중복 금지).

**영향**: `12:51`·`14:05` 두 분의 GEX 입력이 각 2·4레그다. 특히 **`14:05:52`는 14:50 컷오프
45분 전** — 신규 진입 판단이 실제로 걸릴 수 있는 시간대다. 오늘 실집행 경로는 없다.

**Fix**: 12:30 회차 §4 Fix#4와 동일. 새 계획을 세우지 않는다.

---

### 2-3. [P1·이월 2일째, 미조치 확인] 이벤트 캘린더

```
14:38:10 [WARNING] 이벤트 캘린더 미기입: not_covered — covered_through(2026-08-18)가 오늘(2026-08-19)보다 앞선다.
  이벤트 근접 페널티가 걸리지 않는 상태다
```

12:30 회차 §2-4가 *"사람이 지금 1분 안에 할 수 있고 재기동이 필요 없는 것 하나"* 로 지목했으나
**2시간 뒤에도 미조치.** 당일 9건(12:30 시점 6건). **오후 내내 메타 라벨이 이벤트 페널티
없이 계산됐다** — `STANDARD` 100건 중 12:37 이후분 전량이 해당된다.

**조치(사람, 재기동 불필요)**: `uv run python scripts/suggest_event_calendar.py` → `covered_through` 이동.

---

### 2-4. [추론·P2] 레짐 전이 채터링이 오후에 잦아졌다 — 실측 아님, 관찰

```
14:03:01 VOL_COMPRESSION → RANGE_BREAK_PREP (안정=True, 모델=predict, 창=120분)
14:06:01 RANGE_BREAK_PREP → VOL_COMPRESSION
14:07:01 VOL_COMPRESSION → RANGE_BREAK_PREP
14:08:01 RANGE_BREAK_PREP → VOL_COMPRESSION
14:11:01 VOL_COMPRESSION → RANGE_BREAK_PREP
14:14:02 RANGE_BREAK_PREP → VOL_COMPRESSION
```

**11분간 6회 전이.** 당일 누적 34회(12:30 시점 20회) — `2026-08-12-fix7` 대가 지표
`db.regime.visits` 임계 **100회**에는 한참 못 미친다.

**이것을 이상점으로 올리지 않는 이유**: 전이 자체는 정상 동작이고 `안정=True · 모델=predict`다.
**추론**이라 명시한다 — 「채터링이 늘었다」는 인상이지 기준 위반이 아니다.
장후에 `db.regime.visits` 실측으로 확인할 것을 권고한다. **오늘 숫자로 판정하지 않는다.**

---

## §3. 14:50 컷오프 전 개입 판단 — 손익 (결정은 사람)

**현재 14:42. 신규 진입 컷오프(v6 §4.2) 14:50까지 남은 시간 — 8분.**

### 권고: **개입 불필요.** 그리고 그 결론은 산술적으로 강제된다.

| | **개입하지 않으면** | **지금 재시동하면** |
|---|---|---|
| 얻는 것 | 관측 연속성. 14:42 현재 p50/TO **0.37**, rows=0 **0.00**, 앙상블 **4/6**, 레짐 **predict(창 120분)** — **모든 게이지가 정상선 아래다** | 없다. 09:54·10:36 재기동이 **당일 커밋 10건을 이미 프로세스에 실었다**(12:30 §3) |
| 잃는 것 | 없음 | **확정 29분 warmup**(오늘 09:55→10:24, 10:37→11:06 두 번 실측) **+ 관측 3~4분 공백** |
| 시간 산술 | — | 14:42 재시동 → 관측 재개 **~14:46** → `predict` 복귀 **~15:15**. **컷오프 14:50을 25분 초과한다** |

> **재시동은 오늘 남은 시간 안에 이득을 낼 수 없다.** warmup 29분이라는 상수가
> 컷오프까지의 8분보다 크다. **비교할 필요가 없는 결정이다** — 손익표는 사람이
> 나중에 근거를 되짚을 수 있도록 남긴다.

### 지금 8분 안에 할 수 있고, 잃는 것이 없는 것 — 1건

- **`event_calendar.yaml`의 `covered_through` 갱신**(§2-3). 읽기 시점 평가라 **재기동 불필요**,
  코드 변경 아님(설정 파일). 12:30 회차가 권고했고 아직 미조치다.
  **이것 하나만은 14:50 전에 해도 손해가 없다.** 다만 이 세션은 손대지 않았다.

### 감시만 할 것 (15:20까지, 개입 없이)

- p50/TO가 **다시 0.8을 넘는가.** 오늘 두 번(10:09, 13:01) 넘었고 두 번 다 회복했다.
  세 번째가 오면 **패턴이지 사건이 아니다** — 장후 §2-1의 논점을 바꾼다.
- `기동 4회`가 되는가. 되면 12:30 §2-1이 **에스컬레이션**이다.

---

## §4. Fix — 계획 없음 (장중 규약 + 중복 금지)

**이 회차는 새 Fix를 세우지 않는다.** 12:30 회차 §4에 Fix#1~#7이 이미 서 있고,
오늘 오후 관측은 그중 어느 것도 뒤집지 않았다. **다만 오후 실측이 두 항목의 근거를 보강한다.**

| 12:30 Fix | 오후 실측이 더한 근거 | 우선순위 변화 |
|---|---|---|
| **Fix#4** [P1] 데드라인 컷 먼슬리 보호 | 실물 **1건 → 3건**(`12:51:53`·`14:05:52` 추가). 그중 하나는 **컷오프 45분 전** | **유지 P1, 근거 3배** |
| **Fix#5** [P1·사람] `event_calendar.yaml` | 2시간 추가 미조치, 9건 | **유지** |
| **Fix#7** [P2] 수집기 §5-1에 재기동 경계 표시 | — | 유지 |

### 신규 제안 1건 — **장후 검토**

**Fix#8 [P2] 수집기 §5-1을 시간대가 아니라 창 단위로 인쇄한다**

- **파일**: `docs/동작점검/tools/collect_evidence.py` §5-1 (`hourly_latency_p50`, line 558)
- **문제**: 현행 표는 **시간대(1시간) 단위**다. 오늘 13시 행은 「창 최대 1.01 ⛔」 한 줄인데,
  실제로는 `13:01` 0.84 → `13:06` **1.01** → `13:11` 0.69로 **6분 만에 오르내렸다.**
  **이 회차는 그 시계열을 손으로 다시 계산해야 했다**(로그 원본 재파싱).
- **변경**: `hourly_latency_p50`을 `window_latency_p50()`로 확장해 **5분 창 전량**을
  `(창시각, 건수, p50, p50/timeout)` 리스트로 반환하고, §5-1 아래에
  **「경고선 최초 돌파 창」·「위험선 최초 돌파 창」을 한 줄로 인쇄**한다.
- **근거**: 이 회차의 산출물은 **수준이 아니라 기울기와 선행 시간**인데, 그것을 내는 데
  매번 사람(또는 세션)이 로그를 다시 파싱하고 있다. **선행 시간을 자동으로 뽑는 것이
  14:30 회차 존재 이유의 자동화다.**
- **회귀 위험**: 창이 많아 표가 길어진다 → **돌파 창과 최신 3창만 인쇄**하고 전량은
  `auto/<날짜>_지연창.tsv`로 뺀다.
- **검증**: 08-14 로그로 돌려 `13:51` 창 0.90(경고 최초)·`14:06` 창 1.01(위험 최초)이
  자동으로 찍히는가. **그 두 숫자는 사람이 이미 손으로 구해 놨으므로 정답이 있다.**

**적용은 장후.** 이 세션은 코드를 바꾸지 않았다.

---

## §5. 장후(16:00) 회차 인계

1. **13시 절벽의 DB 축 확인** — `chain_input_source`로 `12:55`~`13:06`·`13:41`~`13:42` 12분의
   판단이 실제로 **직전 스냅샷**을 썼는지, 신선도 창(5분)을 넘겨 stale로 갔는지.
   로그에는 축이 없다(증거 §5-3). **전멸 6분 연속은 신선도 창 5분을 넘긴다** — 여기가 핵심이다.
2. **두 절벽(10:09, 13:06)이 같은 원인인가.** 10시는 재기동(10:36)으로 오염돼 있었다(12:30 §2-5).
   13시는 재기동이 없다 — **오염 없는 첫 관측이다.**
3. **레버 2종의 논점**: `OPTION_CHAIN_READ_TIMEOUT_SECONDS`(현 4.0초)와
   `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`(현 `{}`, **무조건발동일 08-25 = D+6**).
   오늘 13시가 정확히 그 레버들이 겨냥한 사건이다. **켤지는 장후에 판단한다.**
4. **`db.regime.visits` 실측** — §2-4의 추론을 숫자로 가른다.
5. **두 장중 회차 파일을 모두 읽어 §2 장중 절로 흡수한다**: `_점검_intra.md`(12:30) + 이 파일(14:30).
   **12:30이 P0를 세웠고, 14:30이 그것이 안 커졌음을 확인했다** — 그 순서가 보고서에 남아야 한다.

---

## §6. dev_memory 반영 초안 (이 세션은 직접 수정하지 않았다)

> ### [MW0601] 2026-08-19 장중 2차(14:30) — 13시 절벽을 넘었다가 스스로 물러났다
>
> - **증상**: `13:06` 창에서 `inquire-price` p50 4.05초가 read timeout 4.0초를 추월(비율 1.01).
>   `12:55`~`12:57`(3분)·`13:01`~`13:06`(6분)·`13:41`~`13:42`(2분) 체인 전멸 총 11분.
> - **경과**: `13:11` 창부터 0.69로 회복, `14:41` 창 **0.37**. rows=0 30분 비율은
>   `13:06`에 0.30(경고선)을 처음 넘어 19분 유지 후 `14:15`에 0.00 해소.
> - **회복 기전**: 13시에 「연속 타임아웃 조기 포기」(`2026-08-11 Fix#1`)가 **30회** 작동해
>   확정 실패 호출을 스스로 줄였다. **08-14에는 없던 안전장치이고, 08-14는 회복하지 못했다.**
> - **08-14 대비**: 경고선 돌파가 50분, 위험선 돌파가 60분 **이르다**. 그런데도 절벽으로
>   가지 않았다 — **시각이 아니라 안전장치가 결과를 갈랐다는 증거.**
> - **결정**: 장중 규약대로 코드 변경 없음. 14:50 컷오프까지 8분 — **재시동은 warmup 29분
>   상수 때문에 산술적으로 불가능**하므로 손익 비교 없이 개입 불필요.
> - **Why 기록하는가**: 「경고선을 넘었다」가 곧 「절벽」이 아니다. 오늘로 **경고선 돌파 후
>   회복한 사례가 3건**(10:09, 13:06, 13:36) 생겼다 — 임계값 해석의 기준선이다.
> - **부수**: 먼슬리 컷 `=예`가 1건 → **3건**(`12:51:53`, `14:05:52` 추가). 08-18 Fix#1 미구현의 대가.
> - **검증**: 라이브 미검증. 장후에 `chain_input_source`로 그 12분의 판단 입력을 확인.
>
> ### NEXT_TODO 추가안 (12:30 회차분에 더해)
> - [ ] [P2] 수집기 §5-1을 **창 단위**로 — 경고/위험선 최초 돌파 창을 자동 인쇄 (Fix#8).
>       검증: 08-14 로그로 `13:51` 0.90 / `14:06` 1.01이 자동으로 찍히는가
> - [ ] [장후] `OPTION_CHAIN_READ_TIMEOUT_SECONDS` · `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`
>       (무조건발동일 **2026-08-25, D+6**) — 오늘 13시가 이 레버들이 겨냥한 사건이다
> - [ ] [장후] `db.regime.visits` 실측 — 14:03~14:14 11분 6회 전이가 채터링인가 (현재 **추론**)

---

## §7. 자체 검증

- [x] 실행 시각(14:40 / 예정 14:30)을 확인하고 **정시 실행**(15:20 이전)임을 첫 줄에 밝혔다 — 밀린 실행 아님
- [x] 보고서를 `docs/동작점검/` **루트**에 `2026-08-19_점검_intra_1430.md`로 썼다.
      **12:30 파일(`_점검_intra.md`)을 덮어쓰지 않았다** · 증거는 `auto/`에 그대로
- [x] 오후 열화 게이지를 **창 단위 시계열**로 냈다 — `inquire-price` 5분 창 30개(§1-1),
      rows=0 30분 롤링(§1-2). **단일 스냅샷이 아니다**
- [x] 경고선 최초 돌파 창을 특정했다 — 당일 **`10:09:34` 0.90**, 오후 **`13:01:36` 0.84**,
      위험선 **`13:06:36` 1.01**
- [x] 12:30 회차 인계 5건 전건에 §0 표로 답했다
- [x] **14:50까지 남은 시간(8분)** 과 권고(**개입 불필요**)를 명시했다 —
      재시동이 warmup 29분 때문에 산술적으로 불가능함을 근거로
- [x] 모든 이상점에 로그 시각과 인용을 붙였다. **추론(§2-4)은 추론이라 명시**했다
- [x] 「건수 0」을 규약 C로 갈랐다 — 실패예산 0·WS 단절 0은 **진짜 0**(대체 축 확인)
- [x] 12:30 내용을 옮겨 적지 않고 **차이만** 보고했다 (먼슬리 컷은 「1→3 증가」만, Fix는 근거 보강만)
- [x] **코드·설정·`hypotheses.yaml`·`dev_memory`를 하나도 바꾸지 않았다.** 커밋·재기동 없음
