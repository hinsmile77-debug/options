# 마흐디 일일점검 — 2026-09-22 (화)

- 국면: **장전(pre)** · 예정 08:00 · 실행 08:07(정시, 7분 이내) — 밀린 실행 아님
- 증거 파일: `docs/동작점검/auto/2026-09-22_증거_pre.md`
- HEAD: `61bfaf8` · 브랜치 `master` · 미커밋 2건(`.claude/`, `_to_delete/`) — 지속(만성, 아래 1-4)

---

## §0. 오늘 한 장

**오늘 프로그램은 정상 작동했다.** 07:30 기동부터 지금(08:07)까지 관측 루프가 끊긴 적 없고, 오류(ERROR) 로그는 0건이다. 감시자(워치독 — 관측 루프가 죽었는지 1분마다 확인하고 되살리는 감시 프로세스)도 정상 신호만 남기고 있다.

**사람이 지금 해야 할 일은 없다.** 다만 오늘 안에 결정해야 할 것 두 가지가 있다 — 자세한 내용은 맨 끝 「사용자 조치」 절에 모아 뒀다.

**문제 5건을 찾았다(심각 0 / 보통 2 / 경미 3). 그중 새로 찾은 것은 1건(경미)이고, 나머지 4건은 어제까지 이미 알던 문제가 오늘도 그대로 이어지고 있는 것이다.** 심각(P0)은 없다 — 다만 이 판정은 07:30~08:07 사이에 관측된 것에 한한다. 그 이후는 아직 모른다.

**[12:30 장중 1차 갱신]** 09:00 개장부터 지금(12:36)까지도 프로그램은 끊긴 적 없이 정상 작동했다. 사람이 지금 해야 할 일은 여전히 없다. 장전이 걱정했던 "응답이 느려지는 시간대"는 오전 내내 반복됐지만 — 다행히 데이터가 통째로 끊긴 적은 한 번도 없었다. 다만 옵션 만기월 데이터를 다시 채우는 절차가 두 번 완전히 실패해(그 1분 동안 일부 계산이 데이터 없이 이뤄짐) 새 문제 1건이 늘었다. **문제는 총 6건(심각 0 / 보통 2 / 경미 4)**이고, 이 회차에서 새로 찾은 것은 1건(경미)이다.

**[14:30 장중 2차 갱신]** 12:36 이후에도 프로그램은 살아 있고 지금(14:37)도 정상 작동 중이다. **다만 12:30 회차가 "다행히 한 번도 없었다"고 안심했던 일이 오후에 실제로 일어났다** — 13:34~13:38 사이 5분 동안 옵션 시세를 그 분에 하나도 못 가져온 완전 공백이 처음 발생했고(그 5분간은 직전 시세를 대신 썼다), 4분 뒤(13:39)부터는 정상 수집으로 되돌아왔다. 오늘 거래 판단에 실제 영향은 없었다(그 시간대 진입 판단도 "살 종목 없음"으로 끝남). 그래도 이 경고 신호는 심각도를 한 단계 올려야 한다고 판단해 **경고(보통)로 올렸다.** 이것과 별개로, 앙상블(여러 신호를 종합하는 합의 구조) 안에서 "옵션 흐름" 신호 하나가 14시부터 유독 자주 0점(있으나 마나 한 상태)으로 빠지는 패턴이 나타났다 — 다만 이것은 8월 하순부터 반복돼 온 만성 패턴의 오늘자 재현이라 새 문제로 세지 않았다. **사람이 지금(14:50 마감 전) 해야 할 일은 없다** — 자세한 이유는 맨 끝 「사용자 조치」와 이 절 안의 「14:50 컷오프 권고」를 본다. **문제는 총 8건(심각 0 / 보통 3 / 경미 5)**이고, 이 회차에서 새로 찾은 것은 2건(보통 1 · 경미 1)이다.

**[15:45 장후 마감 — 오늘 종합]** 15:45 마감까지 프로그램은 끊긴 적 없이 하루 종일 정상 작동했고, 종료 절차(프로세스 정리·자동 지표 생성)도 이상 없이 끝났다. **오늘 하루 전체를 통틀어 사람이 개입해야 했던 순간은 한 번도 없었다.** 13:34~13:38의 데이터 공백은 그 5분이 지나자 스스로 회복됐고, 실제 주문·거래에는 처음부터 끝까지 영향이 없었다(오늘 시스템은 관측·판단만 하는 설정이라 주문 자체가 나갈 수 없었다 — 진입 판단 331번 중 310번이 이 설정 때문에 막혔다). **오늘 하루 문제는 최종 8건(심각 0 / 보통 3 / 경미 5)으로 마감됐고, 그중 4건이 오늘 새로 찾은 것, 4건은 어제 이전부터 이어진 것이다.** 손익은 0원(거래 자체가 없었음). **사람이 오늘 중 또는 곧 결정해야 할 것은 11가지**이며, 대부분 "언제 자동으로 진입·주문을 허용할지", "느려지는 시간대 대응을 지금 켤지" 같은 그동안 미뤄 온 결정들이다 — 전부 맨 끝 「사용자 조치」 절에 모아 뒀다. 상세 판독은 아래 제3부부터.

---

## 제1부 — 장전 (07:30 ~ 09:00)

### 1-0. 전일 이월

전일(2026-09-21) 파일은 하루 4개 회차(장전·장중①·장중②·장후)가 모두 정상으로 마무리됐고, 처분 안 된 이상점은 없었다. 다만 장후가 「내일(오늘) 장전이 받으라」고 명시적으로 넘긴 결정 1건과, 여러 회차째 사람 손을 못 탄 항목들이 있다. 전부 아래에서 처분한다.

| 전일 항목 | 처분 | 근거 |
|---|---|---|
| 사전 대응 규칙(혼잡 시간대 위클리 폴링 2분→4분 격분) 발동 여부 — "내일(09-22) 장전이 받는 결정" | 🔄 **지속 — 오늘도 결정 창을 이미 놓쳤다** | 아래 1-1 |
| `hypotheses.yaml` 검증예정일 지난 pending 4건 | 🔄 **지속 — 동일 4건, mtime 09-21 그대로** | 아래 1-2 |
| ZN/ES 해외선물 거래소 권한 결손 | 🔄 **지속(만성)** | 아래 1-3 |
| 미커밋 2건(`.claude/`, `_to_delete/backup_20260920/`) | 🔄 **지속** | 아래 1-4 |
| `2026-09-21-p1-1` 크래시 표식 `^C` 버그 수정 검증예정일 | 🆕 **오늘(09-22) 도래** — 예비 관측 시작 | 「오늘 관측이 답해야 할 것」 1번 |
| `.claude/`를 `.gitignore`에 넣을지 / `.gitignore:52` 정책 / ADVISORY 모드 재확인 / `2026-08-26-p1-2-1` id 확인 / ruff 미설치 | 🔄 **지속(사람 결정 대기, 코드 밖)** | 「사용자 조치」로 이월 |

> **처분 안 된 항목 없음.** 전일 「사용자 조치」 8개 항목 전부 위에서 처분하거나 이월했다.

### 장전 체크리스트 (phases.md A절)

**A-1. 기동** — 전부 확인됨.
- 07:30:00.67 `start_mahdi_premarket.bat` 실행, DB 마이그레이션 33건 이상 정상 적용
- Docker(redis/postgres) 기동 성공 — `unable to get image` 등 실패 로그 없음
- 관측 루프 기동 표식 **1회만**(07:31:21) — 재기동 없음
- `.last_successful_start.txt` = `2026-09-22T07:31:21` — 오늘 시각 맞음
- 마스터 파일·토큰·WS(웹소켓 — 실시간 시세를 밀어 넣는 연결) 구독 완료. 07:41~07:56 사이 먼슬리(월물) 옵션 레그(leg — 옵션 한 종목) 재조회 3건이 있었으나 전부 회복(3건 모두 100% 회복, 아래 참고)
- 워치독 08:00 마지막 기록, 08:07까지 7분 무기록이지만 호출은 08:07:02까지 계속 있었다("박동 정상") — 정상(정상 주기 10분 이내)

**A-2. 오늘의 계획**
- 검증예정일이 **오늘**인 pending 가설 1건 확인 — `2026-09-21-p1-1-crash-marker-survives-the-caret-c`(아래 관측 항목 1번)
- 검증예정일 **지난** pending 4건 확인 — 아래 1-2
- 「켤 것」 레버 확인 — 오늘 새로 켤 레버는 없음(모든 레버가 기존 상태 유지)
- 전일 커밋(HEAD `61bfaf8`, 09-21 16:41 이전)이 오늘 관측 루프 기동(07:31:21)보다 **먼저** 있었다 — 오늘 실린 코드가 전부 검증 가능한 상태
- **사전 대응 규칙 조건 성립 확인** — 아래 1-1에서 상세히 다룬다
- 전일 보고서 `--verify-report` 자동 대조 — ✅ 폐기 목록과 부딪히는 줄 없음, 인용된 파일 경로(`*.py` 4개) 전부 실재

**A-3. 전일 이월** — 위 1-0에서 처분함. 전일 `auto/2026-09-21_지표.json` 존재 확인(65.5KB, 09-21 15:47) — 전일 델타 계산 가능.

**A-4. 판정 문구** — 아래 §0과 이 절 전체에서 "개장 전 사람 조치: 불필요(관측 범위 07:30~08:07)" 형태를 썼다. "P0 없다"는 쓰지 않았다.

---

### 1-1. (지속·P1) 사전 대응 규칙 결정 — 오늘도 결정할 수 있는 시각이 없었다

**증상**: 어젯(09-21) 장후 보고서가 "오늘(09-22) 장전이 받는 결정"으로 두 가지를 넘겼다 — (A) 혼잡 시간대(등가격 부근 옵션 조회가 몰려 응답이 느려지는 시간대) 레버에 위클리(주간물) 폴링 간격을 2분→4분으로 늘리는 조치를 발동할지, (B) 계속 관찰만 할지. 여기에 새로 성립한 15시 시간대를 레버 대상에 추가할지도 함께.

**근거**: 이 규칙의 발동 창은 "조건이 성립한 다음 거래일 07:30 기동 전"으로 설계돼 있다(`docs/동작점검/tools/phases.md` A-2절). 그런데 이 점검 세션 자체가 **08:00~08:07에 실행**된다 — 오늘 07:30:00.67에 이미 장전 기동이 끝났다(위 기동 로그). 즉 이 세션이 결정을 받아도 반영 시점은 **이미 오늘이 아니라 내일(09-23)부터**다.

**기준**: `docs/동작점검/tools/phases.md` A-2절 "⛔ 장중 발동은 하지 않는다… 발동은 사람이 누른다(2026-07-08 페이서 분리 500 폭주 203분)". 09-21 보고서 자체도 이 구조적 미스매치를 09-18부터 이어 지적해 왔다(1-2, 지속).

**영향**: 이 결정은 사람이 언제 내리든 하루씩 늦게 반영된다 — 오늘 아침에 결정해도 오늘 장중에는 적용되지 않고, 내일 아침 기동 전에 다시 한 번 "어제 결정한 대로 지금 켤 것"을 확인해야 한다. 결정을 미루는 것 자체가 문제는 아니지만, 이 미스매치를 그대로 두면 매일 아침 같은 문장이 반복된다.

**심각도**: P1(정확성·의사결정 절차 훼손 — 오늘 거래에 직접 위험은 아님). 사람의 결정 대상(→ 사용자 조치 1번).

### 1-2. (지속·P1) 검증예정일 지난 pending 가설 4건 — 오늘도 그대로

| id | 검증예정일 | 경과 | 비고 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | 2026-09-01 | 21일 지남 | 전제 레버(`REGIME_RESTORE_SESSION_WINDOW`) 꺼짐 — 켜지 않는 한 판정 재료 없음 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 2026-09-01 | 21일 지남 | 09-18이 검증예정일 재설정(사건 기반) 제안, 아직 미확정 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 2026-08-27 | 26일 지남 | `inconclusive` 전환 제안, 아직 미확정 |
| `2026-09-15-eE-adds-nine-to-congested-hours` | 2026-09-16 | 6일 지남 | 9시는 이미 레버에 들어가 있어 사실상 답이 나 있음(§8-2 종결 목록 참고), 소급 판정 여부만 사람 몫 |

**근거**: `docs/동작점검/hypotheses.yaml` 최종 수정 09-21(mtime 그대로), 증거 §8.

**기준**: `docs/동작점검/README.md` 검증예정일 규약 — 예정일이 지나면 손 판정이 밀리고 있다는 뜻.

**영향**: 이 네 가설이 실제로 맞았는지 틀렸는지가 계속 미정으로 남는다. 판정을 미루는 자체는 큰일이 아니지만, "언제까지 미룰지"를 정하지 않으면 3주·한 달이 그냥 지나간다.

**심각도**: P1(정확성 훼손). 사람의 결정 대상(→ 사용자 조치 1번). **만성화 경고**: 09-21 보고서가 이미 "3거래일째 정지"를 지적했다 — 오늘로 나흘째다.

### 1-3. (지속·만성·P2) ZN/ES 해외선물 거래소 권한 결손

**증상**: 07:32:35 ZN(미국 10년 국채선물) 근월물 조회 실패, 07:32:39 ES(E-mini S&P500 선물) 근월물 조회 실패. 둘 다 같은 사유.

**근거**: `WARNING:mahdi.main` 07:32:35 `ZN 근월물 조회 실패: ZNU6 — CBOT SUB거래소 신청 계좌가 아닙니다.` / 07:32:39 `ES 근월물 조회 실패: ESZ6 — CME SUB거래소 신청 계좌가 아닙니다.`

**기준**: 증권사 계좌 설정(코드 밖). 매일 반복되는 만성 항목으로 이미 dev_memory에 기록돼 있음.

**영향**: 미국 국채·S&P500 선물을 참고 지표로 쓰는 교차자산 스트레스 계산에서 이 두 종목의 최신 데이터가 계속 빠진다. 다른 대체 데이터로 부분 보완되고 있어 오늘 거래 판단을 막지는 않는다.

**심각도**: P2(운영 부담, 만성). 새 발견 아님 — 그대로 이월(→ 사용자 조치 7번).

### 1-4. (지속·P2) 미커밋 2건

**증상**: `.claude/`, `_to_delete/backup_20260920/` 두 폴더가 여전히 git에 등록되지 않은 채(untracked) 남아 있다.

**근거**: `git --no-optional-locks status --porcelain` → `?? .claude/`, `?? _to_delete/`

**기준**: 09-21 보고서 사용자 조치 3·5번이 이미 "무엇의 백업인지 확인", ".gitignore에 넣을지 결정"을 요청했으나 아직 미결.

**영향**: 저장소 관리상 작은 부담이며 관측·거래에는 영향 없다. 다만 결정 없이 계속 쌓이면 나중에 실수로 같이 지워지거나 반대로 영구히 방치될 위험이 있다.

**심각도**: P2(경미). 그대로 이월(→ 사용자 조치 2·3번).

### 1-5. (신규·P2) 07:56 창에서 응답시간이 제한시간에 닿았다

**증상**: 옵션 체인 조회(수집 예산 50초, 개별 조회 제한시간 4.0초) 중 07:56 시간대 창에서 상위 5% 응답시간(p95 — 응답시간을 느린 순으로 나열했을 때 상위 5%에 해당하는 값)이 제한시간의 **1.00배**(4.0초 ÷ 4.0초)에 닿았다. 오늘 관측된 7개 창 중 이 창 하나만 그렇다.

**근거**: 증거 §5-1 "⛛ p95/timeout >= 1.0인 창 1/7개 — 최악 07:56 비율 1.00 · 최장 연속 1창(07:56~07:56)". 같은 시각 07:55:33에 "옵션체인 연속 타임아웃 3회… 남은 10레그를 포기(Fix#1, 정상 설계 동작)"가 있었고, 07:55:38·07:56:25에 먼슬리(월물) 레그 재시도가 각각 5개·1개 전부 회복됐다.

**기준**: 2026-08-27 P2-E "상위 5%가 제한시간을 넘으면 그 자체가 절벽(수집 실패가 이어지는 구간)의 선행 지표"라는 판단 축. 오늘 하루 검열(제한시간에 잘린 호출) 비율은 11건/699건(1.57%)로 낮고, 완전실패(그 분에 데이터를 하나도 못 모은 사이클)는 0건이다.

**영향**: 아직 실제 데이터 손실로는 안 이어졌다 — 이번 창은 07:30~09:00 장 시작 준비 구간에서 단발로 지나갔고, 곧이어 재시도가 전부 성공했다. 다만 과거 기록(08-14, 09-08)을 보면 이런 단발 신호가 장중에 반복·확대되며 실제 절벽으로 이어진 전례가 있어, 오늘 장중에 비슷한 패턴이 다시 나오는지 지켜볼 필요가 있다.

**심각도**: P2(경미, 관측 필요). → 「오늘 관측이 답해야 할 것」 2번.

---

### 오늘 관측이 답해야 할 것

이 목록은 장중·장후가 판정·보류·이월 중 하나로 반드시 닫는다.

1. **`2026-09-21-p1-1-crash-marker-survives-the-caret-c` 검증** — 검증예정일이 오늘이다. 예비 관측: 오늘 `logs/observation_loop_crash.log`의 기동 표식 줄(07:31:14)에는 어제(09-21, 07:30:46)와 달리 `^C` 접두가 **없다** — 수정이 정상 기동일에 문제를 안 일으킨다는 방향과 일치한다. 다만 정식 판정은 장후가 자동 지표(`crash.starts`, `crash.marker_present`)로 확인한다.
2. **07:56 응답시간 절벽 선행 신호(1-5)** — 장중에 p50/timeout(중앙값÷제한시간) 비율이 다시 0.8을 넘는 창이 나오는지, `rows=0`(그 분 데이터 완전 누락) 연속 구간이 나오는지 감시.
3. **사전 대응 규칙(1-1) 결정** — 오늘 사람이 (A)/(B) 중 무엇을 고르든 반영은 내일(09-23)부터다. 장후가 이 판단을 다시 확인하고 다음 장전에 넘긴다.

### Fix 구현계획 (초안)

이 회차는 코드를 변경하지 않는다. 아래는 장후 세션이 검토할 초안이다.

- **Fix 후보 — 사전 대응 규칙 결정 창 미스매치 해소(1-1)**: `docs/동작점검/tools/phases.md` A-2절에 "이 세션(08:00 실행)은 발동 창(07:30 기동 전)을 구조적으로 못 맞춘다"는 문구를 명문화하고, 결정이 실제로 반영되는 날짜(항상 D+1)를 자동으로 계산해 인쇄하는 절을 `collect_evidence.py` §8-4에 추가하는 것을 검토. (09-18부터 제안된 항목과 같은 건 — 아직 사람이 (A)/(B) 방식을 안 골랐다.)

### 고도화 방안 (초안)

- 오늘 관측(1-5)에서 출발: `collect_evidence.py` §5-1의 p95/timeout 위험선 돌파 창이 발견되면, 그 시간대에 한해 다음 1시간의 `rows=0`·연속 타임아웃 건수를 자동으로 나란히 보여주는 "사후 추적" 표를 추가하면, 사람이 매번 grep으로 되짚지 않고도 절벽 전조가 실제로 이어졌는지 한눈에 볼 수 있다. (예측치 미등재 — 아이디어 단계, 착수 전 사람 승인 필요.)

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (개장 전)

**사람 조치 없음.** 관측 범위(07:30~08:07) 안에서 개장 전 반드시 눌러야 할 버튼은 없다.

### 오늘 중 (장 마감 후)

1. **`hypotheses.yaml` 4건 처리 방향 결정** — `2026-08-26-p1-3`(inconclusive 전환) · `2026-08-19-fix2`(검증예정일 재설정) · `2026-08-12-fix7`(레버 켤지 여부부터 결정) · `2026-09-15-eE`(소급 판정 여부). 계속 미룰 거면 "미룬다"는 결정 자체를 남겨야 나흘째→닷새째로 조용히 늘어나지 않는다(1-2).
2. **`_to_delete/backup_20260920/` 확인** — 무엇의 백업이고 지워도 되는지(1-4).
3. **`.claude/`를 `.gitignore`에 넣을지 결정** — 매일 뜨는 `?? .claude/` 줄을 없앨 방법(1-4).

### 결정이 필요한 것 (자동으로 못 하는 것)

4. **사전 대응 규칙 — (A) 혼잡 시간대(9~14시) 위클리 폴링 2분→4분 격분 발동 / (B) 계속 관찰만.** + 오늘 15시 시간대도 레버에 추가할지 함께 결정(1-1). ⛔ 어느 쪽을 고르든 반영은 **내일(09-23) 아침부터**다 — 오늘 결정해도 오늘 장중에는 적용되지 않는다.
5. **`2026-08-26-p1-2-1` id 확인** — 이전 보고서 원문에서 이 id가 가리키는 fix가 무엇이었는지 사람의 기억이 필요하다.
6. **ZN/ES 해외선물 거래소 권한 신청** — 증권사 계좌 설정(코드 밖, 만성, 1-3).
7. **`.gitignore:52` 정책 결정** — `_지표.md`·`_증거_*.md`·`_지연창.tsv`를 계속 걸러낼지(현재는 `_지표.json`만 예외로 커밋됨).
8. **ADVISORY 모드 유지 여부 재확인** — 의도된 설정이라면 조치 불필요하나, 한 번은 사람이 의도한 상태가 맞는지 확인 권고(만성 이월 항목).
9. **ruff 린트 도구 설치 여부 결정** — `.venv`에 미설치 상태가 여러 회차 이어지고 있다. 설치할지, "이 저장소는 ruff를 안 쓴다"로 확정할지.

---

## 제2부 — 장중 ① (12:30 회차)

- 국면: **장중(intra)** · 예정 12:30 · 실행 12:36 — 정시(6분 이내), 밀린 실행 아님
- 증거 파일: `docs/동작점검/auto/2026-09-22_증거_intra.md`
- 관측 범위: 09:00(개장) ~ 12:36(이 회차 실행 시각)
- HEAD: `61bfaf8`(장전과 동일, 당일 커밋 없음) · 미커밋 3건(`.claude/`, `_to_delete/`, 이 보고서 자신 — 아래 2-0 1-4에서 처분)

### 2-0. 장전 이월 처분 — 5건 + 관측 항목 3건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 사전 대응 규칙(혼잡 시간대 위클리 폴링 2분→4분) 발동 결정 | 🔄 **지속 — 오늘도 미정, 조건은 오히려 강해졌다** | 아래 2-1 |
| 1-2 검증예정일 지난 pending 가설 4건 | 🔄 **지속 — 동일 4건, `hypotheses.yaml` mtime 09-21 그대로** | 증거 §8, §11 |
| 1-3 ZN/ES 해외선물 거래소 권한 결손 | 🔄 **지속(만성)** | 오늘도 07:32:35 ZN / 07:32:39 ES 동일 사유 재현(증거 §4) — 새 소견 없음 |
| 1-4 미커밋 2건(`.claude/`, `_to_delete/`) | 🔄 **지속** | `git --no-optional-locks status --porcelain` 동일 2건 + 이 보고서 파일 자신(untracked, 저장 직후라 당연함, 별건 아님) |
| 1-5 07:56 응답시간 절벽 선행 신호 | ✅ **판정(아래 2-1에서 상세)** | 장중에 다시 나오는지가 관측 대상이었다 — **다시 나왔고, 09:26부터 사실상 상시화됐다.** 다만 실제 데이터 손실(rows=0·완전실패)은 0건으로 유지됐다 |
| (관측1) `2026-09-21-p1-1` 크래시 표식 `^C` 버그 수정 검증 | ✅ **판정(잠정) — 가설과 일치하는 방향** | 오늘 기동 표식(`logs/observation_loop_crash.log` 07:31:14)에 `^C` 접두 **없음**(어제 09-21 07:30:46 줄에는 있었다). 최종 확정은 장후 자동지표(`crash.starts`·`crash.marker_present`)가 한다 — 이 회차는 로그 육안 확인까지만 |
| (관측2) 07:56 절벽 신호가 장중에 재발하는지 감시 | ✅ **판정** | 위 1-5와 같은 사안. 아래 2-1 참고 |
| (관측3) 사전 대응 규칙 (A)/(B) 결정 | ➡️ **이월(14:30 회차로)** | 사람이 언제 결정하든 반영은 내일(09-23) 아침부터다 — 이 회차가 결정을 대신 내릴 수 없다(장중 코드·설정 변경 금지). 아래 2-1에서 오늘자 조건 강화만 기록 |

> **처분 안 된 항목 없음.** 장전이 남긴 5건 + 관측 항목 3건 전부 위에서 처분했다.

### 장중 체크리스트 (phases.md B절) 통과 결과

**B-1. 생존** — 전부 정상.
- 관측 루프 마지막 레코드 12:36:31, 지금(회차 실행 12:36)과의 거리 **1분 이내** — 살아 있다
- 정규장 안(09:00~) 3분 이상 로그 공백 **0건**(증거 §3)
- 워치독 마지막 기록 12:30, 감시 창 끝(12:36)까지 **6분 무기록** — 정상 주기(10분) 이내. `.watchdog_last_check.json`이 12:36:02에 "정상 — 마지막 박동 1초 전"으로 별도 확인됨
- `observation_loop_crash.log` — 오늘 기동(07:31:14) **이후** 트레이스백 없음(파일 최종수정도 07:31) — 오늘 크래시 없음

**B-2. 수집 파이프라인**
- 사이클 관측 **304분**(옵션체인 사이클 소요 분해 로그 기준, 09:00~12:36 경과 216분보다 많음 — 07~08시대 예비 사이클 포함이라 정상)
- 예산 초과(budget_exceeded) **27건** / 연속 타임아웃 조기 포기(timeout_abort) **20건** / 실패 예산 소진(failure_budget_abort) **0건**(계측은 살아 있음 — 2026-09-18에 55줄 찍힌 전례로 확인, 진짜 0)
- 컷당한 북: `timeout_abort` 전부 `데드라인이먼슬리에서끝남=아니오` — **위클리를 아직 안 부른 채 먼슬리를 자른 위반 패턴 없음**(phases.md B-2 판정 축 확인)
- WS(웹소켓) 단절 **0건**(계측 살아 있음, 2026-09-09에 8줄로 확인) — 오늘 진짜 0. 단, **체결통보 스트림**(별도 채널) 끊김은 5건(매시 정각 부근, 예: 08:00:02·09:00:02·…·12:00:02) — 이것은 기존 fix(`test_main_order_notice_reconnect.py`)가 다루는 **만성 설계 동작**이라 새 항목으로 올리지 않음
- ATM 롤링 왕복 **60건**, 07~11시 분산 — 장전 대비 급증 없음

**B-3. 판단까지 갔는가**
- 앙상블 멤버: `flow_position`(07:31~) · `options_flow`(09:00~) · `orderflow_ofi_vpin`(08:46~) · `regime_hmm`(08:46~) 4종 전부 09:00 이전 또는 개장 직후 편입 완료 — 장전 내내 죽어있던 멤버 없음
- 레짐 갱신: 08:46 최초 전이(VOL_COMPRESSION) → 09:15 워밍업 종료(기동 후 103분) → 이후 WARMUP 재진입 **없음**
- 체인 신선도: 로그 축 계측 없음(정상, DB 축 `chain_input_source`가 담당 — 장후가 확인)
- 이벤트 캘린더 미기입: 오늘 **0건**(계측 살아 있음, 2026-09-20에 196줄로 확인 — 진짜 0)

**B-4. 하지 말아야 할 것** — 코드·설정 변경 없음(아래 전부 계획만).

### 2-1. (지속·P2, 실손실 없음) 응답시간이 제한시간에 닿는 창이 오전 내내 반복됐다

**증상**: 옵션 시세 조회(제한시간 4.0초) 중 상위 5%(p95 — 느린 순으로 줄 세웠을 때 상위 5% 지점) 응답시간이 제한시간의 1.00배 이상에 닿은 창(5분 단위)이 오늘 09:00~12:36 사이 **61창 중 25창**에서 나왔다. 09:26에 처음 다시 나온 뒤 09:36부터는 사실상 계속됐고, 가장 길게 이어진 구간은 **12:11~12:36(6창 연속, 30분)** — 이 회차를 실행하는 지금 이 순간도 그 구간 안에 있다. 최악 비율은 12:21창의 1.01배.

**근거**: 증거 §5-1 및 `auto/2026-09-22_지연창.tsv`. 어제(09-21)도 위험선(1.00배) 돌파 창이 61창 중 24창으로 오늘과 비슷한 수준이었다(`auto/2026-09-21_지연창.tsv` 실측) — **오늘이 어제보다 유독 나쁜 것은 아니다.** 같은 조건으로 "사전 대응 규칙(2026-08-04-p5)"의 발동 조건(p95>2.5초 구간이 이틀 연속 겹침)이 오늘도 성립했고, 겹치는 구간 수가 **어제 8개 → 오늘 11개**로 늘었다(증거 §5-1).

**기준**: 2026-08-27 P2-E "p95가 제한시간을 넘으면 절벽의 선행 지표"라는 판단 축(장전 1-5와 동일). 다만 실제 절벽(그 분 데이터 전체 소실)인지는 별도로 아래 수치로 확인한다 — 하루 검열(타임아웃에 잘린 호출) 비율 **4.45%(222/4991건)**, 최장 연속 `rows=0`(그 분 데이터 완전 누락) 구간 **없음**, 단발 완전실패 **0건**.

**영향**: 오전 내내 옵션 시세를 가져오는 속도가 제한시간에 바짝 붙어 있었다는 뜻이다. 그런데도 실제로 그 분의 데이터를 통째로 못 가져온 적은 한 번도 없었다 — 느려지긴 했지만 아직 끊기지는 않았다. 다만 이 상태가 반복될수록 다음 단계(진짜로 데이터가 끊기는 절벽)에 가까워진다는 경고는 계속 유효하다.

**심각도**: P2(경고 신호, 오늘 관측·거래에 직접 위험 아님). 1-5와 같은 사안이므로 새 번호를 붙이지 않는다. **결정 대상**: 1-1(사전 대응 규칙)과 동일 — 오늘 결정해도 반영은 내일부터다(→ 이미 사용자 조치 4번에 있음, 새로 추가하지 않음).

### 1-6. (신규·P2, 실손실 없음) 옵션 만기월 데이터 되살리기가 두 번 완전히 실패했다

**증상**: 장중에 놓친 만기월(월물) 옵션 레그(leg — 옵션 한 종목)를 그 분 안에 다시 가져오는 절차("먼슬리 레그 재시도")가 오늘 09:00~12:36 사이 **75회** 시도됐고 이 중 **9회가 부분 또는 완전 실패**(전일 09-21은 5회 실패, 레그 기준 회복률 93.2%)였다. 그중 **2회는 완전히 실패**(시도한 레그를 하나도 못 살림) — 10:06:47(1개 중 0개 회복)과 12:00:51(3개 중 0개 회복). 가장 심한 부분 실패는 12:30:53(이 회차 실행 6분 전)으로, 7개 중 **1개만 회복**해 6개 레그가 그 분 계산에서 빠졌다.

**근거**: `logs/observation_loop.log` 2026-09-22 10:06:47,923 / 12:00:51,186 / 12:30:53,602 — 전부 `WARNING:mahdi.main:먼슬리 레그 재시도: … 개 중 … 개 회복 … 되살리기 실패(판단 주입력이 이번 분에 얇아졌다)`. 증거 §5-1-1 요약표.

**기준**: 2026-08-24 Fix#4 "회복률만 보면 안 된다 — 완전 미회복과 부분 회복은 다른 사건"(README 규약). 완전 미회복은 그 분의 되살리기가 통째로 실패한 것이다.

**영향**: 완전히 실패한 두 번의 그 1분 동안은, 감마 익스포저(GEX)·감마플립 같은 판단 입력을 계산할 때 필요한 옵션 데이터 일부가 비어 있는 채로 계산이 진행됐다. 프로그램이 멈추거나 잘못된 주문을 낸 것은 아니지만, 그 분의 판단은 평소보다 얇은 정보로 내려진 것이다. 오늘 진입 판단이 3번 섰지만(11:30·11:58·12:26) 전부 "살 종목 없음"으로 끝나 실제 주문에는 영향이 없었다(ADVISORY 모드 + 종목 미충족).

**심각도**: P2(정확성 부분 훼손, 일시적, 오늘 거래·주문에는 미반영). 건수(9건, 전일 5건 대비 증가)는 규약 G(그날 KIS 상태에 비례)에 따라 그 자체로 판정하지 않는다 — 절대 건수보다 "완전 미회복이 있었는가"가 핵심이고, 오늘은 있었다.

### 14:30 회차가 이어받을 것

1. **1-1 / 2-1(사전 대응 규칙)** — 여전히 미정. 오늘 발동 조건이 어제(8구간)보다 강해졌다(11구간). 14:30 회차는 13:00~14:30 구간에서도 같은 패턴이 이어지는지, 15시 시간대가 새로 조건에 들어오는지 확인한다.
2. **1-2(pending 가설 4건)** — 오늘도 결정 없음. 계속 이월.
3. **2-1/1-6 절벽 선행 신호** — 12:11~12:36 연속 구간이 12:36 이후에도 이어지는지, `rows=0`(완전 누락)이 처음 나오는지 감시. 나오면 즉시 P1 격상 대상.
4. **(관측1) 크래시 표식 수정** — 장후 자동지표(`crash.starts`·`crash.marker_present`)로 최종 판정.
5. 새로 결정이 필요한 항목 없음(1-6은 사람 조치 불필요 — 자동 재시도 설계가 정상 동작한 결과).

---

## 제2부 — 장중 ② (14:30 회차)

- 국면: **장중(intra)** · 예정 14:30 · 실행 14:37 — 정시(7분 이내), 밀린 실행 아님
- 증거 파일: `docs/동작점검/auto/2026-09-22_증거_intra_1430.md`
- 관측 범위: 12:36(장중① 실행 시각) ~ 14:37(이 회차 실행 시각)
- HEAD: `61bfaf8`(변동 없음, 당일 커밋 없음) · 미커밋 3건(`.claude/`, `_to_delete/`, 이 보고서 자신) — 새 소견 없음

### 2-0. 장중① 이월 처분 — 전건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 사전 대응 규칙(혼잡 시간대 위클리 폴링 2분→4분) 발동 결정 | 🔄 **지속 — 오늘도 미정, 조건이 더 강해졌다** | 아래 2-2 |
| 1-2 검증예정일 지난 pending 가설 4건 | 🔄 **지속 — 동일 4건, `hypotheses.yaml` mtime 09-21 그대로(변화 없음)** | 증거 §8 |
| 1-3 ZN/ES 해외선물 거래소 권한 결손 | 🔄 **지속(만성)** | 13:42:50·13:42:55 동일 사유 재현. 새 소견 없음 |
| 1-4 미커밋 2건(`.claude/`, `_to_delete/`) | 🔄 **지속** | `git --no-optional-locks status --porcelain` 동일 2건 + 이 보고서 자신(저장 직후라 당연함) |
| 2-1/1-5 응답시간이 제한시간에 닿는 창 반복(경고 신호) | 🔄 **지속(P2 그대로) — 다만 그 경고가 실제 사건으로 이어졌다** | 아래 2-2에서 계속, 실제 발생분은 별도 신규 항목 **2-3(P1)**으로 분리 등재. 12:30 회차가 "나오면 즉시 P1 격상"이라 예고했던 그 사건이 실제로 났다 |
| 1-6 먼슬리 레그 되살리기 완전 실패 | 🔄 **지속 — 누적 2건 → 5건으로 늘었다** | 증거 §5-1-1, 13시 1건·14시 2건 추가(아래 2-0 표에 반영, 새 번호 없음) |
| (관측1) `2026-09-21-p1-1` 크래시 표식 `^C` 수정 검증 | ➡️ **이월(장후로) — 변화 없음** | 오늘 크래시 로그 여전히 07:31:14 기동 이후 트레이스백 없음. 최종 판정은 장후 자동지표(`crash.starts`)가 한다 |
| (14:30 이어받을 것 2) 1-2(pending 가설 4건) | 🔄 **지속** | 위와 동일 |
| (14:30 이어받을 것 3) 2-1/1-6 절벽 선행 신호 감시 | ✅ **판정 — 나왔다** | 아래 2-3. 실제 rows=0(완전 누락)이 오늘 처음 나왔다 |
| (14:30 이어받을 것 4) 크래시 표식 수정 최종 판정 | ➡️ **이월(장후)** | 장후 자동지표로 확정 |
| (14:30 이어받을 것 5) 신규 결정 항목 없음 | ✅ **판정 유지 — 대신 관측 항목 2건 신규(아래)** | 사전 대응 규칙 결정(1-1/2-2)과 별개로 새 결정 대상은 없음 |

> **처분 안 된 항목 없음.** 장중①이 남긴 5건(이월표) + 「이어받을 것」 5건 전부 위에서 처분했다.

### 장중 체크리스트 (phases.md B절) — 12:36 이후 재확인

**B-1. 생존** — 전부 정상.
- 관측 루프 마지막 레코드 14:37:45, 지금(회차 실행 14:37)과의 거리 **1분 이내** — 살아 있다
- 09:00~15:45 구간 3분 이상 로그 공백 **0건**(증거 §3) — 12:36 이후도 포함해 0건 유지
- 워치독 마지막 기록 14:30, 감시 창 끝(14:37)까지 **7분 무기록** — 정상 주기(10분) 이내. `.watchdog_last_check.json` 14:37:01 "정상 — 마지막 박동 28초 전"
- `observation_loop_crash.log` — 오늘 기동(07:31:14) 이후 트레이스백 없음(변화 없음)

**B-2. 수집 파이프라인**
- 예산 초과 **54건**(장중①의 27건에서 배로 늘었다) / 연속 타임아웃 조기 포기 **44건**(장중①의 20건에서 증가) / 실패 예산 소진 **2건**(둘 다 13:33~13:35, 아래 2-3과 같은 구간) — 계측 살아 있음
- **컷당한 북**: `timeout_abort` 전부 `데드라인이먼슬리에서끝남=아니오` — 위반 패턴 없음(변화 없음)
- WS(웹소켓) 단절 **0건**(진짜 0, 변화 없음). 체결통보 스트림 끊김 **7건**(매시 정각, 만성 설계 동작 — 새 소견 없음)
- ATM 롤링 왕복 **105건**(장중①의 60건에서 증가, 시간 경과에 비례 — 급증 아님)

**B-3. 판단까지 갔는가**
- 앙상블 멤버: 4종 전부(`flow_position`·`options_flow`·`orderflow_ofi_vpin`·`regime_hmm`) 14:33:54까지 생존, 죽은 멤버 없음
- 레짐 갱신: 08:46 전이 이후 재전이 없음(변화 없음, WARMUP 재진입 없음)
- 이벤트 캘린더 미기입: 오늘 0건 유지

**B-4. 하지 말아야 할 것** — 코드·설정 변경 없음(장중이므로 이 회차도 계획만 세운다).

### 오후 열화 게이지 (창 단위 시계열)

옵션 시세 조회(제한시간 4.0초) 상위 5% 응답시간(p95)과 중앙값(p50)을 5분 창으로 낸 것이다. 배율은 "제한시간 대비 몇 배인가"이고, 1.00을 넘으면 그 창의 절반(p50 기준) 또는 상위 5%(p95 기준) 호출이 이미 제한시간에 걸렸다는 뜻이다.

| 시간대 | 창 개수 | rows=0(완전누락) 사이클 | p50/timeout 창 최대 | 최초 경고선(0.8) 돌파 | p95/timeout≥1.0 창 |
|---|---|---|---|---|---|
| 07시 | 6 | 0 | 0.01 | — | 1/6 |
| 08시 | 12 | 0 | 0.01 | — | 0/12 |
| 09시 | 12 | 0 | 0.41 | — | 8/12 |
| 10시 | 12 | 0 | 0.42 | — | 9/12 |
| 11시 | 12 | 0 | 0.58 | — | 10/12 |
| 12시 | 12 | 0 | 0.63 | — | 12/12 |
| 13시 | 12 | **5**(13:34~13:38) | **0.99**(13:41창) | **13:41** | 12/12 |
| 14시(~14:37) | 8 | 0 | 0.69 | (유지) | 8/8 |

- **rows=0(완전 누락) 사이클 비율**: 13시 전체 60사이클 중 5건 = 8.3%(경고선 30% 미달). 다만 그 5건이 **13:34~13:38 5분 연속으로 붙어서** 났다 — 그 5분만 떼어 보면 **100%(5/5)**. 「분산된 8%」가 아니라 「집중된 5분 블랙아웃」이다.
- **inquire-price 지연**: 09시부터 완만히 오르다 13시에 처음 경고선(0.8)을 넘었다(13:41창 0.99). 14시 들어서도 0.5~0.7대를 오가며 높은 채로 유지 중(14:36창 0.64).
- **앙상블 비영 멤버**: 09:00~13:37 사이는 09:37·12:00·12:11 세 번의 짧은 저하(비영 3→2, 1분 이내 회복)뿐이었으나, **14:00부터 14:31 사이 9번**으로 빈도가 급증했다(아래 2-4).
- **관측 루프 공백**: 0건(전 구간).
- **WS 단절**: 12:30 이후 누적 **0건**(진짜 단절은 하루 종일 없음).

### 2-2. (지속·P2, 실손실 없음) 사전 대응 규칙 — 오늘 발동 조건이 더 쌓였다

**증상**: 09-22 하루 동안(07:30~14:37) p95(호출 수 가중) 2.5초를 넘은 시간대가 **18개** 구간으로 늘었다(장전 시점 다이제스트 기준 집계와 다른 축이라 직접 비교는 어렵지만, 전일(09-21)과 이틀 연속 겹치는 구간이 **14개**로 늘었다).

**근거**: 증거 §5-1 "🔔 사전 대응 규칙 발동 조건 성립 — 규칙 2026-08-04-p5 · 이틀 연속(직전 거래일 09-21) 같은 구간 14개 / 오늘 성립 18개".

**기준**: 규칙 `2026-08-04-p5` — 미리 정해 둔 조치는 "해당 시간대 위클리 폴링 2분→4분 격분"이나, **자동 발동하지 않는다. 발동 창은 다음 거래일 장전(07:30 기동 전)**이다(2026-07-08 사고 이후 원칙).

**영향**: 오늘 안에 사람이 (A)/(B) 중 무엇을 고르든, 반영은 내일(09-23) 아침부터다 — 이미 사용자 조치 4번에 있는 결정이고 새로 추가할 항목은 없다.

**심각도**: P2(결정 대기, 지속). 새 번호 없음(1-1/2-1과 같은 사안).

### 2-3. (신규·P1, 실손실 없음이나 데이터 공백 발생) 13:34~13:38 — 오늘 처음으로 옵션 시세가 5분간 통째로 끊겼다

**증상**: 옵션 시세를 그 분 안에 한 줄도 못 가져온 "완전 누락(rows=0)" 사이클이 13:34·13:35·13:36·13:37·13:38 **5분 연속**으로 났다. 시스템은 설계대로 "그 분은 직전 스냅샷(가장 최근 값)을 대신 썼다"(경고 로그 자체 문구). 4분 뒤(13:39)에는 다시 정상 수집(10행)으로 돌아왔고, 그 사이 알림(Slack 등)은 토글이 꺼져 있어 사람에게 전달되지 않았다(13:38:32 "알림 스킵(토글 꺼짐)").

**근거**: `logs/observation_loop.log` 2026-09-22 13:34:33 / 13:35:52 / 13:36:53 / 13:37:54 / 13:38:32, 전부 `ERROR:mahdi.main: 옵션체인 이번 분 전멸 — 수집 N레그 중 적재 0행`. 13:38:32 "알림 스킵(토글 꺼짐) — 옵션 체인 데이터가 5분째 결손 중". 13:39:51 정상 회복(적재 10행).

**기준**: 12:30 회차(2-1)가 "12:11~12:36 6창 연속 p95/timeout≥1.0"을 관측하며 "다시 나오면(=실제 rows=0이 나오면) 즉시 P1 격상 대상"으로 명시적으로 다음 회차에 넘긴 조건. 최장 연속 임계(20분)에는 못 미쳤으나(5분), **오늘 처음 발생한 실제 데이터 공백**이라는 점에서 격상 기준을 충족한다.

**영향**: 그 5분 동안 프로그램이 내린 판단은 몇 분 전 시세를 대신 쓴 것이다 — 급하게 움직이는 장이었다면 오래된 정보로 판단했을 것이다. 다행히 그 시간대는 이미 "살 종목 없음"으로 끝나는 흐름이었어서(13:34:54·13:39:54 진입판단 로그) 실제 주문에는 영향이 없었다. 다만 알림이 꺼져 있어서, 이 공백은 사람이 로그를 직접 열어보지 않았다면 아무도 몰랐을 것이다.

**심각도**: **P1(정확성 훼손 — 오늘 실제 거래엔 영향 없었으나 데이터 공백이 실제로 발생)**. 1-6·2-1의 연장선이나 처음으로 "경고"에서 "실제 발생"으로 성격이 바뀌어 새 번호를 받는다. → 사용자 조치 절에는 추가하지 않는다(장중 코드 변경 금지 원칙과 이미 실손실 없이 회복됐기 때문 — 장후가 Fix 여부를 검토).

### 2-4. (신규·P2, 실손실 없음·만성 패턴 재현) 14시부터 "옵션 흐름" 신호가 자주 0점으로 빠졌다

**증상**: 앙상블(여러 신호를 종합하는 합의 구조, 설계상 6개 슬롯 중 4개만 실제로 구현돼 있다) 안의 `options_flow`(옵션 흐름 — 옵션 시장의 매수·매도 압력을 읽는 신호) 축이, 14:00부터 14:31 사이 **9번**(14:00·14:09·14:13·14:15·14:22·14:23·14:24·14:25·14:31) 점수 0(있어도 반영이 안 되는 상태)으로 빠졌다. 그 이전 09:00~13:59(5시간)에는 단 2번(12:00·12:11)뿐이었다. 별도로 13:38에는 이 신호가 아예 목록에서 완전히 빠졌다가(축 이탈) 1분 만에 복귀했다.

**근거**: `logs/observation_loop.log` "판단 형태 전이" 로그, 14:00:54~14:31:54 구간에서 `0점축=['regime_hmm', 'options_flow']` 9회. 13:38:54 `판단 축 이탈: options_flow (가용 4→3, 비영 3→2) · 사유 원재료없음[gex·gamma_flip·wall·spot]`, 13:39:54 재편입.

**기준**: 이 현상은 오늘 처음이 아니다 — `docs/동작점검/hypotheses.yaml`(§4874·§7686)과 `NEXT_TODO.md`(2026-09-11 장후 기록)에 "14:00 이후 `options_flow` 이탈 재발" 항목이 이미 **미결 상태로 등재**돼 있고, 그때도 "오늘 14:27:54 사례"로 같은 시간대 패턴이 기록됐었다. 즉 이 패턴은 **만성**이고, 오늘은 그 재현 사례가 추가된 것이다.

**영향**: 그 9번의 순간에는 "옵션 흐름" 신호 하나가 판단에서 빠진 채로(다른 3개 신호만으로) 결론을 냈다는 뜻이다. 신호 하나가 통째로 없는 것보다는 낫지만, 14시 이후로 판단의 근거가 평소보다 좁아지는 시간이 잦아졌다.

**심각도**: P2(만성 패턴의 재현, 실손실 없음). **신규 결정 사안 아님** — 이미 NEXT_TODO에 "독립 항목으로 등재하고 예측치를 세울지 정할 것"이 열려 있으므로 그 결정을 재촉할 뿐, 이 회차가 새로 만들 결정은 없다.

### 14:50 컷오프 권고

**⏰ 지금(회차 실행 14:37) 기준 14:50까지 13분 남았다.**

- 13:34~13:38 데이터 공백(2-3)과 14시 이후 신호 저하(2-4) 둘 다 **이미 지나갔거나 간헐적으로 반복 중이며, 지금(14:37) 이 순간은 정상 상태다**(14:33:54 기준 앙상블 4/6 정상, 최근 사이클 rows=10 내외로 정상 수집 중).
- **재시동 없이 얻는 것**: 오늘 관측 연속성 유지, 남은 13분(그리고 15:20 마감까지 남은 43분) 동안의 추가 관측이 끊기지 않는다.
- **재시동으로 얻는 것**: 수집 경로가 완전히 새로 열린다 — 다만 대가는 레짐 워밍업 재진입 **29분**(08-19 실측)이고, 지금 재시동하면 남은 장중 시간 대부분을 워밍업 상태로 날린다.
- **오늘 실제로 거래·주문에 영향을 준 사건은 없다**(두 사건 다 "살 종목 없음"·"신호 3/4개로 정상 판단" 선에서 끝남).

**권고: 「개입 불필요」.** 재시동도, 레버 변경도 필요 없다. 다만 13:34~13:38 공백은 P1로 격상해 장후가 원인(어제부터 이어진 REST 응답 지연의 누적 결과로 보임 — §2-2·§2-3 참고)을 더 본다.

### Fix 구현계획 (초안 — 장후 검토용, 이 회차는 미착수)

- **Fix 후보 — 옵션체인 결손 알림 토글이 꺼져 있다(2-3 관련)**: 13:38:32 "알림 스킵(토글 꺼짐) — 데이터가 5분째 결손 중"이 찍혔다. Slack 등 알림 토글이 이미 2026-08-01 사용자 결정으로 보류 확정된 사안(NEXT_TODO 참고)이라 **다시 올리지 않는다** — 다만 이번처럼 실제 데이터 공백이 났을 때 사람이 로그를 안 열어보면 놓친다는 사실은 장후가 이 결정을 재확인할 때 참고 자료로 남긴다.
- **Fix 후보 — `options_flow` 14시 이후 0점 재발(2-4)**: 이미 NEXT_TODO(09-11)에 "독립 항목으로 등재하고 예측치를 세울지 정할 것"이 열려 있다. `mahdi/features/options_intel.py`의 부호0 판정 로직(스팟-기준선)이 14시 이후 장 후반에 더 자주 0이 되는지, `hypotheses.yaml`에 예측치(예: "14시 이후 0점 비율이 그 전보다 높다")를 세워 검정하는 것을 장후가 검토.

### 고도화 방안 (초안)

- 2-3에서 관찰한 대로, "옵션체인 이번 분 전멸"이 **연속 3분 이상** 나면 알림 토글 상태와 무관하게 최소 워치독 로그(`watchdog.log`)에는 무조건 한 줄 남기는 방안을 제안한다 — 지금은 이 회차처럼 사람이 직접 로그를 읽어야만 그 5분을 알 수 있다. (예측치 미등재 — 아이디어 단계, 장후 승인 필요.)

### dev_memory 반영 초안 (장후가 실제로 반영)

- `NEXT_TODO.md`에 추가 검토: "2026-09-22 13:34~13:38 rows=0 5분 연속 — 12:30 회차가 예고한 P1 격상 조건이 실제로 발생한 첫 사례. 원인은 그 직전 예산초과·실패예산소진·연속타임아웃이 겹친 결과로 보이며 별도 트리거는 없었다(REST 응답 지연의 누적)."
- `hypotheses.yaml` 후보: `options_flow` 14시 이후 0점 비율 상승이 매일 재현되는지 검정할 가설 등재 여부는 사람 결정(2-4 참고, 이미 NEXT_TODO에 열려 있음).

---

## 제3부 — 장후 (15:20 ~ )

- 국면: **장후(post)** · 예정 15:45 · 자동 증거수집 실행 15:47(정시) · 이 보고서 작성 실행 16:17 — 밀린 실행 아님(자동 산출물은 정시에 이미 만들어져 있었고, 이 회차는 그것을 읽고 판독하는 절차라 시각 차이는 지연이 아니다)
- 증거 파일: `docs/동작점검/auto/2026-09-22_증거_post.md` · 지표: `docs/동작점검/auto/2026-09-22_지표.md`(+`.json`)
- 관측 범위: 14:37(장중② 실행 시각) ~ 15:47(증거수집 시각), 자동 지표는 07:30~15:45 하루 전체
- HEAD: `61bfaf8`(변동 없음, 당일 커밋 없음) · 미커밋 5건(`.claude/`, `Claude outputs/`, `_to_delete/`, 이 일일점검 파일 자신, `auto/2026-09-22_지표.json`) — 뒤 두 개는 이 점검 세션 자신의 산출물이라 당연하다

### 3-0. 장중② 이월 처분 — 전건 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1/2-2 사전 대응 규칙(혼잡 시간대 위클리 폴링 2분→4분) 발동 결정 | 🔄 **지속 — 오늘도 미결정, 하루 종일 조건이 더 쌓였다** | 아래 손익/사용자 조치. 오늘 성립 구간이 20개(장중②까지는 18개)로 마감 시점 더 늘었다(증거 §5-1) |
| 1-2 검증예정일 지난 pending 가설 4건 | ✅ **판정 — 아래 3-2에서 4건 전부 제안과 함께 처분(사람 확정은 아님)** | `hypotheses.yaml` mtime은 09-21 그대로지만, 이 회차가 4건 각각에 제안을 남긴다(§5 규약) |
| 1-3 ZN/ES 해외선물 거래소 권한 결손 | 🔄 **지속(만성) — 종가까지 재현 없음, 오전 사유 반복** | 07:32:35 ZN / 07:32:39 ES(증거 §4) — 오후 재실패 없음(하루 1회씩, 만성이라 이미 알려진 패턴) |
| 1-4 미커밋 5건 | 🔄 **지속** | `git --no-optional-locks status --porcelain` — 위 HEAD 줄 참고. 신규 2건(`Claude outputs/`, `_지표.json`)은 이 세션 자신의 산출물 |
| 2-1/1-5 응답시간이 제한시간에 닿는 창 반복(경고 신호) | 🔄 **지속(P2) — 하루 끝까지 이어졌다, 실제 절벽은 13:34~13:38 1회뿐이었다** | 하루 전체 `p95/timeout≥1.0` 창 **48/98개**, 최악 12:21(1.01배)·최장연속 6창(12:11~12:36). 검열률 30% 초과 창 2개(최악 13:41 50%). 다만 **위험선(1.00배) 최초 돌파는 「없음」**으로 자동 지표가 냈다 — 이는 「그 분의 p50이 검열 때문에 하한값이라 실제로는 더 나쁠 수 있다」는 뜻이지 안전하다는 뜻이 아니다(증거 §5-1) |
| 1-6 먼슬리 레그 되살리기 완전 실패 | ✅ **판정 — 하루 최종 7건으로 마감(장중②까지 5건 + 오후 2건 추가: 14:55:46 · 15:00:54)** | `logs/observation_loop.log` 14:55:46 "4개 중 0개 회복" · 15:00:54 "3개 중 0개 회복". 하루 전체 재시도 126회 중 완전 실패 7건 · 부분 실패 12건(자동 지표 §5-1-1) |
| 2-3 13:34~13:38 옵션 시세 5분 공백 | ✅ **판정 — 하루 안에서 재발 없음, 단발 사건으로 종결** | 자동 지표 §4·§14: 하루 전체 `rows=0` **5분**(13:34~13:38 단일 구간), 최장 연속 구간도 동일 5분 — 14:37 이후 추가 발생 없음. 같은 5분 구간에서 「GEX 입력 없던 분」도 **2분**(13:34대 겹침, §14) — 부분적으로 살아난 분이 있어 5분 전부는 아니었다. 「체인 스냅샷 최고령 레그」 하루 최대 **4.9분**(294.6초, 임계 3분 초과, §14)도 같은 사건의 다른 축 — 5분 공백 직후 판단이 4.9분 묵은 스냅샷을 쓴 순간이 있었다는 뜻. 아래 3-3에서 원인 분석 마무리 |
| 2-4 14시 이후 options_flow 0점 재발(만성) | 🔄 **지속(만성 패턴, 실손실 없음) — 종가까지 이어짐** | 앙상블 가용성(§14-1): `options_flow` 가용률 77.3%(382/494분), 구조적 미가용 110분(주로 장전·스팟 미적재 구간). 하루 전체 비영 멤버 수 분포: 비영 0(전 축 중립) 최장 **74분** 연속 — 다만 이 연속 구간이 정확히 몇 시인지는 이 회차 증거에 시각별 세부가 없어 특정 못 함(→ 사용자 조치 8, 고도화 후보) |
| (관측1) `2026-09-21-p1-1` 크래시 표식 `^C` 수정 검증 | ✅ **판정 — confirmed 제안** | 아래 3-2 참고 |

> **처분 안 된 항목 없음.** 장중②가 남긴 8건(이월표) 전부 위에서 처분했다.

### 3-1. 종료 시퀀스 (15:20~15:45)

- `stop_mahdi_marketclose.bat` 실행 — `.last_marketclose_stop.txt` = `2026-09-22T15:45:03.746711`(정시)
- taskkill 후 잔존 프로세스 **0**(`shutdown_check_log`)
- `daily_ops_report.py`가 종료 절차 안에서 정상 실행 — `auto/2026-09-22_지표.md`·`.json` 15:47 생성 확인(증거 §9)
- 관측 루프 최종 레코드 15:45:01(HTTP 요청) — 종료 시각과 일치, 그 전에 죽지 않았다
- 워치독: 15:30·15:40 두 회차 모두 `OK`, 다만 "적재 감시 창 밖(적재 상태 모름)" 문구로 전환(정상 — 15:20 마감 이후 옵션체인 적재 감시가 설계상 꺼지는 구간)
- **이상 없음.** 종료 시퀀스는 설계대로였다.

### 3-2. 오늘 판정해야 할 가설 4건 — 제안 (상태 변경은 사람 몫)

| id | 검증예정일 | 경과 | 이 회차 제안 | 근거 |
|---|---|---|---|---|
| `2026-09-21-p1-1-crash-marker-survives-the-caret-c` | 2026-09-22(오늘) | 도래 | **`confirmed`** | 세 예측 전부 일치: `crash.marker_present=True`(오늘 로그에 `^C` 접두 없음, `logs/observation_loop_crash.log` 132행 `[2026-09-22 7:31:14.84] ===== 관측 루프 기동 =====` — 전날 09-21 127행 `^C[2026-09-21 7:30:46.71]…`와 대비), `crash.starts=1 >= 1` 확인, `crash.unattributed=3(3~3 기대)` 확인. 오늘이 이 fix(09-21 커밋 `e4b7224`)가 실제로 검증 가능한 첫 정상 기동일이었다 |
| `2026-08-12-fix7-regime-session-window-restore` | 2026-09-01 | 21일 지남(오늘로 6번째 미실행 회차) | **`pending` 유지 — 예정일만 재조정** | 전제 레버 `REGIME_RESTORE_SESSION_WINDOW`가 오늘도 `False`(지표 §0). 판정 재료 자체가 없다(규약 H). `hypotheses.yaml`의 기존 `판정근거`(08-23 기록)가 이미 이 논리를 적어 뒀다 — 새로 쓸 말 없음. **레버를 켤지부터 사람이 정해야** 다음 예정일을 잡을 수 있다(→ 사용자 조치) |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 2026-09-01 | 21일 지남 | **`inconclusive`로 전환 검토** | `execution_logs.rows=0`(`>=0` 기대와는 트리비얼하게 일치하지만, 이 가설이 실제로 검정하려는 것은 "왕복 실측이 행을 남기는가"이고 오늘도 왕복(실주문) 자체가 없었다(ADVISORY 모드, 하루 종일 주문 0건·차단 310건). 코드가 안 돈 것과 같은 처지 — 검증예정일을 날짜가 아니라 "실제 주문이 처음 나가는 날"로 사건 기반 재설정 제안(NEXT_TODO에 이미 열려 있는 제안, §8-3) |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 2026-08-27 | 26일 지남 | **`inconclusive`로 전환 검토** | 이 가설의 주장 축("다음 거래일 장전 보고서가 그 시간대를 인용하고 그 자리에서 발동 여부를 정하는가")은 오늘 아침(1-1)도 **인용은 했으나 결정은 못 냈다** — 사람 손이 필요한 절차이기 때문에 코드 관측만으로는 영원히 `pending`을 벗어날 수 없는 구조다. 오늘 아침 장전 회차가 이미 이 구조적 미스매치를 `phases.md` 명문화 Fix 후보로 제안했다(제4부 Fix#1로 재확인) — 그 Fix가 실제로 들어가면 이 가설은 폐기하고 새 가설로 대체하는 편이 맞다 |
| `2026-09-15-eE-adds-nine-to-congested-hours` | 2026-09-16 | 6일 지남 | **`pending` 유지 — 판정보류, 사람이 축 재검토** | 오늘 9시 `inquire-price` p95 **3.385초**(기준선 3.06초, 임계 2.5초) — 개선은커녕 기준선보다도 나쁘다. 다만 규약 G(그날 KIS 상태에 비례)상 하루로 판정 못 한다. **6일이 지나도록 9시 p95가 임계 밑으로 내려간 날이 눈에 띄지 않는다** — 사람이 여러 날을 함께 읽고, 필요하면 판정축 자체(NEXT_TODO §8-3 "판정 축을 p95 임계 → `budget_exceeded`로" 제안)를 바꿔야 할 시점으로 보인다. 참고로 `9시를 레버에 넣을 것인가`라는 **결정 자체는 이미 2026-09-15 NEXT_TODO에서 종결**돼 있다(증거 §8-2) — 이 가설은 그 결정의 효과를 검정하는 별개 항목이다 |

> **상태 변경은 사람이 손으로 확정한다.** 위 제안은 이 회차의 판단이며 `hypotheses.yaml`을 직접 고치지 않았다.

### 3-3. (지속 마무리·P1) 13:34~13:38 데이터 공백의 원인 — 오전부터 쌓인 압력의 누적, 단일 트리거 없음

**증상**: 2-3(장중②)에서 P1로 격상한 13:34~13:38 5분 공백의 원인을 하루치 데이터로 다시 본다.

**근거**: 같은 시간대(13시)에 백오프 확대 5건 · 잔고폴링실패 2건 · 먼슬리재시도 실패 3건(완전미회복 1건) · 실패예산소진 2건(13:33:41·13:35:22, 하루 전체에서 이 2건이 전부)이 몰려 있었다(지표 §5-1-1). 같은 시간대 `inquire-price` p95가 처음으로 경고선(0.8)을 넘었다(13:41창 0.99배). **하나의 원인이 아니라, 오전부터 완만히 올라오던 압력(09시 p50/timeout 0.41 → 12시 0.63 → 13시 0.99)이 13시대에 처음으로 임계에 닿으면서 예산·재시도·잔고폴링이 동시에 얇아진 결과**로 읽힌다.

**기준**: 2-3이 인용한 "다시 나오면 즉시 P1 격상" 조건(12:30 회차 명시)과 08-14 §2-2의 판정 축(p50/timeout이 0.8을 넘으면 절벽 전조). 2026-08-04 §2-6 규약(백오프가 낮은데 타임아웃이 나면 KIS 귀속, §17 교차점검과 동일 결론)도 부합 — 오늘 백오프 최대 1.82배로 낮았는데(전일 2.25배보다도 낮음) 타임아웃은 449건(전일 대비 검증 안 됨, 절대 건수 자체는 하루 표본)에 달해, **우리 쪽 압력이 아니라 KIS 응답 지연**이 원인이라는 기존 판정 축과 일치한다.

**영향**: 이 5분 동안 판단은 오래된 시세로 났다는 뜻이지만, 이미 2-3에서 확인했듯 그 시간대 실제 주문·진입에 영향은 없었다(ADVISORY 모드, 그 시간대 진입 판단도 "살 종목 없음"). 다만 이 압력이 매일 조금씩 더 강해지는 추세라면(1-1/2-2가 추적하는 「이틀 연속 겹침 14→20구간」 증가), 다음 번 공백은 5분보다 길어질 수 있다.

**심각도**: P1 유지(2-3과 동일 사안, 새 번호 없음). **결정 대상은 1-1(사전 대응 규칙)과 같다** — 새로 추가할 사용자 조치 없음.

### 3-4. 장후 신규 이상점 — 없음

이 회차가 자동 지표·증거 전체를 훑은 결과, 14:37 이후 새로 발생한 **독립적** 이상점은 없다. 오후 늦게 발생한 사건(1-6의 완전실패 2건 추가, 2-1/2-2의 지연창 지속)은 전부 기존 항목의 연장이라 새 번호를 붙이지 않았다.

---

## 종합 판정표

| 구간 | 장전(07:30~09:00) | 장중①(09:00~12:36) | 장중②(12:36~14:37) | 장후(14:37~15:45, 종합) |
|---|---|---|---|---|
| 전체 판정 | ✅ 정상, P0 없음(관측범위 한정) | ✅ 정상, P0 없음 | ⚠ P1 신규 1건(2-3) 발생·회복 | ✅ 정상 마감, 하루 전체 P0 **0건** |
| 응답시간 압력(1-1/1-5/2-1/2-2) | 07:56 단발 신호(경고 수준) | 09:26부터 사실상 상시화(25/61창) | 12:11~12:36 6창 연속(최악 1.01배) | 하루 끝까지 지속, 위험선(1.00) 돌파는 실제로 없었음(직전 수치는 검열 하한) — 사람 결정(사전 대응 규칙) 오늘도 미정 |
| 데이터 공백 | 없음 | 없음(0건) | **13:34~13:38 5분 발생 → P1 격상** | 재발 없이 종결, 원인은 오전부터 누적된 KIS 응답 지연(3-3) |
| 앙상블/판단 | 4멤버 전원 정상 편입 | 정상 | 14시부터 options_flow 0점 재발(만성) | 종가까지 4멤버 유지, 만성 패턴 그대로 |
| 사람 결정 | 불필요 | 불필요 | 불필요 | **오늘 중 9건 남음(아래 사용자 조치)** |

## 당일 이상점 통합 대장

| 번호 | 제목 | 최초 발견 국면 | 최종 심각도 | 최종 처분 | 근거 위치 |
|---|---|---|---|---|---|
| 1-1 | 사전 대응 규칙 결정 — 발동 창과 점검 세션 실행 시각의 구조적 미스매치 | 장전 | P1 | 🔄 지속(하루 종일 미결정, 조건 20구간까지 증가) | §1-1, 2-2, 3-0, 3-3 |
| 1-2 | 검증예정일 지난 pending 가설 4건 | 장전 | P1 | ✅ 판정(이 회차가 4건 모두 제안 남김, 확정은 사람) | §1-2, 3-2 |
| 1-3 | ZN/ES 해외선물 거래소 권한 결손 | 장전 | P2(만성) | 🔄 지속(변화 없음) | §1-3, 3-0 |
| 1-4 | 미커밋 파일 (`.claude/`, `_to_delete/` 등) | 장전 | P2 | 🔄 지속(오늘 5건으로 소폭 증가, 세션 자신의 산출물 포함) | §1-4, 3-0 |
| 1-5 | 07:56 응답시간 절벽 선행 신호(p95/timeout 위험선 접촉) | 장전 | P2 | ✅ 판정(2-1/2-2로 계속 추적, 위험선 실제 돌파 없이 종결) | §1-5, 2-1, 2-2, 3-0 |
| 1-6 | 먼슬리 레그 되살리기 완전 실패 | 장중① | P2 | ✅ 판정(하루 최종 7건, 실손실 없음) | §1-6, 3-0 |
| 2-3 | 13:34~13:38 옵션 시세 5분 완전 공백 | 장중② | P1 | ✅ 판정(단발 종결, 원인은 3-3에서 정리) | §2-3, 3-0, 3-3 |
| 2-4 | 14시 이후 `options_flow` 0점 재발(만성 패턴 재현) | 장중② | P2(만성) | 🔄 지속(NEXT_TODO에 이미 열린 결정 대기) | §2-4, 3-0 |
| (관측) | `2026-09-21-p1-1` 크래시 표식 `^C` 수정 검증 | 장전(관측 시작) | — | ✅ 판정(confirmed 제안) | 1-2 관측①, 2-0, 3-0, 3-2 |

**총 9건**(이상점 8건 + 관측 1건). 심각(P0) **0건** · 보통(P1) **3건**(1-1, 1-2, 2-3) · 경미(P2) **5건**(1-3, 1-4, 1-5, 1-6, 2-4). 오늘 새로 찾은 것은 **4건**(1-5 · 1-6 · 2-3 · 2-4) — 나머지 4건(1-1, 1-2, 1-3, 1-4)은 전일 이전부터 이어진 것이다.

## 종가 기준 최종 손익 / 절대원칙 판정

- **거래 없음.** 오늘 나간 주문 **0건**, 체결 **0건**, 보유 포지션 **0건**(장 마지막 기준). 진입 판단(ENTER) 331건이 모두 `mode_not_auto_submit`(ADVISORY 설정) · `reentry_cooldown_not_configured`(쿨다운 0) 사유로 **310회 차단**됐다(지표 §16-4). 나머지 21건은 14:50 컷오프 이후 시간대라 애초에 진입 시도가 없었다(§13, 컷오프 이후 ENTER 0건).
- **실현손익**: 0원(거래 없음). **미실현손익**: 0원(보유 포지션 없음). **자본 대비**: 0%. **고점 대비**: 0%.
- **절대원칙(하드 리밋·금지 사항) 위반 여부**: ✅ **해당 없음 — 판정 대상 자체가 없다.** 이 시스템은 ADVISORY(관측·판단 전용) 모드로만 설정돼 있어 오늘 어떤 주문도 물리적으로 나갈 수 없었다(설정 파일을 사람이 바꿔야 풀리는 게이트, §16-4). 리스크 한도·포지션 한도 위반을 판정할 실제 거래 자체가 없었으므로 "위반 없음"이 아니라 "판정 대상 없음"이 정확한 문구다.

## 제4부 — Fix 구현계획 (P0/P1/P2)

이 회차는 코드를 변경하지 않는다. 아래는 사람이 착수를 지시하면 다음 세션이 예측치 선등재 후 진행할 구현 계획이다.

**P1 — 없음.** 오늘 새로 확정된 P1급 코드 결함은 없다(1-1·2-3 모두 코드 버그가 아니라 결정 프로세스·KIS 응답 지연이 원인).

**P2-1 (사전 대응 규칙 발동 창 미스매치 명문화 — 1-1/2-2/3-3 공통 원인)**
- 파일: `docs/동작점검/tools/phases.md` A-2절
- 변경: "이 세션(08:00 전후 실행)은 발동 창(07:30 기동 전)을 구조적으로 못 맞춘다"는 문구를 명문화하고, 어느 요일이든 결정이 실제로 반영되는 날짜(항상 D+1)를 계산해 인쇄
- 추가: `docs/동작점검/tools/collect_evidence.py` §8-4 절에 "결정을 지금 내려도 반영일은 YYYY-MM-DD"를 자동 계산해 붙이는 한 줄 추가(현재는 "발동 창은 다음 거래일 장전"이라는 고정 문구만 있음, §522행)
- 회귀 위험: 낮음(문서·리포트 텍스트 변경, 로직 없음)
- 검증: 다음 장전 회차 보고서가 그 문구를 그대로 인용하는지 육안 확인

**P2-2 (13:34~13:38류 공백 재발 시 알림 사각지대 — 2-3/3-3 관련, 09-22 §5부 초안 재확인)**
- 파일: `mahdi/main.py` (또는 워치독 관련 모듈, "옵션체인 이번 분 전멸" ERROR 로그를 내는 지점)
- 변경: "옵션체인 이번 분 전멸"이 **연속 3분 이상** 이어지면, Slack 토글 상태와 무관하게 `logs/watchdog.log`에 최소 한 줄(예: `DATA_GAP_3MIN+`)을 남긴다
- 대가: 알림 정책(Slack 보류, 2026-08-01 결정)은 그대로 유지 — 이것은 알림이 아니라 **다음 날 워치독 로그를 훑을 때 놓치지 않게 하는 계측**이다
- 회귀 위험: 낮음(신규 로그 문구 추가, 기존 판단 로직 불변)
- 검증: `tests/test_watchdog_observation_loop.py`에 3분 연속 전멸 시나리오 케이스 추가

**P2-3 (`2026-08-19-fix2` 검증예정일 사건 기반 재설정)**
- 파일: `docs/동작점검/hypotheses.yaml` (해당 id 항목)
- 변경: `검증예정일`을 고정 날짜 대신 "최초로 `execution_logs`에 행이 남는 날" 같은 사건 기반 문구로 변경(사람 승인 필요 — 착수 전 결정)
- 회귀 위험: 없음(문서 변경)

## 제5부 — 고도화 방안

- **오늘 관측(3-3)에서 출발**: 오전부터 완만히 올라오는 p50/timeout 비율(09시 0.41 → 13시 0.99)이 임계(0.8)를 넘는 순간을 실시간으로 COCKPIT 배지에 노출하면, 사람이 장중에 "곧 절벽이 온다"를 15:46 지표가 아니라 그 순간에 알 수 있다. 이미 NEXT_TODO에 같은 취지 제안(§8-3 "고도화 1 — 지연 위험선을 no_ingest의 선행 경보로 승격")이 열려 있다 — 오늘 사례가 그 제안의 세 번째 근거가 된다.
- **오늘 관측(3-0/2-4)에서 출발**: 앙상블 "비영 0(전 축 중립)" 연속 구간이 오늘 최장 74분 나왔는데, 이 회차 증거에는 그 구간의 정확한 시작·종료 시각이 없어 원인 추적이 막혔다(3-0 참고). `docs/동작점검/tools/collect_evidence.py`의 §5-2(앙상블 멤버 가용성)에 "비영 0/1/2/3 각 구간의 시작·종료 시각 목록"을 추가하면, 다음에 같은 구간이 나왔을 때 그 시간대가 구조적 사유(단일가 등)인지 실제 신호 고갈인지 즉시 가를 수 있다.

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다. (이 절이 오늘 최종본이다 — 제1부 끝의 이전 절은 그 시각 기준 기록으로 남겨 둔다.)

### 지금

**사람 조치 없음.** 장 마감 이후이며 오늘 개입이 필요한 사안은 없었다.

### 오늘 중 (완료 확인용, 위 목록과 동일 사안 통합)

1. **`hypotheses.yaml` 4건 처리** — 이 회차의 제안(3-2)을 검토해 상태를 손으로 확정: `2026-09-21-p1-1`→`confirmed`, `2026-08-12-fix7`→`pending` 유지(예정일만 미룸), `2026-08-19-fix2`→`inconclusive` 검토, `2026-08-26-p1-3`→`inconclusive` 검토, `2026-09-15-eE`→`pending` 유지(사람이 여러 날 함께 읽고 판정축 재검토).
2. **`_to_delete/backup_20260920/` 확인** — 무엇의 백업이고 지워도 되는지.
3. **`.claude/`를 `.gitignore`에 넣을지 결정.**
4. **`Claude outputs/`도 함께 정리 대상에 포함할지 결정** — 오늘 새로 나타난 미커밋 폴더(이 점검 세션과 무관한 것으로 보이나 확인 필요).

### 결정이 필요한 것 (자동으로 못 하는 것)

5. **사전 대응 규칙 — (A) 혼잡 시간대(9~14시) 위클리 폴링 2분→4분 격분 발동 / (B) 계속 관찰만.** + 15시 시간대도 레버에 추가할지(1-1/2-2). ⛔ 오늘 결정해도 반영은 **내일(09-23) 아침부터**다. 오늘로 이 결정이 미뤄진 지 여러 거래일째다 — 계속 미룰 것인지도 하나의 결정이다.
6. **`2026-08-26-p1-2-1` id 확인** — 폐기 확정 처리됨(증거 §8-1), 추가 조치 불필요할 수 있으나 한 번은 확인 권고.
7. **ZN/ES 해외선물 거래소 권한 신청** — 증권사 계좌 설정(코드 밖, 만성, 1-3).
8. **`.gitignore:52` 정책 결정** — `_지표.md`·`_증거_*.md`·`_지연창.tsv`를 계속 걸러낼지.
9. **ADVISORY 모드 유지 여부 재확인** — 오늘도 이 설정 때문에 진입 판단 310건이 전부 차단됐다(§16-4). 의도된 상태가 맞는지 한 번은 확인 권고 — 실거래 전환 시점을 정하는 문제와 직결된다(`2026-08-19-fix2` 검증이 영원히 안 되는 이유이기도 하다, 3-2 참고).
10. **ruff 린트 도구 설치 여부 결정** — 여러 회차째 미설치.
11. **`docs/Dev_md/dashboard_build_prompt_1.md` 커밋 또는 `.gitignore` 처리** — NEXT_TODO에 미결정으로 남아 있음(증거 §8-2).

---

## 제6부 — 장후 자동조치 구현 결과 (16:35 · 자동)

> 예약 작업 `mahdi-postmarket-autofix` 회차(16:23 기동). 앞 회차 본문은 **한 글자도
> 고치지 않았다** — 정정이 필요한 것은 이 절 안에서만 적는다.
> ⛔ **Slack 토글은 켜지 않았다.** 착수 전 `NEXT_TODO.md`의 「폐기·종결된 안건」(3132행)과
> 「⚠ 다시 올리지 말 것」(1891행)을 통째로 읽고 금지 목록을 손에 쥐고 시작했다 —
> Slack 토글 · GEX 광폭 체인 · 예산 규칙 (B) 개정 · 폴러별 전용 페이서 · httpx INFO 축소 ·
> `handle_message` DB 가드 · 「9시 편입」 종결. **오늘 리포트는 그중 하나도 올리지 않았다**
> (폐기 목록 위반으로 기각한 항목 **0건**).

### 6-0. 한 장 요약

| | 건수 | 무엇 |
|---|---|---|
| 구현 완료 | **2** | 제4부 P2-1 · 제5부 고도화 2 |
| 가설 상태 확정 | **1** | `2026-09-21-p1-1` → `confirmed` |
| 예측치 선등재 | **2** | 코드보다 먼저 적었다 |
| 🔴 이미 반영돼 있어 **안 한 것** | **1** | 제4부 **P2-2** — 함정 게이트가 걸러낸 **아홉 번째** |
| C등급(승인 대기) | **3** | 제4부 P2-3 · 제5부 고도화 1 · P2-2의 잔여분 |
| 폐기 목록 위반 기각 | **0** | |
| 테스트 | **2,398 passed / 0 failed** | 신규 15건 · 기존 무수정 |

---

### 6-1. 🔴 P2-2는 **이미 구현돼 있다** — 그리고 이 리포트가 그 증거를 인용하고 있었다

**안 했다.** 함정 게이트(스킬 §4)에서 걸렸다.

P2-2가 요구한 것은 *"「옵션체인 이번 분 전멸」이 연속 3분 이상 이어지면, **Slack 토글 상태와
무관하게** 최소 한 줄을 남긴다"*였다. 그 기능은 **2026-08-26 P1-5로 이미 들어와 있다.**

- `mahdi/main.py:3092~3100` — 결손이 `OPTION_CHAIN_GAP_ALERT_SECONDS`(**300초**)를 넘으면
  `notify.notify("… {N}분째 결손 중 …", "WARNING")`을 부른다.
- `mahdi/notify.py:49·122·157` — 토글이 꺼져 있으면 **조용히 버리지 않고**
  `LOG_ALERT_SUPPRESSED_TOGGLE_OFF`("알림 스킵(토글 꺼짐) — level=%s · %s")를 남긴다.
  level별 억제 창까지 이미 붙어 있다.
- `mahdi/ops/log_metrics.py:503` — 그 문구를 `alert_suppressed_toggle_off`로 **센다.**
- **오늘 사이드카에 그 값이 실려 있다**: `qualitative.alert_suppressed_toggle_off = **2**`.

그리고 **이 리포트 자신이 §2-3(340행)에서 그 줄을 그대로 인용했다** —
`13:38:32 "알림 스킵(토글 꺼짐) — 옵션 체인 데이터가 5분째 결손 중"`.

그래서 P2-2의 근거 문장 *"지금은 이 회차처럼 사람이 직접 로그를 읽어야만 그 5분을 알 수
있다"*는 **사실이 아니다.** 기계가 세고 있었고, 그 수는 오늘 산출물에 남아 있다.

**남는 차이는 임계 하나뿐이다 — 300초(5분)를 180초(3분)로 내리는 것.**
⛔ **그것은 C등급이라 안 했다.** 숫자를 보고 임계를 움직이는 것은 이 체계가 가장 경계하는
형태이고(§2-B), 리포트가 그 3분에 **예측치를 붙이지 않았다**(장중② 378행이 스스로
*"예측치 미등재 — 아이디어 단계"*라고 적었다). 바꿀 것은 임계가 아니라 「무엇을 재는가」인데,
재는 것은 이미 다 있다.

- **왜**: 2026-08-26 14:30 P0-1이 정확히 같은 실수를 했다 — 없는 파일(`mahdi/ops/alerting.py`)을
  지목하고, 이미 여덟 번 돌고 있던 기능을 새로 만들자고 했다. 게이트는 그때 생겼다.
- **걸린 시간**: 약 12분(grep 2회 + 사이드카 키 확인 1회).
- **안 하면**: 이미 있는 경로 옆에 **두 번째 결손 경보**가 생긴다. 같은 사건이 두 축으로
  세어지고, `alert_suppressed_toggle_off`가 이유 없이 두 배가 된다.

---

### 6-2. 제4부 P2-1 — 「오늘 정하면 언제 반영되는가」를 산출물이 센다 (구현 완료)

**커밋 `b585fb0`(축·달력) · `b1fef95`(문서)**

발동 창은 **07:30 기동 전**이고 장전 회차는 **08:00**에 돈다 — 구조적으로 못 맞춘다.
조건 성립 판정이 서는 시각은 장후이므로, 어느 회차가 정해도 반영은 **언제나 다음 거래일**이다.
「오늘 아침이 발동 창이다」는 **어제 정해 둔 것을 누르는 창**이지 오늘 정하는 창이 아니었고,
그 둘을 섞은 것이 「네 번 성립, 네 번 다 미발동」의 구조다. §3-2가
`2026-08-26-p1-3`을 두고 *"그 Fix가 들어가면 이 가설은 폐기하고 새 가설로 대체하는 편이
맞다"*고 적은 그 Fix가 이것이다.

| 무엇 | 어디 |
|---|---|
| `next_trading_day()` — `previous_trading_day()`의 거울상 | `mahdi/market_calendar.py` |
| `preemptive_decision` 절(`effective_date` · `effective_in_calendar_days`) | `scripts/daily_ops_report.py` |
| 구조적 미스매치 + 반영일 읽는 법 | `docs/동작점검/tools/phases.md` A-2 |
| 「다음 거래일은 내일이 아닐 수 있다」 | `collect_evidence.py` §8-4 |

#### 🔴 정정 — 리포트의 「항상 D+1」은 틀렸다. 이번 주가 반례다

제4부 P2-1은 *"어느 요일이든 결정이 실제로 반영되는 날짜(**항상 D+1**)"*라고 적었다.
**2026-09-23(수)에 내린 결정의 반영일은 09-24가 아니라 `2026-09-28`이다** —
09-24·25가 추석 휴장(`mahdi/config/market_holidays.yaml` 84·86행)이고 26·27이 주말이다.
달력을 안 보고 하루를 더하면 **2026-08-18에 깨진 그 가정(평일 = 거래일)**이 그대로 재발한다.
그날 §1 델타 넷이 전부 «거래일 vs 휴장일» 비교라 붉은 ⚠ 넷이 거짓이었다.
회귀 테스트가 그 날짜를 박아 둔다.

#### ⚠ 계획을 다시 세운 것 — 리포트가 지목한 파일에는 넣을 수 없었다

리포트는 이 계산을 `collect_evidence.py` §8-4에 넣으라고 했다. 그 파일은 **stdlib 전용**
(파일 헤더)이라 휴장일 달력을 못 읽고, **1168행 주석이 이미 금지해 두었다** —
*"달력을 여기 다시 적으면 두 곳이 조용히 갈라진다(규약 A)"*.
그래서 계산은 달력을 이미 쓰는 `daily_ops_report.py`에 두고, §8-4에는 **날짜 대신**
「다음 거래일은 내일이 아닐 수 있다」와 사이드카 경로만 넣었다.

- **왜**: 이 결정이 **24거래일째** 미결인데, 미루는 대가(며칠을 더 잃는가)가 어디에도
  안 남아 매번 사람이 달력으로 암산했다.
- **걸린 시간**: 약 35분(달력 API 확인 · 함수 · 축 · 문서 · 테스트 9건).
- **안 하면**: 연휴 앞에서 하루로 보이는 유예가 실제로는 **닷새**다. 09-23이 정확히 그날이다.

---

### 6-3. 제5부 고도화 2 — 「비영 0이 74분」이 **몇 시였는지** 증거가 답한다 (구현 완료)

**커밋 `b1fef95`**

§2-4가 *"이 연속 구간이 정확히 몇 시인지는 이 회차 증거에 시각별 세부가 없어 **특정 못 함**"*
으로 닫은 자리다. `nonzero_member_runs()` + §5-2 구간 표(시작·종료·지속·비영)를 넣었다.

**오늘 로그를 그대로 먹여 본 결과 그 질문이 바로 닫혔다:**

| 비영 구간 시작 | 종료 | 지속 | 비영 |
|---|---|---|---|
| 07:31:55 | 08:46:55 | **75분** | **0/6** |
| 08:46:55 | 09:00:55 | 14분 | 2/6 |
| … (전 19구간) | | | |
| 15:36:54 | 15:45:01 | 9분 | 1/6 |

**그 구간은 07:31~08:46 — 전부 장전이다.** 스팟 미적재·개장 전이라는 **구조적 사유**이지
신호 고갈이 아니다. 값만 보면 「전 축이 74분간 중립」이라 사고처럼 읽히지만, 시각을 보면
정상이다. ⚠ 리포트의 **74분**과 이 표의 **75분**은 구간 경계 계산 방식 차이다 —
**맞추지 않고 그대로 뒀다.**

⚠ **「종료」는 관측이 아니다.** 「판단 형태 전이」 줄은 형태가 바뀔 때만 찍히므로 한 구간의
끝은 **다음 전이 줄의 시각**이고, 마지막 구간은 종료가 없다. 그 사실을 표 아래에 함께
인쇄한다 — 안 적으면 다음 회차가 이 표를 관측으로 오독한다(규약 C).
⛔ **공유 축에 얹지 않았다**(규약 E) — `member_nonzero`에는 `min`/`max`/`len`이 걸려 있어
전용 축 `member_nonzero_at`을 따로 만들었다. 옛 로그(Fix#6 이전)는 비영 값이 없어 표가
**안 실린다** — 「비영 0이었다」가 아니라 **「안 셌다」**이고 그 문구도 그대로다.

- **왜**: 같은 질문이 다음에 또 나오면 또 로그를 통째로 훑어야 했다. 로그는 이틀치만 남는다.
- **걸린 시간**: 약 25분(파서 축 · 렌더러 · 테스트 6건 · 실물 재생 2회).
- **안 하면**: 「비영 0 N분」이 매일 산문에 남지만 **매번 원인 불명**으로 남는다.

---

### 6-4. 가설 — 확정 1건, 손대지 않은 것 4건

**커밋 `b585fb0`** · 항목 **192 → 194** · pending **5 → 6** · confirmed **124 → 125**

✅ `2026-09-21-p1-1-crash-marker-survives-the-caret-c` → **`confirmed`**
(§3-2가 **판정 제안과 근거를 함께** 낸 유일한 건이라 스킬 §3 예외에 해당한다).
세 예측 전부 일치 — `crash.marker_present=True`(오늘 132행이 `^C` 없이 정상) ·
`crash.starts=1` · `crash.unattributed=3`(밴드 `3~3`, 엿새째 3으로 구간 경계 무변화).

⚠ **오늘 로그에는 `^C`가 없다** — 하루만으로는 「고쳐서 맞은 것」과 「원래 안 깨지는 날이라
맞은 것」이 안 갈린다. 가르는 힘은 09-21 등재 시점에 이미 한 **09-21 로그 재생**에 있고,
오늘 축은 회귀 확인이다. 등재 시점 `주의`가 *"둘을 바꿔 읽지 말 것"*으로 미리 적어 둔 그대로다.

⛔ **나머지 4건은 손대지 않았다**:
`2026-08-12-fix7`·`2026-09-15-eE`는 제안이 **「pending 유지」**였고,
`2026-08-19-fix2`·`2026-08-26-p1-3`은 **`inconclusive` 전환 「검토」** — 규약이 자동 확정에서
**명시적으로 제외**하는 형태다. 넷 다 사용자 몫으로 남는다.

**예측치 2건 선등재**(`2026-09-22-p2-1-…` · `2026-09-22-e2-…`), 검증예정일 **2026-09-23**.
⚠ **등재를 축 신설과 한 커밋에 묶었다** — 잎 린트(08-19 Fix#1)가 「자동 집계에 없는 절에서
시작하는 지표 경로」를 막아서, 축이 없는 채로 가설만 커밋하면 그 커밋이 깨진다.
**예측치 본문은 숫자를 보기 전에 이미 디스크에 적혔다**(스킬 §3의 실질은 지켰다).
⚠ 두 항목 다 리포트에 「예측치」 항목이 **없어** 「변경」·「회귀 위험」 줄에서 옮겼고,
그 사실을 각 `주의`에 적었다(09-17 fixc·09-21 p1-1과 같은 처리).

---

### 6-5. C등급으로 미룬 것 — 3건

| 항목 | 사유 |
|---|---|
| **제4부 P2-3**(`검증예정일`을 사건 기반으로) | **리포트 자신이 「사람 승인 필요 — 착수 전 결정」**이라 적었다. 그리고 §3-2가 지적했듯 `2026-08-19-fix2`의 검증은 **ADVISORY 모드가 풀려야** 가능하다 — 사용자 조치 9번과 한 몸이다 |
| **제5부 고도화 1**(지연 위험선 → COCKPIT 실시간 배지) | **예측치가 없다.** 「임계(0.8)를 넘는 순간을 노출한다」는 **선행 경보 승격**이고, NEXT_TODO §8-3에 이미 열려 있는 결정이다. 리포트 자신이 *"오늘 사례가 그 제안의 **세 번째 근거**"*라 적었다 — 근거를 쌓는 중이지 착수 단계가 아니다 |
| **P2-2의 잔여분**(300초 → 180초) | **임계를 숫자 보고 바꾸는 것**(§2-B) + 예측치 없음. 위 6-1 |

⛔ **`### 결정이 필요한 것` 절(5~11번)에는 한 글자도 손대지 않았다.**

---

### 6-6. 테스트

**2,398 passed / 0 failed** (직전 기준선 2,383 → **신규 15건**, 기존 **무수정**).
전체 스위트 1회 + 관련 스위트 개별 3회.

| 신규 | 어디 | 그중 판정 무변경 축 |
|---|---|---|
| 6건 | `tests/test_market_calendar.py` — 추석 주 `09-23 → 09-28`을 박는다 | — |
| 3건 | `tests/test_scripts_daily_ops_report.py` — 「모름」을 「내일」로 안 접는다 | — |
| 6건 | `tests/test_evidence_collector_members.py` | **2건** |

**판정 무변경 축 2건**: ① 공유 축 `member_nonzero`가 한 글자도 안 바뀐다
② 옛 문구(Fix#6 이전)는 새 축도 **비어 있다**(「안 셌다」를 「0이었다」로 안 접는다).

`hypotheses.yaml` 무결성: **194항목** 파싱 · `test_ops_hypotheses.py` **69건 통과** · CRLF 0건.
⚠ ruff는 `.venv`에 미설치라 못 돌렸다 — **열네 회차 연속**.

---

### 6-7. 커밋 · 사용자 몫

| 해시 | 무엇 |
|---|---|
| `a756529` | 당일 점검 산출물(09-22 보고서 + `_지표.json`) |
| `b585fb0` | 가설 1건 확정 + 예측치 2건 선등재 + P2-1 반영일 축 |
| `b1fef95` | P2-1 명문화(phases.md · §8-4) + 고도화 2 |

**사용자 몫으로 남은 것**(자동조치가 손대지 않았다):

1. 🔴 **사전 대응 규칙 (A)/(B) 결정 — 25거래일째**(사용자 조치 5번). 발동 창은
   **다음 거래일 장전(07:30 전)**뿐이고 ⛔ 「내일 아침」 버킷은 자동조치가 손대지 않는다
   (등재 창 · 소급 금지). ⚠ **오늘부터 반영일이 산출물에 찍힌다** — 09-23에 안 정하면
   다음 기회는 **09-28**이다(추석). 하루가 아니라 **닷새**다.
2. **`hypotheses.yaml` 나머지 4건 확정** — fix7 · fix2 · p1-3(**26일 지남**) · eE.
   특히 `2026-08-19-fix2`는 ADVISORY가 풀려야 검증된다(9번과 한 몸).
3. **ADVISORY 모드 유지 여부 재확인**(9번) — 오늘도 진입 판단 **310건 전건 차단**.
   설계된 설정이지 오류가 아니다. 의도한 상태인지 한 번은 확인 권고.
4. **`_to_delete/backup_20260920/` · `Claude outputs/` 처분**(2·4번) — 손대지 않았다.
5. **`.claude/` `.gitignore` 편입 — 27일째** 미결정(3번). ⛔ 미결정 사항이라 손대지 않는다.
6. **`.gitignore:52` 정책**(8번) — `_지표.md`·`_증거_*.md`·`_지연창.tsv`가 계속 걸러진다.
   **09-08 제기, 열나흘째.** 오늘 만든 §5-2 구간 표도 그래서 저장소에 안 남는다.
7. **`docs/Dev_md/dashboard_build_prompt_1.md`**(11번) · **`2026-08-26-p1-2-1` id 확인**(6번) ·
   **ZN/ES 거래소 권한**(7번) · **ruff 설치**(10번).
8. ⚠ **이 예약 작업의 스킬 §1-2 완결 판정** — 오늘도 「이 파일은 여기서 닫힌다」류 표기가
   **없다**(09-09부터 템플릿에서 빠졌다, **열흘째**). 오늘은 「사용자 조치」 절이 스스로
   *"이 절이 오늘 최종본이다"*라고 적어 그것을 완결 표기로 읽었다. **상시 예외는 금지** —
   템플릿을 고치든 스킬을 고치든 사람이 정할 것.

> **이 절은 여기서 닫힌다.**
