# 마흐디 일일점검 — 2026-09-08 (화)

- 국면: **장전(pre)** · 실행: 예정 08:00 → 실제 08:01:11 KST — **정시 실행**(1분 이내, 밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-08_증거_pre.md`
- HEAD: `05dfead`(브랜치 `master`, 미커밋 25건, 당일 커밋 0건)
- 직전 거래일: 2026-09-07(월)

---

## §0. 오늘 한 장

**오늘 프로그램은 정상적으로 켜졌다.** 07:30에 자동 시작 배치가 정시에 돌았고, 데이터베이스·화면·
실시간 시세 연결까지 08:01 기준 전부 정상이다. 에러(심각한 오류)는 0건이었고, 경고도 평소와 같은
수준(4건 — 전부 만성이거나 2초 만에 스스로 복구된 것)이었다.

**사람이 지금(개장 전) 해야 할 일이 있다.** 어제 저녁(09-07 15:45) 자동 분석이 "옵션 시세를
받아오는 응답이 느려지는 시간대가 이틀 연속(09-04, 09-07) 같은 시간대에 나타났다"는 것을
확인했고, 이번엔 **오전 9시대도 그 목록에 새로 들어갔다.** 미리 정해 둔 규칙대로면 **오늘 아침이
이 조건에 대한 조치(그 시간대 시세 조회 주기를 늘릴지)를 결정할 차례**다. 이 결정은 프로그램을
장중에는 내릴 수 없고(재시작이 필요해서), 이미 한 번(지난 금요일과 오늘 사이) 결정 없이
지나갔다. 자세한 선택지는 이 파일 맨 끝 「사용자 조치」 2번을 볼 것.

**문제 건수**: 오늘 새로 찾은 것 없음(0건). 전부 이전부터 이어져 온 것 6건(심각 0 / 보통 3 / 경미 3)이고,
그중 1건(위 사전 대응 규칙)은 오늘 상황이 더 급해졌다.

**개장 전 사람 조치: 결정 1건 있음(위 참조)** (관측 범위 07:30~08:01 KST). 이 범위 밖은 모른다.

**[12:30 갱신 — 장중① 점검 결과]** 낮 12시 반 기준으로도 프로그램은 계속 정상 작동 중이다(중단
없음, 오류 0건). 다만 아침에 정하기로 했던 일(위 2번 항목 — 옵션 시세를 조회하는 주기를 늦출지
말지)은 09시가 지나도록 결정이 안 내려졌다. 이 결정은 프로그램을 다시 시작해야만 반영되는데,
장이 열린 지금은 다시 시작할 수 없어서 **오늘은 이 결정을 더 낼 수 없게 됐다** — 지난주 금요일,
그제에 이어 **세 번째로 그냥 지나간 것**이다. 그 밖에 새로 생긴 문제는 없다(신규 0건 — 이전부터
있던 6건이 계속 이어지는 중이고, 그중 방금 말한 결정 건은 이번에 "안 냈다"로 결론이 났다).
**사람이 지금 당장 해야 할 일은 없다.**

**[14:30 갱신 — 장중② 점검 결과]** 오후 2시 반이 지난 지금도 프로그램은 계속 정상
작동 중이다(중단 없음, 오류 0건). 아침에 정하기로 했던 옵션 시세 조회 주기 건은 이제
"오늘 안에는 절대 못 낸다"로 완전히 굳었다(장중엔 재시작이 있어야 반영되는데 이미
한참 지났다) — 이걸로 같은 결정이 세 번째로 그냥 지나간 것이 최종 확정됐다. 새로
찾은 것은, 시세 조회 응답이 오래 걸리는 정도가 오늘 처음으로 **25분 동안(오후 2시
11분~2시 31분) 계속 "위험한 수준"**(요청 중 상위 5%가 제한시간 안에 못 들어오는
정도)에 머물렀다는 점이다 — 지금까지는 이런 순간이 짧게 한 번씩 스쳐 지나갔는데,
오늘은 처음으로 길게 이어졌다. 다만 이 시간에도 시세를 아예 못 받아온 적(데이터
완전 유실)은 없었다. **오후 2시 50분(오늘 신규 진입을 받는 마지막 시각) 전에 사람이
할 일은 없다** — 결정할 수 있는 시간도, 코드를 바꿀 수 있는 시간도 이미 지나서
지금은 지켜보는 것 외에 할 수 있는 일이 없다. 새로 찾은 문제는 1건(위 지연 심화,
심각도 보통)이고 나머지는 전부 오전부터 이어지는 것들이다.

**[장후 갱신 — 오늘 하루 최종 결과]** 오후 2시 반의 걱정이 실제로 벌어졌다. 오후 3시
1분부터 28분 동안, 프로그램이 옵션 시장 시세를 아예 못 받아오는 상태(오늘까지 다섯 번째로
나타난 이런 종류의 사건, 이 프로그램은 이것을 "절벽"이라 부른다)가 있었다. 다행히 3시
30분경 저절로 풀렸고, 이 프로그램은 아직 실제 주문을 내는 기능이 연결돼 있지 않아서
(관측 전용 모드) 이 28분 동안 실제 돈이 오간 것은 없다. 오늘 아침부터 미뤄지던 결정(옵션
시세를 더 자주 확인할지)은 결국 세 번째로 그냥 지나갔고, 오늘 오후의 그 28분이 바로
그 미뤄진 결정의 실제 대가였다 — 다음 결정 기회는 내일 아침이고, 이번엔 반드시 정할
것을 사용자 조치에 못 박아 둔다. **하루 전체 결론**: 프로그램 자체(켜지고, 안 죽고,
꺼지는 것)는 오늘도 완벽했다. 문제는 전부 증권사 쪽 응답 속도와, 그로 인해 프로그램이
"오늘 무엇을 볼지" 하나였다. **사람이 지금 당장 해야 할 일은 없다** — 내일 아침의 결정
1건만 남아 있다(→ 사용자 조치 1번). 오늘 문제 건수는 최종 6건(심각 0 / 보통 3 / 경미 3, 2-1·3-1은 4-1과 같은 문제라
번호만 이어받고 따로 세지 않았다), 그중 신규 1건(오늘 오후 실제로 벌어진 절벽)이다.

---

## 제1부 — 장전 (07:30 ~ 09:00)

### 1-0. 전일(2026-09-07) 이월 처분 — 「사용자 몫으로 남은 것」 9건 전부 처분

| 09-07 항목 | 처분 | 근거 |
|---|---|---|
| 1. PC가 07:30에 꺼져 있었는지 확인(07:30 미기동의 원인) | 🔄 **지속 — 원인 여전히 미확인.** 다만 오늘은 07:30:00.97에 정시 기동해 **재발하지 않았다** | `logs/premarket_startup.log` 오늘 첫 줄 `[2026-09-08 7:30:00.97] ===== Mahdi 장전 기동 시작 =====`. 재발 안 함이 원인 규명은 아니다 |
| 2. 사전 대응 규칙(`2026-08-04-p5`) 발동 여부 — 내일(09-08) 아침 결정 | ⬆️ **격상 — 오늘이 바로 그 결정일이고, 조건이 더 넓어졌다.** 아래 1-1 | 09-07 `auto/2026-09-07_지표.md`:409 `🔔 연속 판정 성립: 15개 구간`(9시 포함) |
| 3. `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`에 09시 추가 여부 | 🔄 **지속 — 2번과 사실상 같은 결정으로 합류.** 아래 1-1 | 위 15구간 목록에 `9시 inquire-price` 포함 |
| 4. 이벤트 캘린더 `covered_through` 9월분 갱신 | 🔄 지속(만성, 11일째 정지) | 아래 1-2, 오늘도 같은 경고 재발 |
| 5. 확정 대기 가설 검토·확정 | 🔄 지속 | 아래 1-3. `hypotheses.yaml` 현재 `상태: pending` **11건** |
| 6. 레버 3건 무조건발동일 정리 | 🔄 지속(관리) | 아래 1-4 |
| 7. `.claude/`·`dashboard_build_prompt_1.md` 커밋 또는 `.gitignore` 등록 여부 | 🔄 지속(만성, 15일째) | 아래 1-5, git 상태 09-07과 완전히 동일(파일 목록 25건 1자도 안 바뀜) |
| 8. `MEMBER_DROPOUT_ALERT_MIN` 재검토 · ZN/ES 거래소 신청 · 매매 실행 경로 배선 시점 | 🔄 지속(만성) | 오늘 새로 다루지 않음, 변화 없음 |
| 9. `DECISION_LOG.md` 09-03 절 위치 정리 여부 | 🔄 지속(경미, 사람 몫) | 오늘 확인 안 함(우선순위 낮음) |

전일 보고서가 남긴 항목은 위 표로 전부 처분했다 — 조용히 사라진 항목 없음.

### 장전 체크리스트 (phases.md A절)

**A-1 기동** — ✅ 전부 정상, 재기동 0회:
- 기동 배치: `[2026-09-08 7:30:00.97]` 정시 시작(어제와 달리 지연 없음)
- Docker: 07:30:06~07:30:21 데몬 준비 완료, 마이그레이션 37개 전부 성공, 실패 없음
- 관측 루프: `직전 정상 기동` 표식 **1회**(07:31:00, "2026-09-07 08:01:45 대비 23.5시간 전") — 오늘 재기동 없음
- `.last_successful_start.txt`: 2026-09-08T07:31:00.995378(오늘 시각), `.last_cockpit_start.txt`: 07:31:09
- 워치독: 당일 3행(OK 3 / 비-OK 0) — 정상. 호출 127회(idle 105 · ok 22), 호출 간격 5분 이상 공백 0건,
  기록 간격 25분 이상 공백 0건. 09-07 09-07-fix-a로 새로 생긴 「호출 흔적」 축(`.watchdog_check_trail.tsv`)이
  오늘도 정상 채워지고 있는 것으로 보인다(관측 항목 2번 참조)

**A-2 오늘의 계획**
- 검증예정일 도래·경과 pending 가설 **10건**(증거 §8) — 그중 2건(`2026-09-07-fix-a-watchdog-invocation-gap`,
  `2026-09-07-fix-c-monthly-total-failure-count`)은 **오늘이 검증예정일**, 나머지 8건은 4~12일 지남 — 아래 1-3
- `NEXT_TODO.md`에 오늘 "켤 것"으로 지정된 레버 없음
- 전일 커밋 실림 여부: 당일 커밋 0건 — 직전 커밋(`05dfead`, 09-07 장후 자동조치)은 이미 09-07 운영에
  실린 것들이라 오늘 기동에 자동으로 실려 있음(같은 코드베이스)
- **사전 대응 규칙 조건**: 09-07 지표에 `🔔 연속 판정 성립: 15개 구간`(이틀 연속, 9시 신규 포함) —
  **오늘 아침이 발동 창이다.** 아래 1-1(⚠ 결정 필요)
- `--verify-report`로 09-07 보고서 통과: 폐기 목록과 부딪히는 줄 없음(✅). 경로 미확인 2건
  (`mahdi/ops/alerting.py` · `tools/collect_evidence.py`)은 09-01부터 반복되는 **알려진 오탐 패턴**
  (알림 모듈의 실제 이름은 `mahdi/notify.py`, 상대경로 표기 차이) — 새 이상점 아님
- 규약 H 점검 — `REGIME_RESTORE_SESSION_WINDOW`가 여전히 **꺼짐(False)** 이므로, 그 레버가 전제인
  `2026-08-12-fix7-regime-session-window-restore` 가설은 오늘도 판정 불가(레버가 꺼진 채로 간다)

**A-3 전일 이월**
- 전일(09-07) `auto/2026-09-07_지표.json` 존재 확인(09-07 15:46 기록) — 전일 델타 정상 확보 가능
- 만성 항목(이벤트 캘린더 만료, ZN/ES 권한 결손) 오늘도 그대로 — 만성 유지, 새 발견 아님

---

### 1-1. ⬆️ 격상(09-07 §1-2·1-7 후속, 오늘이 결정일) — 사전 대응 규칙이 이틀 연속 성립, 09시도 새로 포함됐다 ⚠

**증상.** 옵션 시세를 조회할 때 응답이 느려지는 시간대를 매일 감시하는데, "느림"의 기준(초당
2.5초를 넘는 응답이 상위 5%에서 나옴)에 걸린 시간대가 **09-04(금)와 09-07(월) 이틀 연속** 같은
시간대들에서 나타났다. 이번엔 목록에 **오전 9시대**(장 시작 직후)도 처음 들어갔다.

**근거.** `docs/동작점검/auto/2026-09-07_지표.md`:409
```
🔔 연속 판정 성립: 15개 구간 — 10시 `inquire-balance`, 10시 `inquire-price`, 11시 `inquire-balance`,
11시 `inquire-price`, 12시 `inquire-balance`, 12시 `inquire-price`, 13시 `inquire-asking-price`,
13시 `inquire-balance`, 13시 `inquire-price`, 14시 `inquire-asking-price`, 14시 `inquire-balance`,
14시 `inquire-price`, 15시 `inquire-balance`, 15시 `inquire-price`, 9시 `inquire-price`
(오늘 + 2026-09-04 모두 2.5초 초과)
```
10~14시는 이미 `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`(혼잡 시간대 완화 레버)가 켜져 있어
대응 중이다(오늘 증거 §7). **09시는 그 레버에 아직 없다.**

**기준.** `phases.md` A-2: "전 거래일 지표·증거에 `🔔 사전 대응 규칙 조건 성립`이 있으면 오늘
아침이 발동 창이다." 규칙 원문(`hypotheses.yaml` 2026-08-04-p5): "p95(응답시간 상위 5% 지점)가
2.5초를 넘는 시간대가 이틀 연속 같은 시간대에 나타나면, 그 시간대에 한해 위클리(주간물) 옵션
조회 주기를 2분 → 4분으로 늘린다(먼슬리(월물)는 건드리지 않는다 — 판단이 보는 핵심 입력이라서)."
발동은 **사람이 결정**하고 **장중에는 못 한다**(재기동이 필요해서다) — `phases.md` A-2 세부 항목.

**영향(평문).** 이 결정은 오늘 아침에만 내릴 수 있고, 이미 지난 금요일에 한 번 결정 없이
지나갔다. 오늘도 그냥 지나가면 다음 기회는 내일 아침이고, 그 사이 낮 시간대(9~15시)에 시세를
받아오는 속도가 계속 빠듯한 채로 간다 — 지금까지는 데이터가 통째로 빈 적은 없었지만, 여유가
줄어들수록 그럴 위험은 커진다. 반대로 지금 조치를 켜려면 프로그램을 다시 시작해야 하고, 그러면
15~30분 정도 "판단 준비 중" 상태(레짐 웜업)가 생긴다.

**결정 선택지** (→ 사용자 조치 2번, 오늘 09:00 전에만 낼 수 있다):
- **(A) 지금 재기동해서 09시를 혼잡 시간대에 추가한다.** 얻는 것: 오전장 시세 수집 여유 확보,
  절벽(시세 통째 유실) 위험 감소. 잃는 것: 재기동에 따른 웜업 구간(과거 사례로는 재기동 시각과
  무관하게 09:15경 끝났다 — 지금(08:01~08:05경) 재기동해도 개장 전에 끝날 가능성이 있으나
  표본이 2건뿐이라 확실하지 않다), 그 시간대 위클리 관측 커버리지 일부 손실.
- **(B) 오늘도 유예하고 계속 관찰한다.** 얻는 것: 재기동 없이 오늘 관측 연속성 유지. 잃는 것:
  같은 결정이 세 번째로 미뤄지고, 9시대 지연 위험은 그대로 남는다.
- 장중에는 (A)를 실행할 수 없다 — 지금(개장 전)이 유일한 창이다.

---

### 1-2. 🔄 지속(만성) — 이벤트 캘린더 만료, 오늘도 재발

**증상.** `mahdi/config/event_calendar.yaml`의 이벤트 반영 기준일(`covered_through`)이
2026-08-28에 멈춰 있고, 오늘(09-08)까지 **11일** 지났다.

**근거.** `logs/observation_loop.log` 07:31:55
```
[WARNING] 이벤트 캘린더 미기입: not_covered — `mahdi/config/event_calendar.yaml`의
covered_through(2026-08-28)가 오늘(2026-09-08)보다 앞선다. 이벤트 근접 페널티가 걸리지 않는 상태다
```

**기준.** `phases.md` D: 만성 항목은 만성으로 쓰고 새 발견으로 올리지 않는다. 09-03부터 매일 재발.

**영향(평문).** 오늘 하루, 금리 발표 같은 예정된 이벤트가 있어도 프로그램이 그 이벤트를 몰라서
평소처럼 베팅 크기를 줄이지 않는다. 이벤트가 없는 날이면 이 경고는 사실상 영향이 없다.

---

### 1-3. 🔄 지속·심화 — 손 판정 대기 가설이 계속 쌓이고 있다

**증상.** `hypotheses.yaml`의 `상태: pending`이 **11건**이고, 그중 검증예정일이 도래했거나 지난
것이 **10건**이다. 파일 자체는 09-07 보고서 이후 한 줄도 안 바뀌었다.

**근거.** 오늘 증거 §8(10건 표) · §12-4 "`hypotheses.yaml`이 2026-09-07 보고서 이후 한 줄도
안 바뀌었다" · 직접 확인 `grep -c "상태: pending"` → 11.

| id | 검증예정일 | 지남 | 전제레버 상태 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | 09-01 | 7일 | **꺼짐** — 규약 H로 오늘도 판정 불가 |
| `2026-08-12-eE-on-congested-hours` | 09-02 | 6일 | 켜짐 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 09-01 | 7일 | 레버 없음 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 08-27 | 12일 | 레버 없음(1-1과 연동) |
| `2026-09-02-fix10-phase-offset-55s` | 09-04 | 4일 | 켜짐 |
| `2026-09-03-p2-3-watchdog-cites-recovery-history` | 09-04 | 4일 | 레버 없음 |
| `2026-09-03-p1-b-dropout-cross-checks-the-cycle-log` | 09-04 | 4일 | 레버 없음 |
| `2026-09-03-adv3-cliff-day-flags-the-hypothesis-table` | 09-04 | 4일 | 레버 없음 |
| `2026-09-04-p2-b-isolated-zero-row-count` | 09-05 | 3일 | 레버 없음 |
| `2026-09-07-fix-a-watchdog-invocation-gap` | **09-08(오늘)** | 도래 | 레버 없음(계측 신설) |
| `2026-09-07-fix-c-monthly-total-failure-count` | **09-08(오늘)** | 도래 | 레버 없음(계측 신설) |

**기준.** `phases.md` A-2 "예정일이 지난 pending 항목 — 손 판정이 밀리고 있다는 뜻이다."
소급 적용 금지(같은 문서 C-3) — 예정일 지난 항목에 새 예측을 붙이지 않는다.

**영향(평문).** 판정을 기다리는 가설이 계속 늘고 있어서, 언젠가 한꺼번에 몰아서 봐야 할 양이
쌓이고 있다. 특히 `fix7-regime-session-window-restore`는 그 전제 레버가 계속 꺼져 있어서
**오늘 어떤 숫자가 나와도 그 가설의 답이 될 수 없다** — 판정 창구 자체가 막혀 있다.

---

### 1-4. 🔄 지속(관리) — 무조건발동일 지난 레버 3건, 날짜 정리만 필요

**증상.** 세 레버 모두 **이미 켜져 있는데**, 코드에 적힌 "무조건 켜야 하는 기한"만 지난 채
안 지워졌다.

**근거.** 오늘 증거 §7:
- `use_effective_member_count: true`(켜짐) — 기한 2026-08-23, **16일 지남**
- `SIGNAL_FUSION_PHASE_OFFSET_SECONDS = 55.0`(켜짐) — 기한 2026-09-02, **6일 지남**
- `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`(10~14시, 켜짐) — 기한 2026-08-31, **8일 지남**

**기준.** README.md "무조건발동일이 비어 있으면 그 레버의 유예는 영원히 조용히 성립한다."
이 셋은 유예가 아니라 **이미 발동 완료**된 것이라 성격이 다르다 — 그냥 날짜 정리 대상이다.

**영향(평문).** 기능은 이미 정상 작동 중이라 지금 당장 문제는 없다. 다만 날짜가 방치되면
다음에 진짜로 유예 중인 레버와 헷갈릴 수 있다.

---

### 1-5. 🔄 지속(만성) — 미커밋 25건, `.claude/`·`dashboard_build_prompt_1.md` 포함

**증상.** 작업 폴더에 커밋 안 된 변경이 25건 있고, 09-07과 **한 글자도 다르지 않다**(같은 4개
수정 파일 + 21개 새 파일 목록). 오늘로 15일째다.

**근거.** `git --no-optional-locks status --porcelain` 오늘 출력 = 09-07 증거 §1의 출력과 완전히
동일(파일명·순서까지 일치).

**기준.** `phases.md` A-3 "만성 항목은 만성이라고 쓰고 새 발견으로 올리지 않는다."

**영향(평문).** 지금 당장 작동에는 지장이 없지만, 이 상태가 길어질수록 "왜 아직도 커밋을
안 했는지" 이유를 나중에 기억하기 어려워진다. 새로 늘어난 파일은 없다 — 정체된 상태 그대로다.

---

## 오늘 관측이 답해야 할 것 (장중·장후가 닫을 목록)

1. **1-1의 결정(A/B)이 오늘 개장 전 실제로 내려졌는가.** 내려졌다면 어느 쪽이고, (A)를
   골랐다면 재기동 웜업이 몇 시에 끝나 정상 판단으로 돌아왔는가.
2. **09-07 장후에 새로 만든 「워치독 호출 흔적」(`logs/.watchdog_check_trail.tsv`)이 오늘
   실제로 매분 쌓이고 있는가** — 오늘이 그 계측이 실전에서 처음 하루를 도는 날이다
   (`2026-09-07-fix-a-watchdog-invocation-gap` 가설의 검증예정일이 오늘).
3. **09-07 신설 「먼슬리 완전미회복」 축(`priority_retry.total_failure_cycles`)이 오늘
   장중에 실제 사건이 나면 값을 내는가** (`2026-09-07-fix-c-monthly-total-failure-count`
   가설의 검증예정일도 오늘).
4. 오늘 낮 시간대(특히 09시·10~14시) REST 응답 지연이 어제보다 나아졌는지 악화됐는지 —
   1-1 결정과 무관하게 실측으로 추적.
5. 08:00:01 체결통보 스트림 2초 단절이 오늘 더 반복되는가, 아니면 오늘 그 1건으로 끝나는가.

---

## Fix 구현계획 (초안 — 장후 적용, 이 회차는 코드 변경 없음)

오늘 새로 코드를 바꿔야 할 만큼 급한 것(P0)은 없다. 아래는 모두 **계획만** 세운다.

### Fix 후보 D (P2, 신규) — 사전 대응 규칙의 "결정 창을 놓쳤다"를 다음 장전이 스스로 알게 한다

**문제.** 1-1과 같은 유형의 결정이 09-04→09-07에 이미 한 번 지나갔고, 오늘이 두 번째다.
지금은 사람이 매번 전일 지표 파일을 열어 `🔔` 줄을 직접 찾아야 안다.

**방향.** `docs/동작점검/tools/collect_evidence.py`의 §8(오늘 판정해야 할 가설) 근처에,
전일 `_지표.md`에 `🔔 연속 판정 성립`이 있고 그 다음 거래일(오늘) 장전 보고서가 아직
없으면 "결정 미룬 지 N번째"를 세는 줄을 추가한다. 대상 파일: `collect_evidence.py`
(신규 절 또는 §8 확장), `docs/동작점검/tools/phases.md`(A-2에 카운터 항목 추가).
회귀 위험: 낮음(신규 집계). 검증: 09-04→09-07→09-08 세 번째 사례를 픽스처로 테스트 추가.

### Fix 후보 E (P2, 이월 제안 — 09-07 Fix 후보 B 그대로) — 레버 무조건발동일 자동 정리

이미 켜진 레버의 지난 무조건발동일을 "실제로 켠 날짜"로 갱신하는 것을 반자동화할지 검토.
우선순위 낮음, 다음 장후 세션에서 논의.

---

## 고도화 방안 (초안 — 당일 관측 근거)

1. **1-1이 보여주는 것**: 사전 대응 규칙의 "발동 시점이 사람이 결정할 수 있는 유일한 창(다음
   거래일 장전)"이라는 설계가, 그 창을 실제로 챙기는 것까지는 보장하지 않는다. 위 Fix 후보 D가
   그 간극을 메우자는 제안이다.
2. **09시가 새로 걸린 것**: 지금까지 혼잡 시간대 완화는 10~14시만 다뤘는데, 이번이 09시가
   걸린 첫 사례다. 만약 오늘 (A)를 고른다면, 개장 직후 시간대에 웜업과 완화 조치가 겹치는
   첫 사례가 되므로 그 결과를 다음 레버 설계에 참고할 만하다.

---

## dev_memory 반영 초안 (장후 세션이 실제로 반영)

- **NEXT_TODO 후보**: "[ ] 사전 대응 규칙(2026-08-04-p5) 발동 결정이 09-04·09-07에 이어
  09-08도 미뤄질 뻔했다(3번째) — 결정 미룸 카운터를 자동화할 것(Fix 후보 D, 신규)."
- **DECISION_LOG 후보**: 오늘 (A)/(B) 중 실제로 어느 쪽을 골랐는지는 장후 세션이 결과와
  함께 기록한다(이 회차는 결정 이전이라 기록할 결과가 없다).

---

## 제2부 — 장중 ① (12:30 회차, 실행 12:34 KST — 정시 실행)

증거 파일: `docs/동작점검/auto/2026-09-08_증거_intra.md`. 조사 범위 07:30~12:34 KST(관측
사이클 303분). HEAD `05dfead`(미커밋 26건 — 장전 25건에서 +1, 그 1건은 이 일일점검 파일
자신이라 실질적으로 그대로). 당일 커밋 0건, 재기동 0회(프로세스 기동 07:31:00 그 1회뿐).

### 2-0. 장전 이월 처분 — 5건 + 관측 항목 5건, 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 사전 대응 규칙 결정(A/B) | ↩️ **정정 — 결정 자체가 안 내려진 채 창이 닫혔다.** 09:00 이후 재기동 기록이 없다(프로세스 기동 07:31:00 1회뿐, `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`에 9시 키 없음 — `mahdi/main.py:275` 그대로). 사실상 (B) 유예이지만 **사람이 고른 (B)가 아니라 창이 그냥 닫힌 것**이다. 이틀 전(09-04→09-07)에 이어 **3번째 미룸**. → 아래 2-1 | 증거 §2·§7, `mahdi/main.py:275` |
| 1-2 이벤트 캘린더 만료 | 🔄 지속(만성, 12일째) | 07:31:55·08:31:55·…·12:32:54 매시 같은 경고 재발(증거 §5) |
| 1-3 pending 가설 적체 | 🔄 지속·불변 | `hypotheses.yaml` mtime 09-07 그대로, pending 11건 불변(도래 2건은 아래 2-2·2-3에서 진행 상황만 확인, 최종 판정은 장후) |
| 1-4 레버 3건 무조건발동일 정리 | 🔄 지속(관리, 변화 없음) | 증거 §7, 지난 지 16일·6일·8일로 그대로 하루씩 늘어남 |
| 1-5 미커밋 25→26건 | 🔄 지속(만성) — **내용은 그대로다** | 늘어난 1건은 이 일일점검 파일 자신(`?? docs/동작점검/2026-09-08_마흐디_일일점검.md`) — 새 이상점 아님. 그 외 25건은 09-07·09-08 장전과 파일명까지 동일 |
| (관측) 1. 1-1 결정 여부·(A) 골랐다면 웜업 종료 시각 | ✅ **판정 — 결정 안 내려짐, (A) 미선택, 웜업 해당 없음.** 위 1-1 처분 참조 | 증거 §2·§3 「프로세스 기동 1회」 |
| (관측) 2. 워치독 호출 흔적이 매분 쌓이는가 | ✅ **판정 — 정상 작동.** 당일 호출 400회(idle 105·ok 295), 호출 간격 5분 이상 공백 0건 — 새로 만든 계측(`2026-09-07-fix-a`)이 오늘 실전에서 처음 하루를 돌며 정상 채워지는 중이다 | 증거 §6-1 |
| (관측) 3. 먼슬리 완전미회복 축이 실제 사건에서 값을 내는가 | ✅ **판정 — 정상 작동.** 오늘 먼슬리 되살리기 실패 3건 중 **완전 미회복 1건**(10시), 부분 회복 2건(11·12시)으로 새 축(`2026-09-07-fix-c`)이 둘을 갈라서 냈다 | 증거 §5-1-1 |
| (관측) 4. 09시·10~14시 REST 지연이 어제보다 나아졌는가 | ✅ **판정 — 나아지지 않았다, 오히려 근소 악화.** 이틀 연속(어제와 같은 구간) 성립 수가 어제 7개 → 오늘 8개로 늘었고, 응답이 실제로 제한시간을 넘는 창(p95÷타임아웃 1.0배 이상)이 오늘 **4/60창** 나왔다(최악 11:01, 1.01배) — 어제는 이 축이 없어 직접 비교는 못 하지만, "위험선을 넘는 창이 나왔다" 자체가 09-07보다 한 단계 나빠진 것이다 → 아래 2-1 | 증거 §5-1 |
| (관측) 5. 08:00:01 체결통보 단절이 반복되는가 | ✅ **판정 — 반복되지만 새 이상점 아니다.** 08:00:01·09:00:01·10:00:01·11:00:01·12:00:01, **정확히 매시 정각 +1.7~1.9초**에 5회 발생, 5회 다 2초 만에 재연결 성공. `DECISION_LOG.md`:670에 이미 "매시 정각" 패턴으로 원인 규명·성격 확정된 **만성 현상**(2026-08-26·08-31 조사, KIS 서버 쪽 정기 재연결로 추정, 재연결 성공률 그동안 8/8)이다 — 신규로 올리면 §8-2가 경고하는 "폐기된 것을 되살리는" 실수가 된다 | `logs/observation_loop.log` 08:00:01~12:00:01(연속 1회·오늘 5회 표기), `DECISION_LOG.md:670,689` |

전일 이월 10건(이상점 5 + 관측 5) 전부 처분했다 — 조용히 사라진 항목 없음.

### 장중 체크리스트 (phases.md B절)

**B-1 생존** — ✅ 정상. 관측 루프 마지막 레코드 **12:34:36**(회차 실행 시각과 거의 동시).
07:30~15:45 구간 3분 이상 로그 공백 **0건**. 워치독 마지막 기록 12:30, 감시 창 끝(12:34)까지
4분 무기록 — 정상 주기(10분) 안. 워치독 호출 자체는 400회, 5분 이상 공백 0건(둘 다 작으므로
"불렸는데 남길 것이 없었던" 정상 상태). `observation_loop_crash.log` 최종 수정은 07:30 기동
표식뿐이고 **그 이후 트레이스백 없음** — 오늘 크래시 없음(mtime 함정 확인 완료).

**B-2 수집 파이프라인** — ERROR 0건. WARNING 122건(27종) 중 대부분은 예산초과(29)·체결 분류
폴백(34)·타임아웃(19) 등 만성 패턴. 예산 초과/연속 타임아웃 조기포기/실패예산 소진을
나눠 세면 각각 **29건 / 4건 / 0건** — "실패 예산 소진"(가장 심한 단계, 전멸)은 0건으로
계측이 살아 있는 채로(2026-08-26에 36줄 찍힌 전례) 진짜 0이다. WS 단절 0건(정상 — 체결통보
스트림 단절과는 다른 축, 그쪽은 위 관측 5번 참조). ATM 롤링 왕복 114회, 시간대별 고르게 분포
(급격한 쏠림 없음).

**B-3 판단까지 갔는가** — 앙상블 비영 멤버 최소 0·최대 3(분모 6). 6개 중 상시 미가용 2개
(`xgboost_tabular`·`lstm_temporal`)는 Phase 3 미착수(학습 데이터 0건)로 인한 **알려진 만성**이고,
나머지 4개(`flow_position`·`options_flow`·`orderflow_ofi_vpin`·`regime_hmm`)는 오늘도 정상
편입됐다. 레짐은 08:46 최초 전이 후 09:15:01에 웜업 종료(기동 후 103분), 이후 갱신 정상.
이벤트 캘린더 미기입 경고 6건(매시, 만성 — 위 1-2). 체인 신선도 stale 비율은 로그 축이 없어
DB 축(§14, `chain_input_source`)이 담당 — 장후 세션이 확인.

**B-4 코드 변경** — ✅ 이 회차 코드 변경 없음.

### 2-1. ⬆️ 격상(09-07 §1-1 후속) — 결정 창이 사람 개입 없이 닫혔다. 3번째 미룸이고, 오늘은 실제로 위험선을 넘는 창도 나왔다

**증상.** 장전 §1-1이 "오늘 09:00 전에만 낼 수 있는 결정"이라고 못박은 사전 대응 규칙
발동 여부가, 09:00을 넘긴 지금(12:34)까지 아무 결정 없이 지나갔다. 재기동 기록이 없고
(`직전 정상 기동` 표식이 07:31:00 1회뿐) 혼잡 시간대 레버에도 9시가 추가되지 않았다
(`mahdi/main.py:275` `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS = {10: 4, 11: 4, 12: 4, 13: 4, 14: 4}`,
9시 키 없음).

**근거.** 증거 §2 "프로세스 기동 1회" · §7 레버 표 · `mahdi/main.py:275`. 그리고 오늘 실측으로
이 문제가 추상적 우려에서 실측 위험으로 넘어갔다: 증거 §5-1 "⛔ `p95/timeout >= 1.0`인 창
**4/60개** — 최악 **11:01** 비율 **1.01**". p95(상위 5% 응답시간)가 타임아웃(4.0초)의 1.0배를
넘었다는 것은 **그 창에서는 상위 5% 호출의 절반 이상이 이미 시간 초과였다**는 뜻이다(08-27
P2-E가 지정한 판정 방식). 사전 대응 규칙의 이틀 연속 성립 구간도 어제 7개에서 오늘 8개로
늘었다(증거 §5-1 🔔줄, 9시 `inquire-price`가 새로 포함된 것은 어제 이미 확인된 것과 동일).

**기준.** `phases.md` A-2 "안 켜기로 정했으면 그 사유를 그날 보고서에 적는다. 사유 없는
유예는 결정이 아니라 망각이다(레버 F가 세 번, E가 일곱 번 미뤄졌다)." 이번 건은 그 문구가
말하는 상황 그 자체다 — 09-04→09-07→09-08, 사유 없이 세 번째로 지나갔다.

**영향(평문).** 시세를 조회하는 속도가 여유 있는 시간대와 빠듯한 시간대가 있는데, 그 여유가
줄어드는 시간대(오전 9시)에 대한 대응을 켤지 말지를 사람이 아직 못 정했다. 오늘 그 시간대의
응답이 실제로 위험한 수준(요청 20건 중 절반 이상이 막히는 수준)까지 간 창이 있었다는 것이
새로 확인됐다 — 지금까지는 이게 "다음에 그럴 수도 있다"는 걱정이었는데, 오늘은 "실제로 한
번 그랬다"로 바뀌었다. 오늘은 이미 재기동 창이 닫혀서 손을 못 대고, 다음 기회는 **내일
아침**이다.

> ↩️ **위 "최악 11:01"은 위험선 최초 돌파 시각이 아니다 — §3-1(장중② 14:30 회차)
> 참조.** (본문은 당시 대조 그대로 보존)

**14:30 회차가 이어받을 것.**
1. 12:34 이후~15:20까지 09시·10~14시대 REST 응답이 더 악화되는지(위험선 돌파 창이
   더 느는지) 시계열로 추적할 것.
2. 오늘 결정이 사람 개입 없이 지나간 이 사례를, Fix 후보 D(아래)가 다루는 "결정 미룸
   카운터"에 3번째 사례로 반영할 것.
3. 1-3(pending 가설 11건, 2건 도래) — 오늘 도래분(fix-a·fix-c) 두 건이 하루 종일 정상
   데이터를 내는지 계속 지켜보고, 최종 판정은 장후로 넘길 것(소급 금지, `phases.md` C-3).

### 이 회차의 신규 이상점 — 없음

이월 처분표(2-0)에서 처리한 것 외에 새로 발견된 것은 없다. p95/timeout 위험선 돌파(위
2-1)는 **1-1의 연장**으로 처리했다(같은 근본 원인·같은 결정 대기 상태라 새 번호를 붙이면
1-1과 2-1이 같은 것을 가리키는 두 항목으로 쪼개져 헷갈린다). 매시 정각 체결통보 단절(관측
5번)은 이미 폐기 목록에 준하는 만성 현상이라 신규로 올리지 않았다(§8-2 경고 대상).

---

## Fix 구현계획 갱신 (초안 — 여전히 장후 적용, 이 회차는 코드 변경 없음)

**Fix 후보 D(P2 → 사실상 P1 근접, 장전에서 이어짐)** — 오늘이 바로 그 3번째 사례가 됐다.
장전 §Fix 후보 D의 방향(전일 지표에 `🔔`가 있는데 다음 장전 보고서가 아직 없으면 "결정
미룬 지 N번째"를 세는 줄 추가)은 그대로 유효하고, 이번 사례로 픽스처가 하나 더 늘었다
(09-04→09-07→09-08 3연속). 장후 세션에서 착수 우선순위를 재검토할 것.

새 Fix 후보는 없음(이 회차는 계획 변경 없이 장전 초안을 그대로 승계).

## 고도화 방안 갱신 (초안 — 당일 관측 근거)

장전 §고도화 1·2에 추가할 것: 오늘 실측으로 p95/timeout 위험선(1.0배) 돌파가 실제로
나왔다는 것이, "위험선을 선행 신호가 아니라 동시 신호로 재정의할 것인가"라는 이미 열려 있는
질문(`NEXT_TODO.md`:258, 아직 미결정)에 새 표본을 하나 보탰다. 장후 세션이 이 표본을
`cliff_episodes.md` 계열 기록에 반영할지 검토할 것.

## dev_memory 반영 초안 추가 (장후 세션이 실제로 반영)

- **NEXT_TODO 후보(장전 항목 갱신)**: "[ ] 사전 대응 규칙 발동 결정이 09-04·09-07·09-08
  **3연속** 사람 개입 없이 지나갔다 — Fix 후보 D(결정 미룸 카운터)의 착수 우선순위를
  높일 것. 오늘은 위험선(p95/timeout≥1.0) 돌파 창까지 실측됐다(11:01, 1.01배)."
- **DECISION_LOG 후보**: 오늘 사전 대응 규칙 결정이 (A)/(B) 둘 다 사람이 고른 것이 아니라
  "재기동 창이 그냥 지나갔다"는 사실을 남긴다 — 다음에 같은 상황이 오면 이 기록으로
  "왜 매번 놓치는가"를 볼 수 있게.

---

## 제2부 — 장중 ② (14:30 회차, 실행 14:40 KST — 정시 실행, 예정 대비 +10분)

증거 파일: `docs/동작점검/auto/2026-09-08_증거_intra_1430.md`. 조사 범위 12:34~14:40
KST(전 회차 이후 126분). HEAD `05dfead`(미커밋 26건 — 장중①과 완전히 동일, 새 파일
없음). 당일 커밋 0건, 재기동 0회(프로세스 기동 07:31:00 그 1회 그대로).

### 3-0. 장중① 이월 처분 — 이상점 4건 + 이어받을 것 3건, 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-2 이벤트 캘린더 만료 | 🔄 지속(만성) | 07:31:55~14:33:54 매시 재발 8건, 07시~14시 시간대 전부(증거 §5) |
| 1-3 pending 가설 적체 | 🔄 지속·불변 | `hypotheses.yaml` mtime 09-07 그대로, pending 11건 불변(증거 §8) |
| 1-4 레버 3건 무조건발동일 정리 | 🔄 지속(관리, 변화없음) | 지난 지 16일·6일·8일 그대로(증거 §7) — 하루 안이라 날짜가 더 늘지 않았다 |
| 1-5 미커밋 26건 | 🔄 지속(만성) — 내용 그대로 | `git --no-optional-locks status --porcelain` 12:34와 완전 동일(파일명·순서 일치) |
| 2-1 결정창 닫힘 + REST 지연 위험선 돌파 | 🔄 지속·심화 — 위험선을 넘는 창이 늘었고, 처음으로 25분 연속 구간이 나왔다 | 아래 3-1 |
| (이어받을 것) ① REST 응답 지연 시계열 추적 | ✅ 판정 — 아래 3-1 및 「오후 열화 게이지」 | 증거 §5-1, `auto/2026-09-08_지연창.tsv` |
| (이어받을 것) ② 결정 미룸 카운터(Fix 후보 D)에 3번째 사례 반영 | ✅ 판정 — 09-04→09-07→09-08 3연속으로 최종 확정. 계획 변경 없음(장후 착수 유지) | 증거 §12-2, 장전 §1-1 |
| (이어받을 것) ③ pending 가설(fix-a·fix-c) 오후에도 정상 데이터를 내는가 | ✅ 판정 — 둘 다 오후에도 정상 작동 중. **최종 확정(상태 변경)은 장후로 이월**(소급 금지, `phases.md` C-3) | 증거 §6-1(호출 526회·5분 이상 공백 0) · §5-1-1(완전미회복 2건/부분회복 6건으로 정상 분리) |

전 회차가 남긴 4건 + 이어받을 것 3건, 전부 처분했다 — 조용히 사라진 항목 없음.

### 장중 체크리스트 재확인 (phases.md B절, 12:34 이후 변화만)

**B-1 생존** — ✅ 정상. 관측 루프 마지막 레코드 **14:40:27**. 07:30~15:45 구간 3분 이상
로그 공백 **0건**(장중① 이후 변화 없음). 워치독 당일 43행(OK 43·비-OK 0), 호출
**526회**(idle 105·ok 421, 장중①의 400회에서 +126), 호출 간격 5분 이상 공백 **0건**,
기록 간격 25분 이상 공백 **0건**, 마지막 기록 14:40 → 감시 창 끝(14:40)까지 **0분
무기록**. `observation_loop_crash.log`는 07:30 기동 표식 이후 트레이스백 없음 — 오늘
크래시 없음(mtime 함정 재확인 완료).

**B-2 수집 파이프라인** — ERROR **0건**(장중①과 동일, 하루 종일 0건). WARNING
204건(29종, 장중①의 122건에서 +82 — 시간 경과분). 예산초과/연속타임아웃 조기포기/
실패예산소진 = **45건 / 16건 / 0건**(비율 그대로, "전멸"은 여전히 진짜 0). 체결통보
스트림 단절(만성 축, WS_client 축과 다름)이 13:00:01·14:00:01 **2건 추가**(누적
**7건**) — 전부 매시 정각 +1.7~1.9초, 2초 내 재연결 성공. `DECISION_LOG.md`:670에
이미 원인·성격이 규명된 만성 현상이라 신규로 올리지 않는다(§8-2 경고 대상). WS_client
자체 단절(observation_loop 계측)은 **0건**으로 그대로.

**B-3 판단까지 갔는가** — 앙상블 비영 멤버, 이 구간(12:34~14:40) 최소 **2**·최대 **3**
(분모 6, 가용은 상시 4/6 — `xgboost_tabular`·`lstm_temporal` 알려진 만성 미가용).
최신 사이클(14:39:54)은 4/6 가용 중 비영 **3**(HIGH_CONVICTION). 2/6까지 내려간
것은 이 구간에서 **14:00:54가 최초**이고 이후 14:03·14:05·14:13·14:16·14:24·14:25·
14:28·14:31에 반복됐다 — 다만 오늘 하루 전체로 보면 09:30·10:15·10:45에도 이미 2였고
(장중① §B-3 "최소 0·최대 3"), **오늘의 구조적 특성이지 이 회차에서 새로 나빠진 것은
아니다**(아래 오후 열화 게이지에서 판정 근거를 표로 낸다).

### 오후 열화 게이지 (창 단위 시계열 — 12:34~14:40, 5분 창)

| 지표 | 계산 | 경고선 | 위험선 | 이 구간 창 최대 | 경고선 최초 돌파 | 위험선 최초 돌파 |
|---|---|---|---|---|---|---|
| rows=0 사이클 비율 | 최근 30분 내 rows=0 사이클/총 사이클 | ≥0.3 | ≥0.7 | **0.00**(0/60, 09~14시 전 시간대 0건) | 없음 | 없음 |
| inquire-price 지연(p50÷timeout 4.0초) | 창별 p50÷타임아웃 | ≥0.80 | ≥1.00 | **0.70배**(13:41:06 창) | **미도달**(오늘 최대 0.70) | 미도달 |
| inquire-price 지연(p95÷timeout, 보조지표·08-27 P2-E 축) | 창별 p95÷타임아웃 | ≥0.80 | ≥1.00 | **1.01배**(다수 창, 최장 연속 5창) | **09:06:06(0.86배)** | **10:01:06(1.00배)** — ↩️ 정정(아래 3-1) |
| 앙상블 비영 멤버 | 최신 사이클 비영/전체 | ≤5/6 | ≤3/6 | **2/6**(14:00:54 최초) | 09시 이전부터 상시 하회(만성) | 09시 이전부터 상시 하회(만성) |
| 관측 루프 공백 | 마지막 레코드~현재 | ≥3분 | ≥5분 | **0분** | 없음 | 없음 |
| WS 단절(체결통보, 다른 축) | 12:30 이후 누적 | 증가 추세 명시 | — | 누적 **7건**(13:00·14:00 +2) | — | — (만성, 전부 2초 내 재연결) |

> **p50 기준으로는 오늘 경고선도 안 넘었다.** 이 파일이 지금까지 위험 신호로 인용해 온
> 것은 **p95÷timeout**(2026-08-27 P2-E가 지정한 축, 12:30 회차 §2-1도 이 축을 썼다) —
> 두 지표를 같이 적은 이유는 "p50은 안전한데 p95는 위험"이 그 자체로 정보이기 때문이다
> (20종목 순차 수집에서는 "절반이 느려진다"보다 "5%가 완전히 막힌다"가 먼저 온다).

### 3-1. 🔄 지속·심화(2-1/1-1 후속) — 결정창은 이미 완전히 닫혔고, 오늘 처음으로 25분 연속 위험선 돌파 구간이 나왔다

**증상.** 사전 대응 규칙 발동 결정은 09:00 전에만 낼 수 있었는데(장전 §1-1), 그 창이
사람 개입 없이 지나간 지 이미 5시간 넘게 지났다 — 오늘은 더 이상 되돌릴 수 없는 결정이
됐다. 그 사이 실측 위험은 더 커졌다: `inquire-price` 응답시간의 상위 5%(p95)가
타임아웃(4.0초)의 1.0배를 넘는 창이 12:34 시점 4/60개에서 지금 **16/85개(18.8%)**로
늘었고, **14:11:06~14:31:06 구간에서 처음으로 5창 연속(25분) 위험선을 넘는 사례**가
나왔다(`auto/2026-09-08_지연창.tsv`). 이전까지는 위험선 돌파가 한두 창씩 흩어져
있었는데, 오늘 처음으로 "한 번 넘으면 계속 넘는" 구간이 생겼다.

**근거.** `auto/2026-09-08_지연창.tsv` 14:11:06(1.01)·14:16:06(1.01)·14:21:06(1.00)·
14:26:06(1.00)·14:31:06(1.01) — 5창 연속. 증거 §5-1 "⛔ p95/timeout >= 1.0인 창
16/85개 — 최악 11:01 비율 1.01 · 최장 연속 5창(14:11~14:31)".

**기준.** `phases.md` B절 오후 열화 게이지 — "판정은 창 최대로 한다", "경고선을 처음
넘은 창의 시각을 반드시 특정한다".

**정정(↩️, 2-1 절 참조 — 본문은 그대로 둔다).** 2-1이 "위험선(1.0배) 최초 돌파"의
근거로 인용한 시각은 **11:01**(비율 1.01)이었지만, 오늘 전체 창을 다시 대조하니
**10:01:06에 이미 1.00배**를 찍었다 — 2-1이 고른 것은 "그 시점까지의 최악"이지
"최초 돌파"가 아니었다. 결론(위험선을 넘었다, 사람 개입 없이 지나갔다)은 바뀌지
않지만, 위험선이 실제로는 1시간 더 일찍부터 걸려 있었다는 뜻이다. → 2-1 절 본문은
고치지 않고, 이 사실만 여기 기록한다.

**영향(평문).** 시세를 조회하는 속도가 오늘 오후 들어 더 자주, 더 오래 빠듯한
수준에 머물렀다. 다만 이 순간에도 시세를 통째로 못 받아온 적(사이클 전체 유실)은
없었다 — 상위 5%가 늦게 들어왔을 뿐 판단에 쓸 데이터 자체가 끊긴 것은 아니다. 이
대응(위클리 조회 주기를 늘리는 것)은 오늘은 이미 손을 댈 수 없고, 다음 기회는
내일 아침이다.

**14:50 컷오프까지 남은 시간과 권고.** 이 보고서를 쓰는 지금(14:40~14:43) 기준으로
컷오프까지 **7~10분** 남았다. **개입 불필요.** 이유는 둘이다 — ① 사전 대응 규칙
발동은 재기동이 있어야 반영되는데 장중에는 재기동을 하지 않는다(코드 변경·재기동
금지, `phases.md`). ② 오늘 위험선 돌파는 "판단이 못 쓸 정도로 데이터가 빈" 상황
(rows=0)으로 이어진 적이 한 번도 없다(오후 열화 게이지 rows=0 행 참조) — 아직은
"느려졌다"이지 "끊겼다"가 아니다. 남은 10~19분(15:20 마감까지) 동안 rows=0 사이클이
**처음으로** 나오는지만 지켜보면 되고, 그것은 사람이 지금 뭘 정해서 막을 수 있는
종류의 위험이 아니다.

### 이 회차의 신규 이상점 — 없음

3-0에서 처분한 것 외에 새로 발견된 것은 없다. 위험선 돌파 구간이 길어진 것(위 3-1)은
**1-1/2-1의 연장**으로 처리했다(같은 근본 원인·같은 "결정 대기가 굳어버린" 상태라
새 번호를 붙이면 1-1·2-1·3-1이 같은 것을 가리키는 세 항목으로 쪼개져 헷갈린다).
체결통보 단절 2건 추가(위 B-2)는 이미 폐기 목록에 준하는 만성 현상이라 신규로
올리지 않았다(§8-2 경고 대상).

---

## Fix 구현계획 갱신 (14:30 — 변경 없음)

장전·장중①에서 세운 **Fix 후보 D**(결정 미룸 카운터)의 방향과 우선순위는 그대로다.
3-0 이월표에서 "3연속 미룸"이 최종 확정됐을 뿐, 새로 착수할 코드 변경은 없다(장중
코드 변경 금지). 장후 세션이 착수 여부를 정한다.

## 고도화 방안 갱신 (14:30 — 당일 관측 근거 추가)

장전 §고도화 2·장중① §고도화 갱신에 추가할 것: 오늘 실측으로 위험선(p95/timeout
≥1.0) 돌파가 **처음으로 25분 연속** 나타났다는 것이, `NEXT_TODO.md`:258의 열려 있는
질문("경고선을 선행 신호에서 동시 신호로 재정의할 것인가")에 **연속성 있는 첫
표본**을 보탰다. 지금까지는 돌파가 산발적이어서 "선행 신호"로만 봤는데, 오늘은
"그 자체로 지속되는 상태"에 가까웠다 — 장후 세션이 `cliff_episodes.md` 계열 기록에
이 구간(14:11~14:31)을 추가할지 검토할 것.

## dev_memory 반영 초안 추가 (14:30, 장후 세션이 실제로 반영)

- **NEXT_TODO 후보**: "[ ] 사전 대응 규칙 미룸이 09-04·09-07·09-08 3연속으로 최종
  확정됐다(장전 §1-1·장중① §2-1·장중② §3-1). 같은 날 위험선(p95/timeout≥1.0) 돌파가
  처음으로 25분(5창) 연속 나타났다(14:11~14:31) — `cliff_episodes.md` 편입 검토."
- **DECISION_LOG 후보**: "위험선 최초 돌파 시각 정정 — 장중① §2-1이 인용한 11:01은
  '그 시점까지의 최악'이었고, 장중② 재대조로 10:01:06이 실제 최초 돌파임을 확인.
  향후 '최초 돌파'를 인용할 때는 전체 창 재대조를 거칠 것(같은 실수 재발 방지)."

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (개장 전, 09:00까지) — 결정이 필요한 것

1. **PC가 어제(09-07) 07:30에 꺼져 있었는지 확인한다.** (근거: 1-0표 1번, 여전히 미확인)
   급하지 않음 — 오늘은 재발하지 않았다.
2. **사전 대응 규칙(09시 포함 여부)을 지금 결정한다.** (근거: 1-1, ⚠ 오늘 아침이 사실상
   마지막 창) 선택지:
   - **(A)** 지금 재기동해 09시를 혼잡 시간대에 추가한다 — 오전장 시세 수집 여유 확보,
     대신 재기동 웜업(과거 사례 기준 09:15경 종료 추정, 확실치 않음)과 그 시간대 위클리
     커버리지 일부 손실.
   - **(B)** 오늘도 유예하고 관찰만 계속한다 — 재기동 없음, 대신 같은 결정이 세 번째로
     미뤄지고 9시대 지연 위험은 그대로.
   - 장중에는 (A)를 실행할 수 없다.

### 오늘 중 (장 마감 후, 급하지 않음)

3. **이벤트 캘린더 `covered_through`를 9월분으로 갱신한다.** (근거: 1-2, 11일째 정지)
4. **가설 11건(그중 도래분 10건)을 검토해 확정하거나 `inconclusive`로 닫는다.** (근거: 1-3)
5. **레버 3건의 무조건발동일을 실제로 켠 날짜로 정리한다.** (근거: 1-4)

### 결정이 필요한 것 (자동으로 못 하는 것, 급하지 않음)

6. **`.claude/`·`dashboard_build_prompt_1.md` 커밋 또는 `.gitignore` 등록 여부.** (근거: 1-5,
   15일째)
7. (만성, 재확인만) ZN/ES 해외선물 거래소 이용 신청 여부, `MEMBER_DROPOUT_ALERT_MIN` 문턱
   재검토, 매매 실행 경로 배선 시점 — 오늘 새로 다루지 않았으나 여전히 열려 있다.
8. `DECISION_LOG.md`의 09-03 두 절 위치 정리 여부(사소, 사람 몫).

*이 절은 장전(pre) 기준이다. 장중·장후 회차가 이 파일 끝에 이어 붙이며, 위 항목의 처리
결과를 각 국면의 「이월 처분표」에서 갱신한다.*

---

## 제3부 — 장후 (15:20 ~ 15:46, 실행 15:46 — 정시 실행)

증거 파일: `docs/동작점검/auto/2026-09-08_증거_post.md`(865행) · `auto/2026-09-08_지표.md`
· `auto/2026-09-08_지표.json` · `auto/2026-09-08_지연창.tsv`(98개 5분 창). 조사 범위
07:30~15:46 KST(하루 전체). HEAD `05dfead`(브랜치 `master`, 미커밋 27건 — 장중②의 26건과
**내용은 동일**, 차이는 git이 `.claude/`를 폴더 1줄로 묶어 세느냐 파일별로 펼쳐 세느냐뿐이다).
당일 커밋 0건, 재기동 0회(프로세스 기동 07:31:00 그 1회, 하루 종일 유지).

### 4-0. 장중② 이월 처분 — 이상점 5건 + 이어받을 것 3건, 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-2 이벤트 캘린더 만료 | 🔄 지속(만성, **13일째**) | 07:31:55~15:33:54 매시 재발 9건, 07~15시 전 시간대(증거 §5) |
| 1-3 pending 가설 적체 | 🔄 지속 — 그러나 이 회차가 **10건에 판정 제안**을 부기한다(소급 금지, 확정은 사람 몫) | 아래 4-2 |
| 1-4 레버 3건 무조건발동일 정리 | 🔄 지속(관리, 하루 더 지남 — 17일·7일·9일) | 증거 §7 |
| 1-5 미커밋 27건 | 🔄 지속(만성) — **내용은 그대로다** | 위 참조. `.claude/` 표기 방식 차이일 뿐, 새 파일 없음 |
| 3-1(→2-1→1-1) 결정창 닫힘 + REST 지연 위험선 돌파 | ⬆️ **격상·확정 — 오늘 처음으로 실제 절벽(데이터 완전 유실)이 발생했다.** 걱정이 사실이 됐다 | 아래 4-1 |
| (이어받을 것) ① 12:34 이후~15:20까지 지연 악화 시계열 추적 | ✅ 판정 — 아래 4-1(15:01부터 절벽 시작) | `auto/2026-09-08_지연창.tsv` |
| (이어받을 것) ② 결정 미룸 3번째 사례를 Fix 후보 D에 반영 | ✅ 판정 — 3연속 확정에 더해, 오늘은 그 3번째 미룸의 **실제 대가(절벽)까지 같은 날 관측됐다.** Fix 후보 D 우선순위 P1로 재확정(아래 제4부) | 4-1 |
| (이어받을 것) ③ pending 가설(fix-a·fix-c) 최종 확정 | ✅ 판정 — 둘 다 도래일(오늘) 실측 기준으로 **성립(확인) 제안** | 아래 4-2 |

전 회차가 남긴 5건 + 이어받을 것 3건, 전부 처분했다 — 조용히 사라진 항목 없음.

### 장후 체크리스트 (phases.md C절)

**C-1 종료 시퀀스** — ✅ 전부 정상:
- `stop_mahdi_marketclose.bat` 15:45:01.32 "intentional stop" 기록, `.last_marketclose_stop.txt`
  15:45:03.671073로 갱신 — 정시(예정 15:45).
- `daily_ops_report.py`가 종료 절차 안에서 정상 실행 — `auto/2026-09-08_지표.md`(76.3KB)·
  `_지표.json`(66.3KB) 둘 다 15:46 존재.
- 관측 루프 최종 레코드 15:45:01(체결 스트림 인용 시각과 거의 동시) — 종료 시각과 일치, 그
  전에 죽지 않았다. 15:44:18 마지막 정상 사이클(rows=20)로 **하루를 정상 상태로 마감**했다
  (절벽은 15:28 전후로 이미 저절로 풀렸다 — 아래 4-1).
- 잔존 프로세스 확인은 이 세션 권한 밖(원격 로그만 봄) — 다음 07:30 기동 로그의 "기동 전
  잔존 프로세스 정리" 줄이 비정상(강제 종료 필요)으로 나오는지가 사후 증거가 된다.

**C-2 자동 지표** — §0 레버 상태 그대로(3개 ON·3개 OFF, 변화 없음). §0-1 가설 검정에
**"⚠ 오늘은 절벽 발생일 — 대가 지표 해석 주의"** 배너가 자동으로 떴다(아래 4-2에서 다룸).
§1 한눈에: 인프라 축은 대부분 ✅(REST 호출 ▲127·초당수요 ▼·사이클 수 ▲30)인데 **판단
입력 축은 전부 ⚠**(먼슬리 절대커버리지 94.3%▼5.5·GEX입력없던분 25분▲24·최장연속0행
28분▲27) — `phases.md` D절이 경고하는 "인프라는 멀쩡한데 판단이 눈을 감았다"의 정확한
사례다.

**C-3 판정과 기록** — 아래 4-2에서 도래분 10건에 제안을 적는다(확정은 사람 몫, 소급 금지
준수). dev_memory 3파일은 이 회차가 직접 고치지 않고 "반영 초안"만 적는다(자동 예약 회차
규칙).

---

### 4-1. ⬆️ 격상·확정(3-1/2-1/1-1의 최종 결말) — 오늘 실제로 절벽이 발생했다. 09-03 이후 다섯 번째 표본이고, 회복 패턴이 처음으로 달랐다

**증상.** 장전부터 "결정을 못 내리면 여유가 준다"고 경고했던 그 위험이, 오늘 15:01부터
실제로 **데이터가 완전히 빈 것**으로 나타났다. 옵션체인 수집이 20레그 중 0행만 적재된
분이 **28분 연속**(15:01~15:28) 이어졌고, 그 사이 판단은 5분 신선도 창을 넘긴 낡은
스냅샷을 봐야 했다. 워치독은 15:10부터 "관측 루프가 살아는 있는데 아무것도 못 가져온다"는
경보(`no_ingest`, DEGRADED)를 매분 냈고, 15:30에 자연 회복했다.

**근거.**
- 최초 ERROR `logs/observation_loop.log` 15:01:32
  ```
  [ERROR] 옵션체인 이번 분 전멸 — 수집 20레그 중 적재 0행(예산 28.5초 소진, 재시도 함).
  이 분의 체인은 비었다 — 판단은 신선도 창 안의 직전 스냅샷을 쓴다
  ```
  같은 문구가 15:28:53까지 27회, 15:20:52에 재시도 없이 1회 더(증거 §4).
- `auto/2026-09-08_지표.md` §4: **최장 연속 0행 구간 28분**(임계 20분 초과), 전일 1분 대비
  **▲27분**. GEX 입력 없던 분 **25분**(전일 1분 대비 ▲24분).
- `auto/2026-09-08_지연창.tsv`: p50÷타임아웃(4.0초) 비율이 **15:06:06 창에서 경고선(0.80)과
  위험선(1.00)을 같은 창에서 동시에** 돌파(1.01) — **간격 0분**. 이후 15:11(1.01)·15:16(1.12,
  오늘 최댓값)·15:21(0.76, 유일하게 일시 완화)·15:26(1.01)·15:31(1.00)까지 위험선 근처를
  맴돌다 **15:36:06에야 0.01로 완전히 내려갔다.**
- 워치독 `logs/watchdog.log` — 15:10:02 최초 DEGRADED부터 15:29:01까지 매분 20행, **15:30:01
  RECOVERED**(적재 정지 20분 지속 후 회복). 그 첫 줄부터 "같은 유형(no_ingest)의 과거 표본
  3건(08-26·09-01·09-03)은 45~85분 만에 15:25~15:26(정규장 마감+5~6분)에 자연 회복했고
  3건 다 우리 쪽 조치가 없었다"는 참고 이력을 실었다(`2026-09-03-p2-3-watchdog-cites-recovery-history`
  fix가 실전에서 처음 발동한 사례 — 아래 4-2).

**기준.** `docs/동작점검/cliff_episodes.md` "옵션체인 적재가 수십 분 연속 rows=0이 되고,
그 원인이 우리 쪽 변경이 아닌 구간"이 절벽의 정의다. 오늘은 이 정의에 정확히 들어맞는
**09-03 이후 다섯 번째 표본**이다(08-14·08-26·09-01·09-03·**09-08**). 원인 귀속도 과거
넷과 같은 근거로 KIS 쪽이다 — 오늘 HEAD `05dfead` 하루 종일 고정, 당일 커밋 0건, 재기동
0회인데 지연이 나빠졌다(우리 쪽 변경이 없다).

**영향(평문).** 오후 3시 1분부터 28분 동안, 프로그램이 옵션 시장을 보는 눈이 완전히
멈췄다 — 새로 들어오는 시세가 하나도 없어서, 그동안 프로그램이 내린 모든 판단은 그 전에
찍힌 오래된 스냅샷에 의존했다. 다행히 오늘은 이 프로그램이 실제 주문을 내는 기능이
아직 연결돼 있지 않아서(관측 전용 모드) 이 28분 동안 잘못된 판단으로 돈을 잃을 위험은
없었다. 하지만 나중에 실제 주문 기능이 연결된 뒤 오늘과 같은 날이 오면, 이 28분은 "잘못된
가격으로 잘못된 시점에 진입하거나 빠져나오지 못하는" 실제 손실 위험이 된다 — 오늘이 바로
그런 날의 예행연습이었던 셈이다.

**과거 네 번 표본과 다른 점 — 회복이 처음으로 늦었다.** `cliff_episodes.md`가 08-26·09-01·
09-03 세 번 다 "정규장 마감(15:20) +5~6분인 15:25~15:26에 회복했다"고 기록해 뒀는데,
오늘은 (워치독 기준) 15:30(+10분), (REST 지연 p50 축 기준) 15:36(+16분)으로 **처음
어긋났다.** 같은 파일이 "다섯 번째가 나오면 물을 것 1번 — 회복이 네 번째로 마감+6분인가"를
미리 걸어 뒀는데, 답은 **아니다**다. 다만 "질문 2번 — 간격 0분이 재발하는가"는 **그렇다,
세 번째로 0분**이다(09-01·09-03에 이어) — 이 파일이 09-03에 걸어 둔 조건("두 번째로
0분이 나오면 선행 시간은 그냥 없는 것")이 이번에 **세 번째로 재확인**됐다.

**14:50 컷오프와의 관계.** 오늘 신규 진입 컷오프(14:50)는 절벽 시작(15:01)보다 11분
앞서 있어 컷오프 자체에는 안 걸렸다. 다만 09-03 표본은 컷오프(14:50)가 절벽 구간(14:31~
15:20) **안에** 있었던 것과 달리, 오늘은 컷오프가 절벽 **밖**에 있었다는 점에서 이번
표본은 "컷오프를 삼킨 절벽"이 아니라 "마감 막바지만 삼킨 절벽"이다 — 다섯 표본의 시작
시각·지속시간·컷오프 관계가 매번 다르다는 것 자체가 하나의 패턴이다(아래 제5부).

**14:50 컷오프까지 남은 시간과 개입 필요성 (사후 재구성).** 절벽이 시작된 15:01은 이미
컷오프(14:50) 이후이고 정규장 마감(15:20)까지 19분 남은 시점이었다 — 이 시점에는 (1)
사전 대응 규칙 발동이 재기동을 요구해 장중엔 못 하고, (2) 이미 신규 진입이 끝난 뒤라
개입해도 되돌릴 거래가 없었다. **사람이 그 28분 동안 할 수 있었던 일은 없었다** —
관망밖에 없다는 15:20 회차(3-1)의 판단은 오늘 실제로 벌어진 절벽에도 그대로 유효했다.

---

### 4-2. 오늘 도래·경과한 pending 가설 10건 — 판정 제안 (확정은 사람 몫, 소급 금지 준수)

> 아래는 **제안**이다. `hypotheses.yaml`의 `상태`는 이 회차가 바꾸지 않는다(스킬 규칙).
> ⚠ 오늘은 절벽 발생일이라 **대가(cost) 지표는 오염됐을 수 있다** — 특히 GEX 입력 없던
> 분·먼슬리 커버리지 관련 대가 지표는 절벽의 값일 수 있으므로 그대로 확정하지 말 것
> (자동 §0-1 경고 그대로 인용).

| id | 검증예정일 | 실측 | 제안 | 근거 |
|---|---|---|---|---|
| `2026-09-07-fix-a-watchdog-invocation-gap` | 09-08(오늘, 도래) | `invocation_gap_over_cadence_ratio` 1.25 (기준 ≤5) | **성립(확인)** | 지표.md §0-1. 오늘 워치독 호출 592회, 5분 이상 공백 0건 — 계측이 실전에서 하루를 정상적으로 돌았다 |
| `2026-09-07-fix-c-monthly-total-failure-count` | 09-08(오늘, 도래) | `total_failure_cycles` 4 (기준 ≥0, 존재 확인) | **성립(확인)** | 지표.md §0-1. `total_failure_cycles`(4) ≤ `failed_cycles`(10) 부분집합 관계도 깨지지 않았다 — 새 축이 옳게 세고 있다 |
| `2026-09-03-adv3-cliff-day-flags-the-hypothesis-table` | 08-27(12일 경과) | `zero_row_longest_run` 28분 | **성립(확인) — 오늘이 그 절벽일이었고, 실제로 플래그가 떴다** | 지표.md §0-1 맨 위 "⚠ 오늘은 절벽 발생일 — 대가 지표 해석 주의" 배너를 오늘 처음 실전에서 확인. 이 가설이 다루는 정확한 상황(절벽 20분 이상)이 오늘 실제로 벌어졌고, 계측이 정확히 반응했다 |
| `2026-09-03-p2-3-watchdog-cites-recovery-history` | 09-04(4일 경과) | 15:10:02 DEGRADED 첫 줄에 과거 3건 회복 이력 인용문 실림 | **성립(확인) — 오늘이 이 fix의 첫 실전 발동 사례** | 위 4-1 인용문 그대로. "사람이 지금 재기동할 것인가"를 판단할 근거가 경보 자체에 실린 것을 오늘 처음 실측으로 확인했다 |
| `2026-09-04-p2-b-isolated-zero-row-count` | 09-05(3일 경과) | `zero_row_isolated_count` 0, `zero_row_count` 28 (전부 연속 구간 소속) | **성립(확인)** | 지표.md §0-1. 오늘처럼 절벽이 하나의 연속 구간(28분)뿐인 날엔 고립 0행이 0건이어야 하고 실제로 0이 나왔다 — "총 0행 = 고립 + 연속 소속" 정합성도 깨지지 않았다(28=0+28) |
| `2026-09-02-fix10-phase-offset-55s` | 09-04(4일 경과) | `chain_input_source.stale_pct` 0.8(≤20 기준 통과) · `chain_newest_age_seconds.p50` 54.9초(≤55.0 기준, 간발의 차) | **성립(확인)이나 여유가 거의 없다** — 다음 도래 시 55.0초 문턱을 실제로 넘는 날이 나오는지 지켜볼 것 | 지표.md §0-1. 두 주장 지표 다 통과했지만 후자는 기준값에 0.1초 차로 붙어 있다 |
| `2026-08-12-eE-on-congested-hours` | 09-02(6일 경과) | `budget_exceeded.count` 58(기준선 74건 대비 감소) · `book_coverage.weekly_mon` 31.8%(기대 약 25%보다 높음) · `gex_input_missing_minutes` 25분(기준선 4분 대비 크게 증가) | **부분 확인(대가 지표 오염) — `pending` 유지 제안**(09-02·09-07에 이어 3연속 이월) | 지표.md §0-1 경고 그대로: 오늘 GEX 입력 없던 분 25분은 레버의 대가가 아니라 **절벽(4-1)의 값**일 가능성이 크다. 절벽 없는 날 재실측 필요 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 08-27(12일 경과) | 오늘 장전 §1-1이 09시 지연을 정확히 인용했으나, 결정 자체는 3번째로 안 내려짐(위 4-1) | **부분 확인(다른 경로로 충족) — `pending` 유지 제안** | 규약 문구가 요구하는 "인용"은 매번 됐다(1-1·2-1·3-1·4-1). 다만 "그 자리에서 발동 여부를 정한다"는 사람의 결정이라 이 가설 자체의 성패와는 별개다 — NEXT_TODO :265와 같은 결론 |
| `2026-08-12-fix7-regime-session-window-restore` | 09-01(7일 경과) | 전제 레버 `REGIME_RESTORE_SESSION_WINDOW` 오늘도 꺼짐 | **미실행 — 규약 H로 판정 불가.** 예정일을 "레버를 켠 다음 거래일"로 재설정 제안(날짜는 레버를 켜는 날 함께 정할 것 — 날짜 없는 연기는 금지) | 지표.md §0-1 "미실행 1건" 명시 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 09-01(7일 경과) | `execution_logs.rows` 0 | **실측 없음(왕복 자체가 없었다) — 결함이 아니라 검정 대상 부재.** 예정일을 "거래가 발생하는 날"로 재설정 제안(날짜 미정) | 지표.md §0-1. ADVISORY(관측 전용) 모드라 오늘도 실주문 왕복 자체가 없었다 — 아래 손익 절 참조 |

### 이 회차의 신규 이상점 — 위 4-1 하나

4-0에서 처분한 것 외에 새로 발견된 것은 4-1(절벽 실발생)뿐이다. ERROR 33건·워치독
비-OK 21행·체결통보 단절 2건 추가는 전부 4-1의 증상이거나(전자 둘) 이미 폐기 목록에
준하는 만성 현상(후자, §8-2 경고 대상)이라 별도 번호를 붙이지 않았다.

---

## 종합 판정표

| 시각 | 국면 | 그 순간의 판정 | 최종 판정(장후 기준) |
|---|---|---|---|
| 07:30~08:01 | 장전 | ✅ 정상 기동, 결정 1건 대기(1-1) | 그대로 — 결정 대기는 실제로 안 내려짐 |
| 09:00~12:34 | 장중① | ✅ 정상 지속, 결정창 사람 개입 없이 닫힘(2-1) | ⬆️ 격상 — 그 결정 미룸이 오늘 오후 실제 절벽으로 이어졌다 |
| 12:34~14:40 | 장중② | 🔄 지속·심화, 25분 연속 위험선 돌파 첫 사례(3-1) | ⬆️ 격상·확정 — 20분 뒤 실제 데이터 완전 유실(절벽)로 진행 |
| 15:01~15:28 | (관측 사후 재구성) | — | 🆕 **절벽 발생·자연 회복**(4-1) — 09-03 이후 5번째 표본, 회복 패턴 처음 이례적 |
| 15:20~15:46 | 장후 | — | ✅ 종료 시퀀스 정상, 절벽은 종료 전 이미 해소, 하루 정상 마감 |

**하루 전체를 한 문장으로**: 아침에 "결정을 안 내리면 위험이 커진다"고 경고했던 것이,
저녁에 실제로 "데이터가 28분 완전히 끊겼다"로 실현된 하루였다. 인프라(기동·재기동·크래시)는
하루 종일 완벽했고, 무너진 것은 KIS 쪽 응답 속도와 그로 인한 "판단이 볼 것을 못 본" 28분
뿐이었다.

---

## 당일 이상점 통합 대장

| 번호 | 제목 | 최초 발견 국면 | 최종 심각도 | 최종 처분 | 근거 위치 |
|---|---|---|---|---|---|
| 1-1 | 사전 대응 규칙(09시 포함) 결정 대기 | 장전 | P1(최초 P2에서 격상) | ⬆️ 격상·확정 — 3연속 미룸, 실제 절벽으로 실현 | §1-1, §2-1, §3-1, §4-1 |
| 1-2 | 이벤트 캘린더 `covered_through` 만료 | 장전 | P2 | 🔄 지속(만성, 13일째) | §1-2, §4-0 |
| 1-3 | pending 가설 11건 적체 | 장전 | P1 | 🔄 지속 — 이 회차가 10건에 판정 제안 부기 | §1-3, §4-2 |
| 1-4 | 레버 3건 무조건발동일 경과 | 장전 | P2 | 🔄 지속(관리) | §1-4, §4-0 |
| 1-5 | 미커밋 27건(`.claude/`·대시보드 프롬프트 등) | 장전 | P2 | 🔄 지속(만성, 16일째) | §1-5, §4-0 |
| 2-1 | (1-1 후속) 결정창이 사람 개입 없이 닫힘 + 위험선 최초 돌파 | 장중① | P1 | ⬆️ 격상·확정(4-1로 흡수) | §2-1 |
| 3-1 | (2-1 후속) 25분 연속 위험선 돌파 첫 사례 | 장중② | P1 | ⬆️ 격상·확정(4-1로 흡수) | §3-1 |
| 4-1 | **절벽 실제 발생 — rows=0 28분 연속(15:01~15:28), 워치독 20분 DEGRADED, 회복 패턴 이례적** | 장후 | **P1**(ADVISORY라 오늘은 실손실 0, 실주문 배선 시 P0급) | 🆕 신규 — 자연 회복 완료, 근본 원인(KIS)은 미해결로 관측 계속 | §4-1 |

---

## 종가 기준 최종 손익 / 절대원칙 판정

| 항목 | 값 | 판정 |
|---|---|---|
| 실현 손익 | 0원 | ✅ 해당 없음 |
| 미실현 손익 | 0원 | ✅ 해당 없음 |
| 실현 손익(자본 대비 %) | 0% | ✅ |
| 미실현 손익(고점 대비 %) | 0% | ✅ |
| 보유 포지션 수 | 0포지션 | ✅ |
| 보유 계약 수 | 0계약 | ✅ |
| 절대원칙(하드 리밋·금지 사항) 위반 | 없음 | ✅ |

**거래 없음 — 이유.** 마흐디는 현재 `ADVISORY`(관측 전용) 모드로 운영된다
(`docs/dev_memory/CURRENT_STATE.md`:26,82 "실행 모드: ADVISORY(관측 전용) — 변경 없음",
`mahdi/main.py` — 관측 전용 오케스트레이터). 오늘 API 호출도 전부 모의투자
엔드포인트(`openapivts.koreainvestment.com`)로 나갔고, 실주문 제출 로그(`order_submit`
등)는 하루 종일 0건이다. 따라서 절대원칙 위반 여부를 가를 실제 거래 자체가 구조적으로
없다 — "위반이 없다"는 "그 위험이 아직 배선되지 않았다"는 뜻이지 "리스크 관리가 통과했다"는
뜻이 아니다. 위 4-1의 절벽이 실주문이 연결된 뒤 재발하면 그것이 Kill Switch
(`data_quality_fail`류)의 첫 실전 시험이 된다(`cliff_episodes.md` 09-03 절의 같은 지적을
오늘도 반복해 둔다).

---

## 제4부 — Fix 구현계획 (P0/P1/P2)

### Fix 후보 D (P1로 재확정, 장전·장중에서 이어짐) — 사전 대응 규칙 "결정 미룸"을 다음 장전이 스스로 세게 한다

**문제.** 09-04→09-07→09-08 3연속으로 같은 결정(위클리 폴링 09시 포함 여부)이 사람
개입 없이 지나갔고, 오늘은 그 대가가 실제 절벽(4-1)으로 나타났다. 지금은 사람이 매번
전일 지표 파일을 열어 `🔔` 줄을 직접 찾아야 안다.

**방향.** `docs/동작점검/tools/collect_evidence.py`의 §8(오늘 판정해야 할 가설) 근처에,
전일 `_지표.md`에 `🔔 연속 판정 성립`이 있고 그 다음 거래일(오늘) 장전 보고서가 결정
문구("(A)"/"(B)" 선택 기록) 없이 마감되면 "결정 미룬 지 N번째"를 세는 줄을 추가한다.
대상 파일: `collect_evidence.py`(신규 절 또는 §8 확장) · `docs/동작점검/tools/phases.md`
(A-2에 카운터 항목 추가). 회귀 위험: 낮음(신규 집계, 기존 판정에 안 끼어듦). 검증:
09-04→09-07→09-08 세 번째 사례 + 오늘 절벽까지를 픽스처로 테스트 추가
(`tests/test_ops_hypotheses.py` 계열).

### Fix 후보 F (신규, P2) — 09시를 혼잡 시간대 레버에 넣을지 다음 장전이 최종 결정하게 강제

**문제.** `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`(`mahdi/main.py:275`)에 09시가 없다.
6일째(NEXT_TODO:275) 이월된 이 결정이 오늘 처음 실측 근거(절벽 4-1)를 얻었다.

**방향.** 내일(09-09) 장전 회차가 이 파일의 4-1·4-2를 근거로 (A)/(B)를 **반드시** 선택하게
`docs/동작점검/tools/phases.md` A-2에 "전전 거래일 이후 3회 이상 미뤄진 결정은 다음 장전이
그 자리에서 반드시 (A)/(B) 중 하나를 명시적으로 고른다(유예 재연장 금지)"는 조항을 추가한다.
대상 파일: `phases.md`. 회귀 위험: 없음(문서 조항). 검증: 09-09 장전 보고서에 실제로 (A)/(B)
선택 문구가 나타나는지 확인.

### Fix 후보 E (P2, 이월 — 09-07 Fix 후보 B 그대로) — 레버 무조건발동일 자동 정리

이미 켜진 레버의 지난 무조건발동일을 "실제로 켠 날짜"로 갱신하는 것을 반자동화할지 검토.
우선순위 낮음, 다음 장후 세션에서 논의.

### Fix 후보 G (신규, P2) — 검열된(`>=4.0` 하한) p50/p95 값의 실제 소요시간 계측

**문제.** 오늘 15:06~15:31 구간의 p50·p95가 `>=4.0`(하한)으로만 찍혀, "타임아웃을 늘리면
몇 %가 더 들어왔을까"를 계산할 수 없다(증거 §5-1, "레버보다 계측이 먼저다").

**방향.** `mahdi/broker/rest_client.py`의 `inquire-price` 호출에 타임아웃 발생 시에도
실제 경과시간을 별도 필드로 남기는 계측 추가(타임아웃으로 예외가 나도 `time.monotonic()`
차이는 잴 수 있다). 대상 파일: `mahdi/broker/rest_client.py`. 회귀 위험: 낮음(관측 전용
필드 추가). 검증: 다음 절벽일에 그 필드가 4.0초를 넘는 실측값을 내는지 확인.

---

## 제5부 — 고도화 방안 (당일 관측 근거만)

1. **절벽 5개 표본의 시작·지속·컷오프 관계가 매번 다르다는 것 자체가 패턴이다.**
   08-14(14시대, 84분)·08-26(13:40, 85분)·09-01(15:00, 두 파도 7분)·09-03(14:31, 53분,
   컷오프를 삼킴)·09-08(15:01, 28분, 컷오프는 비켜감). 다섯 번째 표본이 "회복은 항상
   마감+5~6분"이라는, 넷까지 유지되던 유일한 규칙성마저 깼다(오늘 +10~16분). 이는
   `cliff_episodes.md`가 이미 표로 추적 중인 질문(다섯 번째가 나오면 물을 것)에 대한
   실제 답이므로, 다음 장후 세션이 그 파일에 09-08 열을 추가할 것을 제안한다(이 회차는
   그 파일이 dev_memory 3파일에 속하지 않는다고 판단해 직접 손대지 않았다 — 아래
   dev_memory 반영 초안 참조).
2. **지연 경고선(0.8배)을 "선행 신호"로 믿는 관행이 오늘로 세 번째 무너졌다.** 09-01·
   09-03에 이어 오늘도 경고선과 위험선이 같은 5분 창(15:06)에서 동시에 뚫렸다 —
   `NEXT_TODO.md`:258의 "선행 신호 → 동시 신호 재정의" 질문에 세 번째 표본이 쌓였다.
   사람의 결정 사안이라 이 보고서가 결론 내지 않지만, 표본이 3/5로 늘었다는 사실 자체는
   다음 결정에 참고할 근거로 명확히 적어 둔다.
3. **워치독 회복 이력 인용(fix-a-2026-09-03-p2-3)이 실전에서 정확히 의도대로 작동했다.**
   다음 단계는 이 참고 문구가 사람의 실제 재기동 판단에 도움이 됐는지를 사후에 기록하는
   것 — 오늘은 사람이 개입할 필요 자체가 없었지만(4-1 "개입 불필요" 판단), 다음에 더
   애매한 절벽(예: 45분 이상 지속)이 오면 이 인용문이 실제 결정을 좌우할 첫 사례가 될 것이다.

---

## dev_memory 반영 초안 (장후 세션이 실제로 반영 — 이 회차는 dev_memory 3파일을 직접 고치지 않았다)

- **NEXT_TODO 후보(신규)**: "[ ] 절벽 5번째 표본(09-08, 15:01~15:28, 28분) — `cliff_episodes.md`에
  열 추가할 것. 회복 시각이 처음으로 마감+5~6분 패턴을 벗어났다(워치독 기준 +10분, REST
  지연 p50 축 기준 +16분). 경고선/위험선 동시 돌파(간격 0분)는 세 번째로 재확인됐다."
- **NEXT_TODO 후속(기존 항목 갱신)**: "[ ] 사전 대응 규칙 발동 결정이 09-04·09-07·09-08
  3연속 미뤄졌고, **그 대가가 09-08 오후 실제 절벽으로 나타났다.** Fix 후보 D(결정 미룸
  카운터)·Fix 후보 F(3회 초과 시 다음 장전 강제 선택) 우선순위를 P1로 상향 제안."
- **DECISION_LOG 후보**: "[MW0601] 관측: 2026-09-08 절벽(15:01~15:28, 28분) — 09-03 이후
  다섯 번째 표본. `2026-09-03-p2-3-watchdog-cites-recovery-history` fix가 실전에서 첫
  발동(15:10:02 DEGRADED 첫 줄에 과거 3건 회복 이력 인용 확인). `2026-09-03-adv3-cliff-day-flags-the-hypothesis-table`도
  동시에 실전 확인(§0-1 절벽 배너 정상 발동). 두 fix 모두 `성립(확인)` 제안, 확정은 사람 몫."
- **hypotheses.yaml 확정 대기(사람 몫, 이 회차는 상태를 바꾸지 않음)**: 위 4-2 표의 10건 —
  특히 `2026-09-07-fix-a` · `2026-09-07-fix-c` · `2026-09-03-adv3` · `2026-09-03-p2-3`
  4건은 오늘 실측으로 확인 근거가 뚜렷하다.

---

## 사용자 조치 (최종본 — 이 절이 오늘의 최종 사용자 조치다. 위 장전 절의 「사용자 조치」는
그 시점의 스냅샷으로 그대로 보존한다)

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (즉시)

**사람 조치 없음.** 절벽(4-1)은 이미 15:30~15:36 사이 저절로 해소됐고, 장이 마감돼
새로 개입할 대상이 없다.

### 오늘 중 / 이번 주 안 (급하지 않음)

1. **사전 대응 규칙(09시 포함 여부)을 다음 장전(09-09)에는 반드시 결정한다.** (근거: 1-1
   → 4-1, 3연속 미룸이 오늘 실제 절벽으로 이어졌다) 선택지는 장전 §1-1과 동일 — (A) 재기동해
   09시 추가 vs (B) 유예 지속. **이번엔 (B)를 고르더라도 그 사유를 다음 장전 보고서에
   명시할 것**(사유 없는 유예는 결정이 아니라 망각이다).
2. **이벤트 캘린더 `covered_through`를 9월분으로 갱신한다.** (근거: 1-2, 13일째)
3. **가설 11건을 검토해 확정한다.** 특히 위 4-2에서 확인 근거가 뚜렷한 4건
   (`2026-09-07-fix-a` · `fix-c` · `2026-09-03-adv3` · `2026-09-03-p2-3`)부터.
4. **레버 3건의 무조건발동일을 실제로 켠 날짜로 정리한다.** (근거: 1-4)
5. **`cliff_episodes.md`에 오늘(09-08) 열을 추가한다.** (근거: 4-1, 제5부 1번 — 다섯 번째
   표본이고 회복 패턴이 처음 어긋났다)

### 결정이 필요한 것 (자동으로 못 하는 것, 급하지 않음)

6. **`.claude/`·`dashboard_build_prompt_1.md` 커밋 또는 `.gitignore` 등록 여부.** (근거: 1-5,
   16일째)
7. **지연 경고선(0.8배)을 "선행 신호"에서 "동시 신호"로 재정의할 것인가.** (근거: 제5부 2번,
   3/5 표본이 간격 0분) — `docs/동작점검/README.md` P1-4 규약 문구를 고치는 사람의 결정.
8. (만성, 재확인만) ZN/ES 해외선물 거래소 이용 신청 여부, `MEMBER_DROPOUT_ALERT_MIN` 문턱
   재검토, 매매 실행 경로(실주문) 배선 시점 — 오늘 절벽(4-1)이 배선 이후의 위험을 구체적으로
   보여줬으니, 배선 시점을 정할 때 Kill Switch(`data_quality_fail`) 설계를 먼저 검토할 것.
9. `DECISION_LOG.md`의 09-03 두 절 위치 정리 여부(사소, 사람 몫).
---

## 제6부 — 장후 자동조치 구현 결과 (16:26~17:0x · 자동)

> 이 절은 **append**다. 앞 회차 본문은 한 글자도 고치지 않았다 — 정정이 필요한 것은
> 이 절 안에서만 적는다.
> 가드 4종 통과: 장후 절(586행) · 제4부(782행) 존재 / 완결 표기(866행 「사용자 조치
> (최종본 …)」) / 상태파일 날짜 09-07 ≠ 오늘 / 16:26(15:50 이후) · 마흐디 프로세스 0개.
> 브랜치 `master`. 병행 세션 없음(당일 커밋 0건).
> ⛔ **Slack 토글은 켜지 않았다.** 착수 전 폐기 목록(`NEXT_TODO.md`:2221 · 980)을 손에
> 쥐고 시작했고, 오늘 리포트는 폐기 항목을 하나도 올리지 않았다 — **기각 0건**.

### 구현한 것 — 셋

#### 1. 제4부 Fix 후보 D (P1) — 「아직 안 덮은 시간대가 며칠째인가」 (커밋 `d84415f`)

**왜.** 오늘 §4-1이 적은 「3연속」이 **어느 산출물에도 없는 수**였다. 다음 회차가 이어
세려면 앞 회차의 문장을 읽고 1을 더해야 하고, 세는 규칙이 없으니 회차마다 다른 수가 나온다.

**⚠ 계획을 다시 세웠다.** 리포트는 *"전일 `_지표.md`에 `🔔 연속 판정 성립`이 있고 그 다음
거래일 장전 보고서가 결정 문구 없이 마감되면 세라"* 고 적었다. 실물을 확인하니 두 가지가
어긋났다:

- **🔔은 08-06부터 거의 매일 뜬다**(23개 사이드카 중 23개). 그것을 세면 **25일**이 되고,
  이 저장소가 스스로 적어 둔 규약(*"틀린 경보는 진짜 경보를 죽인다"*)에 정면으로 걸린다.
- **보고서에서 「결정 문구」를 파싱하는 것이 불안정하다.** 「결정 선택지」 블록 표기가
  회차마다 다르다(08-24·08-31·09-08만 그 형식이고 나머지는 산문이다).

그래서 **재는 대상을 바꿨다**: 「🔔이 뜬 날 수」가 아니라 **「🔔에 뜬 `inquire-price`
시간대 중 혼잡 레버가 아직 안 덮은 것」**이다. 이렇게 재면 ① 산문이 아니라 사이드카
(`rest_latency.p95_by_hour`)에서 나오므로 **매 회차가 같은 값을 다시 얻고**, ② 사람이
레버를 켜는 순간 **다음 거래일에 저절로 0으로 떨어진다**(「닫혔다」를 적어 줄 필요가 없다).
**주장은 그대로이고 가리키는 자리만 옮겼다.**

**그래서 나온 수 — 산문의 3이 아니라 5다.**

| 날짜 | 이틀 연속 성립(price) 중 레버가 안 덮은 시 |
|---|---|
| 2026-09-08 | 9시·15시 |
| 2026-09-07 | 9시·15시 |
| 2026-09-04 | 15시 |
| 2026-09-03 | 15시 |
| 2026-09-02 | 9시·15시 |
| 2026-09-01 | 없음 — 여기서 끊긴다 |

> 🔴 **산문이 놓친 것은 15시다.** 09-02부터 **닷새 내리** 안 덮였는데 **어느 회차도 15시를
> 결정 대상으로 올린 적이 없다** — 장전·장중·장후 전부 9시만 보고 있었다. 오늘 절벽이
> 시작된 시각이 **15:01**이다.

**바꾼 것.** `docs/동작점검/tools/collect_evidence.py` — 상수 3개
(`PREEMPTIVE_LEVER_KEY` · `PREEMPTIVE_ACTIONABLE_ENDPOINT` · `PREEMPTIVE_STREAK_MAX_DAYS`),
함수 6개(`congested_lever_hours` · `_sidecar_latency` · `_price_breach_hours` ·
`_previous_latency_day` · `preemptive_uncovered_history` · `preemptive_uncovered_streak`),
새 절 **§8-4**(§8-2·§8-3 바로 옆 — 「아직 안 정한 것」 계열이다).

**⛔ 판정하지 않는다.** 임계도 경보선도 없다. 이 절이 적신호를 내는 경우는 **「레버를 못
읽었다」 하나**뿐이고(규약 C), 그것을 테스트가 **소스 수준에서** 못 박는다
(`test_section_raises_a_flag_only_when_it_could_not_measure` — `flags.append`가 그 분기
안에 정확히 하나여야 통과한다).

**⚠ `inquire-price`만 센다.** 규칙이 정한 조치가 위클리 옵션 조회 주기라, 잔고만 걸린
시간대는 이 레버로 덮을 대상이 아니다. 그것까지 세면 08-28·08-31·09-01이 거짓으로 걸려
연속이 5 → 9로 부푼다.

**⚠ 규약 C 두 자리.** ① 레버를 못 읽으면 `None`이지 빈 집합이 아니다(빈 집합으로 접으면
전 시간대가 거짓 미덮임이 된다). ② `p95_by_hour`가 빈 사이드카는 **거래일로 안 센다** —
08-17(대체공휴일)이 그 형태였고, 그날을 세면 08-18의 직전 거래일이 08-14가 아니라 08-17이
돼 §5의 🔔과 **두 곳이 다른 날을 가리킨다.**

**걸린 시간.** 약 25분(설계 재수립 포함). **안 하면.** 다음 회차도 앞 회차 산문을 읽고
1을 더해야 하고, 15시는 계속 아무도 안 본다.

#### 2. 제4부 Fix 후보 F (P2) — 유예 재연장 금지 조항 (커밋 `67677af`)

**왜.** 종전 `phases.md` A-2 조항은 *"안 켜기로 정했으면 그 사유를 그날 보고서에 적는다"*
까지였다. **사유를 적는 것은 유예를 막지 못한다** — 09-04·09-07·09-08 세 번 다 사유도
안 적혔다. 「사유 없는 유예는 결정이 아니라 망각이다」라고 써 놓고 세 번 망각했다.

**바꾼 것.** `docs/동작점검/tools/phases.md` A-2에 두 항목 추가 — ① §8-4가 며칠째인지를
인쇄한다는 안내 ② **연속 3거래일 이상이면 그 자리에서 (A)/(B) 중 하나를 명시적으로
고른다(유예 재연장 금지)**. 「오늘도 지켜본다」는 선택지가 아니다.
09-08 실측(연속 5거래일 · 미덮임 9시·15시)을 조항 안에 박아, **산문이 아니라 §8-4의 수를
보고 고르라**고 적었다.

**걸린 시간.** 약 5분. **안 하면.** 09-09 장전이 네 번째로 그냥 지나간다.

#### 3. 제5부 고도화 1 / 사용자 조치 5 — `cliff_episodes.md` 09-08 열 (커밋 `5bf1282`)

**왜.** 그 파일이 **09-03에 미리 걸어 둔 두 질문**("다섯 번째가 나오면 물을 것")에 오늘
답이 나왔다. 표본을 모으는 자리에 표본이 안 들어가면 그 질문들은 영영 안 닫힌다.

**답 둘.**
1. **회복이 네 번째로 마감+6분인가? → 아니다.** 워치독 기준 15:30(+10분) · REST 지연 p50 축
   기준 15:36(+16분). 넷까지 유지되던 **유일한 규칙성**이 깨졌다 — 09-03이 걸어 둔
   *"세 번째로 같으면 KIS 쪽의 어떤 주기"* 를 「알려진 주기」로 올릴 근거는 서지 않았다.
2. **간격 0분이 다시 안 나오는가? → 아니다. 세 번째로 0분(3/5).** 09-03이 걸어 둔 조건
   (*"0분이 세 번 연속이면 경고선은 선행 신호가 아니다로 굳는다"*)이 **성립했다.**
   ⛔ 그래도 **이 파일이 규약을 고치지 않았다** — `README.md` P1-4 문구는 사람 결정이다.

**새 축 하나** — 「신규 진입 컷오프(14:50)와의 관계」. 09-03은 컷오프를 **삼킨** 절벽이고
오늘은 컷오프 **밖**이다(절벽 시작 15:01이 11분 뒤). 규약대로 모르는 과거 칸은 `—`다.

**🔴 새로 보인 것 — 우리 쪽 압력이 넷과 반대 방향이다.** 08-26·09-01은 *"우리 쪽 압력이
낮은데 지연이 나빠졌다"* 가 KIS 귀속 근거 하나였는데, 오늘은 백오프 최대가 전일 1.5 →
**2.25배로 올랐다.** 이것을 「우리가 원인」으로 읽지 않는다 — **백오프 확대는 지연의
결과이기도 하다**. 방향을 가르는 축은 총 수요이고 그쪽은 초당 0.306 → **0.291로 낮아졌다**.
⚠ **앞으로 KIS 귀속을 말할 때 백오프 축을 인용하면 오늘은 반대로 읽힌다 — 초당수요 축을 쓸 것.**

**걸린 시간.** 약 10분. **안 하면.** 다섯 번째 표본이 유실되고, 특히 **회복 패턴이 처음
어긋난 사실**이 기록되지 않아 워치독 참고 문구가 낡은 채로 계속 인용된다.

### 이미 반영돼 있어 **안 한 것** — 제4부 Fix 후보 G (P2)

**리포트의 진단이 두 군데서 어긋났다.**

- **① 그 계측은 이미 있다.** `mahdi/broker/rest_client.py:848~857`의 `finally` 블록이
  타임아웃 경로에서도 `time.monotonic() - http_started`를 재서 `_log_if_slow(...,
  timed_out=True)`로 넘긴다. 주석에도 *"예외(타임아웃 등)로 끝난 호출이야말로 계측이
  필요하다 — finally에서 재고 넘긴다"* 고 적혀 있다(08-05 Fix#4).
- **② 그 값으로는 묻는 것에 답할 수 없다.** 리포트가 답하려던 질문은
  *"타임아웃을 늘리면 몇 %가 더 들어왔을까"* 인데, 타임아웃 경과시간의 상한은 **우리가 건
  타임아웃(4.0초)**이다 — 클라이언트가 거기서 포기하므로 **KIS가 얼마나 더 걸렸을지는
  원리적으로 못 잰다.** p50/p95가 `>=4.0`으로 검열되는 것은 계측이 없어서가 아니라
  **관측을 거기서 끊었기 때문**이고, 수집기 주석(`SLOW_CALL_RE` 위, 08-23)이 이미
  *"「6초로 늘리면 몇 %가 더 들어오는가」를 이 데이터로는 계산할 수 없다"* 고 적어 두었다.

**그래서 구현하지 않았다.** 검열을 푸는 유일한 길은 레버
`OPTION_CHAIN_READ_TIMEOUT_SECONDS`를 실제로 늘려 보는 것이고, 그것은 실행 파라미터 변경 =
**사람 결정**이다. `NEXT_TODO.md`에 기각 사유와 함께 올렸다.

### C등급으로 미룬 것 — 셋

| 항목 | 사유 |
|---|---|
| **제5부 고도화 2** — 지연 경고선을 「선행 신호」에서 「동시 신호」로 재정의 | 🔴 **임계 재정의 = 사람 결정.** 사용자 조치 7번(「결정이 필요한 것」)에도 같은 항목이 있다. 오늘 표본이 3/5가 됐다는 사실만 `NEXT_TODO`·`cliff_episodes.md`에 적었다 |
| **제4부 Fix 후보 E** — 레버 무조건발동일 자동 정리 | 리포트 자신이 *"우선순위 낮음, 다음 장후 세션에서 논의"* 로 미뤘고 **예측치가 비어 있다**(규약상 「방안이 아니라 아이디어」). 09-07 Fix 후보 B·09-04 P2-A와 **같은 자리다 — 셋을 한 건으로 볼 것** |
| **제5부 고도화 3** — 워치독 인용이 사람 판단에 도움이 됐는지 사후 기록 | 대상 파일도 예측치도 없다. 오늘은 사람이 개입할 필요 자체가 없었고(§4-1), 45분 이상 지속되는 애매한 절벽이 와야 첫 사례가 된다 |

### 폐기 목록 위반으로 기각한 것 — **0건**

오늘 리포트는 폐기 항목을 하나도 올리지 않았다. Slack 토글도, GEX 광폭 체인도, 폴러별
전용 페이서도, httpx INFO 축소도 없었다.

### `hypotheses.yaml` — 등재 1건 + 확정 6건 (커밋 `47074de`)

**등재(코드보다 먼저, 숫자를 보기 전에).**
`2026-09-08-fix-d-preemptive-decision-deferral-streak` · 검증예정일 **09-09** ·
주장 `rest_latency.warnings`(수기) · 대가 `rest_latency.p95_warn_threshold`(`== 2.5`) ·
`levers`(수기, 판정 무변경).

**확정 6건** — 리포트 §4-2가 **판정 제안과 근거 표를 함께 제시한 것만** 그 제안대로 고쳤고,
근거는 산출물로 **다시 확인한 뒤** 적었다.

| id | 확정 | 재확인한 실측 |
|---|---|---|
| `2026-09-07-fix-a-watchdog-invocation-gap` | `confirmed` | 비율 **1.25**(≤5) · 호출 592회 · 최대 호출 공백 1.2분 · `invocation_trail_available: true` |
| `2026-09-07-fix-c-monthly-total-failure-count` | `confirmed` | `total_failure_cycles` **4**(10:00·14:10·14:43·15:00) ≤ `failed_cycles` **10** |
| `2026-09-03-adv3-cliff-day-flags-the-hypothesis-table` | `confirmed` | `지표.md`:23에 절벽 배너 실측 — **오늘이 그 절벽일이었다** |
| `2026-09-03-p2-3-watchdog-cites-recovery-history` | `confirmed` | `watchdog.log` 15:10:02 첫 줄에 과거 3건 인용문 실림 — **첫 실전 발동** |
| `2026-09-04-p2-b-isolated-zero-row-count` | `confirmed` | `zero_row_isolated_count` 0 · `zero_row_count` 28 — **28 = 0 + 28** |
| `2026-09-02-fix10-phase-offset-55s` | `confirmed` | `stale_pct` 0.8(≤20) · `p50` **54.9초**(≤55.0) — 여유 0.1초 |

**손대지 않은 4건** — 제안이 「`pending` 유지」·「판정 불가」·「날짜 미정」이라 규칙대로 뒀다:
`2026-08-12-eE-on-congested-hours`(대가 지표 오염) ·
`2026-08-26-p1-3-preemptive-rule-fires-at-premarket`(부분 확인) ·
`2026-08-12-fix7-regime-session-window-restore`(규약 H — 레버가 꺼져 있다) ·
`2026-08-19-fix2-roundtrip-leaves-db-evidence`(왕복 자체가 없다).

> ⚠ **확정 대기가 15건 → 6건으로 줄었다.** 09-04·09-07 두 회차가 「사람 몫」으로 넘긴 것이
> 쌓여 §12 적신호로 매일 올라오던 항목이다. 이 회차는 스킬 규약의 예외(제안+근거 표가
> 함께 있는 것만)를 적용했고, **각 `검정`에 「어디까지 확인했고 무엇은 확인하지 않았는지」를
> 명시**했다(예: fix-a·fix-c의 09-07 재집계는 돌리지 않았다). 되돌릴 수 있는 형태다.

### 테스트

**2,252 → 2,268 (+16, 0 failed).** 신규 파일 하나 —
`tests/test_evidence_collector_preemptive_streak.py`(16건).
레버 파싱 4 · 사이드카/규약 C 4 · 연속일 5 · **판정 무변경(소스 수준) 2** · 상한 1.

**「판정 무변경」은 실측으로도 확인했다.** HEAD 수집기(`git show HEAD:...`)와 새 수집기를
**같은 시각에** `--date 2026-09-08`로 돌려 §12 적신호 25줄을 대조했고, **차이는 벽시계
1분뿐**이었다(「post 점검이 53분 밀렸다」 → 「54분」). 나머지 24줄은 한 글자도 다르지 않다.

⚠ **`ruff`는 `.venv`에 미설치라 못 돌렸다** — 09-02·09-03·09-04·09-07에 이어 **다섯 회차
연속**이다. `NEXT_TODO`에 「설치할지 안 쓰기로 확정할지」로 올려 뒀다.

⚠ **오늘 `증거_post.md`는 재생성하지 않았다**(09-04의 *"재생성은 절반만 복구다"* 주의를
따랐다). 그래서 **§8-4는 09-09 회차 산출물부터 실린다** — 오늘 값은 위 표가 정본이다.

### 커밋 — 여섯

| 해시 | 내용 |
|---|---|
| `bb4aa84` | 09-08 점검 산출물(일일점검 + `지표.json`) |
| `47074de` | 예측치 선등재 1건 + 판정 제안 6건 확정 |
| `67677af` | Fix F — `phases.md` A-2 유예 재연장 금지 조항 |
| `d84415f` | Fix D — 수집기 §8-4 + 테스트 16건 |
| `5bf1282` | 고도화 1 — `cliff_episodes.md` 09-08 열 |
| (아래) | dev_memory 3파일 + 이 절 |

**⚠ `_지표.md` · `_증거_*.md` · `_지연창.tsv`는 커밋되지 않았다.** `.gitignore:52`의
`docs/동작점검/auto/*`가 걸러내고 **`_지표.json`만 예외**다(09-07 회차와 같다).
그런데 **로그는 이틀치만 남으므로 잃으면 다시 만들 수 없고**, 특히 `_지연창.tsv`는 창
단위 `p50/timeout`의 유일한 기록이며 `cliff_episodes.md`가 매 표본마다 인용한다.
자동조치가 `.gitignore`를 고치지 않았다(저장소 규약 변경 = 사람 결정) — `NEXT_TODO`에 올렸다.

### 사용자 몫으로 남은 것

1. 🔴 **09-09 장전: 혼잡 레버에 9시·15시를 넣을지 (A)/(B) 중 하나를 고를 것.**
   오늘 생긴 조항이 강제한다. **15시를 빠뜨리지 말 것** — 닷새째인데 아무도 안 올렸다.
2. 이벤트 캘린더 `covered_through` 9월분 갱신(조치 2, **13일째**).
3. 레버 3건 무조건발동일 정리(조치 4).
4. `.claude/`·`dashboard_build_prompt_1.md` 커밋 여부(조치 6, **16일째**) — 손대지 않았다.
5. 지연 경고선 재정의 여부(조치 7) — 표본 3/5.
6. ZN/ES 신청 · `MEMBER_DROPOUT_ALERT_MIN` 문턱 · 매매 경로 배선 시점(조치 8).
7. `DECISION_LOG.md` 09-03 두 절 위치 정리(조치 9) — 남의 절은 옮기지 않았다.
8. `ruff` 설치 여부 · `.gitignore` 산출물 예외 여부(이 회차가 새로 올린 둘).

### 부수 정정 — 앞 회차 본문은 안 고쳤다

**「위험선 최초 돌파 11:01」(장중① §2-1)은 최초가 아니다.** 장중②(14:30) 재대조가 실제
최초를 **10:01:06**으로 확인했고, 11:01은 *"그 시점까지의 최악"* 이었다. 09-07 §1-9의
「회복률 72.7%」(전일 값이 붙은 것)와 **같은 형태의 실수**다 — 인접한 두 수가 붙는다.
**How to apply**: 「최초 N」을 인용할 때는 그때까지 본 창이 아니라 **전체 창을 다시 훑고**
적을 것. `DECISION_LOG.md` 2026-09-08 절에 남겼다.

**이 파일은 여기서 닫힌다.**
