# 마흐디 운영점검 보고서 — 2026-08-07 (금)

> **이 보고서는 반나절(07:30~12:20, 287분) 실측 위에서 쓰였다.** 08-06(472분)과의 **건수 비교는
> 그대로 읽으면 안 되고** 비율·중앙값만 비교 가능하다. 장마감 자동 집계가 종일 값으로 다시
> 내며, 그때 재판정할 항목은 `NEXT_TODO`에 적어 뒀다.
>
> 자동 집계: `docs/동작점검/auto/2026-08-07_지표.{md,json}`

---

## 요약 — 롤은 잘못이 없었다. 잘못은 롤의 대가를 아무도 안 센 것이다

어제 넣은 것들은 대부분 먹혔다. 먼슬리 설계 미만 분이 **27.1% → 1.0%**, 예산 컷 43회 → 8회,
백오프 최대 2.25배 → 1.00배, 결손 21분 → 0분.

그런데 그렇게 인프라가 조용해지자, **넉 달 동안 자기가 잰다고 주장한 것과 다른 것을 재고 있던
지표 둘**이 드러났다.

1. `chain_oldest_leg_age_seconds`(임계 180초)는 **먼슬리 폴링 지연**을 잰다고 적혀 있었는데
   실제로는 **ATM 롤 잔상**을 재고 있었다. 그래서 08-06 250.086초 / 08-07 250.139초로
   **창 길이에 붙어 고정**돼 있었다.
2. `atm_roll_count_today`(임계 20회)는 "중간점 근처에서 오간 결과"를 잰다고 적혀 있었는데,
   08-07에 롤 76회를 **전수 검산**하니 전부 정당한 롤이었다. 그날 지수가 40p를 움직였다.

두 지표가 가리키던 진짜 문제는 하나다: **장중 판단의 78%가 서로 다른 두 개 이상의 ATM 창을
섞어 GEX를 계산하고 있었고, 그 창 교체가 옵션 1분봉을 매번 끊고 있었다.**

---

## 1. 장전 (07:30 ~ 09:00)

### 1-1. 07:31 옵션체인 1분이 통째로 비었다 — 기동 첫 사이클의 분 라벨이 한 칸 앞으로 밀렸다

`option_analysis_1m`: 07:30 = 20행, **07:31 = 0행**, 07:32 = 20행. 로그 축은 결손 0분
(07:31:19에 rows=20으로 완주). 두 축이 어긋난 이유가 이것이다.

원인은 [main.py](../../mahdi/main.py)의 `poll_time = db.local_now().replace(second=0, microsecond=0)` —
**벽시계에서 바닥을 깎는다.** `_sleep_until_first_wall_tick`/`_advance_fixed_tick`이 깨우는 시각은
격자점(:00)이지만 `asyncio.sleep`은 일찍 깨는 것을 금지하지 않는다. 07:30:59.9x에 깨면 07:30으로
내려깎여, 스케줄러가 의도한 07:31이 아니라 직전 분에 적재된다. 그 분은 다음 사이클이 07:32를
쓰므로 영영 빈다.

하루 1분이라 통계로는 안 보이지만 **경계 레이스**라 밀림이 큰 날엔 정상 사이클에서도 날 수 있다.

### 1-2. ZN/ES 권한 미신청 (각 5건) — 계정 사안, 폴백 정상

`EGW00552`(CBOT) / `EGW00550`(CME). yfinance 폴백이 받고 있어(`zn_front=108.48`) 판단은 안 끊긴다.

`zn_front` non-null 5행 / `us10y_yield`·`usdkrw`·`move_index` non-null 1행은 **LOCF(4일 창,
[db.py:227](../../mahdi/data/db.py))가 설계대로 흡수**하므로 결함이 아니다 — 확인했다.

### 1-3. 장운영정보(H0UNMKO0) 수신 누적 0건인데 배지는 초록이었다

구독은 07:30:50 확립. `market_halt_event_history` **0행**, `last_message_at` **NULL**.
07-05 이후 넉 달 동안 이 경로가 살아 있다는 증거가 한 번도 없었다.

08-03에 구독 성립 판정을 넣어 (C)의 빈 칸을 메웠는데, 그것으로도 안 채워지는 칸이 하나 남아
있었다: **구독이 성립해도 데이터가 실제로 오는지는 여전히 모른다.**

---

## 2. 장중 (09:00 ~ 12:20)

### 2-1. ⛔ [최우선] 장중 판단의 78%가 서로 다른 두 ATM 창을 섞어 GEX를 냈다

`chain_leg_count` × `chain_oldest_leg_age_seconds` 교차(장중 202분):

| 스냅샷 레그 | 분 | 최고령 평균 | 의미 |
|---|---|---|---|
| **10 (설계)** | 45 | **62초** | 롤 없이 한 창으로 계산 |
| 12 | 88 | 185초 | 롤 잔상 1세트 혼입 |
| 14 | 45 | 191초 | 2세트 |
| 16 | 20 | 229초 | 3세트 |
| 18 | 3 | **250초** | 4세트 |

초과분은 **ATM 창 밖으로 빠져 더 이상 폴링되지 않는 행사가**이고, `DISTINCT ON`이 그 마지막
값을 5분 창 만료까지 붙들고 있다. GEX는 OI 가중 합이라 행사가가 늘면 |GEX|가 그만큼 부풀고,
그 값이 v6 §11.4 프리미엄 매도 게이트의 입력이다. 오늘 GEX는 전 구간 음수(평균 −1.4e10)였고
`short_gamma_requires_not_met` 49건이 거기서 나왔다 — **거부 사유의 근거가 오염된 입력일 수 있다.**

COCKPIT Gamma Map이 설계 5행사가가 아니라 **8행사가(974~988)** 를 그린 것도 같은 원인이다.

이것은 5분 창의 **문서화된 대가**다(08-04 §2-2). 새 결함이 아니라, 오늘 처음 그 대가의 크기를
쟀다 — 그리고 그 크기는 `chain_leg_over_design_minutes` 기준 **210분**이었다.

### 2-2. ⛔ [최우선] ATM 롤 76회 · 즉시 왕복 31.6% — WS 구독을 798회 갈아엎었다

- 롤 76회 / 287분 = **3.8분마다 1회**. 배지 임계 20회의 3.8배.
- WS 구독 요청 410건 / 해제 388건. 롤 1회당 옵션 5~10종목이 끊기고 다시 붙는다.
- **Flow Radar의 「봉 없음 8분」은 이것의 직접 결과다.** `BAFBRW980` 실측:
  `11:35:00 구독 해제` → `11:36:59 재구독` → `11:42:59 또 해제`.
  유동성 공백이 아니라 **우리가 끊은 것**이다.

**히스테리시스(0.75)는 고장 나지 않았다.** 롤 76회를 전수 검산하면 전부 임계(1.875p)를 정당하게
넘었다 — 오늘 지수가 1004→964→982(40p)를 오갔고 격자가 2.5p라, 어떤 임계값을 써도 이 왕복은
남는다. 08-04 리플레이가 이미 같은 결론을 냈고 코드 주석이 "그 수정은 구독 정책 변경이라
별건"이라고 적어둔 그 별건이, 오늘 실측으로 청구서가 왔다.

> 참고: 로그 228줄 / 이벤트 76건은 **3북 × 1이벤트**로 정상이다(중복 발사 아님 — 확인했다).

### 2-3. `chain_age_max` 250초 경고는 매일 뜨는 구조적 오경보였다

임계 180초의 근거는 [db_metrics.py](../../mahdi/ops/db_metrics.py)의 *"먼슬리가 2사이클 이상
밀렸다"*. 그런데 §2-1의 층화가 보여주듯 250초는 **12레그 이상 분에서만** 난다 — 10레그 분의
최고령은 최대 130초로 임계 아래다.

08-06 예측 `p4`(`chain_age_max ≤ 180`, `chain_leg_median == 10`)가 반증으로 찍혔지만,
**반증된 건 시스템이 아니라 예측이다** — 5분 창을 계산에 넣지 않고 세운 예측이었다.
`hypotheses.yaml`에서 `confirmed`로 닫고 검정란에 틀린 지점을 남겼다.

### 2-4. ReadTimeout 92건 — KIS 귀속, 사전 대응 규칙 발동 조건 이틀 연속 성립

- `read_timeout` 92건(줄 51 + 억제 41), 옵션체인 레그 실패 46건 중 43건.
- `inquire-price` p95: 10시 3.16초 / 11시 3.03초 / 12시 3.58초 — **08-06과 같은 세 시간대에서
  이틀 연속 2.5초 초과.**
- §17 교차점검 모순 1건: 백오프 최대 1.00배(우리는 한가)인데 타임아웃 92건 → **KIS 귀속**.
  스케줄링으론 안 줄어든다.

**레버는 계속 내려 둔다.** 08-06 고도화#4가 "고도화#1이 먼슬리 두께를 회복시키면 감축은 불필요"
라며 내려뒀는데, 오늘 먼슬리 두께는 27.1% → 1.0%로 실제 회복됐다. 그 판단이 맞았다는 뜻이다.

### 2-5. `regime_hmm` 212분 전량 중립 — 「가용 4멤버」의 실질은 2.15개

- 실질 평균 **2.15** vs 가용 3.16(죽은 축 1.02). 08-06 고도화#2의 계측이 정확히 잡아냈다.
- 멤버별: `regime_hmm` 212분 전부 0점 / `options_flow` −0.675 / `flow_position` +0.467 /
  `orderflow_ofi_vpin` −0.033.
- **부호 일치율 `flow_position ↔ options_flow` 22.3%** — 197분 중 44분만 부호가 같았다.
  무작위면 50% 근처여야 한다.
- `feature_store` 7,634 / 8,000 (**95.4%**) — **08-10(월) 도달**.

### 2-6. 옵션 WS 1분봉이 사실상 안 쌓인다

`market_raw_1m` 532행 / 212분인데 심볼별로는 선물 `A01609` 215분, 그다음이 `BAFBRW980` **66분**,
나머지는 한 자릿수~수십 분. 21개 옵션을 구독하고도 **분당 평균 1.5종목**만 봉이 생긴다.
§2-2의 구독 교체가 이걸 더 잘게 부순다 — `orderflow_ofi_vpin`의 입력이 바로 이 테이블이다.

### 2-7. 판단 파이프라인 자체는 건강하다 (이상 없음 — 명시)

287분 전부 판단 생성, `risk_gate_state` 23종(고정 출력 아님), ENTER 113건(STANDARD 61 /
SMALL_TEST 52), 컷오프 차단 0분·위반 0건, `meta_label:no_trade` 75건은 **전부 07~08시 장전**
(장중 0건). 감마플립 0%는 이 북의 정상값이고 **범위 밖 0건 불변식이 지켜졌다.**

지수 정지는 총 1분 / 최장 연속 1분 — 08-04·08-05의 9~15분에서 크게 줄었고 **3일 연속 재현
안 함**이므로 원인 규명 착수 조건이 성립하지 않았다.

---

## 3. Fix 구현 결과 (6종, 전부 구현 완료)

> 전부 **08-10(월) 07:30 기동**부터 라이브에 실린다. 오늘 밤 재기동하지 않는다 —
> 08-04 p4에서 커밋 07:45 / 기동 07:30의 15분 차이로 하루를 잃은 전례가 있다.

| # | 내용 | 파일 | 예측 |
|---|---|---|---|
| 1 | 체인 스냅샷을 **최신 사이클의 행사가 범위**로 자른다 | `db._restrict_to_latest_cycle_window` | `2026-08-07-fix1-chain-window` |
| 2 | `chain_age_max` 임계의 의미를 재정의 + 상하한 불변식 | `db_metrics.SIGNAL_REACH_WARNINGS` | 테스트 불변식 |
| 3 | `poll_time`을 분 경계 직전에서 올려 자른다 | `main._grid_poll_minute` | `2026-08-07-fix3-grid-poll-minute` |
| 4 | `2026-08-06-p4`를 **예측 오류**로 확정 | `hypotheses.yaml` | — |
| 5 | `2026-08-06-p2` 대가 지표 판정 보류(반나절 규약 명문화) | `hypotheses.yaml` | 장마감 재판정 |
| 6 | 장운영정보 배지를 **"미검증"** 으로 정정 | `data_source._market_halt_check` | 육안 |

### Fix#1 설계 선택 — 세 후보를 오늘 실데이터 327분으로 리플레이했다

| 후보 | 중앙 레그 | 최대 | 설계 미만 분 | 최고령 중앙/최대 |
|---|---|---|---|---|
| 현행(5분 창 전체) | 12 | 20 | 0분 | 120초 / 300초 |
| 최신 사이클의 행사가 **집합** | 10 | 10 | **1분** ⚠ | 0초 / 0초 |
| 최신 사이클의 행사가 **범위** | **10** | **10** | **0분** | 0초 / **60초** |

집합안은 그 사이클에서 ReadTimeout으로 빠진 레그를 **이월로도 못 살린다**(레그 단위 타임아웃은
08-06에 119건 중 111건 — 상시 현상이다). **범위안 채택.**

**창 길이(시간축)는 안 건드렸다** — 5분 창의 목적(결손 이월)은 여전히 유효하고, 오염의 원인은
길이가 아니라 집합이 겹쳐 쌓이는 것이기 때문이다.

---

## 4. 고도화 구현 결과 (5종, 전부 구현 완료)

### 고도화#1 — WS 구독을 ATM 창에서 분리한다 (`SubscriptionRetentionPool`)

창을 벗어난 구독을 **슬롯이 남는 동안 유지**하고, 슬롯이 필요하면 오래된 것부터 축출(LRU).

- **공짜에 가까운 근거**: WS 구독은 REST 페이서 예산을 1건도 안 쓰고, REST 폴링 대상은
  `desired_strikes`(창)에서 나오므로 유지 구독이 체인 수집을 1콜도 늘리지 않는다.
  08-07 실측 활성 슬롯 21~22/41.
- **구현 중 정정**: 예약 슬롯을 8 → **2**로 내렸다. 8이면 3북 날(설계 32/41)에 유지 가능
  슬롯이 1이 돼 이 고도화가 통째로 무력화된다. 롤 경로의 슬롯 부족은 예약이 아니라
  `ensure_free()`가 그때그때 축출해 해결한다.
- **롤 횟수는 안 줄어든다** — 이 고도화는 롤을 줄이는 게 아니라 롤의 대가를 없앤다.

### 고도화#2 — 롤 지표를 횟수에서 **대가**로 옮긴다 (마이그레이션 028)

`atm_roll_dropped_subs_today`(끊긴 WS 구독 수) + `chain_leg_over_design_minutes`(잔상 레그 분).
배지 판정 축을 후자로 옮겼고, `_ATM_ROLL_WARNING = 20`은 마이그레이션 028 미적용 구간의
**대리 지표**로만 남겼다.

목표 상태: 배지가 **"76회 · 그 때문에 실제로 끊긴 WS 구독 0건"** 으로 초록.

### 고도화#3 — 확신도 분모 전환은 **레버만 만들고 내려 둔다**

`strategy_params`의 `use_effective_member_count`(기본 false). 08-10 재학습 성공 확인
**다음 영업일**에 올린다 — 재학습과 같은 날 켜면 확신도 변화를 둘 중 무엇이 만들었는지
영영 못 가른다.

### 고도화#4 — HMM 드라이런 실행 + **수렴 실패 시 저장 거부**

드라이런(7,682샘플)이 두 가지를 알려줬다:

- ✅ **잠재상태 방문 8/8개** — 08-03 드라이런의 경고("1~2개면 모델 구성을 의심")가 해소됐다.
- ⛔ **`Model is not converging` 다수** (로그우도 델타 음수). `fit()`은 예외를 안 던지므로
  **08-10에 그대로 저장했으면 수렴하지 않은 모델이 조용히 라이브에 올라갔을 것**이다.

→ `monitor_.converged`가 True가 아니면 **저장을 거부**하는 것을 기본값으로 했다.
안 저장하면 WARMUP 폴백이 하루 더 이어질 뿐이지만, 나쁜 모델을 저장하면 그날부터 판단이
그것을 믿는다. 억지로 저장하려면 `--allow-nonconverged`.

### 고도화#5 — 멤버 부호 일치율에 **직전 3영업일**을 붙인다

`daily_ops_report`가 사이드카를 3개 읽어 §14-3에 나란히 낸다. **임계는 걸지 않았다** —
정상 범위를 모르는 상태에서 임계를 먼저 정하면 그 임계가 곧 결론이 된다(08-05 스팟 소스
괴리율에서 같은 실수를 했다).

---

## 5. 08-10 검정 기준선 (오늘 = 적용 전)

| 지표 | 오늘(적용 전) | 08-10 예측 |
|---|---|---|
| `db.signal_reach.chain_leg_over_design_minutes` | **210분** | `== 0` |
| `db.signal_reach.chain_leg_median` | 12 | `== 10` |
| `db.signal_reach.chain_age_seconds_max` | 250.14초 | `<= 150` |
| `db.monthly_leg_completeness.below_design_pct` | 1.4 | `<= 5` (대가) |
| `db.chain_minute_coverage.zero_row_count` | 1 (07:31) | `== 0` |
| `db.ws_status.atm_roll_dropped_subs_today` | NULL(구 코드) | `<= 4` |
| `db.tables.market_raw_1m.rows` | 726(반나절) | `>= 900` |
| `rest.by_group.옵션체인` | 5,426 | `<= 9,500` (대가) |
| `db.feature_store.total` | 7,709 | `>= 8,000` |

전 경로가 자동 집계에서 해석되는 것을 확인했다 — **「경로 없음」 0건**
(08-06에 6건이 「실측 없음」에 묻힌 실수의 재발 방지).

---

## 6. 결정 사항 (사용자 확인)

1. **고도화#1 구현** — 권고대로 진행했다. 파급이 가장 크고 REST 예산을 안 건드려 회귀 위험이
   가장 낮다. Fix#1과 함께 넣었다(같은 병의 두 증상이다).
2. **KIS 지연 사전 대응 레버** — 발동 조건은 성립했으나 **계속 내려 둔다**. 먼슬리 두께가
   이미 회복됐다.
3. **HMM 드라이런** — 오늘 실행했고, 그 결과로 저장 가드를 하나 더 넣었다.

---

## 부록 — 이날의 교훈 한 줄

> 인프라가 조용해지자 계측이 거짓말하고 있었다는 것이 드러났다.
> **지표가 자기 주석대로 재고 있는지는, 그 지표로 설명되는 다른 지표와 층화해 봐야 알 수 있다.**
