# 마흐디 장중 점검 — 2026-08-18 (화) · 12:30 회차 (실발화 13:39)

> ⏰ **밀린 실행 — 예정 12:30 / 실발화 13:39 (69분 지연).** 장중 구간 안이라 **실시간 생존 판정은 유효하다.**
> 예약 `mahdi-intraday-check`의 종전 `lastRunAt`은 `2026-08-17T07:45:48Z`였다 — 오늘 09:00~13:38에
> 이 예약이 발화한 적이 없고, 장전 점검(13:28)·이 회차(13:39)가 **앱 기동 직후 밀린 예약 일괄 발화**로
> 연달아 떴다. 08-17·08-18 장전에 이어 **사흘째 같은 형태**다(장전 §1).
>
> ⚠ **파일명이 `_1340`인 이유**: `docs/동작점검/2026-08-18_점검_intra.md`는 오늘 **12:54에 사용자 수동
> 세션**이 이미 썼고, 그 문서에는 이 세션이 재현할 수 없는 것(코드·마이그레이션 032 변경 기록 §4)이
> 들어 있다. 덮으면 그 기록이 사라진다. **12:54본을 정본으로 두고 이 문서는 그 이후의 델타**를 적는다.
> 장후 회차는 `_intra.md` · `_intra_1340.md` · (있으면) `_intra_1430.md` **셋을 함께** 흡수해야 한다.
>
> 증거: `docs/동작점검/auto/2026-08-18_증거_intra.md` (13:39:10 생성)
> 조사 범위: 07:30:15 기동 ~ **13:42** (관측 371 사이클, 프로세스 **1회 기동**)
> 이 세션은 **코드·설정·`hypotheses.yaml`·`dev_memory`를 하나도 변경하지 않았다.**

---

## 지금 살아 있는가 — 한 줄

**살아 있다.** 관측 루프 최종 레코드 `13:42:44`(수집 시각 13:39:10 기준 최종 `13:39:13`), 워치독
최종 박동 `[2026-08-18 13:40:02] OK — 정상 — 마지막 박동 0초 전`. 프로세스 재기동 0회 · ERROR 0건 ·
정규장 로그 공백 3분 이상 **0건** · WS 단절 **0건**.

**P0 — 없음.** P1 2건(1건은 신규) · P2 2건.

---

## §0. 12:54본 이후에 새로 생긴 것만 — 이 회차가 파는 것

12:54본은 「배선은 살아 있고 p1은 반증됐다」로 닫혔다. 그 뒤 45분에 **12:54본이 구조적으로 볼 수
없었던 사건이 둘 일어났다**(13:00·13:30). 아래 §1이 그것이다.

| 축 | 12:54본 시점(~12:29) | 지금(~13:42) | 방향 |
|---|---|---|---|
| 예산 컷(`budget_exceeded`) | 73건 | **111건** | +38 |
| 조기 포기(`timeout_abort`) | 6건 | **11건** | +5 |
| 그중 **먼슬리(regular)를 포함해 컷** | **0건** | **2건** (13:00:37 · 13:30:33) | **신규** |
| 감마플립 산출 불가 | **0건** | **2건** (13:01:10 · 13:31:10) | **신규** |
| 프로세스 재기동 | 0 | 0 | — |
| WS 단절 | 0 | 0 | — |
| ERROR | 0 | 0 | — |

---

## §1. P1-1 (신규) — 판단 주입력이 두 분 통째로 끊겼다 (13:01 · 13:31)

### 증상

오늘 처음으로 **먼슬리(regular)까지 컷당한 사이클**이 나왔고, 그 다음 분 감마플립이 산출되지 않았다.

### 근거 — 인과 사슬이 로그에 그대로 있다

```
13:00:37 옵션체인 연속 타임아웃 3회 — 남은 13레그를 포기하고 이번 사이클을 접습니다
         … 적재 1행 · 컷당한북=regular,weekly_mon · 우선순위위반=아니오
13:00:51 옵션체인 사이클 소요 분해: REST수집 51.09초 … (rows=4)
13:00:51 먼슬리 레그 재시도: 3개 중 3개 회복(남은 예산 0.0초)
13:01:10 감마플립 산출 불가 — BS 계산 가능 레그 4개(전체 4개, 최소 6 필요). iv=0 0개 / 잔존만기<=0 0개

13:30:33 옵션체인 연속 타임아웃 3회 — 남은 13레그를 포기하고 이번 사이클을 접습니다
         … 적재 1행 · 컷당한북=regular,weekly_mon · 우선순위위반=아니오
13:30:53 옵션체인 사이클 소요 분해: REST수집 53.80초 … (rows=2)
13:30:53 먼슬리 레그 재시도: 4개 중 1개 회복(남은 예산 0.0초)   ← 3개는 못 살렸다
13:31:10 감마플립 산출 불가 — BS 계산 가능 레그 2개(전체 2개, 최소 6 필요). iv=0 0개 / 잔존만기<=0 0개
```

이 두 분의 `rows`는 **4와 2**다. 09:00~12:59 240분 중 `rows<=5`인 분은 **1분**뿐이었다.

### 기준

- `phases.md` B-2 — *"컷당한 북 분포 — 먼슬리(regular)가 먼저 잘렸으면 우선순위 불변식 위반"*
- `mahdi/main.py:1891` 08-12 Fix#4 주석 — 조기 포기의 2단계 접기
- `mahdi/main.py` 먼슬리 재시도 로그 문구 — *"판단 주입력(GEX/감마플립)의 두께다"*

### **불변식 위반은 아니다** — 이것을 먼저 못 박는다

`우선순위위반=아니오`는 **정확하다.** `mahdi/main.py:2004-2006`이 판정한다:

```python
if non_priority_ahead[index + 1] and not dropped_non_priority_first:
    priority_cut_before_others = True
```

즉 **1단계에서 위클리를 이미 버린 뒤**에 먼슬리가 잘렸으면 위반이 아니다 — Fix#4가 설계대로
동작한 결과다. 두 사이클 다 `컷당한북=regular,weekly_mon`으로 위클리가 함께 적혀 있으므로 2단계
(`_ABORT_SCOPE_ALL`)까지 간 분이고, 규약 G가 경계하는 *"성립할 수 없는 상황에 임계를 거는"* 오판을
여기서 반복하지 않는다. **오늘 우선순위위반=예는 111건 중 0건이다.**

### 그래서 무엇이 문제인가 — 위반이 아닌데도 남는 것

Fix#4는 *"위클리를 버리고 먼슬리를 계속 부른다"*로 먼슬리를 지키게 설계됐다. 오늘 그 방어가
**두 번 뚫렸다** — 1단계로 위클리를 버리고 나서도 먼슬리가 연속 3회 타임아웃을 또 맞았다.
그리고 뒤를 받치는 먼슬리 재시도는 두 번 다 **남은 예산 0.0초**에서 진입해, 13:30에는 4개 중
1개만 살렸다. **두 겹의 방어가 같은 원인(예산 고갈)으로 동시에 무력해지는 구조다.**

### 영향

그 두 분에 GEX·감마플립이 없다 → `options_flow` 멤버의 입력이 없다. 다만 판단 자체는
계속 돌았다(13:01·13:31 전후로 `판단 형태 전이`가 4/6 가용으로 이어진다) — **판단이 멈춘 게
아니라 그 분의 판단이 얇은 입력 위에서 내려졌다.** 오늘은 ADVISORY라 손실이 없지만, 실행이
켜진 날 같은 분이 오면 그 판단은 근거 두 레그로 내려간 것이 된다.

### 추론 (실측 아님 — 예측치로 바꿔 검정할 것)

두 사건이 **13:00과 13:30, 정확히 30분 간격**이다. 두 사이클 다 `타폴러동시호출추정=0건`이라
동시 호출로는 설명되지 않는다. **관측 2회로는 위상 패턴이라 단정할 수 없다.** 14:00·14:30에
같은 형태가 반복되는지가 이 추론의 검정이고, **오늘 14:30 회차가 그것을 볼 수 있는 유일한 창**이다.

---

## §2. P1-2 (이월·악화) — 예산이 천장에 닿았다. 장전 §2의 레버 E 처방을 시계열이 뒷받침한다

### 증상 — 창 단위 기울기 (단일 스냅샷이 아니다)

| 30분 창 | n | rows 평균 | rows<10 | REST수집 평균(초) | **소요>50초 분** |
|---|---|---|---|---|---|
| 09:00 | 30 | **19.8** | 0 | 30.0 | 1 (3%) |
| 09:30 | 30 | 19.3 | 0 | 35.8 | 8 |
| 10:00 | 30 | 18.9 | 0 | 37.2 | 9 |
| 10:30 | 30 | 18.1 | 0 | 40.3 | 12 |
| 11:00 | 30 | 17.2 | 2 | 43.7 | 17 |
| 11:30 | 30 | 16.2 | 1 | 46.3 | 21 |
| 12:00 | 30 | 16.1 | 2 | 44.9 | 18 |
| 12:30 | 30 | 16.8 | 0 | 45.9 | 17 |
| 13:00 | 30 | 16.5 | 1 | **47.7** | **20 (67%)** |
| 13:30 | 12 | **13.6** | 2 | 46.3 | **8 (67%)** |

- 12:30 이전(장중 210분) rows 평균 **17.9** → 12:30 이후(72분) **16.2**
- 소요 최대 **54.7초**(12:30창) — 예산 50초를 **9% 초과**한 분이 실재한다

### 근거

수집 예산은 `OPTION_CHAIN_CYCLE_COLLECT_BUDGET_SECONDS = 50`초인데, 실측 소요가 50초를 넘는 분이
**13시대 3분의 2**다(예: `13:30:53 REST수집 53.80초`, `12:59:54 54.17초`). 컷은 진행 중인 레그
호출을 중단시키지 못하므로 예산은 **하한이지 상한이 아니다.** 밀림은 전 사이클 `0.0초`라 다음 분
시작은 아직 안 밀렸지만, 60초 주기에서 남는 여유가 **6초**까지 줄었다.

### 기준

- 장전 §2 (P1-2) — 레버 `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS = {}` **유예 8회째**, 무조건발동일 2026-08-25
- 증거 §5-1 — 창 최대 p50 ÷ read timeout(4.0초) = **최대 0.55**(11시). 1.0에서 멀다

### 영향 — 그리고 **원인 귀속**

**KIS가 느려서가 아니다.** p50÷timeout이 0.55로 08-14 절벽 전조(0.88)에서 멀고, 페이서 배율도
정상이다. 소요가 늘어난 것은 **우리가 부르는 레그 수 대비 예산이 모자란 것**이고, §1의 두 사건도
그 결과다. 장전 보고서가 *"예산 96%는 KIS가 느려서가 아니라 우리 레그 수 때문"*이라고 쓴 것을
이 회차의 시계열이 **10개 창으로 확인**한다 — rows는 단조 감소, 소요는 단조 증가, p50은 평탄.

**레버 E(`{10:4, 11:4, 12:4, 13:4, 14:4}`)가 정확히 이 축의 처방이다.** 오늘은 켜지 않는다(§5).

---

## §3. 체크리스트 통과 기록 (phases.md B절)

### B-1 생존

| 항목 | 실측 | 판정 |
|---|---|---|
| 관측 루프 생존 | 최종 레코드 `13:42:44` — 현재와 1분 이내 | 확인 |
| 정규장 로그 공백 3분↑ | **0건** | 확인 |
| 워치독 무기록 구간 | 당일 36행, 10분 간격 **결손 0**. 최종 `[13:40:02] OK` — 무기록 **2분** | 확인 |
| `observation_loop_crash.log` | mtime **08-17 15:03** — 오늘 갱신 **안 됨** | 확인 |
| 프로세스 재기동 | `직전 정상 기동` 표식 **1회**(07:30:19) | 확인 |

> `logs/.watchdog_state.json`이 `{"date": "2026-08-17", "restarts": 3}`로 어제 날짜다. **이상이 아니다** —
> 이 파일은 재기동/경보가 있을 때만 갱신되고, 매 사이클 갱신되는 것은
> `.watchdog_last_check.json`(`2026-08-18T13:40:02`, `"action": "ok"`)이다. 오늘 재기동이 0회라 어제
> 값이 남은 것이 정상 동작이다. **08-12의 「재기동 오보로 5시간 31분 무력화」와는 다른 형태다.**

### B-2 수집 파이프라인

| 항목 | 실측 | 판정 |
|---|---|---|
| 사이클 관측 분 | **371분** · `rows=0` 사이클 **0개** · 최장 연속 rows=0 **없음** | 확인 |
| 예산 초과 | **111건** (weekly_mon 60 / weekly_thu 51) | 확인 |
| 연속 타임아웃 조기 포기 | **11건** (weekly_mon 5 / weekly_thu 4 / **regular+weekly_mon 2**) | §1 |
| 실패 예산 소진 | **0건** — 08-14에 17줄 있었으므로 **「잰 0」**(증거 §5-3) | 확인 |
| 먼슬리 레그 재시도 | **27건** — 13:30:53만 부분 회복(4개 중 1개) | §1 |
| **우선순위 불변식** | `우선순위위반=예` **0건 / 111건** | 확인 |
| WS 단절 | **0건** — 08-12에 2줄 있었으므로 **「잰 0」** | 확인 |
| REST 실패 분해 | ReadTimeout 다수 · `EGW00201`(TPS 초과) 25건 · `other` 급증 **없음** | 확인 |
| ATM 롤링 | 240건 (08시 12 · 09시 48 · 10시 54 · 11시 66 · 12시 42 · 13시 18) — 11시 정점 후 감소 | 확인 |

### B-3 판단까지 갔는가

| 항목 | 실측 | 판정 |
|---|---|---|
| 앙상블 멤버 | **4/6 ×63 · 3/6 ×6 · 1/6 ×1**. 4멤버 전부 마지막 관측 13:32:10 | 확인 |
| 확신도 분포 | STANDARD 34 · SMALL_TEST 21 · HIGH_CONVICTION 14 · NO_TRADE 1 | 확인 |
| 레짐 갱신 | 전이 5회 — §4 참조 | **§4** |
| WARMUP 회귀 | 08:45:58 최초 `warmup_fallback` 이후 전부 `predict` — **되돌아간 구간 없음** | 확인 |
| 체인 신선도 | 로그 축 없음 — DB 축 `chain_input_source`가 답한다(장후) | 계측 대체 |
| 이벤트 캘린더 미기입 | **0건** — 08-17에 26줄 있었으므로 **「잰 0」** | 확인 |

---

## §4. P2-1 — 레짐이 1분 만에 되돌아왔다 (13:26 → 13:27)

### 증상 · 근거

```
08:45:58 레짐 전이: (최초) → RANGE_BREAK_PREP (안정=False, 모델=warmup_fallback, 창=1분)
11:45:58 레짐 전이: RANGE_BREAK_PREP → VOL_COMPRESSION (안정=True, 모델=predict, 창=120분)
13:18:59 레짐 전이: VOL_COMPRESSION → RANGE_BREAK_PREP (안정=True, 모델=predict, 창=120분)
13:26:58 레짐 전이: RANGE_BREAK_PREP → VOL_COMPRESSION (안정=True, 모델=predict, 창=120분)
13:27:58 레짐 전이: VOL_COMPRESSION → RANGE_BREAK_PREP (안정=True, 모델=predict, 창=120분)
```

오늘 전이 5회 중 **3회가 13:18~13:27 9분에 몰렸고, 마지막 왕복은 60초**다. 다섯 전부
`안정=True`(`state.stability_flag`)로 찍혀 있다.

전략 선택이 그대로 따라 흔들렸다:

```
13:26:10 판단 형태 전이: … STANDARD · 전략 ['small_strangle_buy']
13:27:10 판단 형태 전이: … STANDARD · 전략 ['straddle_accumulate']
13:28:10 판단 형태 전이: … SMALL_TEST · 전략 ['small_strangle_buy']
```

### 기준

`mahdi/engines/regime_pipeline.py:449` — 전이 판정은 `if self._last_regime != state.regime` **한 줄**이고,
최소 체류 시간(dwell)이나 히스테리시스가 **없다**. `stability_flag`는 로그에 찍기만 하고
전이를 막지 않는다.

### 영향 — 오늘은 P2다

관측 층은 온전하고 ADVISORY라 실손실이 없다. 다만 v6 §11.5는 레짐으로 전략 셀을 고르므로,
실행이 켜진 날 1분 왕복은 **진입 직후 반대 전략으로 갈아타는 형태**가 될 수 있다.
`reentry_cooldown_minutes: 0`(레버, 강제력 없음)과 겹치면 그 위험이 커진다.

### 무엇을 아직 모르는가

`stability_flag=True`인데 1분 왕복한 것이 (a) 모델 확률이 실제로 경계에 앉아 있었던 것인지,
(b) 플래그가 전이를 안 막게 설계된 것인지 로그만으로는 못 가른다. **장후에 `regime_state`
테이블의 그 분 확률값을 봐야 답이 나온다** — 그 전에 히스테리시스를 넣는 것은 원인 없이
증상을 덮는 것이다.

---

## §5. P2-2 — 오늘 커밋은 프로세스에 없다. 그래도 **재시동하지 않는다**

### 사실관계

- HEAD `abbe8d6` (08-17 17:37) < 관측 루프 기동 07:30 → **08-17 커밋 3건은 오늘 프로세스에 실려 있다**
- **당일 커밋 0건.** 12:54 세션이 바꾼 것(`db._account_balance_snapshot_row_to_dict()` NULL 가드,
  마이그레이션 032 `option_analysis_1m.price`)은 **디스크에만 있고 미커밋 상태**다(미커밋 127건)
- 마이그레이션 032는 **라이브 DB에 이미 적용**됐다(12:54본 §4-2)

### 손익 — 결정은 사람이 한다

| | 재시동 **안 함**(현행) | 재시동 **함** |
|---|---|---|
| 얻는 것 | 오늘 371분 관측이 **한 프로세스로 연속**. 08-17의 「5회 기동」 같은 오염 없음 | NULL 가드와 `price` 적재가 **오늘 검증**된다 |
| 잃는 것 | `price` 컬럼이 오늘 내내 빈다. 두 fix는 `untested`로 남는다 | 재기동 분의 관측 공백 + WS 재구독 + §2의 예산 압박이 재구독 직후 더 커진다 |
| 오늘의 위험 | **없음** — 032는 컬럼 추가라 구 코드가 INSERT해도 깨지지 않는다 | 지금 13:45. 남은 정규장 95분을 「반은 구코드, 반은 신코드」로 만든다 |

**권고: 재시동하지 않는다.** 오늘 얻을 것(fix 검증)보다 잃을 것(오늘 하루 관측의 단일성)이 크고,
032 적용 순서가 이미 안전하게 잡혀 있어 **재기동은 장후·다음 장전 어느 쪽에서 해도 손실이 없다.**
`2026-08-16-b1-position-side-must-not-fail-silently`의 조치는 오늘 `untested`로 닫고 08-19에 검정한다.

---

## §6. 장전 지적사항의 현재 상태 — 물은 것에 답한다

| 장전 항목 | 13:42 현재 |
|---|---|
| **P1-1** 예약이 개장 후 발화(298분) | **해소 안 됨 — 이 회차가 같은 증상이다.** 12:30 회차가 13:39에 발화(69분). 오늘 09:00 이전 발화 점검 예약 **0건** |
| **P1-2** 레버 E 발동 조건 성립, 유예 9회째 | **여전히 꺼져 있다.** `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS = {}` 그대로. 조건은 §2에서 **더 강하게** 성립(13시 소요>50초 67%) |
| **P1-3** 판정 예정 8건 중 가능한 것 3건 | **변동 없음.** `hypotheses.yaml` 미변경. `c1`/`c2`(왕복 실측)는 **사람 손이 필요하고 15:20까지 95분 남았다** |
| **P0로 올린 것** | 장전 P0 **0건** — 올라간 것이 없다 |
| 레버 F(`use_effective_member_count`) | **여전히 키 없음.** 무조건발동일 08-25(D+7) |

---

## §7. Fix 작업 구현계획 — **적용은 전부 장후 이후**

### Fix#1 [P1] 먼슬리 2차 방어가 예산 0초에서 진입하는 것을 막는다 (§1)

- 파일: `mahdi/main.py::_retry_priority_legs()` 호출부
- 내용: 재시도 진입 조건에 **최소 잔여 예산**을 건다. 현재는 `deadline`만 보고 들어가 `남은 예산
  0.0초`에서도 진입해 13:30에 4개 중 1개만 살렸다. `RETRY_MIN_REMAINING_SECONDS`(초안 5.0초) 미만이면
  **진입하지 않고 그 사실을 로그로 남긴다** — 지금은 "0.0초에서 1개 회복"이 성공처럼 보인다
- 회귀 위험: 재시도가 아예 안 도는 분이 늘 수 있다. **그래서 이 fix의 대가 지표를 함께 등재한다**
  (`monthly_retry_skipped_count`)
- 검증: `tests/test_ops_*.py` + 08-19 로그에서 `먼슬리 레그 재시도` 중 부분회복 건수

### Fix#2 [P1] 소요가 예산을 넘은 분을 지표로 센다 (§2)

- 파일: `docs/동작점검/tools/collect_evidence.py` §5-1 + `scripts/daily_ops_report.py`
- 내용: 시간대 표에 **`소요>예산` 분 수**를 열로 추가. 지금 표는 평균만 보여 주고, 평균은
  「67%의 분이 예산을 넘었다」를 **구조적으로 못 보여 준다**(증거 §5-1이 p50에 대해 경고하는 것과 같은 형태)
- 회귀 위험: 없음(읽기 전용 집계)

### Fix#3 [P2] 레짐 전이에 최소 체류를 넣을지 **결정 전에 확률을 본다** (§4)

- **장후에 먼저 조사한다**: `regime_state` 테이블 13:18~13:28 구간의 모델 확률
- 확률이 경계에 앉아 있었으면 → `regime_pipeline.py`에 `_MIN_DWELL_MINUTES` 도입 검토
- 플래그가 원래 전이를 안 막는 설계면 → `stability_flag`의 의미를 문서에 못 박고 로그 문구를 고친다
- **오늘 고치지 않는다.** 원인 없이 히스테리시스를 넣으면 08-04 결정 8과 같은 오류(자기가 고친 것을
  반증으로 찍는 지표)를 만든다

### Fix#4 [P1] 점검 예약이 그날 안에 사람에게 닿는 경로 (사흘째 이월)

- 08-17 보고서 · 08-18 장전 §5가 이미 계획을 갖고 있다. **이 회차는 새 계획을 만들지 않고
  「사흘 연속」이라는 사실만 더한다** — 만성을 새 발견으로 올리지 않는다(README 규약)

---

## §8. 고도화 방안 — 오늘 관측에서 출발한 것만

1. **`rows` 시계열을 게이지로 승격한다.** 오늘 rows 평균이 09:00창 19.8 → 13:30창 13.6으로
   4시간 반에 **-31%** 단조 감소했는데, 이 값에는 경고선이 없다. 08-14 절벽은 `rows=0`으로
   왔지만 오늘은 **0에 닿지 않고 얇아지는 형태**다 — 현재 계측은 이 형태를 「정상」으로 읽는다.
   14:30 회차 규약 2가 요구하는 게이지 표에 `rows 30분 창 평균`을 넣고 경고선을 박는다(초안: 경고 15 / 위험 10).

2. **판단 입력 결손을 분 단위로 센다.** `감마플립 산출 불가`는 지금 WARNING 한 줄로만 남고
   어떤 지표에도 안 잡힌다. 오늘 2분이 그렇게 지나갔다. `gamma_flip_unavailable_minutes`를
   `daily_ops_report.py`에 넣으면 **「관측은 살았는데 판단 입력이 죽은 분」**이라는 축이 처음 생긴다 —
   현재 지표는 `rows=0`(관측 죽음)과 판단 정상 사이의 중간지대를 못 본다.

3. **예약 발화 지연을 그날 안에 알리는 경로.** 사흘 연속 같은 형태이고, 세 번 다 **보고서를 쓰는
   시점에야** 알았다. 워치독이 이미 10분마다 도니, 「오늘 예정 회차 중 미발화분」을 워치독 점검
   항목에 넣는 것이 비용이 가장 싸다(§7 Fix#4와 한 몸).

---

## §9. dev_memory 반영 초안 (이 세션은 파일을 고치지 않았다)

**`DECISION_LOG.md` append 초안**

> ### [MW0601] 2026-08-18 장중 — 먼슬리 2차 방어가 예산 0초에서 진입해 부분 회복에 그쳤다
>
> - **증상**: 13:00:37·13:30:33 조기 포기가 `컷당한북=regular,weekly_mon`으로 2단계까지 갔고,
>   그 분 rows가 4·2였다. 13:30:53 먼슬리 재시도가 `4개 중 1개 회복(남은 예산 0.0초)`에 그쳐
>   13:31:10 `감마플립 산출 불가 — 레그 2개(최소 6 필요)`가 떴다.
> - **원인**: Fix#4(2단계 접기)와 고도화#1(먼슬리 재시도)이 **같은 예산 풀을 공유**한다. 예산이
>   마르면 두 방어가 동시에 무력해진다.
> - **결정**: 재시도 진입에 최소 잔여 예산 게이트를 건다(초안 5.0초). **적용은 장후.**
> - **Why**: `남은 예산 0.0초`에서의 부분 회복은 지표상 성공으로 보이지만 판단 입력을 못 채운다.
> - **검증**: 08-19 로그에서 부분 회복 건수 및 `감마플립 산출 불가` 분 수. 대가 지표 동반 등재.
>
> ### [MW0601] 2026-08-18 장중 — 우선순위위반=아니오는 옳다 (오판 방지 기록)
>
> `regular`가 컷당한 두 사이클은 **위반이 아니다** — `main.py:2004` 조건이 1단계 선행 여부를 본다.
> 컷당한북에 `regular`가 보인다는 이유로 불변식 위반을 보고하지 않는다. 08-12 §2-6과 같은 함정이다.

**`NEXT_TODO.md` 추가 초안**

- [ ] **[P1]** 먼슬리 재시도 최소 잔여 예산 게이트 (`_retry_priority_legs` 호출부) — 장후
- [ ] **[P1]** 증거 §5-1에 `소요>예산` 분 수 열 추가
- [ ] **[P2]** 장후 조사: `regime_state` 13:18~13:28 모델 확률 — 히스테리시스 판단의 선행조건
- [ ] **[고도화]** `rows` 30분 창 평균 게이지 + 경고선(15/10)
- [ ] **[고도화]** `gamma_flip_unavailable_minutes` 지표 신설
- [ ] **[예측치]** 위 fix 착수 **전에** `hypotheses.yaml` 등재 — 주장 지표 + 대가 지표 쌍으로

---

## §10. 14:50 컷오프까지 — 사람용 결론

**13:45 현재, 14:50 신규 진입 컷오프까지 65분. 개입 불필요 — 재시동하지 않는다.**

오늘 남은 시간에 사람이 할 수 있는 것은 둘뿐이다:

1. **왕복 실측(수동 주문)** — `2026-08-16-c1`/`c2`의 유일한 검정 수단이고 **오늘이 마지막 기회**다
   (15:20까지 95분). 안 하면 나흘째 밀린다.
2. **14:30 회차가 실제로 발화하는지 지켜보는 것** — 발화하면 §1의 「30분 간격」 추론이
   14:00·14:30에 반복됐는지 그 회차가 답한다. 안 발화하면 그 자체가 §6 P1-1의 네 번째 증거다.

**레버 E는 오늘 켜지 않는다**(장중 설정 변경 금지). 장후에 켜고 08-19를 검정일로 잡는다 —
무조건발동일 08-25까지 거래일이 다섯 남았다.
