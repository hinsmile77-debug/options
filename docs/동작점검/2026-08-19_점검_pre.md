# 마흐디 장전 점검 — 2026-08-19 (수)

- 국면 **pre** · 실행 **08:31 KST** (예약 `mahdi-premarket-check` cron `30 8 * * 1-5`, 지터 27초 → 08:30:48 발화)
- 개장까지 **29분**. 정시 실행이다 — 밀린 실행이 아니다.
- 증거: `auto/2026-08-19_증거_pre.md` (08:31:15 생성, 38.0KB)
- 리포 HEAD `595f75d` (2026-08-18 18:12:44) · 브랜치 `master` · 당일 커밋 없음

---

## ⚠ P0 — **없다**

오늘 관측·거래에 직접 위험한 항목은 발견되지 않았다. ERROR 0건, 프로세스 기동 1회,
워치독 정상, 정규장 전 로그 공백 0건, 수집 예산 39%.
**개장 전에 사람이 해야 할 조치는 없다.**

이상점 **P0 0 / P1 2 / P2 2** — 신규 **1건**(§1-3 미커밋 실변경), 나머지는 이월·만성.

> **개장 전에 사람이 볼 값어치가 있는 한 줄**: HEAD `595f75d` 밖에 있는 **미커밋 실변경 10파일**이
> 오늘 07:30 기동에 실렸다(§1-3). 위험하지는 않으나, **오늘 산출물을 커밋 이력으로 설명할 수 없다.**

---

## §0. 장전 체크리스트 (phases.md A절) — 전항 통과

### A-1. 기동 — 확인됨

| 항목 | 실측 | 판정 |
|---|---|---|
| 07:30 `start_mahdi_premarket.bat` | `[2026-08-19 7:30:00.78] ===== Mahdi 장전 기동 시작 =====` | ✅ |
| docker(redis/postgres) | `[7:30:05.66] Docker 데몬 준비 완료` → `docker compose up -d` · `unable to get image` 없음 | ✅ |
| DB 마이그레이션 | 001~033 전건 적용 완료 (`[7:30:11.47] … 033_option_analysis_series.sql`) | ✅ |
| COCKPIT | `[7:30:15.59] COCKPIT 대시보드 실행` · `.last_cockpit_start.txt` = 07:30:25 | ✅ |
| 관측 루프 `직전 정상 기동` 표식 | **1회** — `07:30:19 직전 정상 기동: 2026-08-18 07:30:19 (24.0시간 전)` | ✅ 재기동 없음 |
| `.last_successful_start.txt` | `2026-08-19T07:30:19.348652` | ✅ 당일 |
| WS 구독 | `07:30:22.720~22.977` 요청 **32/41** → `07:30:23.5xx` 전건 `SUBSCRIBE SUCCESS` · 재시도 0 | ✅ |
| 워치독 무장 | `07:40:01 / 07:50 / 08:00 / 08:10 / 08:20 / 08:30` **OK 6회 / 비-OK 0** · `.watchdog_last_check.json` `{"at":"2026-08-19T08:31:01","action":"ok","detail":"정상 — 마지막 박동 7초 전"}` | ✅ |

- 사이클(`옵션체인 사이클 소요 분해`) 관측 **61분** · 3분 이상 공백 **0건**
- REST 수집 07시·08시 모두 평균 19.5초 / 예산 50초 = **39%** · `rows=0` 사이클 **0개**
- `inquire-price` 창 최대 p50 **0.02초** / read timeout 4.0초 = **0.01** — 절벽 선행 지표 정상권

### A-2. 오늘의 계획 — 확인됨

**커밋 선후(규약: 커밋 시각 < 관측 루프 기동 시각)** — ✅ 통과

```
595f75d  2026-08-18 18:12:44  [MW0601] P1 다섯 건 …
관측 루프 기동  2026-08-19 07:30:19
```

08-18 세션의 P1 5건(커밋 1~4)이 **오늘 07:30 기동에 실렸다.** `main.py`의 라벨 개명
(`우선순위위반` → `데드라인이먼슬리에서끝남`, `mahdi/ops/log_metrics.py:122`가 옛 이름도
읽는 이중 파서)도 재기동으로 반영됐다. **오늘 산출물은 이 5건을 판정할 자격이 있다.**

⚠ **다만 이 검사는 커밋 밖 변경을 못 본다** — 미커밋 실변경 10파일도 함께 실려 있다(§1-3).
커밋 선후는 통과했지만 *"오늘 도는 코드 = HEAD"* 는 오늘 성립하지 않는다.

**오늘 검증예정일이 도래한 가설: 1건 — 그런데 판정 불가다(규약 H)**

| id | 전제레버 | 레버 현재값 | 오늘 판정 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | `REGIME_RESTORE_SESSION_WINDOW` | `= False` (`mahdi/engines/regime_pipeline.py`) | **미실행** |

가설 자신이 이것을 예고해 뒀다 — *"⚠ 레버가 기본 OFF다. 켜기 전에 오프라인 재생으로
「복원한 창의 예측 == 연속 실행의 예측」을 확인한다 … 규약 H의 첫 실사용"*.
**오프라인 재생이 아직 안 돌았으므로 오늘 아침에 켤 근거가 없다.** 게다가 이 가설은
*재기동이 있는 날에만* 판정 가능한데(주장 지표 `db.regime.warmup_minutes`), 오늘은 기동 1회다
— 재기동이 없으면 그 경로 자체가 안 돈다. → **`inconclusive`로 닫고 오프라인 재생 완료일로
검증예정일을 옮기는 것이 맞다.** (상태 변경은 사람이 한다 — 이 세션은 손대지 않았다.)

**08-19 등재분 5건**(`fix2`/`fix3`/`fix4`/`fix5`/`fix6`)은 전부 **검증예정일 2026-08-20**이다.
오늘은 데이터를 만드는 날이고 판정하는 날이 아니다.

**레버 상태 — 오늘 켤 레버는 없다**

| 레버 | 현재 | 무조건발동일 | 오늘 |
|---|---|---|---|
| `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` (E) | `{}` = OFF | 2026-08-25 (D-6) | **의도적 유예. 켜지 않는다** |
| `use_effective_member_count` | 키 없음(기본값) | 2026-08-25 (D-6) | 변동 없음 |
| `REGIME_RESTORE_SESSION_WINDOW` | `False` | — | 오프라인 재생 선행 |
| `OPTION_CHAIN_READ_TIMEOUT_SECONDS` | `None`(전역 4.0초) | — | 변동 없음 |
| `SIGNAL_FUSION_PHASE_OFFSET_SECONDS` | `10.0` | — | 로그가 `:10`에 찍혀 일치 확인 |
| `reentry_cooldown_minutes` | `0` | — | 변동 없음 |

### A-3. 전일 이월 — 확인됨

- 전일 사이드카 `auto/2026-08-18_지표.json` **있음** (58.8KB, 08-18 15:45) → 오늘 델타 계산 가능
- 만성 항목 그대로: **ZN(`EGW00552` CBOT SUB거래소 미신청) · ES(CME SUB거래소 미신청)**
  07:32:35 / 07:32:39. `CURRENT_STATE.md`가 2026-07-10부터 「KIS 계좌 게이트, 코드는 완성 상태」로
  기록한 항목이다 — **만성이며 새 발견이 아니다.**

---

## §1. 이상점

### 1-1. [P1·이월] 이벤트 캘린더가 만료된 채 하루가 시작됐다 — 메타라벨 페널티가 오늘 종일 안 걸린다

**증상**: `event_proximity_minutes`가 하루 종일 `None`이 되어 메타 라벨의 이벤트 근접 ×0.5
페널티가 **한 번도 걸리지 않는다.**

**근거** (기동 배치와 관측 루프가 각각 한 번씩, 총 2회 경고):

```
[2026-08-19 07:30:15] *** 이벤트 메모 캘린더가 1일 전에 만료됐다 - 오늘 판정에 이벤트 페널티가 안 걸린다
                         (logs/premarket_startup.log — check_lever_due)
07:31:10 [WARNING] 이벤트 캘린더 미기입: not_covered — `mahdi/config/event_calendar.yaml`의
                   covered_through(2026-08-18)가 오늘(2026-08-19)보다 앞선다
                         (logs/observation_loop.log)
```

**기준**: `phases.md` B-3 *"이벤트 캘린더 미기입 건수(미기입은 페널티가 아예 안 걸린 것과 같다)"*.

**영향**: 오늘 ENTER 신호가 나오면 이벤트 근접 감쇄 없이 나온다. 다만 오늘 8월 만기(8/13)는
지났고 다음 만기까지 거리가 있어 실제 페널티 대상 시각이 있었을지는 별건이다 —
**"페널티가 안 걸렸다"는 확정, "그래서 오판했다"는 미확정.**

**왜 P0가 아닌가**: 오늘 현재 앙상블 가용멤버 **1/6 · NO_TRADE**(07:31:10)라 메타 라벨까지
가는 신호 자체가 아직 없다. 08-18에도 09:00 이후에야 형태가 움직였다.

**조치**: 개장 전 1분이면 끝난다 — `uv run python scripts/suggest_event_calendar.py`가 만기
항목을 만들어 주고 `covered_through`만 사람이 확인해 옮기면 된다. **다만 코드 변경 금지
규약에 걸리지 않는 설정 파일 갱신이고 재기동이 필요 없다**(`get_event_calendar()`는 읽기 시점
평가). 사람이 판단할 것.

> **손익 한 줄씩** —
> (a) **지금 갱신한다**: 오늘 페널티 경로가 살아난다. 잃는 것 없음(재기동 불필요).
> (b) **장후로 미룬다**: 오늘도 페널티 0회. 이 경고가 며칠째인지 세는 눈이 없어 조용히 만성화된다.

### 1-2. [P2·이월] 점검 회차 예약이 `phases.md`가 정한 08:00이 아니라 08:30이다 (08-18 Fix#7)

**증상**: 수집기가 첫 줄에 「예정 08:00 · 실행 08:31 — **31분 밀렸다 ⚠**」을 적었다.
**이것은 밀린 실행이 아니다** — Claude 앱 예약 자체가 08:30이다.

**근거**:

```
mahdi-premarket-check · cronExpression "30 8 * * 1-5" · jitterSeconds 27
  lastRunAt 2026-08-19T08:30:48 KST  (정시 발화)
phases.md 「점검 회차」표 · 장전 = 08:00
collect_evidence.PHASE_SCHEDULE = 08:00
```

**기준**: `phases.md` — *"⚠ 점검 회차의 진실원천은 「Claude 앱 예약 작업」이다"* / 08-18 보고서
Fix#7 `- [ ] [P2] 예약 회차 시각 통일 — cron "0 8 * * 1-5"` (미이행).

**영향**: 두 가지가 겹친다. ① 개장 전 조치 시간이 설계보다 30분 짧다(오늘 29분 남았다 —
08-14에 「P0 둘인데 25분」으로 앞당기기로 한 바로 그 상황의 재연). ② 수집기가 매일 「밀렸다 ⚠」
**허위 적신호**를 낸다 — 진짜 지연과 구별되지 않아 적신호가 무뎌진다.

**조치**: 장후. `mcp__scheduled-tasks__update_scheduled_task`로 `mahdi-premarket-check`의
cron을 `0 8 * * 1-5`로 바꾼다. 코드가 아니라 예약 설정이므로 리포 변경 없음.

### 1-3. [P1·신규] 오늘 도는 코드에 **커밋되지 않은 실변경 8파일**이 실려 있다

> **⚠ 초안 정정** — 이 절의 초안은 *"미커밋 144건은 전부 줄바꿈 노이즈"* 였다.
> 자체 검증에서 뒤집혔다. **지우지 않고 정정 표로 남긴다.**
>
> | 초안이 적은 것 | 실제 |
> |---|---|
> | 미커밋 144건 = 전부 CRLF/LF 노이즈 · 내용 변경 0 | 수정(M) 123건 중 113건은 노이즈가 맞다. **나머지 10건은 진짜 내용 변경**이다 |
> | 「결함이 아니라 계측 노이즈」 P2 | 노이즈 축은 P2 그대로, **실변경 축은 P1 신규**다 |
>
> (증거 §1의 「144건」은 미추적 파일을 포함한 수다. `git status --porcelain` 수정분은 123건.)
>
> 초안이 틀린 이유: `mahdi/main.py` 하나만 열어 보고(4599+/4599− = 전 줄 교체) 전체로
> 일반화했다. `git diff --numstat`에서 `insertions != deletions`를 세면 즉시 갈렸다.
> **한 파일로 144건을 판정한 것이 오류의 형태다.**

**증상**: 커밋 이력으로 「오늘 무엇이 도는가」를 판정하는 A-2 절차의 전제가 깨져 있다.
HEAD `595f75d` 밖에 있는 코드가 07:30 기동에 실렸다.

**근거** — `git diff --numstat` 에서 `insertions != deletions` 인 **10파일 전건**.
런타임 5개는 전부 **mtime < 07:30:19**이므로 **오늘 프로세스에 실려 있다**:

```
+/-        mtime                파일
1968/1960  2026-08-18 17:12:38  mahdi/data/db.py                      (순증 +8)
 682/499   2026-08-18 17:12:05  mahdi/fusion/instrument_selection.py  (순증 +183)
 383/360   2026-08-18 17:14:44  mahdi/data/subscription_manager.py    (순증 +23)
 397/389   2026-08-19 06:33:10  mahdi/ops/campaign.py                 (독스트링 정정)
  85/74    2026-08-19 06:32:57  scripts/stop_mahdi_marketclose.bat    (--prune-days 7 신설)
  75/0     2026-08-19 06:32:17  docs/동작점검/tools/collect_evidence.py (prune 구현 — 오늘 이 수집기가 돌았다)
  53/34                         .gitignore                            (_지표.json 추적 복원)
1532/1525 · 465/439 · 415/260   tests/test_data_db · _subscription_manager · _fusion_instrument_selection

미추적: docs/Dev_md/SERIES_ROTATION_RULE_v1.md · PLAN_SERIES_ROTATION_IMPL_v1.md
        · db/migrations/033_option_analysis_series.sql (기동 배치가 07:30:11에 적용했다)
```

**기준**: `phases.md` A-2 *"전일 커밋이 오늘 프로세스에 실렸는가 — 커밋 시각 < 관측 루프 기동
시각"*. 이 검사는 **변경이 커밋돼 있다는 전제** 위에 있다. 커밋 밖 변경은 이 검사를 그냥 통과한다.

**영향 — 셋을 나눠 본다**

1. **`mahdi/fusion/instrument_selection.py` 순증 +183 · 미커밋** — 종목 선발 축이다.
   미추적 문서 `SERIES_ROTATION_RULE_v1.md`·`PLAN_SERIES_ROTATION_IMPL_v1.md`와
   미적용 마이그레이션 `033_option_analysis_series.sql`(기동 배치는 07:30:11에 적용했다)이
   같은 작업으로 보인다. **`hypotheses.yaml`에 이 변경을 주장하는 가설이 없다** — 오늘 이
   코드가 만드는 결과는 어떤 예측치와도 대조되지 않는다.
2. **`scripts/stop_mahdi_marketclose.bat` `--prune-days 7` — 오늘 15:45에 처음 돈다.**
   주석이 삭제 범위를 *"`_증거_*.md` 파일명 패턴으로만"* 으로 한정한다고 적었다.
   **실제 삭제 범위는 오늘 장후에 실측으로 확인해야 한다** — 보고서·`_지표.json`이
   함께 지워지면 되돌릴 수 없다(로그는 이틀치만 남는다).
3. **`.gitignore`의 `_지표.json` 추적 복원이 절반만 됐다.** 규칙은 고쳐졌는데
   `git ls-files docs/동작점검/auto/` 가 **비어 있다** — 아직 인덱스에 없다.
   `campaign.py`가 `min_days: 10`으로 여러 날을 접는 원자재인데 여전히 백업이 없다.
   같은 커밋의 `.gitignore` 주석이 *"지우면 영구 소실이고 PC가 죽으면 캠페인 표본이 통째로
   사라진다"* 고 적은 바로 그 상태가 지금 유지 중이다.

**왜 P0가 아닌가**: 세 항목 다 **오늘 개장 중 거래·관측을 즉시 위협하지 않는다.**
①은 관측 품질 축이고 07:30 이후 이미 61분간 정상 사이클을 돌았다. ②는 15:45에 발생한다.
③은 손실이 이미 진행 중이나 오늘 새로 악화되지 않는다. **개장 전 조치는 불필요하다.**

**조치**(전부 장후):
- 실변경 10파일을 **작업 단위로 나눠** 커밋한다(선발 축 / prune / gitignore·campaign).
  선발 축은 커밋과 **동시에 `hypotheses.yaml`에 주장 지표를 등재**한다 — 숫자를 보기 전에.
- `git add -f docs/동작점검/auto/*_지표.json` 로 인덱스에 실제로 넣어 ③을 닫는다.
- 15:45 prune 직후 `auto/` 파일 목록을 **삭제 전후로 비교**해 범위를 실측한다.

### 1-4. [P2·만성] 미커밋 113건은 줄바꿈 노이즈다 — 실변경 10건을 가린다

**근거**: `mahdi/main.py` diff가 `4599 insertions / 4599 deletions` = 전 줄 교체 = CRLF/LF.
런타임 핵심 파일 mtime은 커밋 시점과 일치한다:

```
mahdi/main.py                        2026-08-18 18:00:47
scripts/watchdog_observation_loop.py 2026-08-18 17:59:55
mahdi/ops/log_metrics.py             2026-08-18 18:07:07
mahdi/config/strategy_params.yaml    2026-08-17 14:27:31
```

**영향**: §1-3이 오늘 늦게 발견된 이유가 이것이다. 「미커밋 144건」이 노이즈로 읽히는 순간
그 안의 10건은 아무도 안 연다. **노이즈는 그 자체로 해롭지 않고, 신호를 덮어서 해롭다.**

**조치**: 장후. `collect_evidence.py` §1이 `git diff --numstat`으로
**「내용 변경 N건 · 줄바꿈만 M건」을 나눠 인쇄**하고, 내용 변경분은 파일명을 전건 나열한다.
(`.gitattributes` 정규화는 134파일을 한 커밋으로 만들어 `git log -p` 이력을 오염시키므로 후순위)

---

## §2. 자동 적신호를 규약으로 걸렀다 — 5건 중 4건이 오탐

수집기 §12는 출발점이지 결론이 아니다. 규약 E·F·H와 손 대조로 하나씩 검정했다.

| # | 자동 적신호 | 판정 | 근거 |
|---|---|---|---|
| 1 | pre 점검이 31분 밀렸다 | **오탐(설정 불일치)** | 예약 cron이 08:30 · 정시 발화 → §1-2 |
| 2 | `flow_position`가 07:31:10 이후 **60분째 미관측** | **오탐(관측 형식)** | `판단 형태 전이`는 **전이 시에만** 찍힌다. 08-18도 07:31:10 다음이 **08:46**이었다 — 즉 08:31 시점의 08-18 로그도 똑같이 1줄뿐이었다. 형태가 안 바뀐 것이지 축이 죽은 것이 아니다 |
| 3 | 예정일 지난 pending 8건 | **사실이나 신규 아님** | 08-18 보고서 §6이 *"8건 도래 — 이 세션은 상태를 바꾸지 않았다. 아래는 제안이다"*로 이미 열어 둔 것. 사람 확정 대기분 |
| 4 | `hypotheses.yaml`이 08-18 보고서 이후 **한 줄도 안 바뀌었다** | **오탐(날짜 해상도)** | mtime `2026-08-18 17:50:24`. 그 안에 **`2026-08-19-fix2/3/4/5/6` 5건이 실제로 등재돼 있다**. 수집기가 날짜만 비교해 시각을 못 본다 |
| 5 | 전일 보고서 언급 id 4건 미등재 | **부분 오탐** | `2026-08-18-p5-deadline-cut-respects-priority`는 **폐기됐다** — NEXT_TODO가 *"⚠ 보고서 Fix#1을 폐기한다 — 착수 전 코드 검증에서 진단이 뒤집혔다"*로 사유를 적고 닫았다. 나머지 3건(`2026-08-13-p1-chain-input-phase`·`2026-08-14-fix1`·`2026-08-14-fix4`)은 **6일째 진짜 미등재**(08-18 §1-4의 이월) |

> **적신호 4번은 수집기의 결함이다.** `hypotheses.yaml` mtime을 **날짜**가 아니라 **직전 보고서
> 작성 시각**과 비교해야 한다. 오늘처럼 등재가 실제로 끝난 날에 「전부 무행동」이라는 가장
> 무거운 문구를 내면, 진짜로 무행동인 날에 아무도 안 읽는다. → §4 Fix#2.

### 레버 E — 열 번째 유예이나 **이번엔 잊힌 유예가 아니다**

08-18까지 아홉 번의 유예 중 사유가 적힌 적이 없었다. 오늘은 다르다 —
`NEXT_TODO.md:75-91`이 사유·해제조건·기한을 전부 적었다:

- **사유**: 08-18 §2-5가 E의 처방 자체를 흔들었다. 판단이 죽은 넉 분(`13:01:10`·`13:31:10`·
  `14:01:10`·`15:01:10`)은 전부 `:01`이고 페이서 대기는 1.0~1.8초로 평범했다 — 우리 백오프가
  아니라 KIS 응답이다. 위클리를 격분화해도 정각 분의 먼슬리 호출은 그 창에 그대로 부딪힌다.
- **지금 켜면 원인 귀속이 불가능하다**: 커밋 3(검열 건수 분리)이 08-19·08-20 이틀치를 내기
  전에 켜면, 변화를 「E가 줄인 것」과 「그날 KIS가 덜 느렸던 것」 중 무엇이 만들었는지 영영 못 가른다.
- **해제 조건(날짜가 아니라 관측)**: `censored.phase_concentration` 이틀치가 균등선(13.3%)에
  가까우면 켠다 / 여전히 30% 근처면 켜되 주장 지표를 `budget_exceeded.count`로만 읽는다.
  (08-18 재집계 실측 **33.8%**)
- **기한**: 무조건발동일 **2026-08-25 (D-6)**. 그때까지 조건이 안 풀리면 사유 없이 미루지 말고 켠다.

**따라서 오늘 아침 레버 E를 켜라고 권고하지 않는다.** 이것은 규약이 요구한 형태의 유예다.

---

## §3. 오늘 관측이 답해야 할 것 (장중·장후 회차로 인계)

08-18 커밋 4건이 오늘 처음 실전으로 돈다. **오늘의 산출물이 그 5개 가설의 유일한 증거다.**

1. **`slow_calls.censored.*`가 실제로 값을 낸다** (커밋 3) — 08-18 재집계는 290건/33.8%였다.
   라이브 집계에서도 나오는가. **레버 E 결정의 이틀치 중 첫째 날이다.**
2. **`watchdog.missing_check_alerts`의 첫 실전 검정** (커밋 4) —
   `scripts/watchdog_observation_loop.py:94`가 *"지금이 09:00을 넘겼는데 오늘 날짜의 장전 점검
   산출물이 없는가"* 로 판정한다. **이 보고서가 09:00 전에 저장되므로 경보는 울리지 않아야 정상이다.**
   09:00 이후 `watchdog.log`에 `MISSING_CHECK`가 찍히면 그것은 판정 로직 결함이다.
3. **`delta_baseline.*`가 08-18을 기준일로 잡는가** (커밋 2) — `previous_trading_day()` 신설분.
4. **`db.tables.execution_logs.rows`** (커밋 1) — 오늘 주문 왕복이 없으면 다음 왕복일로 이월.
5. **`데드라인이먼슬리에서끝남` 라벨의 이중 파서** (커밋 4) — `log_metrics.py:122`
   `_PRIORITY_LABEL = r"(?:우선순위위반|데드라인이먼슬리에서끝남)"`. 로테이션에 남은 이틀치
   옛 이름 로그가 「못 쟀다」로 떨어지지 않는지 장후에 확인.
6. **08-18 §7-1 질문** — 정각/30분 위상에 무엇이 겹치는가. 오늘 엔드포인트별 초 단위 분해.
7. **[신규] 15:45 `--prune-days 7`의 실제 삭제 범위** (§1-3) — 종료 배치 직후
   `auto/` 파일 목록을 삭제 **전후로 비교**한다. `_증거_*.md`만 사라져야 정상이고,
   `_지표.json`이나 루트 보고서가 함께 사라지면 즉시 P0다(로그는 이틀치만 남아 복구 불가).
8. **[신규] `instrument_selection.py` 순증 +183이 오늘 관측에 무엇을 바꿨는가** (§1-3) —
   선발 사이클 비용·선정 종목 분포를 08-18과 대조한다. 예측치가 없으므로 **판정이 아니라
   기술(記述)로 남기고**, 장후에 가설을 등재한 뒤 다음 거래일부터 검정한다.

---

## §4. Fix 구현계획 — **전부 장후 적용**

> 이 세션은 코드·설정·커밋·재기동을 **하지 않았다.** 아래는 계획이다.

### Fix#1 [P2] 예약 회차 시각 통일 (08-18 Fix#7 이월)

- **대상**: Claude 앱 예약 `mahdi-premarket-check` (리포 파일 아님)
- **변경**: `cronExpression` `"30 8 * * 1-5"` → `"0 8 * * 1-5"`
- **회귀 위험**: 없음. 07:36(REST 응답시간 첫 인쇄) 이후 08:33까지 새 사건 0건이 이미 확인됐다(08-14)
- **검증**: 다음 거래일 증거 다이제스트 첫 줄에 「밀렸다 ⚠」가 사라진다
- **부수**: `phases.md`와 `collect_evidence.PHASE_SCHEDULE`은 이미 08:00이므로 손댈 것 없음

### Fix#2 [P2] 수집기 §12 적신호 4번의 오탐 제거

- **대상**: `docs/동작점검/tools/collect_evidence.py` — `hypotheses.yaml` mtime 판정부
- **변경**: 날짜 문자열 비교 → **직전 보고서 파일의 mtime과 초 단위 비교**.
  `yaml.mtime < 직전보고서.mtime` 일 때만 「보고서 이후 무행동」을 낸다.
  오늘의 경우 `yaml 17:50:24 < 보고서 18:0x` 라 여전히 걸리므로, 한 단계 더 —
  **직전 보고서가 등재를 제안한 id가 yaml에 실재하는지**로 판정을 대체한다(§8-1이 이미 그 계산을 한다).
- **회귀 위험**: 낮음. 순수 리포팅 경로
- **검증**: 오늘 증거로 재실행해 적신호 4번이 사라지고 5번의 3건만 남는가

### Fix#3 [P1] 미커밋 **실변경**을 증거 §1이 이름으로 드러내게 한다

- **대상**: `docs/동작점검/tools/collect_evidence.py` §1 (「미커밋 N건」 생성부)
- **변경**: `git status --porcelain` 카운트 → `git diff --numstat` 으로
  `insertions != deletions` 인 항목만 **「내용 변경 N건」으로 분리하고 파일명을 전건 나열**한다.
  나머지는 「줄바꿈만 M건」 한 줄로 접는다. 미추적(`??`) 중 `mahdi/`·`scripts/`·`db/migrations/`
  아래 항목도 같은 절에 올린다 — 오늘 `033_option_analysis_series.sql`이 그렇게 숨었다.
- **회귀 위험**: 낮음. 순수 리포팅 경로. `git diff --numstat`은 바이너리에 `-`를 내므로 그 처리만 주의
- **검증**: 오늘 상태로 재실행해 「미커밋 123건(**내용 변경 10건** · 줄바꿈만 113건)」 +
  10개 파일명이 인쇄되는가. §1-3을 사람이 아니라 수집기가 먼저 잡는가
- **자기참조 주의**: 이 수집기 자신이 지금 미커밋 실변경(+75/0)이다. 고치는 김에 커밋한다
- **왜 P1인가**: 이 결함은 §1-3을 **덮은** 결함이다. 계측이 못 보는 축을 만드는 것보다,
  이미 있는 축을 노이즈로 덮는 것이 더 위험하다 — 후자는 「확인했다」는 착각을 준다
- (`.gitattributes` 정규화는 134파일을 한 커밋으로 만들어 `git log -p` 이력을 오염시키므로 **후순위**)

### Fix#4 [P1·사람] 이벤트 캘린더 갱신

- **대상**: `mahdi/config/event_calendar.yaml` — `covered_through`
- **절차**: `uv run python scripts/suggest_event_calendar.py` → 사람이 일정 확인 → 붙여넣기
- **주의**: 스크립트도 생성기도 **날짜를 옮기지 않는다**(설계상 사람 확인 강제)
- **고도화 후보**: `check_lever_due`가 *만료 며칠째*인지 세어 인쇄한다. 지금은 매일 같은 문구라
  하루 만료와 열흘 만료가 구별되지 않는다

### 미등재 가설 3건 [P2·6일째 이월]

`2026-08-13-p1-chain-input-phase` · `2026-08-14-fix1-expired-chain-blocks-options-flow` ·
`2026-08-14-fix4-priority-cut-visible`.
**등재 창은 개장(09:00)에 닫힌다**(소급 금지 — 숫자를 보고 예측을 고치는 것이 된다).
오늘 등재를 못 하면 이 셋은 `inconclusive`로 닫는 것이 정직하다. 08-19 5건은 제 시간에
등재됐으므로 **습관이 고쳐지는 중**이다 — 남은 셋만 정리하면 된다.

---

## §5. 고도화 방안 (당일 관측 근거)

### 고도화 1 — 「자동 적신호」에 **신뢰도 등급**을 붙인다

오늘 5개 적신호 중 **4개가 오탐**이었다(§2). 이 비율이면 다음 사람은 §12를 안 읽는다.
적신호를 **확정(기계가 단독으로 판정 가능)** / **후보(사람 확인 필요)** 두 절로 나눈다.
- 확정: ERROR 건수, 프로세스 기동 횟수, 로그 공백, 산출물 부재
- 후보: 회차 지연(예약 설정과 대조 필요), 멤버 미관측(전이 로깅이라 해석 필요), yaml 무행동
근거: 오늘 §12-2는 08-18 같은 시각과 대조하는 **한 줄**이면 즉시 걸러졌다.

### 고도화 2 — 전이 로깅 지표에 「마지막 전이 이후 경과」 대신 **직전 거래일 동시각 대조**를 인쇄

§12-2가 오탐이 된 이유는 `판단 형태 전이`가 전이 시에만 찍히는데 수집기가 그것을
연속 관측으로 읽었기 때문이다. 08-18 동시각(08:31) 로그도 **똑같이 1줄**이었다.
수집기 §5-2에 `직전 거래일 동시각: 1줄(동일)` 열을 추가하면 이 오탐이 구조적으로 사라진다.
**이 대조는 `delta_baseline.*`(커밋 2)이 이미 만든 「직전 거래일」 계산을 재사용한다** — 새 배선 아님.

### 고도화 3 — 레버 유예에 **사유 유무를 기계로 강제**한다

오늘 처음으로 레버 E의 유예에 사유·해제조건·기한이 다 적혔다(§2). 앞선 아홉 번은 없었다.
이 개선을 사람 의지가 아니라 테스트로 고정한다 —
`tests/test_ops_levers.py`에 *"`NEXT_TODO.md`에서 레버 id가 언급된 가장 최근 블록에
`사유:`와 `해제 조건`이 둘 다 있는가"* 를 넣는다. 없으면 실패.
`test_repo_levers_have_not_blown_their_unconditional_deadline`의 짝이다 —
그것은 *기한*을 지키게 하고, 이것은 *기록*을 지키게 한다.

---

## §6. dev_memory 반영 초안 (이 세션은 직접 수정하지 않았다)

> `DECISION_LOG.md` 에 append 제안. 헤딩 첫 단어는 `[MW0601]`.

```
### [MW0601] 2026-08-19 장전 — 자동 적신호 5건 중 4건이 오탐이었다

증상: 08-19 08:31 증거 §12가 적신호 5건을 냈고, 규약 대조 결과 4건이 오탐이었다.
      ① 「31분 밀렸다」 — 예약 cron이 08:30이라 정시 발화였다(phases.md만 08:00).
      ② 「flow_position 60분째 미관측」 — `판단 형태 전이`는 전이 시에만 찍히고
         08-18 동시각도 1줄뿐이었다.
      ③ 「yaml 무행동」 — mtime 08-18 17:50에 2026-08-19-fix2~fix6 5건이 실제 등재돼 있다.
         수집기가 날짜만 비교하고 시각을 안 본다.
      ④ 「미등재 4건」 중 2026-08-18-p5는 폐기된 항목이다(NEXT_TODO가 사유를 적고 닫았다).

원인: 수집기의 적신호가 전부 「기계 단독 판정」인 척한다. 실제로는 절반이
      다른 진실원천(예약 설정·직전 거래일 로그·보고서 mtime)과 대조해야 답이 갈린다.

결정: 적신호를 확정/후보 두 절로 나눈다(고도화 1). yaml 판정은 mtime이 아니라
      「직전 보고서가 제안한 id의 실재 여부」로 대체한다(Fix#2).

Why: 오탐률 80%짜리 경보는 진짜 경보를 죽인다. 08-04에 WARNING→INFO 강등으로
     362건이 0건으로 보고된 것과 방향만 반대인 같은 병이다.

How to apply: docs/동작점검/tools/collect_evidence.py — §12 생성부 분할,
              hypotheses mtime 비교 제거, git status → git diff --numstat.

검증: 오늘 증거로 재실행해 적신호가 5건 → 2건(미등재 3건 · 예정일 지난 8건)이 되는가.
      라이브 미검증 — 적용은 장후.
```

> `NEXT_TODO.md` 추가 제안 (체크박스):
> ```
> - [ ] [P2] 예약 mahdi-premarket-check cron "30 8" → "0 8" (08-18 Fix#7 이월, 리포 밖 설정)
> - [ ] [P2] collect_evidence §12를 확정/후보로 분할 + yaml mtime 판정 폐기
> - [ ] [P1] collect_evidence §1 — 내용 변경/줄바꿈-only 분리 + 실변경 파일명 전건 나열
> - [ ] [P1] 미커밋 실변경 10파일 작업 단위 커밋 (선발 축 / prune / gitignore·campaign)
> - [ ] [P1] 선발 축(`instrument_selection.py` +183) 가설 등재 — 커밋과 동시에, 숫자 보기 전에
> - [ ] [P1] `git add -f docs/동작점검/auto/*_지표.json` — .gitignore만 고치고 인덱스에 안 넣었다
> - [ ] [관측] 15:45 prune 전후 `auto/` 목록 비교 — 삭제 범위 실측
> - [ ] [P1] event_calendar.yaml covered_through 갱신 — 08-19 만료 1일째. 매일 재발 중
> - [ ] [P2] check_lever_due에 「만료 N일째」 카운터 추가
> - [ ] [P2] tests/test_ops_levers.py — 레버 유예 블록에 사유+해제조건 강제
> - [ ] 미등재 3건(08-13-p1 · 08-14-fix1 · 08-14-fix4) — 오늘 09:00까지 등재 못 하면 inconclusive로 닫는다
> ```

---

## §7. 자체 검증

- [x] 실행 시각 확인 — 08:31, **밀린 실행 아님**(예약이 08:30). 첫 줄에 명시
- [x] 보고서를 `docs/동작점검/` **루트**에 썼다 (`auto/` 아님)
- [x] 모든 이상점에 로그 시각·인용·파일명이 붙었다
- [x] 기준 대응: `phases.md` A절·B-3 / `README` 규약 H·「확정과 확인필요 분리」 / `NEXT_TODO.md:75-91` / `CURRENT_STATE.md`(ZN·ES 만성)
- [x] 이미 아는 항목을 새 발견으로 올리지 않았다 — 신규 **1건**(§1-3), 나머지는 이월·만성으로 표기
- [x] 초안이 틀린 곳을 지우지 않고 **정정 표로 남겼다** (§1-3)
- [x] 오늘 판정할 가설(1건, 규약 H로 미실행)과 레버 6종 상태를 전부 실었다
- [x] 자동 §0/§12 판정을 규약 E·F·H로 걸렀다 (§2)
- [x] **코드·설정·커밋·재기동 없음** — `git status` 변동 없음, 읽기와 보고서 작성만 수행

---

*증거 원본: `docs/동작점검/auto/2026-08-19_증거_pre.md` · 다음 회차 `mahdi-intraday-check` 12:33*
