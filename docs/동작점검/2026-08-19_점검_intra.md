# 마흐디 장중 점검 — 2026-08-19 (수)

- 국면 **intra** · 실행 **12:34~12:40 KST** (예약 `mahdi-intraday-check` 예정 12:30) — **정시 실행**
- 조사 범위 **07:30 기동 ~ 12:37**(정규장 진행 중) · 증거: `auto/2026-08-19_증거_intra.md` (12:34:52 생성, 54.2KB)
- 리포 HEAD `cd594df` · 브랜치 `master`
- **코드·설정·`hypotheses.yaml`·`dev_memory`를 하나도 바꾸지 않았다.** 장중 규약대로 Fix는 계획만 세운다.

---

## 한 줄 요약

**지금은 살아 있다 — 마지막 레코드 12:37:24(점검 시각 12:37:28, 지연 4초), 워치독
`{"at":"12:37:02","action":"ok","detail":"정상 — 마지막 박동 24초 전"}`.
그러나 오늘 정규장 안에서 관측 루프가 두 번 죽었고(09:51·10:32), 그 대가로 레짐이
`warmup_fallback`으로 58분을 돌았다.**

**⚠ P0 — 1건** (§2-1 정규장 중 프로세스 2회 사망, 그중 한 번은 트레이스백이 아예 없다).
P1 3건 · P2 3건.

> **개장 후 처음으로 사람이 볼 값어치가 있는 한 줄**: 장전 점검이 「기동 1회·공백 0건」으로
> 통과시킨 항목이 09:50 이후 전부 뒤집혔다. 오늘은 **`REGIME_RESTORE_SESSION_WINDOW`(fix7,
> 검증예정일 오늘)가 판정 가능한 날이 됐다** — 장전엔 "재기동이 없어 판정 불가"였다. 다만
> 레버가 OFF라 규약 H로 여전히 「미실행」이고, **대신 그 레버를 켰을 때 얻을 것의 크기가
> 오늘 두 번 실측됐다(29분 × 2).**

---

## §0. 장전 지적사항의 현재 상태 (`2026-08-19_점검_pre.md` 대조)

| 장전 항목 | 장전 판정 | 12:37 현재 | 변화 |
|---|---|---|---|
| **P0** | 없음 | **1건 발생** (§2-1) | ⬆ **악화** |
| §1-1 [P1] 이벤트 캘린더 만료 | 이월·미조치 | **미조치 그대로.** 개장 후에도 계속 경고 — `09:55:10` / `10:37:10` / `11:38:10` (총 6회, 시간당 ~60건 억제) | 변화 없음 |
| §1-2 [P2] 점검 회차 08:30 (규정 08:00) | 이월 | 오늘 장중 회차는 정시(12:30 예정 → 12:34 실행) | 장중은 정상 |
| §1-3 [P1·신규] 미커밋 실변경이 오늘 코드에 실림 | 신규 | **해소 방향.** 당일 커밋 10건(`cd594df`…`4a6902d`)이 들어갔으나 미커밋 **157건** 잔존 — 커밋 시각이 07:30 기동보다 늦으므로 **오늘 프로세스에 실린 것은 아니다** … **단, 09:54·10:36 재기동이 그 코드를 집어 올렸다**(§2-1 주) | ⚠ **전제 변경** |
| §1-4 [P2·만성] 미커밋 줄바꿈 노이즈 | 만성 | 157건으로 증가 | 변화 없음 |
| 레버 6종 | 「오늘 켤 레버 없음」 | 전부 장전과 동일값 (`OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS={}`, `REGIME_RESTORE_SESSION_WINDOW=False`, …) | **켜진 레버 없음** |
| 가설 `2026-08-12-fix7` (검증예정일 오늘) | 「재기동이 없어 그 경로가 안 돈다 → inconclusive 권고」 | **재기동 2회 발생 → 경로는 돌았다.** 그러나 레버 OFF이므로 규약 H로 **미실행** | ⚠ **전제 변경** |

> **장전이 틀린 것이 아니라 하루가 바뀌었다.** 장전 §A-2가 *"오늘은 기동 1회다 — 재기동이
> 없으면 그 경로 자체가 안 돈다"* 라고 적은 근거는 08:31 시점에 참이었다. 09:54·10:36에
> 그 전제가 깨졌고, **그 사실이 fix7의 판정 문구를 바꾼다**(§2-2).

---

## §1. 장중 체크리스트 (`phases.md` B절) — 전항 판정

### B-1. 생존

| 항목 | 실측 | 판정 |
|---|---|---|
| 관측 루프 생존 | 마지막 레코드 `12:37:24`, 점검 `12:37:28` — **지연 4초** | ✅ 살아 있다 |
| 정규장 안 3분+ 로그 공백 | **2건** — `09:50~09:54`(4분) · `10:32~10:36`(4분). 합계 **8분** | ❌ **위반** (§2-1) |
| 프로세스 기동 횟수 | **3회** — `07:30:19` / `09:54:23` / `10:36:22` | ❌ **재기동 2회** |
| 워치독 무기록 구간 | 07:40~12:37 10분 주기 34행, **25분+ 공백 0건**. 마지막 `12:37:02` → 현재까지 **26초** | ✅ 감시자 살아 있음 |
| `observation_loop_crash.log` | mtime **09:51:02 — 오늘 갱신됨** | ❌ **크래시 있음** (§2-1) |

### B-2. 수집 파이프라인

| 항목 | 실측 | 판정 |
|---|---|---|
| 사이클 관측 분 | 294분 (07시 29 / 08시 60 / 09시 55 / 10시 55 / 11시 60 / 12시 37) | ✅ 결손은 §2-1 공백 8분과 일치 |
| **예산 초과** | **117건** (09시 19 / 10시 29 / 11시 44 / 12시 25) | ⚠ 증가 추세 |
| **연속 타임아웃 조기 포기** | **26건** (09시 1 / 10시 20 / 11시 1 / 12시 4) — 10시에 집중 | ⚠ |
| **실패 예산 소진** | **0건** — 08-18에 19줄이 있었으므로 **계측은 살아 있고 진짜 0** | ✅ |
| 컷당한 북 분포 | `weekly_thu` 68 · `weekly_mon` 65 · **`regular,weekly_mon` 7** · **`regular,weekly_thu` 4** | ⚠ 먼슬리 11회 피컷 |
| **우선순위 불변식** | `데드라인이먼슬리에서끝남=예` **1건** — `10:11:51` | ❌ **위반 1건** (§2-3) |
| WS 단절 | **0건** — 08-12에 2줄 있었으므로 **계측 살아 있고 진짜 0** | ✅ |
| REST 실패 분해 | ReadTimeout 대다수(오늘 108번째까지 카운트) · `EGW00201` 13건 · ZN/ES 권한 12건 · `other` 급증 없음 | ⚠ 만성 범위 |
| ATM 롤링 | 180건 (08시 12 / 09시 75 / 10시 48 / 11시 27 / 12시 18) — 09시 이후 감소 | ✅ 왕복 폭주 아님 |

### B-3. 판단까지 갔는가

| 항목 | 실측 | 판정 |
|---|---|---|
| 앙상블 멤버 | 4/6 ×90 · 3/6 ×12 · 1/6 ×1. `options_flow` 최초 편입 **09:01:10**(08-14 이후 유지) | ✅ 죽은 축 없음 |
| 확신도 분포 | STANDARD 60 · SMALL_TEST 41 · HIGH_CONVICTION 1(`10:32:10`) · NO_TRADE 1(`07:31:10`) | ✅ |
| **레짐 갱신** | 전이 20건. 그러나 `(최초) → … 모델=warmup_fallback` 이 **3회**(08:46 / **09:55** / **10:37**) | ❌ **WARMUP 재진입 2회** (§2-2) |
| 체인 신선도(stale) | 로그 축 없음 — DB 축 `chain_input_source`가 답한다(장후) | — 계측 부재 아님 |
| 이벤트 캘린더 미기입 | 6건, 하루 종일 | ❌ 장전 §1-1 이월 |

### B-4. 하지 말아야 할 것

- [x] 코드·설정 변경 없음 · 커밋 없음 · 재기동 없음
- [x] 개입 필요성은 §3에 **손익으로** 제시 — 결정은 사람이 한다

---

## §2. 이상점

### 2-1. [P0·신규] 정규장 중 관측 루프가 두 번 죽었다 — 한 번은 postgres, 한 번은 **흔적이 없다**

**증상**: 09:51과 10:32에 관측 루프가 정지했고, 워치독이 각각 3분 뒤 재기동했다.
정규장 관측이 **8분** 사라졌다.

**근거**

```
logs/watchdog.log
[2026-08-19 09:50:01] OK — 정상 — 마지막 박동 6초 전
[2026-08-19 09:54:01] RESTART — 관측 루프 생존 신호 이상(stale) — 생존 신호가 185초째 갱신되지 않음(임계 180초, PID 8052)
[2026-08-19 09:54:01] 재기동 시도: 기동 스크립트 실행 완료
[2026-08-19 10:30:01] OK — 정상 — 마지막 박동 26초 전
[2026-08-19 10:36:01] RESTART — 관측 루프 생존 신호 이상(stale) — 생존 신호가 236초째 갱신되지 않음(임계 180초, PID 24840)
```

```
logs/observation_loop.log — 사망 직전 마지막 줄과 재개
09:50:52,591 WARNING:mahdi.main:옵션체인 수집 예산(50초) 초과 — … 컷당한북=weekly_mon
09:54:23,353 INFO:mahdi.main:직전 정상 기동: 2026-08-19 07:30:19 (2.4시간 전)      ← 1차 재기동
10:32:10,140 INFO:mahdi.fusion.engine:판단 형태 전이: … (4/6) · HIGH_CONVICTION · 전략 ['straddle_accumulate']
10:36:22,458 INFO:mahdi.main:직전 정상 기동: 2026-08-19 09:54:23 (0.7시간 전)      ← 2차 재기동
```

**1차 사망의 원인 — 확정.** `logs/observation_loop_crash.log` mtime **09:51:02**, 마지막 프레임:

```
  File "…\mahdi\main.py", line 1481, in handle_message
    with db.get_connection() as conn:
  File "…\mahdi\data\db.py", line 97, in get_connection
    conn = psycopg.connect(settings.dsn)
psycopg.OperationalError: connection failed: connection to server at "127.0.0.1", port 5432 failed:
    server closed the connection unexpectedly
```

WS 메시지 핸들러(`main.py:1481`)가 매 메시지마다 새 커넥션을 여는 구조인데, postgres가
연결을 끊자 그 예외가 **`asyncio.gather` 최상단까지 올라가 프로세스를 종료시켰다.**
`handle_message`에 DB 예외 가드가 없다.

**2차 사망의 원인 — 불명. 그것이 이 항목을 P0로 만든다.**
크래시 로그 mtime은 **09:51:02에서 움직이지 않았다**(점검 시각 12:37 확인). 즉 10:32의
정지는 **stderr에 한 줄도 남기지 않았다.** 관측 로그에도 psycopg/OperationalError 문자열은
당일 **0건**이다(`grep -iE 'psycopg|OperationalError' → 0`). 남은 가능성은 둘 —
(a) 프로세스가 **행(hang)** 상태였고 워치독의 taskkill이 트레이스백 없이 죽였거나,
(b) 죽었는데 stderr 버퍼가 유실됐다. **지금 로그로는 가를 수 없다.**

**기준**: `phases.md` B-1 *"정규장 안의 로그 공백은 1분마다 사이클이 도는 게 정상이라 **3분이면
사건이다**"*. 그리고 규약 C — *"건수 0은 두 가지다"*: 2차 사망의 「원인 0건」은 **계측이 없는
쪽**이다.

**영향**
- 관측 8분 소실(09:50~09:54, 10:32~10:36) — 그 분들의 체인·수급·VPIN은 영구 결손이다.
- 재기동이 딸린 대가는 §2-2(레짐 WARMUP 58분).
- ⚠ **재기동 스크립트가 `start_mahdi_premarket.bat` 전체다** — 장중에 `docker compose up -d`와
  마이그레이션 **001~033 전건**을 라이브 DB에 재적용했다(`premarket_startup.log` 09:54:08~09:54:13,
  10:36:08~10:36:13). 오늘은 무사했으나 이것은 설계 의도가 아니다.
- ⚠ **오늘 코드 동일성 전제가 깨졌다.** 07:30 프로세스는 HEAD `595f75d` + 미커밋분이었는데,
  09:54·10:36 프로세스는 **당일 커밋 10건이 반영된 디스크**를 읽었다. **09:54 이후 산출물과
  09:54 이전 산출물은 같은 코드의 것이 아니다** — 장후 판정에서 이 경계를 지켜야 한다.

**왜 P0인가**: 장중 두 번, 각각 3~4분의 사각지대가 생겼고 **두 번째 원인을 모른다.**
세 번째가 14:50 컷오프 근처에 오면 그 분의 판단이 통째로 없어진다.

---

### 2-2. [P1·신규 관측] 재기동의 대가가 오늘 두 번 실측됐다 — 레짐 `warmup_fallback` **58분**

**증상**: 재기동마다 레짐 파이프라인이 세션 창을 잃고 `(최초)`부터 다시 시작한다.

**근거**

```
08:46:00 레짐 전이: (최초) → VOL_COMPRESSION (안정=False, 모델=warmup_fallback, 창=1분)   ← 정상 기동
09:15:00 레짐 전이: VOL_COMPRESSION → RANGE_BREAK_PREP (안정=True, 모델=predict, 창=30분)
  … 재기동(09:54:23) …
09:55:01 레짐 전이: (최초) → VOL_COMPRESSION (안정=False, 모델=warmup_fallback, 창=1분)
10:24:01 레짐 전이: VOL_COMPRESSION → RANGE_BREAK_PREP (안정=True, 모델=predict, 창=30분)   ← 29분
  … 재기동(10:36:22) …
10:37:01 레짐 전이: (최초) → VOL_COMPRESSION (안정=False, 모델=warmup_fallback, 창=1분)
11:06:00 레짐 전이: VOL_COMPRESSION → RANGE_BREAK_PREP (안정=True, 모델=predict, 창=30분)   ← 29분
```

**기준**: `hypotheses.yaml` `2026-08-12-fix7-regime-session-window-restore` 근거 —
*"08-12 실측: predict 09:31~10:13 → 재기동(10:14) → warmup 10:14~10:42 → predict 10:43 재개.
**29분의 판단이 `regime_hmm` 없이 갔다**"*.
**오늘 29분이 두 번 더 나왔다. 08-12의 29분은 우연이 아니라 상수다**(warmup 최소창 30분).

**판정 — 규약 H로 「미실행」이 맞다.** 검증예정일이 오늘이고 재기동도 있었으나
`REGIME_RESTORE_SESSION_WINDOW = False`다(`mahdi/engines/regime_pipeline.py`). 레버가 꺼진
날의 숫자로 그 레버의 가설을 판정하지 않는다. **오늘 값은 확인도 반증도 아니라 「기준선」이다.**

- 주장 지표 `db.regime.warmup_minutes` — **레버 OFF 기준선 29분 × 2 = 58분**(예측 목표 ≤5분)
- 대가 지표 `db.regime.visits` — 전이 **20회**(임계 100회). 채터링 회귀 없음. **레버 OFF 기준선이다**

**영향**: 정규장 09:00~12:37 **217분** 중 **58분(26.7%)** 이 `predict`가 아니라
`warmup_fallback` 위에서 판단했다. 여기에 관측 공백 8분을 더하면 **66분(30.4%)** 이 열화 구간이다.
그 구간에도 `regime_hmm`은 가용멤버로 계속 인쇄됐다(`09:55:10` 3/6 → `09:56:10` 4/6) —
**「멤버가 살아 있다」는 「모델이 predict다」가 아니다.** 형태 전이 로그만 보는 눈은 이 58분을
구조적으로 못 본다.

**왜 P0가 아닌가**: 판단이 멈춘 것이 아니라 열화한 것이고, 오늘 실집행은 없다.

---

### 2-3. [P1·재발] 우선순위 불변식 위반 1건 — `10:11:51`

**증상**: 데드라인 컷이 **먼슬리(regular)에서 끝났다.** 20레그 중 **1레그만 적재**하고 17레그를 버렸다.

**근거**

```
10:11:51,359 WARNING:mahdi.main:옵션체인 수집 예산(50초) 초과 — 남은 17레그를 포기하고 1레그로
  이번 분을 마감합니다 (다음 분 사이클을 정시에 시작하기 위함, §2-6/Fix#8)
  컷당한북=regular,weekly_thu · 데드라인이먼슬리에서끝남=예
```

당일 전체 144건 중 `=예`는 **이 1건뿐**이다(`=아니오` 143건). 먼슬리가 컷 목록에 포함된 것은
11건(`regular,weekly_mon` 7 · `regular,weekly_thu` 4)이나, **그중 데드라인이 먼슬리에서 끝난 것은
이 1건**이다 — 나머지 10건은 연속 타임아웃 경로라 우선순위 판단이 걸리지 않는다.

**기준**: `phases.md` B-2 *"컷당한 북 분포 — **먼슬리(regular)가 먼저 잘렸으면 우선순위 불변식
위반**"*. 그리고 `2026-08-18_마흐디_운영점검보고서.md` 미완료 체크박스 첫 줄 —
`- [ ] [P1] 데드라인 컷 경로 우선순위 보호 배선 + 회귀 테스트 (Fix#1)`.

**만성/신규 구분**: **신규가 아니다.** 08-18에 이미 P1으로 잡혀 배선이 미구현인 채 이월된
결함이고, 오늘은 그 결함의 **실물 1건**이 관측됐다. 08-18의 계측(라벨 개명 `우선순위위반` →
`데드라인이먼슬리에서끝남`)이 실제로 일을 했다는 증거이기도 하다 — 옛 라벨은 오늘 0건,
새 라벨로 정확히 잡혔다.

**영향**: 10:11 한 분의 GEX/감마플립 입력이 1레그다. 같은 시간대에 감마플립 산출 불가 경고가
9건 있었다(`10:06:10`~`12:31:10`, *"BS 계산 가능 레그 1개(전체 1개, 최소 6 필요)"*).

---

### 2-4. [P1·이월 2일째] 이벤트 캘린더가 만료된 채 반나절이 지났다

**증상**: 장전 §1-1의 조치 권고가 이행되지 않았고, 개장 후에도 계속 경고 중이다.

**근거**

```
07:31:10 / 08:32:10 / 09:32:10 / 09:55:10 / 10:37:10 / 11:38:10 [WARNING] 이벤트 캘린더 미기입:
  not_covered — covered_through(2026-08-18)가 오늘(2026-08-19)보다 앞선다.
  이벤트 근접 페널티가 걸리지 않는 상태다   (최근 3600초간 59~60건 추가 억제됨)
```

**기준**: `phases.md` B-3 *"이벤트 캘린더 미기입 건수(미기입은 페널티가 아예 안 걸린 것과 같다)"*.

**장전 대비 변화**: 장전은 *"가용멤버 1/6·NO_TRADE라 메타 라벨까지 가는 신호 자체가 아직 없다"*
를 근거로 P0가 아니라고 했다. **그 근거가 지금은 성립하지 않는다** — 09:01 이후 4/6이 유지되고
`STANDARD` 60분 · `HIGH_CONVICTION` 1분(`10:32:10`)이 나왔다. 즉 **오늘 오후 메타 라벨은
이벤트 페널티 없이 계산된다.** 그래도 P1인 이유는 실집행 경로가 없기 때문이다.

**조치(사람)**: `uv run python scripts/suggest_event_calendar.py` → `covered_through` 확인.
**재기동 불필요**(`get_event_calendar()`는 읽기 시점 평가) — 장중 코드 변경 금지에 걸리지 않는
설정 파일 갱신이다. 다만 이 세션은 손대지 않았다.

---

### 2-5. [P2] 10시 창이 절벽 앞 한 뼘까지 갔다가 물러났다 — 그러나 12시가 다시 오르고 있다

**증상**: `inquire-price` 창 최대 p50 ÷ read timeout(4.0초)이 10시에 **0.91**로 경고선(0.8)을 넘었다.

**근거** (`auto/2026-08-19_증거_intra.md` §5-1, 창 최대 기준)

| 시간대 | 사이클 | rows=0 | REST수집 평균 | 예산 대비 | 창 최대 p50 | **최대/timeout** | rows<10 |
|---|---|---|---|---|---|---|---|
| 07시 | 29 | 0 | 19.5초 | 39% | 0.02 | 0.01 | 0 |
| 08시 | 60 | 0 | 19.5초 | 39% | 0.02 | 0.01 | 0 |
| 09시 | 55 | 0 | 41.1초 | 82% | 2.08 | 0.52 | 1 |
| **10시** | 55 | **1** | 50.4초 | **101%** | **3.64** | **0.91 ⚠** | **19** |
| 11시 | 60 | 0 | 49.0초 | 98% | 2.47 | 0.62 | 2 |
| 12시 | 37 | 0 | 49.3초 | 99% | 2.72 | **0.68** | 5 |

당일 유일한 ERROR도 그 창에 있다:

```
10:10:53 [ERROR] mahdi.main: 옵션체인 이번 분 전멸 — 수집 20레그 중 적재 0행(예산 48.7초 소진,
  재시도 함). 이 분의 체인은 비었다 — 판단은 신선도 창 안의 직전 스냅샷을 쓴다
```

**기준**: `phases.md` B-2 · 증거 §5-1 주석 — *"창 최대 p50 ÷ read timeout이 1.0에 닿으면 그 창의
호출 절반 이상이 타임아웃이고, 20레그 순차 수집의 기대 성공 수는 0에 수렴한다."*

**판정**: **절벽에 닿지 않고 물러났다.** 연속 `rows=0` 최장 구간은 **1분**(10:10~10:10, 임계 20분).
08-14형 전멸(24분 뒤 절벽)은 오늘 재현되지 않았다. 다만 **10시 rows<10이 55분 중 19분**으로,
체인 두께 자체는 그 시간대에 반토막이었다.

**왜 P2인가**: 자기 회복했고 안전장치(연속 타임아웃 조기 포기, 먼슬리 우선 재시도 26건)가
설계대로 동작했다. **다만 12시 비율이 0.62 → 0.68로 다시 오르고 있다 — 14:30 회차가 볼 것**(§5).

**⚠ 교란 요인**: 10시 창 안에 재기동(10:36)이 있어 **레이트리밋 백오프 상태와 토큰이 초기화됐다.**
10시 숫자를 시장 요인만으로 해석하면 안 된다.

---

### 2-6. [P2·만성] ZN/ES 권한 결손 — 새 발견이 아니다

```
07:32:35 / 09:47:38 / 10:37:46 ZN(10년 국채선물) 근월물 조회 실패: ZNU26 — EGW00552 "CBOT SUB거래소 신청 계좌가 아닙니다"
07:32:39 / 09:47:42 / 10:37:52 ES(E-mini S&P500) 근월물 조회 실패: ESU26 — EGW00550 "CME SUB거래소 신청 계좌가 아닙니다"
```

`CURRENT_STATE.md`가 2026-07-10부터 「KIS 계좌 게이트, 코드는 완성 상태」로 기록한 항목이다.
각 6건. **만성으로만 적고 이상점으로 올리지 않는다.**

---

## §3. 지금 개입할 것인가 — 손익 (결정은 사람)

**권고: 개입하지 않는다.** 다만 판단 재료를 그대로 둔다.

| | 재시동 **없이** 두면 | 지금 **재시동**하면 |
|---|---|---|
| 얻는 것 | 오늘 남은 관측의 연속성. 12:37 현재 레짐은 `predict`이고 멤버 4/6이다 — 지금 끊으면 **또 29분 warmup**이다(§2-2 실측 2회) | 당일 커밋 10건이 프로세스에 실린다. `f24c452`(깨진 master 복구)·`83bb764`(import 검증)가 오늘 검증된다 |
| 잃는 것 | 당일 커밋 10건은 오늘 미검증(`untested`) | **확실히 29분 warmup + 관측 3~4분 공백.** 14:50 컷오프까지 여유가 줄어든다 |
| 사실 | **09:54·10:36 재기동이 이미 당일 커밋을 집어 올렸다** — 지금 도는 프로세스는 10:36 시점 디스크다. 즉 **재시동으로 새로 얻을 코드가 거의 없다** | — |

**결론**: 재시동의 이득(코드 반영)은 **이미 우발적으로 실현됐고**, 비용(29분 warmup)은 확정이다.
**개입 불필요.** §2-1의 원인 추적과 §4 Fix는 전부 장후로 넘긴다.

**단, 사람이 지금 1분 안에 할 수 있고 재기동이 필요 없는 것 하나**: `event_calendar.yaml`의
`covered_through` 갱신(§2-4). 이것만은 지금 해도 잃는 것이 없다.

---

## §4. Fix 구현계획 — **전부 장후 적용** (장중 규약)

### Fix#1 [P0] `handle_message`의 DB 예외가 프로세스를 죽이지 못하게 한다

- **파일**: `mahdi/main.py::handle_message` (line 1481 근처), `mahdi/data/db.py::get_connection`
- **변경**: `with db.get_connection() as conn:` 블록을 `try/except psycopg.OperationalError`로 감싸고,
  실패 시 (a) `WARNING:mahdi.main:WS 적재 스킵 — DB 커넥션 실패 N회 연속` 로그를 남기고
  (b) 그 메시지를 버리되 **루프는 유지**한다. 연속 실패가 임계(예: 30회)를 넘으면 그때 ERROR로 승격.
- **회귀 위험**: DB가 정말 죽었을 때 조용히 데이터를 버리게 된다 → 그래서 **연속 실패 카운터와
  ERROR 승격이 세트여야 한다.** 카운터 없이 except만 다는 것은 금지.
- **검증**: `tests/test_main_ws_handler.py`(신규) — `get_connection`이 `OperationalError`를 던지도록
  monkeypatch하고 `handle_message` 100회 호출 후 예외가 밖으로 안 나오는가 + 카운터가 100인가.

### Fix#2 [P0] 2차 사망처럼 **흔적 없는 정지**를 다음엔 잡는다

- **파일**: `scripts/watchdog_observation_loop.py::_restart()`
- **변경**: taskkill **직전에** 대상 PID의 스택을 덤프해 `logs/observation_loop_stall_<PID>_<시각>.txt`로
  남긴다(Windows: `py-spy dump --pid` 또는 최소한 `Get-Process`의 스레드 수·CPU·핸들 수).
  **행(hang)과 사망을 가르는 유일한 증거가 그것이다.**
- **회귀 위험**: 덤프가 느리면 재기동이 늦어진다 → 타임아웃 3초, 실패해도 재기동은 그대로 진행.
- **검증**: 장후에 손으로 관측 루프를 SIGSTOP 유사 상태로 만들고 워치독을 1회 돌려 파일 생성 확인.

### Fix#3 [P1] 재기동이 **장전 배치 전체**를 돌리지 않게 한다

- **파일**: `scripts/start_mahdi_premarket.bat`, `scripts/watchdog_observation_loop.py`
- **변경**: `start_mahdi_premarket.bat`에 `--observation-only` 스위치를 추가해
  **docker compose·마이그레이션 001~033·COCKPIT 기동을 건너뛰고** 관측 루프만 띄운다.
  워치독의 재기동 호출을 그 스위치로 바꾼다.
- **근거**: 09:54:08~09:54:13, 10:36:08~10:36:13에 라이브 DB에 33개 마이그레이션이 재적용됐다.
  오늘은 멱등이라 무사했지만 **장중에 스키마를 건드리는 경로가 열려 있는 것 자체가 결함**이다.
- **회귀 위험**: docker가 실제로 죽어서 재기동이 필요한 경우를 못 살린다 → `docker info` 1회만
  확인하고 실패 시에만 전체 경로로 폴백.

### Fix#4 [P1] 데드라인 컷 우선순위 보호 배선 (**08-18 Fix#1 이월 — 오늘 실물 1건**)

- **파일**: `mahdi/main.py` 옵션체인 수집 루프의 데드라인 컷 분기
- **변경**: 남은 예산이 바닥나도 **`regular`(먼슬리) 레그는 마지막까지 컷 대상에서 제외**한다.
  현재는 잔여 레그 순서대로 잘려 `10:11:51`처럼 먼슬리에서 끝나는 경우가 생긴다.
- **검증**: 회귀 테스트 — 예산 0에 가까운 상태에서 `regular` 레그가 남으면
  `데드라인이먼슬리에서끝남=예`가 절대 발생하지 않는가.
- **예측치 등재**(구현 세션에서 숫자 보기 전에): `logs.priority_violation.count == 0`,
  대가 지표로 `logs.chain.rows_p50`(먼슬리 보호로 위클리 두께가 얇아지는 대가).

### Fix#5 [P1·사람] `event_calendar.yaml` 갱신 — 2일째

- `uv run python scripts/suggest_event_calendar.py` → `covered_through` 이동. 재기동 불필요.
- **매일 재발 중이므로 「며칠째인가」를 세는 눈이 필요하다** → Fix#6.

### Fix#6 [P2] 「N일째 이월」을 수집기가 세게 한다

- **파일**: `docs/동작점검/tools/collect_evidence.py` §5 「항상 인용하는 사건」
- **변경**: `event_calendar` 같은 항상-인용 사건에 **직전 거래일 다이제스트를 읽어 연속 일수**를
  붙인다 — `이벤트 캘린더 미기입 ×6 (연속 2일째)`.
- **근거**: 장전 §1-1이 「이월」이라 적었으나 며칠째인지는 사람이 세야 했다.

### Fix#7 [P2] 시간대 요약에 **재기동 경계**를 표시한다

- **파일**: `docs/동작점검/tools/collect_evidence.py` §5-1 표
- **변경**: 시간대 행에 그 창 안의 재기동 시각을 주석으로 단다(`10시 … ⟳10:36`).
- **근거**: §2-5의 10시 숫자는 재기동으로 오염됐는데 표만 보면 알 수 없다.

### 미등재 가설 4건 [P2·7일째 이월]

`2026-08-13-p1-chain-input-phase` · `2026-08-14-fix1-expired-chain-blocks-options-flow` ·
`2026-08-14-fix4-priority-cut-visible` · `2026-08-18-p5-deadline-cut-respects-priority` —
**등재 창(개장 09:00)은 이미 닫혔다.** 소급 금지이므로 오늘은 등재하지 않고,
장후에 `inconclusive`로 닫거나 내일 08:00 이전 등재를 결정한다.

### 예정일 지난 pending 8건

`2026-08-14-fix2/fix3/fix6`, `2026-08-16-p1/fix1/b1/c1/c2` — 전부 **1일 지남**(예정 08-18).
장후 회차에서 손 판정하거나 `inconclusive`로 닫는다. **오늘 숫자로 어제 항목을 판정하지 않는다.**

---

## §5. 14:30 회차가 반드시 볼 것 (인계)

1. **세 번째 사망이 있었는가.** `기동 4회`가 되면 §2-1은 P0 유지가 아니라 **에스컬레이션**이다.
   확인: `logs/watchdog.log` RESTART 행 수 · `observation_loop_crash.log` mtime이 09:51에서 움직였는가.
2. **12시 기울기.** 창 최대 p50/timeout이 09시 0.52 → 10시 **0.91** → 11시 0.62 → 12시 **0.68**.
   **13시·14시 창 최대가 0.8을 다시 넘는가**가 14:50 컷오프 개입 판단의 재료다.
   08-14는 13:51 창에서 0.90을 처음 넘고 그 뒤 전멸이 왔다.
3. **먼슬리 컷.** `데드라인이먼슬리에서끝남=예`가 오후에 늘어나는가(현재 1건).
4. **레짐이 `predict`를 유지하는가.** 11:06 이후 재진입 없음. 또 warmup으로 돌아가면 재기동이 있었다는 뜻이다.
5. **이벤트 캘린더가 갱신됐는가**(§2-4).

---

## §6. dev_memory 반영 초안 (이 세션은 직접 수정하지 않았다)

> ### [MW0601] 2026-08-19 장중 — 정규장 중 2회 사망, 그중 하나는 트레이스백이 없다
>
> - **증상**: 09:51·10:32에 관측 루프 정지 → 워치독이 09:54:01·10:36:01 재기동. 관측 8분 소실.
> - **원인**: 1차는 확정 — `main.py:1481 handle_message`의 `db.get_connection()`에서
>   `psycopg.OperationalError: server closed the connection unexpectedly`가 최상단까지 전파.
>   **2차는 불명** — `observation_loop_crash.log` mtime이 09:51에서 안 움직였고 관측 로그에도
>   psycopg 문자열 0건. 행(hang) 후 taskkill인지 stderr 유실인지 가를 계측이 없다.
> - **결정**: 장중 규약대로 코드 변경 없음. 장후 Fix#1(예외 가드+연속 실패 카운터)·
>   Fix#2(taskkill 직전 스택 덤프)·Fix#3(재기동 시 장전 배치 전체 금지) 적용.
> - **Why**: 프로세스 사망은 관측 공백만이 아니라 **레짐 warmup 29분**을 딸려 온다 —
>   오늘 두 번, 각각 정확히 29분(09:55→10:24, 10:37→11:06). 08-12의 29분과 같은 값이다.
> - **부수 발견**: 워치독 재기동이 `start_mahdi_premarket.bat` 전체를 돌려 **장중에 마이그레이션
>   001~033을 라이브 DB에 재적용**했다(09:54:08, 10:36:08). 멱등이라 무사했으나 설계 의도 아님.
> - **부수 발견 2**: 재기동이 **당일 커밋 10건을 우발적으로 프로세스에 실었다.** 09:54 이전과
>   이후 산출물은 같은 코드의 것이 아니다 — 장후 판정에서 이 경계를 지킬 것.
> - **검증**: 라이브 미검증. Fix 적용 후 다음 재기동일에 warmup 재진입 시간으로 확인.
>
> ### NEXT_TODO 추가안
> - [ ] [P0] `handle_message` DB 예외 가드 + 연속 실패 카운터 + ERROR 승격 (Fix#1)
> - [ ] [P0] 워치독 taskkill 직전 스택 덤프 — 행/사망 구분 (Fix#2)
> - [ ] [P1] 재기동 경로 `--observation-only` — 장중 마이그레이션 재적용 차단 (Fix#3)
> - [ ] [P1] 데드라인 컷 먼슬리 보호 — 08-18 Fix#1 이월, 08-19 10:11:51 실물 1건 (Fix#4)
> - [ ] [P1] `event_calendar.yaml` covered_through 갱신 — **2일째** (Fix#5)
> - [ ] [P2] 수집기: 항상-인용 사건에 연속 일수 인쇄 (Fix#6)
> - [ ] [P2] 수집기 §5-1 시간대 표에 재기동 경계 표시 (Fix#7)
> - [ ] [가설] `2026-08-12-fix7` — 레버 OFF 기준선 **warmup 29분/회, 전이 20회** 를 판정근거에 남기고
>       검증예정일을 오프라인 재생 완료일로 이동. **오늘 값으로 확인/반증하지 않는다(규약 H)**

---

## §7. 자체 검증

- [x] 실행 시각(12:34 / 예정 12:30)을 확인하고 **정시 실행**임을 첫 줄에 밝혔다 — 밀린 실행 아님
- [x] 보고서를 `docs/동작점검/` **루트**에 썼다(`auto/`가 아니다). 증거는 `auto/`에 그대로 뒀다
- [x] 관측 루프가 지금 살아 있는지를 **시각으로** 답했다 — 마지막 레코드 12:37:24 / 점검 12:37:28
- [x] 워치독 무기록 구간을 확인했다 — 25분+ 공백 0건, 마지막 기록 12:37:02(26초 전)
- [x] 장전 지적사항 5종의 현재 상태를 §0 표로 전건 답했다
- [x] 모든 이상점에 로그 시각과 인용을 붙였다
- [x] 「건수 0」을 규약 C로 갈랐다 — WS 단절 0·실패예산 0은 **진짜 0**(대체 축 확인), 2차 사망 원인 0은 **계측 부재**
- [x] 자동 §0/§12 적신호를 규약 E·F·H로 걸렀다 — fix7은 재기동이 있었어도 **레버 OFF라 미실행**(H)
- [x] 만성(ZN/ES)과 신규를 분리했고, 08-18 이월분(먼슬리 컷)을 새 발견으로 올리지 않았다
- [x] **코드·설정·`hypotheses.yaml`·`dev_memory`를 하나도 바꾸지 않았다.** 커밋·재기동 없음
- [x] Fix 계획이 파일·함수 수준까지 내려갔다
