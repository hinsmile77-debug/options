# 마흐디 일일점검 — 2026-09-14 (월)

- 국면: **장전(pre)** · 실행: 예정 08:00 → 실제 08:01:29 KST — **정시 실행**(1분 29초 이내, 밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-14_증거_pre.md`
- HEAD: `1186372`(브랜치 `master`, 미커밋 25건, 당일 커밋 0건)
- 직전 거래일: 2026-09-11(금). 09-12(토)·09-13(일)은 비거래일이라 건너뛴다.

---

## §0. 오늘 한 장

**오늘 아침 프로그램은 어제(9/11)와 마찬가지로 깔끔하게 켜졌다.** 07시 30분 정각에 기동을
시작해 07시 30분대에 데이터베이스·화면·실시간 시세 연결이 전부 끝났고, 지금(08시 01분) 기준
심각한 오류(ERROR)는 0건, 감시 프로세스(워치독)도 정상이다.

**사람이 지금(개장 전) 정해야 할 결정이 하나 이월돼 있다.** "오전 9시대 시세 조회가 유독
느려지는 문제"에 대응해 조회 주기를 늘릴지 정하는 결정이 9월 9~11일 **사흘 연속** 안
정해진 채 넘어갔다(그때 만든 규칙 — "3거래일 이상 미루면 다음 장전이 반드시 고른다,
유예 재연장 금지" — 을 사흘 만에 어겼다). 9/11 보고서는 "내일(9/12) 장전이 반드시 정할
것"이라 적었지만 9/12는 토요일이라 그 장전 점검 자체가 없었다 — **그래서 이 결정은 아직
한 번도 다시 다뤄지지 않은 채 오늘까지 왔다.** 자세한 내용과 선택지는 이 파일 맨 끝
「사용자 조치」 1번을 볼 것.

**문제 건수**: 오늘 새로 찾은 것 0건. 어제 이전부터 이어지는 것 7건 — 그중 **결정이 급한
것 1건**(위 혼잡 시간대 레버)과 판정이 밀린 가설 6건, 나머지는 만성 운영 부담(이벤트
캘린더 만료, ZN/ES 거래소 권한 결손, 미커밋 파일 누적)이다. 전부 합쳐 **심각한 것(P0,
오늘 즉시 관측·거래를 위협하는 수준)은 없다.**

**개장 전 사람 조치: 결정 1건 필요** (9시 혼잡 레버 — 아래 1-1 및 「사용자 조치」 참조)
**(관측 범위 07:30~08:01 KST). 이 범위 밖은 모른다.**

---

## 제1부 — 장전 (07:30 ~ 09:00)

### 1-0. 전일 이월

전일(2026-09-11) 보고서의 「사용자 몫으로 남은 것」 8건을 전부 확인한다. 그 사이 9/12·9/13은
비거래일이라 어느 것도 시장에서 재검증될 기회가 없었다 — 아래 처분은 전부 **"그대로
이월"**이다.

| 전일 항목 | 처분 | 근거 |
|---|---|---|
| 1. 혼잡 시간대(9시) 레버 결정 — "내일(9/12) 반드시" | 🔄 지속 (미착수, 결정 기회 자체가 없었음) | 9/12는 토요일. `NEXT_TODO.md:22` 그대로 |
| 2. Fix 후보 K(§8-4 계산기 축 변경) 착수 여부 | 🔄 지속 | `NEXT_TODO.md:36`, 09-10 fix-d refuted 이후 미착수 |
| 3. 이벤트 캘린더 `covered_through` 9월분 | 🔄 지속 (만성) | `event_calendar.yaml:70` 여전히 `2026-08-28` |
| 4. 미커밋 27건 정리 | 🔄 지속 (25건으로 소폭 변동) | 오늘 `git status --porcelain` 25건 — 아래 1-6 |
| 5. `.gitignore:52` (`_지표.md`·`_증거_*.md` 저장 정책) | 🔄 지속 | 정책 변경 항목, 미착수 |
| 6. 점검 세션 읽기 전용 DB 접근 | 🔄 지속 | 이번 회차도 DB 미접근(`--no-db` 전제로 수집) |
| 7. `ruff` 미설치 | 🔄 지속 (여덟 회차 연속) | 오늘 세션은 코드 변경이 없어 재확인 안 함 |
| 8. 예약 작업 완결 판정(스킬 §1-2) | ✅ 참고만 — 장후 세션 소관 | 장전 세션은 이 항목을 판정하지 않는다 |

관측 항목도 이월한다 — 9/11 §6-6이 스스로 남긴 것과 §3-4의 "지난 가설 4건"은 모두 아래
「오늘 관측이 답해야 할 것」에서 다시 연다.

### 장전 체크리스트 (phases.md A절)

**A-1 기동** — 전부 확인됨.
- `premarket_startup.log`: 07:30:00.29 시작, docker 07:30:22 준비, 마이그레이션 33건 이상
  적용(021~033 표시 확인), 07:30:47 완료. **재기동 0회**(「직전 정상 기동」 표식 1회만).
- `.last_successful_start.txt` = `2026-09-14T07:30:47`, `.last_cockpit_start.txt` =
  `2026-09-14T07:30:58` — 둘 다 오늘 시각.
- 워치독: 당일 3행(OK 3 / 비-OK 0), 호출 40회(idle 18 · ok 22), 5분 이상 호출 공백 0건.

**A-2 오늘의 계획**
- `hypotheses.yaml` 검증예정일 도래(지남 포함) pending 6건 — 아래 1-2.
- `NEXT_TODO.md`의 「켤 것」 레버 확인: 오늘 새로 켤 레버는 없다. 다만 **이미 켜져 있는데
  무조건발동일이 지난 채 남은 레버 3건**을 찾음 — 아래 1-5(정리 항목, 위반 아님).
- 커밋 선후: 당일 커밋 0건. 직전 커밋(`1186372`, 09-11 16:37경)은 관측 루프 기동(07:30:47)보다
  훨씬 이르므로 오늘 프로세스에 정상적으로 실려 있다.
- 🔔 사전 대응 규칙: 어제(09-11) 로그·지표에서 `🔔 사전 대응 규칙 조건 성립` 문구를 찾지
  못했다 — 발동 창 문제 자체는 오늘 새로 열리지 않는다. 단, 아래 1-1은 **같은 뿌리(9시
  혼잡)의 별개 결정**(위클리 폴링 주기)이라 헷갈리지 않는다.
- `--verify-report` (전일 09-11 보고서 대상) 실행 결과: 폐기 목록과 부딪히는 줄 없음(✅).
  다만 **리포가 지목한 경로 1개가 저장소에 없다** — `tests/test_evidence_collector_deferral_axis.py`.
  ⚠ 판정 아님(오탐일 수 있음) — 아래 관측 항목 ②로 넘긴다.

**A-3 전일 이월** — 위 1-0 표로 처리 완료. `docs/동작점검/auto/2026-09-11_지표.json` 존재
확인(65.3KB, 09-11 15:46).

**A-4 판정 문구** — 이 파일은 "P0 없다"고 쓰지 않는다. 위 §0·1-0의 「개장 전 사람 조치:
결정 1건 필요」가 그 대체 문구다. 관측 범위는 07:30~08:01로 명시했다.

### 1-1. 혼잡 시간대(9시) 레버 결정 — 사흘 연속 위반 후 결정 기회 자체가 없었다

- **증상**: "9시대 시세 조회가 KIS(증권사) 응답 지연으로 느려지는 문제"에 대응해, 위클리
  옵션 폴링 주기를 지금의 2분에서 4분으로 늘리는 조치(레버
  `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`)의 적용 범위에 **9시를 넣을지**가 9월 4일부터
  미결이다. `mahdi/main.py:275` 현재 값은 `{10: 4, 11: 4, 12: 4, 13: 4, 14: 4}` — 9시가
  빠져 있다.
- **근거**: 09-11 보고서(`NEXT_TODO.md:22~28`)가 "Fix F 규칙 3거래일 연속 위반이 09-11에
  확정됐다(09-09·09-10·09-11)"고 적었고, "내일(09-12) 장전이 반드시 정한다"고 못박았다.
  그런데 09-12는 토요일, 09-13은 일요일이라 **그 사이 장전 점검 자체가 없었다** — 오늘
  (09-14) 08:01 시점 `hypotheses.yaml` mtime은 09-11 그대로이고, 레버 값도 09-11과
  동일하다(변경 없음).
- **기준**: `docs/동작점검/tools/phases.md` A-2 — "연속 3거래일 이상이면 그 자리에서
  (A) 켠다 / (B) 유예한다 중 하나를 명시적으로 고른다 — 유예 재연장 금지". 근거 사건:
  2026-09-08 오후 28분 절벽(15:01~15:28)이 같은 유형의 미결정 뒤에 발생했다.
- **영향**: 9시대에 시세 조회가 다시 막히면, 이번에도 아무 대응 규칙 없이 그대로 겪는다.
  다만 **오늘 아직 09시가 오지 않았으므로 지금은 위험이 아니라 미결 상태**다. (→
  사용자 조치 1번)

⚠ **일수 계산에 관한 유보**: `NEXT_TODO.md`와 `hypotheses.yaml`(`2026-09-08-fix-d-…`) 사이에
"9시 미결 며칠째"를 세는 잣대가 두 가지였고(p95 임계 vs `budget_exceeded` 발생), 09-10에
그 불일치 때문에 `fix-d` 가설이 `refuted`로 닫혔다(규약 F — 주장 축 불일치). 그래서 이
회차는 "정확히 며칠째"라는 숫자를 새로 만들지 않는다 — **분명한 사실은 "09-09·10·11
사흘 연속 미결, 그 이후 재론 기회가 없었다"까지다.**

### 1-2. 검증예정일 지난 pending 가설 6건 — 손 판정이 밀리고 있다

- **증상**: `hypotheses.yaml`에서 검증예정일이 오늘(09-14) 이전인데 상태가 아직 `pending`인
  항목이 6건 있다.
- **근거** (증거 §8, 지남 일수는 오늘 기준):

| id | 검증예정일 | 지남 |
|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | 2026-09-01 | 13일 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 2026-09-01 | 13일 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 2026-08-27 | 18일 |
| `2026-09-03-p1-b-dropout-cross-checks-the-cycle-log` | 2026-09-04 | 10일 |
| `2026-09-11-p1-3-axis-exit-says-why` | 2026-09-12 | 2일 |
| `2026-09-11-close-window-zero-rows-axis` | 2026-09-12 | 2일 |

- **기준**: `docs/동작점검/tools/phases.md` C-3 — 검증예정일 도래분은 그 예정일 당일
  실측으로 판정해야 하고, 소급 적용은 금지다. 09-11 보고서 §3-4가 이미 앞의 4건을 "오늘
  숫자로 판정하지 않는다"고 명시한 바 있어 그 4건은 **의도된 보류**이지 새 발견이 아니다.
  뒤의 2건(09-11에 막 등재된 것)은 검증예정일(09-12)이 비거래일이라 아직 실측 기회가
  없었다.
- **영향**: 실측 기회를 놓친 가설은 나중에 `inconclusive`로 닫힐 위험이 커지고, 그러면 그
  가설이 검증하려던 개선(레짐 복원, 왕복 실측, 사전 대응 규칙 발동, 축 이탈 원인 표기 등)이
  "확인된 적 없는 채로 방치"된다. (→ 사용자 조치는 없음, 장후가 가능한 만큼 판정)

### 1-3. 이벤트 캘린더 `covered_through` 만료 — 만성

- **증상**: `mahdi/config/event_calendar.yaml:70`의 `covered_through`가 `2026-08-28`로,
  오늘(09-14) 기준 17일 지났다. 오늘 07:30:55에 WARNING 1건 발생.
- **근거**: `이벤트 캘린더 미기입: not_covered — covered_through(2026-08-28)가 오늘
  (2026-09-14)보다 앞선다. 이벤트 근접 페널티가 걸리지 않는 상태다` (07:30:55, 오늘 1건).
- **기준**: `phases.md` D — 만성 항목은 만성이라고 쓰고 새 발견으로 올리지 않는다. 09-11
  보고서도 같은 항목을 "15일째"로 이월했었다.
- **영향**: 금리 발표 같은 외부 이벤트가 다가와도 프로그램이 그 사실을 모른 채 평소와 똑같이
  베팅한다. 사람이 캘린더를 채워야 풀리는 문제다. (→ 사용자 조치 목록, 신규 아님)

### 1-4. ZN/ES 해외선물 근월물 조회 실패 — 만성

- **증상**: 07:32:35 `ZN(미국 10년 국채선물) 근월물 조회 실패`, 07:32:38 `ES(E-mini
  S&P500) 근월물 조회 실패` — 둘 다 `EGW00201`(해당 거래소 서브 계좌 미신청).
- **근거**: 위 두 줄, 오늘 각 1건.
- **기준**: 여러 회차에서 반복 확인된 만성 항목(계좌 권한 결손). 새 발견 아님.
- **영향**: 매크로 스냅샷 중 ZN/ES 기반 지표만 공백이고, 나머지 판단 로직에는 영향 없다.

### 1-5. 레버 3건 — 발동 끝났는데 날짜가 안 옮겨짐 (정리 항목, 위반 아님)

- **증상**: 아래 세 레버는 **이미 켜져 있다**(현재 값이 그 사실을 보여 준다). 그런데
  `hypotheses.yaml`의 "무조건발동일"만 지난 채 남아 있다.

| 레버 | 현재 줄 | 무조건발동일 | 지남 |
|---|---|---|---|
| `use_effective_member_count` | `true` | 2026-08-23 | 22일 |
| `SIGNAL_FUSION_PHASE_OFFSET_SECONDS` | `55.0` | 2026-09-02 | 12일 |
| `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` | `{10:4,11:4,12:4,13:4,14:4}` | 2026-08-31 | 14일 |

- **기준**: `docs/동작점검/tools/collect_evidence.py`의 레버 상태 점검부(§7 주석) — "무조건발동일이
  비어 있으면 유예가 조용히 성립한다"는 우려는 여기 해당 안 됨(이미 켰다), 다만 날짜
  주석 정리가 안 됐을 뿐.
- **영향**: 실제 동작에는 영향 없다. 다음 사람이 "이 레버 아직도 유예 중인가"로 오독할
  여지만 있다. (→ 사용자 조치, 급하지 않음)

### 1-6. 미커밋 25건 — 만성, 09-11(27건) 대비 소폭 감소

- **증상**: `git --no-optional-locks status --porcelain` 기준 수정 4건 + 미추적 21건 = 25건.
- **근거**: 수정 — `mahdi/dashboard/app.py`, `mahdi/dashboard/data_source.py`,
  `scripts/daily_ops_report.py`, `scripts/start_mahdi_premarket.bat`. 미추적 — 연구 문서
  5건(`RESEARCH_MIREUK_*`, `RESEARCH_PREMARKET_*`), `premarket_levels` 관련 신규
  모듈·스크립트 11건, 테스트 1건, `.claude/`, 개인 메모 1건.
- **기준**: 09-11 보고서가 "미커밋 27건 — 사람 결정, `.bat` 수정분이 미추적
  `premarket_levels_publish.py`를 부른다(멀티 PC 이식성 문제)"로 이월한 바로 그 항목의
  연장선. 이번엔 건수가 27→25로 줄었지만 구성은 거의 동일 계열이다.
- **영향**: 저장소 상태와 실제 실행 코드가 계속 어긋난 채로 쌓인다. 다른 PC에서 그대로
  받으면 `.bat`이 없는 스크립트를 부르는 오류가 날 수 있다. (→ 사용자 조치, 만성)

### 1-7. 전일 보고서가 언급한 가설 id 1개가 `hypotheses.yaml`에 없음

- **증상**: 09-11 보고서의 미완료 체크박스가 언급한 `2026-08-26-p1-2-1`이 현재
  `hypotheses.yaml`에서 검색되지 않는다.
- **근거**: 증거 §8-1 — "직전 보고서가 언급했는데 yaml에 없는 id 1개: `2026-08-26-p1-2-1`".
- **기준**: `phases.md` A-2 마지막 항목 — 전일 보고서 지시 이행 여부를 기계로 대조하는 절차.
- **영향**: 그 id가 원래 존재했다가 지워진 것인지, 애초에 오탈자였는지 이 회차로는 못
  가른다. 방치하면 "이미 닫힌 결정"인지 "등재 자체가 누락된 것"인지 계속 헷갈린다. (→
  관측 항목 ③)

---

## 오늘 관측이 답해야 할 것

장중·장후가 아래를 판정 / 보류 / 이월 중 하나로 닫을 것.

1. **판단 축 `flow_position`의 31분 무변화가 진짜 정체인가, 단순히 형태가 안 바뀌어 로그가
   조용했던 것인가.** 자동 적신호 #2(증거 §12) — DB `member_scores`를 볼 수 있는 회차(장중
   이후, `--no-db` 아닐 때)가 확인할 것.
2. **`--verify-report`가 지목한 `tests/test_evidence_collector_deferral_axis.py` 부재가
   오탐인지 실제 누락인지.** 장후가 테스트 디렉터리를 직접 확인해 닫을 것.
3. **`2026-08-26-p1-2-1`이 `hypotheses.yaml`에서 사라진 경위.** 오탈자였는지, 실제로
   지워졌는지 확인. 지워졌다면 그 결정이 어디에 기록됐는지 dev_memory에서 찾을 것.
4. **9시 혼잡 레버 결정(1-1)이 오늘 실제로 이뤄졌는가.** 오늘 결정이 나면 장중·장후가
   그 값(A/B)과 사유를 인용으로 남길 것. 안 나면 "왜 또 미뤄졌는가"를 사유와 함께 적을 것
   — 사유 없는 유예는 이 스킬 규약상 결정이 아니라 망각이다.
5. **`hypotheses.yaml` 검증예정일 도래 6건(1-2)** 중 오늘 실측으로 판정 가능한 것이 있는가.

## Fix 구현계획 (초안) — 장후 적용 대상, 오늘은 미적용

이 회차는 코드를 바꾸지 않는다. 아래는 착수 시 참고할 초안이다.

| # | 무엇 | 파일 | 비고 |
|---|---|---|---|
| Fix 1 | 레버 3건(1-5)의 `hypotheses.yaml` 무조건발동일 주석을 실제 발동일로 정리 | `docs/동작점검/hypotheses.yaml` (`use_effective_member_count`, `SIGNAL_FUSION_PHASE_OFFSET_SECONDS`, `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` 각 항목) | 동작 변경 없음, 문서 정리만 |
| Fix 2 | `2026-08-26-p1-2-1` id 존재 여부 확인 후 재등재 또는 이력 기록 | `docs/동작점검/hypotheses.yaml` | 사람 확인 선행 필요(1-7) |
| Fix 3 | 9시 혼잡 레버 결정 이후, 결정된 값을 `mahdi/main.py:275` 딕셔너리에 반영 | `mahdi/main.py` | **결정(사용자 조치 1번)이 먼저** — 결정 없이 착수 금지 |

## 고도화 방안 (초안) — 오늘 관측에서 출발한 것만

1. **"9시 미결 며칠째" 집계 축을 하나로 통일한다.** 1-1에서 본 것처럼 `NEXT_TODO.md`(수기
   서술)와 `hypotheses.yaml`(`fix-d`, p95 vs `budget_exceeded` 축)이 서로 다른 잣대로
   같은 문제를 세고 있다. 비거래일(주말)을 건너뛰는 계산도 아직 어디에도 코드화돼 있지
   않다 — 오늘처럼 "내일 장전이 반드시"가 토요일에 걸리면 그 문장 자체가 무효가 된다.
   `docs/동작점검/tools/collect_evidence.py` §8-4에 **거래일 캘린더를 반영한 단일 카운터**를
   추가하는 안을 제안한다(오늘 관측: 09-12/13 비거래일 처리를 사람이 수작업으로 확인해야
   했다).
2. **`--verify-report`가 지목한 경로 누락(1-7, 관측 항목 ②)에 대해, "테스트 경로가
   실재하는가"까지 자동 대조하는 옵션을 추가하는 안.** 지금은 파일 존재만 보고 판정은
   안 하는데, 존재 여부 자체는 기계가 확정적으로 답할 수 있는 축이라 사람이 매번
   확인하는 수고를 줄일 수 있다.

## dev_memory 반영 초안 (장후가 실제 반영)

- `NEXT_TODO.md`에 "9시 혼잡 레버 결정은 09-12(토)에 재론 기회가 없었고, 09-14(월)로
  이월됐다"는 사실을 한 줄 추가할 것 — 다음에 "3연속 위반인데 왜 아직도 안 정했나"를
  오독하지 않도록.
- `CURRENT_STATE.md`의 "운영 점검 산출물 규약" 절 근처에, 비거래일을 낀 이월 사례(오늘)를
  하나 추가해 두면 다음에 같은 패턴(금요일 확정 → 주말 → 월요일 재론)을 빨리 알아본다.

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다.

### 지금 (개장 전, 08:01~09:00)

1. **혼잡 시간대(9시) 레버를 켤지 정한다.** (→ 1-1)
   - **(A) 켠다** — `mahdi/main.py:275`의 `OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS`에
     `9: 4`를 추가. **얻는 것**: 9시대 위클리 폴링 주기가 2분→4분으로 늘어 예산 컷이 줄고
     체인 신선도가 개선될 가능성. **잃는 것**: 위클리 커버리지가 9시대에도 50%→약 25%로
     반토막 — 위클리 만기일에는 9시가 핀 리스크 관측 창의 일부라 그 창이 좁아진다.
   - **(B) 유예한다** — 9시는 아직 워밍업 구간으로 보고 그대로 둔다. **얻는 것**: 위클리
     관측 유지. **잃는 것**: Fix F 규칙 재위반이 이어지고, 09-08과 같은 혼잡 절벽이 9시대에
     재현될 경우 대응 규칙이 없다.
   - 이 세션은 위 결정을 **적용하지 않는다.** 결정만 사람이 내리고, 적용은 장후 세션(또는
     사람이 직접)이 한다. (A)를 고르고 **지금 바로 반영**하고 싶다면 코드 수정이 필요하므로
     직접 편집하거나 별도로 요청할 것.
   - 어느 쪽도 못 정하겠으면 **최소한 "오늘도 유예, 사유: …"를 이 파일 다음 이어붙임(장중)에
     남길 것** — 사유 없는 유예는 규약 위반이다.

### 오늘 중 (장 마감 후)

2. **Fix 후보 K(§8-4 계산기 축 변경) 착수 여부 결정.** (→ 1-0 표 2번) 09-10에 관련 가설
   `fix-d`가 반증으로 닫혔고, 그 반증이 요구하는 후속 조치가 이것이다.
3. **이벤트 캘린더 `covered_through` 9월분 갱신.** (→ 1-3) 외부 일정 확인 필요, 만성 15일→
   현재 17일째.
4. **미커밋 25건 정리 여부.** (→ 1-6) 특히 `.bat` 수정분이 미추적 `premarket_levels_publish.py`를
   호출하는 구조 — 함께 커밋할지, 되돌릴지 결정 필요.
5. **레버 3건의 무조건발동일 주석 정리.** (→ 1-5, Fix 1) 동작에는 영향 없는 문서 정리.

### 결정이 필요한 것 (자동으로 못 하는 것)

6. **`2026-08-26-p1-2-1`이 `hypotheses.yaml`에서 사라진 경위 확인.** (→ 1-7) 오탈자인지
   실제 삭제인지 사람이 기억하거나 커밋 이력을 뒤져야 한다.

**이번 회차 요약**: P0 0건 · P1 2건(1-1 결정 미착수, 1-2 판정 지연 6건) · P2 5건(1-3~1-7,
전부 만성 또는 정리 항목). 새로 찾은 것은 0건 — 전부 이월.

---

## 제2부 — 장중 ① (12:30 회차)

- 국면: **장중(intra)** · 실행: 예정 12:30 → 실제 12:34 KST — **정시 실행**(밀린 실행 아님)
- 증거 파일: `docs/동작점검/auto/2026-09-14_증거_intra.md`
- 관측 범위: 07:30~12:34 KST

### §0-추가. 이번 회차 결론 한 줄

**지금(12시 34분) 프로그램은 정상 작동 중이다.** 마지막 신호가 2초 전에 찍혔고(감시
프로세스 확인), 오늘 오전 내내 3분 넘게 조용했던 구간이 한 번도 없었다. **사람이 지금
당장 해야 할 일은 없다.** 다만 장전이 남긴 「9시 혼잡 레버를 켤지 정한다」는 결정은
아직도 안 내려졌다 — 오늘 안에는 정할 수 없는 결정이다(다음 거래일 장전에만 적용
가능). 새로 찾은 문제는 사소한 것 2건(P2)뿐이고, 심각한 것(P0)은 없다.

### 2-0. 장전 이월 처분 — 7건 + 관측 항목 5건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 혼잡 시간대(9시) 레버 결정 | 🔄 지속 (12:34 기준 여전히 미결) | `mahdi/main.py` 오늘 무변경(git status에 없음). 다만 근거는 더 강해졌다 — 사전 대응 규칙(`2026-08-04-p5`) 조건 성립 구간이 09-11의 "이틀 연속 10구간"에서 오늘 **11구간**으로 늘었다(증거 §5-1). `p95/timeout≥1.0`인 창이 9/60개, 최악 11:00에 1.01배, 최장 연속 3창(11:25~11:35) — 위험선을 이미 여러 번 넘었다. **그래도 발동창은 다음 거래일 장전이라 지금 적용할 수 없다.** |
| 1-2 검증예정일 지난 pending 가설 6건 | 🔄 지속 (오늘 실측으로 판정 가능한 것 없음) | 사유는 가설마다 다르다 — 아래 「관측 ⑤ 답변」 참조 |
| 1-3 이벤트 캘린더 만료 | 🔄 지속 (만성) | 오늘도 WARNING 계속(07:30:55~11:34:57, 5건 인용) |
| 1-4 ZN/ES 조회 실패 | 🔄 지속 (만성) | 오늘도 각 5건(07:32:35~11:47:37) |
| 1-5 레버 3건 날짜 정리 필요 | 🔄 지속 (정리 항목, 위반 아님) | 지남 일수만 갱신(22일·12일·14일). 값 변경 없음 |
| 1-6 미커밋 25건 | 🔄 지속 (25→**26건**, 원인 확인함 — 실질 증가 아님) | 늘어난 1건은 이 일일점검 파일 자신(`docs/동작점검/2026-09-14_마흐디_일일점검.md`) — 장전 회차가 그 파일을 쓰는 도중에 자기 자신의 git 상태를 쟀기 때문에 그때는 안 잡혔을 뿐, 실제 작업 파일이 늘어난 것은 아니다 |
| 1-7 전일 보고서 언급 가설 id 실종 | ✅ **판정 도착 — 이 표의 (관측③) 행 및 §2-8 참조** | 오탈자로 확인됐다. 앞 절 판단(「이 회차로는 못 가른다」)은 그 시점 증거로는 맞는 판단이었다 |
| (관측①) `flow_position` 31분 무변화가 진짜 정체인가 | ⏸ 보류 (사유: DB 미접근) | 이 점검 세션은 애초에 읽기전용 DB 접근 권한이 없다(1-0#6과 같은 구조적 제약 — 이 세션이 도는 환경에는 Docker/DB가 없다). 대신 로그로 보완: §5-2 「판단 형태 전이」 표에서 `flow_position`이 07:30:55~**12:31:57**까지 계속 편입 상태로 관측됐다(정지된 채로 빠진 것은 아니다). 다만 이것은 「형태가 유지됐다」이지 「값이 계속 갱신됐다」는 아니라 완전한 답은 아니다 → 14:30·장후로 이월 |
| (관측②) `tests/test_evidence_collector_deferral_axis.py` 부재가 오탐인지 | ✅ 판정 — 오탐 아님, 신규 발견도 아님 | 09-10 보고서가 이 테스트를 "신설 예정"으로 예고했으나 09-11 보고서 §3-0이 이미 "최종 확정 — 미착수"로 닫았다(`2026-09-11_마흐디_일일점검.md:732`). 즉 이 부재는 **Fix 후보 K(1-0#2) 미착수라는 이미 아는 사실의 재확인**이지 새 결함이 아니다 |
| (관측③) `2026-08-26-p1-2-1` 실종 경위 | ✅ 판정 — 오탈자로 추정 | `NEXT_TODO.md:42`가 "`hypotheses.yaml:6069~6097`의 `2026-08-26-p1-2-1`"이라 적었는데, 그 줄 범위(정확히는 6084~6118행)에는 그런 id가 없고 대신 `2026-08-26-p2-1-lever-warning-needs-both-conditions`가 있다 — **이미 2026-08-31에 confirmed 상태다.** id 표기가 `p1-2-1`↔`p2-1`로 어긋난 단순 오탈자로 보인다. → 아래 신규 1-8 |
| (관측④) 9시 혼잡 레버 결정이 오늘 이뤄졌는가 | 🔄 지속(위 1-1과 같은 항목) | 12:34 기준 아직 결정 안 남. "사유 없는 유예"가 되지 않도록 이 회차가 사유를 남긴다: 사유는 **"장중에는 이 결정을 적용할 수 없어(코드 변경 금지) 미룬다"**이고, 이것이 3번째 사유 없는 유예가 되지 않게 다음 장전이 반드시 정할 것 |
| (관측⑤) 가설 6건 중 오늘 판정 가능한 것 | ✅ 판정 — 0건 (사유 개별 확인) | `2026-08-12-fix7-…`: 전제 레버(`REGIME_RESTORE_SESSION_WINDOW`) 여전히 꺼짐(규약 H, 판정 불가). `2026-08-19-fix2-…`: 왕복 실측 자체가 DB 미접근으로 확인 불가. `2026-08-26-p1-3-…`: 1-1 결정이 전제인데 미결. `2026-09-03-p1-b-…`: 판정 조건인 "멤버 이탈" 이벤트가 오늘 0건 발생(로그에 `판단 축 이탈` 없음) — 일어나지 않은 일은 오늘 판정 못 함. `2026-09-11-p1-3-…`: 마찬가지로 오늘 이탈 이벤트 0건. `2026-09-11-close-window-…`: 판정 창(15:00~15:20)이 아직 안 왔다 — 장후 몫 |

### 2-8. 신규 — `NEXT_TODO.md:42`가 존재하지 않는 가설 id를 가리키고 있다

- **증상**: `NEXT_TODO.md:42`(Fix 4의 선행 조건 설명)가 "`hypotheses.yaml:6069~6097`의
  `2026-08-26-p1-2-1` 가설을 사람이 먼저 닫아야 착수 가능"이라 적었는데, `hypotheses.yaml`
  전체에 `p1-2-1`이라는 id는 **한 번도 존재한 적이 없다**(`grep` 결과 0건).
- **근거**: `hypotheses.yaml:6084`에 있는 실제 id는 `2026-08-26-p2-1-lever-warning-needs-both-conditions`이고,
  이 가설은 이미 **2026-08-31에 `상태: confirmed`로 닫혔다**(같은 파일 6118행). `NEXT_TODO.md:41~43`은
  이것을 가리키려 한 것으로 보이나 `p1-2-1`이라는 존재하지 않는 이름을 썼다.
- **기준**: `phases.md` — 전일 지시 이행 여부를 기계로 대조하는 절차(A-2). 증거 §8-1이
  같은 불일치를 이미 자동으로 잡아냈다.
- **영향**: `NEXT_TODO.md`를 읽는 사람이 "이 가설이 아직 안 닫혀서 Fix 4에 못 들어간다"고
  오해하게 만든다. 실제로는 이미 8월 31일에 검증까지 끝난 가설이라 **Fix 4(레버 3건
  「정리 필요」 경보 조건식, `docs/동작점검/tools/collect_evidence.py:2681`)의 착수 조건은
  이미 충족돼 있을 가능성이 높다** — 사람이 오탈자만 확인하면 착수해도 되는 상태일 수
  있다. (→ 사용자 조치 신규 항목)

### 2-9. 신규(경미) — 먼슬리 레그 되살리기 완전 미회복 1건, 11:33:42

- **증상**: 11:33:42 사이클에서 되살리기를 시도한 먼슬리 레그 1개가 하나도 회복되지
  못했다(증거 §5-1-1, "완전 미회복 1건").
- **근거**: 증거 §5-1-1 11시 행 — 되살리기 시도 15건 중 회복실패 1건, 그중 완전
  미회복(0개 회복) 1건. 남은예산 창최소 0.0초.
- **기준**: `phases.md` B-2 — 먼슬리 되살리기는 판단 입력(GEX·감마플립)의 두께에 직결되므로
  "부분 회복"과 "완전 미회복"을 구분해서 본다(2026-08-24 Fix#4).
- **영향**: 11시 33분 그 1분 동안 계산된 감마·GEX 판단이 핵심 레그 몇 개가 빈 채로 나왔다는
  뜻이다. 다만 **오늘 하루 전체로 보면 오히려 크게 개선됐다** — 직전 거래일(09-11)에는
  같은 유형의 실패가 16건이었는데 오늘은 1건뿐이다(회복률 81.2%). 심각도는 낮다.

### 14:30 회차가 이어받을 것

1. **(관측①)** `flow_position` 31분 무변화 — 아직 DB로 확인 못 함. 14:30 회차도 DB 접근이
   안 되면 장후가 반드시 확인할 것(장후는 `--no-db` 없이 돌 수 있는지 먼저 확인).
2. **1-1** 9시 혼잡 레버 결정 — 여전히 미결. 14:30 회차는 사전 대응 규칙 조건 성립 구간이
   더 늘었는지(현재 11구간) 갱신할 것.
3. **2-8** `NEXT_TODO.md:42` 오탈자 — 사람이 확인 후 정정 필요(사용자 조치 참조).
4. **(관측⑤)** 가설 6건 — 15:00~15:20 마감 직전 구간이 와야 `2026-09-11-close-window-…`를
   판정할 수 있다. 14:30 회차 또는 장후가 그 구간 로그로 판정할 것.
5. 미커밋 26건(1-6) — 여전히 정리 안 됨, 장후 소관.

## Fix 구현계획 (갱신) — 이 회차도 코드를 바꾸지 않는다

장전 Fix 1~3은 그대로 유효하다. 추가:

| # | 무엇 | 파일 | 비고 |
|---|---|---|---|
| Fix 4(신규, §2-8) | `NEXT_TODO.md:42`의 `2026-08-26-p1-2-1`을 실제 id `2026-08-26-p2-1-lever-warning-needs-both-conditions`로 정정 | `docs/dev_memory/NEXT_TODO.md` | 정정 후 Fix 4(레버 3건 경보 조건식) 착수 가능 여부 재확인 |

## dev_memory 반영 초안 (장후가 실제 반영) — 추가

- `NEXT_TODO.md:42`의 가설 id 오탈자(`p1-2-1` → `p2-1-lever-warning-needs-both-conditions`)를
  정정하고, 그 가설이 이미 08-31 confirmed임을 명시할 것.

---

## 사용자 조치 (장중 12:30 회차 갱신)

> 장전 「사용자 조치」 절은 그대로 유효하다. 아래는 이번 회차가 새로 확인한 것만 추가한다.

### 결정이 필요한 것 (자동으로 못 하는 것) — 추가

7. **`NEXT_TODO.md:42`의 가설 id 오탈자를 확인·정정한다.** (→ 2-8) `2026-08-26-p1-2-1`은
   존재한 적 없는 id로 보이고, 실제로 그 자리를 가리키려 한 가설(`2026-08-26-p2-1-lever-warning-needs-both-conditions`)은
   이미 8월 31일에 검증 완료됐다. 정정하면 Fix 4 착수를 막던 조건이 풀릴 수 있다.

### 이번 회차에 추가로 필요한 조치는 없음

> 나머지는 장전 절이 이미 다뤘다. P0는 이번 회차도 0건이다.

**이번 회차 요약**: P0 0건 · 신규 P2 2건(2-8 문서 오탈자, 2-9 경미한 되살리기 실패 1건) ·
이월 처분 12건(장전 항목 7 + 관측 항목 5) 전부 처분 완료.

> ↩️ **이 회차의 판단은 뒤에서 갱신됐다 — §2-10·§2-11(장중 14:30) 참조.** (본문은 당시 판단
> 그대로 보존. 특히 1-1의 "사전 대응 규칙 11구간"과 2-9의 "먼슬리 완전미회복 1건"은 14:30
> 시점 기준으로 각각 18구간·3건으로 늘었다 — 그때는 맞았던 숫자다.)

---

## 제2부 — 장중 ② (14:30 회차)

- 국면: **장중(intra)** · 실행: 예정 14:30 → 실제 14:40 KST — **정시 실행**(10분 이내, 밀린
  실행 아님). 14:50이 오늘 신규 진입 컷오프(v6 §4.2)라 이 회차의 결론은 "그 전에 사람이
  할 일이 있는가"로 끝난다.
- 증거 파일: `docs/동작점검/auto/2026-09-14_증거_intra_1430.md` (+ `2026-09-14_지연창.tsv` 85행)
- 관측 범위: 12:34~14:40 KST (그 이전은 장중① 절 참조)

### §0-추가. 이번 회차 결론 한 줄 (전문용어 없음)

**지금(14시 40분) 프로그램은 살아서 정상적으로 신호를 찍고 있다** — 마지막 기록이 38초
전이고, 감시 프로그램(워치독)도 "정상"이라고 확인했다. 다만 **오후 들어 두 가지가
서서히 나빠지고 있다.** 첫째, 증권사(KIS)에 데이터를 요청했을 때 상위 5%가 느려서
제한시간을 넘기는 구간이 점심 이후 계속 늘고 있다(12시 반엔 9번이었는데 지금은 27번).
둘째, 오후 2시 4분부터 "여러 판단 근거 중 하나(옵션 수급 축)가 신호를 못 내는" 상태가
30분 넘게 이어지고 있다 — 이건 지난 9월 11일에도 오후 2시대에 한 번 있었던 바로 그
패턴이 이틀 만에 다시 나타난 것이다. **그래도 아직 실제 주문이나 손익에 문제가 생긴
것은 아니고, 심각한 오류(P0)도 없다.** 9시 혼잡 레버 결정은 여전히 미정이지만 그것은
어차피 내일 아침에만 적용할 수 있는 결정이라 지금 할 수 있는 일이 아니다.

**사람이 지금 당장 해야 할 일은 없다.** 14시 50분(오늘 마지막으로 새 포지션을 열 수
있는 시각) 전까지 코드를 고치거나 프로그램을 다시 켤 필요는 없다 — 아래 「14:50 컷오프
권고」에 손익 비교로 적어 둔다.

### 2-0(14:30 회차). 장중① 이월 처분 — 12건 전부 처분

> ⚠ 위 「제2부 — 장중 ① (12:30 회차)」에도 같은 번호(2-0)의 이월 처분표가 있다. 국면 안에
> 회차가 두 번이라 번호가 겹친다 — 시각을 붙여 구분한다. 이상점 번호(1-1…2-11)는 겹치지
> 않는다.

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 혼잡 시간대(9시) 레버 결정 | 🔄 지속 (14:40 기준 여전히 미결) · ⬆️ **근거 격상** | `mahdi/main.py` 오늘 무변경. 사전 대응 규칙(`2026-08-04-p5`) 조건 성립 구간이 12:30 시점 "11구간"에서 **18구간**으로 늘었다(증거 §5-1). `p95/timeout≥1.0`인 창 **27/85개**(12:30엔 9/60) · 최악 여전히 11:00 1.01배 · **최장 연속 9창(13:00~13:40, 45분)**(12:30엔 3창·15분) — 위험선을 넘는 구간이 점심 이후 조용히 넓어졌다. 발동창은 여전히 다음 거래일 장전이라 지금 적용 불가 |
| 1-2 검증예정일 지난 pending 가설 6건 | 🔄 지속 (오늘 실측으로 판정 가능한 것 여전히 없음) | 아래 「관측⑤ 갱신」 참조. `2026-09-11-close-window-…`만 판정 창(15:00~15:20)이 아직 안 왔다 |
| 1-3 이벤트 캘린더 만료 | 🔄 지속 (만성) | 14:35:57까지 WARNING 계속(오늘 8건, §5) |
| 1-4 ZN/ES 조회 실패 | 🔄 지속 (만성) | 07:32:35~07:32:38 각 1건, 이후 재시도 없음(오전에 이미 실패 확정) |
| 1-5 레버 3건 날짜 정리 필요 | 🔄 지속 (정리 항목, 위반 아님) | 지남 일수만 갱신(22일·12일·14일). 값 변경 없음 |
| 1-6 미커밋 26건 | 🔄 지속 (변화 없음) | `git --no-optional-locks status --porcelain` 오늘도 26건, HEAD `1186372` 그대로. 실질 증가 아님(12:30 판단 유지) |
| 1-7 전일 보고서 가설 id 실종 | ✅ 해소 (12:30에 이미 판정 완료) | 2-8에서 정정 대상으로 재확인 |
| 2-8 `NEXT_TODO.md:42` 오탈자 | 🔄 지속 (사람 확인 대기, 아직 정정 안 됨) | `git diff`에 `NEXT_TODO.md` 변경 없음 — 사용자 조치 7번 미이행 |
| 2-9 먼슬리 레그 완전 미회복 | 🔄 지속 · **건수 갱신(1건→3건)** | 12:30엔 11:33:42 1건뿐이었다. 이후 **13:30:56**(3개 중 0개 회복) · **14:00:56**(4개 중 0개 회복) 추가 확인(증거 §5-1-1 13시·14시 행) — 셋 다 "판단 주입력이 이번 분에 얇아졌다"류. 심각도는 그대로 낮음(전일 16건 대비 오늘 누적 7건, 회복률 여전히 개선 추세) |
| (관측①) `flow_position` 31분 무변화가 진짜 정체인가 | ➡️ 이월 (사유: 이 회차도 DB 접근 불가) | 이 점검 세션 환경엔 Docker/DB가 없다(장중①과 같은 구조적 제약). 로그로는 `flow_position`이 07:30:55~14:35:57까지 계속 편입 상태로 관측됨(§5-2) — 형태 유지는 확인되지만 값 갱신 여부는 여전히 못 본다. **장후로 이월**(장후는 `--no-db` 없이 도는지 먼저 확인할 것) |
| (관측②) 테스트 경로 부재 오탐 여부 | ✅ 판정 완료 (12:30에 이미 닫힘) | 재확인 불필요 |
| (관측③) 가설 id 오탈자 경위 | ✅ 판정 완료 (12:30에 이미 닫힘, 2-8로 등재됨) | 재확인 불필요 |
| (관측④) 9시 레버 결정이 오늘 이뤄졌는가 | 🔄 지속 (14:40 기준도 미결, 사유 동일) | "장중엔 코드 변경 금지라 적용 불가"라는 사유가 이번에도 유효 — 3번째가 아니라 **같은 사유의 연장**이다. 다음 장전이 반드시 정할 것(이번이 4거래일째 미결: 09-09·10·11·**14**) |
| (관측⑤) 가설 6건 중 오늘 판정 가능한 것 | ✅ 판정 — 이번 회차도 0건 (사유 갱신) | `2026-09-11-close-window-…`를 제외한 5건은 12:30과 사유 동일(전제 레버 OFF · DB 미접근 · 1-1 미결 · 이탈 이벤트 0건 — 단, 아래 2-10에서 **이탈 이벤트가 실제로 발생**했으므로 `2026-09-03-p1-b-…`·`2026-09-11-p1-3-…`는 **다음 회차(장후)가 이 이벤트로 재판정을 시도할 후보**로 승격. `close-window` 항목은 판정 창(15:00~15:20)이 아직 안 와 여전히 대기 |

### 오후 열화 게이지 (창 단위 시계열)

수집 예산 50초 · `inquire-price`(개별 종목 시세 조회) read timeout 4.0초(응답이 이 시간을
넘으면 그 호출은 실패로 끊긴다) 기준.

| 시간대(KST) | 사이클 수 | rows=0(전멸) 비율 | p50÷timeout | p95÷timeout(창 최대) | 비영 멤버(최신) | 관측루프 공백 | WS 단절(누적) |
|---|---|---|---|---|---|---|---|
| 09시 | 60 | 0% | 0.39 | 0.91 | 3/6 | 0건 | 2회(09:00) |
| 10시 | 60 | 0% | 0.56 | 0.99 | 3/6 | 0건 | 3회(10:00) |
| 11시 | 60 | 0% | 0.57 | **1.01**⛔(최초 위험선 돌파) | 3/6 | 0건 | 4회(11:00) |
| 12시 | 60 | 0% | 0.58 | 1.00 | 3/6 | 0건 | 5회(12:00) |
| 12:30~13:00 | 6 | 0% | 0.58 | 1.00 | 3/6 | 0건 | — |
| 13:00~13:40 | 8 | 12.5%(13:00 1분) | 0.68(창 최대, 13:10~13:35) | **1.01, 9창 연속(45분)**⛔ | 3/6 | 0건 | 6회(13:00) |
| 13:40~14:00 | 4 | 0% | 0.51 | 0.93~1.00 | 3/6 | 0건 | — |
| 14:00~14:10 | 2 | 50%(14:10 1분) | 0.59~0.67 | **1.01** | **2/6**⚠(14:04:57부터) | 0건 | 7회(14:00) |
| 14:10~14:35 | 5 | 0% | 0.62~0.65 | 0.88~1.01 | **2/6**⚠(14:34만 3/6) | 0건 | — |

- **경고선(p50÷timeout≥0.8) 최초 돌파: 오늘 없음.** p50 하루 최대치는 0.68배(13:10·13:35
  창)로 아직 경고선 아래다 — **이 축은 정상.**
- **위험선(p95÷timeout≥1.0) 최초 돌파: 11:00 창, 1.01배.** 이후 27/85창(전체의 32%)이
  이 선을 넘었고, 넘은 구간이 점점 길어지고 있다 — 12:30 시점엔 최장 3창(15분)이었는데
  지금은 **최장 9창(13:00~13:40, 45분)**. 응답의 "느린 쪽 5%"가 이미 자주 시간 초과되고
  있고, 그 구간이 넓어지는 중이라는 뜻이다. (→ 1-1과 같은 근거, 대응은 내일 아침 결정
  사항)
- **rows=0(그 1분의 시세를 하나도 못 모은 경우) 비율: 시간대별로 봐도 최대 50%(14:00~14:10
  창, 실제는 14:10 단 1분)** — 위험선(≥70%)에는 한참 못 미친다. **이 축은 정상.**
- **비영 멤버(6개 판단 축 중 실제로 신호를 내고 있는 것의 수): 오늘 기준선 3/6이 
  14시 4분부터 2/6으로 떨어져 지금(14:35)까지 이어지고 있다.** 자세한 내용은 2-10.
- **관측 루프 공백**: 07:30~15:45 구간 3분 이상 공백 0건 — **이 축은 정상.**
- **WS(웹소켓, 실시간 시세 연결) 단절**: 매 정시(08:00·09:00·…·14:00)에 정확히 1회씩,
  누적 7회. 12:30 이후 늘어난 것은 14:00 1건뿐 — **패턴이 일정해 구조적 현상으로 보이고
  증가 추세는 아니다.** 매번 2초 안에 재연결됨.

### 2-10. 신규(P1) — 옵션 수급 판단 축(`options_flow`)이 14:04부터 31분 넘게 죽어 있다 — 9/11과 같은 "오후 2시 패턴"의 재현

- **증상**: 판단을 만드는 6개 축 중 지금 실제로 점수를 내고 있는 것(비영 멤버)이
  14:04:57부터 3개(3/6)에서 2개(2/6)로 줄었다. 빠진 것은 `options_flow`(옵션 수급 흐름을
  읽는 축)다. 14:34:57 딱 한 사이클만 잠깐 3개로 돌아왔다가 14:35:57에 다시 2개로
  떨어졌다 — **사실상 14:04부터 지금(14:40)까지 31분 넘게 이어지는 중이다.**
- **근거**: `logs/observation_loop.log` 14:04:57 `판단 형태 전이: 가용멤버 [...](4/6, 비영
  2) · 0점축=['regime_hmm', 'options_flow']`. 14:06:57·14:09:57·14:10:57·14:12:57·
  14:14:57·14:15:57·14:16:57·14:18:57·14:21:57·14:25:57·14:29:57·14:30:57·14:31:57·
  14:33:57·14:35:57 — 14:34:57 한 번을 빼고 전부 같은 패턴(직접 grep 확인, 총 15개
  사이클 중 14개). 그 직전(09:00~14:00)엔 하루 종일 비영 3(0점축은 `regime_hmm` 하나뿐)이
  기준선이었다(§5-2, 68/71 사이클).
- **기준**: `docs/dev_memory/NEXT_TODO.md:59` — 9월 11일 장후 자동조치가 이미 이 정확한
  패턴을 예고해 뒀다: *"14:00 이후 `options_flow` 이탈 재발 — 오늘(09-11) 14:27:54 사례 +
  08-25 계열 선례. … '14:00 이후 이탈이 매일 재현되는가'를 검정할 예측치를 세울지 정할
  것."* 오늘이 바로 그 재현이다 — 다만 09-11은 한 사이클짜리 순간 이탈이었는데(§
  15:24:09 기록 별개 사례), 오늘은 **30분 넘게 지속**돼 지속 시간이 더 길다.
- **영향**: 이 30분 동안 프로그램이 내리는 판단은 평소 3개 근거 중 2개(주문 흐름·레짐
  판단)만 보고 내린 것이고, 옵션 시장의 수급 쏠림 정보는 빠져 있다. 다만 확신도 분포를
  보면 이 구간에도 HIGH_CONVICTION과 SMALL_TEST가 번갈아 나오고 있어(§5-2) 판단 자체가
  멈춘 것은 아니다 — **정보 하나가 빠진 채로 계속 도는 것**이지 판단 정지는 아니다.
  실제 주문·손익에 아직 문제가 확인된 것은 없다.
- **P1인 이유**: 정확성 저하(판단 입력 축소)이지 오늘 관측·거래를 직접 위협하는 P0는
  아니다. 다만 "매일 재현되는가"를 검정하자던 예고가 이틀 연속으로 맞아떨어지고 있어
  방치하면 만성으로 굳는다. (→ 사용자 조치 신규 항목, 예측치 등재 권고)

### 2-11. 신규(P2, 참고용) — 사전 대응 규칙 성립 구간이 반나절 만에 11→18구간으로 늘었다

- **증상**: 2-0 표의 1-1 항목과 같은 근거이나 규모가 회차마다 달라 별도로 번호를 매긴다.
  `2026-08-04-p5` 규칙의 성립 구간이 장전 시점엔 계산 전, 장중①(12:30) 시점엔 11구간,
  이번(14:30) 시점엔 **18구간**이다.
- **근거**: 증거 §5-1 "🔔 사전 대응 규칙 발동 조건 성립 … 오늘 성립 18개" (10시·11시·12시·
  13시·14시 `inquire-asking-price`/`inquire-balance`/`inquire-price` 조합). 최장 연속 구간도
  12:30 시점 3창(15분)에서 **9창(13:00~13:40, 45분)**으로 늘었다.
- **기준**: `phases.md` — 위험선 돌파 구간은 「창 최대」로 판정하고 첫 돌파 시각을 특정한다
  (이번 회차 처음 준수: 최초 돌파 11:00, §5-1).
  이 규칙 자체의 발동(위클리 폴링 2분→4분)은 **다음 거래일 장전에만** 가능하다 — 이미
  1-1에서 다루는 결정과 동일 건이라 **새 결정 항목은 아니다.**
- **영향**: 지금 당장 뭔가 해야 하는 것은 아니다(발동 창이 이미 닫혀 있다). 다만 내일
  장전이 1-1을 결정할 때 "11구간이 반나절 만에 18구간이 됐다"는 이 속도감을 함께 봐야
  한다 — 사흘 미룬 값이 아니라 하루 안에서도 빠르게 늘어나는 값이라는 뜻이다.

### 14:50 컷오프 권고

**남은 시간: 이 절 작성 시각(14:44) 기준 약 6분.**

- **개입 필요 항목 없음.** 위 이상점(1-1/2-11의 지연 심화, 2-10의 options_flow 이탈,
  2-9의 되살리기 실패)은 전부 **코드 변경이나 재시동 없이 관찰만 계속하면 되는 성격**이다.
- **재시동을 검토할 이유가 없는 이유**: 2-10(options_flow 이탈)의 원인은 "판단 주입력이
  얇아졌다"는 신호 강도 문제이지 프로세스 장애가 아니다 — 워치독은 0건 이상, 관측 루프
  공백도 0건이라 **살아서 정상적으로 도는 프로세스가 신호가 약한 것**뿐이다. 이런 유형은
  과거에도(08-21 6회, 09-11 1회) 재시동 없이 자연 회복됐다(다음 사이클에 `options_flow`가
  다시 편입되는 패턴, §5-2 4/6 목록 자체는 07:30:55부터 안 끊겼다). **재시동으로 얻는 것**
  (수집 경로 초기화) **대비 잃는 것**(레짐 재웜업 약 29분, 08-19 실측 — 오늘 남은 장중
  관측 시간이 74분뿐인데 그중 29분을 워밍업으로 쓰게 된다)이 명백히 크다.
- **9시 혼잡 레버(1-1) 결정도 지금 적용 대상이 아니다** — 적용 시점 자체가 "내일 07:30
  기동 전"으로 못 박혀 있어(§5-1), 지금 결정해도 오늘 반영할 수 없다.
- **결론: 14:50 컷오프 전 사람이 할 일은 없다.** 다음 확인 시점은 15:20~15:45(마감 시퀀스)
  또는 장후.

### Fix 구현계획 (갱신) — 이 회차도 코드를 바꾸지 않는다

장전 Fix 1~3, 장중① Fix 4는 그대로 유효하다. 추가:

| # | 무엇 | 파일 | 비고 |
|---|---|---|---|
| Fix 5(신규, §2-10) | `2026-09-11-p1-3-axis-exit-says-why`·`2026-09-03-p1-b-…` 두 가설을 오늘 14:04:57~14:35:57 `options_flow` 이탈 이벤트로 재판정 시도 | `docs/동작점검/hypotheses.yaml` | 장후가 착수(관측⑤ 갱신 참조) — DB 접근 가능 여부 먼저 확인 |
| Fix 6(신규, §2-10) | "14:00 이후 options_flow 이탈이 매일 재현되는가"를 검정할 신규 예측치 등재 (NEXT_TODO.md:59가 이미 제안) | `docs/동작점검/hypotheses.yaml` | 오늘로 09-11·09-14 **2거래일 연속** 관측 — 등재 근거 충분. 장후 권고 |

### dev_memory 반영 초안 (장후가 실제 반영) — 추가

- `NEXT_TODO.md:59`의 "14:00 이후 options_flow 이탈 재발" 항목에 오늘(09-14) 14:04:57~
  14:35:57 사례를 **2번째 재현**으로 추가 기록하고, 지속시간이 09-11(순간)보다 길다는
  점(30분+)을 함께 남길 것.
- `NEXT_TODO.md:22~28`(9시 레버 결정)에 "09-14 14:30 기준 사전 대응 규칙 성립 18구간(반나절
  전 11구간)"으로 근거 수치를 갱신할 것.

---

## 사용자 조치 (장중 14:30 회차 갱신)

> 장전·장중① 「사용자 조치」 절은 그대로 유효하다. 아래는 이번 회차가 새로 확인한 것만
> 추가한다.

### 이번 회차에 추가로 필요한 즉시 조치는 없음

> 14:50 컷오프 전 사람이 할 일은 없다(위 「14:50 컷오프 권고」 참조). P0는 이번 회차도
> 0건이다.

### 오늘 중(장 마감 후) — 추가

8. **`options_flow` 이탈 재발(2-10)에 대해 예측치 등재 여부를 정한다.** (→ Fix 6)
   "14:00 이후 이탈이 매일 재현되는가"를 검정하는 항목을 오늘 사례로 새로 세울지, 아니면
   더 지켜볼지 — 장후 세션이 제안하면 확정만 하면 된다.

**이번 회차 요약**: P0 0건 · 신규 P1 1건(2-10 options_flow 이탈 재발, 30분+ 지속) · 신규
P2 1건(2-11, 1-1과 같은 근거의 규모 갱신 — 11→18구간) · 이월 처분 14건(장중① 항목 9 +
관측 항목 5) 전부 처분 완료 · **14:50 컷오프 전 사람 조치 불필요.**

---

## 제3부 — 장후 (15:20 ~ )

- 국면: **장후(post)** · 예정 15:45(증거·지표) → 실제 15:46:33 KST — **정시 실행**. 이 문서
  작성 회차(Claude 앱 예약, 평일 16:00)는 그 증거를 읽어 지금 쓴다.
- 증거 파일: `docs/동작점검/auto/2026-09-14_증거_post.md` · `2026-09-14_지표.md`/`.json` ·
  `2026-09-14_지연창.tsv`(98개 시간창)
- 관측 범위: 07:30~15:45 KST(오늘 정규장 전체). 이 회차는 **DB에 접근할 수 있었다** —
  장중 두 회차가 "이 세션 환경엔 Docker/DB가 없다"고 이월한 항목들을 여기서 실제 수치로
  닫는다.

### §0-추가(장후). 오늘 하루 결론 (전문용어 없음)

**오늘 프로그램은 하루 종일 정상적으로 켜져 있었고, 사람이 지금 당장 해야 할 일은 없다.**
07시 30분에 깔끔하게 켜져서 15시 45분 정시에 스스로 마감 절차를 마쳤고, 그사이 프로그램이
죽거나 재시작된 적은 한 번도 없었다. **오늘도 실제 주문은 한 건도 나가지 않았다** — 지금
설정이 "지켜만 보고 스스로 사지 않는" 모드(ADVISORY)이기 때문에 정상이다. 사려는 판단
자체는 321번 있었지만 그중 259번이 이 모드 설정 때문에 막혔다.

**다만 장 마감 직전 6분 동안(15시 16분~15시 21분) 옵션 시세 조회가 통째로 실패했다.**
증권사(KIS) 응답이 느려지면서 그 6분 동안은 수집한 시세가 0건이었고, 15시 20분에는
평소보다 응답이 하도 늦어져서 "표준 대기시간(4.0초)을 넘긴 호출이 절반을 넘는" 첫 순간이
오늘 처음 나왔다. 같은 유형의 문제가 지난 9월 11일에도 마감 직전 3분 동안 있었는데,
오늘은 그 길이가 **두 배(6분)**로 늘었다 — 아직은 "심각(P0)"까지는 아니지만, 내일도
반복되면 "장 마감 무렵엔 항상 이런다"는 구조적 문제로 확정해야 한다. (→ 사용자 조치는
없음, 다음 장전이 지켜볼 것)

**문제 건수**: 오늘 하루 전체로 찾은 이상점은 총 13건. 그중 1건은 오늘 안에 해소(단순
오탈자로 확인)됐고, 나머지 12건이 아직 남아 있다 — **심각한 것(P0)은 0건**, 급한 결정이
필요한 것(P1) 4건, 운영 부담 수준(P2) 8건이다. 오늘 새로 찾은 것은 2건(마감 직전 6분
전멸, 그리고 아래에서 설명하는 예측치 검정 방식 자체의 결함)이다.

**사람이 오늘 안에 정해야 할 결정은 변함없이 하나다** — "9시 혼잡 레버를 켤지" 결정이
**오늘로 나흘째(9/9·10·11·14)** 미뤄졌다. 이 결정은 코드 성격상 **내일 아침(07:30 기동
전)에만 적용할 수 있어** 오늘 밤 안에 사람이 골라 둬야 다음 장전이 그대로 반영할 수 있다.
(→ 사용자 조치 1번, 그대로 유효)

### 3-0. 장중② 이월 처분 — 14건 전부 처분

| 항목 | 처분 | 근거 |
|---|---|---|
| 1-1 혼잡 시간대(9시) 레버 결정 | 🔄 지속 · ⬆️ **긴급도 격상** | 15:45 시점까지 `mahdi/main.py` 무변경. 사전 대응 규칙 성립 구간이 하루 마감 기준 **20구간**(장중② 18구간 대비 갱신, §3-1)으로 최종 확정됐고, **오늘로 나흘째(09-09·10·11·14) 미결**이다 — phases.md A-2가 못 박은 "3거래일 이상이면 그 자리에서 고른다"를 이미 넘어섰다. 적용 시점이 내일 장전뿐이라 오늘 밤 안에 사람이 결정해야 다음 장전이 반영할 수 있다 |
| 1-2 검증예정일 지난 pending 가설 6건 | ✅ 대부분 판정 (아래 §3-2) | DB 접근이 가능해져 6건 중 4건에 제안 판정을 냈다. 나머지 2건은 사유와 함께 보류/이월 |
| 1-3 이벤트 캘린더 만료 | 🔄 지속 (만성, 17일째) | 15:35:56까지 WARNING 계속(오늘 9건) |
| 1-4 ZN/ES 조회 실패 | 🔄 지속 (만성) | 07:32:35~07:32:38 각 1건, 오늘 추가 재시도 없음(오전에 이미 실패 확정) |
| 1-5 레버 3건 날짜 정리 필요 | 🔄 지속 (정리 항목, 위반 아님) | 지남 일수만 갱신(22일·12일·14일). 값 변경 없음. Fix 1 미착수 |
| 1-6 미커밋 26건 | 🔄 지속 (26건 변화 없음) | `git --no-optional-locks status --porcelain` 오늘 마지막 확인 27건 — 늘어난 1건(`.claude/`)은 **이 점검 세션 자체가 남긴 흔적으로 보인다**(사람 작업물이 아니다). 실질 미커밋은 26건 그대로. HEAD `1186372` 무변경 |
| 1-7 전일 보고서 가설 id 실종 | ✅ 해소 (12:30에 이미 판정 완료) | 재확인 불필요 |
| 2-8 `NEXT_TODO.md:42` 오탈자 | 🔄 지속 (미정정) | `git diff`에 `NEXT_TODO.md` 변경 없음. 정정은 이 회차도 하지 않는다(사람 결정 대기, 사용자 조치 7번) |
| 2-9 먼슬리 레그 완전 미회복 | 🔄 지속 · **최종 건수 확정(4건)** | 하루 합계 시도 74회 중 회복실패 8건, 그중 완전 미회복 **4건**(11시 1 · 13시 1 · 14시 1 · 15시 1, §5-1-1). 전일(09-11) 16건 대비 **▼8**, 회복률 81.2%(레그 기준) — 개선 추세 그대로 유지. 심각도 낮음 |
| 2-10 `options_flow` 이탈 재발(14:04~14:35) | ⬇️ **완화 — 원래 우려는 해소, 별개 사건이 마감 후 새로 발생** | 14:04~14:35 구간(장중② 관측분)은 14:40:57 사이클에서 4개 축 모두 정상 복귀한 채 15:20까지 **44분간 안정 유지**됐다 — 그 구간 자체는 "일시적 정보 얇아짐"이었다는 장중② 판단이 맞았다. 다만 **15:20:56에 `options_flow`가, 15:36:56에 `orderflow_ofi_vpin`이 각각 다시 이탈**했다 — 이번엔 15:20 정규장 마감 직후라 "체결이 끊겨 실시간 입력 자체가 사라진" 구조적 이탈로 보인다(아래 §3-1). 새 사건이라 2-10 번호를 그대로 쓰지 않고 마감 이탈은 §3-1에서 별도로 다룬다 |
| 2-11 사전 대응 규칙 성립 구간 | 🔄 지속 · **하루 최종치 확정(20구간)** | 장전 계산전 → 12:30 11구간 → 14:30 18구간 → **15:45(하루 전체) 20구간**. 최장 연속 구간은 13:00~13:40(9창·45분)으로 14:30 이후 갱신 없음 — 늘어나는 속도는 오후 들어 둔화됐다 |
| (관측①) `flow_position` 31분 무변화가 진짜 정체인가 | ✅ **판정 — 정체 아니었다** | DB(§14-1) 기준 `flow_position`은 **오늘 495분 중 495분(100%) 계속 가용**했다 — 하루 종일 한 번도 빠지지 않았다. 값도 고정이 아니었다(§14-3): 약세(−) 421분 · 중립(0) 74분 · 강세(+) **0분**. 즉 "07:30:55부터 로그가 조용했다"는 장전의 관측은 **"이 축이 죽었다"가 아니라 "형태(멤버 구성)가 안 바뀌어 전이 로그가 안 찍혔을 뿐"**이었다는 09-03 §1-6의 설명이 맞았다. 다만 **하루 495분 내내 단 한 번도 양(+)의 점수를 낸 적이 없다**는 것은 이번에 새로 확인된 사실이다 — 이것은 정체가 아니라 구조적 편향 가능성이라 별도로 지켜볼 만하다(P2, 새 이상점으로 올리진 않는다 — 임계도 비교 기준도 아직 없다, 규약 G) |
| (관측②) 테스트 경로 부재 오탐 여부 | ✅ 판정 완료 (재확인 불필요) | — |
| (관측③) 가설 id 오탈자 경위 | ✅ 판정 완료 (재확인 불필요) | — |
| (관측④) 9시 레버 결정이 오늘 이뤄졌는가 | ✅ **판정 — 안 됐다, 나흘째** | 15:45까지 `mahdi/main.py` 무변경 확정. 사유는 3회차와 동일("장중 코드 변경 금지"). 오늘로 09-09·10·11·14 **나흘 연속** — phases.md의 "3거래일" 기준을 이미 넘었다. 다음 장전이 반드시 결정할 것 |
| (관측⑤) 가설 6건 중 오늘 판정 가능한 것 | ✅ 판정 — 4건 제안, 2건 보류/이월 (아래 §3-2) | DB 접근 가능으로 4건에 제안 판정을 냈다 |

### 3-1. 종료 시퀀스 (15:20 ~ 15:45)

- **15:45:03** `stop_mahdi_marketclose.bat` 정상 실행 — `.last_marketclose_stop.txt` 갱신
  확인. taskkill 후 잔존 프로세스 **0**(`shutdown_check_log`).
- **`scripts/daily_ops_report.py`가 종료 절차 안에서 정상 실행**됨 — `auto/2026-09-14_지표.md`
  ·`.json`(각 67.0KB·66.5KB, 15:46 기록) 존재 확인. **전일 델타 복구 불능 위험 없음.**
- 관측 루프 최종 레코드: **15:45:00** `ATM 롤링`(스팟 1050.30pt, 코스피200 지수) — 마감
  시각과 일치, 그 전에 죽은 흔적 없음.
- 워치독: 하루 49행(OK 49 / 비정상 0), 마지막 기록 15:40 → 15:46:01 "idle, 감시 창 밖"으로
  정상 종료 인지. 감시 자신도 문제없이 하루를 마쳤다.

**마감 직전 6분 연속 전멸 — 09-11보다 두 배 길어졌다** (→ 3-12로 신규 등재)

- 15:16:57 · 15:17:52 · 15:18:57 · 15:19:56 · 15:20:56 · 15:21:56 — **6분 연속**
  "옵션체인 이번 분 전멸"(수집 20레그 중 적재 0행). 09-11의 같은 유형은 15:01·15:02·15:20
  세 분(최장 연속 2분)이었다 — **연속 길이가 2분 → 6분으로 3배, 총 결손 분도 3분 → 6분으로
  2배** 늘었다.
- **15:20:54에 오늘 처음으로 `inquire-price`(개별 시세 조회) p50(응답의 중앙값)이 표준
  대기시간(4.0초) 자체를 넘겼다** — p50 4.03초 ÷ 4.00초 = **1.01배**, 이 5분 창의 호출
  43건 중 **43건(100%)이 대기시간 초과로 끊겼다.** 그 전까지 하루 내내 이 축은 "상위
  5%(p95)만 가끔 넘는" 수준이었는데(11:00 최초 1.01배), 15:20 창은 **중앙값 자체가
  넘었다** — 절반 넘는 호출이 통째로 비었다는 뜻이라 이전 위험선 돌파보다 한 단계 더
  나쁘다.
- **15:22~15:23에 자연 회복**됐다(15:22 rows=14, 15:23 rows=20) — 재시동이나 사람 개입
  없이 수집량 자체가 15:20 이후 줄면서(마감 후 레이트리밋 경합 감소) 스스로 풀렸다.
  15:23~15:45 구간은 REST 수집이 오히려 19~30초로 오전보다 훨씬 빨라졌다(장 마감 후
  주문 경합이 줄어든 정상적 현상).
- **`options_flow`(옵션 수급 판단 축)가 15:20:56에, `orderflow_ofi_vpin`(주문 흐름·체결
  강도 축)이 15:36:56에 각각 이탈했고 15:45까지 복귀하지 않았다.** 둘 다 사유 코드가
  "원재료없음"(옵션수급: `gex·gamma_flip·wall·spot` 없음 / 주문흐름: `ofi·queue_imbalance`
  없음)으로 찍혔다 — **정규장(09:00~15:20) 마감으로 실시간 체결·호가가 끊기면서 그
  입력을 쓰는 두 축이 차례로 조용해진 것**이라, 09-11에도 마감 후 같은 패턴이 있었던
  구조적 현상으로 보인다(신규 결함 아님).

**영향**: 이 6분 동안 프로그램의 판단은 그 직전 스냅샷(신선도 창 안의 옛 시세)을 대신 썼다
— 실제로 새 정보를 못 본 6분이었다는 뜻이다. 오늘은 ADVISORY 모드라 실제 주문에는 영향이
없었지만, 만약 이 시간대에 자동매매가 켜져 있었다면 그 6분의 판단은 낡은 시세로 내려졌을
것이다. 15:10 강제 평탄화 이후 구간이라 신규 진입 자체는 이미 막혀 있어 진입 판단에도
영향이 없었다.

### 3-12. 신규(P1) — 장 마감 직전 6분 연속 시세 전멸 + 당일 첫 p50 자체 타임아웃, 09-11 대비 2배로 악화

- **증상**: 15:16~15:21(6분 연속) 옵션체인 수집이 통째로 실패(0행)했고, 15:20 창에서는
  중앙값 응답시간 자체가 표준 대기시간을 넘겼다(위 §3-1 인용 그대로).
- **근거**: `logs/observation_loop.log` 15:16:57~15:21:56 ERROR 6건, 15:20:54 ERROR
  "지연 경고선 돌파"(p50 4.03초÷4.00초=1.01배, 검열 43/43건=100%). 09-11 기준선(3분,
  최장연속 2분)은 `hypotheses.yaml`의 `2026-09-11-close-window-zero-rows-axis` 항목
  "근거"란에 그대로 인용돼 있다.
- **기준**: `phases.md` D — "고쳤다고 기록된 것의 재발은 최우선"에 준하는 사례는 아니지만,
  09-08 28분 절벽(마감 무렵 미결정 뒤 발생)과 같은 계열의 "마감 혼잡" 패턴이라 §12
  자동 적신호 6번(사전 대응 규칙 조건 성립)과 같은 근본 원인(KIS 응답 지연)을 공유한다.
- **영향**: 장 마감 직전 판단이 낡은 시세로 내려질 위험이 매일 조금씩 커지고 있다. 오늘은
  ADVISORY라 실제 손실은 없었지만, 실거래 전환 후 같은 시간대에 이 정도 공백이 나면 마감
  직전 포지션 정리 판단이 옛 가격을 보고 이뤄질 수 있다.
- **P1인 이유**: 아직 한 번(오늘)뿐이라 "구조적 패턴"이라 확정할 수는 없다(그 확정은
  `close-window` 가설의 몫인데 검정 설계 자체에 결함이 있다 — 아래 3-13). 다만 09-11 대비
  뚜렷이 악화된 실측이라 다음 장전이 반드시 확인해야 한다. (→ 사용자 조치는 없음, 다음
  장전이 이월 항목으로 받는다)

### 3-13. 신규(P2) — `close-window` 가설의 검정 설계가 "오늘 실측 vs 09-11 고정값"을 비교해 구조적으로 반증만 낸다

- **증상**: `hypotheses.yaml`의 `2026-09-11-close-window-zero-rows-axis` 가설을 오늘
  실측으로 판정하면 예측 4개 전부 "반증"으로 나온다 — `cycles.close_window.rows_zero_count`
  실측 6 vs 예측 `== 3`, `max_consecutive` 실측 5 vs 예측 `== 2`, `cycles.count` 실측 494
  vs 예측 `== 493`, `qualitative.chain_cycle_empty` 실측 10 vs 예측 `== 3`.
- **근거**: `docs/동작점검/auto/2026-09-14_지표.md` §0-1 해당 행 4개. 그런데 그 예측값
  `== 3`·`== 2`·`== 493`은 가설 등재 당시(09-11) **그날 하루의 실측치를 그대로 옮겨 적은
  것**이다(`hypotheses.yaml:7736` "당일 사이드카 기준선: … = 3" 참조) — 즉 이 가설은
  "새로 만든 축이 09-11 그날 수치를 정확히 재현하는가"를 검정하도록 설계됐는데, 검증예정일
  (09-12)이 비거래일이라 실측 기회를 놓쳤고, 지금은 **전혀 다른 날(09-14)의 실측을 09-11의
  고정값과 직접 비교**하고 있다. 시장 상황이 다른 두 날을 같은 숫자로 맞추라는 요구라
  **구조적으로 통과할 수 없다**(규약 F — 건수는 그날 시장 상태에 비례하는데 이 가설은
  그것을 상수로 취급한다).
- **기준**: `hypotheses.yaml:7754` 그 항목의 "주의"란이 이미 스스로 "⛔ 임계를 만들지
  않는다 — 이틀 연속이면 구조적 패턴은 사람이 판정할 문장이지 도구가 그을 경보선이 아니다"
  라고 적어 뒀다. 자동 판정이 그 경고를 어기고 고정 임계처럼 동작한 것이다.
- **영향**: 이 가설의 "반증" 딱지만 보면 "마감 혼잡 축이 잘못 설계됐다"로 오독하기 쉽지만,
  **축 자체(개수를 세는 기능)는 멀쩡히 작동하고 있다** — 방금 3-12에서 그 수치(6분·연속
  5~6분)를 그대로 인용해 새 이상점을 보고했다. 잘못된 것은 "그 숫자를 09-11과 똑같이
  맞추라"는 예측 설계뿐이다. 이대로 두면 매일 반증이 나 사람이 "또 반증이네"로 무시하게
  될 위험이 있다(양치기 효과).
- (→ 제4부 Fix 7, 예측치는 사람이 손으로 `inconclusive`로 닫을지 검정 방식을 바꿀지 결정할
  것 — 사용자 조치 신규 항목)

### 3-2. 검증예정일 지난 가설 6건 — 제안 판정

> ⛔ 이 회차는 `hypotheses.yaml`의 `상태`를 직접 바꾸지 않는다. 아래는 **제안**이고, 확정은
> 사람이 한다(스킬 규약 — 자동 판정이 틀렸을 때 조용히 덮이는 것을 막기 위함).

| id | 검증예정일(지남) | 제안 판정 | 근거 |
|---|---|---|---|
| `2026-08-12-fix7-regime-session-window-restore` | 2026-09-01(13일) | **미실행 유지 — 검증예정일을 레버 켜진 다음 영업일로 재설정** | 전제 레버 `REGIME_RESTORE_SESSION_WINDOW`가 오늘도 꺼짐(규약 H). 오늘 숫자(`db.regime.warmup_restart_minutes`=0)는 이 가설과 무관하다 — 그 재기동 경로 자체가 오늘 한 번도 안 돌았다 |
| `2026-08-19-fix2-roundtrip-leaves-db-evidence` | 2026-09-01(13일) | **inconclusive — 다음 실거래 발생일로 재설정** | `execution_logs` 0행. 실측이 `>= 0`을 만족해 표에는 "확인"으로 뜨지만, 이것은 **오늘도 실주문이 0건이라 자동으로 참이 되는 공허한 통과**다(ADVISORY 모드, §16-4). 이 가설이 실제로 검증하려는 것("왕복이 실제로 일어났을 때 그 증거가 DB에 남는가")은 오늘도 시험되지 않았다 |
| `2026-08-26-p1-3-preemptive-rule-fires-at-premarket` | 2026-08-27(18일) | **오늘(장후)은 판정 주체가 아님 — 내일 장전이 처음으로 판정** | 이 가설의 주장 자체가 "조건 성립 다음 거래일 **장전** 보고서가 그 시간대를 인용하고 발동 여부를 정하는가"라 장후 세션은 원리적으로 판정할 수 없다. 다만 대가 지표(§0-1 `db.book_coverage`)는 참고로 남긴다 — 위클리 커버리지가 08-26 기준선(45.9%/46.4%) 대비 오늘 33.5%/32.9%로 이미 낮다 |
| `2026-09-03-p1-b-dropout-cross-checks-the-cycle-log` | 2026-09-04(10일) | **확인 (제안)** | 오늘 §3-0 (관측①)을 실제로 `옵션체인 사이클 소요 분해`(로그) 대 DB `signal_decisions`/`risk_gate_state`(§14-1·§14-3) 교차로 판별했다 — "형태 무변화"와 "진짜 이탈"이 서로 다른 문구·축으로 갈렸다. 오늘 이탈 로그(15:20:56·15:36:56)도 전부 사유 코드(`원재료없음[...]`)를 동반해 이 가설의 대가 지표(사유 코드 동반)를 만족한다 |
| `2026-09-11-p1-3-axis-exit-says-why` | 2026-09-12(2일) | **확인 (자동 §0-1과 동일, 그대로 확정 제안)** | `qualitative.member_axis_exit`=2 (대가 `<=20` 충족) · `qualitative.member_axis_return`=3 (대가 `<=20` 충족) · `cycles.count`=494 (대가 `>=480` 충족). 오늘 실측 이탈 줄 2건(15:20:56·15:36:56) 모두 "사유 <코드>" 형식을 갖췄다(위 3-1 인용) |
| `2026-09-11-close-window-zero-rows-axis` | 2026-09-12(2일) | **inconclusive — 검정 설계 재정의 필요(3-13 참조), "반증"을 그대로 확정하지 말 것** | 자동 판정은 4개 전부 반증이지만, 그 예측이 09-11 고정값과의 일치를 요구하도록 잘못 설계됐다(3-13). 축 자체는 정상 작동 중(3-12에서 그 수치를 실제로 인용해 썼다) |

---

## 종합 판정표 — 하루 전체 국면별 변화

| 항목 | 장전(07:30~09:00) | 장중①(12:30) | 장중②(14:30) | 장후(15:45) |
|---|---|---|---|---|
| 9시 혼잡 레버 결정 | 미결(사흘째) | 미결(사유: 장중 변경 금지) | 미결(4번째 사유 동일 연장) | ✅ 미결 **나흘째 확정** — 오늘 밤 결정 필요(P1) |
| 사전 대응 규칙 성립 구간 | 계산 전 | 11구간 | 18구간(최장 9창) | **20구간(최종, 최장 9창 그대로)** |
| 검증예정일 지난 가설 6건 | 미판정(6건) | 판정 가능 0건(사유 개별) | 판정 가능 0건(사유 갱신) | **4건 제안 판정 · 2건 보류/이월** |
| `flow_position` 31분 무변화 | 자동 적신호로 제기 | ⏸ 보류(DB 미접근, 로그로 "형태 유지"만 확인) | ➡️ 이월(DB 접근 불가 지속) | ✅ **판정 — 정체 아님, 100% 가용, 단 하루종일 양(+) 0회(새 관찰)** |
| `options_flow` 이탈 | — | — | 🆕 14:04~14:35(31분+, P1) | ⬇️ **완화 — 그 건은 14:40 정상 복귀·44분 안정. 마감 후 별개 구조적 이탈(15:20·15:36) 신규 확인** |
| 먼슬리 완전 미회복 | — | 🆕 1건 | 🔄 3건 | **최종 4건(전일 16건 대비 ▼8, 개선 추세 유지)** |
| 마감 직전(15:00~15:20대) 시세 전멸 | — | — | — | 🆕 **6분 연속(09-11의 2배) + 당일 첫 p50 자체 타임아웃(P1, →3-12)** |
| `NEXT_TODO.md:42` 오탈자 | — | 🆕 발견 | 🔄 미정정 | 🔄 **미정정 지속(사용자 결정 대기)** |
| 미커밋 | 25건 | 26건(파일 자체 반영) | 26건(변화없음) | **26건(실질), `.claude/` 1건은 세션 자체 흔적으로 별도 취급** |

---

## 당일 이상점 통합 대장

| 번호 | 제목 | 최초 발견 국면 | 최종 심각도 | 최종 처분 | 근거 위치 |
|---|---|---|---|---|---|
| 1-1 | 혼잡 시간대(9시) 레버 결정 미착수 | 장전 | P1 | 🔄 지속 — 나흘째, 오늘 밤 결정 필요 | §1-1 / §2-0(각 회차) / §3-0 |
| 1-2 | 검증예정일 지난 pending 가설 6건 | 장전 | P1 | ✅ 대부분 판정 — 4건 제안, 2건 보류/이월 | §1-2 / §3-2 |
| 1-3 | 이벤트 캘린더 `covered_through` 만료 | 장전 | P2 | 🔄 지속(만성, 17일째) | §1-3 |
| 1-4 | ZN/ES 근월물 조회 실패 | 장전 | P2 | 🔄 지속(만성) | §1-4 |
| 1-5 | 레버 3건 무조건발동일 주석 정리 필요 | 장전 | P2 | 🔄 지속(정리 항목, Fix 1 미착수) | §1-5 |
| 1-6 | 미커밋 25~26건 | 장전 | P2 | 🔄 지속(만성) | §1-6 |
| 1-7 | 전일 보고서 가설 id 실종 | 장전 | P2 | ✅ 해소(오탈자로 판정) | §1-7 / §2-0(12:30) |
| 2-8 | `NEXT_TODO.md:42` 가설 id 오탈자 | 장중① | P2 | 🔄 지속(미정정) | §2-8 |
| 2-9 | 먼슬리 레그 완전 미회복 | 장중① | P2 | 🔄 지속 — 최종 4건(전일 대비 ▼8) | §2-9 / §3-0 |
| 2-10 | `options_flow` 이탈 재발(14:04~14:35) | 장중② | P1 | ⬇️ 완화(그 건은 정상 복귀 확인) | §2-10 / §3-0 |
| 2-11 | 사전 대응 규칙 성립 구간 확대 | 장중② | P2(참고) | 🔄 지속 — 최종 20구간 | §2-11 / §3-0 |
| 3-12 | 마감 직전 6분 연속 시세 전멸 + 당일 첫 p50 타임아웃 | 장후 | P1 | 🆕 신규 — 다음 장전이 반복 여부 확인 | §3-1 / §3-12 |
| 3-13 | `close-window` 가설 검정 설계 결함 | 장후 | P2 | 🆕 신규 — Fix 7 제안 | §3-13 |

**하루 합계**: 13건 발견 · 1건 해소(1-7) · 12건 잔존(P1 4건 · P2 8건) · 오늘 신규 2건(3-12·3-13).

---

## 종가 기준 최종 손익 / 절대원칙 판정

**오늘 거래 없음.** 진입 판단은 321건 나왔으나 실제 주문은 **0건**이다. 이유는 둘이다:
설정이 "지켜만 보는" 모드(ADVISORY, `mode_not_auto_submit`)이고, 재진입 대기시간 설정이
꺼져 있다(`reentry_cooldown_not_configured`) — 둘 다 사람이 설정 파일을 손으로 바꿔야
풀리는 조건이라, 코드가 스스로 주문을 낸 것이 아니다(§16-4). 보유 포지션도 0건, 체결통보도
0건이다(§16-1~16-3).

| 항목 | 절대원칙(출처: v6 §12.2~12.3, `risk_limits.yaml`) | 오늘 실측 | 판정 |
|---|---|---|---|
| 단일 트레이드 손실 한도 | 계좌 대비 −0.5% 넘으면 진입 거부 | 거래 없음(적용 대상 없음) | ✅ |
| 일일 손실 한도 | 계좌 대비 −2% 넘으면 신규 거래 중단 | 실현·미실현 손익 **0원(0%)** | ✅ |
| 주간 손실 한도 | −5% 넘으면 Review 모드 | 오늘 기여분 0% | ✅ |
| 최대 드로우다운 | −10% 넘으면 자동매매 정지 | 0% | ✅ |
| 동일 방향 동시 포지션 한도 | 최대 3개 | 보유 **0개** | ✅ |
| 서킷브레이커(HALT) | daily_loss/drawdown/VPIN위기/VIX급등/원달러급변 등 | 오늘 발동 로그 0건 | ✅ |
| 15:10 강제 평탄화 | 15:10 이후 보유 포지션 강제 청산 | 애초에 포지션이 없어 청산 대상 없음 | ✅ (해당 없음) |
| Full Kelly 금지(Quarter Kelly 0.25 기본) | 포지션 사이징 배율 상한 | 주문이 없어 사이징 로직 자체가 안 돌았다 | ✅ (미실행) |
| ADVISORY 모드 유지 | `hybrid_mode.default: ADVISORY` | 설정값 오늘 무변경, 실제로 주문 0건으로 그대로 지켜짐 | ✅ |

**결론: 절대원칙 위반 없음(위반 가능성 자체가 없었던 하루 — 포지션이 없었다). 손익 0원.**

---

## 제4부 — Fix 구현계획 (P0/P1/P2)

장전 Fix 1~3, 장중① Fix 4, 장중② Fix 5~6은 그대로 유효하다(아래 §4-표에 다시 정리).
추가:

| # | 우선순위 | 무엇 | 파일 · 함수 | 비고 |
|---|---|---|---|---|
| Fix 1 | P2 | 레버 3건(1-5)의 `hypotheses.yaml` 무조건발동일 주석을 실제 발동일로 정리 | `docs/동작점검/hypotheses.yaml`(`use_effective_member_count`·`SIGNAL_FUSION_PHASE_OFFSET_SECONDS`·`OPTION_CHAIN_SLOW_SERIES_CONGESTED_HOURS` 각 항목) | 동작 변경 없음, 문서 정리만. 나흘째 미착수 |
| Fix 2 | P2 | `2026-08-26-p1-2-1` id 존재 여부 확인 후 재등재 또는 이력 기록 | `docs/동작점검/hypotheses.yaml` | 09-14 12:30에 오탈자로 확인됨(2-8) — 사람 확인만 남음 |
| Fix 3 | P1 | 9시 혼잡 레버 결정 이후, 결정된 값을 `mahdi/main.py:275` 딕셔너리에 반영 | `mahdi/main.py` | **결정(사용자 조치 1번)이 선행 조건** — 오늘 밤 결정 나면 내일 장전 세션이 반영 |
| Fix 4 | P2 | `NEXT_TODO.md:42`의 `2026-08-26-p1-2-1`을 실제 id `2026-08-26-p2-1-lever-warning-needs-both-conditions`로 정정 | `docs/dev_memory/NEXT_TODO.md` | 정정 후 레버 3건 경보 조건식(`collect_evidence.py:2681`) 착수 가능 여부 재확인 |
| Fix 5 | P1 | `2026-09-11-p1-3-axis-exit-says-why`·`2026-09-03-p1-b-…` 두 가설을 오늘 14:04~15:36 이탈 이벤트 3건으로 재판정(§3-2 제안대로 확정) | `docs/동작점검/hypotheses.yaml` | 오늘 DB 접근으로 제안 판정까지 완료 — 사람 확정만 남음 |
| Fix 6 | P2 | "14:00 이후 `options_flow` 이탈이 매일 재현되는가" 검정 예측치 등재 | `docs/동작점검/hypotheses.yaml` | 09-11·09-14 2거래일 연속 관측(단, 09-14의 14:04~14:35 건은 정상 복귀로 판명 — 등재 시 "일시적 얇아짐"과 "마감 구조적 이탈"을 별개 지표로 분리할 것을 제안) |
| **Fix 7(신규, §3-13)** | **P2** | `close-window-zero-rows-axis` 가설의 예측 4건(`== 3`·`== 2`·`== 493`·`== 3`)을 **고정값 비교에서 "전일 대비 증감" 또는 "임계 없이 세기만" 방식으로 재설계** — `2026-08-07 고도화#B`가 이미 쓴 동일 패턴(`flow_position ↔ options_flow` 일치율 표) 참고 | `docs/동작점검/hypotheses.yaml`(해당 가설 `예측` 블록) | 착수 전 사람이 `상태`를 `inconclusive`로 먼저 닫을지 결정 |
| **Fix 8(신규, §3-12)** | **P1(관찰)** | 마감 직전(15:00~15:20대) 시세 전멸이 내일도 재현되면 그때 "2거래일 연속 = 구조적 패턴" 판단 기준으로 `docs/동작점검/README.md` 규약에 정식 등재할지 검토 | `docs/동작점검/README.md` · `hypotheses.yaml` | 코드 변경 없음, 관찰만. 내일 장후가 이어받을 것 |

---

## 제5부 — 고도화 방안 (당일 관측에서 출발한 것만)

1. **"9시 미결 며칠째" 집계를 거래일 캘린더 기반으로 자동화한다.** 오늘로 실제 나흘째
   (09-09·10·11·14)인데, 이 숫자를 이 보고서를 쓰는 사람이 매번 손으로 센다 — 09-12·13
   비거래일을 건너뛰는 계산이 코드에 없기 때문이다(장전 §고도화 1에서 이미 제안된 항목,
   오늘 실측으로 한 번 더 확인됨 — 나흘째라는 숫자 자체를 이 회차가 손으로 셌다).
2. **마감 직전(15:00~15:20대) 시세 전멸을 "선행 신호"가 아니라 "동시 신호"로 오후 열화
   게이지(§오후 열화 게이지, 장중② 표)에 정식 편입한다.** 오늘 실측(6분 연속·당일 첫
   p50 타임아웃)이 그 표의 마지막 구간(14:10~14:35)에서 끊겨 있어 정작 가장 나빴던
   15:16~15:21 구간이 표에 안 잡혔다 — 수집기가 정규장 마감 이후 구간까지 표를 연장하도록
   `docs/동작점검/tools/collect_evidence.py` §5-1에 15:00~15:45 행을 추가하는 안을
   제안한다.
3. **가설 검정 설계 자체에 "고정값 대 오늘값" 패턴이 남아 있는지 전수 점검한다.** 3-13에서
   발견한 결함(09-11 고정값과 09-14 실측을 직접 비교)이 `close-window` 가설 하나만의
   문제인지, 다른 최근 등재 가설에도 같은 패턴이 있는지 오늘 관측만으로는 못 가른다 — 다음
   장후가 `hypotheses.yaml`의 최근 10건 정도를 훑어 "예측값이 등재 당일의 실측 그대로인가"를
   확인하는 것을 제안한다.

## dev_memory 반영 초안 (장후가 실제 반영할 내용 — 이 회차는 적어만 둔다)

- `NEXT_TODO.md:22~28`(9시 레버 결정)에 "09-14 15:45 기준 나흘째(09-09·10·11·14) 미결,
  사전 대응 규칙 성립 20구간(최장 연속 9창·45분)"으로 최종 수치를 갱신할 것.
- `NEXT_TODO.md:59`(14:00 이후 `options_flow` 이탈 재발)에 오늘 사례를 "14:04~14:35(31분,
  정상 복귀 확인)"과 "15:20:56 이후(마감에 따른 구조적 이탈, 별개 사건)"로 나눠 기록할 것 —
  하나로 뭉뚱그리면 "매일 재현되는가" 검정이 서로 다른 두 현상을 하나로 섞게 된다.
- `NEXT_TODO.md`에 신규 절 "09-14 — 마감 직전 6분 연속 시세 전멸(09-11의 2배)"을 추가하고,
  `close-window-zero-rows-axis` 가설의 검정 설계 결함(3-13)을 함께 적을 것 — 이대로 두면
  다음 장후가 "또 반증이네"로 지나칠 위험이 있다.
- `2026-08-12-fix7-regime-session-window-restore`·`2026-08-19-fix2-roundtrip-leaves-db-evidence`
  두 가설의 검증예정일을 "레버가 켜진 날" · "다음 실거래 발생일"로 각각 재설정할 것을
  `DECISION_LOG.md`에 남길 것 — 지금처럼 매번 "미실행"·"공허한 통과"로만 반복되면 검증예정일
  갱신 자체가 의미를 잃는다.
- `CURRENT_STATE.md`에 오늘(09-14)을 "하루 한 파일 전환 이후 첫 완결 장후 사례"로 짧게
  남길 것 — Fix 1~6까지 이어진 예측치 제안·이월 처분표 형식이 실제로 하루를 끝까지
  재구성해 냈다(이 파일 하나만 읽어도 오늘 전체가 재구성된다).

---

## 사용자 조치

> 이 절에 없는 것은 사람이 안 해도 되는 일이다. 장전·장중① ·장중②「사용자 조치」 절은
> 그대로 유효하다. 아래는 장후가 새로 확인했거나 갱신한 것만 다시 적는다.

### 지금 / 오늘 중

1. **혼잡 시간대(9시) 레버를 켤지 정한다 — 오늘로 나흘째, 오늘 밤 안에 정할 것.** (→ 1-1,
   Fix 3) 선택지와 손익은 장전 절에 이미 적혀 있다((A) 켠다: 위클리 커버리지 9시대
   50%→약 25% 반토막 대가로 지연 완화 / (B) 유예: 위클리 커버리지 유지하되 09-08과 같은
   혼잡 절벽 재현 시 대응 규칙 없음). **적용은 내일 07:30 기동 전에만 가능**하므로 오늘
   밤 결정해야 다음 장전이 반영한다.
2. **`close-window-zero-rows-axis` 가설(3-13)의 판정을 `inconclusive`로 확정할지, 검정
   방식을 다시 설계할지 정한다.** (→ Fix 7) 지금 "반증"인 채로 두면 그 아래 깔린 진짜
   문제(마감 직전 시세 전멸이 09-11 대비 2배로 악화됐다는 사실, 3-12)가 가려진다.
3. **오늘 제안한 가설 판정 4건(§3-2)을 `hypotheses.yaml`에 손으로 확정한다.** 이미
   근거까지 갖춰 제안돼 있어 사람이 검토 후 상태 필드만 바꾸면 된다.

### 결정이 필요한 것 (자동으로 못 한다)

4. **`NEXT_TODO.md:42`의 가설 id 오탈자를 확인·정정한다.** (→ 2-8, Fix 4) 정정하면 레버
   3건 경보 조건식(Fix 5부 고도화 대상) 착수 조건이 풀릴 수 있다.
5. **Fix 후보 K(§8-4 계산기 축 변경) 착수 여부 결정.** (→ 09-11 이월 항목, 아직 미착수)
6. **이벤트 캘린더 `covered_through` 9월분 갱신.** (→ 1-3) 만성 17일째.
7. **미커밋 26건 정리 여부.** (→ 1-6) `.bat` 수정분이 미추적 `premarket_levels_publish.py`를
   부르는 구조가 그대로 남아 있다.
8. **레버 3건의 무조건발동일 주석 정리.** (→ 1-5, Fix 1) 동작 영향 없는 문서 정리, 나흘째
   미착수.

### 이번 회차 요약

**P0 0건 · P1 4건**(1-1 9시 레버 나흘째 미결 · 1-2 가설 판정 대부분 완료했으나 최종 확정
대기 · 2-10 완화됐으나 마감 후 별개 이탈 발생 · 3-12 마감 직전 6분 전멸 신규)**· P2 8건**
(1-3~1-6 만성/정리 · 2-8·2-9·2-11 지속 · 3-13 검정 설계 결함 신규)**· 이월 처분 14건 전부
처분 완료 · 하루 합계 이상점 13건(1건 해소) · 절대원칙 위반 없음(거래 없음) · 손익 0원.**

**이 파일 하나만 읽고 오늘 하루가 전부 재구성된다** — 장전의 미결정 하나가 장중 두
회차를 거쳐 장후에도 그대로 남았고(나흘째), 장중이 DB 접근 불가로 이월한 관측 두 건은
장후가 DB로 닫았으며(flow_position 정체 아님, 가설 4건 제안 판정), 마감 직전의 새로운
악화(6분 연속 전멸)는 오늘 처음 발견돼 내일로 넘어간다.
