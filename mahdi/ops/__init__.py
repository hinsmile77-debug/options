"""운영 점검 자동 집계 — `scripts/daily_ops_report.py`가 쓰는 로직 계층.

2026-08-01(운영점검보고서 2026-07-31 §5-2): 10회째 손으로 재작성하던 집계를 코드로 고정한다.
07-31 조사에서는 신규 집계 3종(버스트 점유 시간, 창 안 타폴러 호출 실측, 연속 지연 에피소드)이
더 필요했고, 조사 시간의 약 8할이 "표 만들기"였다.

**로직을 `scripts/`가 아니라 여기 두는 이유**: `scripts/log_marketclose_stop.py`가 이미 같은
규약을 문서로 남기고 있다 — "배치파일이 한 줄로 호출하기만 하면, 실제 로직은 파이썬(pytest로
테스트됨) 쪽에 있어 같은 위험이 없다". 파서를 스크립트에 두면 테스트가 어려워진다.

모듈 구성:
- `log_metrics`: 순수 파서(로그 라인 이터러블 → 지표 dict). 파일 I/O 없음 → 픽스처 문자열로 전수 검증.
- `db_metrics`: SQL 집계((conn, date) → 지표 dict).
- `hypotheses`: 가설 파일 로드 + 예측 대조(§5-1).
- `report`: 순수 렌더러(지표 dict → 마크다운). 파일 I/O 없음.
"""
